# W6.1 — DEFECT REGISTER (re-assessment post W6.0-A–F)

Fecha: 2026-08-28 · Modo: documental + verificación read-only sobre `costpro_audit_v2` (espejo S1≡prod, DIFF=0) + call graph del repo `e9f466b1`/HEAD `522a519e`.
Base: evidencia ejecutada W6.0 (commits `f628e5e7`, `b125152d`) + revisión crítica 67 + verificaciones SQL de esta fase.
**Ningún defecto se cierra por estar documentado. Ningún defecto se cierra por existir una función alternativa.**

## 0. Tabla de mapeo de IDs (resuelve CR-W6-8)

| ID histórico W0–W5 | Denominación dueño (W6.0 §3) | Evidencia ejecutada A–F | ID de este registro |
|---|---|---|---|
| WAC-DF-01 | CRÍTICO 2 — doble motor WAC | Caso D (D.3 FAIL) | WAC-DF-01 |
| WAC-DF-02 | CRÍTICO 1 — COGS client-side | Caso C (C.1/C.2/C.3 FAIL) | WAC-DF-02 |
| WAC-DF-03 | HIGH — contra-asiento financiero | Caso F (F.5 FAIL) | WAC-DF-03 |
| WAC-DF-04 | MEDIUM — históricos (W4) | (estadística W4; no re-testeado) | WAC-DF-04 |
| WAC-DF-05 | CRÍTICO 3 — producción costo cero | Caso E (E.1/E.2/E.4 FAIL) | WAC-DF-05 |
| WAC-DF-06 | — (transfer blend; solo diseño) | no testado en A–F | WAC-DF-06 |
| WAC-DF-07 | HIGH — devoluciones sin tope/race | Caso F (F.3/F.8) + F7 | WAC-DF-07 |
| — (NUEVO) | HIGH — close production order | Caso E (E.7 FAIL) | **WAC-DF-08 (nuevo)** |
| — (NUEVO) | — ambigüedad de overloads withdraw | Ejecución harness + call graph | **WAC-DF-09 (nuevo)** |

## 1. Ficha por defecto

### WAC-DF-01 · Doble (realmente múltiple) motor WAC — `cost_average` sin escritor único
```text
ID:            WAC-DF-01
severity:      CRITICAL
status:        TRUE APPLICATION DEFECT — OPEN
root cause:    PR-1 violada en despliegue: trigger heterodoxo vivo + RPCs con blends propios + rutas legacy
evidence:      Caso D.3 (WAC operativo 150 sobrescrito → 137.5 por INSERT de receipt_item PENDIENTE);
               inventario de escritores de esta fase (v2≡prod, S1=0): ≥14 funciones escriben cost_average:
               update_product_wac (trigger), confirm_pending_reception, fn_process_receipt (×2), cancel_reception,
               reverse_receipt_v2 (aritmética propia — estilo 3 de V2/67), perform_inventory_adjustment,
               receive_production_output (×2), create_devolution 62776 (blend en devolución, anti-A1),
               reverse_production_order, void_closed_production_order, void_reception_with_reversal,
               reset_store_data (admin); + trigger trg_auto_kardex en la superficie del campo.
affected:      products.cost_average; kardex_entries; COGS; valorización PR-5
accounting:    La «fuente de verdad» depende del ORDEN de llegada del último escritor (no de doctrina);
               coexisten semántica A (blend D-01) y B (promedio histórico) en producción.
reproduction:  case-D.sql (A–F) + w61-writers.sql (esta fase)
remediation:   D-01=A ratificado → fn_recalc_wac escritor único; trigger → guard pasivo o eliminación (W7/W8);
               inventario de 14 rutas como lista de conversión obligatoria (no opcional).
owner decision: D-01 (A vs B) — RESERVADA. La ratificación debe ser informada de que B también está
               «vivo» hoy vía trigger y de que el caso D demostró su capacidad de sobrescribir A.
blocked by:    ratificación D-01 + bloque D-02..D-12
```

### WAC-DF-02 · COGS determinado por el cliente en ventas
```text
ID:            WAC-DF-02
severity:      CRITICAL
status:        TRUE APPLICATION DEFECT — OPEN (no existe variante server-side para ventas)
root cause:    create_sale_v2 (oid 62894): v_cost := COALESCE(item->>'cost_at_sale', item->>'cost', 0)
               y el POS DEL REPO lo alimenta: src/app/api/pos/checkout/route.ts L100-104 mapea
               «cost → cost_at_sale» explícitamente (comentario en el código) y lo envía al RPC.
evidence:      A–F Caso C: cost_at_sale=7777 impuesto y registrado (C.1, C.3); sin claves → 0 (C.2);
               kardex a 7777 (C.3). Call graph: checkout + sync/batch llaman create_sale_v2 (v1 create_sale
               solo en tests que ASSERTEN su no-uso).
affected:      transaction_items.cost_at_sale, stock_movements.unit_cost, kardex, márgenes, INV-06/07
accounting:    COGS manipulable por el cliente → utilidad falsificable por evento.
reproduction:  case-C.sql; payload del checkout con cost_at_sale arbitrario.
remediation:   D-03: servidor impone units×WAC_prev snapshot FOR UPDATE; request deprecado; el ROUTE
               deja de enviar cost (cambio de app en W7/W8, no solo de RPC).
owner decision: ratificar D-03 con alcance explícito de cambio en la capa app (route checkout).
blocked by:    ratificación D-03
```

### WAC-DF-03 · Devolución sin contra-asiento financiero
```text
ID:            WAC-DF-03
severity:      HIGH
status:        ACCOUNTING POLICY DECISION (D-05) + implementación ausente — OPEN
root cause:    create_devolution_v2 no registra reembolso; devolución física ≠ reversión financiera.
evidence:      A–F F.5: 0 cash_movements con reason devolución; payment_transactions_ref_type_check es
               ARRAY CERRADO sin 'devolution' (verificado esta fase) → la vía D-05(i) exige ALTER CONSTRAINT
               (alcance W8, confirma CR-W6-4); devolutions admite 'store_credit' que payment_transactions
               no puede registrar (métodos: cash/transfer/zelle) → hueco CR-W6-7 real.
affected:      cash_movements, payment_transactions, caja, INV-10
accounting:    ingreso/COGS revertidos en kardex pero caja/CxC intactos → descuadre por devolución.
reproduction:  case-F.sql F.5.
remediation:   D-05(i): ref_type='devolution' + amount>0 + dirección refund; store_credit con política
               espejo definida (recomendación técnica: exigir fila espejo también para store_credit).
owner decision: SÍ — política contable del reembolso (split, store_credit, dirección de caja).
blocked by:    ratificación D-05 (+ decisión store_credit)
```

### WAC-DF-04 · Históricos: WAC=0 masivo y COGS venenado (era pre-canon)
```text
ID:            WAC-DF-04
severity:      MEDIUM
status:        ACCOUNTING POLICY DECISION (H-FINAL/D-12) — PARTIALLY MITIGATED (Puerto Padre 36/36 MATCH)
root cause:    datos históricos: ENERVIDA 79/124 cost_average=0; ventas con unit_cost=0: ENERVIDA 83%,
               Puerto Padre 100%; COGS pre-canon irrecuperable (veneno W4).
evidence:      estadística W4 (proceso anterior, perdida la copia cruda; cifras preservadas en 60/62 y
               re-citadas aquí como historia forense). NO se re-testeó (no destructivo, no necesario).
affected:      valorización histórica, márgenes históricos, reporting pre-T_canon.
accounting:    separación estricta requerida: (1) inventario físico [reconstruible], (2) valoración
               reconstruible [T_canon+backfill determinista], (3) COGS histórico [irrecuperable],
               (4) margen histórico [irrecuperable], (5) no-verificable [LEGACY_UNVERIFIABLE_MARGIN
               etiquetado, nunca fingido].
reproduction:  n/a (análisis W4 histórico)
remediation:   H-FINAL (doc 62-historics-policy): T_canon + backfill determinista de WAC (solo inventario/
               valoración, JAMÁS COGS/margen retroactivo) + control tipo Puerto Padre como calibración
               + regla CR-W6-6: entrada pre-T con costo NULL/0 → excluir del numerador con marca
               recon_gap o SKU 0-documentado; nunca ingestar un cero como costo válido.
owner decision: SÍ — ratificar H-FINAL y la regla de entradas pre-T a costo ausente.
blocked by:    ratificación D-12 + despliegue W8 con ventanas aprobadas
```

### WAC-DF-05 · Producción: costo cero aceptado en la ruta legacy y PT contaminado
```text
ID:            WAC-DF-05
severity:      CRITICAL
status:        FIX DEPLOYED BUT BYPASSABLE — la variante endurecida existe y funciona (E.1b=50),
               pero el camino que usa la app es el legacy vulnerable.
root cause:    withdraw_production_item: overload legacy 6-arg (oid 62874) usa p_unit_cost del cliente
               SIN validación; el route de la app además hace «p_unit_cost: unit_cost || 0» (fallback
               a cero en la capa API). La overload endurecida 9-arg (oid 62903) con p_server_side_cost=true
               lee cost_average FOR UPDATE sin fallback (ERR_PRODUCT_COST_UNAVAILABLE si NULL) —
               desplegada, PERO (i) nadie la llama con true, (ii) la coexistencia de overloads rompe la
               resolución de la ruta de 6 args (ver WAC-DF-09).
evidence:      A–F Caso E: E.1 legacy acepta 0 (production_out unit_cost=0); E.2 PT cost_average=0;
               E.4 contaminación propagada (PT final 25 ≠ 50); E.1b server_side=true → 50 correcto.
               receive_production_output (62738) hace «cost_average = v_new_cost, cost_price = v_new_cost»
               → perpetúa el espejo cp (contradice D-02) y el cierre contamina en vez de corregir.
affected:      production_order_items.actual_unit_cost, stock_movements(production_out/in), products.
               cost_average/cost_price del PT, kardex, INV-13
accounting:    PT entra a inventario con valor 0 con información válida existente → destrucción de
               valor contable silenciosa y propagación por blend a futuras entradas.
reproduction:  case-E.sql (E.1→E.4, E.1b).
remediation:   D-07: forzar server_side=true (false→error, como prescribe 61 E-P ya redactado);
               eliminar overload legacy o renombrar; route deja de enviar unit_cost; PT rechaza materiales
               a 0 salvo bandera approve_zero_cost_material auditada (INV-13); cp deja de espejarse (D-02).
owner decision: SÍ — política de la bandera admin de costo-cero legítimo (CR-W6-2/INV-06 interacción).
blocked by:    ratificación D-07/D-02
```

### WAC-DF-06 · Transferencia destino sin blend (solo diseño; no testado en A–F)
```text
ID:            WAC-DF-06
severity:      HIGH (potencial)
status:        OPEN — UNTESTED IN A–F (sin caso canónico de transferencia; fuera del alcance W6.0)
root cause:    diseño 61 E-T exige blend destino con uc_transfer congelado; estado real del writer de
               transfer_in no verificado ejecutivamente en esta fase.
evidence:      diseño (doc 61 E-T «Cierra DF-06 parcial (transfer)»); sin evidencia ejecutiva A–F.
affected:      products.cost_average destino, kardex, INV-15 (ventanas cruzando transferencias).
accounting:    destino heredaría WAC ajeno a su histórico real (si as-is) → valoración distorsionada.
reproduction:  pendiente (caso canónico E-T del harness en W7).
remediation:   implementación 61 E-T tras ratificación.
owner decision: no adicional (cubierto por ratificación en bloque D-02..D-12).
blocked by:    ratificación en bloque; demostración W7.
```

### WAC-DF-07 · Devoluciones: sin tope acumulado, sin lock, race aceptada
```text
ID:            WAC-DF-07
severity:      CRITICAL
status:        TRUE APPLICATION DEFECT — OPEN (race demostrada; no se repite prueba destructiva)
root cause:    create_devolution_v2 no valida Σ devuelta ≤ vendida ni toma lock por original_transaction_id;
               el CHECK-FUNC prometido (61 E-D) no existe en el esquema desplegado (verificado: ninguna
               constraint de tope en devolutions/devolution_items).
evidence:      A–F F.3 (6>5 aceptado secuencial), F.8 (stock 11 > 10 recibidos);
               F7 clon: dos devoluciones CONCURRENTES claves distintas ambas success → 8 devuelto > 5
               vendido, stock_final=13. (Misma clave: índice único sostiene idempotencia — F7 R2, 1 sola.)
affected:      devolutions, devolution_items, stock_movements(return), inventario, INV-09
accounting:    sobre-devolución = salida de valor sin contrapartida de ingreso; inventario puede
               exceder lo jamás recibido.
reproduction:  case-F.sql + af-f7-runner.sh (clon efímero; NO repetir contra v2).
remediation:   tope acumulado + advisory lock por original_transaction_id ANTES de leer el acumulado
               (doctrina CR-W6-1, ya exigida por el dueño) + estados de devolución en el acumulado.
owner decision: no adicional (ya ordenado por el dueño en W6.0; queda como condición de aceptación W7).
blocked by:    implementación W7 (con la doctrina de bloqueo como criterio de aceptación).
```

### WAC-DF-08 · (NUEVO) close_production_order_v2 roto con idempotency_key — drift uuid/text
```text
ID:            WAC-DF-08
severity:      HIGH
status:        SCHEMA DRIFT — OPEN · CONFIRMED REACHABLE: el route de la app SIEMPRE envía
               p_idempotency_key (`close-${orderId}-${uuid}`, src/app/api/production-orders/[id]/route.ts
               ~L110-119) → el cierre vía aplicación falla SIEMPRE con el esquema desplegado.
root cause:    línea 18 del RPC: «AND record_id = p_order_id::text» contra audit_logs.record_id que es
               uuid (esquema real verificado esta fase) → ERROR operator does not exist: uuid = text.
evidence:      A–F E.7 (orden queda in_progress); SQL de esquema (audit_logs.record_id uuid);
               código de app (siempre non-null key).
affected:      cierre de órdenes, idempotencia de cierre, pista de auditoría PRODUCTION_ORDER_CLOSED.
accounting:    PT no llega a inventario por la vía atómica → rutas alternativas manuales sin las mismas
               garantías; identidad INV-13/15 sin cerrar por este evento.
reproduction:  case-E.sql E.7 (con key → error; sin key → funciona).
remediation:   corrección del cast en el RPC (W7/W8 scope; NO corregido aquí); mientras tanto la app
               debería pasar key NULL (workaround operativo a ratificar por el dueño).
owner decision: aprobar el workaround operativo (cerrar sin key) hasta el fix, o congelar cierres.
blocked by:    ratificación del workaround; implementación del fix en ventana W7/W8.
```

### WAC-DF-09 · (NUEVO) Ambigüedad de overloads withdraw — la ruta API del app no es resoluble
```text
ID:            WAC-DF-09
severity:      HIGH (operacional)
status:        SCHEMA DRIFT — OPEN · demostrado en harness: llamada con los 6 argumentos nombrados
               comunes → «function public.withdraw_production_item(...) is not unique» (ejecución W6.0).
               El route de la app envía EXACTAMENTE esos 6 args nombrados → el endpoint de withdraw
               queda en riesgo PGRST203 (multiple choices) contra el esquema desplegado (S1=0 prueba que
               producción tiene ambas overloads).
root cause:    coexistencia de 6-arg legacy (62874) y 9-arg endurecida (62903, defaults en los 3 últimos).
evidence:      ejecución harness (E, error literal en case-E.out del primer intento — preservado);
               frontend route (6 named args); oids 62874/62903.
affected:       endpoint /api/production-orders/[id]/withdraw; operación de producción.
accounting:    indirecta: bloquea la vía de consumo de MP → sin PT → flujo de costo detenido.
reproduction:  SELECT public.withdraw_production_item(p_item_id=>..., p_qty=>..., p_unit_cost=>...,
               p_store_id=>..., p_user_id=>..., p_idempotency_key=>...); → error de resolución.
remediation:   eliminar/renombrar la overload legacy (W7/W8); mientras tanto, llamar la 9-arg con los
               3 params extra (disambiguación) o fijar p_server_side_cost.
owner decision: aprobar la estrategia de desambiguación (renombrar legacy vs migrar route a 9-arg).
blocked by:    decisión técnica del dueño (ratificación D-07 la arrastra).
```

## 2. Respuesta a la cuestión especial §5 (server-side ya desplegado)

```text
¿El overload 9-arg con p_server_side_cost significa que el defecto está parcialmente corregido?
→ NO como protección efectiva:
   (1) la variante SÍ impone costo servidor (E.1b: 50 correcto; NULL → ERR_PRODUCT_COST_UNAVAILABLE;
       cost_average=0 → usa 0: cero «legítimo» solo si el WAC realmente es 0 — interacción CR-W6-2/H-FINAL);
   (2) PERO el frontend NUNCA la invoca con true: envía 6 args nombrados con p_unit_cost = unit_cost || 0;
   (3) y esa llamada 6-arg NI SIQUIERA RESUELVE contra el par de overloads desplegado (WAC-DF-09);
   (4) por tanto: FIX DEPLOYED BUT BYPASSABLE + ruta operativa rota/ambigua.
SECURE PATH (producción, solo si se invoca explícitamente): 9-arg server_side=true → cost_average FOR UPDATE.
LEGACY PATH (el que usa la app): 6-arg → costo del cliente (0 por fallback del route).
Regla §6 aplicada: no se considera seguro hasta demostrar legacy eliminado/bloqueado/gobernado.
```

## 3. Call graph verificado (POS → RPC)

```text
VENTA:
  POS UI → POST /api/pos/checkout → rpc('create_sale_v2', items con cost_at_sale: i.cost ← CLIENTE)
        → create_sale_v2 (62894) → v_cost = COALESCE(cost_at_sale, cost, 0)
        → transaction_items.cost_at_sale + stock_movements.unit_cost (idénticos, ambos del cliente)
  [create_sale v1: solo tests que assertan su NO-uso]
  → alineación item↔movement: SÍ (mismo v_cost) — pero ambos contaminados: INV-07 pasa con dato venenado.

PRODUCCIÓN:
  UI → POST /api/production-orders/[id]/withdraw → rpc('withdraw_production_item',
        {6 args, p_unit_cost: unit_cost || 0}) → [AMBIGÜEDAD WAC-DF-09] → (si resolviera legacy 62874)
        → actual_unit_cost = p_unit_cost (cliente) → production_out unit_cost = p_unit_cost
        → close: UI siempre envía p_idempotency_key → close_production_order_v2 (62883) → ERROR uuid=text
          [WAC-DF-08] → receive_production_output → PT = Σ(qty×uc_cliente) → blend cost_average+cost_price.

DEVOLUCIÓN:
  UI → POST /api/devolutions → rpcName = USE_V2_REVERSE ? 'create_devolution_v2' : 'create_devolution'
        (FEATURES.USE_V2_REVERSE default FALSE — env NEXT_PUBLIC_USE_V2_REVERSE)
        → v2 (62891): uc reversión = cost_at_sale original, WAC intacto (A1) — lo testado en F;
        → v1-62746: kardex devolution_in uc = v_price (¡PRECIO DE VENTA como costo!);
        → v1-62776: BLEND de cost_average en devolución (anti-A1) + mismo kardex a precio.
        → TRES semánticas contables coexistiendo según flag y firma → sin fuente de verdad única.
```

## 4. Regla de cierre aplicada (§6) — checklist por defecto

```text
reachable?  authoritative?  enforced?  atomic?  concurrent-safe?  idempotent?  auditable?
WAC-DF-01:  SÍ (trigger)    NO         NO       NO (orden llegada)  n/a          parcial
WAC-DF-02:  SÍ (checkout)   NO         SÍ       SÍ (por venta)      SÍ           SÍ (del dato malo)
WAC-DF-03:  SÍ              NO         n/a      n/a                 SÍ (v2)      parcial
WAC-DF-05:  SÍ (route 6-arg) NO        SÍ       parcial             SÍ           SÍ
WAC-DF-07:  SÍ              NO         SÍ       NO (race F7)        SÍ           parcial
WAC-DF-08:  SÍ (app siempre) n/a       NO       n/a                 ROTO         ROTO
WAC-DF-09:  SÍ (route)      n/a        n/a      n/a                 n/a          n/a
```

Ningún defecto cumple la checklist completa → **NINGÚN defecto se cierra en W6.1**.
Estados finales: 4 OPEN (TRUE APPLICATION DEFECT), 1 FIX DEPLOYED BUT BYPASSABLE, 1 PARTIALLY MITIGATED
(policy), 2 SCHEMA DRIFT, 1 OPEN-UNTESTED (transfer), + políticas contables pendientes (D-05, D-12, D-07-flag).
