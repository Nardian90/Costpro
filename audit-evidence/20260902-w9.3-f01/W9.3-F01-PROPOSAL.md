# W9.3-F01-PROPOSAL — Endurecimiento RLS para F-01 (GATE HUMANO)

| Campo | Valor |
|---|---|
| FECHA (UTC) | 2026-09-02T18:20Z |
| BASE GIT | `70f978b6` (W9.2 closed) · tree CLEAN · staging 0 · stash 0 |
| BD | Supabase `wthkddeleylijmonclxg` · PostgreSQL 17.6 |
| ALCANCE | Exclusivamente F-01: RLS deshabilitado / exposición en tablas nuevas o sensibles |
| MODO | Propuesta PREVIA a mutación — ninguna instrucción DDL ha sido ejecutada |

---

## 1. TABLAS AFECTADAS

Inventario físico fresco (W9.3-B, 18:08–18:18Z): **146 tablas en `public`, 6 con RLS OFF, 0 policies definidas sobre ellas, 0 vistas que las referencien, 0 FKs que las toquen.** Ninguna de las 6 aparece en `supabase/migrations/` (0 referencias) — fueron creadas fuera del pipeline de migraciones (patrón típico de Supabase Studio: RLS OFF + `GRANT ALL` a anon/authenticated por defecto).

| # | Tabla | Filas | RLS | FORCE | Owner | ACL (resumen) |
|---|---|---|---|---|---|---|
| 1 | `store_credit_ledger` | 0 | OFF | OFF | postgres | `postgres=arwdDxtm, anon=arwdDxtm, authenticated=arwdDxtm, service_role=arwdDxtm` |
| 2 | `wac_change_log` | 0 | OFF | OFF | postgres | ídem (FULL a anon/auth) |
| 3 | `w62_df04_design_params` | 10 | OFF | OFF | postgres | ídem |
| 4 | `w62_df04_synthetic_rows` | 8 | OFF | OFF | postgres | ídem |
| 5 | `w62_zero_cost_flags` | 0 | OFF | OFF | postgres | ídem |
| 6 | `transaction_recovery_ledger` | 1 | OFF | OFF | postgres | `postgres=arwdDxtm, service_role=arwdDxtm, costpro_snapshot_restorer=ar` |

Leyenda: a=INSERT r=SELECT w=UPDATE d=DELETE D=TRUNCATE x=REFERENCES t=TRIGGER m=MAINTAIN → `arwdDxtm` = ALL PRIVILEGES (PG17).

## 2. ACL PRE

Evidencia completa: `evidence/f01/pre/b2_rls_off_tables.json` (ACL cruda), `b2b_rls_off_grants.json` (expandida), `b2c_rls_off_priv_matrix.json` (has_table_privilege). Síntesis:

- Tablas 1–5: anon y authenticated tienen **todas** las privilegios de tabla, incluido `TRUNCATE` y `MAINTAIN`.
- Tabla 6: anon/authenticated **sin ningún privilegio** (probe REST → HTTP 401); solo `service_role`, `postgres` y `costpro_snapshot_restorer` (SELECT+INSERT, no grantable).

## 3. RLS PRE

`relrowsecurity=false` y `relforcerowsecurity=false` en las 6 (b2_rls_off_tables.json). Confirmado en vivo y por probe físico.

## 4. POLICIES PRE

**0 policies** sobre las 6 tablas (`b17_policies_on_targets.json`). Con RLS OFF las policies no aplican; el acceso queda gobernado exclusivamente por los grants de tabla — por eso la exposición es total en las tablas 1–5.

Observación de alcance (NO se corrige aquí, clasificada para fase posterior — pertenece a la clase "RLS ON + policies insuficientes/excesivas", no a "RLS OFF"):
- `audit_events`: policy PERMISSIVE ALL `TO public` con `USING (true)` → anon con SELECT de tabla puede leer eventos de auditoría (escritura bloqueada por `WITH CHECK (false)`). RLS ON ⇒ fuera del alcance F-01 de esta fase.
- `idempotency_registry`: policy `idem_reg_service_role_all` declarada `TO authenticated` con `USING/WITH CHECK (true)` (nombre engañoso); el grant de tabla solo da SELECT a authenticated ⇒ lectura expuesta, escritura bloqueada por grants. RLS ON ⇒ fuera de alcance.
- 4 tablas RLS ON sin policies (`exchange_rate_audit` FORCE, `idempotency_keys`, `migration_history_snapshots`, `units_of_measure`): deny-by-default ya correcto — patrón que esta migración extiende a las 6 restantes.
- 25 policies con `=true` revisadas: el resto son patrones service_role-only estándar o SELECT-reference para authenticated (aceptables; 0 policies de escritura anon).

## 5. CONSUMIDORES (W9.3-C — blast radius demostrado)

| Tabla | Escritores BD | Lectores BD | Referencias en src/ | anon directo (probe) | Clasificación |
|---|---|---|---|---|---|
| store_credit_ledger | `create_devolution_v2` (SD, owner postgres) | ninguno | 0 | 200 accesible | Escritor interno SD único; sin consumidores directos |
| wac_change_log | `fn_recalc_wac` (SD) | ninguno | 0 | 200 accesible | Ídem |
| w62_zero_cost_flags | `create_sale_v2`, `withdraw_production_item_v3` (SD) | ninguno | 0 | 200 accesible | Ídem |
| w62_df04_design_params | ninguno (params estáticos W6.1) | `w62_df04_classify` (INVOKER, **0 llamadores en BD y src/**) | 0 | **206 — 10 filas expuestas** | SIN CONSUMIDORES |
| w62_df04_synthetic_rows | ninguno | ninguno | 0 | **206 — 8 filas expuestas** | SIN CONSUMIDORES |
| transaction_recovery_ledger | `restore_transaction_snapshot` (SD); trigger `trg_ledger_append_only`→`enforce_ledger_append_only` (SD) | ninguno | 0 | 401 (sin grants) | Escritor SD único + rol dormido |

Hechos clave del blast radius:
1. **Todos los escritores son SECURITY DEFINER owned by `postgres`** y `postgres` tiene `rolbypassrls=true` (b6_roles.json) → activar RLS **no altera ningún camino de escritura**.
2. `service_role` tiene `rolbypassrls=true` → **todas las rutas API del servidor** (getSupabaseAdminClient/Safe) **siguen intactas** sin necesidad de policies.
3. **0 referencias** a las 6 tablas en `src/`, `e2e/`, `tests/` (búsqueda por nombre literal; la mención `store_credit` en `devolutions/route.ts` es un valor de enum `payment_method`, no acceso a tabla).
4. No hay FKs (b8), ni vistas/materialized (b11), ni secuencias con grants públicos asociadas (b7), ni triggers salvo `trg_ledger_append_only` en la tabla 6 (b9).
5. `costpro_snapshot_restorer`: NOLOGIN, sin BYPASSRLS, sin dueño de funciones; solo aparece en checks `current_user IN ('costpro_snapshot_restorer','postgres')` del patrón restore_mode (migraciones 20260817000001/2/4). Es un **consumidor dormido con grants explícitos** (SELECT+INSERT) → se preserva su capacidad exacta vía policy explícita (ver §7) para que el cambio neto sea únicamente "anon/auth pierden acceso".

## 6. RIESGO POR TABLA (PRE → sin corregir)

| Tabla | Riesgo | Detalle |
|---|---|---|
| store_credit_ledger | CRÍTICO | Ledger financiero legible/insertable/borrable/**TRUNCABLE** por anon; hoy vacía, pero `create_devolution_v2` la puebla con créditos de tienda |
| wac_change_log | CRÍTICO | Log de auditoría de WAC anon-escribible/borrable; hoy vacía porque `fn_recalc_wac` aún no ha registrado cambios |
| w62_df04_design_params | ALTO | 10 filas de parámetros de diseño (W6.1) expuestas en claro a anon (206 con datos) |
| w62_df04_synthetic_rows | ALTO | 8 filas sintéticas de reconstrucción expuestas a anon |
| w62_zero_cost_flags | CRÍTICO | Flags de costo cero anon-manipulables; `create_sale_v2` escribe aquí en cada venta a costo cero |
| transaction_recovery_ledger | BAJO (defensa en profundidad) | Sin exposición directa; RLS OFF es desviación del estándar del proyecto (146−6=140 tablas con RLS ON) |

## 7. SQL EXACTO PROPUESTO

Migración versionada `supabase/migrations/20260902000001_w9_f01_rls_hardening.sql` (transaccional, con guard abortante, sin SQL dinámico, sin tocar datos):

```sql
BEGIN;

-- GUARD (solo lectura de catálogo; aborta si el estado no es el esperado):
--   * exactamente las 6 tablas target existen, relkind='r', RLS OFF, FORCE OFF, owner=postgres
--   * 0 policies existentes sobre ellas
--   * anon tiene SELECT en las 5 expuestas y NO en transaction_recovery_ledger
--   * costpro_snapshot_restorer existe con grants exactamente {SELECT, INSERT}
DO $guard$ ... RAISE EXCEPTION si algo difiere ... $guard$;

-- (A) Las 5 tablas expuestas: revocar todo acceso no-administrativo
REVOKE ALL PRIVILEGES ON TABLE public.store_credit_ledger      FROM anon, authenticated, PUBLIC;
REVOKE ALL PRIVILEGES ON TABLE public.wac_change_log           FROM anon, authenticated, PUBLIC;
REVOKE ALL PRIVILEGES ON TABLE public.w62_df04_design_params   FROM anon, authenticated, PUBLIC;
REVOKE ALL PRIVILEGES ON TABLE public.w62_df04_synthetic_rows  FROM anon, authenticated, PUBLIC;
REVOKE ALL PRIVILEGES ON TABLE public.w62_zero_cost_flags      FROM anon, authenticated, PUBLIC;

-- (B) Activar RLS deny-by-default (sin policies: solo roles BYPASSRLS acceden)
ALTER TABLE public.store_credit_ledger      ENABLE ROW LEVEL SECURITY;
ALTER TABLE public.wac_change_log           ENABLE ROW LEVEL SECURITY;
ALTER TABLE public.w62_df04_design_params   ENABLE ROW LEVEL SECURITY;
ALTER TABLE public.w62_df04_synthetic_rows  ENABLE ROW LEVEL SECURITY;
ALTER TABLE public.w62_zero_cost_flags      ENABLE ROW LEVEL SECURITY;

-- (C) transaction_recovery_ledger: RLS + policy explícita que preserva la
--     capacidad actual del rol dormido costpro_snapshot_restorer (SELECT/INSERT,
--     el resto sigue gated por grants). anon/auth ya carecían de grants.
ALTER TABLE public.transaction_recovery_ledger ENABLE ROW LEVEL SECURITY;
CREATE POLICY f01_snapshot_restorer_access
  ON public.transaction_recovery_ledger
  AS PERMISSIVE FOR ALL
  TO costpro_snapshot_restorer
  USING (true) WITH CHECK (true);

NOTIFY pgrst, 'reload schema';
COMMIT;
```

Decisiones de diseño (principio deny-by-default + mínimo privilegio + consumidor explícito):
- **Sin policies genéricas `USING(true)` para anon/authenticated**: ninguna tabla recibe acceso público.
- **0 policies en las tablas 1–5**: el único consumidor legítimo es `service_role` (BYPASSRLS, evidenciado) y los escritores SD (postgres BYPASSRLS). Menor superficie posible.
- **`USING(true)` solo en la policy TO `costpro_snapshot_restorer`**: rol interno NOLOGIN con grants preexistentes — evidencia inequívoca de intención; la policy mantiene el estado efectivo 1:1 (además gated por grants: solo SELECT/INSERT).
- **NO `FORCE ROW LEVEL SECURITY`** (decisión justificada por tabla, las 6): el owner es `postgres`, que ya tiene BYPASSRLS ⇒ FORCE sería un no-op para el único owner y no añade protección; se evita complejidad innecesaria.
- **Se REVOCA también TRUNCATE/MAINTAIN** (REVOKE ALL): crítico, porque **RLS no gobierna TRUNCATE** — dejar el grant con RLS ON seguiría permitiendo TRUNCATE anónimo.
- `NOTIFY pgrst, 'reload schema'` al final (patrón W9.2) para refrescar el schema cache de PostgREST.

## 8. RESULTADO ESPERADO (POST)

1. `total rls_off = 0` en `public` (146/146 con RLS ON).
2. Tablas 1–5: `has_table_privilege(anon|authenticated, *) = false` en todo; service_role/postgres = true (grants intactos).
3. Tabla 6: grants sin cambios; RLS ON; 1 policy nueva (`f01_snapshot_restorer_access`).
4. Probes PostgREST: anon → HTTP 401/42501 en las 5 expuestas; `transaction_recovery_ledger` sigue 401; service_role → 200 en todas; control `stores` anon → 206/200 (no-regresión).
5. Los 23 conteos de integridad de datos idénticos al PRE (ventana mínima).
6. Identidad de tablas/policies/ACLs restantes: únicamente los cambios previstos.

## 9. ROLLBACK EXACTO (1:1 con PRE)

```sql
BEGIN;
-- Restaurar ACL de las 5 expuestas (eran arwdDxtm = ALL PRIVILEGES):
GRANT ALL PRIVILEGES ON TABLE public.store_credit_ledger     TO anon, authenticated;
GRANT ALL PRIVILEGES ON TABLE public.wac_change_log          TO anon, authenticated;
GRANT ALL PRIVILEGES ON TABLE public.w62_df04_design_params  TO anon, authenticated;
GRANT ALL PRIVILEGES ON TABLE public.w62_df04_synthetic_rows TO anon, authenticated;
GRANT ALL PRIVILEGES ON TABLE public.w62_zero_cost_flags     TO anon, authenticated;
-- Desactivar RLS y eliminar la policy nueva:
ALTER TABLE public.store_credit_ledger      DISABLE ROW LEVEL SECURITY;
ALTER TABLE public.wac_change_log           DISABLE ROW LEVEL SECURITY;
ALTER TABLE public.w62_df04_design_params   DISABLE ROW LEVEL SECURITY;
ALTER TABLE public.w62_df04_synthetic_rows  DISABLE ROW LEVEL SECURITY;
ALTER TABLE public.w62_zero_cost_flags      DISABLE ROW LEVEL SECURITY;
DROP POLICY IF EXISTS f01_snapshot_restorer_access ON public.transaction_recovery_ledger;
ALTER TABLE public.transaction_recovery_ledger DISABLE ROW LEVEL SECURITY;
NOTIFY pgrst, 'reload schema';
COMMIT;
```
(service_role/postgres nunca perdieron grants ⇒ no se re-conceden; `PUBLIC` nunca tuvo grants ⇒ no se re-concede.)

## 10. TESTS QUE SE EJECUTARÁN (post-aplicación)

1. **Matriz RLS**: `relrowsecurity/relforcerowsecurity` por tabla (esperado ON/OFF respectivamente) + `total rls_off=0`.
2. **Matriz de privilegios**: `has_table_privilege` × {PUBLIC via has_schema… no aplica; anon, authenticated, service_role, postgres} × {SELECT, INSERT, UPDATE, DELETE} para las 6 tablas.
3. **Policies POST**: única policy nueva en tabla 6; 0 en las demás; resto de las 390 policies del esquema sin cambios (diff PRE/POST).
4. **Probes PostgREST no destructivos** (GET count-only): anon/svc/control stores.
5. **Regresión de datos**: 23 métricas idénticas.
6. **Regresión estructural**: número de tablas/columnas/FKs/triggers/funciones sin cambios (diff catálogo).
7. **Regresión de aplicación**: health del servidor local :3000 + verificación estática de consumidores (git diff = solo migración); TypeScript/lint no aplican (0 cambios en src/).
8. **Consumidor legítimo**: `service_role` conserva SELECT/INSERT/UPDATE/DELETE en las 6 (matriz) y probe svc 200.

## 11. IMPACTO SOBRE LA APLICACIÓN

- **Nulo en runtime**: 0 consumidores directos de las 6 tablas en `src/`; todos los escritores son SECURITY DEFINER (postgres); las rutas API usan service_role (BYPASSRLS).
- La tienda pública/storefront no consulta estas tablas (0 referencias).
- Único cambio observable: clientes anon/authenticated que hoy consulten directamente `/rest/v1/{tabla}` pasarán de 200/206 a 401/42501 — **es exactamente el comportamiento de seguridad buscado**.
- Sin cambios en `src/`, sin redeploy, sin secretos, sin Vercel.

## VEREDICTO DEL GATE

- Consumidores: **sin ambigüedad** (0 consumidores directos; escritores SD; service_role bypass; rol dormido preservado 1:1).
- Operaciones legítimas en riesgo: **ninguna identificada** (probes y catálogo lo demuestran).
- ⇒ `W9.3-E = PASS` — se autoriza continuar con W9.3-F..L bajo el mandato "Si esos gates son PASS, puedes continuar con F–L".
- Si tras la aplicación CUALQUIER verificación falla: STOP + rollback exacto de §9 + reporte FAIL.
