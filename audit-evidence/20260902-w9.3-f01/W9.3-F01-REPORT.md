# W9.3 — F-01 RLS HARDENING REPORT

| Campo | Valor |
|---|---|
| FASE | W9.3 (F-01 exclusivamente) |
| FECHA (UTC) | 2026-09-02 · PRE 18:05Z → APLICACIÓN 18:22:53Z → POST 18:25Z |
| BASE GIT | `70f978b6` (W9.2 closed) · branch main · tree CLEAN · staging 0 · stash 0 |
| BD | Supabase `wthkddeleylijmonclxg` · PostgreSQL 17.6 · 146 tablas en public |
| MIGRACIÓN | `supabase/migrations/20260902000001_w9_f01_rls_hardening.sql` · SHA256 `21ed6d3120cd7b5546cdf08cbfe1c7609a72496db4afa8718022f1b733e09bde` |
| VEREDICTO | **W9.3 = PASS · F-01 = CLOSED · READY FOR W9.4 = YES** |

---

## 1. PRE STATE

- Git: HEAD=70f978b6 = commit W9.2; remote main=1c204d1e (W9.2 sin push, conforme a mandato); tree CLEAN.
- W9.2 presente: migración `20260831000001_w9_f07_reset_store_data_acl.sql` (SHA256 `d979b015…`) commitada en 70f978b6.
- Producción en estado W9.2: 2 overloads de `reset_store_data` con matriz {public/anon/auth=false, svc/pg=true}; 0 llamadores BD.
- Evidencia PRE: `evidence/f01/pre/` — b1…b18 (inventario físico completo, roles, grants, policies, FKs, triggers, funciones, secuencias, probes), data_integrity (23 métricas), queries.log.

## 2. TABLAS AFECTADAS (F-01) Y CLASIFICACIÓN W9.3-B

6 tablas en `public` con RLS OFF (ninguna existe en `supabase/migrations/` — creadas fuera del pipeline; patrón Studio con GRANT ALL a anon+authenticated):

| Tabla | Filas | Clasificación W9.3-B | Exposición física (probe anon PRE) |
|---|---|---|---|
| store_credit_ledger | 0 | RLS OFF + acceso PUBLIC/anon | HTTP 200 — accesible (leer/escribir/TRUNCATE) |
| wac_change_log | 0 | RLS OFF + acceso PUBLIC/anon | HTTP 200 — accesible |
| w62_df04_design_params | 10 | RLS OFF + acceso PUBLIC/anon | **HTTP 206 — 10 filas de datos expuestas** |
| w62_df04_synthetic_rows | 8 | RLS OFF + acceso PUBLIC/anon | **HTTP 206 — 8 filas de datos expuestas** |
| w62_zero_cost_flags | 0 | RLS OFF + acceso PUBLIC/anon | HTTP 200 — accesible |
| transaction_recovery_ledger | 1 | RLS OFF + únicamente service_role | HTTP 401 — sin grants públicos |

## 3. BLAST RADIUS (W9.3-C)

- **0 consumidores** de las 6 tablas en `src/`, `e2e/`, `tests/` (búsqueda por nombre literal; `store_credit` en `devolutions/route.ts` es un enum de payment_method).
- Escritores BD: todos SECURITY DEFINER owned by postgres (BYPASSRLS): `create_devolution_v2`→store_credit_ledger; `fn_recalc_wac`→wac_change_log; `create_sale_v2`+`withdraw_production_item_v3`→w62_zero_cost_flags; `restore_transaction_snapshot`+trigger `trg_ledger_append_only`→transaction_recovery_ledger.
- `service_role` tiene `rolbypassrls=true` (b6_roles.json) ⇒ rutas API del servidor inmunes a RLS.
- `w62_df04_classify` (única función INVOKER, lee w62_df04_design_params): **0 llamadores** en BD y src/ ⇒ sin consumidores.
- 0 FKs, 0 vistas, 0 secuencias públicas asociadas. `costpro_snapshot_restorer`: NOLOGIN, dormido, grants {SELECT,INSERT}, referenciado solo en checks `current_user IN (...)` del patrón restore_mode.
- Clasificación de consumidores: **CONSUMIDOR LEGÍTIMO** = service_role (API server) + escritores SD; **SIN CONSUMIDORES** = acceso directo anon/authenticated; **CONSUMIDOR DORMIDO PRESERVADO** = costpro_snapshot_restorer. Consumidor LEGACY/DESCONOCIDO: ninguno.

## 4. DISEÑO (W9.3-D) Y GATE (W9.3-E)

Propuesta completa en `W9.3-F01-PROPOSAL.md` (11 secciones + veredicto PASS). Principio: deny by default + mínimo privilegio + consumidor explícito. Sin políticas genéricas `USING(true)` para roles públicos. `FORCE RLS` NO activado (justificado por tabla: owner=postgres con BYPASSRLS ⇒ no-op).

## 5. SQL APLICADO (W9.3-F/G)

`20260902000001_w9_f01_rls_hardening.sql` — transaccional (BEGIN/COMMIT), con guard abortante de 5 checks (existencia/tipo/owner de las 6, 0 policies previas, exposición PRE esperada de anon, grants exactos del restorer), sin SQL dinámico, sin tocar datos:
1. `REVOKE ALL PRIVILEGES … FROM anon, authenticated, PUBLIC` × 5 tablas expuestas (incluye TRUNCATE/MAINTAIN — RLS no gobierna TRUNCATE).
2. `ALTER TABLE … ENABLE ROW LEVEL SECURITY` × 6.
3. `CREATE POLICY f01_snapshot_restorer_access ON transaction_recovery_ledger AS PERMISSIVE FOR ALL TO costpro_snapshot_restorer USING (true) WITH CHECK (true)`.
4. `NOTIFY pgrst, 'reload schema'`.
Aplicada vía Management API (único canal disponible en sandbox): **HTTP 201, respuesta `[]`, 18:22:53Z**. Ventana PRE→POST ≈ 35 s sin cambios concurrentes (HEAD y tracked-dirty verificados 0 justo antes).

## 6. PRIVILEGIOS POST (matriz verificada — h3_priv_matrix.json)

| Tablas | anon | authenticated | service_role | postgres | costpro_snapshot_restorer |
|---|---|---|---|---|---|
| 5 expuestas (sel/ins/upd/del) | **false×4** | **false×4** | true×4 | true | false (nunca tuvo) |
| transaction_recovery_ledger | false×4 | false×4 | true×4 | true | **sel=true, ins=true (preservado)** |

## 7. POSTGREST VERIFICATION (sin ejecutar funciones destructivas; solo GET count-only)

anon: 6/6 tablas → **HTTP 401** (antes 200/206 en 5, con 18 filas de datos expuestas). service_role: 200/206 en todas las probadas. Control de no-regresión: anon `/stores` → 206 (0-0/4) idéntico al PRE. Cache PostgREST recargada vía NOTIFY.

## 8. SECURITY REGRESSION (ONLY INTENDED CHANGED)

- RLS: 6 tablas ON (previsto); global `rls_off`: 6→**0**; FORCE: 7 sin cambios.
- Policies: 390→391 (+1 prevista); las 390 restantes byte-a-byte iguales.
- ACL cruda: 5 tablas = `{postgres, service_role}`; tabla 6 sin cambios; resto del esquema sin cambios.
- Estructural: 146 tablas / 11 vistas / 481 funciones / 81 triggers / 261 FKs — 0 cambios.
- **W9.2 intacto**: matriz F-07 de `reset_store_data` idéntica (h6_f07_intact.json).
- `audit_events` (policy pública de lectura) e `idempotency_registry` (policy a authenticated): clasificados, **NO tocados** (RLS ON ⇒ otra clase de hallazgo, fase futura).

## 9. DATA INTEGRITY (W9.3-I)

23/23 métricas idénticas PRE→POST (diff vacío): products=323, transactions=520, transaction_items=555, stock_movements=702, inventory=141/4978.6289, payment_transactions=366, devolutions=13, receipts=6, cash_closures=11, WAC_sum=6852321.2457, WAC=0→105, WAC<0→0, las 6 tablas target con conteos exactos, audit_logs=7375, total_tables=146. **0 cambios de datos no intencionados.**

## 10. APPLICATION REGRESSION (W9.3-J)

- Servidor pm2: costpro + telegram/whatsapp pollers online, uptime 60m sin reinicios; GET :3000/ → 200; GET :3000/api/health → 200.
- Git: único cambio = migración nueva (untracked); 0 cambios en src/ ⇒ TypeScript/lint no aplican.
- Consumidor legítimo estático: service_role conserva privilegios completos (matriz) + probes svc 200.

## 11. ROLLBACK PLAN

`evidence/f01/rollback/rollback_w9_f01.sql` — reversión 1:1 exacta (GRANT ALL a anon/authenticated ×5, DISABLE RLS ×6, DROP POLICY), NO ejecutado (todas las verificaciones PASS). Reversibilidad verificada documentalmente contra `pre/b2_rls_off_tables.json`.

## 12. EVIDENCE FILES (SHA256 en W93-EVIDENCE-SHA256SUMS)

- `evidence/f01/pre/` (26 archivos): b01…b18 inventario+matrices+probes, data_integrity, queries.log.
- `evidence/f01/post/` (17 archivos): h1…h6 verificaciones, probes, data_integrity, queries.log.
- `evidence/f01/comparison/`: comparison_report.md (7 secciones).
- `evidence/f01/rollback/`: rollback_w9_f01.sql.
- `evidence/f01/`: migration_apply_response.json, migration_file.sha256.
- Espejo commiteado en repo: `audit-evidence/20260902-w9.3-f01/`.

## 13. LIMITACIONES

1. Aplicación vía Management API (sandbox sin psql/docker): no hubo ensayo local en PostgreSQL 17; mitigado por guard abortante + reversibilidad 1:1 + ventana mínima.
2. Los probes REST usan GET count-only; no se ejecutó ninguna escritura de prueba anon (hubiera requerido mutar datos).
3. `w62_df04_classify` queda expuesto a anon/auth a nivel EXECUTE (función sin llamadores); su comportamiento interno bajo RLS cambia solo si alguien la invocara sin bypass — observación documentada, fuera de alcance.
4. Items observados NO corregidos (fuera de alcance F-01, requieren fase propia por ambigüedad de consumidores browser): policy pública de lectura en `audit_events`; policy `idem_reg_service_role_all` mal rotulada (TO authenticated) en `idempotency_registry`.
5. No se hizo push (mandato); el commit local contiene migración + evidencia espejo + documentación.

## 14. CONCLUSIÓN

```
W9.3 = PASS
F-01 = CLOSED
READY FOR W9.4 = YES
```

Las 6 tablas con RLS OFF del proyecto pasaron a 0; la exposición anónima real (18 filas de datos + 5 tablas manipulables incl. TRUNCATE) quedó eliminada con 0 cambios de datos, 0 impactos en consumidores legítimos (service_role BYPASSRLS + escritores SECURITY DEFINER), el rol dormido `costpro_snapshot_restorer` preservado 1:1, y W9.2/F-07 verificado intacto. Rollback exacto disponible y no ejecutado.
