# W9.4-F06-REPORT — SECURITY DEFINER EXECUTE HARDENING (subfase W9.4.1)

Fecha cierre: 2026-09-02T19:30:52Z · Proyecto: wthkddeleylijmonclxg · Base: `2787b22c` (W9.3)

## 0. Veredicto

```text
W9.4 = PASS WITH CONDITIONS
F-06 = PARTIALLY CLOSED (subfase W9.4.1 aplicada y verificada)
READY FOR W9.4.2 = YES
```

- Migración aplicada: `supabase/migrations/20260902000002_w9_f06_secdef_execute_hardening.sql`
  (sha256 c4aa5510…, 292 statements, 119 funciones por firma completa).
- Resultado físico: 119/119 candidatas con cambio ÚNICAMENTE de `proacl`; 362/362 funciones restantes byte-idénticas; 0 cambios de identidad; 0 cambios de datos.
- Pendiente documentado para W9.4.2/3: 123 funciones deliberadamente NO tocadas (§7).

## 1. Qué función cambió y por qué

Cada una de las 119 funciones cambió **solo su ACL de EXECUTE** hacia `{postgres(owner), service_role}`:

| Set | Criterio (clasificación W9.4-C/D) | n |
|---|---|---:|
| C4 | Sin consumidores en ningún canal (src estático+dinámico, scripts, e2e, llamadores BD, triggers, policies, vistas, pg_depend, cron, literales repo-wide) | 60 |
| C1B | Consumidor solo BD-interno: trigger functions (EXECUTE se verifica solo al crear el trigger) o llamadores 100% SECURITY DEFINER owner postgres (contexto definer conserva EXECUTE owner) | 37 |
| C2 | Consumidor 100% service_role trazado por receptor: rutas con `getSupabaseAdminSafe()`/`getSupabaseAdminClient()` + contract test (service key) | 22 |

Detalle por función (PRE ACL → POST ACL, con consumidores y flags):

- `evidence/f06/comparison/acl_diff_per_function.json` (119 registros: pre_acl, post_acl, pre_exec, post_exec)
- `evidence/f06/comparison/privilege_matrix_119.json` (matriz has_function_privilege POST)
- `evidence/f06/f06_classification.json` (clasificación completa de las 242 SD con caps, guards y consumidores)

## 2. Qué tenía antes y qué tiene después

| Patrón ACL PRE | n | POST |
|---|---:|---|
| `{postgres, authenticated, service_role}` | 79 | `{postgres, service_role}` |
| `{PUBLIC, authenticated, postgres, service_role}` | 24 | `{postgres, service_role}` |
| `{PUBLIC, anon, authenticated, postgres, service_role}` | 15 | `{postgres, service_role}` |
| `{authenticated, costpro_snapshot_restorer, postgres, service_role}` | 1 | `{postgres, service_role, costpro_snapshot_restorer}` |

Métricas globales de exposición SD (has_function_privilege sobre 242):

| Métrica | PRE | POST | Δ |
|---|---:|---:|---:|
| ejecutables por PUBLIC | 65 | 26 | −39 |
| ejecutables por anon | 65 | 26 | −39 |
| ejecutables por authenticated | 222 | 103 | −119 |
| sin exposición (C0 efectivo) | 20 | 139 | +119 |
| no-SD ejecutables por anon (control, fuera de alcance) | 200 | 200 | 0 |

## 3. Quién las utiliza

- **C4**: nadie (evidencia de ausencia en 10 canales; 29 de ellas tienen consumidor de *contract
  test* `scripts/test_contract_rpcs_store_access.mjs`, que usa SOLO `SUPABASE_SERVICE_ROLE_KEY` →
  sigue funcional).
- **C1B trigger** (10): `audit_*` (9) + `reverse_commissions_on_sale_void` — invocadas por triggers
  (`evidence/f06/comparison/structure_triggers_unchanged.json` → triggers intactos, 81/81).
- **C1B guards**: llamadas solo desde funciones SD (p.ej. `has_management_access_as` ←
  `reset_store_data`; `has_store_access_as` ← múltiples SD) — en contexto definer el EXECUTE se
  evalúa contra el owner (postgres), que lo conserva.
- **C2 service**: rutas API con cliente service (p.ej. `close_cash_shift` ← `/api/cash-closures/close`,
  `managed_*` ← rutas `/api/users/*`) — trazado por receptor en `pre/src_rpc_receiver_roles.md`.
- Conservación explícita: `costpro_snapshot_restorer` mantiene EXECUTE en
  `validate_pre_restore_fk_integrity(uuid)` (grant preexistente, NO revocado: verificado).

## 4. Qué rol conserva acceso

POST: `service_role` (GRANT explícito) y `postgres` (owner) en las 119. `PUBLIC`, `anon` y
`authenticated` pierden EXECUTE. Prueba: `comparison/privilege_matrix_119.json` — 119/119 con
`public=false, anon=false, authenticated=false, service_role=true, postgres=true`.

## 5. Qué prueba demuestra que no se rompió

### 5a. BD (W9.4-J) — `verdict: PASS`

- Solo `proacl` cambió: identidad completa (nombre, firma, retorno, owner, secdef, volatilidad,
  lenguaje, kind, proconfig) idéntica en 481/481 + **md5 de prosrc idéntico en 481/481**.
- Estructura inalterada (comparación canónica order-independent): triggers 81=81, policies 391=391,
  vistas 11=11, pg_depend 625=625.
- W9.2 intacto: `reset_store_data` 2/2 overloads con matriz service/postgres-only.
- W9.3 intacto: `rls_off=0` (146 tablas RLS ON), policy `f01_snapshot_restorer_access` presente.

### 5b. PostgREST (W9.4-K)

- anon POST sobre modificadas con params requeridos ({} no ejecutable por diseño):
  `has_management_access_as`/`calculate_receipt_total_cup`/`apply_physical_count`/`register_idempotency`
  → **404 PGRST202** (fuera del cache de anon); `generate_internal_barcode` → **401 42501 permission
  denied**. 5/5 rechazos — ninguna ejecutada.
- service_role positivo READ-ONLY: `has_management_access_as(null,null)` → **HTTP 200 `true`**.
- OpenAPI service: modificadas visibles para service_role (8/8 muestreadas).
- Control: anon `GET /stores?limit=1` → 200 (alcance F-06 solo funciones; tablas intactas).

### 5c. Datos (W9.4-L)

- 23/23 métricas idénticas PRE→POST (products 323, transactions 520, transaction_items 555,
  stock_movements 702, inventory 141/4978.6289, WAC_sum invariado, WAC=0: 105, audit_logs 7375,
  ledgers W9.3 sin cambios). `comparison/data_regression.json` → identical=true. **0 cambios no intencionados.**

### 5d. Aplicación (W9.4-M)

- pm2: 3/3 apps online (uptime continuo 2h+, sin reinicios); `GET /` → 200; `GET /api/health` → 200.
- Smoke de 7 rutas consumidoras de RPCs modificadas sin sesión → 401/405 (autenticación responde,
  sin 500). No se ejecutaron rutas mutadoras reales (regla 7 del mandato).
- **Condición documentada**: `tsc --noEmit` no reproducible en este sandbox (OOM kill ×2 con el
  servidor en ejecución). Mitigación forense: `git status` = 0 cambios tracked ⇒ `src/` byte-idéntico
  al estado W9.3 aprobado ⇒ el resultado de compilación es equivalente por construcción; el servidor
  en vivo ejecuta ese mismo código sin regresión de smoke.

## 6. Aplicación y mecanismo

- GUARD interno de la migración (dry-run aislado previo: `pre/guard_dryrun.json` → HTTP 201 []):
  verifica W9.2/W9.3 intactos, contadores globales de deriva (SD anon=65, expuestas=222) y las 119
  firmas con owner/secdef/exposición PRE exacta. Un catch formal detectó que el check PRE de anon
  debía ser *efectivo* (entrada explícita O herencia PUBLIC) — corregido antes de aplicar.
- Aplicación: Management API HTTP 201 @ 2026-09-02T19:22:48Z, respuesta `[]`, transaccional
  (BEGIN/COMMIT), `NOTIFY pgrst, 'reload schema'` incluido. Anti-concurrencia: HEAD=2787b22c, 0 tracked dirty.

## 7. Pendiente para W9.4.2 / W9.4.3 (condiciones del PASS)

| Grupo | n | Bloqueo | Acción propuesta |
|---|---:|---|---|
| C2 con contexto mixto | 31 | rutas con `getSupabaseForSession` (dual: dev-bypass service / sesión real authenticated) o feature flags v1-v2 — receptor no trazable estáticamente al 100% | evidencia de código por ruta + narrow a service donde proceda |
| C1/C3 con consumidor authenticated | 53 | PostgREST requiere EXECUTE al rol del JWT para la UI; el guard interno (`has_store_access_as`, etc.) es la autorización activa (patrón V2.12.x probado en e2e) | redesign de ruta/guard (fuera del alcance ACL); riesgo residual documentado en PROPOSAL §10 |
| Guards RLS en policies | 16 | revocar EXECUTE rompería la evaluación RLS de anon/authenticated (has_store_access ×97, is_admin ×59…) | NO TOCAR — son el mecanismo RLS |
| C1B con llamador INVOKER | 1 | `get_or_create_product_cost_sheet` — EXECUTE externo puede requerirse | analizar llamador INVOKER |
| Test-consumers | 2 | `is_tenant_member`, `has_store_role_as` solo en src/__tests__ | confirmar contexto de tests y decidir |
| C0 | 20 | sin exposición | nada que hacer |

**Criterio de éxito del mandato aplicado**: no se declara F-06 CLOSED; se declara PARTIALLY CLOSED
con la totalidad del subconjunto cerrable demostrablemente resuelta (119/119 verificadas) y el
resto cuantificado, con bloqueo exacto y ruta de cierre.

## 8. Rollback (NO ejecutado)

`evidence/f06/rollback/rollback_w9_f06.sql` — GRANTs inversos 1:1 por firma (119 funciones, 158
GRANTs) + NOTIFY. Disponible con orden explícita del usuario; no se ha ejecutado.

## 9. Manifiesto de evidencia

- `evidence/f06/pre/` — 40+ archivos: inventario 481 (b01), ACL expandida 2.106 (b02), prosrc 481 (d01),
  pg_depend (d02), triggers (d03), policies (d04), vistas (d05), cron/extensiones (e01-e02), refs de
  policies (e03, policy_fn_refs), consumidores src/scripts/e2e + dinámicos + receiver roles, triage
  literal repo-wide, snapshot de datos PRE, guard dry-run, queries.log.
- `evidence/f06/post/` — h01-h06 (inventario, prosrc, global, W9.2/RLS/policy, triggers/policies/
  vistas/deps), probes PostgREST (5 anon + 1 svc + OpenAPI + health), tsc_noemit.log (OOM documentado), queries.log.
- `evidence/f06/comparison/` — only_acl_changed.json (verdict PASS), acl_diff_per_function.json (119),
  privilege_matrix_119.json, data_regression.json (23/23), structure_*_unchanged.json (4), artefactos
  de clasificación (f06_classification.json, f06_priority_table.csv, f06_summary.md, f06_final_sets.json, f06_migration_candidates.json).
- `evidence/f06/rollback/rollback_w9_f06.sql` — inverso 1:1.
- `W9.4-F06-PROPOSAL.md` (11 secciones) · este informe · `SHA256SUMS`.

