# FASE E-SEC — 12 CI

## Fecha / HEAD
2026-09-26 · HEAD verificado en remoto: **6022ae85b3d29e2573caa22a8538d4a97f2d02a3**
(`git ls-remote origin main` == HEAD local; worktree limpio, salvo el untracked
preexistente `audit-evidence/FASE-C2R/10-reverification-r1.md`).

## Commits de la fase (conceptualmente separados)
```text
3e5758bd fix(pos): enforce server-side sale price integrity — R-SEC-1 (FASE E-SEC)
         (route.ts + cart.ts + usePOSCheckout.ts + useSalesCatalog.ts + migración 20260926000001)
9fe62254 test(pos): cover editable price authorization — R-SEC-1 (FASE E-SEC)   (18 tests)
6022ae85 docs(audit): certify E-SEC evidence — 14 documentos + 13 capturas
Push: 6ac52feb..6022ae85 main → main (verificado con git ls-remote)
```

## CI (GitHub Actions, GET-only vía API) — SHA 6022ae85
| Job | Resultado | Detalle |
|---|---|---|
| TypeCheck + Lint + Unit Tests + Build | **SUCCESS** | gate crítico de compilación y suite unitaria con los cambios E-SEC |
| Security Audit | failure | `8 vulnerabilities (2 critical, 3 high, 3 moderate)` — auditoría ESTÁTICA del lockfile. **0 cambios de dependencias en esta fase** (`git diff 6ac52feb 6022ae85 -- package.json package-lock.json bun.lockb` = vacío). El fallo es drift EXTERNO del advisory DB: nueva advisory `sharp`/libheif <0.35.4 (GHSA-rgj7-g3m4-5g8c, GHSA-g89c-p67h-r497, GHSA-2jg2-4ch7-h545) publicada después del run del baseline. Deuda preexistente documentada desde C2R (7 vulns; ahora 8 por la advisory nueva). |
| E2E Tests (Playwright) | failure | **171 failed / 97 passed — EXACTAMENTE el mismo resultado del SHA base 6ac52feb** (comparación directa de logs: baseline failed=171 passed=97 vs E-SEC failed=171 passed=97). Suite preexistente tolerada desde FASE C2R/D (continue-on-error). Ninguna de las suites fallidas cubre el flujo del precio/checkout (Academy, FC Components, Legal, Accounts Payable, Reports, PWA…). |
| Test Coverage → Unit & Integration | **SUCCESS** | — |

## Comparación baseline vs E-SEC (misma maquinaria, GET-only)
```text
                                6ac52feb (FASE D)     6022ae85 (E-SEC)
TypeCheck+Lint+Unit+Build       success               success
Security Audit                  success               failure  ← advisory NUEVA upstream (sin cambios de deps)
E2E                             failure (171/97)      failure (171/97)  ← IDÉNTICO → 0 regresión E-SEC
```

## Interpretación
- El gate determinante de la fase (compilación + typecheck + lint + 2254 tests
  unitarios/integración, incluidos los 18 nuevos) pasa en CI.
- La regression E2E atribuible a E-SEC es **cero** (mismo número exacto de fallos
  preexistentes que en el SHA base, mismas suites, ninguna del alcance del precio).
- El fallo de Security Audit corresponde a vulnerabilidades de dependencias
  preexistentes y a una advisory publicada aguas arriba entre el run del baseline y
  el de esta fase; su tratamiento (bump de sharp/libheif y remediación de las 2
  criticals históricas) es la recomendación R-DEPS-1 ya registrada desde C2R y queda
  como blocker abierto FUERA del alcance de esta fase (§14: sin refactors oportunistas).
