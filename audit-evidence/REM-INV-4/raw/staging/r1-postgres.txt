2026-09-13 06:15:29.516 UTC [17654] LOG:  starting PostgreSQL 17.6 on x86_64-pc-linux-gnu, compiled by gcc (Ubuntu 7.5.0-3ubuntu1~18.04) 7.5.0, 64-bit
2026-09-13 06:15:29.516 UTC [17654] LOG:  listening on IPv4 address "127.0.0.1", port 55432
2026-09-13 06:15:29.516 UTC [17654] LOG:  listening on Unix socket "/tmp/.s.PGSQL.55432"
2026-09-13 06:15:29.521 UTC [17659] LOG:  database system was shut down at 2026-09-13 06:15:29 UTC
2026-09-13 06:15:29.522 UTC [17654] LOG:  database system is ready to accept connections
2026-09-13 06:15:41.160 UTC [17654] LOG:  received fast shutdown request
2026-09-13 06:15:41.160 UTC [17654] LOG:  aborting any active transactions
2026-09-13 06:15:41.162 UTC [17654] LOG:  background worker "logical replication launcher" (PID 17662) exited with exit code 1
2026-09-13 06:15:41.162 UTC [17657] LOG:  shutting down
2026-09-13 06:15:41.162 UTC [17657] LOG:  checkpoint starting: shutdown immediate
2026-09-13 06:15:41.163 UTC [17657] LOG:  checkpoint complete: wrote 3 buffers (0.0%); 0 WAL file(s) added, 0 removed, 0 recycled; write=0.001 s, sync=0.001 s, total=0.001 s; sync files=2, longest=0.001 s, average=0.001 s; distance=0 kB, estimate=0 kB; lsn=0/14D8A58, redo lsn=0/14D8A58
2026-09-13 06:15:41.165 UTC [17654] LOG:  database system is shut down
2026-09-13 06:16:20.665 UTC [20282] LOG:  starting PostgreSQL 17.6 on x86_64-pc-linux-gnu, compiled by gcc (Ubuntu 7.5.0-3ubuntu1~18.04) 7.5.0, 64-bit
2026-09-13 06:16:20.665 UTC [20282] LOG:  listening on IPv4 address "127.0.0.1", port 55432
2026-09-13 06:16:20.665 UTC [20282] LOG:  listening on Unix socket "/tmp/.s.PGSQL.55432"
2026-09-13 06:16:20.667 UTC [20285] LOG:  database system was shut down at 2026-09-13 06:16:20 UTC
2026-09-13 06:16:20.668 UTC [20282] LOG:  database system is ready to accept connections
2026-09-13 06:16:24.199 UTC [20299] ERROR:  extension "pgjwt" is not available
2026-09-13 06:16:24.199 UTC [20299] DETAIL:  Could not open extension control file "/tmp/my-project/harness/pg/share/postgresql/extension/pgjwt.control": No such file or directory.
2026-09-13 06:16:24.199 UTC [20299] HINT:  The extension must first be installed on the system where PostgreSQL is running.
2026-09-13 06:16:24.199 UTC [20299] STATEMENT:  CREATE EXTENSION IF NOT EXISTS pgjwt;
2026-09-13 06:16:24.852 UTC [20376] ERROR:  type "public.user_role" does not exist at character 301
2026-09-13 06:16:24.852 UTC [20376] STATEMENT:  CREATE OR REPLACE FUNCTION public.bulk_assign_memberships(
	  p_user_id uuid,
	  p_assignments jsonb
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  v_assignment jsonb;
	  v_affected int := 0;
	  v_failed int := 0;
	  v_store_id uuid;
	  v_role public.user_role;
	  v_status text;
	  v_caller_uid uuid := auth.uid();
	  v_changes jsonb := '[]'::jsonb;
	BEGIN
	  IF v_caller_uid IS NULL THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED';
	  END IF;
	
	  FOR v_assignment IN SELECT * FROM jsonb_array_elements(p_assignments) LOOP
	    BEGIN
	      v_store_id := (v_assignment->>'store_id')::uuid;
	      v_role := (v_assignment->>'role')::public.user_role;
	      v_status := COALESCE(v_assignment->>'status', 'active');
	
	      -- Validar caller tiene acceso al store
	      IF NOT public.is_admin() AND NOT public.has_store_role(v_store_id, ARRAY['admin', 'manager']) THEN
	        v_failed := v_failed + 1;
	        CONTINUE;
	      END IF;
	
	      INSERT INTO public.user_store_memberships (user_id, store_id, role, status)
	      VALUES (p_user_id, v_store_id, v_role, v_status)
	      ON CONFLICT (user_id, store_id) DO UPDATE SET
	        role = EXCLUDED.role,
	        status = EXCLUDED.status,
	        updated_at = now();
	
	      v_changes := v_changes || jsonb_build_object(jsonb_build_object(
	        'store_id', v_store_id,
	        'role', v_role::text,
	        'status', v_status
	      ));
	
	      v_affected := v_affected + 1;
	    EXCEPTION
	      WHEN foreign_key_violation THEN
	        v_failed := v_failed + 1;
	    END;
	  END LOOP;
	
	  -- Audit log atómico (solo si hubo cambios)
	  IF v_affected > 0 THEN
	    INSERT INTO public.user_audit_log (performed_by, target_user_id, action, new_values, metadata)
	    VALUES (
	      v_caller_uid, p_user_id, 'MEMBERSHIPS_BULK_ASSIGNED',
	      jsonb_build_object('assignments', v_changes),
	      jsonb_build_object('affected', v_affected, 'failed', v_failed)
	    );
	  END IF;
	
	  RETURN jsonb_build_object(
	    'success', true,
	    'affected', v_affected,
	    'failed', v_failed,
	    'user_id', p_user_id
	  );
	END;
	$function$
2026-09-13 06:16:24.869 UTC [20379] ERROR:  syntax error at or near "REVOKE" at character 5039
2026-09-13 06:16:24.869 UTC [20379] STATEMENT:  CREATE OR REPLACE FUNCTION public.bulk_soft_delete_stores(p_store_ids uuid[], p_deleted_by uuid, p_confirmation_token text, p_override_token text DEFAULT NULL::text, p_reason text DEFAULT NULL::text)
	 RETURNS jsonb
	 LANGUAGE plpgsql
	 SECURITY DEFINER
	 SET search_path TO 'public'
	AS $function$
	DECLARE
	  v_store_id UUID;
	  v_validation JSONB;
	  v_blockers JSONB;
	  v_errors JSONB[] := '{}'::jsonb[];
	  v_processed INTEGER := 0;
	  v_token_valid BOOLEAN;
	  v_has_protected BOOLEAN;
	  v_override_valid BOOLEAN;
	  v_confirmation_record RECORD;
	  v_override_record RECORD;
	  v_caller_role TEXT;
	BEGIN
	  -- ============================================================
	  -- AUTH CHECK: Solo admin puede ejecutar esta función
	  -- ============================================================
	  IF auth.uid() IS NOT NULL THEN
	    SELECT role INTO v_caller_role FROM public.profiles WHERE id = auth.uid();
	    IF v_caller_role IS NULL OR v_caller_role != 'admin' THEN
	      RAISE EXCEPTION 'ERR_PERMISSION_DENIED: Solo admin puede ejecutar bulk_soft_delete_stores';
	    END IF;
	  END IF;
	  -- Si auth.uid() IS NULL, es service_role — permitir
	
	  -- ============================================================
	  -- 1. VALIDATE confirmation_token
	  -- ============================================================
	  SELECT * INTO v_confirmation_record
	  FROM public.bulk_confirmation_tokens
	  WHERE token = p_confirmation_token
	    AND action = 'delete'
	    AND expires_at > NOW()
	    AND consumed_at IS NULL
	    AND is_override = false;
	
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_INVALID_CONFIRMATION_TOKEN';
	  END IF;
	
	  IF v_confirmation_record.store_ids != p_store_ids THEN
	    RAISE EXCEPTION 'ERR_STORE_IDS_MISMATCH';
	  END IF;
	
	  -- ============================================================
	  -- 2. VALIDATE tiendas protegidas requieren override_token
	  -- ============================================================
	  SELECT EXISTS(
	    SELECT 1 FROM public.stores
	    WHERE id = ANY(p_store_ids) AND backup_restore_protected = true
	  ) INTO v_has_protected;
	
	  IF v_has_protected THEN
	    IF p_override_token IS NULL THEN
	      RAISE EXCEPTION 'ERR_OVERRIDE_REQUIRED';
	    END IF;
	
	    SELECT * INTO v_override_record
	    FROM public.bulk_confirmation_tokens
	    WHERE token = p_override_token
	      AND is_override = true
	      AND override_for = p_confirmation_token
	      AND expires_at > NOW()
	      AND consumed_at IS NULL;
	
	    IF NOT FOUND THEN
	      RAISE EXCEPTION 'ERR_INVALID_OVERRIDE_TOKEN';
	    END IF;
	
	    IF v_override_record.store_ids != v_confirmation_record.store_ids THEN
	      RAISE EXCEPTION 'ERR_OVERRIDE_STORE_IDS_MISMATCH';
	    END IF;
	
	    IF v_override_record.created_by = v_confirmation_record.created_by THEN
	      RAISE EXCEPTION 'ERR_SAME_USER_OVERRIDE';
	    END IF;
	
	    UPDATE public.bulk_confirmation_tokens SET consumed_at = NOW()
	    WHERE token = p_override_token;
	  END IF;
	
	  UPDATE public.bulk_confirmation_tokens SET consumed_at = NOW()
	  WHERE token = p_confirmation_token;
	
	  -- ============================================================
	  -- 3. VALIDATE todas las tiendas
	  -- ============================================================
	  FOREACH v_store_id IN ARRAY p_store_ids LOOP
	    IF NOT EXISTS(SELECT 1 FROM public.stores WHERE id = v_store_id AND is_active = true) THEN
	      v_errors := array_append(v_errors, jsonb_build_object(
	        'store_id', v_store_id, 'reason', 'STORE_NOT_FOUND_OR_INACTIVE'
	      ));
	      CONTINUE;
	    END IF;
	
	    SELECT public.validate_store_can_be_modified(v_store_id, 'soft_delete') INTO v_validation;
	    v_blockers := v_validation->'blockers';
	
	    IF v_validation->>'can_modify' != 'true' THEN
	      v_errors := array_append(v_errors, jsonb_build_object(
	        'store_id', v_store_id, 'reason', 'HAS_BLOCKING_DEPENDENCIES', 'blockers', v_blockers
	      ));
	    END IF;
	  END LOOP;
	
	  IF array_length(v_errors, 1) IS NOT NULL THEN
	    RETURN jsonb_build_object(
	      'status', 'FAILED', 'processed', 0,
	      'total_requested', array_length(p_store_ids, 1),
	      'errors', to_jsonb(v_errors), 'reason', p_reason
	    );
	  END IF;
	
	  -- ============================================================
	  -- 4. EXECUTE
	  -- ============================================================
	  FOREACH v_store_id IN ARRAY p_store_ids LOOP
	    PERFORM public.soft_delete_store(v_store_id, p_deleted_by);
	    v_processed := v_processed + 1;
	  END LOOP;
	
	  INSERT INTO public.audit_logs (action, table_name, record_id, metadata)
	  VALUES (
	    'bulk_store_deleted', 'stores', NULL,
	    jsonb_build_object(
	      'store_ids', p_store_ids, 'deleted_by', p_deleted_by,
	      'reason', p_reason, 'processed', v_processed,
	      'had_protected_stores', v_has_protected,
	      'override_used', p_override_token IS NOT NULL, 'deleted_at', NOW()
	    )
	  );
	
	  RETURN jsonb_build_object(
	    'status', 'COMPLETED', 'processed', v_processed,
	    'total_requested', array_length(p_store_ids, 1),
	    'errors', '[]'::jsonb, 'reason', p_reason
	  );
	END;
	$function$
	 
	REVOKE EXECUTE ON FUNCTION public.bulk_soft_delete_stores FROM PUBLIC, anon, authenticated
2026-09-13 06:16:25.008 UTC [20398] ERROR:  relation "received_services" does not exist
2026-09-13 06:16:25.008 UTC [20398] CONTEXT:  compilation of PL/pgSQL function "calculate_service_distribution" near line 3
2026-09-13 06:16:25.008 UTC [20398] STATEMENT:  CREATE OR REPLACE FUNCTION calculate_service_distribution(
	  p_service_id UUID
	) RETURNS TABLE(
	  receipt_item_id UUID,
	  product_id UUID,
	  distribution_amount NUMERIC,
	  distribution_percentage NUMERIC
	) LANGUAGE plpgsql SECURITY DEFINER AS $$
	DECLARE
	  v_service received_services%ROWTYPE;
	  v_method TEXT;
	  v_total_amount NUMERIC;
	  v_total_value NUMERIC DEFAULT 0;
	  v_total_qty NUMERIC DEFAULT 0;
	  v_link service_reception_links%ROWTYPE;
	  v_receipt_id UUID;
	  v_item receipt_items%ROWTYPE;
	  v_allocated NUMERIC;
	BEGIN
	  SELECT * INTO v_service FROM received_services WHERE id = p_service_id;
	  IF NOT FOUND THEN RETURN; END IF;
	  
	  v_method := v_service.distribution_method;
	  v_total_amount := v_service.total_amount;
	
	  -- Para cada recepción vinculada
	  FOR v_link IN SELECT * FROM service_reception_links WHERE service_id = p_service_id AND allocated_amount > 0 LOOP
	    v_receipt_id := v_link.receipt_id;
	    v_allocated := v_link.allocated_amount;
	
	    -- Calcular base de distribución según método
	    IF v_method = 'amount' THEN
	      SELECT COALESCE(SUM(quantity * unit_cost), 0) INTO v_total_value
	      FROM receipt_items WHERE receipt_id = v_receipt_id;
	      
	      IF v_total_value > 0 THEN
	        FOR v_item IN SELECT * FROM receipt_items WHERE receipt_id = v_receipt_id LOOP
	          distribution_amount := v_allocated * (v_item.quantity * v_item.unit_cost / v_total_value);
	          distribution_percentage := (v_item.quantity * v_item.unit_cost / v_total_value) * 100;
	          receipt_item_id := v_item.id;
	          product_id := v_item.product_id;
	          RETURN NEXT;
	        END LOOP;
	      END IF;
	
	    ELSIF v_method = 'quantity' THEN
	      SELECT COALESCE(SUM(quantity), 0) INTO v_total_qty
	      FROM receipt_items WHERE receipt_id = v_receipt_id;
	      
	      IF v_total_qty > 0 THEN
	        FOR v_item IN SELECT * FROM receipt_items WHERE receipt_id = v_receipt_id LOOP
	          distribution_amount := v_allocated * (v_item.quantity / v_total_qty);
	          distribution_percentage := (v_item.quantity / v_total_qty) * 100;
	          receipt_item_id := v_item.id;
	          product_id := v_item.product_id;
	          RETURN NEXT;
	        END LOOP;
	      END IF;
	
	    ELSIF v_method = 'manual' THEN
	      -- En manual, la distribución se guarda directamente desde el cliente
	      RETURN;
	    END IF;
	  END LOOP;
	END;
	$$
2026-09-13 06:16:25.219 UTC [20433] ERROR:  syntax error at or near "REVOKE" at character 678
2026-09-13 06:16:25.219 UTC [20433] STATEMENT:  CREATE OR REPLACE FUNCTION public.check_bulk_ops_hourly_limit(p_user_id uuid, p_plan text)
	 RETURNS jsonb
	 LANGUAGE plpgsql
	 STABLE SECURITY DEFINER
	 SET search_path TO 'public'
	AS $function$
	DECLARE
	  v_limit INTEGER;
	  v_used INTEGER;
	BEGIN
	  v_limit := CASE p_plan
	    WHEN 'free' THEN 1
	    WHEN 'pro' THEN 20
	    WHEN 'enterprise' THEN 999999
	    ELSE 1
	  END;
	
	  SELECT COUNT(*) INTO v_used
	  FROM public.bulk_ops_log
	  WHERE user_id = p_user_id
	    AND initiated_at > NOW() - INTERVAL '1 hour';
	
	  RETURN jsonb_build_object(
	    'allowed', v_used < v_limit,
	    'used', v_used,
	    'limit', v_limit,
	    'remaining', GREATEST(0, v_limit - v_used)
	  );
	END;
	$function$
	 
	REVOKE EXECUTE ON FUNCTION public.check_bulk_ops_hourly_limit FROM PUBLIC, anon, authenticated
2026-09-13 06:16:25.285 UTC [20444] ERROR:  type plan_t does not exist
2026-09-13 06:16:25.285 UTC [20444] STATEMENT:  CREATE OR REPLACE FUNCTION public.check_tenant_store_quota(
	  p_tenant_id uuid,
	  p_plan plan_t DEFAULT NULL
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  v_plan plan_t;
	  v_limit int;
	  v_current int;
	  v_allowed boolean;
	BEGIN
	  -- Get plan from tenant if not passed
	  IF p_plan IS NULL THEN
	    SELECT plan INTO v_plan FROM public.tenants WHERE id = p_tenant_id;
	  ELSE
	    v_plan := p_plan;
	  END IF;
	
	  -- Plan limits (unified source of truth)
	  v_limit := CASE v_plan
	    WHEN 'free'::plan_t THEN 1
	    WHEN 'pro'::plan_t THEN 3
	    WHEN 'enterprise'::plan_t THEN 10
	    ELSE 1
	  END;
	
	  -- Count active stores in tenant
	  SELECT COUNT(*) INTO v_current
	    FROM public.stores
	    WHERE tenant_id = p_tenant_id AND is_active = true;
	
	  v_allowed := v_current < v_limit;
	
	  RETURN jsonb_build_object(
	    'allowed', v_allowed,
	    'current', v_current,
	    'limit', v_limit,
	    'plan', v_plan::text,
	    'tenant_id', p_tenant_id
	  );
	END;
	$function$
2026-09-13 06:16:25.942 UTC [20543] ERROR:  relation "public.profiles" does not exist at character 166
2026-09-13 06:16:25.942 UTC [20543] STATEMENT:  CREATE OR REPLACE FUNCTION public.current_user_tenant_id()
	RETURNS uuid
	LANGUAGE sql
	STABLE
	SECURITY DEFINER
	SET search_path TO public
	AS $$
	  SELECT tenant_id FROM public.profiles WHERE id = auth.uid();
	$$
2026-09-13 06:16:26.075 UTC [20558] ERROR:  relation "received_services" does not exist
2026-09-13 06:16:26.075 UTC [20558] CONTEXT:  compilation of PL/pgSQL function "distribute_service_cost_v2" near line 6
2026-09-13 06:16:26.075 UTC [20558] STATEMENT:  CREATE OR REPLACE FUNCTION public.distribute_service_cost_v2(
	  p_service_id uuid,
	  p_user_id uuid DEFAULT NULL
	) RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO 'public', 'extensions'
	AS $func$
	DECLARE
	  v_store_id uuid;
	  v_status text;
	  v_total_amount numeric;
	  v_service received_services%ROWTYPE;
	  v_method text;
	  v_link service_reception_links%ROWTYPE;
	  v_item receipt_items%ROWTYPE;
	  v_total_value numeric := 0;
	  v_total_qty numeric := 0;
	  v_allocated numeric;
	  v_dist_count integer := 0;
	  v_caller_uid uuid := COALESCE(p_user_id, auth.uid());
	BEGIN
	  PERFORM pg_advisory_xact_lock(hashtext(p_service_id::text));
	
	  SELECT * INTO v_service FROM received_services WHERE id = p_service_id FOR UPDATE;
	  IF NOT FOUND THEN RAISE EXCEPTION 'ERR_SERVICE_NOT_FOUND'; END IF;
	
	  v_store_id := v_service.store_id;
	  v_status := v_service.status;
	  v_total_amount := v_service.total_amount;
	  v_method := v_service.distribution_method;
	
	  IF NOT public.has_store_access(v_store_id) THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED';
	  END IF;
	
	  IF v_status != 'active' THEN
	    RAISE EXCEPTION 'ERR_SERVICE_NOT_ACTIVE: status % is not active', v_status;
	  END IF;
	
	  IF v_method = 'manual' THEN
	    RAISE EXCEPTION 'ERR_MANUAL_METHOD: use link_receipts_to_service for manual distribution';
	  END IF;
	
	  DELETE FROM service_cost_distributions WHERE service_id = p_service_id;
	
	  FOR v_link IN SELECT * FROM service_reception_links WHERE service_id = p_service_id AND allocated_amount > 0 ORDER BY receipt_id LOOP
	    v_allocated := v_link.allocated_amount;
	
	    IF v_method = 'amount' THEN
	      SELECT COALESCE(SUM(quantity * unit_cost), 0) INTO v_total_value
	      FROM receipt_items WHERE receipt_id = v_link.receipt_id;
	      IF v_total_value > 0 THEN
	        FOR v_item IN SELECT * FROM receipt_items WHERE receipt_id = v_link.receipt_id ORDER BY id LOOP
	          INSERT INTO service_cost_distributions
	            (service_id, receipt_id, receipt_item_id, product_id, distribution_amount, distribution_percentage)
	          VALUES
	            (p_service_id, v_link.receipt_id, v_item.id, v_item.product_id,
	             v_allocated * (v_item.quantity * v_item.unit_cost / v_total_value),
	             (v_item.quantity * v_item.unit_cost / v_total_value) * 100);
	          v_dist_count := v_dist_count + 1;
	        END LOOP;
	      END IF;
	
	    ELSIF v_method = 'quantity' THEN
	      SELECT COALESCE(SUM(quantity), 0) INTO v_total_qty
	      FROM receipt_items WHERE receipt_id = v_link.receipt_id;
	      IF v_total_qty > 0 THEN
	        FOR v_item IN SELECT * FROM receipt_items WHERE receipt_id = v_link.receipt_id ORDER BY id LOOP
	          INSERT INTO service_cost_distributions
	            (service_id, receipt_id, receipt_item_id, product_id, distribution_amount, distribution_percentage)
	          VALUES
	            (p_service_id, v_link.receipt_id, v_item.id, v_item.product_id,
	             v_allocated * (v_item.quantity / v_total_qty),
	             (v_item.quantity / v_total_qty) * 100);
	          v_dist_count := v_dist_count + 1;
	        END LOOP;
	      END IF;
	    END IF;
	  END LOOP;
	
	  -- Forzar recalculo de WAC para cada producto afectado
	  UPDATE receipt_items SET updated_at = NOW()
	  WHERE product_id IN (SELECT DISTINCT product_id FROM service_cost_distributions WHERE service_id = p_service_id);
	
	  INSERT INTO audit_logs (user_id, store_id, action, table_name, record_id, metadata)
	  VALUES (v_caller_uid, v_store_id, 'SERVICE_DISTRIBUTED', 'received_services', p_service_id,
	    jsonb_build_object('rows_distributed', v_dist_count, 'method', v_method, 'total_amount', v_total_amount));
	
	  RETURN jsonb_build_object('status', 'success', 'distributed_rows', v_dist_count);
	END;
	$func$
2026-09-13 06:16:26.150 UTC [20569] ERROR:  type "user_role" does not exist at character 116
2026-09-13 06:16:26.150 UTC [20569] STATEMENT:  CREATE OR REPLACE FUNCTION public.enforce_encargado_store_limit()
	RETURNS TRIGGER AS $$
	DECLARE
	    v_creator_role user_role;
	    v_limit integer;
	    v_current_count integer;
	BEGIN
	    IF NEW.created_by IS NULL THEN RETURN NEW; END IF;
	
	    SELECT role, max_stores_limit INTO v_creator_role, v_limit
	    FROM public.profiles WHERE id = NEW.created_by;
	
	    IF v_creator_role = 'encargado' THEN
	        SELECT COUNT(*) INTO v_current_count FROM public.stores WHERE created_by = NEW.created_by;
	        IF v_current_count >= v_limit THEN
	            RAISE EXCEPTION 'ERR_STORE_LIMIT_EXCEEDED: Maximum number of stores reached for this manager (%/%)', v_current_count, v_limit;
	        END IF;
	    END IF;
	    RETURN NEW;
	END;
	$$ LANGUAGE plpgsql
2026-09-13 06:16:26.178 UTC [20572] ERROR:  type "user_role" does not exist at character 115
2026-09-13 06:16:26.178 UTC [20572] STATEMENT:  CREATE OR REPLACE FUNCTION public.enforce_encargado_user_limit()
	RETURNS TRIGGER AS $$
	DECLARE
	    v_creator_role user_role;
	    v_limit integer;
	    v_current_count integer;
	BEGIN
	    IF NEW.created_by IS NULL THEN RETURN NEW; END IF;
	
	    SELECT role, max_users_limit INTO v_creator_role, v_limit
	    FROM public.profiles WHERE id = NEW.created_by;
	
	    IF v_creator_role = 'encargado' THEN
	        SELECT COUNT(*) INTO v_current_count FROM public.profiles WHERE created_by = NEW.created_by;
	        IF v_current_count >= v_limit THEN
	            RAISE EXCEPTION 'ERR_USER_LIMIT_EXCEEDED: Maximum number of users reached for this manager (%/%)', v_current_count, v_limit;
	        END IF;
	    END IF;
	    RETURN NEW;
	END;
	$$ LANGUAGE plpgsql
2026-09-13 06:16:26.451 UTC [20615] ERROR:  syntax error at or near "REVOKE" at character 1436
2026-09-13 06:16:26.451 UTC [20615] STATEMENT:  CREATE OR REPLACE FUNCTION public.generate_bulk_confirmation_token(p_store_ids uuid[], p_action text, p_user_id uuid)
	 RETURNS text
	 LANGUAGE plpgsql
	 SECURITY DEFINER
	 SET search_path TO 'public'
	AS $function$
	DECLARE
	  v_token TEXT;
	  v_has_protected BOOLEAN;
	  v_caller_role TEXT;
	BEGIN
	  -- AUTH CHECK: Solo admin
	  IF auth.uid() IS NOT NULL THEN
	    SELECT role INTO v_caller_role FROM public.profiles WHERE id = auth.uid();
	    IF v_caller_role IS NULL OR v_caller_role != 'admin' THEN
	      RAISE EXCEPTION 'ERR_PERMISSION_DENIED: Solo admin puede generar tokens bulk';
	    END IF;
	    -- Verificar que p_user_id coincide con auth.uid()
	    IF p_user_id != auth.uid() THEN
	      RAISE EXCEPTION 'ERR_PERMISSION_DENIED: p_user_id debe coincidir con el usuario autenticado';
	    END IF;
	  END IF;
	
	  IF p_action NOT IN ('delete', 'archive') THEN
	    RAISE EXCEPTION 'ERR_NON_DESTRUCTIVE_ACTION';
	  END IF;
	
	  SELECT EXISTS(
	    SELECT 1 FROM public.stores
	    WHERE id = ANY(p_store_ids) AND backup_restore_protected = true
	  ) INTO v_has_protected;
	
	  v_token := 'bct_' || replace(gen_random_uuid()::text, '-', '');
	
	  INSERT INTO public.bulk_confirmation_tokens (
	    token, store_ids, action, created_by, expires_at, metadata
	  ) VALUES (
	    v_token, p_store_ids, p_action, p_user_id,
	    NOW() + INTERVAL '10 minutes',
	    jsonb_build_object('has_protected_stores', v_has_protected)
	  );
	
	  RETURN v_token;
	END;
	$function$
	 
	REVOKE EXECUTE ON FUNCTION public.generate_bulk_confirmation_token FROM PUBLIC, anon, authenticated
2026-09-13 06:16:26.469 UTC [20618] ERROR:  syntax error at or near "REVOKE" at character 2432
2026-09-13 06:16:26.469 UTC [20618] STATEMENT:  CREATE OR REPLACE FUNCTION public.generate_bulk_override_token(p_confirmation_token text, p_override_user_id uuid, p_reason text DEFAULT NULL::text)
	 RETURNS text
	 LANGUAGE plpgsql
	 SECURITY DEFINER
	 SET search_path TO 'public'
	AS $function$
	DECLARE
	  v_original RECORD;
	  v_override_token TEXT;
	  v_override_user_role TEXT;
	BEGIN
	  -- AUTH CHECK: Solo admin, y debe ser auth.uid() == p_override_user_id
	  IF auth.uid() IS NOT NULL THEN
	    IF auth.uid() != p_override_user_id THEN
	      RAISE EXCEPTION 'ERR_PERMISSION_DENIED: p_override_user_id debe coincidir con el usuario autenticado';
	    END IF;
	
	    SELECT role INTO v_override_user_role FROM public.profiles WHERE id = p_override_user_id;
	    IF v_override_user_role IS NULL OR v_override_user_role != 'admin' THEN
	      RAISE EXCEPTION 'ERR_PERMISSION_DENIED: Solo admin puede generar override';
	    END IF;
	  END IF;
	
	  SELECT * INTO v_original
	  FROM public.bulk_confirmation_tokens
	  WHERE token = p_confirmation_token
	    AND consumed_at IS NULL
	    AND expires_at > NOW()
	    AND is_override = false;
	
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_INVALID_OR_EXPIRED_TOKEN';
	  END IF;
	
	  IF v_original.created_by = p_override_user_id THEN
	    RAISE EXCEPTION 'ERR_SAME_USER_OVERRIDE';
	  END IF;
	
	  IF EXISTS(
	    SELECT 1 FROM public.bulk_confirmation_tokens
	    WHERE override_for = p_confirmation_token
	      AND consumed_at IS NULL AND expires_at > NOW()
	  ) THEN
	    RAISE EXCEPTION 'ERR_OVERRIDE_ALREADY_EXISTS';
	  END IF;
	
	  v_override_token := 'bot_' || replace(gen_random_uuid()::text, '-', '');
	
	  INSERT INTO public.bulk_confirmation_tokens (
	    token, store_ids, action, created_by, expires_at,
	    is_override, override_for, metadata
	  ) VALUES (
	    v_override_token, v_original.store_ids, v_original.action, p_override_user_id,
	    NOW() + INTERVAL '10 minutes', true, p_confirmation_token,
	    jsonb_build_object('override_reason', p_reason,
	      'original_created_by', v_original.created_by,
	      'override_created_by', p_override_user_id)
	  );
	
	  INSERT INTO public.audit_logs (action, table_name, record_id, metadata)
	  VALUES (
	    'bulk_override_token_generated', 'stores', NULL,
	    jsonb_build_object('confirmation_token', p_confirmation_token,
	      'store_ids', v_original.store_ids,
	      'override_by', p_override_user_id,
	      'original_by', v_original.created_by, 'reason', p_reason)
	  );
	
	  RETURN v_override_token;
	END;
	$function$
	 
	REVOKE EXECUTE ON FUNCTION public.generate_bulk_override_token FROM PUBLIC, anon, authenticated
2026-09-13 06:16:26.616 UTC [20641] ERROR:  relation "public.backup_table_registry" does not exist at character 581
2026-09-13 06:16:26.616 UTC [20641] STATEMENT:  CREATE OR REPLACE FUNCTION public.get_backup_table_list(
	  p_include_excluded BOOLEAN DEFAULT FALSE
	)
	RETURNS TABLE (
	  table_name          TEXT,
	  tier                INTEGER,
	  filter_strategy     TEXT,
	  parent_table        TEXT,
	  parent_foreign_key  TEXT,
	  date_column         TEXT,
	  excluded_from_restore BOOLEAN,
	  exclude_reason      TEXT
	)
	LANGUAGE sql
	SECURITY DEFINER
	SET search_path = public
	AS $$
	  SELECT
	    table_name,
	    tier,
	    filter_strategy,
	    parent_table,
	    parent_foreign_key,
	    date_column,
	    excluded_from_restore,
	    exclude_reason
	  FROM public.backup_table_registry
	  WHERE (p_include_excluded OR excluded_from_restore = FALSE)
	  ORDER BY tier ASC, table_name ASC;
	$$
2026-09-13 06:16:26.770 UTC [20664] ERROR:  relation "v_global_operation_dates" does not exist at character 229
2026-09-13 06:16:26.770 UTC [20664] STATEMENT:  CREATE OR REPLACE FUNCTION public.get_global_max_operation_date(
	  p_store_id UUID DEFAULT NULL
	)
	RETURNS TIMESTAMP WITH TIME ZONE
	LANGUAGE sql
	SECURITY DEFINER
	SET search_path = public
	AS $$
	  SELECT MAX(operation_date)
	  FROM v_global_operation_dates
	  WHERE p_store_id IS NULL OR store_id = p_store_id;
	$$
2026-09-13 06:16:26.789 UTC [20667] ERROR:  relation "exchange_rates" does not exist at character 216
2026-09-13 06:16:26.789 UTC [20667] STATEMENT:  CREATE OR REPLACE FUNCTION get_latest_rate(p_currency TEXT, p_source TEXT)
	RETURNS TABLE(rate NUMERIC, rate_date DATE, captured_at TIMESTAMPTZ)
	LANGUAGE sql STABLE AS $$
	  SELECT rate, rate_date, captured_at
	  FROM exchange_rates
	  WHERE currency = p_currency AND source = p_source
	  ORDER BY rate_date DESC, captured_at DESC
	  LIMIT 1;
	$$
2026-09-13 06:16:26.809 UTC [20670] ERROR:  relation "public.products" does not exist at character 194
2026-09-13 06:16:26.809 UTC [20670] STATEMENT:  CREATE OR REPLACE FUNCTION public.get_low_stock_count(p_store_id uuid DEFAULT NULL)
	RETURNS bigint
	LANGUAGE sql
	SECURITY DEFINER
	SET search_path = public
	AS $$
	  SELECT COUNT(*)::bigint
	  FROM public.products
	  WHERE
	    (p_store_id IS NULL OR store_id = p_store_id)
	    AND is_active = true
	    AND stock_current > 0
	    AND min_stock IS NOT NULL
	    AND min_stock > 0
	    AND stock_current <= min_stock;
	$$
2026-09-13 06:16:26.954 UTC [20689] ERROR:  relation "receipt_items" does not exist at character 754
2026-09-13 06:16:26.954 UTC [20689] STATEMENT:  CREATE OR REPLACE FUNCTION get_product_cost_analysis(
	  p_product_id UUID,
	  p_store_id UUID
	) RETURNS TABLE(
	  receipt_id UUID,
	  receipt_date TIMESTAMPTZ,
	  quantity INT,
	  unit_cost FLOAT,
	  service_type TEXT,
	  service_amount NUMERIC,
	  total_cost NUMERIC,
	  unit_cost_final NUMERIC
	) LANGUAGE sql SECURITY DEFINER AS $$
	  SELECT 
	    ri.receipt_id,
	    r.reception_date,
	    ri.quantity,
	    ri.unit_cost,
	    st.name AS service_type,
	    COALESCE(scd.distribution_amount, 0) AS service_amount,
	    ri.unit_cost * ri.quantity + COALESCE(scd.distribution_amount, 0) AS total_cost,
	    CASE WHEN ri.quantity > 0 
	      THEN (ri.unit_cost * ri.quantity + COALESCE(scd.distribution_amount, 0)) / ri.quantity 
	      ELSE 0 END AS unit_cost_final
	  FROM receipt_items ri
	  JOIN receipts r ON r.id = ri.receipt_id
	  LEFT JOIN service_cost_distributions scd ON scd.receipt_item_id = ri.id
	  LEFT JOIN received_services rs ON rs.id = scd.service_id AND rs.status = 'active'
	  LEFT JOIN service_types st ON st.id = rs.service_type_id
	  WHERE ri.product_id = p_product_id
	    AND r.store_id = p_store_id
	    AND r.status = 'active'
	  ORDER BY r.reception_date DESC;
	$$
2026-09-13 06:16:27.047 UTC [20704] ERROR:  relation "exchange_rates" does not exist at character 252
2026-09-13 06:16:27.047 UTC [20704] STATEMENT:  CREATE OR REPLACE FUNCTION get_rate_history(
	  p_currency TEXT,
	  p_source TEXT,
	  p_days INT DEFAULT 30
	)
	RETURNS TABLE(rate_date DATE, rate NUMERIC, variation_daily NUMERIC)
	LANGUAGE sql STABLE AS $$
	  SELECT rate_date, rate, variation_daily
	  FROM exchange_rates
	  WHERE currency = p_currency AND source = p_source
	    AND rate_date >= CURRENT_DATE - p_days
	  ORDER BY rate_date ASC;
	$$
2026-09-13 06:16:27.065 UTC [20707] ERROR:  relation "exchange_rates" does not exist at character 363
2026-09-13 06:16:27.065 UTC [20707] STATEMENT:  CREATE OR REPLACE FUNCTION get_rate_variation(
	  p_currency TEXT,
	  p_source TEXT,
	  p_start_date DATE,
	  p_end_date DATE
	)
	RETURNS TABLE(
	  start_rate NUMERIC,
	  end_rate NUMERIC,
	  absolute_change NUMERIC,
	  percent_change NUMERIC,
	  daily_avg_growth NUMERIC,
	  monthly_avg_growth NUMERIC
	)
	LANGUAGE sql STABLE AS $$
	  WITH start_val AS (
	    SELECT rate FROM exchange_rates
	    WHERE currency = p_currency AND source = p_source AND rate_date <= p_start_date
	    ORDER BY rate_date DESC LIMIT 1
	  ),
	  end_val AS (
	    SELECT rate FROM exchange_rates
	    WHERE currency = p_currency AND source = p_source AND rate_date <= p_end_date
	    ORDER BY rate_date DESC LIMIT 1
	  )
	  SELECT
	    COALESCE(sv.rate, 0) AS start_rate,
	    COALESCE(ev.rate, 0) AS end_rate,
	    (COALESCE(ev.rate, 0) - COALESCE(sv.rate, 0)) AS absolute_change,
	    CASE WHEN sv.rate IS NOT NULL AND sv.rate > 0 THEN ((ev.rate - sv.rate) / sv.rate * 100) ELSE 0 END AS percent_change,
	    CASE WHEN sv.rate IS NOT NULL AND sv.rate > 0 AND ev.rate IS NOT NULL AND ev.rate > 0 AND p_end_date > p_start_date
	      THEN (POWER(ev.rate::FLOAT / sv.rate::FLOAT, 1.0 / EXTRACT(EPOCH FROM (p_end_date - p_start_date)) * 86400) - 1) * 100
	      ELSE 0 END AS daily_avg_growth,
	    CASE WHEN sv.rate IS NOT NULL AND sv.rate > 0 AND ev.rate IS NOT NULL AND ev.rate > 0 AND p_end_date > p_start_date
	      THEN (POWER(ev.rate::FLOAT / sv.rate::FLOAT, 30.44 / EXTRACT(EPOCH FROM (p_end_date - p_start_date)) * 86400) - 1) * 100
	      ELSE 0 END AS monthly_avg_growth
	  FROM start_val sv, end_val ev;
	$$
2026-09-13 06:16:27.332 UTC [20750] ERROR:  type "public.stores" does not exist
2026-09-13 06:16:27.332 UTC [20750] STATEMENT:  CREATE OR REPLACE FUNCTION public.get_transferable_stores(
	  p_user_id UUID,
	  p_current_store_id UUID
	)
	RETURNS SETOF public.stores
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO 'public'
	AS $$
	DECLARE
	  v_tenant_id UUID;
	BEGIN
	  -- 1. Obtener tenant_id de la tienda actual
	  SELECT tenant_id INTO v_tenant_id
	  FROM public.stores
	  WHERE id = p_current_store_id AND is_active = true;
	
	  -- 2. Devolver tiendas donde el caller tiene acceso (has_store_access_as)
	  --    EXCLUYENDO la tienda actual
	  --    Si tenant_id es NOT NULL, filtrar por mismo tenant.
	  --    Si tenant_id es NULL (legacy), mostrar todas las que el caller tiene acceso.
	  IF v_tenant_id IS NOT NULL THEN
	    RETURN QUERY
	    SELECT s.*
	    FROM public.stores s
	    WHERE s.tenant_id = v_tenant_id
	      AND s.id != p_current_store_id
	      AND s.is_active = true
	      AND s.is_archived = false
	      AND public.has_store_access_as(p_user_id, s.id)
	    ORDER BY s.name;
	  ELSE
	    -- Legacy: tiendas sin tenant_id — usar solo has_store_access_as
	    RETURN QUERY
	    SELECT s.*
	    FROM public.stores s
	    WHERE s.id != p_current_store_id
	      AND s.is_active = true
	      AND s.is_archived = false
	      AND public.has_store_access_as(p_user_id, s.id)
	    ORDER BY s.name;
	  END IF;
	END;
	$$
2026-09-13 06:16:27.502 UTC [20777] ERROR:  type user_role[] does not exist
2026-09-13 06:16:27.502 UTC [20777] STATEMENT:  CREATE OR REPLACE FUNCTION public.has_any_role(required_roles user_role[])
	 RETURNS boolean
	 LANGUAGE plpgsql
	 STABLE SECURITY DEFINER
	AS $$
	DECLARE
	    v_actual_role user_role;
	    r user_role;
	BEGIN
	    SELECT role INTO v_actual_role FROM public.profiles WHERE id = auth.uid();
	
	    FOREACH r IN ARRAY required_roles
	    LOOP
	        IF v_actual_role = r THEN RETURN true; END IF;
	        IF v_actual_role = 'encargado' AND r = 'manager' THEN RETURN true; END IF;
	        IF v_actual_role = 'usuario' AND (r = 'clerk' OR r = 'warehouse') THEN RETURN true; END IF;
	    END LOOP;
	
	    RETURN false;
	END;
	$$
2026-09-13 06:16:27.522 UTC [20780] ERROR:  relation "profiles" does not exist at character 461
2026-09-13 06:16:27.522 UTC [20780] STATEMENT:  CREATE OR REPLACE FUNCTION public.has_management_access_as(
	  p_user_id uuid,
	  p_store_id uuid
	) RETURNS boolean
	LANGUAGE sql SECURITY DEFINER
	SET search_path = public, pg_temp
	AS $$
	  -- service_role always passes
	  SELECT CASE WHEN auth.role() = 'service_role' THEN true
	    ELSE
	      -- Check if user has admin/manager/encargado role in their profile
	      -- OR has a membership with manager/admin role for the store
	      EXISTS (
	        SELECT 1 FROM profiles
	        WHERE id = p_user_id
	        AND role IN ('admin', 'manager', 'encargado')
	      )
	      OR EXISTS (
	        SELECT 1 FROM user_store_memberships
	        WHERE user_id = p_user_id
	        AND store_id = p_store_id
	        AND status = 'active'
	        AND role IN ('admin', 'manager')
	      )
	    END
	$$
2026-09-13 06:16:27.540 UTC [20783] ERROR:  type public.user_role does not exist
2026-09-13 06:16:27.540 UTC [20783] STATEMENT:  CREATE OR REPLACE FUNCTION public.has_role(p_user_id UUID, p_required_role public.user_role)
	 RETURNS boolean
	 LANGUAGE plpgsql
	 STABLE SECURITY DEFINER
	AS $$
	DECLARE
	    v_actual_role public.user_role;
	BEGIN
	    SELECT role INTO v_actual_role FROM public.profiles WHERE id = p_user_id;
	
	    IF v_actual_role IS NULL THEN
	        RETURN false;
	    END IF;
	
	    -- Compatibility mapping (mirroring the 1-arg version)
	    IF v_actual_role = 'encargado' AND p_required_role = 'manager' THEN RETURN true; END IF;
	    IF v_actual_role = 'usuario' AND (p_required_role = 'clerk' OR p_required_role = 'warehouse') THEN RETURN true; END IF;
	
	    RETURN v_actual_role = p_required_role;
	END;
	$$
2026-09-13 06:16:27.608 UTC [20794] ERROR:  relation "public.user_store_memberships" does not exist at character 279
2026-09-13 06:16:27.608 UTC [20794] STATEMENT:  CREATE OR REPLACE FUNCTION public.has_store_role(
	  p_user_id uuid,
	  p_store_id uuid,
	  p_roles text[]
	) RETURNS boolean
	LANGUAGE sql SECURITY DEFINER
	SET search_path = public, pg_temp
	AS $$
	  SELECT CASE WHEN auth.role() = 'service_role' THEN
	    EXISTS (
	      SELECT 1 FROM public.user_store_memberships m
	      WHERE m.user_id = p_user_id
	        AND m.store_id = p_store_id
	        AND m.status = 'active'
	        AND m.role::text = ANY(p_roles)
	    )
	  ELSE
	    EXISTS (
	      SELECT 1 FROM public.user_store_memberships m
	      WHERE m.user_id = auth.uid()
	        AND m.store_id = p_store_id
	        AND m.status = 'active'
	        AND m.role::text = ANY(p_roles)
	    )
	  END
	$$
2026-09-13 06:16:27.698 UTC [20809] ERROR:  relation "public.profiles" does not exist at character 173
2026-09-13 06:16:27.698 UTC [20809] STATEMENT:  CREATE OR REPLACE FUNCTION public.is_global_admin()
	RETURNS BOOLEAN
	LANGUAGE sql
	STABLE
	SECURITY DEFINER
	SET search_path = public
	AS $$
	  SELECT EXISTS (
	    SELECT 1 FROM public.profiles
	    WHERE id = auth.uid()
	      AND role = 'admin'
	  );
	$$
2026-09-13 06:16:27.741 UTC [20816] ERROR:  type user_role does not exist
2026-09-13 06:16:27.741 UTC [20816] STATEMENT:  CREATE OR REPLACE FUNCTION public.is_role_not_changed(p_user_id uuid, p_new_role user_role, p_new_role_id uuid)
	RETURNS boolean
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path = public, extensions
	AS $$
	DECLARE
	  v_old_role user_role;
	  v_old_role_id uuid;
	BEGIN
	  SELECT role, role_id INTO v_old_role, v_old_role_id
	  FROM public.profiles
	  WHERE id = p_user_id;
	
	  RETURN (p_new_role IS NOT DISTINCT FROM v_old_role)
	     AND (p_new_role_id IS NOT DISTINCT FROM v_old_role_id);
	END;
	$$
2026-09-13 06:16:27.786 UTC [20823] ERROR:  relation "public.user_store_memberships" does not exist at character 188
2026-09-13 06:16:27.786 UTC [20823] STATEMENT:  CREATE OR REPLACE FUNCTION public.is_store_member(p_store_id UUID)
	RETURNS BOOLEAN
	LANGUAGE sql
	STABLE
	SECURITY DEFINER
	SET search_path = public
	AS $$
	  SELECT EXISTS (
	    SELECT 1 FROM public.user_store_memberships
	    WHERE store_id = p_store_id
	      AND user_id = auth.uid()
	      AND status = 'active'
	  );
	$$
2026-09-13 06:16:27.809 UTC [20826] ERROR:  function public.current_user_tenant_id() does not exist at character 209
2026-09-13 06:16:27.809 UTC [20826] HINT:  No function matches the given name and argument types. You might need to add explicit type casts.
2026-09-13 06:16:27.809 UTC [20826] STATEMENT:  CREATE OR REPLACE FUNCTION public.is_tenant_member(p_tenant_id uuid)
	RETURNS boolean
	LANGUAGE sql
	STABLE
	SECURITY DEFINER
	SET search_path TO public
	AS $$
	  SELECT p_tenant_id IS NOT NULL
	   AND p_tenant_id = public.current_user_tenant_id();
	$$
2026-09-13 06:16:28.057 UTC [20857] ERROR:  type user_role does not exist
2026-09-13 06:16:28.057 UTC [20857] STATEMENT:  CREATE OR REPLACE FUNCTION public.managed_create_user(p_email text, p_full_name text, p_role user_role, p_store_id uuid DEFAULT NULL::uuid, p_memberships jsonb DEFAULT NULL::jsonb, p_max_stores integer DEFAULT 0, p_max_users integer DEFAULT 0, p_target_user_id uuid DEFAULT NULL::uuid)
	 RETURNS jsonb
	 LANGUAGE plpgsql
	 SECURITY DEFINER
	 SET search_path TO 'public', 'extensions'
	AS $function$
	DECLARE
	    v_user_id uuid;
	    v_role_id uuid;
	    v_active_store_id uuid;
	    m JSONB;
	    v_creator_role user_role;
	BEGIN
	    SELECT role INTO v_creator_role FROM public.profiles WHERE id = auth.uid();
	    IF v_creator_role IS NULL OR v_creator_role NOT IN ('admin', 'encargado') THEN
	        RAISE EXCEPTION 'ERR_UNAUTHORIZED: Only admins and managers can create users.';
	    END IF;
	
	    v_active_store_id := COALESCE((p_memberships->0->>'store_id')::UUID, p_store_id);
	
	    IF v_creator_role = 'encargado' AND v_active_store_id IS NOT NULL THEN
	        IF NOT public.has_store_access(v_active_store_id) THEN
	            RAISE EXCEPTION 'ERR_UNAUTHORIZED: No access to store %', v_active_store_id;
	        END IF;
	    END IF;
	
	    SELECT id INTO v_role_id FROM public.roles WHERE lower(name) = lower(p_role::text) OR (name = 'Cajero' AND p_role = 'clerk') OR (name = 'Almacenero' AND p_role = 'warehouse') LIMIT 1;
	    v_user_id := COALESCE(p_target_user_id, gen_random_uuid());
	
	    INSERT INTO public.profiles (id, email, full_name, role, role_id, active_store_id, is_active, created_by, max_stores_limit, max_users_limit, created_at, updated_at)
	    VALUES (v_user_id, p_email, p_full_name, p_role, v_role_id, v_active_store_id, true, auth.uid(), p_max_stores, p_max_users, now(), now())
	    ON CONFLICT (id) DO UPDATE SET email = EXCLUDED.email, full_name = EXCLUDED.full_name, role = EXCLUDED.role, role_id = EXCLUDED.role_id, active_store_id = EXCLUDED.active_store_id, updated_at = now()
	    RETURNING id INTO v_user_id;
	
	    IF p_memberships IS NOT NULL THEN
	        IF v_creator_role != 'admin' THEN
	            DELETE FROM public.user_store_memberships WHERE user_id = v_user_id AND store_id IN (SELECT store_id FROM public.user_store_memberships WHERE user_id = auth.uid() AND role IN ('encargado', 'manager'));
	        ELSE
	            DELETE FROM public.user_store_memberships WHERE user_id = v_user_id;
	        END IF;
	        FOR m IN SELECT * FROM jsonb_array_elements(p_memberships) LOOP
	            IF (m->>'store_id') IS NOT NULL AND (m->>'store_id') <> '' THEN
	                IF v_creator_role = 'admin' OR public.has_store_access((m->>'store_id')::UUID) THEN
	                    INSERT INTO public.user_store_memberships (user_id, store_id, role) VALUES (v_user_id, (m->>'store_id')::UUID, (m->>'role')::user_role) ON CONFLICT (user_id, store_id) DO UPDATE SET role = EXCLUDED.role;
	                END IF;
	            END IF;
	        END LOOP;
	    ELSIF p_store_id IS NOT NULL THEN
	        INSERT INTO public.user_store_memberships (user_id, store_id, role) VALUES (v_user_id, p_store_id, p_role) ON CONFLICT (user_id, store_id) DO UPDATE SET role = EXCLUDED.role;
	    END IF;
	    RETURN jsonb_build_object('success', true, 'user_id', v_user_id);
	END;
	$function$
2026-09-13 06:16:28.076 UTC [20860] ERROR:  type public.user_role does not exist
2026-09-13 06:16:28.076 UTC [20860] STATEMENT:  CREATE OR REPLACE FUNCTION public.managed_create_user_v2(
	  p_email text,
	  p_full_name text,
	  p_role public.user_role,
	  p_plan plan_t DEFAULT 'free'::plan_t,
	  p_store_id uuid DEFAULT NULL::uuid,
	  p_memberships jsonb DEFAULT NULL::jsonb,
	  p_max_stores int DEFAULT 0,
	  p_max_users int DEFAULT 0,
	  p_target_user_id uuid DEFAULT NULL::uuid,
	  p_creator_id uuid DEFAULT NULL::uuid
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  v_user_id uuid := COALESCE(p_target_user_id, gen_random_uuid());
	  v_role_id uuid;
	  v_active_store_id uuid;
	  m JSONB;
	  v_creator_role public.user_role;
	  v_creator_uid uuid := COALESCE(p_creator_id, auth.uid());
	BEGIN
	  -- Validar caller es admin o encargado
	  SELECT role INTO v_creator_role FROM public.profiles WHERE id = v_creator_uid;
	  IF v_creator_role IS NULL OR v_creator_role NOT IN ('admin', 'encargado', 'superadmin') THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED: Only admins and managers can create users.';
	  END IF;
	
	  -- Validar email único (entre perfiles activos)
	  IF EXISTS (SELECT 1 FROM public.profiles WHERE email = p_email AND deleted_at IS NULL) THEN
	    RAISE EXCEPTION 'ERR_EMAIL_ALREADY_EXISTS: %', p_email;
	  END IF;
	
	  v_active_store_id := COALESCE((p_memberships->0->>'store_id')::UUID, p_store_id);
	
	  -- Encargado solo puede crear en tiendas que gestiona
	  IF v_creator_role = 'encargado' AND v_active_store_id IS NOT NULL THEN
	    IF NOT public.has_store_access(v_active_store_id) THEN
	      RAISE EXCEPTION 'ERR_UNAUTHORIZED: No access to store %', v_active_store_id;
	    END IF;
	  END IF;
	
	  -- Buscar role_id
	  SELECT id INTO v_role_id FROM public.roles
	    WHERE lower(name) = lower(p_role::text)
	       OR (name = 'Cajero' AND p_role = 'clerk')
	       OR (name = 'Almacenero' AND p_role = 'warehouse')
	       OR (name = 'UserCosto' AND p_role = 'costo')
	    LIMIT 1;
	
	  -- INSERT profiles con plan
	  INSERT INTO public.profiles (
	    id, email, full_name, role, role_id, active_store_id, is_active,
	    created_by, max_stores_limit, max_users_limit, plan, created_at, updated_at
	  ) VALUES (
	    v_user_id, p_email, p_full_name, p_role, v_role_id, v_active_store_id, true,
	    v_creator_uid, p_max_stores, p_max_users, p_plan, now(), now()
	  )
	  ON CONFLICT (id) DO UPDATE SET
	    email = EXCLUDED.email,
	    full_name = EXCLUDED.full_name,
	    role = EXCLUDED.role,
	    role_id = EXCLUDED.role_id,
	    active_store_id = EXCLUDED.active_store_id,
	    plan = EXCLUDED.plan,
	    is_active = true,
	    deleted_at = NULL,
	    deletion_reason = NULL,
	    deleted_by = NULL,
	    updated_at = now()
	  RETURNING id INTO v_user_id;
	
	  -- Procesar memberships
	  IF p_memberships IS NOT NULL THEN
	    IF v_creator_role != 'admin' AND v_creator_role != 'superadmin' THEN
	      DELETE FROM public.user_store_memberships
	        WHERE user_id = v_user_id
	        AND store_id IN (
	          SELECT store_id FROM public.user_store_memberships
	          WHERE user_id = v_creator_uid AND role IN ('encargado', 'manager')
	        );
	    ELSE
	      DELETE FROM public.user_store_memberships WHERE user_id = v_user_id;
	    END IF;
	
	    FOR m IN SELECT * FROM jsonb_array_elements(p_memberships) LOOP
	      IF (m->>'store_id') IS NOT NULL AND (m->>'store_id') <> '' THEN
	        IF v_creator_role IN ('admin', 'superadmin') OR public.has_store_access((m->>'store_id')::UUID) THEN
	          INSERT INTO public.user_store_memberships (user_id, store_id, role)
	          VALUES (v_user_id, (m->>'store_id')::UUID, (m->>'role')::public.user_role)
	          ON CONFLICT (user_id, store_id) DO UPDATE SET role = EXCLUDED.role, status = 'active';
	        END IF;
	      END IF;
	    END LOOP;
	  ELSIF p_store_id IS NOT NULL THEN
	    INSERT INTO public.user_store_memberships (user_id, store_id, role)
	    VALUES (v_user_id, p_store_id, p_role)
	    ON CONFLICT (user_id, store_id) DO UPDATE SET role = EXCLUDED.role, status = 'active';
	  END IF;
	
	  -- Audit log atómico (regresión C-2 restaurada)
	  INSERT INTO public.user_audit_log (performed_by, target_user_id, action, new_values, metadata)
	  VALUES (
	    v_creator_uid, v_user_id, 'USER_CREATED',
	    jsonb_build_object(
	      'email', p_email, 'full_name', p_full_name, 'role', p_role::text,
	      'plan', p_plan::text, 'active_store_id', v_active_store_id,
	      'max_stores', p_max_stores, 'max_users', p_max_users
	    ),
	    jsonb_build_object('memberships_count', CASE WHEN p_memberships IS NOT NULL THEN jsonb_array_length(p_memberships) ELSE 0 END)
	  );
	
	  RETURN jsonb_build_object('success', true, 'user_id', v_user_id);
	END;
	$function$
2026-09-13 06:16:28.152 UTC [20871] ERROR:  type "public.user_role" does not exist at character 392
2026-09-13 06:16:28.152 UTC [20871] STATEMENT:  CREATE OR REPLACE FUNCTION public.managed_reset_password(
	  p_user_id uuid,
	  p_caller_id uuid DEFAULT NULL::uuid
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  -- FIX H-7: anti-spoofing
	  v_caller_uid uuid := CASE WHEN auth.role() = 'service_role'
	    THEN COALESCE(p_caller_id, auth.uid()) ELSE auth.uid() END;
	  v_caller_role public.user_role;
	  v_target_email text;
	BEGIN
	  SELECT role INTO v_caller_role FROM public.profiles WHERE id = v_caller_uid;
	  IF v_caller_role IS NULL OR v_caller_role NOT IN ('admin', 'superadmin') THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED';
	  END IF;
	
	  IF p_user_id = v_caller_uid THEN
	    RAISE EXCEPTION 'ERR_SELF_RESET_BLOCKED';
	  END IF;
	
	  SELECT email INTO v_target_email FROM public.profiles WHERE id = p_user_id AND deleted_at IS NULL;
	  IF v_target_email IS NULL THEN
	    RAISE EXCEPTION 'ERR_USER_NOT_FOUND';
	  END IF;
	
	  INSERT INTO public.user_audit_log (performed_by, target_user_id, action, metadata)
	  VALUES (
	    v_caller_uid, p_user_id, 'PASSWORD_RESET_REQUESTED',
	    jsonb_build_object('email', v_target_email, 'method', 'recovery_link')
	  );
	
	  RETURN jsonb_build_object('success', true, 'email', v_target_email);
	END;
	$function$
2026-09-13 06:16:28.199 UTC [20878] ERROR:  type "public.user_role" does not exist at character 427
2026-09-13 06:16:28.199 UTC [20878] STATEMENT:  CREATE OR REPLACE FUNCTION public.managed_soft_delete_user(
	  p_user_id uuid,
	  p_reason text,
	  p_caller_id uuid DEFAULT NULL::uuid
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  v_old RECORD;
	  -- FIX H-7: anti-spoofing
	  v_caller_uid uuid := CASE WHEN auth.role() = 'service_role'
	    THEN COALESCE(p_caller_id, auth.uid()) ELSE auth.uid() END;
	  v_caller_role public.user_role;
	  v_active_memberships_count int;
	  v_anon_email text;
	BEGIN
	  SELECT role INTO v_caller_role FROM public.profiles WHERE id = v_caller_uid;
	  IF v_caller_role IS NULL OR v_caller_role NOT IN ('admin', 'superadmin') THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED';
	  END IF;
	
	  IF p_user_id = v_caller_uid THEN
	    RAISE EXCEPTION 'ERR_SELF_DELETE_BLOCKED';
	  END IF;
	
	  SELECT id, email, full_name, role, plan, is_active INTO v_old
	    FROM public.profiles WHERE id = p_user_id AND deleted_at IS NULL FOR UPDATE;
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_USER_NOT_FOUND_OR_ALREADY_DELETED';
	  END IF;
	
	  SELECT COUNT(*) INTO v_active_memberships_count
	    FROM public.user_store_memberships
	    WHERE user_id = p_user_id AND status = 'active';
	  IF v_active_memberships_count > 0 THEN
	    RAISE EXCEPTION 'ERR_USER_HAS_ACTIVE_MEMBERSHIPS: % active. Revoke memberships first.', v_active_memberships_count;
	  END IF;
	
	  v_anon_email := 'deleted+' || substr(p_user_id::text, 1, 8) || '@anonymized.local';
	
	  UPDATE public.profiles SET
	    deleted_at = now(),
	    deletion_reason = p_reason,
	    deleted_by = v_caller_uid,
	    is_active = false,
	    full_name = '[deleted user]',
	    email = v_anon_email,
	    ai_api_key = NULL,
	    updated_at = now()
	  WHERE id = p_user_id;
	
	  UPDATE public.user_store_memberships SET
	    status = 'revoked',
	    updated_at = now()
	  WHERE user_id = p_user_id AND status = 'active';
	
	  INSERT INTO public.user_audit_log (performed_by, target_user_id, action, old_values, new_values, metadata)
	  VALUES (
	    v_caller_uid, p_user_id, 'USER_SOFT_DELETED',
	    jsonb_build_object(
	      'email', v_old.email, 'full_name', v_old.full_name,
	      'role', v_old.role::text, 'plan', v_old.plan::text, 'is_active', v_old.is_active
	    ),
	    jsonb_build_object(
	      'email', v_anon_email, 'full_name', '[deleted user]',
	      'is_active', false, 'deleted_at', now()
	    ),
	    jsonb_build_object('reason', p_reason, 'memberships_revoked', v_active_memberships_count)
	  );
	
	  RETURN jsonb_build_object(
	    'success', true,
	    'user_id', p_user_id,
	    'status', 'soft_deleted',
	    'note', 'auth.users preserved. API route should ban via auth.admin.updateUser.'
	  );
	END;
	$function$
2026-09-13 06:16:28.248 UTC [20885] ERROR:  type "public.user_role" does not exist at character 435
2026-09-13 06:16:28.248 UTC [20885] STATEMENT:  CREATE OR REPLACE FUNCTION public.managed_toggle_user_status(
	  p_user_id uuid,
	  p_is_active boolean,
	  p_caller_id uuid DEFAULT NULL::uuid
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  v_old RECORD;
	  -- FIX H-7: anti-spoofing
	  v_caller_uid uuid := CASE WHEN auth.role() = 'service_role'
	    THEN COALESCE(p_caller_id, auth.uid()) ELSE auth.uid() END;
	  v_caller_role public.user_role;
	BEGIN
	  SELECT role INTO v_caller_role FROM public.profiles WHERE id = v_caller_uid;
	  IF v_caller_role IS NULL OR v_caller_role NOT IN ('admin', 'superadmin', 'encargado', 'manager') THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED';
	  END IF;
	
	  IF p_is_active = false AND p_user_id = v_caller_uid THEN
	    RAISE EXCEPTION 'ERR_SELF_DEACTIVATE_BLOCKED';
	  END IF;
	
	  SELECT id, is_active, full_name INTO v_old
	    FROM public.profiles WHERE id = p_user_id AND deleted_at IS NULL FOR UPDATE;
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_USER_NOT_FOUND';
	  END IF;
	
	  IF v_old.is_active = p_is_active THEN
	    RETURN jsonb_build_object('success', true, 'no_change', true);
	  END IF;
	
	  UPDATE public.profiles SET is_active = p_is_active, updated_at = now() WHERE id = p_user_id;
	
	  INSERT INTO public.user_audit_log (performed_by, target_user_id, action, old_values, new_values)
	  VALUES (
	    v_caller_uid, p_user_id,
	    CASE WHEN p_is_active THEN 'USER_ACTIVATED' ELSE 'USER_DEACTIVATED' END,
	    jsonb_build_object('is_active', v_old.is_active),
	    jsonb_build_object('is_active', p_is_active)
	  );
	
	  RETURN jsonb_build_object('success', true, 'user_id', p_user_id, 'is_active', p_is_active);
	END;
	$function$
2026-09-13 06:16:28.265 UTC [20888] ERROR:  type public.user_role does not exist
2026-09-13 06:16:28.265 UTC [20888] STATEMENT:  CREATE OR REPLACE FUNCTION public.managed_update_membership(
	  p_membership_id uuid,
	  p_role public.user_role DEFAULT NULL::user_role,
	  p_status text DEFAULT NULL::text,
	  p_caller_id uuid DEFAULT NULL::uuid
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  v_old RECORD;
	  -- FIX H-7: anti-spoofing
	  v_caller_uid uuid := CASE WHEN auth.role() = 'service_role'
	    THEN COALESCE(p_caller_id, auth.uid()) ELSE auth.uid() END;
	  v_changes jsonb := '{}'::jsonb;
	BEGIN
	  SELECT m.id, m.user_id, m.store_id, m.role, m.status
	    INTO v_old
	    FROM public.user_store_memberships m
	    WHERE m.id = p_membership_id
	    FOR UPDATE;
	
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_MEMBERSHIP_NOT_FOUND';
	  END IF;
	
	  -- FIX H-7: Autorización usa nueva sobrecarga has_store_role(p_user_id, p_store_id, p_roles)
	  -- service_role (API route) pasa p_caller_id=session.user.id → verifica acceso de ese user
	  -- authenticated pasa p_caller_id=NULL → v_caller_uid=auth.uid() → verifica su propio acceso
	  IF NOT public.has_store_role(v_caller_uid, v_old.store_id, ARRAY['admin', 'manager']) THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED: Caller must be admin or manager of the store.';
	  END IF;
	
	  IF p_role IS NOT NULL AND p_role <> v_old.role THEN
	    v_changes := v_changes || jsonb_build_object('role', jsonb_build_object('old', v_old.role::text, 'new', p_role::text));
	    UPDATE public.user_store_memberships SET role = p_role, updated_at = now() WHERE id = p_membership_id;
	  END IF;
	
	  IF p_status IS NOT NULL AND p_status <> v_old.status::text THEN
	    IF p_status NOT IN ('active', 'revoked') THEN
	      RAISE EXCEPTION 'ERR_INVALID_STATUS: %', p_status;
	    END IF;
	    v_changes := v_changes || jsonb_build_object('status', jsonb_build_object('old', v_old.status, 'new', p_status));
	    UPDATE public.user_store_memberships SET status = p_status::membership_status, updated_at = now() WHERE id = p_membership_id;
	  END IF;
	
	  IF v_changes <> '{}'::jsonb THEN
	    INSERT INTO public.user_audit_log (performed_by, target_user_id, action, old_values, new_values, metadata)
	    VALUES (
	      v_caller_uid, v_old.user_id, 'MEMBERSHIP_UPDATED',
	      jsonb_build_object('membership_id', p_membership_id, 'store_id', v_old.store_id, 'role', v_old.role::text, 'status', v_old.status),
	      v_changes,
	      jsonb_build_object('store_id', v_old.store_id)
	    );
	  END IF;
	
	  RETURN jsonb_build_object('success', true, 'membership_id', p_membership_id, 'changes', v_changes);
	END;
	$function$
2026-09-13 06:16:28.287 UTC [20891] ERROR:  type plan_t does not exist
2026-09-13 06:16:28.287 UTC [20891] STATEMENT:  CREATE OR REPLACE FUNCTION public.managed_update_tenant_plan(
	  p_tenant_id uuid,
	  p_plan plan_t,
	  p_subscription_status text DEFAULT NULL,
	  p_caller_id uuid DEFAULT NULL
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  -- FIX H-7: anti-spoofing — service_role (Stripe webhook) puede pasar NULL caller_id
	  v_caller_uid uuid := CASE WHEN auth.role() = 'service_role'
	    THEN COALESCE(p_caller_id, auth.uid()) ELSE auth.uid() END;
	  v_old_plan plan_t;
	  v_old_status text;
	  v_role public.user_role;
	BEGIN
	  -- Validación: si caller_uid es NULL (service_role anónimo para Stripe webhook), permitir
	  -- Si caller_uid NO es NULL, debe ser admin/superadmin
	  IF v_caller_uid IS NOT NULL THEN
	    SELECT role INTO v_role FROM public.profiles WHERE id = v_caller_uid;
	    IF v_role IS NULL OR v_role NOT IN ('admin', 'superadmin') THEN
	      RAISE EXCEPTION 'ERR_UNAUTHORIZED: Only admins can change tenant plan';
	    END IF;
	  END IF;
	
	  SELECT plan, subscription_status INTO v_old_plan, v_old_status
	    FROM public.tenants WHERE id = p_tenant_id FOR UPDATE;
	
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_TENANT_NOT_FOUND: %', p_tenant_id;
	  END IF;
	
	  UPDATE public.tenants SET
	    plan = p_plan,
	    subscription_status = COALESCE(p_subscription_status, subscription_status),
	    updated_at = now()
	  WHERE id = p_tenant_id;
	
	  UPDATE public.profiles SET
	    plan = p_plan,
	    updated_at = now()
	  WHERE tenant_id = p_tenant_id AND deleted_at IS NULL;
	
	  INSERT INTO public.user_audit_log (performed_by, target_user_id, action, old_values, new_values, metadata)
	  VALUES (
	    v_caller_uid, NULL, 'TENANT_PLAN_UPDATED',
	    jsonb_build_object('old_plan', v_old_plan::text, 'old_status', v_old_status),
	    jsonb_build_object('new_plan', p_plan::text, 'new_status', COALESCE(p_subscription_status, v_old_status)),
	    jsonb_build_object('tenant_id', p_tenant_id)
	  );
	
	  RETURN jsonb_build_object(
	    'success', true,
	    'tenant_id', p_tenant_id,
	    'old_plan', v_old_plan::text,
	    'new_plan', p_plan::text
	  );
	END;
	$function$
2026-09-13 06:16:28.305 UTC [20894] ERROR:  type public.user_role does not exist
2026-09-13 06:16:28.305 UTC [20894] STATEMENT:  CREATE OR REPLACE FUNCTION public.managed_update_user(
	  p_user_id uuid,
	  p_full_name text DEFAULT NULL::text,
	  p_role public.user_role DEFAULT NULL::user_role,
	  p_role_id uuid DEFAULT NULL::uuid,
	  p_is_active boolean DEFAULT NULL::boolean,
	  p_max_stores_limit int DEFAULT NULL::int,
	  p_max_users_limit int DEFAULT NULL::int,
	  p_plan plan_t DEFAULT NULL::plan_t,
	  p_caller_id uuid DEFAULT NULL::uuid
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  v_old RECORD;
	  -- FIX H-7: anti-spoofing — service_role puede pasar p_caller_id explícito
	  v_caller_uid uuid := CASE WHEN auth.role() = 'service_role'
	    THEN COALESCE(p_caller_id, auth.uid()) ELSE auth.uid() END;
	  v_caller_role public.user_role;
	  v_changes jsonb := '{}'::jsonb;
	BEGIN
	  -- Validar caller es admin (usa v_caller_uid que ahora NO es spoofable para authenticated)
	  SELECT role INTO v_caller_role FROM public.profiles WHERE id = v_caller_uid;
	  IF v_caller_role IS NULL OR v_caller_role NOT IN ('admin', 'superadmin') THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED: Only admins can update users.';
	  END IF;
	
	  SELECT * INTO v_old FROM public.profiles WHERE id = p_user_id AND deleted_at IS NULL FOR UPDATE;
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_USER_NOT_FOUND: %', p_user_id;
	  END IF;
	
	  IF p_is_active = false AND p_user_id = v_caller_uid THEN
	    RAISE EXCEPTION 'ERR_SELF_DEACTIVATE_BLOCKED: Cannot deactivate own account.';
	  END IF;
	
	  IF p_full_name IS NOT NULL AND p_full_name <> v_old.full_name THEN
	    v_changes := v_changes || jsonb_build_object('full_name', jsonb_build_object('old', v_old.full_name, 'new', p_full_name));
	    UPDATE public.profiles SET full_name = p_full_name, updated_at = now() WHERE id = p_user_id;
	  END IF;
	
	  IF p_role IS NOT NULL AND p_role <> v_old.role THEN
	    v_changes := v_changes || jsonb_build_object('role', jsonb_build_object('old', v_old.role::text, 'new', p_role::text));
	    UPDATE public.profiles SET role = p_role, updated_at = now() WHERE id = p_user_id;
	  END IF;
	
	  IF p_role_id IS NOT NULL AND (v_old.role_id IS NULL OR p_role_id <> v_old.role_id) THEN
	    v_changes := v_changes || jsonb_build_object('role_id', jsonb_build_object('old', v_old.role_id, 'new', p_role_id));
	    UPDATE public.profiles SET role_id = p_role_id, updated_at = now() WHERE id = p_user_id;
	  END IF;
	
	  IF p_is_active IS NOT NULL AND p_is_active <> v_old.is_active THEN
	    v_changes := v_changes || jsonb_build_object('is_active', jsonb_build_object('old', v_old.is_active, 'new', p_is_active));
	    UPDATE public.profiles SET is_active = p_is_active, updated_at = now() WHERE id = p_user_id;
	  END IF;
	
	  IF p_max_stores_limit IS NOT NULL AND (v_old.max_stores_limit IS NULL OR p_max_stores_limit <> v_old.max_stores_limit) THEN
	    v_changes := v_changes || jsonb_build_object('max_stores_limit', jsonb_build_object('old', v_old.max_stores_limit, 'new', p_max_stores_limit));
	    UPDATE public.profiles SET max_stores_limit = p_max_stores_limit, updated_at = now() WHERE id = p_user_id;
	  END IF;
	
	  IF p_max_users_limit IS NOT NULL AND (v_old.max_users_limit IS NULL OR p_max_users_limit <> v_old.max_users_limit) THEN
	    v_changes := v_changes || jsonb_build_object('max_users_limit', jsonb_build_object('old', v_old.max_users_limit, 'new', p_max_users_limit));
	    UPDATE public.profiles SET max_users_limit = p_max_users_limit, updated_at = now() WHERE id = p_user_id;
	  END IF;
	
	  IF p_plan IS NOT NULL AND p_plan <> v_old.plan THEN
	    v_changes := v_changes || jsonb_build_object('plan', jsonb_build_object('old', v_old.plan::text, 'new', p_plan::text));
	    UPDATE public.profiles SET plan = p_plan, updated_at = now() WHERE id = p_user_id;
	  END IF;
	
	  IF v_changes <> '{}'::jsonb THEN
	    INSERT INTO public.user_audit_log (performed_by, target_user_id, action, old_values, new_values, metadata)
	    VALUES (
	      v_caller_uid, p_user_id, 'USER_UPDATED',
	      jsonb_build_object(
	        'full_name', v_old.full_name, 'role', v_old.role::text,
	        'is_active', v_old.is_active, 'plan', v_old.plan::text
	      ),
	      v_changes,
	      jsonb_build_object('fields_changed', jsonb_object_keys(v_changes))
	    );
	  END IF;
	
	  RETURN jsonb_build_object('success', true, 'user_id', p_user_id, 'changes', v_changes);
	END;
	$function$
2026-09-13 06:16:28.378 UTC [20905] ERROR:  type "user_role" does not exist at character 204
2026-09-13 06:16:28.378 UTC [20905] STATEMENT:  CREATE OR REPLACE FUNCTION public.on_auth_user_created()
	RETURNS trigger
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO 'public', 'extensions'
	AS $function$
	DECLARE
	  v_role_id UUID;
	  v_role_enum USER_ROLE;
	  v_metadata_role TEXT;
	  v_company_name TEXT;
	  v_full_name TEXT;
	  v_tenant_id UUID;
	BEGIN
	  -- Extract role from metadata if present
	  v_metadata_role := NEW.raw_user_meta_data->>'role';
	
	  -- Normalize and convert to enum
	  IF v_metadata_role IS NOT NULL THEN
	    BEGIN
	      v_role_enum := v_metadata_role::USER_ROLE;
	    EXCEPTION WHEN OTHERS THEN
	      v_role_enum := 'tenant_admin'::USER_ROLE;
	    END;
	  ELSE
	    -- Iteración 13: self-signup users are tenant_admin by default
	    v_role_enum := 'tenant_admin'::USER_ROLE;
	  END IF;
	
	  -- Extract company_name and full_name from metadata
	  v_company_name := NEW.raw_user_meta_data->>'company_name';
	  v_full_name := COALESCE(NEW.raw_user_meta_data->>'full_name', split_part(NEW.email, '@', 1));
	
	  -- Iteración 13: Create tenant for self-signup users
	  -- If user was created by admin (via managed_create_user_v2), they may already
	  -- have a tenant_id set via p_target_user_id. In that case, skip tenant creation.
	  IF v_company_name IS NOT NULL THEN
	    INSERT INTO public.tenants (name, owner_id, plan, subscription_status, trial_ends_at, is_active)
	    VALUES (
	      v_company_name,
	      NEW.id,
	      'free'::plan_t,
	      'trial',
	      now() + interval '14 days',
	      true
	    )
	    RETURNING id INTO v_tenant_id;
	  END IF;
	
	  -- Get matching role_id from public.roles
	  SELECT id INTO v_role_id FROM public.roles
	    WHERE name = v_metadata_role OR lower(name) = lower(v_role_enum::text)
	    LIMIT 1;
	
	  -- Insert profile with tenant_id
	  INSERT INTO public.profiles (id, email, full_name, role, role_id, is_active, tenant_id, plan, created_at, updated_at)
	  VALUES (
	    NEW.id,
	    NEW.email,
	    v_full_name,
	    v_role_enum,
	    v_role_id,
	    true,
	    v_tenant_id,
	    'free'::plan_t,
	    now(),
	    now()
	  )
	  ON CONFLICT (id) DO UPDATE SET
	    email = EXCLUDED.email,
	    full_name = EXCLUDED.full_name,
	    role = EXCLUDED.role,
	    role_id = EXCLUDED.role_id,
	    tenant_id = COALESCE(profiles.tenant_id, EXCLUDED.tenant_id),
	    plan = EXCLUDED.plan,
	    is_active = true,
	    updated_at = now();
	
	  -- Audit log
	  INSERT INTO public.user_audit_log (performed_by, target_user_id, action, new_values, metadata)
	  VALUES (
	    NEW.id, NEW.id, 'USER_REGISTERED_PUBLICLY',
	    jsonb_build_object('email', NEW.email, 'role', v_role_enum::text, 'tenant_id', v_tenant_id),
	    jsonb_build_object('company_name', v_company_name, 'source', 'self_signup')
	  );
	
	  RETURN NEW;
	END;
	$function$
2026-09-13 06:16:28.715 UTC [20948] ERROR:  type adjustment_item[] does not exist
2026-09-13 06:16:28.715 UTC [20948] STATEMENT:  CREATE OR REPLACE FUNCTION public.process_inventory_adjustment(p_store_id uuid, p_cashier_id uuid, p_items adjustment_item[], p_operation_date timestamp with time zone DEFAULT NULL::timestamp with time zone)
	 RETURNS uuid
	 LANGUAGE plpgsql
	 SECURITY DEFINER
	 SET search_path TO 'public'
	AS $function$
	DECLARE
	  v_adjustment_id UUID;
	  v_item public.adjustment_item;
	  v_difference NUMERIC;
	  v_effective_date TIMESTAMP WITH TIME ZONE := COALESCE(p_operation_date, NOW());
	BEGIN
	  -- Validación forward-only locking
	  PERFORM public.validate_operation_date(p_operation_date, p_store_id);
	
	  INSERT INTO public.inventory_adjustments (store_id, created_by, status, created_at)
	  VALUES (p_store_id, p_cashier_id, 'PROCESSING', v_effective_date)
	  RETURNING id INTO v_adjustment_id;
	
	  FOREACH v_item IN ARRAY p_items
	  LOOP
	    v_difference := v_item.counted_quantity - v_item.expected_quantity;
	    INSERT INTO public.inventory_adjustment_items (adjustment_id, product_id, expected_quantity, counted_quantity, created_at)
	    VALUES (v_adjustment_id, v_item.product_id, v_item.expected_quantity, v_item.counted_quantity, v_effective_date);
	
	    PERFORM public.register_stock_movement(
	        p_product_id := v_item.product_id,
	        p_store_id := p_store_id,
	        p_user_id := p_cashier_id,
	        p_quantity := v_difference,
	        p_movement_type := 'adjustment',
	        p_operation_date := v_effective_date
	    );
	  END LOOP;
	
	  UPDATE public.inventory_adjustments SET status = 'COMPLETED', updated_at = v_effective_date
	  WHERE id = v_adjustment_id;
	  RETURN v_adjustment_id;
	END;
	$function$
2026-09-13 06:16:28.845 UTC [20967] ERROR:  type "public.purchase_status_enum" does not exist at character 425
2026-09-13 06:16:28.845 UTC [20967] STATEMENT:  CREATE OR REPLACE FUNCTION public.receive_against_po(
	  p_po_id uuid,
	  p_received_items jsonb DEFAULT '[]'::jsonb,
	  p_user_id uuid DEFAULT NULL,
	  p_reception_date timestamptz DEFAULT now(),
	  p_invoice_number text DEFAULT NULL
	) RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO 'public', 'extensions'
	AS $func$
	DECLARE
	  v_store_id            uuid;
	  v_supplier_name       text;
	  v_po_status           public.purchase_status_enum;
	  v_item                jsonb;
	  v_item_id             uuid;
	  v_qty                 numeric;
	  v_qty_ordered         numeric;
	  v_qty_received_before numeric;
	  v_product_id          uuid;
	  v_unit_cost           numeric;
	  v_product_name        text;
	  v_all_received        boolean;
	  v_any_received        boolean;
	  v_new_status          public.purchase_status_enum;
	  v_receipt_id          uuid;
	  v_reception_items     jsonb := '[]'::jsonb;
	  v_count               integer := 0;
	  v_po_number           text;
	  v_invoice_to_pass     text;
	  v_sorted_items        jsonb;
	BEGIN
	  -- ─── 1. Cargar OC con lock exclusivo ───
	  SELECT po.store_id, po.supplier_name, po.status, po.po_number
	    INTO v_store_id, v_supplier_name, v_po_status, v_po_number
	  FROM public.purchase_orders po
	  WHERE po.id = p_po_id
	  FOR UPDATE;
	
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_PO_NOT_FOUND';
	  END IF;
	
	  -- ─── 2. Validar acceso (tenant-aware via has_store_access) ───
	  IF NOT public.has_store_access(v_store_id) THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED';
	  END IF;
	
	  -- ─── 3. Validar estado de la OC ───
	  IF v_po_status = 'cancelled' THEN
	    RAISE EXCEPTION 'ERR_PO_CANCELLED';
	  END IF;
	  -- Solo se puede recibir contra draft, sent o partial.
	  IF v_po_status NOT IN ('draft', 'sent', 'partial') THEN
	    RAISE EXCEPTION 'ERR_PO_NOT_RECEIVABLE: status % is terminal', v_po_status;
	  END IF;
	
	  -- ─── 4. Validar items no vacío (parity B3 register_reception) ───
	  IF p_received_items IS NULL OR jsonb_array_length(p_received_items) = 0 THEN
	    RAISE EXCEPTION 'ERR_EMPTY_ITEMS';
	  END IF;
	
	  -- ─── 5. Condición #5 (anti-deadlock): Ordenar items por po_item_id ASC ───
	  SELECT jsonb_agg(elem ORDER BY (elem->>'po_item_id'))
	    INTO v_sorted_items
	  FROM jsonb_array_elements(p_received_items) AS elem;
	
	  -- ─── 6. Procesar cada item: VALIDAR antes de actualizar ───
	  FOR v_item IN SELECT * FROM jsonb_array_elements(v_sorted_items) LOOP
	    v_item_id := NULLIF(v_item->>'po_item_id', '')::uuid;
	    v_qty     := COALESCE((v_item->>'quantity_received')::numeric, 0);
	
	    -- Validar po_item_id presente
	    IF v_item_id IS NULL THEN
	      RAISE EXCEPTION 'ERR_ITEM_ID_REQUIRED: missing po_item_id in received_items';
	    END IF;
	
	    -- Medio #6: qty > 0
	    IF v_qty <= 0 THEN
	      RAISE EXCEPTION 'ERR_NEGATIVE_QTY: qty % for item % must be > 0', v_qty, v_item_id;
	    END IF;
	
	    -- Cargar estado actual del item con lock FOR UPDATE
	    SELECT product_id, quantity_ordered, quantity_received, unit_cost, product_name
	      INTO v_product_id, v_qty_ordered, v_qty_received_before, v_unit_cost, v_product_name
	    FROM public.purchase_order_items
	    WHERE id = v_item_id AND po_id = p_po_id
	    FOR UPDATE;
	
	    IF NOT FOUND THEN
	      RAISE EXCEPTION 'ERR_ITEM_NOT_FOUND: %', v_item_id;
	    END IF;
	
	    -- Alto #5: over-receive check
	    IF (v_qty_received_before + v_qty) > v_qty_ordered THEN
	      RAISE EXCEPTION 'ERR_OVER_RECEIVE: item % ordered %, already received %, attempting %',
	        v_item_id, v_qty_ordered, v_qty_received_before, v_qty;
	    END IF;
	
	    -- Condición #1: todo item debe tener product_id NOT NULL
	    IF v_product_id IS NULL THEN
	      RAISE EXCEPTION 'ERR_PRODUCT_ID_REQUIRED: item % has no product_id', v_item_id;
	    END IF;
	
	    -- Medio #9: producto debe existir en store
	    IF NOT EXISTS (
	      SELECT 1 FROM public.products p
	      WHERE p.id = v_product_id AND p.store_id = v_store_id
	    ) THEN
	      RAISE EXCEPTION 'ERR_PRODUCT_NOT_IN_STORE: product % not in store %',
	        v_product_id, v_store_id;
	    END IF;
	
	    -- Acumular para register_reception (formato esperado: product_id, quantity, unit_cost, moneda, tasa)
	    v_reception_items := v_reception_items || jsonb_build_object(
	      'product_id',   v_product_id,
	      'product_name', v_product_name,
	      'quantity',     v_qty,
	      'unit_cost',    v_unit_cost,
	      'moneda_recepcion', 'CUP',
	      'tasa_cambio_recepcion', 1.0
	    );
	    v_count := v_count + 1;
	  END LOOP;
	
	  -- ─── 7. Condición #3: reference_doc NULL si no viene invoice_number ───
	  -- Evita colisión con UNIQUE INDEX idx_receipts_store_reference_doc
	  -- (store_id, reference_doc) en partial indexes.
	  -- El vínculo real receipt↔PO es receipts.po_id (G6).
	  IF p_invoice_number IS NULL OR p_invoice_number = '' THEN
	    v_invoice_to_pass := NULL;
	  ELSE
	    v_invoice_to_pass := p_invoice_number;
	  END IF;
	
	  -- ─── 8. Crear receipt vinculado vía register_reception ───
	  -- register_reception aplica las 8 validaciones B1-B5 + C1-C3 del v2.23.0
	  -- (no se relajan). G6 añade p_po_id opcional.
	  v_receipt_id := public.register_reception(
	    p_store_id        := v_store_id,
	    p_supplier        := v_supplier_name,
	    p_reception_date  := p_reception_date,
	    p_invoice_number  := v_invoice_to_pass,
	    p_items           := v_reception_items,
	    p_user_id         := p_user_id,
	    p_po_id           := p_po_id
	  );
	
	  -- ─── 9. Actualizar quantity_received en purchase_order_items ───
	  -- (SECURITY DEFINER bypasses RLS DENY UPDATE de G5)
	  FOR v_item IN SELECT * FROM jsonb_array_elements(v_sorted_items) LOOP
	    v_item_id := NULLIF(v_item->>'po_item_id', '')::uuid;
	    v_qty     := (v_item->>'quantity_received')::numeric;
	
	    UPDATE public.purchase_order_items
	    SET quantity_received = quantity_received + v_qty
	    WHERE id = v_item_id AND po_id = p_po_id;
	  END LOOP;
	
	  -- ─── 10. Recalcular status global (cast EXPLÍCITO a enum) ───
	  SELECT
	    BOOL_AND(quantity_received >= quantity_ordered),
	    BOOL_OR(quantity_received > 0)
	  INTO v_all_received, v_any_received
	  FROM public.purchase_order_items WHERE po_id = p_po_id;
	
	  v_new_status :=
	    CASE
	      WHEN v_all_received THEN 'received'::public.purchase_status_enum
	      WHEN v_any_received THEN 'partial'::public.purchase_status_enum
	      ELSE 'sent'::public.purchase_status_enum
	    END;
	
	  -- ─── 11. Actualizar OC + vincular receipt en notas ───
	  UPDATE public.purchase_orders
	  SET status       = v_new_status,
	      received_at  = CASE WHEN v_new_status = 'received' THEN NOW() ELSE received_at END,
	      notes        = COALESCE(notes, '') ||
	                     CASE WHEN COALESCE(notes, '') = '' THEN '' ELSE E'\n' END ||
	                     '[Receipt ' || v_receipt_id::text || ' linked at ' || NOW()::text || ']'
	  WHERE id = p_po_id;
	
	  -- ─── 12. CxP (Grupo 6): marcar receipt con payment_status='unpaid' ───
	  -- Solo si aún no está seteado (no sobrescribir si register_reception ya lo puso).
	  UPDATE public.receipts
	  SET payment_status     = COALESCE(NULLIF(payment_status, ''), 'unpaid'),
	      payment_terms_days = COALESCE(payment_terms_days, 30),
	      due_date           = COALESCE(due_date, (p_reception_date::date + INTERVAL '30 days')::date)
	  WHERE id = v_receipt_id;
	
	  -- ─── 13. Auditoría ───
	  INSERT INTO public.audit_logs (user_id, store_id, action, table_name, record_id, metadata)
	  VALUES (
	    p_user_id, v_store_id, 'po_received', 'purchase_orders', p_po_id,
	    jsonb_build_object(
	      'new_status', v_new_status::text,
	      'items_received', v_count,
	      'receipt_id', v_receipt_id,
	      'po_number', v_po_number,
	      'invoice_number', v_invoice_to_pass
	    )
	  );
	
	  RETURN jsonb_build_object(
	    'status', 'success',
	    'po_status', v_new_status::text,
	    'receipt_id', v_receipt_id,
	    'items_received', v_count
	  );
	END;
	$func$
2026-09-13 06:16:29.748 UTC [21102] ERROR:  type purchase_status_enum does not exist
2026-09-13 06:16:29.748 UTC [21102] STATEMENT:  CREATE OR REPLACE FUNCTION public.set_purchase_order_status(p_po_id uuid, p_new_status purchase_status_enum, p_user_id uuid DEFAULT NULL::uuid, p_reason text DEFAULT NULL::text)
	 RETURNS jsonb
	 LANGUAGE plpgsql
	 SECURITY DEFINER
	 SET search_path TO 'public', 'extensions'
	AS $function$
	DECLARE
	  v_store_id     uuid;
	  v_current      public.purchase_status_enum;
	  v_po_number    text;
	  v_allowed      text[];
	  v_is_allowed   boolean := false;
	BEGIN
	  -- ─── 1. Cargar PO con lock exclusivo ───
	  SELECT store_id, status, po_number
	    INTO v_store_id, v_current, v_po_number
	  FROM public.purchase_orders
	  WHERE id = p_po_id
	  FOR UPDATE;
	
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_PO_NOT_FOUND';
	  END IF;
	
	  -- ─── 2. Validar acceso (tenant-aware) ───
	  IF NOT public.has_store_access(v_store_id) THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED';
	  END IF;
	
	  -- ─── 3. Si no hay cambio, retornar success sin hacer nada ───
	  IF v_current = p_new_status THEN
	    RETURN jsonb_build_object(
	      'status', 'no_change',
	      'po_status', v_current::text,
	      'po_number', v_po_number
	    );
	  END IF;
	
	  -- ─── 4. Definir transiciones permitidas (state machine) ───
	  -- 'partial' y 'received' NUNCA son destino válido manualmente
	  -- (solo vía receive_against_po).
	  v_allowed := CASE v_current
	    WHEN 'draft'   THEN ARRAY['sent', 'cancelled']
	    WHEN 'sent'    THEN ARRAY['cancelled']
	    WHEN 'partial' THEN ARRAY['cancelled']
	    ELSE ARRAY[]::text[]  -- received, cancelled: terminal, no transitions
	  END;
	
	  -- ─── 5. Verificar transición permitida ───
	  SELECT EXISTS(SELECT 1 FROM unnest(v_allowed) a WHERE a = p_new_status::text)
	    INTO v_is_allowed;
	
	  IF NOT v_is_allowed THEN
	    RAISE EXCEPTION 'ERR_INVALID_TRANSITION: % → % not allowed (allowed: %)',
	      v_current::text, p_new_status::text,
	      CASE WHEN array_length(v_allowed, 1) IS NULL THEN 'none' ELSE array_to_string(v_allowed, ', ') END;
	  END IF;
	
	  -- ─── 6. Aplicar transición ───
	  UPDATE public.purchase_orders
	  SET status      = p_new_status,
	      received_at = CASE WHEN p_new_status = 'received' THEN NOW() ELSE received_at END
	  WHERE id = p_po_id;
	
	  -- ─── 7. Auditoría (action = PO_STATUS_CHANGED) ───
	  INSERT INTO public.audit_logs (user_id, store_id, action, table_name, record_id, metadata)
	  VALUES (
	    CASE WHEN auth.role() = 'service_role' THEN COALESCE(p_user_id, auth.uid()) ELSE auth.uid() END, v_store_id, 'PO_STATUS_CHANGED', 'purchase_orders', p_po_id,
	    jsonb_build_object(
	      'po_number', v_po_number,
	      'from_status', v_current::text,
	      'to_status', p_new_status::text,
	      'reason', p_reason
	    )
	  );
	
	  RETURN jsonb_build_object(
	    'status', 'success',
	    'po_status', p_new_status::text,
	    'po_number', v_po_number,
	    'previous_status', v_current::text
	  );
	END;
	$function$
2026-09-13 06:16:30.360 UTC [21189] ERROR:  type "user_role" does not exist at character 100
2026-09-13 06:16:30.360 UTC [21189] STATEMENT:  CREATE OR REPLACE FUNCTION public.validate_active_store()
	RETURNS TRIGGER AS $$
	DECLARE
	    v_role user_role;
	BEGIN
	    v_role := NEW.role;
	
	    -- Si role es 'costo' o 'admin' o 'superadmin', active_store_id es opcional
	    IF v_role = 'costo'::user_role
	       OR v_role = 'admin'::user_role
	       OR v_role = 'superadmin'::user_role THEN
	        -- Solo validar si active_store_id cambió o es INSERT
	        IF TG_OP = 'INSERT' OR (OLD.active_store_id IS DISTINCT FROM NEW.active_store_id) THEN
	            IF NEW.active_store_id IS NOT NULL THEN
	                IF NOT EXISTS (
	                    SELECT 1 FROM public.user_store_memberships
	                    WHERE user_id = NEW.id
	                      AND store_id = NEW.active_store_id
	                      AND status = 'active'
	                ) THEN
	                    IF TG_OP = 'UPDATE' THEN
	                        RAISE EXCEPTION 'ERR_INVALID_ACTIVE_STORE: El usuario no tiene membership activa en la tienda seleccionada.';
	                    END IF;
	                END IF;
	            END IF;
	        END IF;
	        RETURN NEW;
	    END IF;
	
	    -- Para roles operativos (encargado, clerk, warehouse, manager, usuario)
	    IF TG_OP = 'INSERT' THEN
	        -- En INSERT: ser permisivo. La función managed_create_user inserta
	        -- memberships justo después. No validar nada aquí.
	        RETURN NEW;
	    ELSE
	        -- UPDATE: solo validar si active_store_id cambió realmente
	        -- (evita bloquear updates de otros campos en perfiles intermedios)
	        IF OLD.active_store_id IS NOT DISTINCT FROM NEW.active_store_id THEN
	            -- active_store_id no cambió → permitir el UPDATE sin validar
	            RETURN NEW;
	        END IF;
	
	        -- active_store_id cambió → validar consistencia
	        IF NEW.active_store_id IS NULL THEN
	            IF v_role IN ('encargado'::user_role, 'clerk'::user_role, 'warehouse'::user_role) THEN
	                RAISE EXCEPTION 'ERR_STORE_REQUIRED: El rol % requiere una tienda activa asignada.', v_role;
	            END IF;
	        ELSE
	            IF NOT EXISTS (
	                SELECT 1 FROM public.user_store_memberships
	                WHERE user_id = NEW.id
	                  AND store_id = NEW.active_store_id
	                  AND status = 'active'
	            ) THEN
	                RAISE EXCEPTION 'ERR_INVALID_ACTIVE_STORE: El usuario no tiene membership activa en la tienda seleccionada.';
	            END IF;
	        END IF;
	        RETURN NEW;
	    END IF;
	END;
	$$ LANGUAGE plpgsql SECURITY DEFINER
2026-09-13 06:16:30.547 UTC [21216] ERROR:  type "public.user_role" does not exist at character 270
2026-09-13 06:16:30.547 UTC [21216] STATEMENT:  CREATE OR REPLACE FUNCTION public.validate_tenant_access(
	  p_user_id uuid,
	  p_store_id uuid
	)
	RETURNS boolean
	LANGUAGE plpgsql
	STABLE SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  v_user_tenant uuid;
	  v_store_tenant uuid;
	  v_user_role public.user_role;
	BEGIN
	  -- Global admin bypasses tenant check
	  SELECT role, tenant_id INTO v_user_role, v_user_tenant
	    FROM public.profiles WHERE id = p_user_id;
	
	  IF v_user_role IN ('admin', 'superadmin') THEN
	    RETURN true;
	  END IF;
	
	  -- Get store's tenant
	  SELECT tenant_id INTO v_store_tenant FROM public.stores WHERE id = p_store_id;
	
	  IF v_store_tenant IS NULL THEN
	    -- Legacy store without tenant — allow (backward compat)
	    RETURN true;
	  END IF;
	
	  -- Check tenant match
	  RETURN v_user_tenant = v_store_tenant;
	END;
	$function$
2026-09-13 06:16:30.987 UTC [21279] ERROR:  type "public.user_role" does not exist at character 301
2026-09-13 06:16:30.987 UTC [21279] STATEMENT:  CREATE OR REPLACE FUNCTION public.bulk_assign_memberships(
	  p_user_id uuid,
	  p_assignments jsonb
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  v_assignment jsonb;
	  v_affected int := 0;
	  v_failed int := 0;
	  v_store_id uuid;
	  v_role public.user_role;
	  v_status text;
	  v_caller_uid uuid := auth.uid();
	  v_changes jsonb := '[]'::jsonb;
	BEGIN
	  IF v_caller_uid IS NULL THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED';
	  END IF;
	
	  FOR v_assignment IN SELECT * FROM jsonb_array_elements(p_assignments) LOOP
	    BEGIN
	      v_store_id := (v_assignment->>'store_id')::uuid;
	      v_role := (v_assignment->>'role')::public.user_role;
	      v_status := COALESCE(v_assignment->>'status', 'active');
	
	      -- Validar caller tiene acceso al store
	      IF NOT public.is_admin() AND NOT public.has_store_role(v_store_id, ARRAY['admin', 'manager']) THEN
	        v_failed := v_failed + 1;
	        CONTINUE;
	      END IF;
	
	      INSERT INTO public.user_store_memberships (user_id, store_id, role, status)
	      VALUES (p_user_id, v_store_id, v_role, v_status)
	      ON CONFLICT (user_id, store_id) DO UPDATE SET
	        role = EXCLUDED.role,
	        status = EXCLUDED.status,
	        updated_at = now();
	
	      v_changes := v_changes || jsonb_build_object(jsonb_build_object(
	        'store_id', v_store_id,
	        'role', v_role::text,
	        'status', v_status
	      ));
	
	      v_affected := v_affected + 1;
	    EXCEPTION
	      WHEN foreign_key_violation THEN
	        v_failed := v_failed + 1;
	    END;
	  END LOOP;
	
	  -- Audit log atómico (solo si hubo cambios)
	  IF v_affected > 0 THEN
	    INSERT INTO public.user_audit_log (performed_by, target_user_id, action, new_values, metadata)
	    VALUES (
	      v_caller_uid, p_user_id, 'MEMBERSHIPS_BULK_ASSIGNED',
	      jsonb_build_object('assignments', v_changes),
	      jsonb_build_object('affected', v_affected, 'failed', v_failed)
	    );
	  END IF;
	
	  RETURN jsonb_build_object(
	    'success', true,
	    'affected', v_affected,
	    'failed', v_failed,
	    'user_id', p_user_id
	  );
	END;
	$function$
2026-09-13 06:16:31.033 UTC [21282] ERROR:  syntax error at or near "REVOKE" at character 5039
2026-09-13 06:16:31.033 UTC [21282] STATEMENT:  CREATE OR REPLACE FUNCTION public.bulk_soft_delete_stores(p_store_ids uuid[], p_deleted_by uuid, p_confirmation_token text, p_override_token text DEFAULT NULL::text, p_reason text DEFAULT NULL::text)
	 RETURNS jsonb
	 LANGUAGE plpgsql
	 SECURITY DEFINER
	 SET search_path TO 'public'
	AS $function$
	DECLARE
	  v_store_id UUID;
	  v_validation JSONB;
	  v_blockers JSONB;
	  v_errors JSONB[] := '{}'::jsonb[];
	  v_processed INTEGER := 0;
	  v_token_valid BOOLEAN;
	  v_has_protected BOOLEAN;
	  v_override_valid BOOLEAN;
	  v_confirmation_record RECORD;
	  v_override_record RECORD;
	  v_caller_role TEXT;
	BEGIN
	  -- ============================================================
	  -- AUTH CHECK: Solo admin puede ejecutar esta función
	  -- ============================================================
	  IF auth.uid() IS NOT NULL THEN
	    SELECT role INTO v_caller_role FROM public.profiles WHERE id = auth.uid();
	    IF v_caller_role IS NULL OR v_caller_role != 'admin' THEN
	      RAISE EXCEPTION 'ERR_PERMISSION_DENIED: Solo admin puede ejecutar bulk_soft_delete_stores';
	    END IF;
	  END IF;
	  -- Si auth.uid() IS NULL, es service_role — permitir
	
	  -- ============================================================
	  -- 1. VALIDATE confirmation_token
	  -- ============================================================
	  SELECT * INTO v_confirmation_record
	  FROM public.bulk_confirmation_tokens
	  WHERE token = p_confirmation_token
	    AND action = 'delete'
	    AND expires_at > NOW()
	    AND consumed_at IS NULL
	    AND is_override = false;
	
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_INVALID_CONFIRMATION_TOKEN';
	  END IF;
	
	  IF v_confirmation_record.store_ids != p_store_ids THEN
	    RAISE EXCEPTION 'ERR_STORE_IDS_MISMATCH';
	  END IF;
	
	  -- ============================================================
	  -- 2. VALIDATE tiendas protegidas requieren override_token
	  -- ============================================================
	  SELECT EXISTS(
	    SELECT 1 FROM public.stores
	    WHERE id = ANY(p_store_ids) AND backup_restore_protected = true
	  ) INTO v_has_protected;
	
	  IF v_has_protected THEN
	    IF p_override_token IS NULL THEN
	      RAISE EXCEPTION 'ERR_OVERRIDE_REQUIRED';
	    END IF;
	
	    SELECT * INTO v_override_record
	    FROM public.bulk_confirmation_tokens
	    WHERE token = p_override_token
	      AND is_override = true
	      AND override_for = p_confirmation_token
	      AND expires_at > NOW()
	      AND consumed_at IS NULL;
	
	    IF NOT FOUND THEN
	      RAISE EXCEPTION 'ERR_INVALID_OVERRIDE_TOKEN';
	    END IF;
	
	    IF v_override_record.store_ids != v_confirmation_record.store_ids THEN
	      RAISE EXCEPTION 'ERR_OVERRIDE_STORE_IDS_MISMATCH';
	    END IF;
	
	    IF v_override_record.created_by = v_confirmation_record.created_by THEN
	      RAISE EXCEPTION 'ERR_SAME_USER_OVERRIDE';
	    END IF;
	
	    UPDATE public.bulk_confirmation_tokens SET consumed_at = NOW()
	    WHERE token = p_override_token;
	  END IF;
	
	  UPDATE public.bulk_confirmation_tokens SET consumed_at = NOW()
	  WHERE token = p_confirmation_token;
	
	  -- ============================================================
	  -- 3. VALIDATE todas las tiendas
	  -- ============================================================
	  FOREACH v_store_id IN ARRAY p_store_ids LOOP
	    IF NOT EXISTS(SELECT 1 FROM public.stores WHERE id = v_store_id AND is_active = true) THEN
	      v_errors := array_append(v_errors, jsonb_build_object(
	        'store_id', v_store_id, 'reason', 'STORE_NOT_FOUND_OR_INACTIVE'
	      ));
	      CONTINUE;
	    END IF;
	
	    SELECT public.validate_store_can_be_modified(v_store_id, 'soft_delete') INTO v_validation;
	    v_blockers := v_validation->'blockers';
	
	    IF v_validation->>'can_modify' != 'true' THEN
	      v_errors := array_append(v_errors, jsonb_build_object(
	        'store_id', v_store_id, 'reason', 'HAS_BLOCKING_DEPENDENCIES', 'blockers', v_blockers
	      ));
	    END IF;
	  END LOOP;
	
	  IF array_length(v_errors, 1) IS NOT NULL THEN
	    RETURN jsonb_build_object(
	      'status', 'FAILED', 'processed', 0,
	      'total_requested', array_length(p_store_ids, 1),
	      'errors', to_jsonb(v_errors), 'reason', p_reason
	    );
	  END IF;
	
	  -- ============================================================
	  -- 4. EXECUTE
	  -- ============================================================
	  FOREACH v_store_id IN ARRAY p_store_ids LOOP
	    PERFORM public.soft_delete_store(v_store_id, p_deleted_by);
	    v_processed := v_processed + 1;
	  END LOOP;
	
	  INSERT INTO public.audit_logs (action, table_name, record_id, metadata)
	  VALUES (
	    'bulk_store_deleted', 'stores', NULL,
	    jsonb_build_object(
	      'store_ids', p_store_ids, 'deleted_by', p_deleted_by,
	      'reason', p_reason, 'processed', v_processed,
	      'had_protected_stores', v_has_protected,
	      'override_used', p_override_token IS NOT NULL, 'deleted_at', NOW()
	    )
	  );
	
	  RETURN jsonb_build_object(
	    'status', 'COMPLETED', 'processed', v_processed,
	    'total_requested', array_length(p_store_ids, 1),
	    'errors', '[]'::jsonb, 'reason', p_reason
	  );
	END;
	$function$
	 
	REVOKE EXECUTE ON FUNCTION public.bulk_soft_delete_stores FROM PUBLIC, anon, authenticated
2026-09-13 06:16:31.115 UTC [21289] ERROR:  relation "received_services" does not exist
2026-09-13 06:16:31.115 UTC [21289] CONTEXT:  compilation of PL/pgSQL function "calculate_service_distribution" near line 3
2026-09-13 06:16:31.115 UTC [21289] STATEMENT:  CREATE OR REPLACE FUNCTION calculate_service_distribution(
	  p_service_id UUID
	) RETURNS TABLE(
	  receipt_item_id UUID,
	  product_id UUID,
	  distribution_amount NUMERIC,
	  distribution_percentage NUMERIC
	) LANGUAGE plpgsql SECURITY DEFINER AS $$
	DECLARE
	  v_service received_services%ROWTYPE;
	  v_method TEXT;
	  v_total_amount NUMERIC;
	  v_total_value NUMERIC DEFAULT 0;
	  v_total_qty NUMERIC DEFAULT 0;
	  v_link service_reception_links%ROWTYPE;
	  v_receipt_id UUID;
	  v_item receipt_items%ROWTYPE;
	  v_allocated NUMERIC;
	BEGIN
	  SELECT * INTO v_service FROM received_services WHERE id = p_service_id;
	  IF NOT FOUND THEN RETURN; END IF;
	  
	  v_method := v_service.distribution_method;
	  v_total_amount := v_service.total_amount;
	
	  -- Para cada recepción vinculada
	  FOR v_link IN SELECT * FROM service_reception_links WHERE service_id = p_service_id AND allocated_amount > 0 LOOP
	    v_receipt_id := v_link.receipt_id;
	    v_allocated := v_link.allocated_amount;
	
	    -- Calcular base de distribución según método
	    IF v_method = 'amount' THEN
	      SELECT COALESCE(SUM(quantity * unit_cost), 0) INTO v_total_value
	      FROM receipt_items WHERE receipt_id = v_receipt_id;
	      
	      IF v_total_value > 0 THEN
	        FOR v_item IN SELECT * FROM receipt_items WHERE receipt_id = v_receipt_id LOOP
	          distribution_amount := v_allocated * (v_item.quantity * v_item.unit_cost / v_total_value);
	          distribution_percentage := (v_item.quantity * v_item.unit_cost / v_total_value) * 100;
	          receipt_item_id := v_item.id;
	          product_id := v_item.product_id;
	          RETURN NEXT;
	        END LOOP;
	      END IF;
	
	    ELSIF v_method = 'quantity' THEN
	      SELECT COALESCE(SUM(quantity), 0) INTO v_total_qty
	      FROM receipt_items WHERE receipt_id = v_receipt_id;
	      
	      IF v_total_qty > 0 THEN
	        FOR v_item IN SELECT * FROM receipt_items WHERE receipt_id = v_receipt_id LOOP
	          distribution_amount := v_allocated * (v_item.quantity / v_total_qty);
	          distribution_percentage := (v_item.quantity / v_total_qty) * 100;
	          receipt_item_id := v_item.id;
	          product_id := v_item.product_id;
	          RETURN NEXT;
	        END LOOP;
	      END IF;
	
	    ELSIF v_method = 'manual' THEN
	      -- En manual, la distribución se guarda directamente desde el cliente
	      RETURN;
	    END IF;
	  END LOOP;
	END;
	$$
2026-09-13 06:16:31.177 UTC [21301] ERROR:  syntax error at or near "REVOKE" at character 678
2026-09-13 06:16:31.177 UTC [21301] STATEMENT:  CREATE OR REPLACE FUNCTION public.check_bulk_ops_hourly_limit(p_user_id uuid, p_plan text)
	 RETURNS jsonb
	 LANGUAGE plpgsql
	 STABLE SECURITY DEFINER
	 SET search_path TO 'public'
	AS $function$
	DECLARE
	  v_limit INTEGER;
	  v_used INTEGER;
	BEGIN
	  v_limit := CASE p_plan
	    WHEN 'free' THEN 1
	    WHEN 'pro' THEN 20
	    WHEN 'enterprise' THEN 999999
	    ELSE 1
	  END;
	
	  SELECT COUNT(*) INTO v_used
	  FROM public.bulk_ops_log
	  WHERE user_id = p_user_id
	    AND initiated_at > NOW() - INTERVAL '1 hour';
	
	  RETURN jsonb_build_object(
	    'allowed', v_used < v_limit,
	    'used', v_used,
	    'limit', v_limit,
	    'remaining', GREATEST(0, v_limit - v_used)
	  );
	END;
	$function$
	 
	REVOKE EXECUTE ON FUNCTION public.check_bulk_ops_hourly_limit FROM PUBLIC, anon, authenticated
2026-09-13 06:16:31.204 UTC [21306] ERROR:  type plan_t does not exist
2026-09-13 06:16:31.204 UTC [21306] STATEMENT:  CREATE OR REPLACE FUNCTION public.check_tenant_store_quota(
	  p_tenant_id uuid,
	  p_plan plan_t DEFAULT NULL
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  v_plan plan_t;
	  v_limit int;
	  v_current int;
	  v_allowed boolean;
	BEGIN
	  -- Get plan from tenant if not passed
	  IF p_plan IS NULL THEN
	    SELECT plan INTO v_plan FROM public.tenants WHERE id = p_tenant_id;
	  ELSE
	    v_plan := p_plan;
	  END IF;
	
	  -- Plan limits (unified source of truth)
	  v_limit := CASE v_plan
	    WHEN 'free'::plan_t THEN 1
	    WHEN 'pro'::plan_t THEN 3
	    WHEN 'enterprise'::plan_t THEN 10
	    ELSE 1
	  END;
	
	  -- Count active stores in tenant
	  SELECT COUNT(*) INTO v_current
	    FROM public.stores
	    WHERE tenant_id = p_tenant_id AND is_active = true;
	
	  v_allowed := v_current < v_limit;
	
	  RETURN jsonb_build_object(
	    'allowed', v_allowed,
	    'current', v_current,
	    'limit', v_limit,
	    'plan', v_plan::text,
	    'tenant_id', p_tenant_id
	  );
	END;
	$function$
2026-09-13 06:16:31.313 UTC [21333] ERROR:  relation "public.profiles" does not exist at character 166
2026-09-13 06:16:31.313 UTC [21333] STATEMENT:  CREATE OR REPLACE FUNCTION public.current_user_tenant_id()
	RETURNS uuid
	LANGUAGE sql
	STABLE
	SECURITY DEFINER
	SET search_path TO public
	AS $$
	  SELECT tenant_id FROM public.profiles WHERE id = auth.uid();
	$$
2026-09-13 06:16:31.349 UTC [21339] ERROR:  relation "received_services" does not exist
2026-09-13 06:16:31.349 UTC [21339] CONTEXT:  compilation of PL/pgSQL function "distribute_service_cost_v2" near line 6
2026-09-13 06:16:31.349 UTC [21339] STATEMENT:  CREATE OR REPLACE FUNCTION public.distribute_service_cost_v2(
	  p_service_id uuid,
	  p_user_id uuid DEFAULT NULL
	) RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO 'public', 'extensions'
	AS $func$
	DECLARE
	  v_store_id uuid;
	  v_status text;
	  v_total_amount numeric;
	  v_service received_services%ROWTYPE;
	  v_method text;
	  v_link service_reception_links%ROWTYPE;
	  v_item receipt_items%ROWTYPE;
	  v_total_value numeric := 0;
	  v_total_qty numeric := 0;
	  v_allocated numeric;
	  v_dist_count integer := 0;
	  v_caller_uid uuid := COALESCE(p_user_id, auth.uid());
	BEGIN
	  PERFORM pg_advisory_xact_lock(hashtext(p_service_id::text));
	
	  SELECT * INTO v_service FROM received_services WHERE id = p_service_id FOR UPDATE;
	  IF NOT FOUND THEN RAISE EXCEPTION 'ERR_SERVICE_NOT_FOUND'; END IF;
	
	  v_store_id := v_service.store_id;
	  v_status := v_service.status;
	  v_total_amount := v_service.total_amount;
	  v_method := v_service.distribution_method;
	
	  IF NOT public.has_store_access(v_store_id) THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED';
	  END IF;
	
	  IF v_status != 'active' THEN
	    RAISE EXCEPTION 'ERR_SERVICE_NOT_ACTIVE: status % is not active', v_status;
	  END IF;
	
	  IF v_method = 'manual' THEN
	    RAISE EXCEPTION 'ERR_MANUAL_METHOD: use link_receipts_to_service for manual distribution';
	  END IF;
	
	  DELETE FROM service_cost_distributions WHERE service_id = p_service_id;
	
	  FOR v_link IN SELECT * FROM service_reception_links WHERE service_id = p_service_id AND allocated_amount > 0 ORDER BY receipt_id LOOP
	    v_allocated := v_link.allocated_amount;
	
	    IF v_method = 'amount' THEN
	      SELECT COALESCE(SUM(quantity * unit_cost), 0) INTO v_total_value
	      FROM receipt_items WHERE receipt_id = v_link.receipt_id;
	      IF v_total_value > 0 THEN
	        FOR v_item IN SELECT * FROM receipt_items WHERE receipt_id = v_link.receipt_id ORDER BY id LOOP
	          INSERT INTO service_cost_distributions
	            (service_id, receipt_id, receipt_item_id, product_id, distribution_amount, distribution_percentage)
	          VALUES
	            (p_service_id, v_link.receipt_id, v_item.id, v_item.product_id,
	             v_allocated * (v_item.quantity * v_item.unit_cost / v_total_value),
	             (v_item.quantity * v_item.unit_cost / v_total_value) * 100);
	          v_dist_count := v_dist_count + 1;
	        END LOOP;
	      END IF;
	
	    ELSIF v_method = 'quantity' THEN
	      SELECT COALESCE(SUM(quantity), 0) INTO v_total_qty
	      FROM receipt_items WHERE receipt_id = v_link.receipt_id;
	      IF v_total_qty > 0 THEN
	        FOR v_item IN SELECT * FROM receipt_items WHERE receipt_id = v_link.receipt_id ORDER BY id LOOP
	          INSERT INTO service_cost_distributions
	            (service_id, receipt_id, receipt_item_id, product_id, distribution_amount, distribution_percentage)
	          VALUES
	            (p_service_id, v_link.receipt_id, v_item.id, v_item.product_id,
	             v_allocated * (v_item.quantity / v_total_qty),
	             (v_item.quantity / v_total_qty) * 100);
	          v_dist_count := v_dist_count + 1;
	        END LOOP;
	      END IF;
	    END IF;
	  END LOOP;
	
	  -- Forzar recalculo de WAC para cada producto afectado
	  UPDATE receipt_items SET updated_at = NOW()
	  WHERE product_id IN (SELECT DISTINCT product_id FROM service_cost_distributions WHERE service_id = p_service_id);
	
	  INSERT INTO audit_logs (user_id, store_id, action, table_name, record_id, metadata)
	  VALUES (v_caller_uid, v_store_id, 'SERVICE_DISTRIBUTED', 'received_services', p_service_id,
	    jsonb_build_object('rows_distributed', v_dist_count, 'method', v_method, 'total_amount', v_total_amount));
	
	  RETURN jsonb_build_object('status', 'success', 'distributed_rows', v_dist_count);
	END;
	$func$
2026-09-13 06:16:31.395 UTC [21344] ERROR:  type "user_role" does not exist at character 116
2026-09-13 06:16:31.395 UTC [21344] STATEMENT:  CREATE OR REPLACE FUNCTION public.enforce_encargado_store_limit()
	RETURNS TRIGGER AS $$
	DECLARE
	    v_creator_role user_role;
	    v_limit integer;
	    v_current_count integer;
	BEGIN
	    IF NEW.created_by IS NULL THEN RETURN NEW; END IF;
	
	    SELECT role, max_stores_limit INTO v_creator_role, v_limit
	    FROM public.profiles WHERE id = NEW.created_by;
	
	    IF v_creator_role = 'encargado' THEN
	        SELECT COUNT(*) INTO v_current_count FROM public.stores WHERE created_by = NEW.created_by;
	        IF v_current_count >= v_limit THEN
	            RAISE EXCEPTION 'ERR_STORE_LIMIT_EXCEEDED: Maximum number of stores reached for this manager (%/%)', v_current_count, v_limit;
	        END IF;
	    END IF;
	    RETURN NEW;
	END;
	$$ LANGUAGE plpgsql
2026-09-13 06:16:31.419 UTC [21347] ERROR:  type "user_role" does not exist at character 115
2026-09-13 06:16:31.419 UTC [21347] STATEMENT:  CREATE OR REPLACE FUNCTION public.enforce_encargado_user_limit()
	RETURNS TRIGGER AS $$
	DECLARE
	    v_creator_role user_role;
	    v_limit integer;
	    v_current_count integer;
	BEGIN
	    IF NEW.created_by IS NULL THEN RETURN NEW; END IF;
	
	    SELECT role, max_users_limit INTO v_creator_role, v_limit
	    FROM public.profiles WHERE id = NEW.created_by;
	
	    IF v_creator_role = 'encargado' THEN
	        SELECT COUNT(*) INTO v_current_count FROM public.profiles WHERE created_by = NEW.created_by;
	        IF v_current_count >= v_limit THEN
	            RAISE EXCEPTION 'ERR_USER_LIMIT_EXCEEDED: Maximum number of users reached for this manager (%/%)', v_current_count, v_limit;
	        END IF;
	    END IF;
	    RETURN NEW;
	END;
	$$ LANGUAGE plpgsql
2026-09-13 06:16:31.476 UTC [21360] ERROR:  syntax error at or near "REVOKE" at character 1436
2026-09-13 06:16:31.476 UTC [21360] STATEMENT:  CREATE OR REPLACE FUNCTION public.generate_bulk_confirmation_token(p_store_ids uuid[], p_action text, p_user_id uuid)
	 RETURNS text
	 LANGUAGE plpgsql
	 SECURITY DEFINER
	 SET search_path TO 'public'
	AS $function$
	DECLARE
	  v_token TEXT;
	  v_has_protected BOOLEAN;
	  v_caller_role TEXT;
	BEGIN
	  -- AUTH CHECK: Solo admin
	  IF auth.uid() IS NOT NULL THEN
	    SELECT role INTO v_caller_role FROM public.profiles WHERE id = auth.uid();
	    IF v_caller_role IS NULL OR v_caller_role != 'admin' THEN
	      RAISE EXCEPTION 'ERR_PERMISSION_DENIED: Solo admin puede generar tokens bulk';
	    END IF;
	    -- Verificar que p_user_id coincide con auth.uid()
	    IF p_user_id != auth.uid() THEN
	      RAISE EXCEPTION 'ERR_PERMISSION_DENIED: p_user_id debe coincidir con el usuario autenticado';
	    END IF;
	  END IF;
	
	  IF p_action NOT IN ('delete', 'archive') THEN
	    RAISE EXCEPTION 'ERR_NON_DESTRUCTIVE_ACTION';
	  END IF;
	
	  SELECT EXISTS(
	    SELECT 1 FROM public.stores
	    WHERE id = ANY(p_store_ids) AND backup_restore_protected = true
	  ) INTO v_has_protected;
	
	  v_token := 'bct_' || replace(gen_random_uuid()::text, '-', '');
	
	  INSERT INTO public.bulk_confirmation_tokens (
	    token, store_ids, action, created_by, expires_at, metadata
	  ) VALUES (
	    v_token, p_store_ids, p_action, p_user_id,
	    NOW() + INTERVAL '10 minutes',
	    jsonb_build_object('has_protected_stores', v_has_protected)
	  );
	
	  RETURN v_token;
	END;
	$function$
	 
	REVOKE EXECUTE ON FUNCTION public.generate_bulk_confirmation_token FROM PUBLIC, anon, authenticated
2026-09-13 06:16:31.496 UTC [21363] ERROR:  syntax error at or near "REVOKE" at character 2432
2026-09-13 06:16:31.496 UTC [21363] STATEMENT:  CREATE OR REPLACE FUNCTION public.generate_bulk_override_token(p_confirmation_token text, p_override_user_id uuid, p_reason text DEFAULT NULL::text)
	 RETURNS text
	 LANGUAGE plpgsql
	 SECURITY DEFINER
	 SET search_path TO 'public'
	AS $function$
	DECLARE
	  v_original RECORD;
	  v_override_token TEXT;
	  v_override_user_role TEXT;
	BEGIN
	  -- AUTH CHECK: Solo admin, y debe ser auth.uid() == p_override_user_id
	  IF auth.uid() IS NOT NULL THEN
	    IF auth.uid() != p_override_user_id THEN
	      RAISE EXCEPTION 'ERR_PERMISSION_DENIED: p_override_user_id debe coincidir con el usuario autenticado';
	    END IF;
	
	    SELECT role INTO v_override_user_role FROM public.profiles WHERE id = p_override_user_id;
	    IF v_override_user_role IS NULL OR v_override_user_role != 'admin' THEN
	      RAISE EXCEPTION 'ERR_PERMISSION_DENIED: Solo admin puede generar override';
	    END IF;
	  END IF;
	
	  SELECT * INTO v_original
	  FROM public.bulk_confirmation_tokens
	  WHERE token = p_confirmation_token
	    AND consumed_at IS NULL
	    AND expires_at > NOW()
	    AND is_override = false;
	
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_INVALID_OR_EXPIRED_TOKEN';
	  END IF;
	
	  IF v_original.created_by = p_override_user_id THEN
	    RAISE EXCEPTION 'ERR_SAME_USER_OVERRIDE';
	  END IF;
	
	  IF EXISTS(
	    SELECT 1 FROM public.bulk_confirmation_tokens
	    WHERE override_for = p_confirmation_token
	      AND consumed_at IS NULL AND expires_at > NOW()
	  ) THEN
	    RAISE EXCEPTION 'ERR_OVERRIDE_ALREADY_EXISTS';
	  END IF;
	
	  v_override_token := 'bot_' || replace(gen_random_uuid()::text, '-', '');
	
	  INSERT INTO public.bulk_confirmation_tokens (
	    token, store_ids, action, created_by, expires_at,
	    is_override, override_for, metadata
	  ) VALUES (
	    v_override_token, v_original.store_ids, v_original.action, p_override_user_id,
	    NOW() + INTERVAL '10 minutes', true, p_confirmation_token,
	    jsonb_build_object('override_reason', p_reason,
	      'original_created_by', v_original.created_by,
	      'override_created_by', p_override_user_id)
	  );
	
	  INSERT INTO public.audit_logs (action, table_name, record_id, metadata)
	  VALUES (
	    'bulk_override_token_generated', 'stores', NULL,
	    jsonb_build_object('confirmation_token', p_confirmation_token,
	      'store_ids', v_original.store_ids,
	      'override_by', p_override_user_id,
	      'original_by', v_original.created_by, 'reason', p_reason)
	  );
	
	  RETURN v_override_token;
	END;
	$function$
	 
	REVOKE EXECUTE ON FUNCTION public.generate_bulk_override_token FROM PUBLIC, anon, authenticated
2026-09-13 06:16:31.533 UTC [21371] ERROR:  relation "public.backup_table_registry" does not exist at character 581
2026-09-13 06:16:31.533 UTC [21371] STATEMENT:  CREATE OR REPLACE FUNCTION public.get_backup_table_list(
	  p_include_excluded BOOLEAN DEFAULT FALSE
	)
	RETURNS TABLE (
	  table_name          TEXT,
	  tier                INTEGER,
	  filter_strategy     TEXT,
	  parent_table        TEXT,
	  parent_foreign_key  TEXT,
	  date_column         TEXT,
	  excluded_from_restore BOOLEAN,
	  exclude_reason      TEXT
	)
	LANGUAGE sql
	SECURITY DEFINER
	SET search_path = public
	AS $$
	  SELECT
	    table_name,
	    tier,
	    filter_strategy,
	    parent_table,
	    parent_foreign_key,
	    date_column,
	    excluded_from_restore,
	    exclude_reason
	  FROM public.backup_table_registry
	  WHERE (p_include_excluded OR excluded_from_restore = FALSE)
	  ORDER BY tier ASC, table_name ASC;
	$$
2026-09-13 06:16:31.570 UTC [21379] ERROR:  relation "v_global_operation_dates" does not exist at character 229
2026-09-13 06:16:31.570 UTC [21379] STATEMENT:  CREATE OR REPLACE FUNCTION public.get_global_max_operation_date(
	  p_store_id UUID DEFAULT NULL
	)
	RETURNS TIMESTAMP WITH TIME ZONE
	LANGUAGE sql
	SECURITY DEFINER
	SET search_path = public
	AS $$
	  SELECT MAX(operation_date)
	  FROM v_global_operation_dates
	  WHERE p_store_id IS NULL OR store_id = p_store_id;
	$$
2026-09-13 06:16:31.593 UTC [21382] ERROR:  relation "exchange_rates" does not exist at character 216
2026-09-13 06:16:31.593 UTC [21382] STATEMENT:  CREATE OR REPLACE FUNCTION get_latest_rate(p_currency TEXT, p_source TEXT)
	RETURNS TABLE(rate NUMERIC, rate_date DATE, captured_at TIMESTAMPTZ)
	LANGUAGE sql STABLE AS $$
	  SELECT rate, rate_date, captured_at
	  FROM exchange_rates
	  WHERE currency = p_currency AND source = p_source
	  ORDER BY rate_date DESC, captured_at DESC
	  LIMIT 1;
	$$
2026-09-13 06:16:31.617 UTC [21385] ERROR:  relation "public.products" does not exist at character 194
2026-09-13 06:16:31.617 UTC [21385] STATEMENT:  CREATE OR REPLACE FUNCTION public.get_low_stock_count(p_store_id uuid DEFAULT NULL)
	RETURNS bigint
	LANGUAGE sql
	SECURITY DEFINER
	SET search_path = public
	AS $$
	  SELECT COUNT(*)::bigint
	  FROM public.products
	  WHERE
	    (p_store_id IS NULL OR store_id = p_store_id)
	    AND is_active = true
	    AND stock_current > 0
	    AND min_stock IS NOT NULL
	    AND min_stock > 0
	    AND stock_current <= min_stock;
	$$
2026-09-13 06:16:31.652 UTC [21392] ERROR:  relation "receipt_items" does not exist at character 754
2026-09-13 06:16:31.652 UTC [21392] STATEMENT:  CREATE OR REPLACE FUNCTION get_product_cost_analysis(
	  p_product_id UUID,
	  p_store_id UUID
	) RETURNS TABLE(
	  receipt_id UUID,
	  receipt_date TIMESTAMPTZ,
	  quantity INT,
	  unit_cost FLOAT,
	  service_type TEXT,
	  service_amount NUMERIC,
	  total_cost NUMERIC,
	  unit_cost_final NUMERIC
	) LANGUAGE sql SECURITY DEFINER AS $$
	  SELECT 
	    ri.receipt_id,
	    r.reception_date,
	    ri.quantity,
	    ri.unit_cost,
	    st.name AS service_type,
	    COALESCE(scd.distribution_amount, 0) AS service_amount,
	    ri.unit_cost * ri.quantity + COALESCE(scd.distribution_amount, 0) AS total_cost,
	    CASE WHEN ri.quantity > 0 
	      THEN (ri.unit_cost * ri.quantity + COALESCE(scd.distribution_amount, 0)) / ri.quantity 
	      ELSE 0 END AS unit_cost_final
	  FROM receipt_items ri
	  JOIN receipts r ON r.id = ri.receipt_id
	  LEFT JOIN service_cost_distributions scd ON scd.receipt_item_id = ri.id
	  LEFT JOIN received_services rs ON rs.id = scd.service_id AND rs.status = 'active'
	  LEFT JOIN service_types st ON st.id = rs.service_type_id
	  WHERE ri.product_id = p_product_id
	    AND r.store_id = p_store_id
	    AND r.status = 'active'
	  ORDER BY r.reception_date DESC;
	$$
2026-09-13 06:16:31.684 UTC [21398] ERROR:  relation "exchange_rates" does not exist at character 252
2026-09-13 06:16:31.684 UTC [21398] STATEMENT:  CREATE OR REPLACE FUNCTION get_rate_history(
	  p_currency TEXT,
	  p_source TEXT,
	  p_days INT DEFAULT 30
	)
	RETURNS TABLE(rate_date DATE, rate NUMERIC, variation_daily NUMERIC)
	LANGUAGE sql STABLE AS $$
	  SELECT rate_date, rate, variation_daily
	  FROM exchange_rates
	  WHERE currency = p_currency AND source = p_source
	    AND rate_date >= CURRENT_DATE - p_days
	  ORDER BY rate_date ASC;
	$$
2026-09-13 06:16:31.707 UTC [21401] ERROR:  relation "exchange_rates" does not exist at character 363
2026-09-13 06:16:31.707 UTC [21401] STATEMENT:  CREATE OR REPLACE FUNCTION get_rate_variation(
	  p_currency TEXT,
	  p_source TEXT,
	  p_start_date DATE,
	  p_end_date DATE
	)
	RETURNS TABLE(
	  start_rate NUMERIC,
	  end_rate NUMERIC,
	  absolute_change NUMERIC,
	  percent_change NUMERIC,
	  daily_avg_growth NUMERIC,
	  monthly_avg_growth NUMERIC
	)
	LANGUAGE sql STABLE AS $$
	  WITH start_val AS (
	    SELECT rate FROM exchange_rates
	    WHERE currency = p_currency AND source = p_source AND rate_date <= p_start_date
	    ORDER BY rate_date DESC LIMIT 1
	  ),
	  end_val AS (
	    SELECT rate FROM exchange_rates
	    WHERE currency = p_currency AND source = p_source AND rate_date <= p_end_date
	    ORDER BY rate_date DESC LIMIT 1
	  )
	  SELECT
	    COALESCE(sv.rate, 0) AS start_rate,
	    COALESCE(ev.rate, 0) AS end_rate,
	    (COALESCE(ev.rate, 0) - COALESCE(sv.rate, 0)) AS absolute_change,
	    CASE WHEN sv.rate IS NOT NULL AND sv.rate > 0 THEN ((ev.rate - sv.rate) / sv.rate * 100) ELSE 0 END AS percent_change,
	    CASE WHEN sv.rate IS NOT NULL AND sv.rate > 0 AND ev.rate IS NOT NULL AND ev.rate > 0 AND p_end_date > p_start_date
	      THEN (POWER(ev.rate::FLOAT / sv.rate::FLOAT, 1.0 / EXTRACT(EPOCH FROM (p_end_date - p_start_date)) * 86400) - 1) * 100
	      ELSE 0 END AS daily_avg_growth,
	    CASE WHEN sv.rate IS NOT NULL AND sv.rate > 0 AND ev.rate IS NOT NULL AND ev.rate > 0 AND p_end_date > p_start_date
	      THEN (POWER(ev.rate::FLOAT / sv.rate::FLOAT, 30.44 / EXTRACT(EPOCH FROM (p_end_date - p_start_date)) * 86400) - 1) * 100
	      ELSE 0 END AS monthly_avg_growth
	  FROM start_val sv, end_val ev;
	$$
2026-09-13 06:16:31.764 UTC [21414] ERROR:  type "public.stores" does not exist
2026-09-13 06:16:31.764 UTC [21414] STATEMENT:  CREATE OR REPLACE FUNCTION public.get_transferable_stores(
	  p_user_id UUID,
	  p_current_store_id UUID
	)
	RETURNS SETOF public.stores
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO 'public'
	AS $$
	DECLARE
	  v_tenant_id UUID;
	BEGIN
	  -- 1. Obtener tenant_id de la tienda actual
	  SELECT tenant_id INTO v_tenant_id
	  FROM public.stores
	  WHERE id = p_current_store_id AND is_active = true;
	
	  -- 2. Devolver tiendas donde el caller tiene acceso (has_store_access_as)
	  --    EXCLUYENDO la tienda actual
	  --    Si tenant_id es NOT NULL, filtrar por mismo tenant.
	  --    Si tenant_id es NULL (legacy), mostrar todas las que el caller tiene acceso.
	  IF v_tenant_id IS NOT NULL THEN
	    RETURN QUERY
	    SELECT s.*
	    FROM public.stores s
	    WHERE s.tenant_id = v_tenant_id
	      AND s.id != p_current_store_id
	      AND s.is_active = true
	      AND s.is_archived = false
	      AND public.has_store_access_as(p_user_id, s.id)
	    ORDER BY s.name;
	  ELSE
	    -- Legacy: tiendas sin tenant_id — usar solo has_store_access_as
	    RETURN QUERY
	    SELECT s.*
	    FROM public.stores s
	    WHERE s.id != p_current_store_id
	      AND s.is_active = true
	      AND s.is_archived = false
	      AND public.has_store_access_as(p_user_id, s.id)
	    ORDER BY s.name;
	  END IF;
	END;
	$$
2026-09-13 06:16:31.810 UTC [21423] ERROR:  type user_role[] does not exist
2026-09-13 06:16:31.810 UTC [21423] STATEMENT:  CREATE OR REPLACE FUNCTION public.has_any_role(required_roles user_role[])
	 RETURNS boolean
	 LANGUAGE plpgsql
	 STABLE SECURITY DEFINER
	AS $$
	DECLARE
	    v_actual_role user_role;
	    r user_role;
	BEGIN
	    SELECT role INTO v_actual_role FROM public.profiles WHERE id = auth.uid();
	
	    FOREACH r IN ARRAY required_roles
	    LOOP
	        IF v_actual_role = r THEN RETURN true; END IF;
	        IF v_actual_role = 'encargado' AND r = 'manager' THEN RETURN true; END IF;
	        IF v_actual_role = 'usuario' AND (r = 'clerk' OR r = 'warehouse') THEN RETURN true; END IF;
	    END LOOP;
	
	    RETURN false;
	END;
	$$
2026-09-13 06:16:31.833 UTC [21426] ERROR:  relation "profiles" does not exist at character 461
2026-09-13 06:16:31.833 UTC [21426] STATEMENT:  CREATE OR REPLACE FUNCTION public.has_management_access_as(
	  p_user_id uuid,
	  p_store_id uuid
	) RETURNS boolean
	LANGUAGE sql SECURITY DEFINER
	SET search_path = public, pg_temp
	AS $$
	  -- service_role always passes
	  SELECT CASE WHEN auth.role() = 'service_role' THEN true
	    ELSE
	      -- Check if user has admin/manager/encargado role in their profile
	      -- OR has a membership with manager/admin role for the store
	      EXISTS (
	        SELECT 1 FROM profiles
	        WHERE id = p_user_id
	        AND role IN ('admin', 'manager', 'encargado')
	      )
	      OR EXISTS (
	        SELECT 1 FROM user_store_memberships
	        WHERE user_id = p_user_id
	        AND store_id = p_store_id
	        AND status = 'active'
	        AND role IN ('admin', 'manager')
	      )
	    END
	$$
2026-09-13 06:16:31.852 UTC [21429] ERROR:  type public.user_role does not exist
2026-09-13 06:16:31.852 UTC [21429] STATEMENT:  CREATE OR REPLACE FUNCTION public.has_role(p_user_id UUID, p_required_role public.user_role)
	 RETURNS boolean
	 LANGUAGE plpgsql
	 STABLE SECURITY DEFINER
	AS $$
	DECLARE
	    v_actual_role public.user_role;
	BEGIN
	    SELECT role INTO v_actual_role FROM public.profiles WHERE id = p_user_id;
	
	    IF v_actual_role IS NULL THEN
	        RETURN false;
	    END IF;
	
	    -- Compatibility mapping (mirroring the 1-arg version)
	    IF v_actual_role = 'encargado' AND p_required_role = 'manager' THEN RETURN true; END IF;
	    IF v_actual_role = 'usuario' AND (p_required_role = 'clerk' OR p_required_role = 'warehouse') THEN RETURN true; END IF;
	
	    RETURN v_actual_role = p_required_role;
	END;
	$$
2026-09-13 06:16:31.882 UTC [21434] ERROR:  relation "public.user_store_memberships" does not exist at character 279
2026-09-13 06:16:31.882 UTC [21434] STATEMENT:  CREATE OR REPLACE FUNCTION public.has_store_role(
	  p_user_id uuid,
	  p_store_id uuid,
	  p_roles text[]
	) RETURNS boolean
	LANGUAGE sql SECURITY DEFINER
	SET search_path = public, pg_temp
	AS $$
	  SELECT CASE WHEN auth.role() = 'service_role' THEN
	    EXISTS (
	      SELECT 1 FROM public.user_store_memberships m
	      WHERE m.user_id = p_user_id
	        AND m.store_id = p_store_id
	        AND m.status = 'active'
	        AND m.role::text = ANY(p_roles)
	    )
	  ELSE
	    EXISTS (
	      SELECT 1 FROM public.user_store_memberships m
	      WHERE m.user_id = auth.uid()
	        AND m.store_id = p_store_id
	        AND m.status = 'active'
	        AND m.role::text = ANY(p_roles)
	    )
	  END
	$$
2026-09-13 06:16:31.916 UTC [21440] ERROR:  relation "public.profiles" does not exist at character 173
2026-09-13 06:16:31.916 UTC [21440] STATEMENT:  CREATE OR REPLACE FUNCTION public.is_global_admin()
	RETURNS BOOLEAN
	LANGUAGE sql
	STABLE
	SECURITY DEFINER
	SET search_path = public
	AS $$
	  SELECT EXISTS (
	    SELECT 1 FROM public.profiles
	    WHERE id = auth.uid()
	      AND role = 'admin'
	  );
	$$
2026-09-13 06:16:31.941 UTC [21444] ERROR:  type user_role does not exist
2026-09-13 06:16:31.941 UTC [21444] STATEMENT:  CREATE OR REPLACE FUNCTION public.is_role_not_changed(p_user_id uuid, p_new_role user_role, p_new_role_id uuid)
	RETURNS boolean
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path = public, extensions
	AS $$
	DECLARE
	  v_old_role user_role;
	  v_old_role_id uuid;
	BEGIN
	  SELECT role, role_id INTO v_old_role, v_old_role_id
	  FROM public.profiles
	  WHERE id = p_user_id;
	
	  RETURN (p_new_role IS NOT DISTINCT FROM v_old_role)
	     AND (p_new_role_id IS NOT DISTINCT FROM v_old_role_id);
	END;
	$$
2026-09-13 06:16:31.963 UTC [21448] ERROR:  relation "public.user_store_memberships" does not exist at character 188
2026-09-13 06:16:31.963 UTC [21448] STATEMENT:  CREATE OR REPLACE FUNCTION public.is_store_member(p_store_id UUID)
	RETURNS BOOLEAN
	LANGUAGE sql
	STABLE
	SECURITY DEFINER
	SET search_path = public
	AS $$
	  SELECT EXISTS (
	    SELECT 1 FROM public.user_store_memberships
	    WHERE store_id = p_store_id
	      AND user_id = auth.uid()
	      AND status = 'active'
	  );
	$$
2026-09-13 06:16:31.982 UTC [21451] ERROR:  function public.current_user_tenant_id() does not exist at character 209
2026-09-13 06:16:31.982 UTC [21451] HINT:  No function matches the given name and argument types. You might need to add explicit type casts.
2026-09-13 06:16:31.982 UTC [21451] STATEMENT:  CREATE OR REPLACE FUNCTION public.is_tenant_member(p_tenant_id uuid)
	RETURNS boolean
	LANGUAGE sql
	STABLE
	SECURITY DEFINER
	SET search_path TO public
	AS $$
	  SELECT p_tenant_id IS NOT NULL
	   AND p_tenant_id = public.current_user_tenant_id();
	$$
2026-09-13 06:16:32.048 UTC [21461] ERROR:  type user_role does not exist
2026-09-13 06:16:32.048 UTC [21461] STATEMENT:  CREATE OR REPLACE FUNCTION public.managed_create_user(p_email text, p_full_name text, p_role user_role, p_store_id uuid DEFAULT NULL::uuid, p_memberships jsonb DEFAULT NULL::jsonb, p_max_stores integer DEFAULT 0, p_max_users integer DEFAULT 0, p_target_user_id uuid DEFAULT NULL::uuid)
	 RETURNS jsonb
	 LANGUAGE plpgsql
	 SECURITY DEFINER
	 SET search_path TO 'public', 'extensions'
	AS $function$
	DECLARE
	    v_user_id uuid;
	    v_role_id uuid;
	    v_active_store_id uuid;
	    m JSONB;
	    v_creator_role user_role;
	BEGIN
	    SELECT role INTO v_creator_role FROM public.profiles WHERE id = auth.uid();
	    IF v_creator_role IS NULL OR v_creator_role NOT IN ('admin', 'encargado') THEN
	        RAISE EXCEPTION 'ERR_UNAUTHORIZED: Only admins and managers can create users.';
	    END IF;
	
	    v_active_store_id := COALESCE((p_memberships->0->>'store_id')::UUID, p_store_id);
	
	    IF v_creator_role = 'encargado' AND v_active_store_id IS NOT NULL THEN
	        IF NOT public.has_store_access(v_active_store_id) THEN
	            RAISE EXCEPTION 'ERR_UNAUTHORIZED: No access to store %', v_active_store_id;
	        END IF;
	    END IF;
	
	    SELECT id INTO v_role_id FROM public.roles WHERE lower(name) = lower(p_role::text) OR (name = 'Cajero' AND p_role = 'clerk') OR (name = 'Almacenero' AND p_role = 'warehouse') LIMIT 1;
	    v_user_id := COALESCE(p_target_user_id, gen_random_uuid());
	
	    INSERT INTO public.profiles (id, email, full_name, role, role_id, active_store_id, is_active, created_by, max_stores_limit, max_users_limit, created_at, updated_at)
	    VALUES (v_user_id, p_email, p_full_name, p_role, v_role_id, v_active_store_id, true, auth.uid(), p_max_stores, p_max_users, now(), now())
	    ON CONFLICT (id) DO UPDATE SET email = EXCLUDED.email, full_name = EXCLUDED.full_name, role = EXCLUDED.role, role_id = EXCLUDED.role_id, active_store_id = EXCLUDED.active_store_id, updated_at = now()
	    RETURNING id INTO v_user_id;
	
	    IF p_memberships IS NOT NULL THEN
	        IF v_creator_role != 'admin' THEN
	            DELETE FROM public.user_store_memberships WHERE user_id = v_user_id AND store_id IN (SELECT store_id FROM public.user_store_memberships WHERE user_id = auth.uid() AND role IN ('encargado', 'manager'));
	        ELSE
	            DELETE FROM public.user_store_memberships WHERE user_id = v_user_id;
	        END IF;
	        FOR m IN SELECT * FROM jsonb_array_elements(p_memberships) LOOP
	            IF (m->>'store_id') IS NOT NULL AND (m->>'store_id') <> '' THEN
	                IF v_creator_role = 'admin' OR public.has_store_access((m->>'store_id')::UUID) THEN
	                    INSERT INTO public.user_store_memberships (user_id, store_id, role) VALUES (v_user_id, (m->>'store_id')::UUID, (m->>'role')::user_role) ON CONFLICT (user_id, store_id) DO UPDATE SET role = EXCLUDED.role;
	                END IF;
	            END IF;
	        END LOOP;
	    ELSIF p_store_id IS NOT NULL THEN
	        INSERT INTO public.user_store_memberships (user_id, store_id, role) VALUES (v_user_id, p_store_id, p_role) ON CONFLICT (user_id, store_id) DO UPDATE SET role = EXCLUDED.role;
	    END IF;
	    RETURN jsonb_build_object('success', true, 'user_id', v_user_id);
	END;
	$function$
2026-09-13 06:16:32.066 UTC [21464] ERROR:  type public.user_role does not exist
2026-09-13 06:16:32.066 UTC [21464] STATEMENT:  CREATE OR REPLACE FUNCTION public.managed_create_user_v2(
	  p_email text,
	  p_full_name text,
	  p_role public.user_role,
	  p_plan plan_t DEFAULT 'free'::plan_t,
	  p_store_id uuid DEFAULT NULL::uuid,
	  p_memberships jsonb DEFAULT NULL::jsonb,
	  p_max_stores int DEFAULT 0,
	  p_max_users int DEFAULT 0,
	  p_target_user_id uuid DEFAULT NULL::uuid,
	  p_creator_id uuid DEFAULT NULL::uuid
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  v_user_id uuid := COALESCE(p_target_user_id, gen_random_uuid());
	  v_role_id uuid;
	  v_active_store_id uuid;
	  m JSONB;
	  v_creator_role public.user_role;
	  v_creator_uid uuid := COALESCE(p_creator_id, auth.uid());
	BEGIN
	  -- Validar caller es admin o encargado
	  SELECT role INTO v_creator_role FROM public.profiles WHERE id = v_creator_uid;
	  IF v_creator_role IS NULL OR v_creator_role NOT IN ('admin', 'encargado', 'superadmin') THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED: Only admins and managers can create users.';
	  END IF;
	
	  -- Validar email único (entre perfiles activos)
	  IF EXISTS (SELECT 1 FROM public.profiles WHERE email = p_email AND deleted_at IS NULL) THEN
	    RAISE EXCEPTION 'ERR_EMAIL_ALREADY_EXISTS: %', p_email;
	  END IF;
	
	  v_active_store_id := COALESCE((p_memberships->0->>'store_id')::UUID, p_store_id);
	
	  -- Encargado solo puede crear en tiendas que gestiona
	  IF v_creator_role = 'encargado' AND v_active_store_id IS NOT NULL THEN
	    IF NOT public.has_store_access(v_active_store_id) THEN
	      RAISE EXCEPTION 'ERR_UNAUTHORIZED: No access to store %', v_active_store_id;
	    END IF;
	  END IF;
	
	  -- Buscar role_id
	  SELECT id INTO v_role_id FROM public.roles
	    WHERE lower(name) = lower(p_role::text)
	       OR (name = 'Cajero' AND p_role = 'clerk')
	       OR (name = 'Almacenero' AND p_role = 'warehouse')
	       OR (name = 'UserCosto' AND p_role = 'costo')
	    LIMIT 1;
	
	  -- INSERT profiles con plan
	  INSERT INTO public.profiles (
	    id, email, full_name, role, role_id, active_store_id, is_active,
	    created_by, max_stores_limit, max_users_limit, plan, created_at, updated_at
	  ) VALUES (
	    v_user_id, p_email, p_full_name, p_role, v_role_id, v_active_store_id, true,
	    v_creator_uid, p_max_stores, p_max_users, p_plan, now(), now()
	  )
	  ON CONFLICT (id) DO UPDATE SET
	    email = EXCLUDED.email,
	    full_name = EXCLUDED.full_name,
	    role = EXCLUDED.role,
	    role_id = EXCLUDED.role_id,
	    active_store_id = EXCLUDED.active_store_id,
	    plan = EXCLUDED.plan,
	    is_active = true,
	    deleted_at = NULL,
	    deletion_reason = NULL,
	    deleted_by = NULL,
	    updated_at = now()
	  RETURNING id INTO v_user_id;
	
	  -- Procesar memberships
	  IF p_memberships IS NOT NULL THEN
	    IF v_creator_role != 'admin' AND v_creator_role != 'superadmin' THEN
	      DELETE FROM public.user_store_memberships
	        WHERE user_id = v_user_id
	        AND store_id IN (
	          SELECT store_id FROM public.user_store_memberships
	          WHERE user_id = v_creator_uid AND role IN ('encargado', 'manager')
	        );
	    ELSE
	      DELETE FROM public.user_store_memberships WHERE user_id = v_user_id;
	    END IF;
	
	    FOR m IN SELECT * FROM jsonb_array_elements(p_memberships) LOOP
	      IF (m->>'store_id') IS NOT NULL AND (m->>'store_id') <> '' THEN
	        IF v_creator_role IN ('admin', 'superadmin') OR public.has_store_access((m->>'store_id')::UUID) THEN
	          INSERT INTO public.user_store_memberships (user_id, store_id, role)
	          VALUES (v_user_id, (m->>'store_id')::UUID, (m->>'role')::public.user_role)
	          ON CONFLICT (user_id, store_id) DO UPDATE SET role = EXCLUDED.role, status = 'active';
	        END IF;
	      END IF;
	    END LOOP;
	  ELSIF p_store_id IS NOT NULL THEN
	    INSERT INTO public.user_store_memberships (user_id, store_id, role)
	    VALUES (v_user_id, p_store_id, p_role)
	    ON CONFLICT (user_id, store_id) DO UPDATE SET role = EXCLUDED.role, status = 'active';
	  END IF;
	
	  -- Audit log atómico (regresión C-2 restaurada)
	  INSERT INTO public.user_audit_log (performed_by, target_user_id, action, new_values, metadata)
	  VALUES (
	    v_creator_uid, v_user_id, 'USER_CREATED',
	    jsonb_build_object(
	      'email', p_email, 'full_name', p_full_name, 'role', p_role::text,
	      'plan', p_plan::text, 'active_store_id', v_active_store_id,
	      'max_stores', p_max_stores, 'max_users', p_max_users
	    ),
	    jsonb_build_object('memberships_count', CASE WHEN p_memberships IS NOT NULL THEN jsonb_array_length(p_memberships) ELSE 0 END)
	  );
	
	  RETURN jsonb_build_object('success', true, 'user_id', v_user_id);
	END;
	$function$
2026-09-13 06:16:32.109 UTC [21469] ERROR:  type "public.user_role" does not exist at character 392
2026-09-13 06:16:32.109 UTC [21469] STATEMENT:  CREATE OR REPLACE FUNCTION public.managed_reset_password(
	  p_user_id uuid,
	  p_caller_id uuid DEFAULT NULL::uuid
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  -- FIX H-7: anti-spoofing
	  v_caller_uid uuid := CASE WHEN auth.role() = 'service_role'
	    THEN COALESCE(p_caller_id, auth.uid()) ELSE auth.uid() END;
	  v_caller_role public.user_role;
	  v_target_email text;
	BEGIN
	  SELECT role INTO v_caller_role FROM public.profiles WHERE id = v_caller_uid;
	  IF v_caller_role IS NULL OR v_caller_role NOT IN ('admin', 'superadmin') THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED';
	  END IF;
	
	  IF p_user_id = v_caller_uid THEN
	    RAISE EXCEPTION 'ERR_SELF_RESET_BLOCKED';
	  END IF;
	
	  SELECT email INTO v_target_email FROM public.profiles WHERE id = p_user_id AND deleted_at IS NULL;
	  IF v_target_email IS NULL THEN
	    RAISE EXCEPTION 'ERR_USER_NOT_FOUND';
	  END IF;
	
	  INSERT INTO public.user_audit_log (performed_by, target_user_id, action, metadata)
	  VALUES (
	    v_caller_uid, p_user_id, 'PASSWORD_RESET_REQUESTED',
	    jsonb_build_object('email', v_target_email, 'method', 'recovery_link')
	  );
	
	  RETURN jsonb_build_object('success', true, 'email', v_target_email);
	END;
	$function$
2026-09-13 06:16:32.141 UTC [21473] ERROR:  type "public.user_role" does not exist at character 427
2026-09-13 06:16:32.141 UTC [21473] STATEMENT:  CREATE OR REPLACE FUNCTION public.managed_soft_delete_user(
	  p_user_id uuid,
	  p_reason text,
	  p_caller_id uuid DEFAULT NULL::uuid
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  v_old RECORD;
	  -- FIX H-7: anti-spoofing
	  v_caller_uid uuid := CASE WHEN auth.role() = 'service_role'
	    THEN COALESCE(p_caller_id, auth.uid()) ELSE auth.uid() END;
	  v_caller_role public.user_role;
	  v_active_memberships_count int;
	  v_anon_email text;
	BEGIN
	  SELECT role INTO v_caller_role FROM public.profiles WHERE id = v_caller_uid;
	  IF v_caller_role IS NULL OR v_caller_role NOT IN ('admin', 'superadmin') THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED';
	  END IF;
	
	  IF p_user_id = v_caller_uid THEN
	    RAISE EXCEPTION 'ERR_SELF_DELETE_BLOCKED';
	  END IF;
	
	  SELECT id, email, full_name, role, plan, is_active INTO v_old
	    FROM public.profiles WHERE id = p_user_id AND deleted_at IS NULL FOR UPDATE;
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_USER_NOT_FOUND_OR_ALREADY_DELETED';
	  END IF;
	
	  SELECT COUNT(*) INTO v_active_memberships_count
	    FROM public.user_store_memberships
	    WHERE user_id = p_user_id AND status = 'active';
	  IF v_active_memberships_count > 0 THEN
	    RAISE EXCEPTION 'ERR_USER_HAS_ACTIVE_MEMBERSHIPS: % active. Revoke memberships first.', v_active_memberships_count;
	  END IF;
	
	  v_anon_email := 'deleted+' || substr(p_user_id::text, 1, 8) || '@anonymized.local';
	
	  UPDATE public.profiles SET
	    deleted_at = now(),
	    deletion_reason = p_reason,
	    deleted_by = v_caller_uid,
	    is_active = false,
	    full_name = '[deleted user]',
	    email = v_anon_email,
	    ai_api_key = NULL,
	    updated_at = now()
	  WHERE id = p_user_id;
	
	  UPDATE public.user_store_memberships SET
	    status = 'revoked',
	    updated_at = now()
	  WHERE user_id = p_user_id AND status = 'active';
	
	  INSERT INTO public.user_audit_log (performed_by, target_user_id, action, old_values, new_values, metadata)
	  VALUES (
	    v_caller_uid, p_user_id, 'USER_SOFT_DELETED',
	    jsonb_build_object(
	      'email', v_old.email, 'full_name', v_old.full_name,
	      'role', v_old.role::text, 'plan', v_old.plan::text, 'is_active', v_old.is_active
	    ),
	    jsonb_build_object(
	      'email', v_anon_email, 'full_name', '[deleted user]',
	      'is_active', false, 'deleted_at', now()
	    ),
	    jsonb_build_object('reason', p_reason, 'memberships_revoked', v_active_memberships_count)
	  );
	
	  RETURN jsonb_build_object(
	    'success', true,
	    'user_id', p_user_id,
	    'status', 'soft_deleted',
	    'note', 'auth.users preserved. API route should ban via auth.admin.updateUser.'
	  );
	END;
	$function$
2026-09-13 06:16:32.169 UTC [21477] ERROR:  type "public.user_role" does not exist at character 435
2026-09-13 06:16:32.169 UTC [21477] STATEMENT:  CREATE OR REPLACE FUNCTION public.managed_toggle_user_status(
	  p_user_id uuid,
	  p_is_active boolean,
	  p_caller_id uuid DEFAULT NULL::uuid
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  v_old RECORD;
	  -- FIX H-7: anti-spoofing
	  v_caller_uid uuid := CASE WHEN auth.role() = 'service_role'
	    THEN COALESCE(p_caller_id, auth.uid()) ELSE auth.uid() END;
	  v_caller_role public.user_role;
	BEGIN
	  SELECT role INTO v_caller_role FROM public.profiles WHERE id = v_caller_uid;
	  IF v_caller_role IS NULL OR v_caller_role NOT IN ('admin', 'superadmin', 'encargado', 'manager') THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED';
	  END IF;
	
	  IF p_is_active = false AND p_user_id = v_caller_uid THEN
	    RAISE EXCEPTION 'ERR_SELF_DEACTIVATE_BLOCKED';
	  END IF;
	
	  SELECT id, is_active, full_name INTO v_old
	    FROM public.profiles WHERE id = p_user_id AND deleted_at IS NULL FOR UPDATE;
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_USER_NOT_FOUND';
	  END IF;
	
	  IF v_old.is_active = p_is_active THEN
	    RETURN jsonb_build_object('success', true, 'no_change', true);
	  END IF;
	
	  UPDATE public.profiles SET is_active = p_is_active, updated_at = now() WHERE id = p_user_id;
	
	  INSERT INTO public.user_audit_log (performed_by, target_user_id, action, old_values, new_values)
	  VALUES (
	    v_caller_uid, p_user_id,
	    CASE WHEN p_is_active THEN 'USER_ACTIVATED' ELSE 'USER_DEACTIVATED' END,
	    jsonb_build_object('is_active', v_old.is_active),
	    jsonb_build_object('is_active', p_is_active)
	  );
	
	  RETURN jsonb_build_object('success', true, 'user_id', p_user_id, 'is_active', p_is_active);
	END;
	$function$
2026-09-13 06:16:32.189 UTC [21480] ERROR:  type public.user_role does not exist
2026-09-13 06:16:32.189 UTC [21480] STATEMENT:  CREATE OR REPLACE FUNCTION public.managed_update_membership(
	  p_membership_id uuid,
	  p_role public.user_role DEFAULT NULL::user_role,
	  p_status text DEFAULT NULL::text,
	  p_caller_id uuid DEFAULT NULL::uuid
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  v_old RECORD;
	  -- FIX H-7: anti-spoofing
	  v_caller_uid uuid := CASE WHEN auth.role() = 'service_role'
	    THEN COALESCE(p_caller_id, auth.uid()) ELSE auth.uid() END;
	  v_changes jsonb := '{}'::jsonb;
	BEGIN
	  SELECT m.id, m.user_id, m.store_id, m.role, m.status
	    INTO v_old
	    FROM public.user_store_memberships m
	    WHERE m.id = p_membership_id
	    FOR UPDATE;
	
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_MEMBERSHIP_NOT_FOUND';
	  END IF;
	
	  -- FIX H-7: Autorización usa nueva sobrecarga has_store_role(p_user_id, p_store_id, p_roles)
	  -- service_role (API route) pasa p_caller_id=session.user.id → verifica acceso de ese user
	  -- authenticated pasa p_caller_id=NULL → v_caller_uid=auth.uid() → verifica su propio acceso
	  IF NOT public.has_store_role(v_caller_uid, v_old.store_id, ARRAY['admin', 'manager']) THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED: Caller must be admin or manager of the store.';
	  END IF;
	
	  IF p_role IS NOT NULL AND p_role <> v_old.role THEN
	    v_changes := v_changes || jsonb_build_object('role', jsonb_build_object('old', v_old.role::text, 'new', p_role::text));
	    UPDATE public.user_store_memberships SET role = p_role, updated_at = now() WHERE id = p_membership_id;
	  END IF;
	
	  IF p_status IS NOT NULL AND p_status <> v_old.status::text THEN
	    IF p_status NOT IN ('active', 'revoked') THEN
	      RAISE EXCEPTION 'ERR_INVALID_STATUS: %', p_status;
	    END IF;
	    v_changes := v_changes || jsonb_build_object('status', jsonb_build_object('old', v_old.status, 'new', p_status));
	    UPDATE public.user_store_memberships SET status = p_status::membership_status, updated_at = now() WHERE id = p_membership_id;
	  END IF;
	
	  IF v_changes <> '{}'::jsonb THEN
	    INSERT INTO public.user_audit_log (performed_by, target_user_id, action, old_values, new_values, metadata)
	    VALUES (
	      v_caller_uid, v_old.user_id, 'MEMBERSHIP_UPDATED',
	      jsonb_build_object('membership_id', p_membership_id, 'store_id', v_old.store_id, 'role', v_old.role::text, 'status', v_old.status),
	      v_changes,
	      jsonb_build_object('store_id', v_old.store_id)
	    );
	  END IF;
	
	  RETURN jsonb_build_object('success', true, 'membership_id', p_membership_id, 'changes', v_changes);
	END;
	$function$
2026-09-13 06:16:32.211 UTC [21483] ERROR:  type plan_t does not exist
2026-09-13 06:16:32.211 UTC [21483] STATEMENT:  CREATE OR REPLACE FUNCTION public.managed_update_tenant_plan(
	  p_tenant_id uuid,
	  p_plan plan_t,
	  p_subscription_status text DEFAULT NULL,
	  p_caller_id uuid DEFAULT NULL
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  -- FIX H-7: anti-spoofing — service_role (Stripe webhook) puede pasar NULL caller_id
	  v_caller_uid uuid := CASE WHEN auth.role() = 'service_role'
	    THEN COALESCE(p_caller_id, auth.uid()) ELSE auth.uid() END;
	  v_old_plan plan_t;
	  v_old_status text;
	  v_role public.user_role;
	BEGIN
	  -- Validación: si caller_uid es NULL (service_role anónimo para Stripe webhook), permitir
	  -- Si caller_uid NO es NULL, debe ser admin/superadmin
	  IF v_caller_uid IS NOT NULL THEN
	    SELECT role INTO v_role FROM public.profiles WHERE id = v_caller_uid;
	    IF v_role IS NULL OR v_role NOT IN ('admin', 'superadmin') THEN
	      RAISE EXCEPTION 'ERR_UNAUTHORIZED: Only admins can change tenant plan';
	    END IF;
	  END IF;
	
	  SELECT plan, subscription_status INTO v_old_plan, v_old_status
	    FROM public.tenants WHERE id = p_tenant_id FOR UPDATE;
	
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_TENANT_NOT_FOUND: %', p_tenant_id;
	  END IF;
	
	  UPDATE public.tenants SET
	    plan = p_plan,
	    subscription_status = COALESCE(p_subscription_status, subscription_status),
	    updated_at = now()
	  WHERE id = p_tenant_id;
	
	  UPDATE public.profiles SET
	    plan = p_plan,
	    updated_at = now()
	  WHERE tenant_id = p_tenant_id AND deleted_at IS NULL;
	
	  INSERT INTO public.user_audit_log (performed_by, target_user_id, action, old_values, new_values, metadata)
	  VALUES (
	    v_caller_uid, NULL, 'TENANT_PLAN_UPDATED',
	    jsonb_build_object('old_plan', v_old_plan::text, 'old_status', v_old_status),
	    jsonb_build_object('new_plan', p_plan::text, 'new_status', COALESCE(p_subscription_status, v_old_status)),
	    jsonb_build_object('tenant_id', p_tenant_id)
	  );
	
	  RETURN jsonb_build_object(
	    'success', true,
	    'tenant_id', p_tenant_id,
	    'old_plan', v_old_plan::text,
	    'new_plan', p_plan::text
	  );
	END;
	$function$
2026-09-13 06:16:32.233 UTC [21486] ERROR:  type public.user_role does not exist
2026-09-13 06:16:32.233 UTC [21486] STATEMENT:  CREATE OR REPLACE FUNCTION public.managed_update_user(
	  p_user_id uuid,
	  p_full_name text DEFAULT NULL::text,
	  p_role public.user_role DEFAULT NULL::user_role,
	  p_role_id uuid DEFAULT NULL::uuid,
	  p_is_active boolean DEFAULT NULL::boolean,
	  p_max_stores_limit int DEFAULT NULL::int,
	  p_max_users_limit int DEFAULT NULL::int,
	  p_plan plan_t DEFAULT NULL::plan_t,
	  p_caller_id uuid DEFAULT NULL::uuid
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  v_old RECORD;
	  -- FIX H-7: anti-spoofing — service_role puede pasar p_caller_id explícito
	  v_caller_uid uuid := CASE WHEN auth.role() = 'service_role'
	    THEN COALESCE(p_caller_id, auth.uid()) ELSE auth.uid() END;
	  v_caller_role public.user_role;
	  v_changes jsonb := '{}'::jsonb;
	BEGIN
	  -- Validar caller es admin (usa v_caller_uid que ahora NO es spoofable para authenticated)
	  SELECT role INTO v_caller_role FROM public.profiles WHERE id = v_caller_uid;
	  IF v_caller_role IS NULL OR v_caller_role NOT IN ('admin', 'superadmin') THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED: Only admins can update users.';
	  END IF;
	
	  SELECT * INTO v_old FROM public.profiles WHERE id = p_user_id AND deleted_at IS NULL FOR UPDATE;
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_USER_NOT_FOUND: %', p_user_id;
	  END IF;
	
	  IF p_is_active = false AND p_user_id = v_caller_uid THEN
	    RAISE EXCEPTION 'ERR_SELF_DEACTIVATE_BLOCKED: Cannot deactivate own account.';
	  END IF;
	
	  IF p_full_name IS NOT NULL AND p_full_name <> v_old.full_name THEN
	    v_changes := v_changes || jsonb_build_object('full_name', jsonb_build_object('old', v_old.full_name, 'new', p_full_name));
	    UPDATE public.profiles SET full_name = p_full_name, updated_at = now() WHERE id = p_user_id;
	  END IF;
	
	  IF p_role IS NOT NULL AND p_role <> v_old.role THEN
	    v_changes := v_changes || jsonb_build_object('role', jsonb_build_object('old', v_old.role::text, 'new', p_role::text));
	    UPDATE public.profiles SET role = p_role, updated_at = now() WHERE id = p_user_id;
	  END IF;
	
	  IF p_role_id IS NOT NULL AND (v_old.role_id IS NULL OR p_role_id <> v_old.role_id) THEN
	    v_changes := v_changes || jsonb_build_object('role_id', jsonb_build_object('old', v_old.role_id, 'new', p_role_id));
	    UPDATE public.profiles SET role_id = p_role_id, updated_at = now() WHERE id = p_user_id;
	  END IF;
	
	  IF p_is_active IS NOT NULL AND p_is_active <> v_old.is_active THEN
	    v_changes := v_changes || jsonb_build_object('is_active', jsonb_build_object('old', v_old.is_active, 'new', p_is_active));
	    UPDATE public.profiles SET is_active = p_is_active, updated_at = now() WHERE id = p_user_id;
	  END IF;
	
	  IF p_max_stores_limit IS NOT NULL AND (v_old.max_stores_limit IS NULL OR p_max_stores_limit <> v_old.max_stores_limit) THEN
	    v_changes := v_changes || jsonb_build_object('max_stores_limit', jsonb_build_object('old', v_old.max_stores_limit, 'new', p_max_stores_limit));
	    UPDATE public.profiles SET max_stores_limit = p_max_stores_limit, updated_at = now() WHERE id = p_user_id;
	  END IF;
	
	  IF p_max_users_limit IS NOT NULL AND (v_old.max_users_limit IS NULL OR p_max_users_limit <> v_old.max_users_limit) THEN
	    v_changes := v_changes || jsonb_build_object('max_users_limit', jsonb_build_object('old', v_old.max_users_limit, 'new', p_max_users_limit));
	    UPDATE public.profiles SET max_users_limit = p_max_users_limit, updated_at = now() WHERE id = p_user_id;
	  END IF;
	
	  IF p_plan IS NOT NULL AND p_plan <> v_old.plan THEN
	    v_changes := v_changes || jsonb_build_object('plan', jsonb_build_object('old', v_old.plan::text, 'new', p_plan::text));
	    UPDATE public.profiles SET plan = p_plan, updated_at = now() WHERE id = p_user_id;
	  END IF;
	
	  IF v_changes <> '{}'::jsonb THEN
	    INSERT INTO public.user_audit_log (performed_by, target_user_id, action, old_values, new_values, metadata)
	    VALUES (
	      v_caller_uid, p_user_id, 'USER_UPDATED',
	      jsonb_build_object(
	        'full_name', v_old.full_name, 'role', v_old.role::text,
	        'is_active', v_old.is_active, 'plan', v_old.plan::text
	      ),
	      v_changes,
	      jsonb_build_object('fields_changed', jsonb_object_keys(v_changes))
	    );
	  END IF;
	
	  RETURN jsonb_build_object('success', true, 'user_id', p_user_id, 'changes', v_changes);
	END;
	$function$
2026-09-13 06:16:32.264 UTC [21491] ERROR:  type "user_role" does not exist at character 204
2026-09-13 06:16:32.264 UTC [21491] STATEMENT:  CREATE OR REPLACE FUNCTION public.on_auth_user_created()
	RETURNS trigger
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO 'public', 'extensions'
	AS $function$
	DECLARE
	  v_role_id UUID;
	  v_role_enum USER_ROLE;
	  v_metadata_role TEXT;
	  v_company_name TEXT;
	  v_full_name TEXT;
	  v_tenant_id UUID;
	BEGIN
	  -- Extract role from metadata if present
	  v_metadata_role := NEW.raw_user_meta_data->>'role';
	
	  -- Normalize and convert to enum
	  IF v_metadata_role IS NOT NULL THEN
	    BEGIN
	      v_role_enum := v_metadata_role::USER_ROLE;
	    EXCEPTION WHEN OTHERS THEN
	      v_role_enum := 'tenant_admin'::USER_ROLE;
	    END;
	  ELSE
	    -- Iteración 13: self-signup users are tenant_admin by default
	    v_role_enum := 'tenant_admin'::USER_ROLE;
	  END IF;
	
	  -- Extract company_name and full_name from metadata
	  v_company_name := NEW.raw_user_meta_data->>'company_name';
	  v_full_name := COALESCE(NEW.raw_user_meta_data->>'full_name', split_part(NEW.email, '@', 1));
	
	  -- Iteración 13: Create tenant for self-signup users
	  -- If user was created by admin (via managed_create_user_v2), they may already
	  -- have a tenant_id set via p_target_user_id. In that case, skip tenant creation.
	  IF v_company_name IS NOT NULL THEN
	    INSERT INTO public.tenants (name, owner_id, plan, subscription_status, trial_ends_at, is_active)
	    VALUES (
	      v_company_name,
	      NEW.id,
	      'free'::plan_t,
	      'trial',
	      now() + interval '14 days',
	      true
	    )
	    RETURNING id INTO v_tenant_id;
	  END IF;
	
	  -- Get matching role_id from public.roles
	  SELECT id INTO v_role_id FROM public.roles
	    WHERE name = v_metadata_role OR lower(name) = lower(v_role_enum::text)
	    LIMIT 1;
	
	  -- Insert profile with tenant_id
	  INSERT INTO public.profiles (id, email, full_name, role, role_id, is_active, tenant_id, plan, created_at, updated_at)
	  VALUES (
	    NEW.id,
	    NEW.email,
	    v_full_name,
	    v_role_enum,
	    v_role_id,
	    true,
	    v_tenant_id,
	    'free'::plan_t,
	    now(),
	    now()
	  )
	  ON CONFLICT (id) DO UPDATE SET
	    email = EXCLUDED.email,
	    full_name = EXCLUDED.full_name,
	    role = EXCLUDED.role,
	    role_id = EXCLUDED.role_id,
	    tenant_id = COALESCE(profiles.tenant_id, EXCLUDED.tenant_id),
	    plan = EXCLUDED.plan,
	    is_active = true,
	    updated_at = now();
	
	  -- Audit log
	  INSERT INTO public.user_audit_log (performed_by, target_user_id, action, new_values, metadata)
	  VALUES (
	    NEW.id, NEW.id, 'USER_REGISTERED_PUBLICLY',
	    jsonb_build_object('email', NEW.email, 'role', v_role_enum::text, 'tenant_id', v_tenant_id),
	    jsonb_build_object('company_name', v_company_name, 'source', 'self_signup')
	  );
	
	  RETURN NEW;
	END;
	$function$
2026-09-13 06:16:32.329 UTC [21504] ERROR:  type adjustment_item[] does not exist
2026-09-13 06:16:32.329 UTC [21504] STATEMENT:  CREATE OR REPLACE FUNCTION public.process_inventory_adjustment(p_store_id uuid, p_cashier_id uuid, p_items adjustment_item[], p_operation_date timestamp with time zone DEFAULT NULL::timestamp with time zone)
	 RETURNS uuid
	 LANGUAGE plpgsql
	 SECURITY DEFINER
	 SET search_path TO 'public'
	AS $function$
	DECLARE
	  v_adjustment_id UUID;
	  v_item public.adjustment_item;
	  v_difference NUMERIC;
	  v_effective_date TIMESTAMP WITH TIME ZONE := COALESCE(p_operation_date, NOW());
	BEGIN
	  -- Validación forward-only locking
	  PERFORM public.validate_operation_date(p_operation_date, p_store_id);
	
	  INSERT INTO public.inventory_adjustments (store_id, created_by, status, created_at)
	  VALUES (p_store_id, p_cashier_id, 'PROCESSING', v_effective_date)
	  RETURNING id INTO v_adjustment_id;
	
	  FOREACH v_item IN ARRAY p_items
	  LOOP
	    v_difference := v_item.counted_quantity - v_item.expected_quantity;
	    INSERT INTO public.inventory_adjustment_items (adjustment_id, product_id, expected_quantity, counted_quantity, created_at)
	    VALUES (v_adjustment_id, v_item.product_id, v_item.expected_quantity, v_item.counted_quantity, v_effective_date);
	
	    PERFORM public.register_stock_movement(
	        p_product_id := v_item.product_id,
	        p_store_id := p_store_id,
	        p_user_id := p_cashier_id,
	        p_quantity := v_difference,
	        p_movement_type := 'adjustment',
	        p_operation_date := v_effective_date
	    );
	  END LOOP;
	
	  UPDATE public.inventory_adjustments SET status = 'COMPLETED', updated_at = v_effective_date
	  WHERE id = v_adjustment_id;
	  RETURN v_adjustment_id;
	END;
	$function$
2026-09-13 06:16:32.368 UTC [21511] ERROR:  type "public.purchase_status_enum" does not exist at character 425
2026-09-13 06:16:32.368 UTC [21511] STATEMENT:  CREATE OR REPLACE FUNCTION public.receive_against_po(
	  p_po_id uuid,
	  p_received_items jsonb DEFAULT '[]'::jsonb,
	  p_user_id uuid DEFAULT NULL,
	  p_reception_date timestamptz DEFAULT now(),
	  p_invoice_number text DEFAULT NULL
	) RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO 'public', 'extensions'
	AS $func$
	DECLARE
	  v_store_id            uuid;
	  v_supplier_name       text;
	  v_po_status           public.purchase_status_enum;
	  v_item                jsonb;
	  v_item_id             uuid;
	  v_qty                 numeric;
	  v_qty_ordered         numeric;
	  v_qty_received_before numeric;
	  v_product_id          uuid;
	  v_unit_cost           numeric;
	  v_product_name        text;
	  v_all_received        boolean;
	  v_any_received        boolean;
	  v_new_status          public.purchase_status_enum;
	  v_receipt_id          uuid;
	  v_reception_items     jsonb := '[]'::jsonb;
	  v_count               integer := 0;
	  v_po_number           text;
	  v_invoice_to_pass     text;
	  v_sorted_items        jsonb;
	BEGIN
	  -- ─── 1. Cargar OC con lock exclusivo ───
	  SELECT po.store_id, po.supplier_name, po.status, po.po_number
	    INTO v_store_id, v_supplier_name, v_po_status, v_po_number
	  FROM public.purchase_orders po
	  WHERE po.id = p_po_id
	  FOR UPDATE;
	
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_PO_NOT_FOUND';
	  END IF;
	
	  -- ─── 2. Validar acceso (tenant-aware via has_store_access) ───
	  IF NOT public.has_store_access(v_store_id) THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED';
	  END IF;
	
	  -- ─── 3. Validar estado de la OC ───
	  IF v_po_status = 'cancelled' THEN
	    RAISE EXCEPTION 'ERR_PO_CANCELLED';
	  END IF;
	  -- Solo se puede recibir contra draft, sent o partial.
	  IF v_po_status NOT IN ('draft', 'sent', 'partial') THEN
	    RAISE EXCEPTION 'ERR_PO_NOT_RECEIVABLE: status % is terminal', v_po_status;
	  END IF;
	
	  -- ─── 4. Validar items no vacío (parity B3 register_reception) ───
	  IF p_received_items IS NULL OR jsonb_array_length(p_received_items) = 0 THEN
	    RAISE EXCEPTION 'ERR_EMPTY_ITEMS';
	  END IF;
	
	  -- ─── 5. Condición #5 (anti-deadlock): Ordenar items por po_item_id ASC ───
	  SELECT jsonb_agg(elem ORDER BY (elem->>'po_item_id'))
	    INTO v_sorted_items
	  FROM jsonb_array_elements(p_received_items) AS elem;
	
	  -- ─── 6. Procesar cada item: VALIDAR antes de actualizar ───
	  FOR v_item IN SELECT * FROM jsonb_array_elements(v_sorted_items) LOOP
	    v_item_id := NULLIF(v_item->>'po_item_id', '')::uuid;
	    v_qty     := COALESCE((v_item->>'quantity_received')::numeric, 0);
	
	    -- Validar po_item_id presente
	    IF v_item_id IS NULL THEN
	      RAISE EXCEPTION 'ERR_ITEM_ID_REQUIRED: missing po_item_id in received_items';
	    END IF;
	
	    -- Medio #6: qty > 0
	    IF v_qty <= 0 THEN
	      RAISE EXCEPTION 'ERR_NEGATIVE_QTY: qty % for item % must be > 0', v_qty, v_item_id;
	    END IF;
	
	    -- Cargar estado actual del item con lock FOR UPDATE
	    SELECT product_id, quantity_ordered, quantity_received, unit_cost, product_name
	      INTO v_product_id, v_qty_ordered, v_qty_received_before, v_unit_cost, v_product_name
	    FROM public.purchase_order_items
	    WHERE id = v_item_id AND po_id = p_po_id
	    FOR UPDATE;
	
	    IF NOT FOUND THEN
	      RAISE EXCEPTION 'ERR_ITEM_NOT_FOUND: %', v_item_id;
	    END IF;
	
	    -- Alto #5: over-receive check
	    IF (v_qty_received_before + v_qty) > v_qty_ordered THEN
	      RAISE EXCEPTION 'ERR_OVER_RECEIVE: item % ordered %, already received %, attempting %',
	        v_item_id, v_qty_ordered, v_qty_received_before, v_qty;
	    END IF;
	
	    -- Condición #1: todo item debe tener product_id NOT NULL
	    IF v_product_id IS NULL THEN
	      RAISE EXCEPTION 'ERR_PRODUCT_ID_REQUIRED: item % has no product_id', v_item_id;
	    END IF;
	
	    -- Medio #9: producto debe existir en store
	    IF NOT EXISTS (
	      SELECT 1 FROM public.products p
	      WHERE p.id = v_product_id AND p.store_id = v_store_id
	    ) THEN
	      RAISE EXCEPTION 'ERR_PRODUCT_NOT_IN_STORE: product % not in store %',
	        v_product_id, v_store_id;
	    END IF;
	
	    -- Acumular para register_reception (formato esperado: product_id, quantity, unit_cost, moneda, tasa)
	    v_reception_items := v_reception_items || jsonb_build_object(
	      'product_id',   v_product_id,
	      'product_name', v_product_name,
	      'quantity',     v_qty,
	      'unit_cost',    v_unit_cost,
	      'moneda_recepcion', 'CUP',
	      'tasa_cambio_recepcion', 1.0
	    );
	    v_count := v_count + 1;
	  END LOOP;
	
	  -- ─── 7. Condición #3: reference_doc NULL si no viene invoice_number ───
	  -- Evita colisión con UNIQUE INDEX idx_receipts_store_reference_doc
	  -- (store_id, reference_doc) en partial indexes.
	  -- El vínculo real receipt↔PO es receipts.po_id (G6).
	  IF p_invoice_number IS NULL OR p_invoice_number = '' THEN
	    v_invoice_to_pass := NULL;
	  ELSE
	    v_invoice_to_pass := p_invoice_number;
	  END IF;
	
	  -- ─── 8. Crear receipt vinculado vía register_reception ───
	  -- register_reception aplica las 8 validaciones B1-B5 + C1-C3 del v2.23.0
	  -- (no se relajan). G6 añade p_po_id opcional.
	  v_receipt_id := public.register_reception(
	    p_store_id        := v_store_id,
	    p_supplier        := v_supplier_name,
	    p_reception_date  := p_reception_date,
	    p_invoice_number  := v_invoice_to_pass,
	    p_items           := v_reception_items,
	    p_user_id         := p_user_id,
	    p_po_id           := p_po_id
	  );
	
	  -- ─── 9. Actualizar quantity_received en purchase_order_items ───
	  -- (SECURITY DEFINER bypasses RLS DENY UPDATE de G5)
	  FOR v_item IN SELECT * FROM jsonb_array_elements(v_sorted_items) LOOP
	    v_item_id := NULLIF(v_item->>'po_item_id', '')::uuid;
	    v_qty     := (v_item->>'quantity_received')::numeric;
	
	    UPDATE public.purchase_order_items
	    SET quantity_received = quantity_received + v_qty
	    WHERE id = v_item_id AND po_id = p_po_id;
	  END LOOP;
	
	  -- ─── 10. Recalcular status global (cast EXPLÍCITO a enum) ───
	  SELECT
	    BOOL_AND(quantity_received >= quantity_ordered),
	    BOOL_OR(quantity_received > 0)
	  INTO v_all_received, v_any_received
	  FROM public.purchase_order_items WHERE po_id = p_po_id;
	
	  v_new_status :=
	    CASE
	      WHEN v_all_received THEN 'received'::public.purchase_status_enum
	      WHEN v_any_received THEN 'partial'::public.purchase_status_enum
	      ELSE 'sent'::public.purchase_status_enum
	    END;
	
	  -- ─── 11. Actualizar OC + vincular receipt en notas ───
	  UPDATE public.purchase_orders
	  SET status       = v_new_status,
	      received_at  = CASE WHEN v_new_status = 'received' THEN NOW() ELSE received_at END,
	      notes        = COALESCE(notes, '') ||
	                     CASE WHEN COALESCE(notes, '') = '' THEN '' ELSE E'\n' END ||
	                     '[Receipt ' || v_receipt_id::text || ' linked at ' || NOW()::text || ']'
	  WHERE id = p_po_id;
	
	  -- ─── 12. CxP (Grupo 6): marcar receipt con payment_status='unpaid' ───
	  -- Solo si aún no está seteado (no sobrescribir si register_reception ya lo puso).
	  UPDATE public.receipts
	  SET payment_status     = COALESCE(NULLIF(payment_status, ''), 'unpaid'),
	      payment_terms_days = COALESCE(payment_terms_days, 30),
	      due_date           = COALESCE(due_date, (p_reception_date::date + INTERVAL '30 days')::date)
	  WHERE id = v_receipt_id;
	
	  -- ─── 13. Auditoría ───
	  INSERT INTO public.audit_logs (user_id, store_id, action, table_name, record_id, metadata)
	  VALUES (
	    p_user_id, v_store_id, 'po_received', 'purchase_orders', p_po_id,
	    jsonb_build_object(
	      'new_status', v_new_status::text,
	      'items_received', v_count,
	      'receipt_id', v_receipt_id,
	      'po_number', v_po_number,
	      'invoice_number', v_invoice_to_pass
	    )
	  );
	
	  RETURN jsonb_build_object(
	    'status', 'success',
	    'po_status', v_new_status::text,
	    'receipt_id', v_receipt_id,
	    'items_received', v_count
	  );
	END;
	$func$
2026-09-13 06:16:32.525 UTC [21547] ERROR:  type purchase_status_enum does not exist
2026-09-13 06:16:32.525 UTC [21547] STATEMENT:  CREATE OR REPLACE FUNCTION public.set_purchase_order_status(p_po_id uuid, p_new_status purchase_status_enum, p_user_id uuid DEFAULT NULL::uuid, p_reason text DEFAULT NULL::text)
	 RETURNS jsonb
	 LANGUAGE plpgsql
	 SECURITY DEFINER
	 SET search_path TO 'public', 'extensions'
	AS $function$
	DECLARE
	  v_store_id     uuid;
	  v_current      public.purchase_status_enum;
	  v_po_number    text;
	  v_allowed      text[];
	  v_is_allowed   boolean := false;
	BEGIN
	  -- ─── 1. Cargar PO con lock exclusivo ───
	  SELECT store_id, status, po_number
	    INTO v_store_id, v_current, v_po_number
	  FROM public.purchase_orders
	  WHERE id = p_po_id
	  FOR UPDATE;
	
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_PO_NOT_FOUND';
	  END IF;
	
	  -- ─── 2. Validar acceso (tenant-aware) ───
	  IF NOT public.has_store_access(v_store_id) THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED';
	  END IF;
	
	  -- ─── 3. Si no hay cambio, retornar success sin hacer nada ───
	  IF v_current = p_new_status THEN
	    RETURN jsonb_build_object(
	      'status', 'no_change',
	      'po_status', v_current::text,
	      'po_number', v_po_number
	    );
	  END IF;
	
	  -- ─── 4. Definir transiciones permitidas (state machine) ───
	  -- 'partial' y 'received' NUNCA son destino válido manualmente
	  -- (solo vía receive_against_po).
	  v_allowed := CASE v_current
	    WHEN 'draft'   THEN ARRAY['sent', 'cancelled']
	    WHEN 'sent'    THEN ARRAY['cancelled']
	    WHEN 'partial' THEN ARRAY['cancelled']
	    ELSE ARRAY[]::text[]  -- received, cancelled: terminal, no transitions
	  END;
	
	  -- ─── 5. Verificar transición permitida ───
	  SELECT EXISTS(SELECT 1 FROM unnest(v_allowed) a WHERE a = p_new_status::text)
	    INTO v_is_allowed;
	
	  IF NOT v_is_allowed THEN
	    RAISE EXCEPTION 'ERR_INVALID_TRANSITION: % → % not allowed (allowed: %)',
	      v_current::text, p_new_status::text,
	      CASE WHEN array_length(v_allowed, 1) IS NULL THEN 'none' ELSE array_to_string(v_allowed, ', ') END;
	  END IF;
	
	  -- ─── 6. Aplicar transición ───
	  UPDATE public.purchase_orders
	  SET status      = p_new_status,
	      received_at = CASE WHEN p_new_status = 'received' THEN NOW() ELSE received_at END
	  WHERE id = p_po_id;
	
	  -- ─── 7. Auditoría (action = PO_STATUS_CHANGED) ───
	  INSERT INTO public.audit_logs (user_id, store_id, action, table_name, record_id, metadata)
	  VALUES (
	    CASE WHEN auth.role() = 'service_role' THEN COALESCE(p_user_id, auth.uid()) ELSE auth.uid() END, v_store_id, 'PO_STATUS_CHANGED', 'purchase_orders', p_po_id,
	    jsonb_build_object(
	      'po_number', v_po_number,
	      'from_status', v_current::text,
	      'to_status', p_new_status::text,
	      'reason', p_reason
	    )
	  );
	
	  RETURN jsonb_build_object(
	    'status', 'success',
	    'po_status', p_new_status::text,
	    'po_number', v_po_number,
	    'previous_status', v_current::text
	  );
	END;
	$function$
2026-09-13 06:16:32.630 UTC [21571] ERROR:  type "user_role" does not exist at character 100
2026-09-13 06:16:32.630 UTC [21571] STATEMENT:  CREATE OR REPLACE FUNCTION public.validate_active_store()
	RETURNS TRIGGER AS $$
	DECLARE
	    v_role user_role;
	BEGIN
	    v_role := NEW.role;
	
	    -- Si role es 'costo' o 'admin' o 'superadmin', active_store_id es opcional
	    IF v_role = 'costo'::user_role
	       OR v_role = 'admin'::user_role
	       OR v_role = 'superadmin'::user_role THEN
	        -- Solo validar si active_store_id cambió o es INSERT
	        IF TG_OP = 'INSERT' OR (OLD.active_store_id IS DISTINCT FROM NEW.active_store_id) THEN
	            IF NEW.active_store_id IS NOT NULL THEN
	                IF NOT EXISTS (
	                    SELECT 1 FROM public.user_store_memberships
	                    WHERE user_id = NEW.id
	                      AND store_id = NEW.active_store_id
	                      AND status = 'active'
	                ) THEN
	                    IF TG_OP = 'UPDATE' THEN
	                        RAISE EXCEPTION 'ERR_INVALID_ACTIVE_STORE: El usuario no tiene membership activa en la tienda seleccionada.';
	                    END IF;
	                END IF;
	            END IF;
	        END IF;
	        RETURN NEW;
	    END IF;
	
	    -- Para roles operativos (encargado, clerk, warehouse, manager, usuario)
	    IF TG_OP = 'INSERT' THEN
	        -- En INSERT: ser permisivo. La función managed_create_user inserta
	        -- memberships justo después. No validar nada aquí.
	        RETURN NEW;
	    ELSE
	        -- UPDATE: solo validar si active_store_id cambió realmente
	        -- (evita bloquear updates de otros campos en perfiles intermedios)
	        IF OLD.active_store_id IS NOT DISTINCT FROM NEW.active_store_id THEN
	            -- active_store_id no cambió → permitir el UPDATE sin validar
	            RETURN NEW;
	        END IF;
	
	        -- active_store_id cambió → validar consistencia
	        IF NEW.active_store_id IS NULL THEN
	            IF v_role IN ('encargado'::user_role, 'clerk'::user_role, 'warehouse'::user_role) THEN
	                RAISE EXCEPTION 'ERR_STORE_REQUIRED: El rol % requiere una tienda activa asignada.', v_role;
	            END IF;
	        ELSE
	            IF NOT EXISTS (
	                SELECT 1 FROM public.user_store_memberships
	                WHERE user_id = NEW.id
	                  AND store_id = NEW.active_store_id
	                  AND status = 'active'
	            ) THEN
	                RAISE EXCEPTION 'ERR_INVALID_ACTIVE_STORE: El usuario no tiene membership activa en la tienda seleccionada.';
	            END IF;
	        END IF;
	        RETURN NEW;
	    END IF;
	END;
	$$ LANGUAGE plpgsql SECURITY DEFINER
2026-09-13 06:16:32.677 UTC [21580] ERROR:  type "public.user_role" does not exist at character 270
2026-09-13 06:16:32.677 UTC [21580] STATEMENT:  CREATE OR REPLACE FUNCTION public.validate_tenant_access(
	  p_user_id uuid,
	  p_store_id uuid
	)
	RETURNS boolean
	LANGUAGE plpgsql
	STABLE SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  v_user_tenant uuid;
	  v_store_tenant uuid;
	  v_user_role public.user_role;
	BEGIN
	  -- Global admin bypasses tenant check
	  SELECT role, tenant_id INTO v_user_role, v_user_tenant
	    FROM public.profiles WHERE id = p_user_id;
	
	  IF v_user_role IN ('admin', 'superadmin') THEN
	    RETURN true;
	  END IF;
	
	  -- Get store's tenant
	  SELECT tenant_id INTO v_store_tenant FROM public.stores WHERE id = p_store_id;
	
	  IF v_store_tenant IS NULL THEN
	    -- Legacy store without tenant — allow (backward compat)
	    RETURN true;
	  END IF;
	
	  -- Check tenant match
	  RETURN v_user_tenant = v_store_tenant;
	END;
	$function$
2026-09-13 06:16:32.866 UTC [21613] ERROR:  type "public.user_role" does not exist at character 301
2026-09-13 06:16:32.866 UTC [21613] STATEMENT:  CREATE OR REPLACE FUNCTION public.bulk_assign_memberships(
	  p_user_id uuid,
	  p_assignments jsonb
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  v_assignment jsonb;
	  v_affected int := 0;
	  v_failed int := 0;
	  v_store_id uuid;
	  v_role public.user_role;
	  v_status text;
	  v_caller_uid uuid := auth.uid();
	  v_changes jsonb := '[]'::jsonb;
	BEGIN
	  IF v_caller_uid IS NULL THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED';
	  END IF;
	
	  FOR v_assignment IN SELECT * FROM jsonb_array_elements(p_assignments) LOOP
	    BEGIN
	      v_store_id := (v_assignment->>'store_id')::uuid;
	      v_role := (v_assignment->>'role')::public.user_role;
	      v_status := COALESCE(v_assignment->>'status', 'active');
	
	      -- Validar caller tiene acceso al store
	      IF NOT public.is_admin() AND NOT public.has_store_role(v_store_id, ARRAY['admin', 'manager']) THEN
	        v_failed := v_failed + 1;
	        CONTINUE;
	      END IF;
	
	      INSERT INTO public.user_store_memberships (user_id, store_id, role, status)
	      VALUES (p_user_id, v_store_id, v_role, v_status)
	      ON CONFLICT (user_id, store_id) DO UPDATE SET
	        role = EXCLUDED.role,
	        status = EXCLUDED.status,
	        updated_at = now();
	
	      v_changes := v_changes || jsonb_build_object(jsonb_build_object(
	        'store_id', v_store_id,
	        'role', v_role::text,
	        'status', v_status
	      ));
	
	      v_affected := v_affected + 1;
	    EXCEPTION
	      WHEN foreign_key_violation THEN
	        v_failed := v_failed + 1;
	    END;
	  END LOOP;
	
	  -- Audit log atómico (solo si hubo cambios)
	  IF v_affected > 0 THEN
	    INSERT INTO public.user_audit_log (performed_by, target_user_id, action, new_values, metadata)
	    VALUES (
	      v_caller_uid, p_user_id, 'MEMBERSHIPS_BULK_ASSIGNED',
	      jsonb_build_object('assignments', v_changes),
	      jsonb_build_object('affected', v_affected, 'failed', v_failed)
	    );
	  END IF;
	
	  RETURN jsonb_build_object(
	    'success', true,
	    'affected', v_affected,
	    'failed', v_failed,
	    'user_id', p_user_id
	  );
	END;
	$function$
2026-09-13 06:16:32.900 UTC [21616] ERROR:  syntax error at or near "REVOKE" at character 5039
2026-09-13 06:16:32.900 UTC [21616] STATEMENT:  CREATE OR REPLACE FUNCTION public.bulk_soft_delete_stores(p_store_ids uuid[], p_deleted_by uuid, p_confirmation_token text, p_override_token text DEFAULT NULL::text, p_reason text DEFAULT NULL::text)
	 RETURNS jsonb
	 LANGUAGE plpgsql
	 SECURITY DEFINER
	 SET search_path TO 'public'
	AS $function$
	DECLARE
	  v_store_id UUID;
	  v_validation JSONB;
	  v_blockers JSONB;
	  v_errors JSONB[] := '{}'::jsonb[];
	  v_processed INTEGER := 0;
	  v_token_valid BOOLEAN;
	  v_has_protected BOOLEAN;
	  v_override_valid BOOLEAN;
	  v_confirmation_record RECORD;
	  v_override_record RECORD;
	  v_caller_role TEXT;
	BEGIN
	  -- ============================================================
	  -- AUTH CHECK: Solo admin puede ejecutar esta función
	  -- ============================================================
	  IF auth.uid() IS NOT NULL THEN
	    SELECT role INTO v_caller_role FROM public.profiles WHERE id = auth.uid();
	    IF v_caller_role IS NULL OR v_caller_role != 'admin' THEN
	      RAISE EXCEPTION 'ERR_PERMISSION_DENIED: Solo admin puede ejecutar bulk_soft_delete_stores';
	    END IF;
	  END IF;
	  -- Si auth.uid() IS NULL, es service_role — permitir
	
	  -- ============================================================
	  -- 1. VALIDATE confirmation_token
	  -- ============================================================
	  SELECT * INTO v_confirmation_record
	  FROM public.bulk_confirmation_tokens
	  WHERE token = p_confirmation_token
	    AND action = 'delete'
	    AND expires_at > NOW()
	    AND consumed_at IS NULL
	    AND is_override = false;
	
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_INVALID_CONFIRMATION_TOKEN';
	  END IF;
	
	  IF v_confirmation_record.store_ids != p_store_ids THEN
	    RAISE EXCEPTION 'ERR_STORE_IDS_MISMATCH';
	  END IF;
	
	  -- ============================================================
	  -- 2. VALIDATE tiendas protegidas requieren override_token
	  -- ============================================================
	  SELECT EXISTS(
	    SELECT 1 FROM public.stores
	    WHERE id = ANY(p_store_ids) AND backup_restore_protected = true
	  ) INTO v_has_protected;
	
	  IF v_has_protected THEN
	    IF p_override_token IS NULL THEN
	      RAISE EXCEPTION 'ERR_OVERRIDE_REQUIRED';
	    END IF;
	
	    SELECT * INTO v_override_record
	    FROM public.bulk_confirmation_tokens
	    WHERE token = p_override_token
	      AND is_override = true
	      AND override_for = p_confirmation_token
	      AND expires_at > NOW()
	      AND consumed_at IS NULL;
	
	    IF NOT FOUND THEN
	      RAISE EXCEPTION 'ERR_INVALID_OVERRIDE_TOKEN';
	    END IF;
	
	    IF v_override_record.store_ids != v_confirmation_record.store_ids THEN
	      RAISE EXCEPTION 'ERR_OVERRIDE_STORE_IDS_MISMATCH';
	    END IF;
	
	    IF v_override_record.created_by = v_confirmation_record.created_by THEN
	      RAISE EXCEPTION 'ERR_SAME_USER_OVERRIDE';
	    END IF;
	
	    UPDATE public.bulk_confirmation_tokens SET consumed_at = NOW()
	    WHERE token = p_override_token;
	  END IF;
	
	  UPDATE public.bulk_confirmation_tokens SET consumed_at = NOW()
	  WHERE token = p_confirmation_token;
	
	  -- ============================================================
	  -- 3. VALIDATE todas las tiendas
	  -- ============================================================
	  FOREACH v_store_id IN ARRAY p_store_ids LOOP
	    IF NOT EXISTS(SELECT 1 FROM public.stores WHERE id = v_store_id AND is_active = true) THEN
	      v_errors := array_append(v_errors, jsonb_build_object(
	        'store_id', v_store_id, 'reason', 'STORE_NOT_FOUND_OR_INACTIVE'
	      ));
	      CONTINUE;
	    END IF;
	
	    SELECT public.validate_store_can_be_modified(v_store_id, 'soft_delete') INTO v_validation;
	    v_blockers := v_validation->'blockers';
	
	    IF v_validation->>'can_modify' != 'true' THEN
	      v_errors := array_append(v_errors, jsonb_build_object(
	        'store_id', v_store_id, 'reason', 'HAS_BLOCKING_DEPENDENCIES', 'blockers', v_blockers
	      ));
	    END IF;
	  END LOOP;
	
	  IF array_length(v_errors, 1) IS NOT NULL THEN
	    RETURN jsonb_build_object(
	      'status', 'FAILED', 'processed', 0,
	      'total_requested', array_length(p_store_ids, 1),
	      'errors', to_jsonb(v_errors), 'reason', p_reason
	    );
	  END IF;
	
	  -- ============================================================
	  -- 4. EXECUTE
	  -- ============================================================
	  FOREACH v_store_id IN ARRAY p_store_ids LOOP
	    PERFORM public.soft_delete_store(v_store_id, p_deleted_by);
	    v_processed := v_processed + 1;
	  END LOOP;
	
	  INSERT INTO public.audit_logs (action, table_name, record_id, metadata)
	  VALUES (
	    'bulk_store_deleted', 'stores', NULL,
	    jsonb_build_object(
	      'store_ids', p_store_ids, 'deleted_by', p_deleted_by,
	      'reason', p_reason, 'processed', v_processed,
	      'had_protected_stores', v_has_protected,
	      'override_used', p_override_token IS NOT NULL, 'deleted_at', NOW()
	    )
	  );
	
	  RETURN jsonb_build_object(
	    'status', 'COMPLETED', 'processed', v_processed,
	    'total_requested', array_length(p_store_ids, 1),
	    'errors', '[]'::jsonb, 'reason', p_reason
	  );
	END;
	$function$
	 
	REVOKE EXECUTE ON FUNCTION public.bulk_soft_delete_stores FROM PUBLIC, anon, authenticated
2026-09-13 06:16:32.967 UTC [21623] ERROR:  relation "received_services" does not exist
2026-09-13 06:16:32.967 UTC [21623] CONTEXT:  compilation of PL/pgSQL function "calculate_service_distribution" near line 3
2026-09-13 06:16:32.967 UTC [21623] STATEMENT:  CREATE OR REPLACE FUNCTION calculate_service_distribution(
	  p_service_id UUID
	) RETURNS TABLE(
	  receipt_item_id UUID,
	  product_id UUID,
	  distribution_amount NUMERIC,
	  distribution_percentage NUMERIC
	) LANGUAGE plpgsql SECURITY DEFINER AS $$
	DECLARE
	  v_service received_services%ROWTYPE;
	  v_method TEXT;
	  v_total_amount NUMERIC;
	  v_total_value NUMERIC DEFAULT 0;
	  v_total_qty NUMERIC DEFAULT 0;
	  v_link service_reception_links%ROWTYPE;
	  v_receipt_id UUID;
	  v_item receipt_items%ROWTYPE;
	  v_allocated NUMERIC;
	BEGIN
	  SELECT * INTO v_service FROM received_services WHERE id = p_service_id;
	  IF NOT FOUND THEN RETURN; END IF;
	  
	  v_method := v_service.distribution_method;
	  v_total_amount := v_service.total_amount;
	
	  -- Para cada recepción vinculada
	  FOR v_link IN SELECT * FROM service_reception_links WHERE service_id = p_service_id AND allocated_amount > 0 LOOP
	    v_receipt_id := v_link.receipt_id;
	    v_allocated := v_link.allocated_amount;
	
	    -- Calcular base de distribución según método
	    IF v_method = 'amount' THEN
	      SELECT COALESCE(SUM(quantity * unit_cost), 0) INTO v_total_value
	      FROM receipt_items WHERE receipt_id = v_receipt_id;
	      
	      IF v_total_value > 0 THEN
	        FOR v_item IN SELECT * FROM receipt_items WHERE receipt_id = v_receipt_id LOOP
	          distribution_amount := v_allocated * (v_item.quantity * v_item.unit_cost / v_total_value);
	          distribution_percentage := (v_item.quantity * v_item.unit_cost / v_total_value) * 100;
	          receipt_item_id := v_item.id;
	          product_id := v_item.product_id;
	          RETURN NEXT;
	        END LOOP;
	      END IF;
	
	    ELSIF v_method = 'quantity' THEN
	      SELECT COALESCE(SUM(quantity), 0) INTO v_total_qty
	      FROM receipt_items WHERE receipt_id = v_receipt_id;
	      
	      IF v_total_qty > 0 THEN
	        FOR v_item IN SELECT * FROM receipt_items WHERE receipt_id = v_receipt_id LOOP
	          distribution_amount := v_allocated * (v_item.quantity / v_total_qty);
	          distribution_percentage := (v_item.quantity / v_total_qty) * 100;
	          receipt_item_id := v_item.id;
	          product_id := v_item.product_id;
	          RETURN NEXT;
	        END LOOP;
	      END IF;
	
	    ELSIF v_method = 'manual' THEN
	      -- En manual, la distribución se guarda directamente desde el cliente
	      RETURN;
	    END IF;
	  END LOOP;
	END;
	$$
2026-09-13 06:16:33.027 UTC [21634] ERROR:  syntax error at or near "REVOKE" at character 678
2026-09-13 06:16:33.027 UTC [21634] STATEMENT:  CREATE OR REPLACE FUNCTION public.check_bulk_ops_hourly_limit(p_user_id uuid, p_plan text)
	 RETURNS jsonb
	 LANGUAGE plpgsql
	 STABLE SECURITY DEFINER
	 SET search_path TO 'public'
	AS $function$
	DECLARE
	  v_limit INTEGER;
	  v_used INTEGER;
	BEGIN
	  v_limit := CASE p_plan
	    WHEN 'free' THEN 1
	    WHEN 'pro' THEN 20
	    WHEN 'enterprise' THEN 999999
	    ELSE 1
	  END;
	
	  SELECT COUNT(*) INTO v_used
	  FROM public.bulk_ops_log
	  WHERE user_id = p_user_id
	    AND initiated_at > NOW() - INTERVAL '1 hour';
	
	  RETURN jsonb_build_object(
	    'allowed', v_used < v_limit,
	    'used', v_used,
	    'limit', v_limit,
	    'remaining', GREATEST(0, v_limit - v_used)
	  );
	END;
	$function$
	 
	REVOKE EXECUTE ON FUNCTION public.check_bulk_ops_hourly_limit FROM PUBLIC, anon, authenticated
2026-09-13 06:16:33.078 UTC [21639] ERROR:  type plan_t does not exist
2026-09-13 06:16:33.078 UTC [21639] STATEMENT:  CREATE OR REPLACE FUNCTION public.check_tenant_store_quota(
	  p_tenant_id uuid,
	  p_plan plan_t DEFAULT NULL
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  v_plan plan_t;
	  v_limit int;
	  v_current int;
	  v_allowed boolean;
	BEGIN
	  -- Get plan from tenant if not passed
	  IF p_plan IS NULL THEN
	    SELECT plan INTO v_plan FROM public.tenants WHERE id = p_tenant_id;
	  ELSE
	    v_plan := p_plan;
	  END IF;
	
	  -- Plan limits (unified source of truth)
	  v_limit := CASE v_plan
	    WHEN 'free'::plan_t THEN 1
	    WHEN 'pro'::plan_t THEN 3
	    WHEN 'enterprise'::plan_t THEN 10
	    ELSE 1
	  END;
	
	  -- Count active stores in tenant
	  SELECT COUNT(*) INTO v_current
	    FROM public.stores
	    WHERE tenant_id = p_tenant_id AND is_active = true;
	
	  v_allowed := v_current < v_limit;
	
	  RETURN jsonb_build_object(
	    'allowed', v_allowed,
	    'current', v_current,
	    'limit', v_limit,
	    'plan', v_plan::text,
	    'tenant_id', p_tenant_id
	  );
	END;
	$function$
2026-09-13 06:16:33.193 UTC [21666] ERROR:  relation "public.profiles" does not exist at character 166
2026-09-13 06:16:33.193 UTC [21666] STATEMENT:  CREATE OR REPLACE FUNCTION public.current_user_tenant_id()
	RETURNS uuid
	LANGUAGE sql
	STABLE
	SECURITY DEFINER
	SET search_path TO public
	AS $$
	  SELECT tenant_id FROM public.profiles WHERE id = auth.uid();
	$$
2026-09-13 06:16:33.236 UTC [21672] ERROR:  relation "received_services" does not exist
2026-09-13 06:16:33.236 UTC [21672] CONTEXT:  compilation of PL/pgSQL function "distribute_service_cost_v2" near line 6
2026-09-13 06:16:33.236 UTC [21672] STATEMENT:  CREATE OR REPLACE FUNCTION public.distribute_service_cost_v2(
	  p_service_id uuid,
	  p_user_id uuid DEFAULT NULL
	) RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO 'public', 'extensions'
	AS $func$
	DECLARE
	  v_store_id uuid;
	  v_status text;
	  v_total_amount numeric;
	  v_service received_services%ROWTYPE;
	  v_method text;
	  v_link service_reception_links%ROWTYPE;
	  v_item receipt_items%ROWTYPE;
	  v_total_value numeric := 0;
	  v_total_qty numeric := 0;
	  v_allocated numeric;
	  v_dist_count integer := 0;
	  v_caller_uid uuid := COALESCE(p_user_id, auth.uid());
	BEGIN
	  PERFORM pg_advisory_xact_lock(hashtext(p_service_id::text));
	
	  SELECT * INTO v_service FROM received_services WHERE id = p_service_id FOR UPDATE;
	  IF NOT FOUND THEN RAISE EXCEPTION 'ERR_SERVICE_NOT_FOUND'; END IF;
	
	  v_store_id := v_service.store_id;
	  v_status := v_service.status;
	  v_total_amount := v_service.total_amount;
	  v_method := v_service.distribution_method;
	
	  IF NOT public.has_store_access(v_store_id) THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED';
	  END IF;
	
	  IF v_status != 'active' THEN
	    RAISE EXCEPTION 'ERR_SERVICE_NOT_ACTIVE: status % is not active', v_status;
	  END IF;
	
	  IF v_method = 'manual' THEN
	    RAISE EXCEPTION 'ERR_MANUAL_METHOD: use link_receipts_to_service for manual distribution';
	  END IF;
	
	  DELETE FROM service_cost_distributions WHERE service_id = p_service_id;
	
	  FOR v_link IN SELECT * FROM service_reception_links WHERE service_id = p_service_id AND allocated_amount > 0 ORDER BY receipt_id LOOP
	    v_allocated := v_link.allocated_amount;
	
	    IF v_method = 'amount' THEN
	      SELECT COALESCE(SUM(quantity * unit_cost), 0) INTO v_total_value
	      FROM receipt_items WHERE receipt_id = v_link.receipt_id;
	      IF v_total_value > 0 THEN
	        FOR v_item IN SELECT * FROM receipt_items WHERE receipt_id = v_link.receipt_id ORDER BY id LOOP
	          INSERT INTO service_cost_distributions
	            (service_id, receipt_id, receipt_item_id, product_id, distribution_amount, distribution_percentage)
	          VALUES
	            (p_service_id, v_link.receipt_id, v_item.id, v_item.product_id,
	             v_allocated * (v_item.quantity * v_item.unit_cost / v_total_value),
	             (v_item.quantity * v_item.unit_cost / v_total_value) * 100);
	          v_dist_count := v_dist_count + 1;
	        END LOOP;
	      END IF;
	
	    ELSIF v_method = 'quantity' THEN
	      SELECT COALESCE(SUM(quantity), 0) INTO v_total_qty
	      FROM receipt_items WHERE receipt_id = v_link.receipt_id;
	      IF v_total_qty > 0 THEN
	        FOR v_item IN SELECT * FROM receipt_items WHERE receipt_id = v_link.receipt_id ORDER BY id LOOP
	          INSERT INTO service_cost_distributions
	            (service_id, receipt_id, receipt_item_id, product_id, distribution_amount, distribution_percentage)
	          VALUES
	            (p_service_id, v_link.receipt_id, v_item.id, v_item.product_id,
	             v_allocated * (v_item.quantity / v_total_qty),
	             (v_item.quantity / v_total_qty) * 100);
	          v_dist_count := v_dist_count + 1;
	        END LOOP;
	      END IF;
	    END IF;
	  END LOOP;
	
	  -- Forzar recalculo de WAC para cada producto afectado
	  UPDATE receipt_items SET updated_at = NOW()
	  WHERE product_id IN (SELECT DISTINCT product_id FROM service_cost_distributions WHERE service_id = p_service_id);
	
	  INSERT INTO audit_logs (user_id, store_id, action, table_name, record_id, metadata)
	  VALUES (v_caller_uid, v_store_id, 'SERVICE_DISTRIBUTED', 'received_services', p_service_id,
	    jsonb_build_object('rows_distributed', v_dist_count, 'method', v_method, 'total_amount', v_total_amount));
	
	  RETURN jsonb_build_object('status', 'success', 'distributed_rows', v_dist_count);
	END;
	$func$
2026-09-13 06:16:33.267 UTC [21677] ERROR:  type "user_role" does not exist at character 116
2026-09-13 06:16:33.267 UTC [21677] STATEMENT:  CREATE OR REPLACE FUNCTION public.enforce_encargado_store_limit()
	RETURNS TRIGGER AS $$
	DECLARE
	    v_creator_role user_role;
	    v_limit integer;
	    v_current_count integer;
	BEGIN
	    IF NEW.created_by IS NULL THEN RETURN NEW; END IF;
	
	    SELECT role, max_stores_limit INTO v_creator_role, v_limit
	    FROM public.profiles WHERE id = NEW.created_by;
	
	    IF v_creator_role = 'encargado' THEN
	        SELECT COUNT(*) INTO v_current_count FROM public.stores WHERE created_by = NEW.created_by;
	        IF v_current_count >= v_limit THEN
	            RAISE EXCEPTION 'ERR_STORE_LIMIT_EXCEEDED: Maximum number of stores reached for this manager (%/%)', v_current_count, v_limit;
	        END IF;
	    END IF;
	    RETURN NEW;
	END;
	$$ LANGUAGE plpgsql
2026-09-13 06:16:33.293 UTC [21680] ERROR:  type "user_role" does not exist at character 115
2026-09-13 06:16:33.293 UTC [21680] STATEMENT:  CREATE OR REPLACE FUNCTION public.enforce_encargado_user_limit()
	RETURNS TRIGGER AS $$
	DECLARE
	    v_creator_role user_role;
	    v_limit integer;
	    v_current_count integer;
	BEGIN
	    IF NEW.created_by IS NULL THEN RETURN NEW; END IF;
	
	    SELECT role, max_users_limit INTO v_creator_role, v_limit
	    FROM public.profiles WHERE id = NEW.created_by;
	
	    IF v_creator_role = 'encargado' THEN
	        SELECT COUNT(*) INTO v_current_count FROM public.profiles WHERE created_by = NEW.created_by;
	        IF v_current_count >= v_limit THEN
	            RAISE EXCEPTION 'ERR_USER_LIMIT_EXCEEDED: Maximum number of users reached for this manager (%/%)', v_current_count, v_limit;
	        END IF;
	    END IF;
	    RETURN NEW;
	END;
	$$ LANGUAGE plpgsql
2026-09-13 06:16:33.349 UTC [21693] ERROR:  syntax error at or near "REVOKE" at character 1436
2026-09-13 06:16:33.349 UTC [21693] STATEMENT:  CREATE OR REPLACE FUNCTION public.generate_bulk_confirmation_token(p_store_ids uuid[], p_action text, p_user_id uuid)
	 RETURNS text
	 LANGUAGE plpgsql
	 SECURITY DEFINER
	 SET search_path TO 'public'
	AS $function$
	DECLARE
	  v_token TEXT;
	  v_has_protected BOOLEAN;
	  v_caller_role TEXT;
	BEGIN
	  -- AUTH CHECK: Solo admin
	  IF auth.uid() IS NOT NULL THEN
	    SELECT role INTO v_caller_role FROM public.profiles WHERE id = auth.uid();
	    IF v_caller_role IS NULL OR v_caller_role != 'admin' THEN
	      RAISE EXCEPTION 'ERR_PERMISSION_DENIED: Solo admin puede generar tokens bulk';
	    END IF;
	    -- Verificar que p_user_id coincide con auth.uid()
	    IF p_user_id != auth.uid() THEN
	      RAISE EXCEPTION 'ERR_PERMISSION_DENIED: p_user_id debe coincidir con el usuario autenticado';
	    END IF;
	  END IF;
	
	  IF p_action NOT IN ('delete', 'archive') THEN
	    RAISE EXCEPTION 'ERR_NON_DESTRUCTIVE_ACTION';
	  END IF;
	
	  SELECT EXISTS(
	    SELECT 1 FROM public.stores
	    WHERE id = ANY(p_store_ids) AND backup_restore_protected = true
	  ) INTO v_has_protected;
	
	  v_token := 'bct_' || replace(gen_random_uuid()::text, '-', '');
	
	  INSERT INTO public.bulk_confirmation_tokens (
	    token, store_ids, action, created_by, expires_at, metadata
	  ) VALUES (
	    v_token, p_store_ids, p_action, p_user_id,
	    NOW() + INTERVAL '10 minutes',
	    jsonb_build_object('has_protected_stores', v_has_protected)
	  );
	
	  RETURN v_token;
	END;
	$function$
	 
	REVOKE EXECUTE ON FUNCTION public.generate_bulk_confirmation_token FROM PUBLIC, anon, authenticated
2026-09-13 06:16:33.368 UTC [21696] ERROR:  syntax error at or near "REVOKE" at character 2432
2026-09-13 06:16:33.368 UTC [21696] STATEMENT:  CREATE OR REPLACE FUNCTION public.generate_bulk_override_token(p_confirmation_token text, p_override_user_id uuid, p_reason text DEFAULT NULL::text)
	 RETURNS text
	 LANGUAGE plpgsql
	 SECURITY DEFINER
	 SET search_path TO 'public'
	AS $function$
	DECLARE
	  v_original RECORD;
	  v_override_token TEXT;
	  v_override_user_role TEXT;
	BEGIN
	  -- AUTH CHECK: Solo admin, y debe ser auth.uid() == p_override_user_id
	  IF auth.uid() IS NOT NULL THEN
	    IF auth.uid() != p_override_user_id THEN
	      RAISE EXCEPTION 'ERR_PERMISSION_DENIED: p_override_user_id debe coincidir con el usuario autenticado';
	    END IF;
	
	    SELECT role INTO v_override_user_role FROM public.profiles WHERE id = p_override_user_id;
	    IF v_override_user_role IS NULL OR v_override_user_role != 'admin' THEN
	      RAISE EXCEPTION 'ERR_PERMISSION_DENIED: Solo admin puede generar override';
	    END IF;
	  END IF;
	
	  SELECT * INTO v_original
	  FROM public.bulk_confirmation_tokens
	  WHERE token = p_confirmation_token
	    AND consumed_at IS NULL
	    AND expires_at > NOW()
	    AND is_override = false;
	
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_INVALID_OR_EXPIRED_TOKEN';
	  END IF;
	
	  IF v_original.created_by = p_override_user_id THEN
	    RAISE EXCEPTION 'ERR_SAME_USER_OVERRIDE';
	  END IF;
	
	  IF EXISTS(
	    SELECT 1 FROM public.bulk_confirmation_tokens
	    WHERE override_for = p_confirmation_token
	      AND consumed_at IS NULL AND expires_at > NOW()
	  ) THEN
	    RAISE EXCEPTION 'ERR_OVERRIDE_ALREADY_EXISTS';
	  END IF;
	
	  v_override_token := 'bot_' || replace(gen_random_uuid()::text, '-', '');
	
	  INSERT INTO public.bulk_confirmation_tokens (
	    token, store_ids, action, created_by, expires_at,
	    is_override, override_for, metadata
	  ) VALUES (
	    v_override_token, v_original.store_ids, v_original.action, p_override_user_id,
	    NOW() + INTERVAL '10 minutes', true, p_confirmation_token,
	    jsonb_build_object('override_reason', p_reason,
	      'original_created_by', v_original.created_by,
	      'override_created_by', p_override_user_id)
	  );
	
	  INSERT INTO public.audit_logs (action, table_name, record_id, metadata)
	  VALUES (
	    'bulk_override_token_generated', 'stores', NULL,
	    jsonb_build_object('confirmation_token', p_confirmation_token,
	      'store_ids', v_original.store_ids,
	      'override_by', p_override_user_id,
	      'original_by', v_original.created_by, 'reason', p_reason)
	  );
	
	  RETURN v_override_token;
	END;
	$function$
	 
	REVOKE EXECUTE ON FUNCTION public.generate_bulk_override_token FROM PUBLIC, anon, authenticated
2026-09-13 06:16:33.406 UTC [21704] ERROR:  relation "public.backup_table_registry" does not exist at character 581
2026-09-13 06:16:33.406 UTC [21704] STATEMENT:  CREATE OR REPLACE FUNCTION public.get_backup_table_list(
	  p_include_excluded BOOLEAN DEFAULT FALSE
	)
	RETURNS TABLE (
	  table_name          TEXT,
	  tier                INTEGER,
	  filter_strategy     TEXT,
	  parent_table        TEXT,
	  parent_foreign_key  TEXT,
	  date_column         TEXT,
	  excluded_from_restore BOOLEAN,
	  exclude_reason      TEXT
	)
	LANGUAGE sql
	SECURITY DEFINER
	SET search_path = public
	AS $$
	  SELECT
	    table_name,
	    tier,
	    filter_strategy,
	    parent_table,
	    parent_foreign_key,
	    date_column,
	    excluded_from_restore,
	    exclude_reason
	  FROM public.backup_table_registry
	  WHERE (p_include_excluded OR excluded_from_restore = FALSE)
	  ORDER BY tier ASC, table_name ASC;
	$$
2026-09-13 06:16:33.443 UTC [21712] ERROR:  relation "v_global_operation_dates" does not exist at character 229
2026-09-13 06:16:33.443 UTC [21712] STATEMENT:  CREATE OR REPLACE FUNCTION public.get_global_max_operation_date(
	  p_store_id UUID DEFAULT NULL
	)
	RETURNS TIMESTAMP WITH TIME ZONE
	LANGUAGE sql
	SECURITY DEFINER
	SET search_path = public
	AS $$
	  SELECT MAX(operation_date)
	  FROM v_global_operation_dates
	  WHERE p_store_id IS NULL OR store_id = p_store_id;
	$$
2026-09-13 06:16:33.461 UTC [21715] ERROR:  relation "exchange_rates" does not exist at character 216
2026-09-13 06:16:33.461 UTC [21715] STATEMENT:  CREATE OR REPLACE FUNCTION get_latest_rate(p_currency TEXT, p_source TEXT)
	RETURNS TABLE(rate NUMERIC, rate_date DATE, captured_at TIMESTAMPTZ)
	LANGUAGE sql STABLE AS $$
	  SELECT rate, rate_date, captured_at
	  FROM exchange_rates
	  WHERE currency = p_currency AND source = p_source
	  ORDER BY rate_date DESC, captured_at DESC
	  LIMIT 1;
	$$
2026-09-13 06:16:33.483 UTC [21718] ERROR:  relation "public.products" does not exist at character 194
2026-09-13 06:16:33.483 UTC [21718] STATEMENT:  CREATE OR REPLACE FUNCTION public.get_low_stock_count(p_store_id uuid DEFAULT NULL)
	RETURNS bigint
	LANGUAGE sql
	SECURITY DEFINER
	SET search_path = public
	AS $$
	  SELECT COUNT(*)::bigint
	  FROM public.products
	  WHERE
	    (p_store_id IS NULL OR store_id = p_store_id)
	    AND is_active = true
	    AND stock_current > 0
	    AND min_stock IS NOT NULL
	    AND min_stock > 0
	    AND stock_current <= min_stock;
	$$
2026-09-13 06:16:33.516 UTC [21725] ERROR:  relation "receipt_items" does not exist at character 754
2026-09-13 06:16:33.516 UTC [21725] STATEMENT:  CREATE OR REPLACE FUNCTION get_product_cost_analysis(
	  p_product_id UUID,
	  p_store_id UUID
	) RETURNS TABLE(
	  receipt_id UUID,
	  receipt_date TIMESTAMPTZ,
	  quantity INT,
	  unit_cost FLOAT,
	  service_type TEXT,
	  service_amount NUMERIC,
	  total_cost NUMERIC,
	  unit_cost_final NUMERIC
	) LANGUAGE sql SECURITY DEFINER AS $$
	  SELECT 
	    ri.receipt_id,
	    r.reception_date,
	    ri.quantity,
	    ri.unit_cost,
	    st.name AS service_type,
	    COALESCE(scd.distribution_amount, 0) AS service_amount,
	    ri.unit_cost * ri.quantity + COALESCE(scd.distribution_amount, 0) AS total_cost,
	    CASE WHEN ri.quantity > 0 
	      THEN (ri.unit_cost * ri.quantity + COALESCE(scd.distribution_amount, 0)) / ri.quantity 
	      ELSE 0 END AS unit_cost_final
	  FROM receipt_items ri
	  JOIN receipts r ON r.id = ri.receipt_id
	  LEFT JOIN service_cost_distributions scd ON scd.receipt_item_id = ri.id
	  LEFT JOIN received_services rs ON rs.id = scd.service_id AND rs.status = 'active'
	  LEFT JOIN service_types st ON st.id = rs.service_type_id
	  WHERE ri.product_id = p_product_id
	    AND r.store_id = p_store_id
	    AND r.status = 'active'
	  ORDER BY r.reception_date DESC;
	$$
2026-09-13 06:16:33.546 UTC [21731] ERROR:  relation "exchange_rates" does not exist at character 252
2026-09-13 06:16:33.546 UTC [21731] STATEMENT:  CREATE OR REPLACE FUNCTION get_rate_history(
	  p_currency TEXT,
	  p_source TEXT,
	  p_days INT DEFAULT 30
	)
	RETURNS TABLE(rate_date DATE, rate NUMERIC, variation_daily NUMERIC)
	LANGUAGE sql STABLE AS $$
	  SELECT rate_date, rate, variation_daily
	  FROM exchange_rates
	  WHERE currency = p_currency AND source = p_source
	    AND rate_date >= CURRENT_DATE - p_days
	  ORDER BY rate_date ASC;
	$$
2026-09-13 06:16:33.565 UTC [21734] ERROR:  relation "exchange_rates" does not exist at character 363
2026-09-13 06:16:33.565 UTC [21734] STATEMENT:  CREATE OR REPLACE FUNCTION get_rate_variation(
	  p_currency TEXT,
	  p_source TEXT,
	  p_start_date DATE,
	  p_end_date DATE
	)
	RETURNS TABLE(
	  start_rate NUMERIC,
	  end_rate NUMERIC,
	  absolute_change NUMERIC,
	  percent_change NUMERIC,
	  daily_avg_growth NUMERIC,
	  monthly_avg_growth NUMERIC
	)
	LANGUAGE sql STABLE AS $$
	  WITH start_val AS (
	    SELECT rate FROM exchange_rates
	    WHERE currency = p_currency AND source = p_source AND rate_date <= p_start_date
	    ORDER BY rate_date DESC LIMIT 1
	  ),
	  end_val AS (
	    SELECT rate FROM exchange_rates
	    WHERE currency = p_currency AND source = p_source AND rate_date <= p_end_date
	    ORDER BY rate_date DESC LIMIT 1
	  )
	  SELECT
	    COALESCE(sv.rate, 0) AS start_rate,
	    COALESCE(ev.rate, 0) AS end_rate,
	    (COALESCE(ev.rate, 0) - COALESCE(sv.rate, 0)) AS absolute_change,
	    CASE WHEN sv.rate IS NOT NULL AND sv.rate > 0 THEN ((ev.rate - sv.rate) / sv.rate * 100) ELSE 0 END AS percent_change,
	    CASE WHEN sv.rate IS NOT NULL AND sv.rate > 0 AND ev.rate IS NOT NULL AND ev.rate > 0 AND p_end_date > p_start_date
	      THEN (POWER(ev.rate::FLOAT / sv.rate::FLOAT, 1.0 / EXTRACT(EPOCH FROM (p_end_date - p_start_date)) * 86400) - 1) * 100
	      ELSE 0 END AS daily_avg_growth,
	    CASE WHEN sv.rate IS NOT NULL AND sv.rate > 0 AND ev.rate IS NOT NULL AND ev.rate > 0 AND p_end_date > p_start_date
	      THEN (POWER(ev.rate::FLOAT / sv.rate::FLOAT, 30.44 / EXTRACT(EPOCH FROM (p_end_date - p_start_date)) * 86400) - 1) * 100
	      ELSE 0 END AS monthly_avg_growth
	  FROM start_val sv, end_val ev;
	$$
2026-09-13 06:16:33.619 UTC [21747] ERROR:  type "public.stores" does not exist
2026-09-13 06:16:33.619 UTC [21747] STATEMENT:  CREATE OR REPLACE FUNCTION public.get_transferable_stores(
	  p_user_id UUID,
	  p_current_store_id UUID
	)
	RETURNS SETOF public.stores
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO 'public'
	AS $$
	DECLARE
	  v_tenant_id UUID;
	BEGIN
	  -- 1. Obtener tenant_id de la tienda actual
	  SELECT tenant_id INTO v_tenant_id
	  FROM public.stores
	  WHERE id = p_current_store_id AND is_active = true;
	
	  -- 2. Devolver tiendas donde el caller tiene acceso (has_store_access_as)
	  --    EXCLUYENDO la tienda actual
	  --    Si tenant_id es NOT NULL, filtrar por mismo tenant.
	  --    Si tenant_id es NULL (legacy), mostrar todas las que el caller tiene acceso.
	  IF v_tenant_id IS NOT NULL THEN
	    RETURN QUERY
	    SELECT s.*
	    FROM public.stores s
	    WHERE s.tenant_id = v_tenant_id
	      AND s.id != p_current_store_id
	      AND s.is_active = true
	      AND s.is_archived = false
	      AND public.has_store_access_as(p_user_id, s.id)
	    ORDER BY s.name;
	  ELSE
	    -- Legacy: tiendas sin tenant_id — usar solo has_store_access_as
	    RETURN QUERY
	    SELECT s.*
	    FROM public.stores s
	    WHERE s.id != p_current_store_id
	      AND s.is_active = true
	      AND s.is_archived = false
	      AND public.has_store_access_as(p_user_id, s.id)
	    ORDER BY s.name;
	  END IF;
	END;
	$$
2026-09-13 06:16:33.659 UTC [21756] ERROR:  type user_role[] does not exist
2026-09-13 06:16:33.659 UTC [21756] STATEMENT:  CREATE OR REPLACE FUNCTION public.has_any_role(required_roles user_role[])
	 RETURNS boolean
	 LANGUAGE plpgsql
	 STABLE SECURITY DEFINER
	AS $$
	DECLARE
	    v_actual_role user_role;
	    r user_role;
	BEGIN
	    SELECT role INTO v_actual_role FROM public.profiles WHERE id = auth.uid();
	
	    FOREACH r IN ARRAY required_roles
	    LOOP
	        IF v_actual_role = r THEN RETURN true; END IF;
	        IF v_actual_role = 'encargado' AND r = 'manager' THEN RETURN true; END IF;
	        IF v_actual_role = 'usuario' AND (r = 'clerk' OR r = 'warehouse') THEN RETURN true; END IF;
	    END LOOP;
	
	    RETURN false;
	END;
	$$
2026-09-13 06:16:33.753 UTC [21759] ERROR:  relation "profiles" does not exist at character 461
2026-09-13 06:16:33.753 UTC [21759] STATEMENT:  CREATE OR REPLACE FUNCTION public.has_management_access_as(
	  p_user_id uuid,
	  p_store_id uuid
	) RETURNS boolean
	LANGUAGE sql SECURITY DEFINER
	SET search_path = public, pg_temp
	AS $$
	  -- service_role always passes
	  SELECT CASE WHEN auth.role() = 'service_role' THEN true
	    ELSE
	      -- Check if user has admin/manager/encargado role in their profile
	      -- OR has a membership with manager/admin role for the store
	      EXISTS (
	        SELECT 1 FROM profiles
	        WHERE id = p_user_id
	        AND role IN ('admin', 'manager', 'encargado')
	      )
	      OR EXISTS (
	        SELECT 1 FROM user_store_memberships
	        WHERE user_id = p_user_id
	        AND store_id = p_store_id
	        AND status = 'active'
	        AND role IN ('admin', 'manager')
	      )
	    END
	$$
2026-09-13 06:16:33.798 UTC [21762] ERROR:  type public.user_role does not exist
2026-09-13 06:16:33.798 UTC [21762] STATEMENT:  CREATE OR REPLACE FUNCTION public.has_role(p_user_id UUID, p_required_role public.user_role)
	 RETURNS boolean
	 LANGUAGE plpgsql
	 STABLE SECURITY DEFINER
	AS $$
	DECLARE
	    v_actual_role public.user_role;
	BEGIN
	    SELECT role INTO v_actual_role FROM public.profiles WHERE id = p_user_id;
	
	    IF v_actual_role IS NULL THEN
	        RETURN false;
	    END IF;
	
	    -- Compatibility mapping (mirroring the 1-arg version)
	    IF v_actual_role = 'encargado' AND p_required_role = 'manager' THEN RETURN true; END IF;
	    IF v_actual_role = 'usuario' AND (p_required_role = 'clerk' OR p_required_role = 'warehouse') THEN RETURN true; END IF;
	
	    RETURN v_actual_role = p_required_role;
	END;
	$$
2026-09-13 06:16:33.825 UTC [21767] ERROR:  relation "public.user_store_memberships" does not exist at character 279
2026-09-13 06:16:33.825 UTC [21767] STATEMENT:  CREATE OR REPLACE FUNCTION public.has_store_role(
	  p_user_id uuid,
	  p_store_id uuid,
	  p_roles text[]
	) RETURNS boolean
	LANGUAGE sql SECURITY DEFINER
	SET search_path = public, pg_temp
	AS $$
	  SELECT CASE WHEN auth.role() = 'service_role' THEN
	    EXISTS (
	      SELECT 1 FROM public.user_store_memberships m
	      WHERE m.user_id = p_user_id
	        AND m.store_id = p_store_id
	        AND m.status = 'active'
	        AND m.role::text = ANY(p_roles)
	    )
	  ELSE
	    EXISTS (
	      SELECT 1 FROM public.user_store_memberships m
	      WHERE m.user_id = auth.uid()
	        AND m.store_id = p_store_id
	        AND m.status = 'active'
	        AND m.role::text = ANY(p_roles)
	    )
	  END
	$$
2026-09-13 06:16:33.855 UTC [21773] ERROR:  relation "public.profiles" does not exist at character 173
2026-09-13 06:16:33.855 UTC [21773] STATEMENT:  CREATE OR REPLACE FUNCTION public.is_global_admin()
	RETURNS BOOLEAN
	LANGUAGE sql
	STABLE
	SECURITY DEFINER
	SET search_path = public
	AS $$
	  SELECT EXISTS (
	    SELECT 1 FROM public.profiles
	    WHERE id = auth.uid()
	      AND role = 'admin'
	  );
	$$
2026-09-13 06:16:33.877 UTC [21777] ERROR:  type user_role does not exist
2026-09-13 06:16:33.877 UTC [21777] STATEMENT:  CREATE OR REPLACE FUNCTION public.is_role_not_changed(p_user_id uuid, p_new_role user_role, p_new_role_id uuid)
	RETURNS boolean
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path = public, extensions
	AS $$
	DECLARE
	  v_old_role user_role;
	  v_old_role_id uuid;
	BEGIN
	  SELECT role, role_id INTO v_old_role, v_old_role_id
	  FROM public.profiles
	  WHERE id = p_user_id;
	
	  RETURN (p_new_role IS NOT DISTINCT FROM v_old_role)
	     AND (p_new_role_id IS NOT DISTINCT FROM v_old_role_id);
	END;
	$$
2026-09-13 06:16:33.902 UTC [21781] ERROR:  relation "public.user_store_memberships" does not exist at character 188
2026-09-13 06:16:33.902 UTC [21781] STATEMENT:  CREATE OR REPLACE FUNCTION public.is_store_member(p_store_id UUID)
	RETURNS BOOLEAN
	LANGUAGE sql
	STABLE
	SECURITY DEFINER
	SET search_path = public
	AS $$
	  SELECT EXISTS (
	    SELECT 1 FROM public.user_store_memberships
	    WHERE store_id = p_store_id
	      AND user_id = auth.uid()
	      AND status = 'active'
	  );
	$$
2026-09-13 06:16:33.921 UTC [21784] ERROR:  function public.current_user_tenant_id() does not exist at character 209
2026-09-13 06:16:33.921 UTC [21784] HINT:  No function matches the given name and argument types. You might need to add explicit type casts.
2026-09-13 06:16:33.921 UTC [21784] STATEMENT:  CREATE OR REPLACE FUNCTION public.is_tenant_member(p_tenant_id uuid)
	RETURNS boolean
	LANGUAGE sql
	STABLE
	SECURITY DEFINER
	SET search_path TO public
	AS $$
	  SELECT p_tenant_id IS NOT NULL
	   AND p_tenant_id = public.current_user_tenant_id();
	$$
2026-09-13 06:16:33.965 UTC [21794] ERROR:  type user_role does not exist
2026-09-13 06:16:33.965 UTC [21794] STATEMENT:  CREATE OR REPLACE FUNCTION public.managed_create_user(p_email text, p_full_name text, p_role user_role, p_store_id uuid DEFAULT NULL::uuid, p_memberships jsonb DEFAULT NULL::jsonb, p_max_stores integer DEFAULT 0, p_max_users integer DEFAULT 0, p_target_user_id uuid DEFAULT NULL::uuid)
	 RETURNS jsonb
	 LANGUAGE plpgsql
	 SECURITY DEFINER
	 SET search_path TO 'public', 'extensions'
	AS $function$
	DECLARE
	    v_user_id uuid;
	    v_role_id uuid;
	    v_active_store_id uuid;
	    m JSONB;
	    v_creator_role user_role;
	BEGIN
	    SELECT role INTO v_creator_role FROM public.profiles WHERE id = auth.uid();
	    IF v_creator_role IS NULL OR v_creator_role NOT IN ('admin', 'encargado') THEN
	        RAISE EXCEPTION 'ERR_UNAUTHORIZED: Only admins and managers can create users.';
	    END IF;
	
	    v_active_store_id := COALESCE((p_memberships->0->>'store_id')::UUID, p_store_id);
	
	    IF v_creator_role = 'encargado' AND v_active_store_id IS NOT NULL THEN
	        IF NOT public.has_store_access(v_active_store_id) THEN
	            RAISE EXCEPTION 'ERR_UNAUTHORIZED: No access to store %', v_active_store_id;
	        END IF;
	    END IF;
	
	    SELECT id INTO v_role_id FROM public.roles WHERE lower(name) = lower(p_role::text) OR (name = 'Cajero' AND p_role = 'clerk') OR (name = 'Almacenero' AND p_role = 'warehouse') LIMIT 1;
	    v_user_id := COALESCE(p_target_user_id, gen_random_uuid());
	
	    INSERT INTO public.profiles (id, email, full_name, role, role_id, active_store_id, is_active, created_by, max_stores_limit, max_users_limit, created_at, updated_at)
	    VALUES (v_user_id, p_email, p_full_name, p_role, v_role_id, v_active_store_id, true, auth.uid(), p_max_stores, p_max_users, now(), now())
	    ON CONFLICT (id) DO UPDATE SET email = EXCLUDED.email, full_name = EXCLUDED.full_name, role = EXCLUDED.role, role_id = EXCLUDED.role_id, active_store_id = EXCLUDED.active_store_id, updated_at = now()
	    RETURNING id INTO v_user_id;
	
	    IF p_memberships IS NOT NULL THEN
	        IF v_creator_role != 'admin' THEN
	            DELETE FROM public.user_store_memberships WHERE user_id = v_user_id AND store_id IN (SELECT store_id FROM public.user_store_memberships WHERE user_id = auth.uid() AND role IN ('encargado', 'manager'));
	        ELSE
	            DELETE FROM public.user_store_memberships WHERE user_id = v_user_id;
	        END IF;
	        FOR m IN SELECT * FROM jsonb_array_elements(p_memberships) LOOP
	            IF (m->>'store_id') IS NOT NULL AND (m->>'store_id') <> '' THEN
	                IF v_creator_role = 'admin' OR public.has_store_access((m->>'store_id')::UUID) THEN
	                    INSERT INTO public.user_store_memberships (user_id, store_id, role) VALUES (v_user_id, (m->>'store_id')::UUID, (m->>'role')::user_role) ON CONFLICT (user_id, store_id) DO UPDATE SET role = EXCLUDED.role;
	                END IF;
	            END IF;
	        END LOOP;
	    ELSIF p_store_id IS NOT NULL THEN
	        INSERT INTO public.user_store_memberships (user_id, store_id, role) VALUES (v_user_id, p_store_id, p_role) ON CONFLICT (user_id, store_id) DO UPDATE SET role = EXCLUDED.role;
	    END IF;
	    RETURN jsonb_build_object('success', true, 'user_id', v_user_id);
	END;
	$function$
2026-09-13 06:16:33.983 UTC [21797] ERROR:  type public.user_role does not exist
2026-09-13 06:16:33.983 UTC [21797] STATEMENT:  CREATE OR REPLACE FUNCTION public.managed_create_user_v2(
	  p_email text,
	  p_full_name text,
	  p_role public.user_role,
	  p_plan plan_t DEFAULT 'free'::plan_t,
	  p_store_id uuid DEFAULT NULL::uuid,
	  p_memberships jsonb DEFAULT NULL::jsonb,
	  p_max_stores int DEFAULT 0,
	  p_max_users int DEFAULT 0,
	  p_target_user_id uuid DEFAULT NULL::uuid,
	  p_creator_id uuid DEFAULT NULL::uuid
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  v_user_id uuid := COALESCE(p_target_user_id, gen_random_uuid());
	  v_role_id uuid;
	  v_active_store_id uuid;
	  m JSONB;
	  v_creator_role public.user_role;
	  v_creator_uid uuid := COALESCE(p_creator_id, auth.uid());
	BEGIN
	  -- Validar caller es admin o encargado
	  SELECT role INTO v_creator_role FROM public.profiles WHERE id = v_creator_uid;
	  IF v_creator_role IS NULL OR v_creator_role NOT IN ('admin', 'encargado', 'superadmin') THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED: Only admins and managers can create users.';
	  END IF;
	
	  -- Validar email único (entre perfiles activos)
	  IF EXISTS (SELECT 1 FROM public.profiles WHERE email = p_email AND deleted_at IS NULL) THEN
	    RAISE EXCEPTION 'ERR_EMAIL_ALREADY_EXISTS: %', p_email;
	  END IF;
	
	  v_active_store_id := COALESCE((p_memberships->0->>'store_id')::UUID, p_store_id);
	
	  -- Encargado solo puede crear en tiendas que gestiona
	  IF v_creator_role = 'encargado' AND v_active_store_id IS NOT NULL THEN
	    IF NOT public.has_store_access(v_active_store_id) THEN
	      RAISE EXCEPTION 'ERR_UNAUTHORIZED: No access to store %', v_active_store_id;
	    END IF;
	  END IF;
	
	  -- Buscar role_id
	  SELECT id INTO v_role_id FROM public.roles
	    WHERE lower(name) = lower(p_role::text)
	       OR (name = 'Cajero' AND p_role = 'clerk')
	       OR (name = 'Almacenero' AND p_role = 'warehouse')
	       OR (name = 'UserCosto' AND p_role = 'costo')
	    LIMIT 1;
	
	  -- INSERT profiles con plan
	  INSERT INTO public.profiles (
	    id, email, full_name, role, role_id, active_store_id, is_active,
	    created_by, max_stores_limit, max_users_limit, plan, created_at, updated_at
	  ) VALUES (
	    v_user_id, p_email, p_full_name, p_role, v_role_id, v_active_store_id, true,
	    v_creator_uid, p_max_stores, p_max_users, p_plan, now(), now()
	  )
	  ON CONFLICT (id) DO UPDATE SET
	    email = EXCLUDED.email,
	    full_name = EXCLUDED.full_name,
	    role = EXCLUDED.role,
	    role_id = EXCLUDED.role_id,
	    active_store_id = EXCLUDED.active_store_id,
	    plan = EXCLUDED.plan,
	    is_active = true,
	    deleted_at = NULL,
	    deletion_reason = NULL,
	    deleted_by = NULL,
	    updated_at = now()
	  RETURNING id INTO v_user_id;
	
	  -- Procesar memberships
	  IF p_memberships IS NOT NULL THEN
	    IF v_creator_role != 'admin' AND v_creator_role != 'superadmin' THEN
	      DELETE FROM public.user_store_memberships
	        WHERE user_id = v_user_id
	        AND store_id IN (
	          SELECT store_id FROM public.user_store_memberships
	          WHERE user_id = v_creator_uid AND role IN ('encargado', 'manager')
	        );
	    ELSE
	      DELETE FROM public.user_store_memberships WHERE user_id = v_user_id;
	    END IF;
	
	    FOR m IN SELECT * FROM jsonb_array_elements(p_memberships) LOOP
	      IF (m->>'store_id') IS NOT NULL AND (m->>'store_id') <> '' THEN
	        IF v_creator_role IN ('admin', 'superadmin') OR public.has_store_access((m->>'store_id')::UUID) THEN
	          INSERT INTO public.user_store_memberships (user_id, store_id, role)
	          VALUES (v_user_id, (m->>'store_id')::UUID, (m->>'role')::public.user_role)
	          ON CONFLICT (user_id, store_id) DO UPDATE SET role = EXCLUDED.role, status = 'active';
	        END IF;
	      END IF;
	    END LOOP;
	  ELSIF p_store_id IS NOT NULL THEN
	    INSERT INTO public.user_store_memberships (user_id, store_id, role)
	    VALUES (v_user_id, p_store_id, p_role)
	    ON CONFLICT (user_id, store_id) DO UPDATE SET role = EXCLUDED.role, status = 'active';
	  END IF;
	
	  -- Audit log atómico (regresión C-2 restaurada)
	  INSERT INTO public.user_audit_log (performed_by, target_user_id, action, new_values, metadata)
	  VALUES (
	    v_creator_uid, v_user_id, 'USER_CREATED',
	    jsonb_build_object(
	      'email', p_email, 'full_name', p_full_name, 'role', p_role::text,
	      'plan', p_plan::text, 'active_store_id', v_active_store_id,
	      'max_stores', p_max_stores, 'max_users', p_max_users
	    ),
	    jsonb_build_object('memberships_count', CASE WHEN p_memberships IS NOT NULL THEN jsonb_array_length(p_memberships) ELSE 0 END)
	  );
	
	  RETURN jsonb_build_object('success', true, 'user_id', v_user_id);
	END;
	$function$
2026-09-13 06:16:34.014 UTC [21802] ERROR:  type "public.user_role" does not exist at character 392
2026-09-13 06:16:34.014 UTC [21802] STATEMENT:  CREATE OR REPLACE FUNCTION public.managed_reset_password(
	  p_user_id uuid,
	  p_caller_id uuid DEFAULT NULL::uuid
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  -- FIX H-7: anti-spoofing
	  v_caller_uid uuid := CASE WHEN auth.role() = 'service_role'
	    THEN COALESCE(p_caller_id, auth.uid()) ELSE auth.uid() END;
	  v_caller_role public.user_role;
	  v_target_email text;
	BEGIN
	  SELECT role INTO v_caller_role FROM public.profiles WHERE id = v_caller_uid;
	  IF v_caller_role IS NULL OR v_caller_role NOT IN ('admin', 'superadmin') THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED';
	  END IF;
	
	  IF p_user_id = v_caller_uid THEN
	    RAISE EXCEPTION 'ERR_SELF_RESET_BLOCKED';
	  END IF;
	
	  SELECT email INTO v_target_email FROM public.profiles WHERE id = p_user_id AND deleted_at IS NULL;
	  IF v_target_email IS NULL THEN
	    RAISE EXCEPTION 'ERR_USER_NOT_FOUND';
	  END IF;
	
	  INSERT INTO public.user_audit_log (performed_by, target_user_id, action, metadata)
	  VALUES (
	    v_caller_uid, p_user_id, 'PASSWORD_RESET_REQUESTED',
	    jsonb_build_object('email', v_target_email, 'method', 'recovery_link')
	  );
	
	  RETURN jsonb_build_object('success', true, 'email', v_target_email);
	END;
	$function$
2026-09-13 06:16:34.046 UTC [21806] ERROR:  type "public.user_role" does not exist at character 427
2026-09-13 06:16:34.046 UTC [21806] STATEMENT:  CREATE OR REPLACE FUNCTION public.managed_soft_delete_user(
	  p_user_id uuid,
	  p_reason text,
	  p_caller_id uuid DEFAULT NULL::uuid
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  v_old RECORD;
	  -- FIX H-7: anti-spoofing
	  v_caller_uid uuid := CASE WHEN auth.role() = 'service_role'
	    THEN COALESCE(p_caller_id, auth.uid()) ELSE auth.uid() END;
	  v_caller_role public.user_role;
	  v_active_memberships_count int;
	  v_anon_email text;
	BEGIN
	  SELECT role INTO v_caller_role FROM public.profiles WHERE id = v_caller_uid;
	  IF v_caller_role IS NULL OR v_caller_role NOT IN ('admin', 'superadmin') THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED';
	  END IF;
	
	  IF p_user_id = v_caller_uid THEN
	    RAISE EXCEPTION 'ERR_SELF_DELETE_BLOCKED';
	  END IF;
	
	  SELECT id, email, full_name, role, plan, is_active INTO v_old
	    FROM public.profiles WHERE id = p_user_id AND deleted_at IS NULL FOR UPDATE;
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_USER_NOT_FOUND_OR_ALREADY_DELETED';
	  END IF;
	
	  SELECT COUNT(*) INTO v_active_memberships_count
	    FROM public.user_store_memberships
	    WHERE user_id = p_user_id AND status = 'active';
	  IF v_active_memberships_count > 0 THEN
	    RAISE EXCEPTION 'ERR_USER_HAS_ACTIVE_MEMBERSHIPS: % active. Revoke memberships first.', v_active_memberships_count;
	  END IF;
	
	  v_anon_email := 'deleted+' || substr(p_user_id::text, 1, 8) || '@anonymized.local';
	
	  UPDATE public.profiles SET
	    deleted_at = now(),
	    deletion_reason = p_reason,
	    deleted_by = v_caller_uid,
	    is_active = false,
	    full_name = '[deleted user]',
	    email = v_anon_email,
	    ai_api_key = NULL,
	    updated_at = now()
	  WHERE id = p_user_id;
	
	  UPDATE public.user_store_memberships SET
	    status = 'revoked',
	    updated_at = now()
	  WHERE user_id = p_user_id AND status = 'active';
	
	  INSERT INTO public.user_audit_log (performed_by, target_user_id, action, old_values, new_values, metadata)
	  VALUES (
	    v_caller_uid, p_user_id, 'USER_SOFT_DELETED',
	    jsonb_build_object(
	      'email', v_old.email, 'full_name', v_old.full_name,
	      'role', v_old.role::text, 'plan', v_old.plan::text, 'is_active', v_old.is_active
	    ),
	    jsonb_build_object(
	      'email', v_anon_email, 'full_name', '[deleted user]',
	      'is_active', false, 'deleted_at', now()
	    ),
	    jsonb_build_object('reason', p_reason, 'memberships_revoked', v_active_memberships_count)
	  );
	
	  RETURN jsonb_build_object(
	    'success', true,
	    'user_id', p_user_id,
	    'status', 'soft_deleted',
	    'note', 'auth.users preserved. API route should ban via auth.admin.updateUser.'
	  );
	END;
	$function$
2026-09-13 06:16:34.088 UTC [21810] ERROR:  type "public.user_role" does not exist at character 435
2026-09-13 06:16:34.088 UTC [21810] STATEMENT:  CREATE OR REPLACE FUNCTION public.managed_toggle_user_status(
	  p_user_id uuid,
	  p_is_active boolean,
	  p_caller_id uuid DEFAULT NULL::uuid
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  v_old RECORD;
	  -- FIX H-7: anti-spoofing
	  v_caller_uid uuid := CASE WHEN auth.role() = 'service_role'
	    THEN COALESCE(p_caller_id, auth.uid()) ELSE auth.uid() END;
	  v_caller_role public.user_role;
	BEGIN
	  SELECT role INTO v_caller_role FROM public.profiles WHERE id = v_caller_uid;
	  IF v_caller_role IS NULL OR v_caller_role NOT IN ('admin', 'superadmin', 'encargado', 'manager') THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED';
	  END IF;
	
	  IF p_is_active = false AND p_user_id = v_caller_uid THEN
	    RAISE EXCEPTION 'ERR_SELF_DEACTIVATE_BLOCKED';
	  END IF;
	
	  SELECT id, is_active, full_name INTO v_old
	    FROM public.profiles WHERE id = p_user_id AND deleted_at IS NULL FOR UPDATE;
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_USER_NOT_FOUND';
	  END IF;
	
	  IF v_old.is_active = p_is_active THEN
	    RETURN jsonb_build_object('success', true, 'no_change', true);
	  END IF;
	
	  UPDATE public.profiles SET is_active = p_is_active, updated_at = now() WHERE id = p_user_id;
	
	  INSERT INTO public.user_audit_log (performed_by, target_user_id, action, old_values, new_values)
	  VALUES (
	    v_caller_uid, p_user_id,
	    CASE WHEN p_is_active THEN 'USER_ACTIVATED' ELSE 'USER_DEACTIVATED' END,
	    jsonb_build_object('is_active', v_old.is_active),
	    jsonb_build_object('is_active', p_is_active)
	  );
	
	  RETURN jsonb_build_object('success', true, 'user_id', p_user_id, 'is_active', p_is_active);
	END;
	$function$
2026-09-13 06:16:34.107 UTC [21813] ERROR:  type public.user_role does not exist
2026-09-13 06:16:34.107 UTC [21813] STATEMENT:  CREATE OR REPLACE FUNCTION public.managed_update_membership(
	  p_membership_id uuid,
	  p_role public.user_role DEFAULT NULL::user_role,
	  p_status text DEFAULT NULL::text,
	  p_caller_id uuid DEFAULT NULL::uuid
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  v_old RECORD;
	  -- FIX H-7: anti-spoofing
	  v_caller_uid uuid := CASE WHEN auth.role() = 'service_role'
	    THEN COALESCE(p_caller_id, auth.uid()) ELSE auth.uid() END;
	  v_changes jsonb := '{}'::jsonb;
	BEGIN
	  SELECT m.id, m.user_id, m.store_id, m.role, m.status
	    INTO v_old
	    FROM public.user_store_memberships m
	    WHERE m.id = p_membership_id
	    FOR UPDATE;
	
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_MEMBERSHIP_NOT_FOUND';
	  END IF;
	
	  -- FIX H-7: Autorización usa nueva sobrecarga has_store_role(p_user_id, p_store_id, p_roles)
	  -- service_role (API route) pasa p_caller_id=session.user.id → verifica acceso de ese user
	  -- authenticated pasa p_caller_id=NULL → v_caller_uid=auth.uid() → verifica su propio acceso
	  IF NOT public.has_store_role(v_caller_uid, v_old.store_id, ARRAY['admin', 'manager']) THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED: Caller must be admin or manager of the store.';
	  END IF;
	
	  IF p_role IS NOT NULL AND p_role <> v_old.role THEN
	    v_changes := v_changes || jsonb_build_object('role', jsonb_build_object('old', v_old.role::text, 'new', p_role::text));
	    UPDATE public.user_store_memberships SET role = p_role, updated_at = now() WHERE id = p_membership_id;
	  END IF;
	
	  IF p_status IS NOT NULL AND p_status <> v_old.status::text THEN
	    IF p_status NOT IN ('active', 'revoked') THEN
	      RAISE EXCEPTION 'ERR_INVALID_STATUS: %', p_status;
	    END IF;
	    v_changes := v_changes || jsonb_build_object('status', jsonb_build_object('old', v_old.status, 'new', p_status));
	    UPDATE public.user_store_memberships SET status = p_status::membership_status, updated_at = now() WHERE id = p_membership_id;
	  END IF;
	
	  IF v_changes <> '{}'::jsonb THEN
	    INSERT INTO public.user_audit_log (performed_by, target_user_id, action, old_values, new_values, metadata)
	    VALUES (
	      v_caller_uid, v_old.user_id, 'MEMBERSHIP_UPDATED',
	      jsonb_build_object('membership_id', p_membership_id, 'store_id', v_old.store_id, 'role', v_old.role::text, 'status', v_old.status),
	      v_changes,
	      jsonb_build_object('store_id', v_old.store_id)
	    );
	  END IF;
	
	  RETURN jsonb_build_object('success', true, 'membership_id', p_membership_id, 'changes', v_changes);
	END;
	$function$
2026-09-13 06:16:34.130 UTC [21816] ERROR:  type plan_t does not exist
2026-09-13 06:16:34.130 UTC [21816] STATEMENT:  CREATE OR REPLACE FUNCTION public.managed_update_tenant_plan(
	  p_tenant_id uuid,
	  p_plan plan_t,
	  p_subscription_status text DEFAULT NULL,
	  p_caller_id uuid DEFAULT NULL
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  -- FIX H-7: anti-spoofing — service_role (Stripe webhook) puede pasar NULL caller_id
	  v_caller_uid uuid := CASE WHEN auth.role() = 'service_role'
	    THEN COALESCE(p_caller_id, auth.uid()) ELSE auth.uid() END;
	  v_old_plan plan_t;
	  v_old_status text;
	  v_role public.user_role;
	BEGIN
	  -- Validación: si caller_uid es NULL (service_role anónimo para Stripe webhook), permitir
	  -- Si caller_uid NO es NULL, debe ser admin/superadmin
	  IF v_caller_uid IS NOT NULL THEN
	    SELECT role INTO v_role FROM public.profiles WHERE id = v_caller_uid;
	    IF v_role IS NULL OR v_role NOT IN ('admin', 'superadmin') THEN
	      RAISE EXCEPTION 'ERR_UNAUTHORIZED: Only admins can change tenant plan';
	    END IF;
	  END IF;
	
	  SELECT plan, subscription_status INTO v_old_plan, v_old_status
	    FROM public.tenants WHERE id = p_tenant_id FOR UPDATE;
	
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_TENANT_NOT_FOUND: %', p_tenant_id;
	  END IF;
	
	  UPDATE public.tenants SET
	    plan = p_plan,
	    subscription_status = COALESCE(p_subscription_status, subscription_status),
	    updated_at = now()
	  WHERE id = p_tenant_id;
	
	  UPDATE public.profiles SET
	    plan = p_plan,
	    updated_at = now()
	  WHERE tenant_id = p_tenant_id AND deleted_at IS NULL;
	
	  INSERT INTO public.user_audit_log (performed_by, target_user_id, action, old_values, new_values, metadata)
	  VALUES (
	    v_caller_uid, NULL, 'TENANT_PLAN_UPDATED',
	    jsonb_build_object('old_plan', v_old_plan::text, 'old_status', v_old_status),
	    jsonb_build_object('new_plan', p_plan::text, 'new_status', COALESCE(p_subscription_status, v_old_status)),
	    jsonb_build_object('tenant_id', p_tenant_id)
	  );
	
	  RETURN jsonb_build_object(
	    'success', true,
	    'tenant_id', p_tenant_id,
	    'old_plan', v_old_plan::text,
	    'new_plan', p_plan::text
	  );
	END;
	$function$
2026-09-13 06:16:34.148 UTC [21819] ERROR:  type public.user_role does not exist
2026-09-13 06:16:34.148 UTC [21819] STATEMENT:  CREATE OR REPLACE FUNCTION public.managed_update_user(
	  p_user_id uuid,
	  p_full_name text DEFAULT NULL::text,
	  p_role public.user_role DEFAULT NULL::user_role,
	  p_role_id uuid DEFAULT NULL::uuid,
	  p_is_active boolean DEFAULT NULL::boolean,
	  p_max_stores_limit int DEFAULT NULL::int,
	  p_max_users_limit int DEFAULT NULL::int,
	  p_plan plan_t DEFAULT NULL::plan_t,
	  p_caller_id uuid DEFAULT NULL::uuid
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  v_old RECORD;
	  -- FIX H-7: anti-spoofing — service_role puede pasar p_caller_id explícito
	  v_caller_uid uuid := CASE WHEN auth.role() = 'service_role'
	    THEN COALESCE(p_caller_id, auth.uid()) ELSE auth.uid() END;
	  v_caller_role public.user_role;
	  v_changes jsonb := '{}'::jsonb;
	BEGIN
	  -- Validar caller es admin (usa v_caller_uid que ahora NO es spoofable para authenticated)
	  SELECT role INTO v_caller_role FROM public.profiles WHERE id = v_caller_uid;
	  IF v_caller_role IS NULL OR v_caller_role NOT IN ('admin', 'superadmin') THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED: Only admins can update users.';
	  END IF;
	
	  SELECT * INTO v_old FROM public.profiles WHERE id = p_user_id AND deleted_at IS NULL FOR UPDATE;
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_USER_NOT_FOUND: %', p_user_id;
	  END IF;
	
	  IF p_is_active = false AND p_user_id = v_caller_uid THEN
	    RAISE EXCEPTION 'ERR_SELF_DEACTIVATE_BLOCKED: Cannot deactivate own account.';
	  END IF;
	
	  IF p_full_name IS NOT NULL AND p_full_name <> v_old.full_name THEN
	    v_changes := v_changes || jsonb_build_object('full_name', jsonb_build_object('old', v_old.full_name, 'new', p_full_name));
	    UPDATE public.profiles SET full_name = p_full_name, updated_at = now() WHERE id = p_user_id;
	  END IF;
	
	  IF p_role IS NOT NULL AND p_role <> v_old.role THEN
	    v_changes := v_changes || jsonb_build_object('role', jsonb_build_object('old', v_old.role::text, 'new', p_role::text));
	    UPDATE public.profiles SET role = p_role, updated_at = now() WHERE id = p_user_id;
	  END IF;
	
	  IF p_role_id IS NOT NULL AND (v_old.role_id IS NULL OR p_role_id <> v_old.role_id) THEN
	    v_changes := v_changes || jsonb_build_object('role_id', jsonb_build_object('old', v_old.role_id, 'new', p_role_id));
	    UPDATE public.profiles SET role_id = p_role_id, updated_at = now() WHERE id = p_user_id;
	  END IF;
	
	  IF p_is_active IS NOT NULL AND p_is_active <> v_old.is_active THEN
	    v_changes := v_changes || jsonb_build_object('is_active', jsonb_build_object('old', v_old.is_active, 'new', p_is_active));
	    UPDATE public.profiles SET is_active = p_is_active, updated_at = now() WHERE id = p_user_id;
	  END IF;
	
	  IF p_max_stores_limit IS NOT NULL AND (v_old.max_stores_limit IS NULL OR p_max_stores_limit <> v_old.max_stores_limit) THEN
	    v_changes := v_changes || jsonb_build_object('max_stores_limit', jsonb_build_object('old', v_old.max_stores_limit, 'new', p_max_stores_limit));
	    UPDATE public.profiles SET max_stores_limit = p_max_stores_limit, updated_at = now() WHERE id = p_user_id;
	  END IF;
	
	  IF p_max_users_limit IS NOT NULL AND (v_old.max_users_limit IS NULL OR p_max_users_limit <> v_old.max_users_limit) THEN
	    v_changes := v_changes || jsonb_build_object('max_users_limit', jsonb_build_object('old', v_old.max_users_limit, 'new', p_max_users_limit));
	    UPDATE public.profiles SET max_users_limit = p_max_users_limit, updated_at = now() WHERE id = p_user_id;
	  END IF;
	
	  IF p_plan IS NOT NULL AND p_plan <> v_old.plan THEN
	    v_changes := v_changes || jsonb_build_object('plan', jsonb_build_object('old', v_old.plan::text, 'new', p_plan::text));
	    UPDATE public.profiles SET plan = p_plan, updated_at = now() WHERE id = p_user_id;
	  END IF;
	
	  IF v_changes <> '{}'::jsonb THEN
	    INSERT INTO public.user_audit_log (performed_by, target_user_id, action, old_values, new_values, metadata)
	    VALUES (
	      v_caller_uid, p_user_id, 'USER_UPDATED',
	      jsonb_build_object(
	        'full_name', v_old.full_name, 'role', v_old.role::text,
	        'is_active', v_old.is_active, 'plan', v_old.plan::text
	      ),
	      v_changes,
	      jsonb_build_object('fields_changed', jsonb_object_keys(v_changes))
	    );
	  END IF;
	
	  RETURN jsonb_build_object('success', true, 'user_id', p_user_id, 'changes', v_changes);
	END;
	$function$
2026-09-13 06:16:34.179 UTC [21824] ERROR:  type "user_role" does not exist at character 204
2026-09-13 06:16:34.179 UTC [21824] STATEMENT:  CREATE OR REPLACE FUNCTION public.on_auth_user_created()
	RETURNS trigger
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO 'public', 'extensions'
	AS $function$
	DECLARE
	  v_role_id UUID;
	  v_role_enum USER_ROLE;
	  v_metadata_role TEXT;
	  v_company_name TEXT;
	  v_full_name TEXT;
	  v_tenant_id UUID;
	BEGIN
	  -- Extract role from metadata if present
	  v_metadata_role := NEW.raw_user_meta_data->>'role';
	
	  -- Normalize and convert to enum
	  IF v_metadata_role IS NOT NULL THEN
	    BEGIN
	      v_role_enum := v_metadata_role::USER_ROLE;
	    EXCEPTION WHEN OTHERS THEN
	      v_role_enum := 'tenant_admin'::USER_ROLE;
	    END;
	  ELSE
	    -- Iteración 13: self-signup users are tenant_admin by default
	    v_role_enum := 'tenant_admin'::USER_ROLE;
	  END IF;
	
	  -- Extract company_name and full_name from metadata
	  v_company_name := NEW.raw_user_meta_data->>'company_name';
	  v_full_name := COALESCE(NEW.raw_user_meta_data->>'full_name', split_part(NEW.email, '@', 1));
	
	  -- Iteración 13: Create tenant for self-signup users
	  -- If user was created by admin (via managed_create_user_v2), they may already
	  -- have a tenant_id set via p_target_user_id. In that case, skip tenant creation.
	  IF v_company_name IS NOT NULL THEN
	    INSERT INTO public.tenants (name, owner_id, plan, subscription_status, trial_ends_at, is_active)
	    VALUES (
	      v_company_name,
	      NEW.id,
	      'free'::plan_t,
	      'trial',
	      now() + interval '14 days',
	      true
	    )
	    RETURNING id INTO v_tenant_id;
	  END IF;
	
	  -- Get matching role_id from public.roles
	  SELECT id INTO v_role_id FROM public.roles
	    WHERE name = v_metadata_role OR lower(name) = lower(v_role_enum::text)
	    LIMIT 1;
	
	  -- Insert profile with tenant_id
	  INSERT INTO public.profiles (id, email, full_name, role, role_id, is_active, tenant_id, plan, created_at, updated_at)
	  VALUES (
	    NEW.id,
	    NEW.email,
	    v_full_name,
	    v_role_enum,
	    v_role_id,
	    true,
	    v_tenant_id,
	    'free'::plan_t,
	    now(),
	    now()
	  )
	  ON CONFLICT (id) DO UPDATE SET
	    email = EXCLUDED.email,
	    full_name = EXCLUDED.full_name,
	    role = EXCLUDED.role,
	    role_id = EXCLUDED.role_id,
	    tenant_id = COALESCE(profiles.tenant_id, EXCLUDED.tenant_id),
	    plan = EXCLUDED.plan,
	    is_active = true,
	    updated_at = now();
	
	  -- Audit log
	  INSERT INTO public.user_audit_log (performed_by, target_user_id, action, new_values, metadata)
	  VALUES (
	    NEW.id, NEW.id, 'USER_REGISTERED_PUBLICLY',
	    jsonb_build_object('email', NEW.email, 'role', v_role_enum::text, 'tenant_id', v_tenant_id),
	    jsonb_build_object('company_name', v_company_name, 'source', 'self_signup')
	  );
	
	  RETURN NEW;
	END;
	$function$
2026-09-13 06:16:34.235 UTC [21837] ERROR:  type adjustment_item[] does not exist
2026-09-13 06:16:34.235 UTC [21837] STATEMENT:  CREATE OR REPLACE FUNCTION public.process_inventory_adjustment(p_store_id uuid, p_cashier_id uuid, p_items adjustment_item[], p_operation_date timestamp with time zone DEFAULT NULL::timestamp with time zone)
	 RETURNS uuid
	 LANGUAGE plpgsql
	 SECURITY DEFINER
	 SET search_path TO 'public'
	AS $function$
	DECLARE
	  v_adjustment_id UUID;
	  v_item public.adjustment_item;
	  v_difference NUMERIC;
	  v_effective_date TIMESTAMP WITH TIME ZONE := COALESCE(p_operation_date, NOW());
	BEGIN
	  -- Validación forward-only locking
	  PERFORM public.validate_operation_date(p_operation_date, p_store_id);
	
	  INSERT INTO public.inventory_adjustments (store_id, created_by, status, created_at)
	  VALUES (p_store_id, p_cashier_id, 'PROCESSING', v_effective_date)
	  RETURNING id INTO v_adjustment_id;
	
	  FOREACH v_item IN ARRAY p_items
	  LOOP
	    v_difference := v_item.counted_quantity - v_item.expected_quantity;
	    INSERT INTO public.inventory_adjustment_items (adjustment_id, product_id, expected_quantity, counted_quantity, created_at)
	    VALUES (v_adjustment_id, v_item.product_id, v_item.expected_quantity, v_item.counted_quantity, v_effective_date);
	
	    PERFORM public.register_stock_movement(
	        p_product_id := v_item.product_id,
	        p_store_id := p_store_id,
	        p_user_id := p_cashier_id,
	        p_quantity := v_difference,
	        p_movement_type := 'adjustment',
	        p_operation_date := v_effective_date
	    );
	  END LOOP;
	
	  UPDATE public.inventory_adjustments SET status = 'COMPLETED', updated_at = v_effective_date
	  WHERE id = v_adjustment_id;
	  RETURN v_adjustment_id;
	END;
	$function$
2026-09-13 06:16:34.345 UTC [21844] ERROR:  type "public.purchase_status_enum" does not exist at character 425
2026-09-13 06:16:34.345 UTC [21844] STATEMENT:  CREATE OR REPLACE FUNCTION public.receive_against_po(
	  p_po_id uuid,
	  p_received_items jsonb DEFAULT '[]'::jsonb,
	  p_user_id uuid DEFAULT NULL,
	  p_reception_date timestamptz DEFAULT now(),
	  p_invoice_number text DEFAULT NULL
	) RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO 'public', 'extensions'
	AS $func$
	DECLARE
	  v_store_id            uuid;
	  v_supplier_name       text;
	  v_po_status           public.purchase_status_enum;
	  v_item                jsonb;
	  v_item_id             uuid;
	  v_qty                 numeric;
	  v_qty_ordered         numeric;
	  v_qty_received_before numeric;
	  v_product_id          uuid;
	  v_unit_cost           numeric;
	  v_product_name        text;
	  v_all_received        boolean;
	  v_any_received        boolean;
	  v_new_status          public.purchase_status_enum;
	  v_receipt_id          uuid;
	  v_reception_items     jsonb := '[]'::jsonb;
	  v_count               integer := 0;
	  v_po_number           text;
	  v_invoice_to_pass     text;
	  v_sorted_items        jsonb;
	BEGIN
	  -- ─── 1. Cargar OC con lock exclusivo ───
	  SELECT po.store_id, po.supplier_name, po.status, po.po_number
	    INTO v_store_id, v_supplier_name, v_po_status, v_po_number
	  FROM public.purchase_orders po
	  WHERE po.id = p_po_id
	  FOR UPDATE;
	
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_PO_NOT_FOUND';
	  END IF;
	
	  -- ─── 2. Validar acceso (tenant-aware via has_store_access) ───
	  IF NOT public.has_store_access(v_store_id) THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED';
	  END IF;
	
	  -- ─── 3. Validar estado de la OC ───
	  IF v_po_status = 'cancelled' THEN
	    RAISE EXCEPTION 'ERR_PO_CANCELLED';
	  END IF;
	  -- Solo se puede recibir contra draft, sent o partial.
	  IF v_po_status NOT IN ('draft', 'sent', 'partial') THEN
	    RAISE EXCEPTION 'ERR_PO_NOT_RECEIVABLE: status % is terminal', v_po_status;
	  END IF;
	
	  -- ─── 4. Validar items no vacío (parity B3 register_reception) ───
	  IF p_received_items IS NULL OR jsonb_array_length(p_received_items) = 0 THEN
	    RAISE EXCEPTION 'ERR_EMPTY_ITEMS';
	  END IF;
	
	  -- ─── 5. Condición #5 (anti-deadlock): Ordenar items por po_item_id ASC ───
	  SELECT jsonb_agg(elem ORDER BY (elem->>'po_item_id'))
	    INTO v_sorted_items
	  FROM jsonb_array_elements(p_received_items) AS elem;
	
	  -- ─── 6. Procesar cada item: VALIDAR antes de actualizar ───
	  FOR v_item IN SELECT * FROM jsonb_array_elements(v_sorted_items) LOOP
	    v_item_id := NULLIF(v_item->>'po_item_id', '')::uuid;
	    v_qty     := COALESCE((v_item->>'quantity_received')::numeric, 0);
	
	    -- Validar po_item_id presente
	    IF v_item_id IS NULL THEN
	      RAISE EXCEPTION 'ERR_ITEM_ID_REQUIRED: missing po_item_id in received_items';
	    END IF;
	
	    -- Medio #6: qty > 0
	    IF v_qty <= 0 THEN
	      RAISE EXCEPTION 'ERR_NEGATIVE_QTY: qty % for item % must be > 0', v_qty, v_item_id;
	    END IF;
	
	    -- Cargar estado actual del item con lock FOR UPDATE
	    SELECT product_id, quantity_ordered, quantity_received, unit_cost, product_name
	      INTO v_product_id, v_qty_ordered, v_qty_received_before, v_unit_cost, v_product_name
	    FROM public.purchase_order_items
	    WHERE id = v_item_id AND po_id = p_po_id
	    FOR UPDATE;
	
	    IF NOT FOUND THEN
	      RAISE EXCEPTION 'ERR_ITEM_NOT_FOUND: %', v_item_id;
	    END IF;
	
	    -- Alto #5: over-receive check
	    IF (v_qty_received_before + v_qty) > v_qty_ordered THEN
	      RAISE EXCEPTION 'ERR_OVER_RECEIVE: item % ordered %, already received %, attempting %',
	        v_item_id, v_qty_ordered, v_qty_received_before, v_qty;
	    END IF;
	
	    -- Condición #1: todo item debe tener product_id NOT NULL
	    IF v_product_id IS NULL THEN
	      RAISE EXCEPTION 'ERR_PRODUCT_ID_REQUIRED: item % has no product_id', v_item_id;
	    END IF;
	
	    -- Medio #9: producto debe existir en store
	    IF NOT EXISTS (
	      SELECT 1 FROM public.products p
	      WHERE p.id = v_product_id AND p.store_id = v_store_id
	    ) THEN
	      RAISE EXCEPTION 'ERR_PRODUCT_NOT_IN_STORE: product % not in store %',
	        v_product_id, v_store_id;
	    END IF;
	
	    -- Acumular para register_reception (formato esperado: product_id, quantity, unit_cost, moneda, tasa)
	    v_reception_items := v_reception_items || jsonb_build_object(
	      'product_id',   v_product_id,
	      'product_name', v_product_name,
	      'quantity',     v_qty,
	      'unit_cost',    v_unit_cost,
	      'moneda_recepcion', 'CUP',
	      'tasa_cambio_recepcion', 1.0
	    );
	    v_count := v_count + 1;
	  END LOOP;
	
	  -- ─── 7. Condición #3: reference_doc NULL si no viene invoice_number ───
	  -- Evita colisión con UNIQUE INDEX idx_receipts_store_reference_doc
	  -- (store_id, reference_doc) en partial indexes.
	  -- El vínculo real receipt↔PO es receipts.po_id (G6).
	  IF p_invoice_number IS NULL OR p_invoice_number = '' THEN
	    v_invoice_to_pass := NULL;
	  ELSE
	    v_invoice_to_pass := p_invoice_number;
	  END IF;
	
	  -- ─── 8. Crear receipt vinculado vía register_reception ───
	  -- register_reception aplica las 8 validaciones B1-B5 + C1-C3 del v2.23.0
	  -- (no se relajan). G6 añade p_po_id opcional.
	  v_receipt_id := public.register_reception(
	    p_store_id        := v_store_id,
	    p_supplier        := v_supplier_name,
	    p_reception_date  := p_reception_date,
	    p_invoice_number  := v_invoice_to_pass,
	    p_items           := v_reception_items,
	    p_user_id         := p_user_id,
	    p_po_id           := p_po_id
	  );
	
	  -- ─── 9. Actualizar quantity_received en purchase_order_items ───
	  -- (SECURITY DEFINER bypasses RLS DENY UPDATE de G5)
	  FOR v_item IN SELECT * FROM jsonb_array_elements(v_sorted_items) LOOP
	    v_item_id := NULLIF(v_item->>'po_item_id', '')::uuid;
	    v_qty     := (v_item->>'quantity_received')::numeric;
	
	    UPDATE public.purchase_order_items
	    SET quantity_received = quantity_received + v_qty
	    WHERE id = v_item_id AND po_id = p_po_id;
	  END LOOP;
	
	  -- ─── 10. Recalcular status global (cast EXPLÍCITO a enum) ───
	  SELECT
	    BOOL_AND(quantity_received >= quantity_ordered),
	    BOOL_OR(quantity_received > 0)
	  INTO v_all_received, v_any_received
	  FROM public.purchase_order_items WHERE po_id = p_po_id;
	
	  v_new_status :=
	    CASE
	      WHEN v_all_received THEN 'received'::public.purchase_status_enum
	      WHEN v_any_received THEN 'partial'::public.purchase_status_enum
	      ELSE 'sent'::public.purchase_status_enum
	    END;
	
	  -- ─── 11. Actualizar OC + vincular receipt en notas ───
	  UPDATE public.purchase_orders
	  SET status       = v_new_status,
	      received_at  = CASE WHEN v_new_status = 'received' THEN NOW() ELSE received_at END,
	      notes        = COALESCE(notes, '') ||
	                     CASE WHEN COALESCE(notes, '') = '' THEN '' ELSE E'\n' END ||
	                     '[Receipt ' || v_receipt_id::text || ' linked at ' || NOW()::text || ']'
	  WHERE id = p_po_id;
	
	  -- ─── 12. CxP (Grupo 6): marcar receipt con payment_status='unpaid' ───
	  -- Solo si aún no está seteado (no sobrescribir si register_reception ya lo puso).
	  UPDATE public.receipts
	  SET payment_status     = COALESCE(NULLIF(payment_status, ''), 'unpaid'),
	      payment_terms_days = COALESCE(payment_terms_days, 30),
	      due_date           = COALESCE(due_date, (p_reception_date::date + INTERVAL '30 days')::date)
	  WHERE id = v_receipt_id;
	
	  -- ─── 13. Auditoría ───
	  INSERT INTO public.audit_logs (user_id, store_id, action, table_name, record_id, metadata)
	  VALUES (
	    p_user_id, v_store_id, 'po_received', 'purchase_orders', p_po_id,
	    jsonb_build_object(
	      'new_status', v_new_status::text,
	      'items_received', v_count,
	      'receipt_id', v_receipt_id,
	      'po_number', v_po_number,
	      'invoice_number', v_invoice_to_pass
	    )
	  );
	
	  RETURN jsonb_build_object(
	    'status', 'success',
	    'po_status', v_new_status::text,
	    'receipt_id', v_receipt_id,
	    'items_received', v_count
	  );
	END;
	$func$
2026-09-13 06:16:34.612 UTC [21880] ERROR:  type purchase_status_enum does not exist
2026-09-13 06:16:34.612 UTC [21880] STATEMENT:  CREATE OR REPLACE FUNCTION public.set_purchase_order_status(p_po_id uuid, p_new_status purchase_status_enum, p_user_id uuid DEFAULT NULL::uuid, p_reason text DEFAULT NULL::text)
	 RETURNS jsonb
	 LANGUAGE plpgsql
	 SECURITY DEFINER
	 SET search_path TO 'public', 'extensions'
	AS $function$
	DECLARE
	  v_store_id     uuid;
	  v_current      public.purchase_status_enum;
	  v_po_number    text;
	  v_allowed      text[];
	  v_is_allowed   boolean := false;
	BEGIN
	  -- ─── 1. Cargar PO con lock exclusivo ───
	  SELECT store_id, status, po_number
	    INTO v_store_id, v_current, v_po_number
	  FROM public.purchase_orders
	  WHERE id = p_po_id
	  FOR UPDATE;
	
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_PO_NOT_FOUND';
	  END IF;
	
	  -- ─── 2. Validar acceso (tenant-aware) ───
	  IF NOT public.has_store_access(v_store_id) THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED';
	  END IF;
	
	  -- ─── 3. Si no hay cambio, retornar success sin hacer nada ───
	  IF v_current = p_new_status THEN
	    RETURN jsonb_build_object(
	      'status', 'no_change',
	      'po_status', v_current::text,
	      'po_number', v_po_number
	    );
	  END IF;
	
	  -- ─── 4. Definir transiciones permitidas (state machine) ───
	  -- 'partial' y 'received' NUNCA son destino válido manualmente
	  -- (solo vía receive_against_po).
	  v_allowed := CASE v_current
	    WHEN 'draft'   THEN ARRAY['sent', 'cancelled']
	    WHEN 'sent'    THEN ARRAY['cancelled']
	    WHEN 'partial' THEN ARRAY['cancelled']
	    ELSE ARRAY[]::text[]  -- received, cancelled: terminal, no transitions
	  END;
	
	  -- ─── 5. Verificar transición permitida ───
	  SELECT EXISTS(SELECT 1 FROM unnest(v_allowed) a WHERE a = p_new_status::text)
	    INTO v_is_allowed;
	
	  IF NOT v_is_allowed THEN
	    RAISE EXCEPTION 'ERR_INVALID_TRANSITION: % → % not allowed (allowed: %)',
	      v_current::text, p_new_status::text,
	      CASE WHEN array_length(v_allowed, 1) IS NULL THEN 'none' ELSE array_to_string(v_allowed, ', ') END;
	  END IF;
	
	  -- ─── 6. Aplicar transición ───
	  UPDATE public.purchase_orders
	  SET status      = p_new_status,
	      received_at = CASE WHEN p_new_status = 'received' THEN NOW() ELSE received_at END
	  WHERE id = p_po_id;
	
	  -- ─── 7. Auditoría (action = PO_STATUS_CHANGED) ───
	  INSERT INTO public.audit_logs (user_id, store_id, action, table_name, record_id, metadata)
	  VALUES (
	    CASE WHEN auth.role() = 'service_role' THEN COALESCE(p_user_id, auth.uid()) ELSE auth.uid() END, v_store_id, 'PO_STATUS_CHANGED', 'purchase_orders', p_po_id,
	    jsonb_build_object(
	      'po_number', v_po_number,
	      'from_status', v_current::text,
	      'to_status', p_new_status::text,
	      'reason', p_reason
	    )
	  );
	
	  RETURN jsonb_build_object(
	    'status', 'success',
	    'po_status', p_new_status::text,
	    'po_number', v_po_number,
	    'previous_status', v_current::text
	  );
	END;
	$function$
2026-09-13 06:16:34.712 UTC [21904] ERROR:  type "user_role" does not exist at character 100
2026-09-13 06:16:34.712 UTC [21904] STATEMENT:  CREATE OR REPLACE FUNCTION public.validate_active_store()
	RETURNS TRIGGER AS $$
	DECLARE
	    v_role user_role;
	BEGIN
	    v_role := NEW.role;
	
	    -- Si role es 'costo' o 'admin' o 'superadmin', active_store_id es opcional
	    IF v_role = 'costo'::user_role
	       OR v_role = 'admin'::user_role
	       OR v_role = 'superadmin'::user_role THEN
	        -- Solo validar si active_store_id cambió o es INSERT
	        IF TG_OP = 'INSERT' OR (OLD.active_store_id IS DISTINCT FROM NEW.active_store_id) THEN
	            IF NEW.active_store_id IS NOT NULL THEN
	                IF NOT EXISTS (
	                    SELECT 1 FROM public.user_store_memberships
	                    WHERE user_id = NEW.id
	                      AND store_id = NEW.active_store_id
	                      AND status = 'active'
	                ) THEN
	                    IF TG_OP = 'UPDATE' THEN
	                        RAISE EXCEPTION 'ERR_INVALID_ACTIVE_STORE: El usuario no tiene membership activa en la tienda seleccionada.';
	                    END IF;
	                END IF;
	            END IF;
	        END IF;
	        RETURN NEW;
	    END IF;
	
	    -- Para roles operativos (encargado, clerk, warehouse, manager, usuario)
	    IF TG_OP = 'INSERT' THEN
	        -- En INSERT: ser permisivo. La función managed_create_user inserta
	        -- memberships justo después. No validar nada aquí.
	        RETURN NEW;
	    ELSE
	        -- UPDATE: solo validar si active_store_id cambió realmente
	        -- (evita bloquear updates de otros campos en perfiles intermedios)
	        IF OLD.active_store_id IS NOT DISTINCT FROM NEW.active_store_id THEN
	            -- active_store_id no cambió → permitir el UPDATE sin validar
	            RETURN NEW;
	        END IF;
	
	        -- active_store_id cambió → validar consistencia
	        IF NEW.active_store_id IS NULL THEN
	            IF v_role IN ('encargado'::user_role, 'clerk'::user_role, 'warehouse'::user_role) THEN
	                RAISE EXCEPTION 'ERR_STORE_REQUIRED: El rol % requiere una tienda activa asignada.', v_role;
	            END IF;
	        ELSE
	            IF NOT EXISTS (
	                SELECT 1 FROM public.user_store_memberships
	                WHERE user_id = NEW.id
	                  AND store_id = NEW.active_store_id
	                  AND status = 'active'
	            ) THEN
	                RAISE EXCEPTION 'ERR_INVALID_ACTIVE_STORE: El usuario no tiene membership activa en la tienda seleccionada.';
	            END IF;
	        END IF;
	        RETURN NEW;
	    END IF;
	END;
	$$ LANGUAGE plpgsql SECURITY DEFINER
2026-09-13 06:16:34.758 UTC [21913] ERROR:  type "public.user_role" does not exist at character 270
2026-09-13 06:16:34.758 UTC [21913] STATEMENT:  CREATE OR REPLACE FUNCTION public.validate_tenant_access(
	  p_user_id uuid,
	  p_store_id uuid
	)
	RETURNS boolean
	LANGUAGE plpgsql
	STABLE SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  v_user_tenant uuid;
	  v_store_tenant uuid;
	  v_user_role public.user_role;
	BEGIN
	  -- Global admin bypasses tenant check
	  SELECT role, tenant_id INTO v_user_role, v_user_tenant
	    FROM public.profiles WHERE id = p_user_id;
	
	  IF v_user_role IN ('admin', 'superadmin') THEN
	    RETURN true;
	  END IF;
	
	  -- Get store's tenant
	  SELECT tenant_id INTO v_store_tenant FROM public.stores WHERE id = p_store_id;
	
	  IF v_store_tenant IS NULL THEN
	    -- Legacy store without tenant — allow (backward compat)
	    RETURN true;
	  END IF;
	
	  -- Check tenant match
	  RETURN v_user_tenant = v_store_tenant;
	END;
	$function$
2026-09-13 06:16:34.925 UTC [21946] ERROR:  type "public.user_role" does not exist at character 301
2026-09-13 06:16:34.925 UTC [21946] STATEMENT:  CREATE OR REPLACE FUNCTION public.bulk_assign_memberships(
	  p_user_id uuid,
	  p_assignments jsonb
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  v_assignment jsonb;
	  v_affected int := 0;
	  v_failed int := 0;
	  v_store_id uuid;
	  v_role public.user_role;
	  v_status text;
	  v_caller_uid uuid := auth.uid();
	  v_changes jsonb := '[]'::jsonb;
	BEGIN
	  IF v_caller_uid IS NULL THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED';
	  END IF;
	
	  FOR v_assignment IN SELECT * FROM jsonb_array_elements(p_assignments) LOOP
	    BEGIN
	      v_store_id := (v_assignment->>'store_id')::uuid;
	      v_role := (v_assignment->>'role')::public.user_role;
	      v_status := COALESCE(v_assignment->>'status', 'active');
	
	      -- Validar caller tiene acceso al store
	      IF NOT public.is_admin() AND NOT public.has_store_role(v_store_id, ARRAY['admin', 'manager']) THEN
	        v_failed := v_failed + 1;
	        CONTINUE;
	      END IF;
	
	      INSERT INTO public.user_store_memberships (user_id, store_id, role, status)
	      VALUES (p_user_id, v_store_id, v_role, v_status)
	      ON CONFLICT (user_id, store_id) DO UPDATE SET
	        role = EXCLUDED.role,
	        status = EXCLUDED.status,
	        updated_at = now();
	
	      v_changes := v_changes || jsonb_build_object(jsonb_build_object(
	        'store_id', v_store_id,
	        'role', v_role::text,
	        'status', v_status
	      ));
	
	      v_affected := v_affected + 1;
	    EXCEPTION
	      WHEN foreign_key_violation THEN
	        v_failed := v_failed + 1;
	    END;
	  END LOOP;
	
	  -- Audit log atómico (solo si hubo cambios)
	  IF v_affected > 0 THEN
	    INSERT INTO public.user_audit_log (performed_by, target_user_id, action, new_values, metadata)
	    VALUES (
	      v_caller_uid, p_user_id, 'MEMBERSHIPS_BULK_ASSIGNED',
	      jsonb_build_object('assignments', v_changes),
	      jsonb_build_object('affected', v_affected, 'failed', v_failed)
	    );
	  END IF;
	
	  RETURN jsonb_build_object(
	    'success', true,
	    'affected', v_affected,
	    'failed', v_failed,
	    'user_id', p_user_id
	  );
	END;
	$function$
2026-09-13 06:16:34.966 UTC [21949] ERROR:  syntax error at or near "REVOKE" at character 5039
2026-09-13 06:16:34.966 UTC [21949] STATEMENT:  CREATE OR REPLACE FUNCTION public.bulk_soft_delete_stores(p_store_ids uuid[], p_deleted_by uuid, p_confirmation_token text, p_override_token text DEFAULT NULL::text, p_reason text DEFAULT NULL::text)
	 RETURNS jsonb
	 LANGUAGE plpgsql
	 SECURITY DEFINER
	 SET search_path TO 'public'
	AS $function$
	DECLARE
	  v_store_id UUID;
	  v_validation JSONB;
	  v_blockers JSONB;
	  v_errors JSONB[] := '{}'::jsonb[];
	  v_processed INTEGER := 0;
	  v_token_valid BOOLEAN;
	  v_has_protected BOOLEAN;
	  v_override_valid BOOLEAN;
	  v_confirmation_record RECORD;
	  v_override_record RECORD;
	  v_caller_role TEXT;
	BEGIN
	  -- ============================================================
	  -- AUTH CHECK: Solo admin puede ejecutar esta función
	  -- ============================================================
	  IF auth.uid() IS NOT NULL THEN
	    SELECT role INTO v_caller_role FROM public.profiles WHERE id = auth.uid();
	    IF v_caller_role IS NULL OR v_caller_role != 'admin' THEN
	      RAISE EXCEPTION 'ERR_PERMISSION_DENIED: Solo admin puede ejecutar bulk_soft_delete_stores';
	    END IF;
	  END IF;
	  -- Si auth.uid() IS NULL, es service_role — permitir
	
	  -- ============================================================
	  -- 1. VALIDATE confirmation_token
	  -- ============================================================
	  SELECT * INTO v_confirmation_record
	  FROM public.bulk_confirmation_tokens
	  WHERE token = p_confirmation_token
	    AND action = 'delete'
	    AND expires_at > NOW()
	    AND consumed_at IS NULL
	    AND is_override = false;
	
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_INVALID_CONFIRMATION_TOKEN';
	  END IF;
	
	  IF v_confirmation_record.store_ids != p_store_ids THEN
	    RAISE EXCEPTION 'ERR_STORE_IDS_MISMATCH';
	  END IF;
	
	  -- ============================================================
	  -- 2. VALIDATE tiendas protegidas requieren override_token
	  -- ============================================================
	  SELECT EXISTS(
	    SELECT 1 FROM public.stores
	    WHERE id = ANY(p_store_ids) AND backup_restore_protected = true
	  ) INTO v_has_protected;
	
	  IF v_has_protected THEN
	    IF p_override_token IS NULL THEN
	      RAISE EXCEPTION 'ERR_OVERRIDE_REQUIRED';
	    END IF;
	
	    SELECT * INTO v_override_record
	    FROM public.bulk_confirmation_tokens
	    WHERE token = p_override_token
	      AND is_override = true
	      AND override_for = p_confirmation_token
	      AND expires_at > NOW()
	      AND consumed_at IS NULL;
	
	    IF NOT FOUND THEN
	      RAISE EXCEPTION 'ERR_INVALID_OVERRIDE_TOKEN';
	    END IF;
	
	    IF v_override_record.store_ids != v_confirmation_record.store_ids THEN
	      RAISE EXCEPTION 'ERR_OVERRIDE_STORE_IDS_MISMATCH';
	    END IF;
	
	    IF v_override_record.created_by = v_confirmation_record.created_by THEN
	      RAISE EXCEPTION 'ERR_SAME_USER_OVERRIDE';
	    END IF;
	
	    UPDATE public.bulk_confirmation_tokens SET consumed_at = NOW()
	    WHERE token = p_override_token;
	  END IF;
	
	  UPDATE public.bulk_confirmation_tokens SET consumed_at = NOW()
	  WHERE token = p_confirmation_token;
	
	  -- ============================================================
	  -- 3. VALIDATE todas las tiendas
	  -- ============================================================
	  FOREACH v_store_id IN ARRAY p_store_ids LOOP
	    IF NOT EXISTS(SELECT 1 FROM public.stores WHERE id = v_store_id AND is_active = true) THEN
	      v_errors := array_append(v_errors, jsonb_build_object(
	        'store_id', v_store_id, 'reason', 'STORE_NOT_FOUND_OR_INACTIVE'
	      ));
	      CONTINUE;
	    END IF;
	
	    SELECT public.validate_store_can_be_modified(v_store_id, 'soft_delete') INTO v_validation;
	    v_blockers := v_validation->'blockers';
	
	    IF v_validation->>'can_modify' != 'true' THEN
	      v_errors := array_append(v_errors, jsonb_build_object(
	        'store_id', v_store_id, 'reason', 'HAS_BLOCKING_DEPENDENCIES', 'blockers', v_blockers
	      ));
	    END IF;
	  END LOOP;
	
	  IF array_length(v_errors, 1) IS NOT NULL THEN
	    RETURN jsonb_build_object(
	      'status', 'FAILED', 'processed', 0,
	      'total_requested', array_length(p_store_ids, 1),
	      'errors', to_jsonb(v_errors), 'reason', p_reason
	    );
	  END IF;
	
	  -- ============================================================
	  -- 4. EXECUTE
	  -- ============================================================
	  FOREACH v_store_id IN ARRAY p_store_ids LOOP
	    PERFORM public.soft_delete_store(v_store_id, p_deleted_by);
	    v_processed := v_processed + 1;
	  END LOOP;
	
	  INSERT INTO public.audit_logs (action, table_name, record_id, metadata)
	  VALUES (
	    'bulk_store_deleted', 'stores', NULL,
	    jsonb_build_object(
	      'store_ids', p_store_ids, 'deleted_by', p_deleted_by,
	      'reason', p_reason, 'processed', v_processed,
	      'had_protected_stores', v_has_protected,
	      'override_used', p_override_token IS NOT NULL, 'deleted_at', NOW()
	    )
	  );
	
	  RETURN jsonb_build_object(
	    'status', 'COMPLETED', 'processed', v_processed,
	    'total_requested', array_length(p_store_ids, 1),
	    'errors', '[]'::jsonb, 'reason', p_reason
	  );
	END;
	$function$
	 
	REVOKE EXECUTE ON FUNCTION public.bulk_soft_delete_stores FROM PUBLIC, anon, authenticated
2026-09-13 06:16:35.011 UTC [21956] ERROR:  relation "received_services" does not exist
2026-09-13 06:16:35.011 UTC [21956] CONTEXT:  compilation of PL/pgSQL function "calculate_service_distribution" near line 3
2026-09-13 06:16:35.011 UTC [21956] STATEMENT:  CREATE OR REPLACE FUNCTION calculate_service_distribution(
	  p_service_id UUID
	) RETURNS TABLE(
	  receipt_item_id UUID,
	  product_id UUID,
	  distribution_amount NUMERIC,
	  distribution_percentage NUMERIC
	) LANGUAGE plpgsql SECURITY DEFINER AS $$
	DECLARE
	  v_service received_services%ROWTYPE;
	  v_method TEXT;
	  v_total_amount NUMERIC;
	  v_total_value NUMERIC DEFAULT 0;
	  v_total_qty NUMERIC DEFAULT 0;
	  v_link service_reception_links%ROWTYPE;
	  v_receipt_id UUID;
	  v_item receipt_items%ROWTYPE;
	  v_allocated NUMERIC;
	BEGIN
	  SELECT * INTO v_service FROM received_services WHERE id = p_service_id;
	  IF NOT FOUND THEN RETURN; END IF;
	  
	  v_method := v_service.distribution_method;
	  v_total_amount := v_service.total_amount;
	
	  -- Para cada recepción vinculada
	  FOR v_link IN SELECT * FROM service_reception_links WHERE service_id = p_service_id AND allocated_amount > 0 LOOP
	    v_receipt_id := v_link.receipt_id;
	    v_allocated := v_link.allocated_amount;
	
	    -- Calcular base de distribución según método
	    IF v_method = 'amount' THEN
	      SELECT COALESCE(SUM(quantity * unit_cost), 0) INTO v_total_value
	      FROM receipt_items WHERE receipt_id = v_receipt_id;
	      
	      IF v_total_value > 0 THEN
	        FOR v_item IN SELECT * FROM receipt_items WHERE receipt_id = v_receipt_id LOOP
	          distribution_amount := v_allocated * (v_item.quantity * v_item.unit_cost / v_total_value);
	          distribution_percentage := (v_item.quantity * v_item.unit_cost / v_total_value) * 100;
	          receipt_item_id := v_item.id;
	          product_id := v_item.product_id;
	          RETURN NEXT;
	        END LOOP;
	      END IF;
	
	    ELSIF v_method = 'quantity' THEN
	      SELECT COALESCE(SUM(quantity), 0) INTO v_total_qty
	      FROM receipt_items WHERE receipt_id = v_receipt_id;
	      
	      IF v_total_qty > 0 THEN
	        FOR v_item IN SELECT * FROM receipt_items WHERE receipt_id = v_receipt_id LOOP
	          distribution_amount := v_allocated * (v_item.quantity / v_total_qty);
	          distribution_percentage := (v_item.quantity / v_total_qty) * 100;
	          receipt_item_id := v_item.id;
	          product_id := v_item.product_id;
	          RETURN NEXT;
	        END LOOP;
	      END IF;
	
	    ELSIF v_method = 'manual' THEN
	      -- En manual, la distribución se guarda directamente desde el cliente
	      RETURN;
	    END IF;
	  END LOOP;
	END;
	$$
2026-09-13 06:16:35.094 UTC [21967] ERROR:  syntax error at or near "REVOKE" at character 678
2026-09-13 06:16:35.094 UTC [21967] STATEMENT:  CREATE OR REPLACE FUNCTION public.check_bulk_ops_hourly_limit(p_user_id uuid, p_plan text)
	 RETURNS jsonb
	 LANGUAGE plpgsql
	 STABLE SECURITY DEFINER
	 SET search_path TO 'public'
	AS $function$
	DECLARE
	  v_limit INTEGER;
	  v_used INTEGER;
	BEGIN
	  v_limit := CASE p_plan
	    WHEN 'free' THEN 1
	    WHEN 'pro' THEN 20
	    WHEN 'enterprise' THEN 999999
	    ELSE 1
	  END;
	
	  SELECT COUNT(*) INTO v_used
	  FROM public.bulk_ops_log
	  WHERE user_id = p_user_id
	    AND initiated_at > NOW() - INTERVAL '1 hour';
	
	  RETURN jsonb_build_object(
	    'allowed', v_used < v_limit,
	    'used', v_used,
	    'limit', v_limit,
	    'remaining', GREATEST(0, v_limit - v_used)
	  );
	END;
	$function$
	 
	REVOKE EXECUTE ON FUNCTION public.check_bulk_ops_hourly_limit FROM PUBLIC, anon, authenticated
2026-09-13 06:16:35.125 UTC [21972] ERROR:  type plan_t does not exist
2026-09-13 06:16:35.125 UTC [21972] STATEMENT:  CREATE OR REPLACE FUNCTION public.check_tenant_store_quota(
	  p_tenant_id uuid,
	  p_plan plan_t DEFAULT NULL
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  v_plan plan_t;
	  v_limit int;
	  v_current int;
	  v_allowed boolean;
	BEGIN
	  -- Get plan from tenant if not passed
	  IF p_plan IS NULL THEN
	    SELECT plan INTO v_plan FROM public.tenants WHERE id = p_tenant_id;
	  ELSE
	    v_plan := p_plan;
	  END IF;
	
	  -- Plan limits (unified source of truth)
	  v_limit := CASE v_plan
	    WHEN 'free'::plan_t THEN 1
	    WHEN 'pro'::plan_t THEN 3
	    WHEN 'enterprise'::plan_t THEN 10
	    ELSE 1
	  END;
	
	  -- Count active stores in tenant
	  SELECT COUNT(*) INTO v_current
	    FROM public.stores
	    WHERE tenant_id = p_tenant_id AND is_active = true;
	
	  v_allowed := v_current < v_limit;
	
	  RETURN jsonb_build_object(
	    'allowed', v_allowed,
	    'current', v_current,
	    'limit', v_limit,
	    'plan', v_plan::text,
	    'tenant_id', p_tenant_id
	  );
	END;
	$function$
2026-09-13 06:16:35.234 UTC [21999] ERROR:  relation "public.profiles" does not exist at character 166
2026-09-13 06:16:35.234 UTC [21999] STATEMENT:  CREATE OR REPLACE FUNCTION public.current_user_tenant_id()
	RETURNS uuid
	LANGUAGE sql
	STABLE
	SECURITY DEFINER
	SET search_path TO public
	AS $$
	  SELECT tenant_id FROM public.profiles WHERE id = auth.uid();
	$$
2026-09-13 06:16:35.268 UTC [22005] ERROR:  relation "received_services" does not exist
2026-09-13 06:16:35.268 UTC [22005] CONTEXT:  compilation of PL/pgSQL function "distribute_service_cost_v2" near line 6
2026-09-13 06:16:35.268 UTC [22005] STATEMENT:  CREATE OR REPLACE FUNCTION public.distribute_service_cost_v2(
	  p_service_id uuid,
	  p_user_id uuid DEFAULT NULL
	) RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO 'public', 'extensions'
	AS $func$
	DECLARE
	  v_store_id uuid;
	  v_status text;
	  v_total_amount numeric;
	  v_service received_services%ROWTYPE;
	  v_method text;
	  v_link service_reception_links%ROWTYPE;
	  v_item receipt_items%ROWTYPE;
	  v_total_value numeric := 0;
	  v_total_qty numeric := 0;
	  v_allocated numeric;
	  v_dist_count integer := 0;
	  v_caller_uid uuid := COALESCE(p_user_id, auth.uid());
	BEGIN
	  PERFORM pg_advisory_xact_lock(hashtext(p_service_id::text));
	
	  SELECT * INTO v_service FROM received_services WHERE id = p_service_id FOR UPDATE;
	  IF NOT FOUND THEN RAISE EXCEPTION 'ERR_SERVICE_NOT_FOUND'; END IF;
	
	  v_store_id := v_service.store_id;
	  v_status := v_service.status;
	  v_total_amount := v_service.total_amount;
	  v_method := v_service.distribution_method;
	
	  IF NOT public.has_store_access(v_store_id) THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED';
	  END IF;
	
	  IF v_status != 'active' THEN
	    RAISE EXCEPTION 'ERR_SERVICE_NOT_ACTIVE: status % is not active', v_status;
	  END IF;
	
	  IF v_method = 'manual' THEN
	    RAISE EXCEPTION 'ERR_MANUAL_METHOD: use link_receipts_to_service for manual distribution';
	  END IF;
	
	  DELETE FROM service_cost_distributions WHERE service_id = p_service_id;
	
	  FOR v_link IN SELECT * FROM service_reception_links WHERE service_id = p_service_id AND allocated_amount > 0 ORDER BY receipt_id LOOP
	    v_allocated := v_link.allocated_amount;
	
	    IF v_method = 'amount' THEN
	      SELECT COALESCE(SUM(quantity * unit_cost), 0) INTO v_total_value
	      FROM receipt_items WHERE receipt_id = v_link.receipt_id;
	      IF v_total_value > 0 THEN
	        FOR v_item IN SELECT * FROM receipt_items WHERE receipt_id = v_link.receipt_id ORDER BY id LOOP
	          INSERT INTO service_cost_distributions
	            (service_id, receipt_id, receipt_item_id, product_id, distribution_amount, distribution_percentage)
	          VALUES
	            (p_service_id, v_link.receipt_id, v_item.id, v_item.product_id,
	             v_allocated * (v_item.quantity * v_item.unit_cost / v_total_value),
	             (v_item.quantity * v_item.unit_cost / v_total_value) * 100);
	          v_dist_count := v_dist_count + 1;
	        END LOOP;
	      END IF;
	
	    ELSIF v_method = 'quantity' THEN
	      SELECT COALESCE(SUM(quantity), 0) INTO v_total_qty
	      FROM receipt_items WHERE receipt_id = v_link.receipt_id;
	      IF v_total_qty > 0 THEN
	        FOR v_item IN SELECT * FROM receipt_items WHERE receipt_id = v_link.receipt_id ORDER BY id LOOP
	          INSERT INTO service_cost_distributions
	            (service_id, receipt_id, receipt_item_id, product_id, distribution_amount, distribution_percentage)
	          VALUES
	            (p_service_id, v_link.receipt_id, v_item.id, v_item.product_id,
	             v_allocated * (v_item.quantity / v_total_qty),
	             (v_item.quantity / v_total_qty) * 100);
	          v_dist_count := v_dist_count + 1;
	        END LOOP;
	      END IF;
	    END IF;
	  END LOOP;
	
	  -- Forzar recalculo de WAC para cada producto afectado
	  UPDATE receipt_items SET updated_at = NOW()
	  WHERE product_id IN (SELECT DISTINCT product_id FROM service_cost_distributions WHERE service_id = p_service_id);
	
	  INSERT INTO audit_logs (user_id, store_id, action, table_name, record_id, metadata)
	  VALUES (v_caller_uid, v_store_id, 'SERVICE_DISTRIBUTED', 'received_services', p_service_id,
	    jsonb_build_object('rows_distributed', v_dist_count, 'method', v_method, 'total_amount', v_total_amount));
	
	  RETURN jsonb_build_object('status', 'success', 'distributed_rows', v_dist_count);
	END;
	$func$
2026-09-13 06:16:35.303 UTC [22010] ERROR:  type "user_role" does not exist at character 116
2026-09-13 06:16:35.303 UTC [22010] STATEMENT:  CREATE OR REPLACE FUNCTION public.enforce_encargado_store_limit()
	RETURNS TRIGGER AS $$
	DECLARE
	    v_creator_role user_role;
	    v_limit integer;
	    v_current_count integer;
	BEGIN
	    IF NEW.created_by IS NULL THEN RETURN NEW; END IF;
	
	    SELECT role, max_stores_limit INTO v_creator_role, v_limit
	    FROM public.profiles WHERE id = NEW.created_by;
	
	    IF v_creator_role = 'encargado' THEN
	        SELECT COUNT(*) INTO v_current_count FROM public.stores WHERE created_by = NEW.created_by;
	        IF v_current_count >= v_limit THEN
	            RAISE EXCEPTION 'ERR_STORE_LIMIT_EXCEEDED: Maximum number of stores reached for this manager (%/%)', v_current_count, v_limit;
	        END IF;
	    END IF;
	    RETURN NEW;
	END;
	$$ LANGUAGE plpgsql
2026-09-13 06:16:35.329 UTC [22013] ERROR:  type "user_role" does not exist at character 115
2026-09-13 06:16:35.329 UTC [22013] STATEMENT:  CREATE OR REPLACE FUNCTION public.enforce_encargado_user_limit()
	RETURNS TRIGGER AS $$
	DECLARE
	    v_creator_role user_role;
	    v_limit integer;
	    v_current_count integer;
	BEGIN
	    IF NEW.created_by IS NULL THEN RETURN NEW; END IF;
	
	    SELECT role, max_users_limit INTO v_creator_role, v_limit
	    FROM public.profiles WHERE id = NEW.created_by;
	
	    IF v_creator_role = 'encargado' THEN
	        SELECT COUNT(*) INTO v_current_count FROM public.profiles WHERE created_by = NEW.created_by;
	        IF v_current_count >= v_limit THEN
	            RAISE EXCEPTION 'ERR_USER_LIMIT_EXCEEDED: Maximum number of users reached for this manager (%/%)', v_current_count, v_limit;
	        END IF;
	    END IF;
	    RETURN NEW;
	END;
	$$ LANGUAGE plpgsql
2026-09-13 06:16:35.381 UTC [22026] ERROR:  syntax error at or near "REVOKE" at character 1436
2026-09-13 06:16:35.381 UTC [22026] STATEMENT:  CREATE OR REPLACE FUNCTION public.generate_bulk_confirmation_token(p_store_ids uuid[], p_action text, p_user_id uuid)
	 RETURNS text
	 LANGUAGE plpgsql
	 SECURITY DEFINER
	 SET search_path TO 'public'
	AS $function$
	DECLARE
	  v_token TEXT;
	  v_has_protected BOOLEAN;
	  v_caller_role TEXT;
	BEGIN
	  -- AUTH CHECK: Solo admin
	  IF auth.uid() IS NOT NULL THEN
	    SELECT role INTO v_caller_role FROM public.profiles WHERE id = auth.uid();
	    IF v_caller_role IS NULL OR v_caller_role != 'admin' THEN
	      RAISE EXCEPTION 'ERR_PERMISSION_DENIED: Solo admin puede generar tokens bulk';
	    END IF;
	    -- Verificar que p_user_id coincide con auth.uid()
	    IF p_user_id != auth.uid() THEN
	      RAISE EXCEPTION 'ERR_PERMISSION_DENIED: p_user_id debe coincidir con el usuario autenticado';
	    END IF;
	  END IF;
	
	  IF p_action NOT IN ('delete', 'archive') THEN
	    RAISE EXCEPTION 'ERR_NON_DESTRUCTIVE_ACTION';
	  END IF;
	
	  SELECT EXISTS(
	    SELECT 1 FROM public.stores
	    WHERE id = ANY(p_store_ids) AND backup_restore_protected = true
	  ) INTO v_has_protected;
	
	  v_token := 'bct_' || replace(gen_random_uuid()::text, '-', '');
	
	  INSERT INTO public.bulk_confirmation_tokens (
	    token, store_ids, action, created_by, expires_at, metadata
	  ) VALUES (
	    v_token, p_store_ids, p_action, p_user_id,
	    NOW() + INTERVAL '10 minutes',
	    jsonb_build_object('has_protected_stores', v_has_protected)
	  );
	
	  RETURN v_token;
	END;
	$function$
	 
	REVOKE EXECUTE ON FUNCTION public.generate_bulk_confirmation_token FROM PUBLIC, anon, authenticated
2026-09-13 06:16:35.400 UTC [22029] ERROR:  syntax error at or near "REVOKE" at character 2432
2026-09-13 06:16:35.400 UTC [22029] STATEMENT:  CREATE OR REPLACE FUNCTION public.generate_bulk_override_token(p_confirmation_token text, p_override_user_id uuid, p_reason text DEFAULT NULL::text)
	 RETURNS text
	 LANGUAGE plpgsql
	 SECURITY DEFINER
	 SET search_path TO 'public'
	AS $function$
	DECLARE
	  v_original RECORD;
	  v_override_token TEXT;
	  v_override_user_role TEXT;
	BEGIN
	  -- AUTH CHECK: Solo admin, y debe ser auth.uid() == p_override_user_id
	  IF auth.uid() IS NOT NULL THEN
	    IF auth.uid() != p_override_user_id THEN
	      RAISE EXCEPTION 'ERR_PERMISSION_DENIED: p_override_user_id debe coincidir con el usuario autenticado';
	    END IF;
	
	    SELECT role INTO v_override_user_role FROM public.profiles WHERE id = p_override_user_id;
	    IF v_override_user_role IS NULL OR v_override_user_role != 'admin' THEN
	      RAISE EXCEPTION 'ERR_PERMISSION_DENIED: Solo admin puede generar override';
	    END IF;
	  END IF;
	
	  SELECT * INTO v_original
	  FROM public.bulk_confirmation_tokens
	  WHERE token = p_confirmation_token
	    AND consumed_at IS NULL
	    AND expires_at > NOW()
	    AND is_override = false;
	
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_INVALID_OR_EXPIRED_TOKEN';
	  END IF;
	
	  IF v_original.created_by = p_override_user_id THEN
	    RAISE EXCEPTION 'ERR_SAME_USER_OVERRIDE';
	  END IF;
	
	  IF EXISTS(
	    SELECT 1 FROM public.bulk_confirmation_tokens
	    WHERE override_for = p_confirmation_token
	      AND consumed_at IS NULL AND expires_at > NOW()
	  ) THEN
	    RAISE EXCEPTION 'ERR_OVERRIDE_ALREADY_EXISTS';
	  END IF;
	
	  v_override_token := 'bot_' || replace(gen_random_uuid()::text, '-', '');
	
	  INSERT INTO public.bulk_confirmation_tokens (
	    token, store_ids, action, created_by, expires_at,
	    is_override, override_for, metadata
	  ) VALUES (
	    v_override_token, v_original.store_ids, v_original.action, p_override_user_id,
	    NOW() + INTERVAL '10 minutes', true, p_confirmation_token,
	    jsonb_build_object('override_reason', p_reason,
	      'original_created_by', v_original.created_by,
	      'override_created_by', p_override_user_id)
	  );
	
	  INSERT INTO public.audit_logs (action, table_name, record_id, metadata)
	  VALUES (
	    'bulk_override_token_generated', 'stores', NULL,
	    jsonb_build_object('confirmation_token', p_confirmation_token,
	      'store_ids', v_original.store_ids,
	      'override_by', p_override_user_id,
	      'original_by', v_original.created_by, 'reason', p_reason)
	  );
	
	  RETURN v_override_token;
	END;
	$function$
	 
	REVOKE EXECUTE ON FUNCTION public.generate_bulk_override_token FROM PUBLIC, anon, authenticated
2026-09-13 06:16:35.437 UTC [22037] ERROR:  relation "public.backup_table_registry" does not exist at character 581
2026-09-13 06:16:35.437 UTC [22037] STATEMENT:  CREATE OR REPLACE FUNCTION public.get_backup_table_list(
	  p_include_excluded BOOLEAN DEFAULT FALSE
	)
	RETURNS TABLE (
	  table_name          TEXT,
	  tier                INTEGER,
	  filter_strategy     TEXT,
	  parent_table        TEXT,
	  parent_foreign_key  TEXT,
	  date_column         TEXT,
	  excluded_from_restore BOOLEAN,
	  exclude_reason      TEXT
	)
	LANGUAGE sql
	SECURITY DEFINER
	SET search_path = public
	AS $$
	  SELECT
	    table_name,
	    tier,
	    filter_strategy,
	    parent_table,
	    parent_foreign_key,
	    date_column,
	    excluded_from_restore,
	    exclude_reason
	  FROM public.backup_table_registry
	  WHERE (p_include_excluded OR excluded_from_restore = FALSE)
	  ORDER BY tier ASC, table_name ASC;
	$$
2026-09-13 06:16:35.474 UTC [22045] ERROR:  relation "v_global_operation_dates" does not exist at character 229
2026-09-13 06:16:35.474 UTC [22045] STATEMENT:  CREATE OR REPLACE FUNCTION public.get_global_max_operation_date(
	  p_store_id UUID DEFAULT NULL
	)
	RETURNS TIMESTAMP WITH TIME ZONE
	LANGUAGE sql
	SECURITY DEFINER
	SET search_path = public
	AS $$
	  SELECT MAX(operation_date)
	  FROM v_global_operation_dates
	  WHERE p_store_id IS NULL OR store_id = p_store_id;
	$$
2026-09-13 06:16:35.493 UTC [22048] ERROR:  relation "exchange_rates" does not exist at character 216
2026-09-13 06:16:35.493 UTC [22048] STATEMENT:  CREATE OR REPLACE FUNCTION get_latest_rate(p_currency TEXT, p_source TEXT)
	RETURNS TABLE(rate NUMERIC, rate_date DATE, captured_at TIMESTAMPTZ)
	LANGUAGE sql STABLE AS $$
	  SELECT rate, rate_date, captured_at
	  FROM exchange_rates
	  WHERE currency = p_currency AND source = p_source
	  ORDER BY rate_date DESC, captured_at DESC
	  LIMIT 1;
	$$
2026-09-13 06:16:35.515 UTC [22051] ERROR:  relation "public.products" does not exist at character 194
2026-09-13 06:16:35.515 UTC [22051] STATEMENT:  CREATE OR REPLACE FUNCTION public.get_low_stock_count(p_store_id uuid DEFAULT NULL)
	RETURNS bigint
	LANGUAGE sql
	SECURITY DEFINER
	SET search_path = public
	AS $$
	  SELECT COUNT(*)::bigint
	  FROM public.products
	  WHERE
	    (p_store_id IS NULL OR store_id = p_store_id)
	    AND is_active = true
	    AND stock_current > 0
	    AND min_stock IS NOT NULL
	    AND min_stock > 0
	    AND stock_current <= min_stock;
	$$
2026-09-13 06:16:35.548 UTC [22058] ERROR:  relation "receipt_items" does not exist at character 754
2026-09-13 06:16:35.548 UTC [22058] STATEMENT:  CREATE OR REPLACE FUNCTION get_product_cost_analysis(
	  p_product_id UUID,
	  p_store_id UUID
	) RETURNS TABLE(
	  receipt_id UUID,
	  receipt_date TIMESTAMPTZ,
	  quantity INT,
	  unit_cost FLOAT,
	  service_type TEXT,
	  service_amount NUMERIC,
	  total_cost NUMERIC,
	  unit_cost_final NUMERIC
	) LANGUAGE sql SECURITY DEFINER AS $$
	  SELECT 
	    ri.receipt_id,
	    r.reception_date,
	    ri.quantity,
	    ri.unit_cost,
	    st.name AS service_type,
	    COALESCE(scd.distribution_amount, 0) AS service_amount,
	    ri.unit_cost * ri.quantity + COALESCE(scd.distribution_amount, 0) AS total_cost,
	    CASE WHEN ri.quantity > 0 
	      THEN (ri.unit_cost * ri.quantity + COALESCE(scd.distribution_amount, 0)) / ri.quantity 
	      ELSE 0 END AS unit_cost_final
	  FROM receipt_items ri
	  JOIN receipts r ON r.id = ri.receipt_id
	  LEFT JOIN service_cost_distributions scd ON scd.receipt_item_id = ri.id
	  LEFT JOIN received_services rs ON rs.id = scd.service_id AND rs.status = 'active'
	  LEFT JOIN service_types st ON st.id = rs.service_type_id
	  WHERE ri.product_id = p_product_id
	    AND r.store_id = p_store_id
	    AND r.status = 'active'
	  ORDER BY r.reception_date DESC;
	$$
2026-09-13 06:16:35.577 UTC [22064] ERROR:  relation "exchange_rates" does not exist at character 252
2026-09-13 06:16:35.577 UTC [22064] STATEMENT:  CREATE OR REPLACE FUNCTION get_rate_history(
	  p_currency TEXT,
	  p_source TEXT,
	  p_days INT DEFAULT 30
	)
	RETURNS TABLE(rate_date DATE, rate NUMERIC, variation_daily NUMERIC)
	LANGUAGE sql STABLE AS $$
	  SELECT rate_date, rate, variation_daily
	  FROM exchange_rates
	  WHERE currency = p_currency AND source = p_source
	    AND rate_date >= CURRENT_DATE - p_days
	  ORDER BY rate_date ASC;
	$$
2026-09-13 06:16:35.597 UTC [22067] ERROR:  relation "exchange_rates" does not exist at character 363
2026-09-13 06:16:35.597 UTC [22067] STATEMENT:  CREATE OR REPLACE FUNCTION get_rate_variation(
	  p_currency TEXT,
	  p_source TEXT,
	  p_start_date DATE,
	  p_end_date DATE
	)
	RETURNS TABLE(
	  start_rate NUMERIC,
	  end_rate NUMERIC,
	  absolute_change NUMERIC,
	  percent_change NUMERIC,
	  daily_avg_growth NUMERIC,
	  monthly_avg_growth NUMERIC
	)
	LANGUAGE sql STABLE AS $$
	  WITH start_val AS (
	    SELECT rate FROM exchange_rates
	    WHERE currency = p_currency AND source = p_source AND rate_date <= p_start_date
	    ORDER BY rate_date DESC LIMIT 1
	  ),
	  end_val AS (
	    SELECT rate FROM exchange_rates
	    WHERE currency = p_currency AND source = p_source AND rate_date <= p_end_date
	    ORDER BY rate_date DESC LIMIT 1
	  )
	  SELECT
	    COALESCE(sv.rate, 0) AS start_rate,
	    COALESCE(ev.rate, 0) AS end_rate,
	    (COALESCE(ev.rate, 0) - COALESCE(sv.rate, 0)) AS absolute_change,
	    CASE WHEN sv.rate IS NOT NULL AND sv.rate > 0 THEN ((ev.rate - sv.rate) / sv.rate * 100) ELSE 0 END AS percent_change,
	    CASE WHEN sv.rate IS NOT NULL AND sv.rate > 0 AND ev.rate IS NOT NULL AND ev.rate > 0 AND p_end_date > p_start_date
	      THEN (POWER(ev.rate::FLOAT / sv.rate::FLOAT, 1.0 / EXTRACT(EPOCH FROM (p_end_date - p_start_date)) * 86400) - 1) * 100
	      ELSE 0 END AS daily_avg_growth,
	    CASE WHEN sv.rate IS NOT NULL AND sv.rate > 0 AND ev.rate IS NOT NULL AND ev.rate > 0 AND p_end_date > p_start_date
	      THEN (POWER(ev.rate::FLOAT / sv.rate::FLOAT, 30.44 / EXTRACT(EPOCH FROM (p_end_date - p_start_date)) * 86400) - 1) * 100
	      ELSE 0 END AS monthly_avg_growth
	  FROM start_val sv, end_val ev;
	$$
2026-09-13 06:16:35.653 UTC [22080] ERROR:  type "public.stores" does not exist
2026-09-13 06:16:35.653 UTC [22080] STATEMENT:  CREATE OR REPLACE FUNCTION public.get_transferable_stores(
	  p_user_id UUID,
	  p_current_store_id UUID
	)
	RETURNS SETOF public.stores
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO 'public'
	AS $$
	DECLARE
	  v_tenant_id UUID;
	BEGIN
	  -- 1. Obtener tenant_id de la tienda actual
	  SELECT tenant_id INTO v_tenant_id
	  FROM public.stores
	  WHERE id = p_current_store_id AND is_active = true;
	
	  -- 2. Devolver tiendas donde el caller tiene acceso (has_store_access_as)
	  --    EXCLUYENDO la tienda actual
	  --    Si tenant_id es NOT NULL, filtrar por mismo tenant.
	  --    Si tenant_id es NULL (legacy), mostrar todas las que el caller tiene acceso.
	  IF v_tenant_id IS NOT NULL THEN
	    RETURN QUERY
	    SELECT s.*
	    FROM public.stores s
	    WHERE s.tenant_id = v_tenant_id
	      AND s.id != p_current_store_id
	      AND s.is_active = true
	      AND s.is_archived = false
	      AND public.has_store_access_as(p_user_id, s.id)
	    ORDER BY s.name;
	  ELSE
	    -- Legacy: tiendas sin tenant_id — usar solo has_store_access_as
	    RETURN QUERY
	    SELECT s.*
	    FROM public.stores s
	    WHERE s.id != p_current_store_id
	      AND s.is_active = true
	      AND s.is_archived = false
	      AND public.has_store_access_as(p_user_id, s.id)
	    ORDER BY s.name;
	  END IF;
	END;
	$$
2026-09-13 06:16:35.694 UTC [22089] ERROR:  type user_role[] does not exist
2026-09-13 06:16:35.694 UTC [22089] STATEMENT:  CREATE OR REPLACE FUNCTION public.has_any_role(required_roles user_role[])
	 RETURNS boolean
	 LANGUAGE plpgsql
	 STABLE SECURITY DEFINER
	AS $$
	DECLARE
	    v_actual_role user_role;
	    r user_role;
	BEGIN
	    SELECT role INTO v_actual_role FROM public.profiles WHERE id = auth.uid();
	
	    FOREACH r IN ARRAY required_roles
	    LOOP
	        IF v_actual_role = r THEN RETURN true; END IF;
	        IF v_actual_role = 'encargado' AND r = 'manager' THEN RETURN true; END IF;
	        IF v_actual_role = 'usuario' AND (r = 'clerk' OR r = 'warehouse') THEN RETURN true; END IF;
	    END LOOP;
	
	    RETURN false;
	END;
	$$
2026-09-13 06:16:35.714 UTC [22092] ERROR:  relation "profiles" does not exist at character 461
2026-09-13 06:16:35.714 UTC [22092] STATEMENT:  CREATE OR REPLACE FUNCTION public.has_management_access_as(
	  p_user_id uuid,
	  p_store_id uuid
	) RETURNS boolean
	LANGUAGE sql SECURITY DEFINER
	SET search_path = public, pg_temp
	AS $$
	  -- service_role always passes
	  SELECT CASE WHEN auth.role() = 'service_role' THEN true
	    ELSE
	      -- Check if user has admin/manager/encargado role in their profile
	      -- OR has a membership with manager/admin role for the store
	      EXISTS (
	        SELECT 1 FROM profiles
	        WHERE id = p_user_id
	        AND role IN ('admin', 'manager', 'encargado')
	      )
	      OR EXISTS (
	        SELECT 1 FROM user_store_memberships
	        WHERE user_id = p_user_id
	        AND store_id = p_store_id
	        AND status = 'active'
	        AND role IN ('admin', 'manager')
	      )
	    END
	$$
2026-09-13 06:16:35.733 UTC [22095] ERROR:  type public.user_role does not exist
2026-09-13 06:16:35.733 UTC [22095] STATEMENT:  CREATE OR REPLACE FUNCTION public.has_role(p_user_id UUID, p_required_role public.user_role)
	 RETURNS boolean
	 LANGUAGE plpgsql
	 STABLE SECURITY DEFINER
	AS $$
	DECLARE
	    v_actual_role public.user_role;
	BEGIN
	    SELECT role INTO v_actual_role FROM public.profiles WHERE id = p_user_id;
	
	    IF v_actual_role IS NULL THEN
	        RETURN false;
	    END IF;
	
	    -- Compatibility mapping (mirroring the 1-arg version)
	    IF v_actual_role = 'encargado' AND p_required_role = 'manager' THEN RETURN true; END IF;
	    IF v_actual_role = 'usuario' AND (p_required_role = 'clerk' OR p_required_role = 'warehouse') THEN RETURN true; END IF;
	
	    RETURN v_actual_role = p_required_role;
	END;
	$$
2026-09-13 06:16:35.758 UTC [22100] ERROR:  relation "public.user_store_memberships" does not exist at character 279
2026-09-13 06:16:35.758 UTC [22100] STATEMENT:  CREATE OR REPLACE FUNCTION public.has_store_role(
	  p_user_id uuid,
	  p_store_id uuid,
	  p_roles text[]
	) RETURNS boolean
	LANGUAGE sql SECURITY DEFINER
	SET search_path = public, pg_temp
	AS $$
	  SELECT CASE WHEN auth.role() = 'service_role' THEN
	    EXISTS (
	      SELECT 1 FROM public.user_store_memberships m
	      WHERE m.user_id = p_user_id
	        AND m.store_id = p_store_id
	        AND m.status = 'active'
	        AND m.role::text = ANY(p_roles)
	    )
	  ELSE
	    EXISTS (
	      SELECT 1 FROM public.user_store_memberships m
	      WHERE m.user_id = auth.uid()
	        AND m.store_id = p_store_id
	        AND m.status = 'active'
	        AND m.role::text = ANY(p_roles)
	    )
	  END
	$$
2026-09-13 06:16:35.787 UTC [22106] ERROR:  relation "public.profiles" does not exist at character 173
2026-09-13 06:16:35.787 UTC [22106] STATEMENT:  CREATE OR REPLACE FUNCTION public.is_global_admin()
	RETURNS BOOLEAN
	LANGUAGE sql
	STABLE
	SECURITY DEFINER
	SET search_path = public
	AS $$
	  SELECT EXISTS (
	    SELECT 1 FROM public.profiles
	    WHERE id = auth.uid()
	      AND role = 'admin'
	  );
	$$
2026-09-13 06:16:35.811 UTC [22110] ERROR:  type user_role does not exist
2026-09-13 06:16:35.811 UTC [22110] STATEMENT:  CREATE OR REPLACE FUNCTION public.is_role_not_changed(p_user_id uuid, p_new_role user_role, p_new_role_id uuid)
	RETURNS boolean
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path = public, extensions
	AS $$
	DECLARE
	  v_old_role user_role;
	  v_old_role_id uuid;
	BEGIN
	  SELECT role, role_id INTO v_old_role, v_old_role_id
	  FROM public.profiles
	  WHERE id = p_user_id;
	
	  RETURN (p_new_role IS NOT DISTINCT FROM v_old_role)
	     AND (p_new_role_id IS NOT DISTINCT FROM v_old_role_id);
	END;
	$$
2026-09-13 06:16:35.835 UTC [22114] ERROR:  relation "public.user_store_memberships" does not exist at character 188
2026-09-13 06:16:35.835 UTC [22114] STATEMENT:  CREATE OR REPLACE FUNCTION public.is_store_member(p_store_id UUID)
	RETURNS BOOLEAN
	LANGUAGE sql
	STABLE
	SECURITY DEFINER
	SET search_path = public
	AS $$
	  SELECT EXISTS (
	    SELECT 1 FROM public.user_store_memberships
	    WHERE store_id = p_store_id
	      AND user_id = auth.uid()
	      AND status = 'active'
	  );
	$$
2026-09-13 06:16:35.853 UTC [22117] ERROR:  function public.current_user_tenant_id() does not exist at character 209
2026-09-13 06:16:35.853 UTC [22117] HINT:  No function matches the given name and argument types. You might need to add explicit type casts.
2026-09-13 06:16:35.853 UTC [22117] STATEMENT:  CREATE OR REPLACE FUNCTION public.is_tenant_member(p_tenant_id uuid)
	RETURNS boolean
	LANGUAGE sql
	STABLE
	SECURITY DEFINER
	SET search_path TO public
	AS $$
	  SELECT p_tenant_id IS NOT NULL
	   AND p_tenant_id = public.current_user_tenant_id();
	$$
2026-09-13 06:16:35.896 UTC [22127] ERROR:  type user_role does not exist
2026-09-13 06:16:35.896 UTC [22127] STATEMENT:  CREATE OR REPLACE FUNCTION public.managed_create_user(p_email text, p_full_name text, p_role user_role, p_store_id uuid DEFAULT NULL::uuid, p_memberships jsonb DEFAULT NULL::jsonb, p_max_stores integer DEFAULT 0, p_max_users integer DEFAULT 0, p_target_user_id uuid DEFAULT NULL::uuid)
	 RETURNS jsonb
	 LANGUAGE plpgsql
	 SECURITY DEFINER
	 SET search_path TO 'public', 'extensions'
	AS $function$
	DECLARE
	    v_user_id uuid;
	    v_role_id uuid;
	    v_active_store_id uuid;
	    m JSONB;
	    v_creator_role user_role;
	BEGIN
	    SELECT role INTO v_creator_role FROM public.profiles WHERE id = auth.uid();
	    IF v_creator_role IS NULL OR v_creator_role NOT IN ('admin', 'encargado') THEN
	        RAISE EXCEPTION 'ERR_UNAUTHORIZED: Only admins and managers can create users.';
	    END IF;
	
	    v_active_store_id := COALESCE((p_memberships->0->>'store_id')::UUID, p_store_id);
	
	    IF v_creator_role = 'encargado' AND v_active_store_id IS NOT NULL THEN
	        IF NOT public.has_store_access(v_active_store_id) THEN
	            RAISE EXCEPTION 'ERR_UNAUTHORIZED: No access to store %', v_active_store_id;
	        END IF;
	    END IF;
	
	    SELECT id INTO v_role_id FROM public.roles WHERE lower(name) = lower(p_role::text) OR (name = 'Cajero' AND p_role = 'clerk') OR (name = 'Almacenero' AND p_role = 'warehouse') LIMIT 1;
	    v_user_id := COALESCE(p_target_user_id, gen_random_uuid());
	
	    INSERT INTO public.profiles (id, email, full_name, role, role_id, active_store_id, is_active, created_by, max_stores_limit, max_users_limit, created_at, updated_at)
	    VALUES (v_user_id, p_email, p_full_name, p_role, v_role_id, v_active_store_id, true, auth.uid(), p_max_stores, p_max_users, now(), now())
	    ON CONFLICT (id) DO UPDATE SET email = EXCLUDED.email, full_name = EXCLUDED.full_name, role = EXCLUDED.role, role_id = EXCLUDED.role_id, active_store_id = EXCLUDED.active_store_id, updated_at = now()
	    RETURNING id INTO v_user_id;
	
	    IF p_memberships IS NOT NULL THEN
	        IF v_creator_role != 'admin' THEN
	            DELETE FROM public.user_store_memberships WHERE user_id = v_user_id AND store_id IN (SELECT store_id FROM public.user_store_memberships WHERE user_id = auth.uid() AND role IN ('encargado', 'manager'));
	        ELSE
	            DELETE FROM public.user_store_memberships WHERE user_id = v_user_id;
	        END IF;
	        FOR m IN SELECT * FROM jsonb_array_elements(p_memberships) LOOP
	            IF (m->>'store_id') IS NOT NULL AND (m->>'store_id') <> '' THEN
	                IF v_creator_role = 'admin' OR public.has_store_access((m->>'store_id')::UUID) THEN
	                    INSERT INTO public.user_store_memberships (user_id, store_id, role) VALUES (v_user_id, (m->>'store_id')::UUID, (m->>'role')::user_role) ON CONFLICT (user_id, store_id) DO UPDATE SET role = EXCLUDED.role;
	                END IF;
	            END IF;
	        END LOOP;
	    ELSIF p_store_id IS NOT NULL THEN
	        INSERT INTO public.user_store_memberships (user_id, store_id, role) VALUES (v_user_id, p_store_id, p_role) ON CONFLICT (user_id, store_id) DO UPDATE SET role = EXCLUDED.role;
	    END IF;
	    RETURN jsonb_build_object('success', true, 'user_id', v_user_id);
	END;
	$function$
2026-09-13 06:16:35.918 UTC [22130] ERROR:  type public.user_role does not exist
2026-09-13 06:16:35.918 UTC [22130] STATEMENT:  CREATE OR REPLACE FUNCTION public.managed_create_user_v2(
	  p_email text,
	  p_full_name text,
	  p_role public.user_role,
	  p_plan plan_t DEFAULT 'free'::plan_t,
	  p_store_id uuid DEFAULT NULL::uuid,
	  p_memberships jsonb DEFAULT NULL::jsonb,
	  p_max_stores int DEFAULT 0,
	  p_max_users int DEFAULT 0,
	  p_target_user_id uuid DEFAULT NULL::uuid,
	  p_creator_id uuid DEFAULT NULL::uuid
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  v_user_id uuid := COALESCE(p_target_user_id, gen_random_uuid());
	  v_role_id uuid;
	  v_active_store_id uuid;
	  m JSONB;
	  v_creator_role public.user_role;
	  v_creator_uid uuid := COALESCE(p_creator_id, auth.uid());
	BEGIN
	  -- Validar caller es admin o encargado
	  SELECT role INTO v_creator_role FROM public.profiles WHERE id = v_creator_uid;
	  IF v_creator_role IS NULL OR v_creator_role NOT IN ('admin', 'encargado', 'superadmin') THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED: Only admins and managers can create users.';
	  END IF;
	
	  -- Validar email único (entre perfiles activos)
	  IF EXISTS (SELECT 1 FROM public.profiles WHERE email = p_email AND deleted_at IS NULL) THEN
	    RAISE EXCEPTION 'ERR_EMAIL_ALREADY_EXISTS: %', p_email;
	  END IF;
	
	  v_active_store_id := COALESCE((p_memberships->0->>'store_id')::UUID, p_store_id);
	
	  -- Encargado solo puede crear en tiendas que gestiona
	  IF v_creator_role = 'encargado' AND v_active_store_id IS NOT NULL THEN
	    IF NOT public.has_store_access(v_active_store_id) THEN
	      RAISE EXCEPTION 'ERR_UNAUTHORIZED: No access to store %', v_active_store_id;
	    END IF;
	  END IF;
	
	  -- Buscar role_id
	  SELECT id INTO v_role_id FROM public.roles
	    WHERE lower(name) = lower(p_role::text)
	       OR (name = 'Cajero' AND p_role = 'clerk')
	       OR (name = 'Almacenero' AND p_role = 'warehouse')
	       OR (name = 'UserCosto' AND p_role = 'costo')
	    LIMIT 1;
	
	  -- INSERT profiles con plan
	  INSERT INTO public.profiles (
	    id, email, full_name, role, role_id, active_store_id, is_active,
	    created_by, max_stores_limit, max_users_limit, plan, created_at, updated_at
	  ) VALUES (
	    v_user_id, p_email, p_full_name, p_role, v_role_id, v_active_store_id, true,
	    v_creator_uid, p_max_stores, p_max_users, p_plan, now(), now()
	  )
	  ON CONFLICT (id) DO UPDATE SET
	    email = EXCLUDED.email,
	    full_name = EXCLUDED.full_name,
	    role = EXCLUDED.role,
	    role_id = EXCLUDED.role_id,
	    active_store_id = EXCLUDED.active_store_id,
	    plan = EXCLUDED.plan,
	    is_active = true,
	    deleted_at = NULL,
	    deletion_reason = NULL,
	    deleted_by = NULL,
	    updated_at = now()
	  RETURNING id INTO v_user_id;
	
	  -- Procesar memberships
	  IF p_memberships IS NOT NULL THEN
	    IF v_creator_role != 'admin' AND v_creator_role != 'superadmin' THEN
	      DELETE FROM public.user_store_memberships
	        WHERE user_id = v_user_id
	        AND store_id IN (
	          SELECT store_id FROM public.user_store_memberships
	          WHERE user_id = v_creator_uid AND role IN ('encargado', 'manager')
	        );
	    ELSE
	      DELETE FROM public.user_store_memberships WHERE user_id = v_user_id;
	    END IF;
	
	    FOR m IN SELECT * FROM jsonb_array_elements(p_memberships) LOOP
	      IF (m->>'store_id') IS NOT NULL AND (m->>'store_id') <> '' THEN
	        IF v_creator_role IN ('admin', 'superadmin') OR public.has_store_access((m->>'store_id')::UUID) THEN
	          INSERT INTO public.user_store_memberships (user_id, store_id, role)
	          VALUES (v_user_id, (m->>'store_id')::UUID, (m->>'role')::public.user_role)
	          ON CONFLICT (user_id, store_id) DO UPDATE SET role = EXCLUDED.role, status = 'active';
	        END IF;
	      END IF;
	    END LOOP;
	  ELSIF p_store_id IS NOT NULL THEN
	    INSERT INTO public.user_store_memberships (user_id, store_id, role)
	    VALUES (v_user_id, p_store_id, p_role)
	    ON CONFLICT (user_id, store_id) DO UPDATE SET role = EXCLUDED.role, status = 'active';
	  END IF;
	
	  -- Audit log atómico (regresión C-2 restaurada)
	  INSERT INTO public.user_audit_log (performed_by, target_user_id, action, new_values, metadata)
	  VALUES (
	    v_creator_uid, v_user_id, 'USER_CREATED',
	    jsonb_build_object(
	      'email', p_email, 'full_name', p_full_name, 'role', p_role::text,
	      'plan', p_plan::text, 'active_store_id', v_active_store_id,
	      'max_stores', p_max_stores, 'max_users', p_max_users
	    ),
	    jsonb_build_object('memberships_count', CASE WHEN p_memberships IS NOT NULL THEN jsonb_array_length(p_memberships) ELSE 0 END)
	  );
	
	  RETURN jsonb_build_object('success', true, 'user_id', v_user_id);
	END;
	$function$
2026-09-13 06:16:35.953 UTC [22135] ERROR:  type "public.user_role" does not exist at character 392
2026-09-13 06:16:35.953 UTC [22135] STATEMENT:  CREATE OR REPLACE FUNCTION public.managed_reset_password(
	  p_user_id uuid,
	  p_caller_id uuid DEFAULT NULL::uuid
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  -- FIX H-7: anti-spoofing
	  v_caller_uid uuid := CASE WHEN auth.role() = 'service_role'
	    THEN COALESCE(p_caller_id, auth.uid()) ELSE auth.uid() END;
	  v_caller_role public.user_role;
	  v_target_email text;
	BEGIN
	  SELECT role INTO v_caller_role FROM public.profiles WHERE id = v_caller_uid;
	  IF v_caller_role IS NULL OR v_caller_role NOT IN ('admin', 'superadmin') THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED';
	  END IF;
	
	  IF p_user_id = v_caller_uid THEN
	    RAISE EXCEPTION 'ERR_SELF_RESET_BLOCKED';
	  END IF;
	
	  SELECT email INTO v_target_email FROM public.profiles WHERE id = p_user_id AND deleted_at IS NULL;
	  IF v_target_email IS NULL THEN
	    RAISE EXCEPTION 'ERR_USER_NOT_FOUND';
	  END IF;
	
	  INSERT INTO public.user_audit_log (performed_by, target_user_id, action, metadata)
	  VALUES (
	    v_caller_uid, p_user_id, 'PASSWORD_RESET_REQUESTED',
	    jsonb_build_object('email', v_target_email, 'method', 'recovery_link')
	  );
	
	  RETURN jsonb_build_object('success', true, 'email', v_target_email);
	END;
	$function$
2026-09-13 06:16:35.980 UTC [22139] ERROR:  type "public.user_role" does not exist at character 427
2026-09-13 06:16:35.980 UTC [22139] STATEMENT:  CREATE OR REPLACE FUNCTION public.managed_soft_delete_user(
	  p_user_id uuid,
	  p_reason text,
	  p_caller_id uuid DEFAULT NULL::uuid
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  v_old RECORD;
	  -- FIX H-7: anti-spoofing
	  v_caller_uid uuid := CASE WHEN auth.role() = 'service_role'
	    THEN COALESCE(p_caller_id, auth.uid()) ELSE auth.uid() END;
	  v_caller_role public.user_role;
	  v_active_memberships_count int;
	  v_anon_email text;
	BEGIN
	  SELECT role INTO v_caller_role FROM public.profiles WHERE id = v_caller_uid;
	  IF v_caller_role IS NULL OR v_caller_role NOT IN ('admin', 'superadmin') THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED';
	  END IF;
	
	  IF p_user_id = v_caller_uid THEN
	    RAISE EXCEPTION 'ERR_SELF_DELETE_BLOCKED';
	  END IF;
	
	  SELECT id, email, full_name, role, plan, is_active INTO v_old
	    FROM public.profiles WHERE id = p_user_id AND deleted_at IS NULL FOR UPDATE;
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_USER_NOT_FOUND_OR_ALREADY_DELETED';
	  END IF;
	
	  SELECT COUNT(*) INTO v_active_memberships_count
	    FROM public.user_store_memberships
	    WHERE user_id = p_user_id AND status = 'active';
	  IF v_active_memberships_count > 0 THEN
	    RAISE EXCEPTION 'ERR_USER_HAS_ACTIVE_MEMBERSHIPS: % active. Revoke memberships first.', v_active_memberships_count;
	  END IF;
	
	  v_anon_email := 'deleted+' || substr(p_user_id::text, 1, 8) || '@anonymized.local';
	
	  UPDATE public.profiles SET
	    deleted_at = now(),
	    deletion_reason = p_reason,
	    deleted_by = v_caller_uid,
	    is_active = false,
	    full_name = '[deleted user]',
	    email = v_anon_email,
	    ai_api_key = NULL,
	    updated_at = now()
	  WHERE id = p_user_id;
	
	  UPDATE public.user_store_memberships SET
	    status = 'revoked',
	    updated_at = now()
	  WHERE user_id = p_user_id AND status = 'active';
	
	  INSERT INTO public.user_audit_log (performed_by, target_user_id, action, old_values, new_values, metadata)
	  VALUES (
	    v_caller_uid, p_user_id, 'USER_SOFT_DELETED',
	    jsonb_build_object(
	      'email', v_old.email, 'full_name', v_old.full_name,
	      'role', v_old.role::text, 'plan', v_old.plan::text, 'is_active', v_old.is_active
	    ),
	    jsonb_build_object(
	      'email', v_anon_email, 'full_name', '[deleted user]',
	      'is_active', false, 'deleted_at', now()
	    ),
	    jsonb_build_object('reason', p_reason, 'memberships_revoked', v_active_memberships_count)
	  );
	
	  RETURN jsonb_build_object(
	    'success', true,
	    'user_id', p_user_id,
	    'status', 'soft_deleted',
	    'note', 'auth.users preserved. API route should ban via auth.admin.updateUser.'
	  );
	END;
	$function$
2026-09-13 06:16:36.009 UTC [22143] ERROR:  type "public.user_role" does not exist at character 435
2026-09-13 06:16:36.009 UTC [22143] STATEMENT:  CREATE OR REPLACE FUNCTION public.managed_toggle_user_status(
	  p_user_id uuid,
	  p_is_active boolean,
	  p_caller_id uuid DEFAULT NULL::uuid
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  v_old RECORD;
	  -- FIX H-7: anti-spoofing
	  v_caller_uid uuid := CASE WHEN auth.role() = 'service_role'
	    THEN COALESCE(p_caller_id, auth.uid()) ELSE auth.uid() END;
	  v_caller_role public.user_role;
	BEGIN
	  SELECT role INTO v_caller_role FROM public.profiles WHERE id = v_caller_uid;
	  IF v_caller_role IS NULL OR v_caller_role NOT IN ('admin', 'superadmin', 'encargado', 'manager') THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED';
	  END IF;
	
	  IF p_is_active = false AND p_user_id = v_caller_uid THEN
	    RAISE EXCEPTION 'ERR_SELF_DEACTIVATE_BLOCKED';
	  END IF;
	
	  SELECT id, is_active, full_name INTO v_old
	    FROM public.profiles WHERE id = p_user_id AND deleted_at IS NULL FOR UPDATE;
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_USER_NOT_FOUND';
	  END IF;
	
	  IF v_old.is_active = p_is_active THEN
	    RETURN jsonb_build_object('success', true, 'no_change', true);
	  END IF;
	
	  UPDATE public.profiles SET is_active = p_is_active, updated_at = now() WHERE id = p_user_id;
	
	  INSERT INTO public.user_audit_log (performed_by, target_user_id, action, old_values, new_values)
	  VALUES (
	    v_caller_uid, p_user_id,
	    CASE WHEN p_is_active THEN 'USER_ACTIVATED' ELSE 'USER_DEACTIVATED' END,
	    jsonb_build_object('is_active', v_old.is_active),
	    jsonb_build_object('is_active', p_is_active)
	  );
	
	  RETURN jsonb_build_object('success', true, 'user_id', p_user_id, 'is_active', p_is_active);
	END;
	$function$
2026-09-13 06:16:36.029 UTC [22146] ERROR:  type public.user_role does not exist
2026-09-13 06:16:36.029 UTC [22146] STATEMENT:  CREATE OR REPLACE FUNCTION public.managed_update_membership(
	  p_membership_id uuid,
	  p_role public.user_role DEFAULT NULL::user_role,
	  p_status text DEFAULT NULL::text,
	  p_caller_id uuid DEFAULT NULL::uuid
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  v_old RECORD;
	  -- FIX H-7: anti-spoofing
	  v_caller_uid uuid := CASE WHEN auth.role() = 'service_role'
	    THEN COALESCE(p_caller_id, auth.uid()) ELSE auth.uid() END;
	  v_changes jsonb := '{}'::jsonb;
	BEGIN
	  SELECT m.id, m.user_id, m.store_id, m.role, m.status
	    INTO v_old
	    FROM public.user_store_memberships m
	    WHERE m.id = p_membership_id
	    FOR UPDATE;
	
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_MEMBERSHIP_NOT_FOUND';
	  END IF;
	
	  -- FIX H-7: Autorización usa nueva sobrecarga has_store_role(p_user_id, p_store_id, p_roles)
	  -- service_role (API route) pasa p_caller_id=session.user.id → verifica acceso de ese user
	  -- authenticated pasa p_caller_id=NULL → v_caller_uid=auth.uid() → verifica su propio acceso
	  IF NOT public.has_store_role(v_caller_uid, v_old.store_id, ARRAY['admin', 'manager']) THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED: Caller must be admin or manager of the store.';
	  END IF;
	
	  IF p_role IS NOT NULL AND p_role <> v_old.role THEN
	    v_changes := v_changes || jsonb_build_object('role', jsonb_build_object('old', v_old.role::text, 'new', p_role::text));
	    UPDATE public.user_store_memberships SET role = p_role, updated_at = now() WHERE id = p_membership_id;
	  END IF;
	
	  IF p_status IS NOT NULL AND p_status <> v_old.status::text THEN
	    IF p_status NOT IN ('active', 'revoked') THEN
	      RAISE EXCEPTION 'ERR_INVALID_STATUS: %', p_status;
	    END IF;
	    v_changes := v_changes || jsonb_build_object('status', jsonb_build_object('old', v_old.status, 'new', p_status));
	    UPDATE public.user_store_memberships SET status = p_status::membership_status, updated_at = now() WHERE id = p_membership_id;
	  END IF;
	
	  IF v_changes <> '{}'::jsonb THEN
	    INSERT INTO public.user_audit_log (performed_by, target_user_id, action, old_values, new_values, metadata)
	    VALUES (
	      v_caller_uid, v_old.user_id, 'MEMBERSHIP_UPDATED',
	      jsonb_build_object('membership_id', p_membership_id, 'store_id', v_old.store_id, 'role', v_old.role::text, 'status', v_old.status),
	      v_changes,
	      jsonb_build_object('store_id', v_old.store_id)
	    );
	  END IF;
	
	  RETURN jsonb_build_object('success', true, 'membership_id', p_membership_id, 'changes', v_changes);
	END;
	$function$
2026-09-13 06:16:36.048 UTC [22149] ERROR:  type plan_t does not exist
2026-09-13 06:16:36.048 UTC [22149] STATEMENT:  CREATE OR REPLACE FUNCTION public.managed_update_tenant_plan(
	  p_tenant_id uuid,
	  p_plan plan_t,
	  p_subscription_status text DEFAULT NULL,
	  p_caller_id uuid DEFAULT NULL
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  -- FIX H-7: anti-spoofing — service_role (Stripe webhook) puede pasar NULL caller_id
	  v_caller_uid uuid := CASE WHEN auth.role() = 'service_role'
	    THEN COALESCE(p_caller_id, auth.uid()) ELSE auth.uid() END;
	  v_old_plan plan_t;
	  v_old_status text;
	  v_role public.user_role;
	BEGIN
	  -- Validación: si caller_uid es NULL (service_role anónimo para Stripe webhook), permitir
	  -- Si caller_uid NO es NULL, debe ser admin/superadmin
	  IF v_caller_uid IS NOT NULL THEN
	    SELECT role INTO v_role FROM public.profiles WHERE id = v_caller_uid;
	    IF v_role IS NULL OR v_role NOT IN ('admin', 'superadmin') THEN
	      RAISE EXCEPTION 'ERR_UNAUTHORIZED: Only admins can change tenant plan';
	    END IF;
	  END IF;
	
	  SELECT plan, subscription_status INTO v_old_plan, v_old_status
	    FROM public.tenants WHERE id = p_tenant_id FOR UPDATE;
	
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_TENANT_NOT_FOUND: %', p_tenant_id;
	  END IF;
	
	  UPDATE public.tenants SET
	    plan = p_plan,
	    subscription_status = COALESCE(p_subscription_status, subscription_status),
	    updated_at = now()
	  WHERE id = p_tenant_id;
	
	  UPDATE public.profiles SET
	    plan = p_plan,
	    updated_at = now()
	  WHERE tenant_id = p_tenant_id AND deleted_at IS NULL;
	
	  INSERT INTO public.user_audit_log (performed_by, target_user_id, action, old_values, new_values, metadata)
	  VALUES (
	    v_caller_uid, NULL, 'TENANT_PLAN_UPDATED',
	    jsonb_build_object('old_plan', v_old_plan::text, 'old_status', v_old_status),
	    jsonb_build_object('new_plan', p_plan::text, 'new_status', COALESCE(p_subscription_status, v_old_status)),
	    jsonb_build_object('tenant_id', p_tenant_id)
	  );
	
	  RETURN jsonb_build_object(
	    'success', true,
	    'tenant_id', p_tenant_id,
	    'old_plan', v_old_plan::text,
	    'new_plan', p_plan::text
	  );
	END;
	$function$
2026-09-13 06:16:36.065 UTC [22152] ERROR:  type public.user_role does not exist
2026-09-13 06:16:36.065 UTC [22152] STATEMENT:  CREATE OR REPLACE FUNCTION public.managed_update_user(
	  p_user_id uuid,
	  p_full_name text DEFAULT NULL::text,
	  p_role public.user_role DEFAULT NULL::user_role,
	  p_role_id uuid DEFAULT NULL::uuid,
	  p_is_active boolean DEFAULT NULL::boolean,
	  p_max_stores_limit int DEFAULT NULL::int,
	  p_max_users_limit int DEFAULT NULL::int,
	  p_plan plan_t DEFAULT NULL::plan_t,
	  p_caller_id uuid DEFAULT NULL::uuid
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  v_old RECORD;
	  -- FIX H-7: anti-spoofing — service_role puede pasar p_caller_id explícito
	  v_caller_uid uuid := CASE WHEN auth.role() = 'service_role'
	    THEN COALESCE(p_caller_id, auth.uid()) ELSE auth.uid() END;
	  v_caller_role public.user_role;
	  v_changes jsonb := '{}'::jsonb;
	BEGIN
	  -- Validar caller es admin (usa v_caller_uid que ahora NO es spoofable para authenticated)
	  SELECT role INTO v_caller_role FROM public.profiles WHERE id = v_caller_uid;
	  IF v_caller_role IS NULL OR v_caller_role NOT IN ('admin', 'superadmin') THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED: Only admins can update users.';
	  END IF;
	
	  SELECT * INTO v_old FROM public.profiles WHERE id = p_user_id AND deleted_at IS NULL FOR UPDATE;
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_USER_NOT_FOUND: %', p_user_id;
	  END IF;
	
	  IF p_is_active = false AND p_user_id = v_caller_uid THEN
	    RAISE EXCEPTION 'ERR_SELF_DEACTIVATE_BLOCKED: Cannot deactivate own account.';
	  END IF;
	
	  IF p_full_name IS NOT NULL AND p_full_name <> v_old.full_name THEN
	    v_changes := v_changes || jsonb_build_object('full_name', jsonb_build_object('old', v_old.full_name, 'new', p_full_name));
	    UPDATE public.profiles SET full_name = p_full_name, updated_at = now() WHERE id = p_user_id;
	  END IF;
	
	  IF p_role IS NOT NULL AND p_role <> v_old.role THEN
	    v_changes := v_changes || jsonb_build_object('role', jsonb_build_object('old', v_old.role::text, 'new', p_role::text));
	    UPDATE public.profiles SET role = p_role, updated_at = now() WHERE id = p_user_id;
	  END IF;
	
	  IF p_role_id IS NOT NULL AND (v_old.role_id IS NULL OR p_role_id <> v_old.role_id) THEN
	    v_changes := v_changes || jsonb_build_object('role_id', jsonb_build_object('old', v_old.role_id, 'new', p_role_id));
	    UPDATE public.profiles SET role_id = p_role_id, updated_at = now() WHERE id = p_user_id;
	  END IF;
	
	  IF p_is_active IS NOT NULL AND p_is_active <> v_old.is_active THEN
	    v_changes := v_changes || jsonb_build_object('is_active', jsonb_build_object('old', v_old.is_active, 'new', p_is_active));
	    UPDATE public.profiles SET is_active = p_is_active, updated_at = now() WHERE id = p_user_id;
	  END IF;
	
	  IF p_max_stores_limit IS NOT NULL AND (v_old.max_stores_limit IS NULL OR p_max_stores_limit <> v_old.max_stores_limit) THEN
	    v_changes := v_changes || jsonb_build_object('max_stores_limit', jsonb_build_object('old', v_old.max_stores_limit, 'new', p_max_stores_limit));
	    UPDATE public.profiles SET max_stores_limit = p_max_stores_limit, updated_at = now() WHERE id = p_user_id;
	  END IF;
	
	  IF p_max_users_limit IS NOT NULL AND (v_old.max_users_limit IS NULL OR p_max_users_limit <> v_old.max_users_limit) THEN
	    v_changes := v_changes || jsonb_build_object('max_users_limit', jsonb_build_object('old', v_old.max_users_limit, 'new', p_max_users_limit));
	    UPDATE public.profiles SET max_users_limit = p_max_users_limit, updated_at = now() WHERE id = p_user_id;
	  END IF;
	
	  IF p_plan IS NOT NULL AND p_plan <> v_old.plan THEN
	    v_changes := v_changes || jsonb_build_object('plan', jsonb_build_object('old', v_old.plan::text, 'new', p_plan::text));
	    UPDATE public.profiles SET plan = p_plan, updated_at = now() WHERE id = p_user_id;
	  END IF;
	
	  IF v_changes <> '{}'::jsonb THEN
	    INSERT INTO public.user_audit_log (performed_by, target_user_id, action, old_values, new_values, metadata)
	    VALUES (
	      v_caller_uid, p_user_id, 'USER_UPDATED',
	      jsonb_build_object(
	        'full_name', v_old.full_name, 'role', v_old.role::text,
	        'is_active', v_old.is_active, 'plan', v_old.plan::text
	      ),
	      v_changes,
	      jsonb_build_object('fields_changed', jsonb_object_keys(v_changes))
	    );
	  END IF;
	
	  RETURN jsonb_build_object('success', true, 'user_id', p_user_id, 'changes', v_changes);
	END;
	$function$
2026-09-13 06:16:36.126 UTC [22157] ERROR:  type "user_role" does not exist at character 204
2026-09-13 06:16:36.126 UTC [22157] STATEMENT:  CREATE OR REPLACE FUNCTION public.on_auth_user_created()
	RETURNS trigger
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO 'public', 'extensions'
	AS $function$
	DECLARE
	  v_role_id UUID;
	  v_role_enum USER_ROLE;
	  v_metadata_role TEXT;
	  v_company_name TEXT;
	  v_full_name TEXT;
	  v_tenant_id UUID;
	BEGIN
	  -- Extract role from metadata if present
	  v_metadata_role := NEW.raw_user_meta_data->>'role';
	
	  -- Normalize and convert to enum
	  IF v_metadata_role IS NOT NULL THEN
	    BEGIN
	      v_role_enum := v_metadata_role::USER_ROLE;
	    EXCEPTION WHEN OTHERS THEN
	      v_role_enum := 'tenant_admin'::USER_ROLE;
	    END;
	  ELSE
	    -- Iteración 13: self-signup users are tenant_admin by default
	    v_role_enum := 'tenant_admin'::USER_ROLE;
	  END IF;
	
	  -- Extract company_name and full_name from metadata
	  v_company_name := NEW.raw_user_meta_data->>'company_name';
	  v_full_name := COALESCE(NEW.raw_user_meta_data->>'full_name', split_part(NEW.email, '@', 1));
	
	  -- Iteración 13: Create tenant for self-signup users
	  -- If user was created by admin (via managed_create_user_v2), they may already
	  -- have a tenant_id set via p_target_user_id. In that case, skip tenant creation.
	  IF v_company_name IS NOT NULL THEN
	    INSERT INTO public.tenants (name, owner_id, plan, subscription_status, trial_ends_at, is_active)
	    VALUES (
	      v_company_name,
	      NEW.id,
	      'free'::plan_t,
	      'trial',
	      now() + interval '14 days',
	      true
	    )
	    RETURNING id INTO v_tenant_id;
	  END IF;
	
	  -- Get matching role_id from public.roles
	  SELECT id INTO v_role_id FROM public.roles
	    WHERE name = v_metadata_role OR lower(name) = lower(v_role_enum::text)
	    LIMIT 1;
	
	  -- Insert profile with tenant_id
	  INSERT INTO public.profiles (id, email, full_name, role, role_id, is_active, tenant_id, plan, created_at, updated_at)
	  VALUES (
	    NEW.id,
	    NEW.email,
	    v_full_name,
	    v_role_enum,
	    v_role_id,
	    true,
	    v_tenant_id,
	    'free'::plan_t,
	    now(),
	    now()
	  )
	  ON CONFLICT (id) DO UPDATE SET
	    email = EXCLUDED.email,
	    full_name = EXCLUDED.full_name,
	    role = EXCLUDED.role,
	    role_id = EXCLUDED.role_id,
	    tenant_id = COALESCE(profiles.tenant_id, EXCLUDED.tenant_id),
	    plan = EXCLUDED.plan,
	    is_active = true,
	    updated_at = now();
	
	  -- Audit log
	  INSERT INTO public.user_audit_log (performed_by, target_user_id, action, new_values, metadata)
	  VALUES (
	    NEW.id, NEW.id, 'USER_REGISTERED_PUBLICLY',
	    jsonb_build_object('email', NEW.email, 'role', v_role_enum::text, 'tenant_id', v_tenant_id),
	    jsonb_build_object('company_name', v_company_name, 'source', 'self_signup')
	  );
	
	  RETURN NEW;
	END;
	$function$
2026-09-13 06:16:36.178 UTC [22170] ERROR:  type adjustment_item[] does not exist
2026-09-13 06:16:36.178 UTC [22170] STATEMENT:  CREATE OR REPLACE FUNCTION public.process_inventory_adjustment(p_store_id uuid, p_cashier_id uuid, p_items adjustment_item[], p_operation_date timestamp with time zone DEFAULT NULL::timestamp with time zone)
	 RETURNS uuid
	 LANGUAGE plpgsql
	 SECURITY DEFINER
	 SET search_path TO 'public'
	AS $function$
	DECLARE
	  v_adjustment_id UUID;
	  v_item public.adjustment_item;
	  v_difference NUMERIC;
	  v_effective_date TIMESTAMP WITH TIME ZONE := COALESCE(p_operation_date, NOW());
	BEGIN
	  -- Validación forward-only locking
	  PERFORM public.validate_operation_date(p_operation_date, p_store_id);
	
	  INSERT INTO public.inventory_adjustments (store_id, created_by, status, created_at)
	  VALUES (p_store_id, p_cashier_id, 'PROCESSING', v_effective_date)
	  RETURNING id INTO v_adjustment_id;
	
	  FOREACH v_item IN ARRAY p_items
	  LOOP
	    v_difference := v_item.counted_quantity - v_item.expected_quantity;
	    INSERT INTO public.inventory_adjustment_items (adjustment_id, product_id, expected_quantity, counted_quantity, created_at)
	    VALUES (v_adjustment_id, v_item.product_id, v_item.expected_quantity, v_item.counted_quantity, v_effective_date);
	
	    PERFORM public.register_stock_movement(
	        p_product_id := v_item.product_id,
	        p_store_id := p_store_id,
	        p_user_id := p_cashier_id,
	        p_quantity := v_difference,
	        p_movement_type := 'adjustment',
	        p_operation_date := v_effective_date
	    );
	  END LOOP;
	
	  UPDATE public.inventory_adjustments SET status = 'COMPLETED', updated_at = v_effective_date
	  WHERE id = v_adjustment_id;
	  RETURN v_adjustment_id;
	END;
	$function$
2026-09-13 06:16:36.215 UTC [22177] ERROR:  type "public.purchase_status_enum" does not exist at character 425
2026-09-13 06:16:36.215 UTC [22177] STATEMENT:  CREATE OR REPLACE FUNCTION public.receive_against_po(
	  p_po_id uuid,
	  p_received_items jsonb DEFAULT '[]'::jsonb,
	  p_user_id uuid DEFAULT NULL,
	  p_reception_date timestamptz DEFAULT now(),
	  p_invoice_number text DEFAULT NULL
	) RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO 'public', 'extensions'
	AS $func$
	DECLARE
	  v_store_id            uuid;
	  v_supplier_name       text;
	  v_po_status           public.purchase_status_enum;
	  v_item                jsonb;
	  v_item_id             uuid;
	  v_qty                 numeric;
	  v_qty_ordered         numeric;
	  v_qty_received_before numeric;
	  v_product_id          uuid;
	  v_unit_cost           numeric;
	  v_product_name        text;
	  v_all_received        boolean;
	  v_any_received        boolean;
	  v_new_status          public.purchase_status_enum;
	  v_receipt_id          uuid;
	  v_reception_items     jsonb := '[]'::jsonb;
	  v_count               integer := 0;
	  v_po_number           text;
	  v_invoice_to_pass     text;
	  v_sorted_items        jsonb;
	BEGIN
	  -- ─── 1. Cargar OC con lock exclusivo ───
	  SELECT po.store_id, po.supplier_name, po.status, po.po_number
	    INTO v_store_id, v_supplier_name, v_po_status, v_po_number
	  FROM public.purchase_orders po
	  WHERE po.id = p_po_id
	  FOR UPDATE;
	
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_PO_NOT_FOUND';
	  END IF;
	
	  -- ─── 2. Validar acceso (tenant-aware via has_store_access) ───
	  IF NOT public.has_store_access(v_store_id) THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED';
	  END IF;
	
	  -- ─── 3. Validar estado de la OC ───
	  IF v_po_status = 'cancelled' THEN
	    RAISE EXCEPTION 'ERR_PO_CANCELLED';
	  END IF;
	  -- Solo se puede recibir contra draft, sent o partial.
	  IF v_po_status NOT IN ('draft', 'sent', 'partial') THEN
	    RAISE EXCEPTION 'ERR_PO_NOT_RECEIVABLE: status % is terminal', v_po_status;
	  END IF;
	
	  -- ─── 4. Validar items no vacío (parity B3 register_reception) ───
	  IF p_received_items IS NULL OR jsonb_array_length(p_received_items) = 0 THEN
	    RAISE EXCEPTION 'ERR_EMPTY_ITEMS';
	  END IF;
	
	  -- ─── 5. Condición #5 (anti-deadlock): Ordenar items por po_item_id ASC ───
	  SELECT jsonb_agg(elem ORDER BY (elem->>'po_item_id'))
	    INTO v_sorted_items
	  FROM jsonb_array_elements(p_received_items) AS elem;
	
	  -- ─── 6. Procesar cada item: VALIDAR antes de actualizar ───
	  FOR v_item IN SELECT * FROM jsonb_array_elements(v_sorted_items) LOOP
	    v_item_id := NULLIF(v_item->>'po_item_id', '')::uuid;
	    v_qty     := COALESCE((v_item->>'quantity_received')::numeric, 0);
	
	    -- Validar po_item_id presente
	    IF v_item_id IS NULL THEN
	      RAISE EXCEPTION 'ERR_ITEM_ID_REQUIRED: missing po_item_id in received_items';
	    END IF;
	
	    -- Medio #6: qty > 0
	    IF v_qty <= 0 THEN
	      RAISE EXCEPTION 'ERR_NEGATIVE_QTY: qty % for item % must be > 0', v_qty, v_item_id;
	    END IF;
	
	    -- Cargar estado actual del item con lock FOR UPDATE
	    SELECT product_id, quantity_ordered, quantity_received, unit_cost, product_name
	      INTO v_product_id, v_qty_ordered, v_qty_received_before, v_unit_cost, v_product_name
	    FROM public.purchase_order_items
	    WHERE id = v_item_id AND po_id = p_po_id
	    FOR UPDATE;
	
	    IF NOT FOUND THEN
	      RAISE EXCEPTION 'ERR_ITEM_NOT_FOUND: %', v_item_id;
	    END IF;
	
	    -- Alto #5: over-receive check
	    IF (v_qty_received_before + v_qty) > v_qty_ordered THEN
	      RAISE EXCEPTION 'ERR_OVER_RECEIVE: item % ordered %, already received %, attempting %',
	        v_item_id, v_qty_ordered, v_qty_received_before, v_qty;
	    END IF;
	
	    -- Condición #1: todo item debe tener product_id NOT NULL
	    IF v_product_id IS NULL THEN
	      RAISE EXCEPTION 'ERR_PRODUCT_ID_REQUIRED: item % has no product_id', v_item_id;
	    END IF;
	
	    -- Medio #9: producto debe existir en store
	    IF NOT EXISTS (
	      SELECT 1 FROM public.products p
	      WHERE p.id = v_product_id AND p.store_id = v_store_id
	    ) THEN
	      RAISE EXCEPTION 'ERR_PRODUCT_NOT_IN_STORE: product % not in store %',
	        v_product_id, v_store_id;
	    END IF;
	
	    -- Acumular para register_reception (formato esperado: product_id, quantity, unit_cost, moneda, tasa)
	    v_reception_items := v_reception_items || jsonb_build_object(
	      'product_id',   v_product_id,
	      'product_name', v_product_name,
	      'quantity',     v_qty,
	      'unit_cost',    v_unit_cost,
	      'moneda_recepcion', 'CUP',
	      'tasa_cambio_recepcion', 1.0
	    );
	    v_count := v_count + 1;
	  END LOOP;
	
	  -- ─── 7. Condición #3: reference_doc NULL si no viene invoice_number ───
	  -- Evita colisión con UNIQUE INDEX idx_receipts_store_reference_doc
	  -- (store_id, reference_doc) en partial indexes.
	  -- El vínculo real receipt↔PO es receipts.po_id (G6).
	  IF p_invoice_number IS NULL OR p_invoice_number = '' THEN
	    v_invoice_to_pass := NULL;
	  ELSE
	    v_invoice_to_pass := p_invoice_number;
	  END IF;
	
	  -- ─── 8. Crear receipt vinculado vía register_reception ───
	  -- register_reception aplica las 8 validaciones B1-B5 + C1-C3 del v2.23.0
	  -- (no se relajan). G6 añade p_po_id opcional.
	  v_receipt_id := public.register_reception(
	    p_store_id        := v_store_id,
	    p_supplier        := v_supplier_name,
	    p_reception_date  := p_reception_date,
	    p_invoice_number  := v_invoice_to_pass,
	    p_items           := v_reception_items,
	    p_user_id         := p_user_id,
	    p_po_id           := p_po_id
	  );
	
	  -- ─── 9. Actualizar quantity_received en purchase_order_items ───
	  -- (SECURITY DEFINER bypasses RLS DENY UPDATE de G5)
	  FOR v_item IN SELECT * FROM jsonb_array_elements(v_sorted_items) LOOP
	    v_item_id := NULLIF(v_item->>'po_item_id', '')::uuid;
	    v_qty     := (v_item->>'quantity_received')::numeric;
	
	    UPDATE public.purchase_order_items
	    SET quantity_received = quantity_received + v_qty
	    WHERE id = v_item_id AND po_id = p_po_id;
	  END LOOP;
	
	  -- ─── 10. Recalcular status global (cast EXPLÍCITO a enum) ───
	  SELECT
	    BOOL_AND(quantity_received >= quantity_ordered),
	    BOOL_OR(quantity_received > 0)
	  INTO v_all_received, v_any_received
	  FROM public.purchase_order_items WHERE po_id = p_po_id;
	
	  v_new_status :=
	    CASE
	      WHEN v_all_received THEN 'received'::public.purchase_status_enum
	      WHEN v_any_received THEN 'partial'::public.purchase_status_enum
	      ELSE 'sent'::public.purchase_status_enum
	    END;
	
	  -- ─── 11. Actualizar OC + vincular receipt en notas ───
	  UPDATE public.purchase_orders
	  SET status       = v_new_status,
	      received_at  = CASE WHEN v_new_status = 'received' THEN NOW() ELSE received_at END,
	      notes        = COALESCE(notes, '') ||
	                     CASE WHEN COALESCE(notes, '') = '' THEN '' ELSE E'\n' END ||
	                     '[Receipt ' || v_receipt_id::text || ' linked at ' || NOW()::text || ']'
	  WHERE id = p_po_id;
	
	  -- ─── 12. CxP (Grupo 6): marcar receipt con payment_status='unpaid' ───
	  -- Solo si aún no está seteado (no sobrescribir si register_reception ya lo puso).
	  UPDATE public.receipts
	  SET payment_status     = COALESCE(NULLIF(payment_status, ''), 'unpaid'),
	      payment_terms_days = COALESCE(payment_terms_days, 30),
	      due_date           = COALESCE(due_date, (p_reception_date::date + INTERVAL '30 days')::date)
	  WHERE id = v_receipt_id;
	
	  -- ─── 13. Auditoría ───
	  INSERT INTO public.audit_logs (user_id, store_id, action, table_name, record_id, metadata)
	  VALUES (
	    p_user_id, v_store_id, 'po_received', 'purchase_orders', p_po_id,
	    jsonb_build_object(
	      'new_status', v_new_status::text,
	      'items_received', v_count,
	      'receipt_id', v_receipt_id,
	      'po_number', v_po_number,
	      'invoice_number', v_invoice_to_pass
	    )
	  );
	
	  RETURN jsonb_build_object(
	    'status', 'success',
	    'po_status', v_new_status::text,
	    'receipt_id', v_receipt_id,
	    'items_received', v_count
	  );
	END;
	$func$
2026-09-13 06:16:36.347 UTC [22213] ERROR:  type purchase_status_enum does not exist
2026-09-13 06:16:36.347 UTC [22213] STATEMENT:  CREATE OR REPLACE FUNCTION public.set_purchase_order_status(p_po_id uuid, p_new_status purchase_status_enum, p_user_id uuid DEFAULT NULL::uuid, p_reason text DEFAULT NULL::text)
	 RETURNS jsonb
	 LANGUAGE plpgsql
	 SECURITY DEFINER
	 SET search_path TO 'public', 'extensions'
	AS $function$
	DECLARE
	  v_store_id     uuid;
	  v_current      public.purchase_status_enum;
	  v_po_number    text;
	  v_allowed      text[];
	  v_is_allowed   boolean := false;
	BEGIN
	  -- ─── 1. Cargar PO con lock exclusivo ───
	  SELECT store_id, status, po_number
	    INTO v_store_id, v_current, v_po_number
	  FROM public.purchase_orders
	  WHERE id = p_po_id
	  FOR UPDATE;
	
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_PO_NOT_FOUND';
	  END IF;
	
	  -- ─── 2. Validar acceso (tenant-aware) ───
	  IF NOT public.has_store_access(v_store_id) THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED';
	  END IF;
	
	  -- ─── 3. Si no hay cambio, retornar success sin hacer nada ───
	  IF v_current = p_new_status THEN
	    RETURN jsonb_build_object(
	      'status', 'no_change',
	      'po_status', v_current::text,
	      'po_number', v_po_number
	    );
	  END IF;
	
	  -- ─── 4. Definir transiciones permitidas (state machine) ───
	  -- 'partial' y 'received' NUNCA son destino válido manualmente
	  -- (solo vía receive_against_po).
	  v_allowed := CASE v_current
	    WHEN 'draft'   THEN ARRAY['sent', 'cancelled']
	    WHEN 'sent'    THEN ARRAY['cancelled']
	    WHEN 'partial' THEN ARRAY['cancelled']
	    ELSE ARRAY[]::text[]  -- received, cancelled: terminal, no transitions
	  END;
	
	  -- ─── 5. Verificar transición permitida ───
	  SELECT EXISTS(SELECT 1 FROM unnest(v_allowed) a WHERE a = p_new_status::text)
	    INTO v_is_allowed;
	
	  IF NOT v_is_allowed THEN
	    RAISE EXCEPTION 'ERR_INVALID_TRANSITION: % → % not allowed (allowed: %)',
	      v_current::text, p_new_status::text,
	      CASE WHEN array_length(v_allowed, 1) IS NULL THEN 'none' ELSE array_to_string(v_allowed, ', ') END;
	  END IF;
	
	  -- ─── 6. Aplicar transición ───
	  UPDATE public.purchase_orders
	  SET status      = p_new_status,
	      received_at = CASE WHEN p_new_status = 'received' THEN NOW() ELSE received_at END
	  WHERE id = p_po_id;
	
	  -- ─── 7. Auditoría (action = PO_STATUS_CHANGED) ───
	  INSERT INTO public.audit_logs (user_id, store_id, action, table_name, record_id, metadata)
	  VALUES (
	    CASE WHEN auth.role() = 'service_role' THEN COALESCE(p_user_id, auth.uid()) ELSE auth.uid() END, v_store_id, 'PO_STATUS_CHANGED', 'purchase_orders', p_po_id,
	    jsonb_build_object(
	      'po_number', v_po_number,
	      'from_status', v_current::text,
	      'to_status', p_new_status::text,
	      'reason', p_reason
	    )
	  );
	
	  RETURN jsonb_build_object(
	    'status', 'success',
	    'po_status', p_new_status::text,
	    'po_number', v_po_number,
	    'previous_status', v_current::text
	  );
	END;
	$function$
2026-09-13 06:16:36.453 UTC [22237] ERROR:  type "user_role" does not exist at character 100
2026-09-13 06:16:36.453 UTC [22237] STATEMENT:  CREATE OR REPLACE FUNCTION public.validate_active_store()
	RETURNS TRIGGER AS $$
	DECLARE
	    v_role user_role;
	BEGIN
	    v_role := NEW.role;
	
	    -- Si role es 'costo' o 'admin' o 'superadmin', active_store_id es opcional
	    IF v_role = 'costo'::user_role
	       OR v_role = 'admin'::user_role
	       OR v_role = 'superadmin'::user_role THEN
	        -- Solo validar si active_store_id cambió o es INSERT
	        IF TG_OP = 'INSERT' OR (OLD.active_store_id IS DISTINCT FROM NEW.active_store_id) THEN
	            IF NEW.active_store_id IS NOT NULL THEN
	                IF NOT EXISTS (
	                    SELECT 1 FROM public.user_store_memberships
	                    WHERE user_id = NEW.id
	                      AND store_id = NEW.active_store_id
	                      AND status = 'active'
	                ) THEN
	                    IF TG_OP = 'UPDATE' THEN
	                        RAISE EXCEPTION 'ERR_INVALID_ACTIVE_STORE: El usuario no tiene membership activa en la tienda seleccionada.';
	                    END IF;
	                END IF;
	            END IF;
	        END IF;
	        RETURN NEW;
	    END IF;
	
	    -- Para roles operativos (encargado, clerk, warehouse, manager, usuario)
	    IF TG_OP = 'INSERT' THEN
	        -- En INSERT: ser permisivo. La función managed_create_user inserta
	        -- memberships justo después. No validar nada aquí.
	        RETURN NEW;
	    ELSE
	        -- UPDATE: solo validar si active_store_id cambió realmente
	        -- (evita bloquear updates de otros campos en perfiles intermedios)
	        IF OLD.active_store_id IS NOT DISTINCT FROM NEW.active_store_id THEN
	            -- active_store_id no cambió → permitir el UPDATE sin validar
	            RETURN NEW;
	        END IF;
	
	        -- active_store_id cambió → validar consistencia
	        IF NEW.active_store_id IS NULL THEN
	            IF v_role IN ('encargado'::user_role, 'clerk'::user_role, 'warehouse'::user_role) THEN
	                RAISE EXCEPTION 'ERR_STORE_REQUIRED: El rol % requiere una tienda activa asignada.', v_role;
	            END IF;
	        ELSE
	            IF NOT EXISTS (
	                SELECT 1 FROM public.user_store_memberships
	                WHERE user_id = NEW.id
	                  AND store_id = NEW.active_store_id
	                  AND status = 'active'
	            ) THEN
	                RAISE EXCEPTION 'ERR_INVALID_ACTIVE_STORE: El usuario no tiene membership activa en la tienda seleccionada.';
	            END IF;
	        END IF;
	        RETURN NEW;
	    END IF;
	END;
	$$ LANGUAGE plpgsql SECURITY DEFINER
2026-09-13 06:16:36.711 UTC [22246] ERROR:  type "public.user_role" does not exist at character 270
2026-09-13 06:16:36.711 UTC [22246] STATEMENT:  CREATE OR REPLACE FUNCTION public.validate_tenant_access(
	  p_user_id uuid,
	  p_store_id uuid
	)
	RETURNS boolean
	LANGUAGE plpgsql
	STABLE SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  v_user_tenant uuid;
	  v_store_tenant uuid;
	  v_user_role public.user_role;
	BEGIN
	  -- Global admin bypasses tenant check
	  SELECT role, tenant_id INTO v_user_role, v_user_tenant
	    FROM public.profiles WHERE id = p_user_id;
	
	  IF v_user_role IN ('admin', 'superadmin') THEN
	    RETURN true;
	  END IF;
	
	  -- Get store's tenant
	  SELECT tenant_id INTO v_store_tenant FROM public.stores WHERE id = p_store_id;
	
	  IF v_store_tenant IS NULL THEN
	    -- Legacy store without tenant — allow (backward compat)
	    RETURN true;
	  END IF;
	
	  -- Check tenant match
	  RETURN v_user_tenant = v_store_tenant;
	END;
	$function$
2026-09-13 06:16:36.980 UTC [22279] ERROR:  type "public.user_role" does not exist at character 301
2026-09-13 06:16:36.980 UTC [22279] STATEMENT:  CREATE OR REPLACE FUNCTION public.bulk_assign_memberships(
	  p_user_id uuid,
	  p_assignments jsonb
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  v_assignment jsonb;
	  v_affected int := 0;
	  v_failed int := 0;
	  v_store_id uuid;
	  v_role public.user_role;
	  v_status text;
	  v_caller_uid uuid := auth.uid();
	  v_changes jsonb := '[]'::jsonb;
	BEGIN
	  IF v_caller_uid IS NULL THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED';
	  END IF;
	
	  FOR v_assignment IN SELECT * FROM jsonb_array_elements(p_assignments) LOOP
	    BEGIN
	      v_store_id := (v_assignment->>'store_id')::uuid;
	      v_role := (v_assignment->>'role')::public.user_role;
	      v_status := COALESCE(v_assignment->>'status', 'active');
	
	      -- Validar caller tiene acceso al store
	      IF NOT public.is_admin() AND NOT public.has_store_role(v_store_id, ARRAY['admin', 'manager']) THEN
	        v_failed := v_failed + 1;
	        CONTINUE;
	      END IF;
	
	      INSERT INTO public.user_store_memberships (user_id, store_id, role, status)
	      VALUES (p_user_id, v_store_id, v_role, v_status)
	      ON CONFLICT (user_id, store_id) DO UPDATE SET
	        role = EXCLUDED.role,
	        status = EXCLUDED.status,
	        updated_at = now();
	
	      v_changes := v_changes || jsonb_build_object(jsonb_build_object(
	        'store_id', v_store_id,
	        'role', v_role::text,
	        'status', v_status
	      ));
	
	      v_affected := v_affected + 1;
	    EXCEPTION
	      WHEN foreign_key_violation THEN
	        v_failed := v_failed + 1;
	    END;
	  END LOOP;
	
	  -- Audit log atómico (solo si hubo cambios)
	  IF v_affected > 0 THEN
	    INSERT INTO public.user_audit_log (performed_by, target_user_id, action, new_values, metadata)
	    VALUES (
	      v_caller_uid, p_user_id, 'MEMBERSHIPS_BULK_ASSIGNED',
	      jsonb_build_object('assignments', v_changes),
	      jsonb_build_object('affected', v_affected, 'failed', v_failed)
	    );
	  END IF;
	
	  RETURN jsonb_build_object(
	    'success', true,
	    'affected', v_affected,
	    'failed', v_failed,
	    'user_id', p_user_id
	  );
	END;
	$function$
2026-09-13 06:16:37.002 UTC [22282] ERROR:  syntax error at or near "REVOKE" at character 5039
2026-09-13 06:16:37.002 UTC [22282] STATEMENT:  CREATE OR REPLACE FUNCTION public.bulk_soft_delete_stores(p_store_ids uuid[], p_deleted_by uuid, p_confirmation_token text, p_override_token text DEFAULT NULL::text, p_reason text DEFAULT NULL::text)
	 RETURNS jsonb
	 LANGUAGE plpgsql
	 SECURITY DEFINER
	 SET search_path TO 'public'
	AS $function$
	DECLARE
	  v_store_id UUID;
	  v_validation JSONB;
	  v_blockers JSONB;
	  v_errors JSONB[] := '{}'::jsonb[];
	  v_processed INTEGER := 0;
	  v_token_valid BOOLEAN;
	  v_has_protected BOOLEAN;
	  v_override_valid BOOLEAN;
	  v_confirmation_record RECORD;
	  v_override_record RECORD;
	  v_caller_role TEXT;
	BEGIN
	  -- ============================================================
	  -- AUTH CHECK: Solo admin puede ejecutar esta función
	  -- ============================================================
	  IF auth.uid() IS NOT NULL THEN
	    SELECT role INTO v_caller_role FROM public.profiles WHERE id = auth.uid();
	    IF v_caller_role IS NULL OR v_caller_role != 'admin' THEN
	      RAISE EXCEPTION 'ERR_PERMISSION_DENIED: Solo admin puede ejecutar bulk_soft_delete_stores';
	    END IF;
	  END IF;
	  -- Si auth.uid() IS NULL, es service_role — permitir
	
	  -- ============================================================
	  -- 1. VALIDATE confirmation_token
	  -- ============================================================
	  SELECT * INTO v_confirmation_record
	  FROM public.bulk_confirmation_tokens
	  WHERE token = p_confirmation_token
	    AND action = 'delete'
	    AND expires_at > NOW()
	    AND consumed_at IS NULL
	    AND is_override = false;
	
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_INVALID_CONFIRMATION_TOKEN';
	  END IF;
	
	  IF v_confirmation_record.store_ids != p_store_ids THEN
	    RAISE EXCEPTION 'ERR_STORE_IDS_MISMATCH';
	  END IF;
	
	  -- ============================================================
	  -- 2. VALIDATE tiendas protegidas requieren override_token
	  -- ============================================================
	  SELECT EXISTS(
	    SELECT 1 FROM public.stores
	    WHERE id = ANY(p_store_ids) AND backup_restore_protected = true
	  ) INTO v_has_protected;
	
	  IF v_has_protected THEN
	    IF p_override_token IS NULL THEN
	      RAISE EXCEPTION 'ERR_OVERRIDE_REQUIRED';
	    END IF;
	
	    SELECT * INTO v_override_record
	    FROM public.bulk_confirmation_tokens
	    WHERE token = p_override_token
	      AND is_override = true
	      AND override_for = p_confirmation_token
	      AND expires_at > NOW()
	      AND consumed_at IS NULL;
	
	    IF NOT FOUND THEN
	      RAISE EXCEPTION 'ERR_INVALID_OVERRIDE_TOKEN';
	    END IF;
	
	    IF v_override_record.store_ids != v_confirmation_record.store_ids THEN
	      RAISE EXCEPTION 'ERR_OVERRIDE_STORE_IDS_MISMATCH';
	    END IF;
	
	    IF v_override_record.created_by = v_confirmation_record.created_by THEN
	      RAISE EXCEPTION 'ERR_SAME_USER_OVERRIDE';
	    END IF;
	
	    UPDATE public.bulk_confirmation_tokens SET consumed_at = NOW()
	    WHERE token = p_override_token;
	  END IF;
	
	  UPDATE public.bulk_confirmation_tokens SET consumed_at = NOW()
	  WHERE token = p_confirmation_token;
	
	  -- ============================================================
	  -- 3. VALIDATE todas las tiendas
	  -- ============================================================
	  FOREACH v_store_id IN ARRAY p_store_ids LOOP
	    IF NOT EXISTS(SELECT 1 FROM public.stores WHERE id = v_store_id AND is_active = true) THEN
	      v_errors := array_append(v_errors, jsonb_build_object(
	        'store_id', v_store_id, 'reason', 'STORE_NOT_FOUND_OR_INACTIVE'
	      ));
	      CONTINUE;
	    END IF;
	
	    SELECT public.validate_store_can_be_modified(v_store_id, 'soft_delete') INTO v_validation;
	    v_blockers := v_validation->'blockers';
	
	    IF v_validation->>'can_modify' != 'true' THEN
	      v_errors := array_append(v_errors, jsonb_build_object(
	        'store_id', v_store_id, 'reason', 'HAS_BLOCKING_DEPENDENCIES', 'blockers', v_blockers
	      ));
	    END IF;
	  END LOOP;
	
	  IF array_length(v_errors, 1) IS NOT NULL THEN
	    RETURN jsonb_build_object(
	      'status', 'FAILED', 'processed', 0,
	      'total_requested', array_length(p_store_ids, 1),
	      'errors', to_jsonb(v_errors), 'reason', p_reason
	    );
	  END IF;
	
	  -- ============================================================
	  -- 4. EXECUTE
	  -- ============================================================
	  FOREACH v_store_id IN ARRAY p_store_ids LOOP
	    PERFORM public.soft_delete_store(v_store_id, p_deleted_by);
	    v_processed := v_processed + 1;
	  END LOOP;
	
	  INSERT INTO public.audit_logs (action, table_name, record_id, metadata)
	  VALUES (
	    'bulk_store_deleted', 'stores', NULL,
	    jsonb_build_object(
	      'store_ids', p_store_ids, 'deleted_by', p_deleted_by,
	      'reason', p_reason, 'processed', v_processed,
	      'had_protected_stores', v_has_protected,
	      'override_used', p_override_token IS NOT NULL, 'deleted_at', NOW()
	    )
	  );
	
	  RETURN jsonb_build_object(
	    'status', 'COMPLETED', 'processed', v_processed,
	    'total_requested', array_length(p_store_ids, 1),
	    'errors', '[]'::jsonb, 'reason', p_reason
	  );
	END;
	$function$
	 
	REVOKE EXECUTE ON FUNCTION public.bulk_soft_delete_stores FROM PUBLIC, anon, authenticated
2026-09-13 06:16:37.042 UTC [22289] ERROR:  relation "received_services" does not exist
2026-09-13 06:16:37.042 UTC [22289] CONTEXT:  compilation of PL/pgSQL function "calculate_service_distribution" near line 3
2026-09-13 06:16:37.042 UTC [22289] STATEMENT:  CREATE OR REPLACE FUNCTION calculate_service_distribution(
	  p_service_id UUID
	) RETURNS TABLE(
	  receipt_item_id UUID,
	  product_id UUID,
	  distribution_amount NUMERIC,
	  distribution_percentage NUMERIC
	) LANGUAGE plpgsql SECURITY DEFINER AS $$
	DECLARE
	  v_service received_services%ROWTYPE;
	  v_method TEXT;
	  v_total_amount NUMERIC;
	  v_total_value NUMERIC DEFAULT 0;
	  v_total_qty NUMERIC DEFAULT 0;
	  v_link service_reception_links%ROWTYPE;
	  v_receipt_id UUID;
	  v_item receipt_items%ROWTYPE;
	  v_allocated NUMERIC;
	BEGIN
	  SELECT * INTO v_service FROM received_services WHERE id = p_service_id;
	  IF NOT FOUND THEN RETURN; END IF;
	  
	  v_method := v_service.distribution_method;
	  v_total_amount := v_service.total_amount;
	
	  -- Para cada recepción vinculada
	  FOR v_link IN SELECT * FROM service_reception_links WHERE service_id = p_service_id AND allocated_amount > 0 LOOP
	    v_receipt_id := v_link.receipt_id;
	    v_allocated := v_link.allocated_amount;
	
	    -- Calcular base de distribución según método
	    IF v_method = 'amount' THEN
	      SELECT COALESCE(SUM(quantity * unit_cost), 0) INTO v_total_value
	      FROM receipt_items WHERE receipt_id = v_receipt_id;
	      
	      IF v_total_value > 0 THEN
	        FOR v_item IN SELECT * FROM receipt_items WHERE receipt_id = v_receipt_id LOOP
	          distribution_amount := v_allocated * (v_item.quantity * v_item.unit_cost / v_total_value);
	          distribution_percentage := (v_item.quantity * v_item.unit_cost / v_total_value) * 100;
	          receipt_item_id := v_item.id;
	          product_id := v_item.product_id;
	          RETURN NEXT;
	        END LOOP;
	      END IF;
	
	    ELSIF v_method = 'quantity' THEN
	      SELECT COALESCE(SUM(quantity), 0) INTO v_total_qty
	      FROM receipt_items WHERE receipt_id = v_receipt_id;
	      
	      IF v_total_qty > 0 THEN
	        FOR v_item IN SELECT * FROM receipt_items WHERE receipt_id = v_receipt_id LOOP
	          distribution_amount := v_allocated * (v_item.quantity / v_total_qty);
	          distribution_percentage := (v_item.quantity / v_total_qty) * 100;
	          receipt_item_id := v_item.id;
	          product_id := v_item.product_id;
	          RETURN NEXT;
	        END LOOP;
	      END IF;
	
	    ELSIF v_method = 'manual' THEN
	      -- En manual, la distribución se guarda directamente desde el cliente
	      RETURN;
	    END IF;
	  END LOOP;
	END;
	$$
2026-09-13 06:16:37.159 UTC [22300] ERROR:  syntax error at or near "REVOKE" at character 678
2026-09-13 06:16:37.159 UTC [22300] STATEMENT:  CREATE OR REPLACE FUNCTION public.check_bulk_ops_hourly_limit(p_user_id uuid, p_plan text)
	 RETURNS jsonb
	 LANGUAGE plpgsql
	 STABLE SECURITY DEFINER
	 SET search_path TO 'public'
	AS $function$
	DECLARE
	  v_limit INTEGER;
	  v_used INTEGER;
	BEGIN
	  v_limit := CASE p_plan
	    WHEN 'free' THEN 1
	    WHEN 'pro' THEN 20
	    WHEN 'enterprise' THEN 999999
	    ELSE 1
	  END;
	
	  SELECT COUNT(*) INTO v_used
	  FROM public.bulk_ops_log
	  WHERE user_id = p_user_id
	    AND initiated_at > NOW() - INTERVAL '1 hour';
	
	  RETURN jsonb_build_object(
	    'allowed', v_used < v_limit,
	    'used', v_used,
	    'limit', v_limit,
	    'remaining', GREATEST(0, v_limit - v_used)
	  );
	END;
	$function$
	 
	REVOKE EXECUTE ON FUNCTION public.check_bulk_ops_hourly_limit FROM PUBLIC, anon, authenticated
2026-09-13 06:16:37.186 UTC [22305] ERROR:  type plan_t does not exist
2026-09-13 06:16:37.186 UTC [22305] STATEMENT:  CREATE OR REPLACE FUNCTION public.check_tenant_store_quota(
	  p_tenant_id uuid,
	  p_plan plan_t DEFAULT NULL
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  v_plan plan_t;
	  v_limit int;
	  v_current int;
	  v_allowed boolean;
	BEGIN
	  -- Get plan from tenant if not passed
	  IF p_plan IS NULL THEN
	    SELECT plan INTO v_plan FROM public.tenants WHERE id = p_tenant_id;
	  ELSE
	    v_plan := p_plan;
	  END IF;
	
	  -- Plan limits (unified source of truth)
	  v_limit := CASE v_plan
	    WHEN 'free'::plan_t THEN 1
	    WHEN 'pro'::plan_t THEN 3
	    WHEN 'enterprise'::plan_t THEN 10
	    ELSE 1
	  END;
	
	  -- Count active stores in tenant
	  SELECT COUNT(*) INTO v_current
	    FROM public.stores
	    WHERE tenant_id = p_tenant_id AND is_active = true;
	
	  v_allowed := v_current < v_limit;
	
	  RETURN jsonb_build_object(
	    'allowed', v_allowed,
	    'current', v_current,
	    'limit', v_limit,
	    'plan', v_plan::text,
	    'tenant_id', p_tenant_id
	  );
	END;
	$function$
2026-09-13 06:16:37.293 UTC [22332] ERROR:  relation "public.profiles" does not exist at character 166
2026-09-13 06:16:37.293 UTC [22332] STATEMENT:  CREATE OR REPLACE FUNCTION public.current_user_tenant_id()
	RETURNS uuid
	LANGUAGE sql
	STABLE
	SECURITY DEFINER
	SET search_path TO public
	AS $$
	  SELECT tenant_id FROM public.profiles WHERE id = auth.uid();
	$$
2026-09-13 06:16:37.329 UTC [22338] ERROR:  relation "received_services" does not exist
2026-09-13 06:16:37.329 UTC [22338] CONTEXT:  compilation of PL/pgSQL function "distribute_service_cost_v2" near line 6
2026-09-13 06:16:37.329 UTC [22338] STATEMENT:  CREATE OR REPLACE FUNCTION public.distribute_service_cost_v2(
	  p_service_id uuid,
	  p_user_id uuid DEFAULT NULL
	) RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO 'public', 'extensions'
	AS $func$
	DECLARE
	  v_store_id uuid;
	  v_status text;
	  v_total_amount numeric;
	  v_service received_services%ROWTYPE;
	  v_method text;
	  v_link service_reception_links%ROWTYPE;
	  v_item receipt_items%ROWTYPE;
	  v_total_value numeric := 0;
	  v_total_qty numeric := 0;
	  v_allocated numeric;
	  v_dist_count integer := 0;
	  v_caller_uid uuid := COALESCE(p_user_id, auth.uid());
	BEGIN
	  PERFORM pg_advisory_xact_lock(hashtext(p_service_id::text));
	
	  SELECT * INTO v_service FROM received_services WHERE id = p_service_id FOR UPDATE;
	  IF NOT FOUND THEN RAISE EXCEPTION 'ERR_SERVICE_NOT_FOUND'; END IF;
	
	  v_store_id := v_service.store_id;
	  v_status := v_service.status;
	  v_total_amount := v_service.total_amount;
	  v_method := v_service.distribution_method;
	
	  IF NOT public.has_store_access(v_store_id) THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED';
	  END IF;
	
	  IF v_status != 'active' THEN
	    RAISE EXCEPTION 'ERR_SERVICE_NOT_ACTIVE: status % is not active', v_status;
	  END IF;
	
	  IF v_method = 'manual' THEN
	    RAISE EXCEPTION 'ERR_MANUAL_METHOD: use link_receipts_to_service for manual distribution';
	  END IF;
	
	  DELETE FROM service_cost_distributions WHERE service_id = p_service_id;
	
	  FOR v_link IN SELECT * FROM service_reception_links WHERE service_id = p_service_id AND allocated_amount > 0 ORDER BY receipt_id LOOP
	    v_allocated := v_link.allocated_amount;
	
	    IF v_method = 'amount' THEN
	      SELECT COALESCE(SUM(quantity * unit_cost), 0) INTO v_total_value
	      FROM receipt_items WHERE receipt_id = v_link.receipt_id;
	      IF v_total_value > 0 THEN
	        FOR v_item IN SELECT * FROM receipt_items WHERE receipt_id = v_link.receipt_id ORDER BY id LOOP
	          INSERT INTO service_cost_distributions
	            (service_id, receipt_id, receipt_item_id, product_id, distribution_amount, distribution_percentage)
	          VALUES
	            (p_service_id, v_link.receipt_id, v_item.id, v_item.product_id,
	             v_allocated * (v_item.quantity * v_item.unit_cost / v_total_value),
	             (v_item.quantity * v_item.unit_cost / v_total_value) * 100);
	          v_dist_count := v_dist_count + 1;
	        END LOOP;
	      END IF;
	
	    ELSIF v_method = 'quantity' THEN
	      SELECT COALESCE(SUM(quantity), 0) INTO v_total_qty
	      FROM receipt_items WHERE receipt_id = v_link.receipt_id;
	      IF v_total_qty > 0 THEN
	        FOR v_item IN SELECT * FROM receipt_items WHERE receipt_id = v_link.receipt_id ORDER BY id LOOP
	          INSERT INTO service_cost_distributions
	            (service_id, receipt_id, receipt_item_id, product_id, distribution_amount, distribution_percentage)
	          VALUES
	            (p_service_id, v_link.receipt_id, v_item.id, v_item.product_id,
	             v_allocated * (v_item.quantity / v_total_qty),
	             (v_item.quantity / v_total_qty) * 100);
	          v_dist_count := v_dist_count + 1;
	        END LOOP;
	      END IF;
	    END IF;
	  END LOOP;
	
	  -- Forzar recalculo de WAC para cada producto afectado
	  UPDATE receipt_items SET updated_at = NOW()
	  WHERE product_id IN (SELECT DISTINCT product_id FROM service_cost_distributions WHERE service_id = p_service_id);
	
	  INSERT INTO audit_logs (user_id, store_id, action, table_name, record_id, metadata)
	  VALUES (v_caller_uid, v_store_id, 'SERVICE_DISTRIBUTED', 'received_services', p_service_id,
	    jsonb_build_object('rows_distributed', v_dist_count, 'method', v_method, 'total_amount', v_total_amount));
	
	  RETURN jsonb_build_object('status', 'success', 'distributed_rows', v_dist_count);
	END;
	$func$
2026-09-13 06:16:37.373 UTC [22343] ERROR:  type "user_role" does not exist at character 116
2026-09-13 06:16:37.373 UTC [22343] STATEMENT:  CREATE OR REPLACE FUNCTION public.enforce_encargado_store_limit()
	RETURNS TRIGGER AS $$
	DECLARE
	    v_creator_role user_role;
	    v_limit integer;
	    v_current_count integer;
	BEGIN
	    IF NEW.created_by IS NULL THEN RETURN NEW; END IF;
	
	    SELECT role, max_stores_limit INTO v_creator_role, v_limit
	    FROM public.profiles WHERE id = NEW.created_by;
	
	    IF v_creator_role = 'encargado' THEN
	        SELECT COUNT(*) INTO v_current_count FROM public.stores WHERE created_by = NEW.created_by;
	        IF v_current_count >= v_limit THEN
	            RAISE EXCEPTION 'ERR_STORE_LIMIT_EXCEEDED: Maximum number of stores reached for this manager (%/%)', v_current_count, v_limit;
	        END IF;
	    END IF;
	    RETURN NEW;
	END;
	$$ LANGUAGE plpgsql
2026-09-13 06:16:37.397 UTC [22346] ERROR:  type "user_role" does not exist at character 115
2026-09-13 06:16:37.397 UTC [22346] STATEMENT:  CREATE OR REPLACE FUNCTION public.enforce_encargado_user_limit()
	RETURNS TRIGGER AS $$
	DECLARE
	    v_creator_role user_role;
	    v_limit integer;
	    v_current_count integer;
	BEGIN
	    IF NEW.created_by IS NULL THEN RETURN NEW; END IF;
	
	    SELECT role, max_users_limit INTO v_creator_role, v_limit
	    FROM public.profiles WHERE id = NEW.created_by;
	
	    IF v_creator_role = 'encargado' THEN
	        SELECT COUNT(*) INTO v_current_count FROM public.profiles WHERE created_by = NEW.created_by;
	        IF v_current_count >= v_limit THEN
	            RAISE EXCEPTION 'ERR_USER_LIMIT_EXCEEDED: Maximum number of users reached for this manager (%/%)', v_current_count, v_limit;
	        END IF;
	    END IF;
	    RETURN NEW;
	END;
	$$ LANGUAGE plpgsql
2026-09-13 06:16:37.461 UTC [22359] ERROR:  syntax error at or near "REVOKE" at character 1436
2026-09-13 06:16:37.461 UTC [22359] STATEMENT:  CREATE OR REPLACE FUNCTION public.generate_bulk_confirmation_token(p_store_ids uuid[], p_action text, p_user_id uuid)
	 RETURNS text
	 LANGUAGE plpgsql
	 SECURITY DEFINER
	 SET search_path TO 'public'
	AS $function$
	DECLARE
	  v_token TEXT;
	  v_has_protected BOOLEAN;
	  v_caller_role TEXT;
	BEGIN
	  -- AUTH CHECK: Solo admin
	  IF auth.uid() IS NOT NULL THEN
	    SELECT role INTO v_caller_role FROM public.profiles WHERE id = auth.uid();
	    IF v_caller_role IS NULL OR v_caller_role != 'admin' THEN
	      RAISE EXCEPTION 'ERR_PERMISSION_DENIED: Solo admin puede generar tokens bulk';
	    END IF;
	    -- Verificar que p_user_id coincide con auth.uid()
	    IF p_user_id != auth.uid() THEN
	      RAISE EXCEPTION 'ERR_PERMISSION_DENIED: p_user_id debe coincidir con el usuario autenticado';
	    END IF;
	  END IF;
	
	  IF p_action NOT IN ('delete', 'archive') THEN
	    RAISE EXCEPTION 'ERR_NON_DESTRUCTIVE_ACTION';
	  END IF;
	
	  SELECT EXISTS(
	    SELECT 1 FROM public.stores
	    WHERE id = ANY(p_store_ids) AND backup_restore_protected = true
	  ) INTO v_has_protected;
	
	  v_token := 'bct_' || replace(gen_random_uuid()::text, '-', '');
	
	  INSERT INTO public.bulk_confirmation_tokens (
	    token, store_ids, action, created_by, expires_at, metadata
	  ) VALUES (
	    v_token, p_store_ids, p_action, p_user_id,
	    NOW() + INTERVAL '10 minutes',
	    jsonb_build_object('has_protected_stores', v_has_protected)
	  );
	
	  RETURN v_token;
	END;
	$function$
	 
	REVOKE EXECUTE ON FUNCTION public.generate_bulk_confirmation_token FROM PUBLIC, anon, authenticated
2026-09-13 06:16:37.480 UTC [22362] ERROR:  syntax error at or near "REVOKE" at character 2432
2026-09-13 06:16:37.480 UTC [22362] STATEMENT:  CREATE OR REPLACE FUNCTION public.generate_bulk_override_token(p_confirmation_token text, p_override_user_id uuid, p_reason text DEFAULT NULL::text)
	 RETURNS text
	 LANGUAGE plpgsql
	 SECURITY DEFINER
	 SET search_path TO 'public'
	AS $function$
	DECLARE
	  v_original RECORD;
	  v_override_token TEXT;
	  v_override_user_role TEXT;
	BEGIN
	  -- AUTH CHECK: Solo admin, y debe ser auth.uid() == p_override_user_id
	  IF auth.uid() IS NOT NULL THEN
	    IF auth.uid() != p_override_user_id THEN
	      RAISE EXCEPTION 'ERR_PERMISSION_DENIED: p_override_user_id debe coincidir con el usuario autenticado';
	    END IF;
	
	    SELECT role INTO v_override_user_role FROM public.profiles WHERE id = p_override_user_id;
	    IF v_override_user_role IS NULL OR v_override_user_role != 'admin' THEN
	      RAISE EXCEPTION 'ERR_PERMISSION_DENIED: Solo admin puede generar override';
	    END IF;
	  END IF;
	
	  SELECT * INTO v_original
	  FROM public.bulk_confirmation_tokens
	  WHERE token = p_confirmation_token
	    AND consumed_at IS NULL
	    AND expires_at > NOW()
	    AND is_override = false;
	
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_INVALID_OR_EXPIRED_TOKEN';
	  END IF;
	
	  IF v_original.created_by = p_override_user_id THEN
	    RAISE EXCEPTION 'ERR_SAME_USER_OVERRIDE';
	  END IF;
	
	  IF EXISTS(
	    SELECT 1 FROM public.bulk_confirmation_tokens
	    WHERE override_for = p_confirmation_token
	      AND consumed_at IS NULL AND expires_at > NOW()
	  ) THEN
	    RAISE EXCEPTION 'ERR_OVERRIDE_ALREADY_EXISTS';
	  END IF;
	
	  v_override_token := 'bot_' || replace(gen_random_uuid()::text, '-', '');
	
	  INSERT INTO public.bulk_confirmation_tokens (
	    token, store_ids, action, created_by, expires_at,
	    is_override, override_for, metadata
	  ) VALUES (
	    v_override_token, v_original.store_ids, v_original.action, p_override_user_id,
	    NOW() + INTERVAL '10 minutes', true, p_confirmation_token,
	    jsonb_build_object('override_reason', p_reason,
	      'original_created_by', v_original.created_by,
	      'override_created_by', p_override_user_id)
	  );
	
	  INSERT INTO public.audit_logs (action, table_name, record_id, metadata)
	  VALUES (
	    'bulk_override_token_generated', 'stores', NULL,
	    jsonb_build_object('confirmation_token', p_confirmation_token,
	      'store_ids', v_original.store_ids,
	      'override_by', p_override_user_id,
	      'original_by', v_original.created_by, 'reason', p_reason)
	  );
	
	  RETURN v_override_token;
	END;
	$function$
	 
	REVOKE EXECUTE ON FUNCTION public.generate_bulk_override_token FROM PUBLIC, anon, authenticated
2026-09-13 06:16:37.517 UTC [22370] ERROR:  relation "public.backup_table_registry" does not exist at character 581
2026-09-13 06:16:37.517 UTC [22370] STATEMENT:  CREATE OR REPLACE FUNCTION public.get_backup_table_list(
	  p_include_excluded BOOLEAN DEFAULT FALSE
	)
	RETURNS TABLE (
	  table_name          TEXT,
	  tier                INTEGER,
	  filter_strategy     TEXT,
	  parent_table        TEXT,
	  parent_foreign_key  TEXT,
	  date_column         TEXT,
	  excluded_from_restore BOOLEAN,
	  exclude_reason      TEXT
	)
	LANGUAGE sql
	SECURITY DEFINER
	SET search_path = public
	AS $$
	  SELECT
	    table_name,
	    tier,
	    filter_strategy,
	    parent_table,
	    parent_foreign_key,
	    date_column,
	    excluded_from_restore,
	    exclude_reason
	  FROM public.backup_table_registry
	  WHERE (p_include_excluded OR excluded_from_restore = FALSE)
	  ORDER BY tier ASC, table_name ASC;
	$$
2026-09-13 06:16:37.554 UTC [22378] ERROR:  relation "v_global_operation_dates" does not exist at character 229
2026-09-13 06:16:37.554 UTC [22378] STATEMENT:  CREATE OR REPLACE FUNCTION public.get_global_max_operation_date(
	  p_store_id UUID DEFAULT NULL
	)
	RETURNS TIMESTAMP WITH TIME ZONE
	LANGUAGE sql
	SECURITY DEFINER
	SET search_path = public
	AS $$
	  SELECT MAX(operation_date)
	  FROM v_global_operation_dates
	  WHERE p_store_id IS NULL OR store_id = p_store_id;
	$$
2026-09-13 06:16:37.583 UTC [22381] ERROR:  relation "exchange_rates" does not exist at character 216
2026-09-13 06:16:37.583 UTC [22381] STATEMENT:  CREATE OR REPLACE FUNCTION get_latest_rate(p_currency TEXT, p_source TEXT)
	RETURNS TABLE(rate NUMERIC, rate_date DATE, captured_at TIMESTAMPTZ)
	LANGUAGE sql STABLE AS $$
	  SELECT rate, rate_date, captured_at
	  FROM exchange_rates
	  WHERE currency = p_currency AND source = p_source
	  ORDER BY rate_date DESC, captured_at DESC
	  LIMIT 1;
	$$
2026-09-13 06:16:37.617 UTC [22384] ERROR:  relation "public.products" does not exist at character 194
2026-09-13 06:16:37.617 UTC [22384] STATEMENT:  CREATE OR REPLACE FUNCTION public.get_low_stock_count(p_store_id uuid DEFAULT NULL)
	RETURNS bigint
	LANGUAGE sql
	SECURITY DEFINER
	SET search_path = public
	AS $$
	  SELECT COUNT(*)::bigint
	  FROM public.products
	  WHERE
	    (p_store_id IS NULL OR store_id = p_store_id)
	    AND is_active = true
	    AND stock_current > 0
	    AND min_stock IS NOT NULL
	    AND min_stock > 0
	    AND stock_current <= min_stock;
	$$
2026-09-13 06:16:37.661 UTC [22391] ERROR:  relation "receipt_items" does not exist at character 754
2026-09-13 06:16:37.661 UTC [22391] STATEMENT:  CREATE OR REPLACE FUNCTION get_product_cost_analysis(
	  p_product_id UUID,
	  p_store_id UUID
	) RETURNS TABLE(
	  receipt_id UUID,
	  receipt_date TIMESTAMPTZ,
	  quantity INT,
	  unit_cost FLOAT,
	  service_type TEXT,
	  service_amount NUMERIC,
	  total_cost NUMERIC,
	  unit_cost_final NUMERIC
	) LANGUAGE sql SECURITY DEFINER AS $$
	  SELECT 
	    ri.receipt_id,
	    r.reception_date,
	    ri.quantity,
	    ri.unit_cost,
	    st.name AS service_type,
	    COALESCE(scd.distribution_amount, 0) AS service_amount,
	    ri.unit_cost * ri.quantity + COALESCE(scd.distribution_amount, 0) AS total_cost,
	    CASE WHEN ri.quantity > 0 
	      THEN (ri.unit_cost * ri.quantity + COALESCE(scd.distribution_amount, 0)) / ri.quantity 
	      ELSE 0 END AS unit_cost_final
	  FROM receipt_items ri
	  JOIN receipts r ON r.id = ri.receipt_id
	  LEFT JOIN service_cost_distributions scd ON scd.receipt_item_id = ri.id
	  LEFT JOIN received_services rs ON rs.id = scd.service_id AND rs.status = 'active'
	  LEFT JOIN service_types st ON st.id = rs.service_type_id
	  WHERE ri.product_id = p_product_id
	    AND r.store_id = p_store_id
	    AND r.status = 'active'
	  ORDER BY r.reception_date DESC;
	$$
2026-09-13 06:16:37.713 UTC [22397] ERROR:  relation "exchange_rates" does not exist at character 252
2026-09-13 06:16:37.713 UTC [22397] STATEMENT:  CREATE OR REPLACE FUNCTION get_rate_history(
	  p_currency TEXT,
	  p_source TEXT,
	  p_days INT DEFAULT 30
	)
	RETURNS TABLE(rate_date DATE, rate NUMERIC, variation_daily NUMERIC)
	LANGUAGE sql STABLE AS $$
	  SELECT rate_date, rate, variation_daily
	  FROM exchange_rates
	  WHERE currency = p_currency AND source = p_source
	    AND rate_date >= CURRENT_DATE - p_days
	  ORDER BY rate_date ASC;
	$$
2026-09-13 06:16:37.732 UTC [22400] ERROR:  relation "exchange_rates" does not exist at character 363
2026-09-13 06:16:37.732 UTC [22400] STATEMENT:  CREATE OR REPLACE FUNCTION get_rate_variation(
	  p_currency TEXT,
	  p_source TEXT,
	  p_start_date DATE,
	  p_end_date DATE
	)
	RETURNS TABLE(
	  start_rate NUMERIC,
	  end_rate NUMERIC,
	  absolute_change NUMERIC,
	  percent_change NUMERIC,
	  daily_avg_growth NUMERIC,
	  monthly_avg_growth NUMERIC
	)
	LANGUAGE sql STABLE AS $$
	  WITH start_val AS (
	    SELECT rate FROM exchange_rates
	    WHERE currency = p_currency AND source = p_source AND rate_date <= p_start_date
	    ORDER BY rate_date DESC LIMIT 1
	  ),
	  end_val AS (
	    SELECT rate FROM exchange_rates
	    WHERE currency = p_currency AND source = p_source AND rate_date <= p_end_date
	    ORDER BY rate_date DESC LIMIT 1
	  )
	  SELECT
	    COALESCE(sv.rate, 0) AS start_rate,
	    COALESCE(ev.rate, 0) AS end_rate,
	    (COALESCE(ev.rate, 0) - COALESCE(sv.rate, 0)) AS absolute_change,
	    CASE WHEN sv.rate IS NOT NULL AND sv.rate > 0 THEN ((ev.rate - sv.rate) / sv.rate * 100) ELSE 0 END AS percent_change,
	    CASE WHEN sv.rate IS NOT NULL AND sv.rate > 0 AND ev.rate IS NOT NULL AND ev.rate > 0 AND p_end_date > p_start_date
	      THEN (POWER(ev.rate::FLOAT / sv.rate::FLOAT, 1.0 / EXTRACT(EPOCH FROM (p_end_date - p_start_date)) * 86400) - 1) * 100
	      ELSE 0 END AS daily_avg_growth,
	    CASE WHEN sv.rate IS NOT NULL AND sv.rate > 0 AND ev.rate IS NOT NULL AND ev.rate > 0 AND p_end_date > p_start_date
	      THEN (POWER(ev.rate::FLOAT / sv.rate::FLOAT, 30.44 / EXTRACT(EPOCH FROM (p_end_date - p_start_date)) * 86400) - 1) * 100
	      ELSE 0 END AS monthly_avg_growth
	  FROM start_val sv, end_val ev;
	$$
2026-09-13 06:16:37.785 UTC [22413] ERROR:  type "public.stores" does not exist
2026-09-13 06:16:37.785 UTC [22413] STATEMENT:  CREATE OR REPLACE FUNCTION public.get_transferable_stores(
	  p_user_id UUID,
	  p_current_store_id UUID
	)
	RETURNS SETOF public.stores
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO 'public'
	AS $$
	DECLARE
	  v_tenant_id UUID;
	BEGIN
	  -- 1. Obtener tenant_id de la tienda actual
	  SELECT tenant_id INTO v_tenant_id
	  FROM public.stores
	  WHERE id = p_current_store_id AND is_active = true;
	
	  -- 2. Devolver tiendas donde el caller tiene acceso (has_store_access_as)
	  --    EXCLUYENDO la tienda actual
	  --    Si tenant_id es NOT NULL, filtrar por mismo tenant.
	  --    Si tenant_id es NULL (legacy), mostrar todas las que el caller tiene acceso.
	  IF v_tenant_id IS NOT NULL THEN
	    RETURN QUERY
	    SELECT s.*
	    FROM public.stores s
	    WHERE s.tenant_id = v_tenant_id
	      AND s.id != p_current_store_id
	      AND s.is_active = true
	      AND s.is_archived = false
	      AND public.has_store_access_as(p_user_id, s.id)
	    ORDER BY s.name;
	  ELSE
	    -- Legacy: tiendas sin tenant_id — usar solo has_store_access_as
	    RETURN QUERY
	    SELECT s.*
	    FROM public.stores s
	    WHERE s.id != p_current_store_id
	      AND s.is_active = true
	      AND s.is_archived = false
	      AND public.has_store_access_as(p_user_id, s.id)
	    ORDER BY s.name;
	  END IF;
	END;
	$$
2026-09-13 06:16:37.828 UTC [22422] ERROR:  type user_role[] does not exist
2026-09-13 06:16:37.828 UTC [22422] STATEMENT:  CREATE OR REPLACE FUNCTION public.has_any_role(required_roles user_role[])
	 RETURNS boolean
	 LANGUAGE plpgsql
	 STABLE SECURITY DEFINER
	AS $$
	DECLARE
	    v_actual_role user_role;
	    r user_role;
	BEGIN
	    SELECT role INTO v_actual_role FROM public.profiles WHERE id = auth.uid();
	
	    FOREACH r IN ARRAY required_roles
	    LOOP
	        IF v_actual_role = r THEN RETURN true; END IF;
	        IF v_actual_role = 'encargado' AND r = 'manager' THEN RETURN true; END IF;
	        IF v_actual_role = 'usuario' AND (r = 'clerk' OR r = 'warehouse') THEN RETURN true; END IF;
	    END LOOP;
	
	    RETURN false;
	END;
	$$
2026-09-13 06:16:37.847 UTC [22425] ERROR:  relation "profiles" does not exist at character 461
2026-09-13 06:16:37.847 UTC [22425] STATEMENT:  CREATE OR REPLACE FUNCTION public.has_management_access_as(
	  p_user_id uuid,
	  p_store_id uuid
	) RETURNS boolean
	LANGUAGE sql SECURITY DEFINER
	SET search_path = public, pg_temp
	AS $$
	  -- service_role always passes
	  SELECT CASE WHEN auth.role() = 'service_role' THEN true
	    ELSE
	      -- Check if user has admin/manager/encargado role in their profile
	      -- OR has a membership with manager/admin role for the store
	      EXISTS (
	        SELECT 1 FROM profiles
	        WHERE id = p_user_id
	        AND role IN ('admin', 'manager', 'encargado')
	      )
	      OR EXISTS (
	        SELECT 1 FROM user_store_memberships
	        WHERE user_id = p_user_id
	        AND store_id = p_store_id
	        AND status = 'active'
	        AND role IN ('admin', 'manager')
	      )
	    END
	$$
2026-09-13 06:16:37.864 UTC [22428] ERROR:  type public.user_role does not exist
2026-09-13 06:16:37.864 UTC [22428] STATEMENT:  CREATE OR REPLACE FUNCTION public.has_role(p_user_id UUID, p_required_role public.user_role)
	 RETURNS boolean
	 LANGUAGE plpgsql
	 STABLE SECURITY DEFINER
	AS $$
	DECLARE
	    v_actual_role public.user_role;
	BEGIN
	    SELECT role INTO v_actual_role FROM public.profiles WHERE id = p_user_id;
	
	    IF v_actual_role IS NULL THEN
	        RETURN false;
	    END IF;
	
	    -- Compatibility mapping (mirroring the 1-arg version)
	    IF v_actual_role = 'encargado' AND p_required_role = 'manager' THEN RETURN true; END IF;
	    IF v_actual_role = 'usuario' AND (p_required_role = 'clerk' OR p_required_role = 'warehouse') THEN RETURN true; END IF;
	
	    RETURN v_actual_role = p_required_role;
	END;
	$$
2026-09-13 06:16:37.890 UTC [22433] ERROR:  relation "public.user_store_memberships" does not exist at character 279
2026-09-13 06:16:37.890 UTC [22433] STATEMENT:  CREATE OR REPLACE FUNCTION public.has_store_role(
	  p_user_id uuid,
	  p_store_id uuid,
	  p_roles text[]
	) RETURNS boolean
	LANGUAGE sql SECURITY DEFINER
	SET search_path = public, pg_temp
	AS $$
	  SELECT CASE WHEN auth.role() = 'service_role' THEN
	    EXISTS (
	      SELECT 1 FROM public.user_store_memberships m
	      WHERE m.user_id = p_user_id
	        AND m.store_id = p_store_id
	        AND m.status = 'active'
	        AND m.role::text = ANY(p_roles)
	    )
	  ELSE
	    EXISTS (
	      SELECT 1 FROM public.user_store_memberships m
	      WHERE m.user_id = auth.uid()
	        AND m.store_id = p_store_id
	        AND m.status = 'active'
	        AND m.role::text = ANY(p_roles)
	    )
	  END
	$$
2026-09-13 06:16:37.920 UTC [22439] ERROR:  relation "public.profiles" does not exist at character 173
2026-09-13 06:16:37.920 UTC [22439] STATEMENT:  CREATE OR REPLACE FUNCTION public.is_global_admin()
	RETURNS BOOLEAN
	LANGUAGE sql
	STABLE
	SECURITY DEFINER
	SET search_path = public
	AS $$
	  SELECT EXISTS (
	    SELECT 1 FROM public.profiles
	    WHERE id = auth.uid()
	      AND role = 'admin'
	  );
	$$
2026-09-13 06:16:37.944 UTC [22443] ERROR:  type user_role does not exist
2026-09-13 06:16:37.944 UTC [22443] STATEMENT:  CREATE OR REPLACE FUNCTION public.is_role_not_changed(p_user_id uuid, p_new_role user_role, p_new_role_id uuid)
	RETURNS boolean
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path = public, extensions
	AS $$
	DECLARE
	  v_old_role user_role;
	  v_old_role_id uuid;
	BEGIN
	  SELECT role, role_id INTO v_old_role, v_old_role_id
	  FROM public.profiles
	  WHERE id = p_user_id;
	
	  RETURN (p_new_role IS NOT DISTINCT FROM v_old_role)
	     AND (p_new_role_id IS NOT DISTINCT FROM v_old_role_id);
	END;
	$$
2026-09-13 06:16:37.965 UTC [22447] ERROR:  relation "public.user_store_memberships" does not exist at character 188
2026-09-13 06:16:37.965 UTC [22447] STATEMENT:  CREATE OR REPLACE FUNCTION public.is_store_member(p_store_id UUID)
	RETURNS BOOLEAN
	LANGUAGE sql
	STABLE
	SECURITY DEFINER
	SET search_path = public
	AS $$
	  SELECT EXISTS (
	    SELECT 1 FROM public.user_store_memberships
	    WHERE store_id = p_store_id
	      AND user_id = auth.uid()
	      AND status = 'active'
	  );
	$$
2026-09-13 06:16:37.984 UTC [22450] ERROR:  function public.current_user_tenant_id() does not exist at character 209
2026-09-13 06:16:37.984 UTC [22450] HINT:  No function matches the given name and argument types. You might need to add explicit type casts.
2026-09-13 06:16:37.984 UTC [22450] STATEMENT:  CREATE OR REPLACE FUNCTION public.is_tenant_member(p_tenant_id uuid)
	RETURNS boolean
	LANGUAGE sql
	STABLE
	SECURITY DEFINER
	SET search_path TO public
	AS $$
	  SELECT p_tenant_id IS NOT NULL
	   AND p_tenant_id = public.current_user_tenant_id();
	$$
2026-09-13 06:16:38.028 UTC [22460] ERROR:  type user_role does not exist
2026-09-13 06:16:38.028 UTC [22460] STATEMENT:  CREATE OR REPLACE FUNCTION public.managed_create_user(p_email text, p_full_name text, p_role user_role, p_store_id uuid DEFAULT NULL::uuid, p_memberships jsonb DEFAULT NULL::jsonb, p_max_stores integer DEFAULT 0, p_max_users integer DEFAULT 0, p_target_user_id uuid DEFAULT NULL::uuid)
	 RETURNS jsonb
	 LANGUAGE plpgsql
	 SECURITY DEFINER
	 SET search_path TO 'public', 'extensions'
	AS $function$
	DECLARE
	    v_user_id uuid;
	    v_role_id uuid;
	    v_active_store_id uuid;
	    m JSONB;
	    v_creator_role user_role;
	BEGIN
	    SELECT role INTO v_creator_role FROM public.profiles WHERE id = auth.uid();
	    IF v_creator_role IS NULL OR v_creator_role NOT IN ('admin', 'encargado') THEN
	        RAISE EXCEPTION 'ERR_UNAUTHORIZED: Only admins and managers can create users.';
	    END IF;
	
	    v_active_store_id := COALESCE((p_memberships->0->>'store_id')::UUID, p_store_id);
	
	    IF v_creator_role = 'encargado' AND v_active_store_id IS NOT NULL THEN
	        IF NOT public.has_store_access(v_active_store_id) THEN
	            RAISE EXCEPTION 'ERR_UNAUTHORIZED: No access to store %', v_active_store_id;
	        END IF;
	    END IF;
	
	    SELECT id INTO v_role_id FROM public.roles WHERE lower(name) = lower(p_role::text) OR (name = 'Cajero' AND p_role = 'clerk') OR (name = 'Almacenero' AND p_role = 'warehouse') LIMIT 1;
	    v_user_id := COALESCE(p_target_user_id, gen_random_uuid());
	
	    INSERT INTO public.profiles (id, email, full_name, role, role_id, active_store_id, is_active, created_by, max_stores_limit, max_users_limit, created_at, updated_at)
	    VALUES (v_user_id, p_email, p_full_name, p_role, v_role_id, v_active_store_id, true, auth.uid(), p_max_stores, p_max_users, now(), now())
	    ON CONFLICT (id) DO UPDATE SET email = EXCLUDED.email, full_name = EXCLUDED.full_name, role = EXCLUDED.role, role_id = EXCLUDED.role_id, active_store_id = EXCLUDED.active_store_id, updated_at = now()
	    RETURNING id INTO v_user_id;
	
	    IF p_memberships IS NOT NULL THEN
	        IF v_creator_role != 'admin' THEN
	            DELETE FROM public.user_store_memberships WHERE user_id = v_user_id AND store_id IN (SELECT store_id FROM public.user_store_memberships WHERE user_id = auth.uid() AND role IN ('encargado', 'manager'));
	        ELSE
	            DELETE FROM public.user_store_memberships WHERE user_id = v_user_id;
	        END IF;
	        FOR m IN SELECT * FROM jsonb_array_elements(p_memberships) LOOP
	            IF (m->>'store_id') IS NOT NULL AND (m->>'store_id') <> '' THEN
	                IF v_creator_role = 'admin' OR public.has_store_access((m->>'store_id')::UUID) THEN
	                    INSERT INTO public.user_store_memberships (user_id, store_id, role) VALUES (v_user_id, (m->>'store_id')::UUID, (m->>'role')::user_role) ON CONFLICT (user_id, store_id) DO UPDATE SET role = EXCLUDED.role;
	                END IF;
	            END IF;
	        END LOOP;
	    ELSIF p_store_id IS NOT NULL THEN
	        INSERT INTO public.user_store_memberships (user_id, store_id, role) VALUES (v_user_id, p_store_id, p_role) ON CONFLICT (user_id, store_id) DO UPDATE SET role = EXCLUDED.role;
	    END IF;
	    RETURN jsonb_build_object('success', true, 'user_id', v_user_id);
	END;
	$function$
2026-09-13 06:16:38.047 UTC [22463] ERROR:  type public.user_role does not exist
2026-09-13 06:16:38.047 UTC [22463] STATEMENT:  CREATE OR REPLACE FUNCTION public.managed_create_user_v2(
	  p_email text,
	  p_full_name text,
	  p_role public.user_role,
	  p_plan plan_t DEFAULT 'free'::plan_t,
	  p_store_id uuid DEFAULT NULL::uuid,
	  p_memberships jsonb DEFAULT NULL::jsonb,
	  p_max_stores int DEFAULT 0,
	  p_max_users int DEFAULT 0,
	  p_target_user_id uuid DEFAULT NULL::uuid,
	  p_creator_id uuid DEFAULT NULL::uuid
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  v_user_id uuid := COALESCE(p_target_user_id, gen_random_uuid());
	  v_role_id uuid;
	  v_active_store_id uuid;
	  m JSONB;
	  v_creator_role public.user_role;
	  v_creator_uid uuid := COALESCE(p_creator_id, auth.uid());
	BEGIN
	  -- Validar caller es admin o encargado
	  SELECT role INTO v_creator_role FROM public.profiles WHERE id = v_creator_uid;
	  IF v_creator_role IS NULL OR v_creator_role NOT IN ('admin', 'encargado', 'superadmin') THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED: Only admins and managers can create users.';
	  END IF;
	
	  -- Validar email único (entre perfiles activos)
	  IF EXISTS (SELECT 1 FROM public.profiles WHERE email = p_email AND deleted_at IS NULL) THEN
	    RAISE EXCEPTION 'ERR_EMAIL_ALREADY_EXISTS: %', p_email;
	  END IF;
	
	  v_active_store_id := COALESCE((p_memberships->0->>'store_id')::UUID, p_store_id);
	
	  -- Encargado solo puede crear en tiendas que gestiona
	  IF v_creator_role = 'encargado' AND v_active_store_id IS NOT NULL THEN
	    IF NOT public.has_store_access(v_active_store_id) THEN
	      RAISE EXCEPTION 'ERR_UNAUTHORIZED: No access to store %', v_active_store_id;
	    END IF;
	  END IF;
	
	  -- Buscar role_id
	  SELECT id INTO v_role_id FROM public.roles
	    WHERE lower(name) = lower(p_role::text)
	       OR (name = 'Cajero' AND p_role = 'clerk')
	       OR (name = 'Almacenero' AND p_role = 'warehouse')
	       OR (name = 'UserCosto' AND p_role = 'costo')
	    LIMIT 1;
	
	  -- INSERT profiles con plan
	  INSERT INTO public.profiles (
	    id, email, full_name, role, role_id, active_store_id, is_active,
	    created_by, max_stores_limit, max_users_limit, plan, created_at, updated_at
	  ) VALUES (
	    v_user_id, p_email, p_full_name, p_role, v_role_id, v_active_store_id, true,
	    v_creator_uid, p_max_stores, p_max_users, p_plan, now(), now()
	  )
	  ON CONFLICT (id) DO UPDATE SET
	    email = EXCLUDED.email,
	    full_name = EXCLUDED.full_name,
	    role = EXCLUDED.role,
	    role_id = EXCLUDED.role_id,
	    active_store_id = EXCLUDED.active_store_id,
	    plan = EXCLUDED.plan,
	    is_active = true,
	    deleted_at = NULL,
	    deletion_reason = NULL,
	    deleted_by = NULL,
	    updated_at = now()
	  RETURNING id INTO v_user_id;
	
	  -- Procesar memberships
	  IF p_memberships IS NOT NULL THEN
	    IF v_creator_role != 'admin' AND v_creator_role != 'superadmin' THEN
	      DELETE FROM public.user_store_memberships
	        WHERE user_id = v_user_id
	        AND store_id IN (
	          SELECT store_id FROM public.user_store_memberships
	          WHERE user_id = v_creator_uid AND role IN ('encargado', 'manager')
	        );
	    ELSE
	      DELETE FROM public.user_store_memberships WHERE user_id = v_user_id;
	    END IF;
	
	    FOR m IN SELECT * FROM jsonb_array_elements(p_memberships) LOOP
	      IF (m->>'store_id') IS NOT NULL AND (m->>'store_id') <> '' THEN
	        IF v_creator_role IN ('admin', 'superadmin') OR public.has_store_access((m->>'store_id')::UUID) THEN
	          INSERT INTO public.user_store_memberships (user_id, store_id, role)
	          VALUES (v_user_id, (m->>'store_id')::UUID, (m->>'role')::public.user_role)
	          ON CONFLICT (user_id, store_id) DO UPDATE SET role = EXCLUDED.role, status = 'active';
	        END IF;
	      END IF;
	    END LOOP;
	  ELSIF p_store_id IS NOT NULL THEN
	    INSERT INTO public.user_store_memberships (user_id, store_id, role)
	    VALUES (v_user_id, p_store_id, p_role)
	    ON CONFLICT (user_id, store_id) DO UPDATE SET role = EXCLUDED.role, status = 'active';
	  END IF;
	
	  -- Audit log atómico (regresión C-2 restaurada)
	  INSERT INTO public.user_audit_log (performed_by, target_user_id, action, new_values, metadata)
	  VALUES (
	    v_creator_uid, v_user_id, 'USER_CREATED',
	    jsonb_build_object(
	      'email', p_email, 'full_name', p_full_name, 'role', p_role::text,
	      'plan', p_plan::text, 'active_store_id', v_active_store_id,
	      'max_stores', p_max_stores, 'max_users', p_max_users
	    ),
	    jsonb_build_object('memberships_count', CASE WHEN p_memberships IS NOT NULL THEN jsonb_array_length(p_memberships) ELSE 0 END)
	  );
	
	  RETURN jsonb_build_object('success', true, 'user_id', v_user_id);
	END;
	$function$
2026-09-13 06:16:38.084 UTC [22468] ERROR:  type "public.user_role" does not exist at character 392
2026-09-13 06:16:38.084 UTC [22468] STATEMENT:  CREATE OR REPLACE FUNCTION public.managed_reset_password(
	  p_user_id uuid,
	  p_caller_id uuid DEFAULT NULL::uuid
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  -- FIX H-7: anti-spoofing
	  v_caller_uid uuid := CASE WHEN auth.role() = 'service_role'
	    THEN COALESCE(p_caller_id, auth.uid()) ELSE auth.uid() END;
	  v_caller_role public.user_role;
	  v_target_email text;
	BEGIN
	  SELECT role INTO v_caller_role FROM public.profiles WHERE id = v_caller_uid;
	  IF v_caller_role IS NULL OR v_caller_role NOT IN ('admin', 'superadmin') THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED';
	  END IF;
	
	  IF p_user_id = v_caller_uid THEN
	    RAISE EXCEPTION 'ERR_SELF_RESET_BLOCKED';
	  END IF;
	
	  SELECT email INTO v_target_email FROM public.profiles WHERE id = p_user_id AND deleted_at IS NULL;
	  IF v_target_email IS NULL THEN
	    RAISE EXCEPTION 'ERR_USER_NOT_FOUND';
	  END IF;
	
	  INSERT INTO public.user_audit_log (performed_by, target_user_id, action, metadata)
	  VALUES (
	    v_caller_uid, p_user_id, 'PASSWORD_RESET_REQUESTED',
	    jsonb_build_object('email', v_target_email, 'method', 'recovery_link')
	  );
	
	  RETURN jsonb_build_object('success', true, 'email', v_target_email);
	END;
	$function$
2026-09-13 06:16:38.111 UTC [22472] ERROR:  type "public.user_role" does not exist at character 427
2026-09-13 06:16:38.111 UTC [22472] STATEMENT:  CREATE OR REPLACE FUNCTION public.managed_soft_delete_user(
	  p_user_id uuid,
	  p_reason text,
	  p_caller_id uuid DEFAULT NULL::uuid
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  v_old RECORD;
	  -- FIX H-7: anti-spoofing
	  v_caller_uid uuid := CASE WHEN auth.role() = 'service_role'
	    THEN COALESCE(p_caller_id, auth.uid()) ELSE auth.uid() END;
	  v_caller_role public.user_role;
	  v_active_memberships_count int;
	  v_anon_email text;
	BEGIN
	  SELECT role INTO v_caller_role FROM public.profiles WHERE id = v_caller_uid;
	  IF v_caller_role IS NULL OR v_caller_role NOT IN ('admin', 'superadmin') THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED';
	  END IF;
	
	  IF p_user_id = v_caller_uid THEN
	    RAISE EXCEPTION 'ERR_SELF_DELETE_BLOCKED';
	  END IF;
	
	  SELECT id, email, full_name, role, plan, is_active INTO v_old
	    FROM public.profiles WHERE id = p_user_id AND deleted_at IS NULL FOR UPDATE;
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_USER_NOT_FOUND_OR_ALREADY_DELETED';
	  END IF;
	
	  SELECT COUNT(*) INTO v_active_memberships_count
	    FROM public.user_store_memberships
	    WHERE user_id = p_user_id AND status = 'active';
	  IF v_active_memberships_count > 0 THEN
	    RAISE EXCEPTION 'ERR_USER_HAS_ACTIVE_MEMBERSHIPS: % active. Revoke memberships first.', v_active_memberships_count;
	  END IF;
	
	  v_anon_email := 'deleted+' || substr(p_user_id::text, 1, 8) || '@anonymized.local';
	
	  UPDATE public.profiles SET
	    deleted_at = now(),
	    deletion_reason = p_reason,
	    deleted_by = v_caller_uid,
	    is_active = false,
	    full_name = '[deleted user]',
	    email = v_anon_email,
	    ai_api_key = NULL,
	    updated_at = now()
	  WHERE id = p_user_id;
	
	  UPDATE public.user_store_memberships SET
	    status = 'revoked',
	    updated_at = now()
	  WHERE user_id = p_user_id AND status = 'active';
	
	  INSERT INTO public.user_audit_log (performed_by, target_user_id, action, old_values, new_values, metadata)
	  VALUES (
	    v_caller_uid, p_user_id, 'USER_SOFT_DELETED',
	    jsonb_build_object(
	      'email', v_old.email, 'full_name', v_old.full_name,
	      'role', v_old.role::text, 'plan', v_old.plan::text, 'is_active', v_old.is_active
	    ),
	    jsonb_build_object(
	      'email', v_anon_email, 'full_name', '[deleted user]',
	      'is_active', false, 'deleted_at', now()
	    ),
	    jsonb_build_object('reason', p_reason, 'memberships_revoked', v_active_memberships_count)
	  );
	
	  RETURN jsonb_build_object(
	    'success', true,
	    'user_id', p_user_id,
	    'status', 'soft_deleted',
	    'note', 'auth.users preserved. API route should ban via auth.admin.updateUser.'
	  );
	END;
	$function$
2026-09-13 06:16:38.143 UTC [22476] ERROR:  type "public.user_role" does not exist at character 435
2026-09-13 06:16:38.143 UTC [22476] STATEMENT:  CREATE OR REPLACE FUNCTION public.managed_toggle_user_status(
	  p_user_id uuid,
	  p_is_active boolean,
	  p_caller_id uuid DEFAULT NULL::uuid
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  v_old RECORD;
	  -- FIX H-7: anti-spoofing
	  v_caller_uid uuid := CASE WHEN auth.role() = 'service_role'
	    THEN COALESCE(p_caller_id, auth.uid()) ELSE auth.uid() END;
	  v_caller_role public.user_role;
	BEGIN
	  SELECT role INTO v_caller_role FROM public.profiles WHERE id = v_caller_uid;
	  IF v_caller_role IS NULL OR v_caller_role NOT IN ('admin', 'superadmin', 'encargado', 'manager') THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED';
	  END IF;
	
	  IF p_is_active = false AND p_user_id = v_caller_uid THEN
	    RAISE EXCEPTION 'ERR_SELF_DEACTIVATE_BLOCKED';
	  END IF;
	
	  SELECT id, is_active, full_name INTO v_old
	    FROM public.profiles WHERE id = p_user_id AND deleted_at IS NULL FOR UPDATE;
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_USER_NOT_FOUND';
	  END IF;
	
	  IF v_old.is_active = p_is_active THEN
	    RETURN jsonb_build_object('success', true, 'no_change', true);
	  END IF;
	
	  UPDATE public.profiles SET is_active = p_is_active, updated_at = now() WHERE id = p_user_id;
	
	  INSERT INTO public.user_audit_log (performed_by, target_user_id, action, old_values, new_values)
	  VALUES (
	    v_caller_uid, p_user_id,
	    CASE WHEN p_is_active THEN 'USER_ACTIVATED' ELSE 'USER_DEACTIVATED' END,
	    jsonb_build_object('is_active', v_old.is_active),
	    jsonb_build_object('is_active', p_is_active)
	  );
	
	  RETURN jsonb_build_object('success', true, 'user_id', p_user_id, 'is_active', p_is_active);
	END;
	$function$
2026-09-13 06:16:38.178 UTC [22479] ERROR:  type public.user_role does not exist
2026-09-13 06:16:38.178 UTC [22479] STATEMENT:  CREATE OR REPLACE FUNCTION public.managed_update_membership(
	  p_membership_id uuid,
	  p_role public.user_role DEFAULT NULL::user_role,
	  p_status text DEFAULT NULL::text,
	  p_caller_id uuid DEFAULT NULL::uuid
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  v_old RECORD;
	  -- FIX H-7: anti-spoofing
	  v_caller_uid uuid := CASE WHEN auth.role() = 'service_role'
	    THEN COALESCE(p_caller_id, auth.uid()) ELSE auth.uid() END;
	  v_changes jsonb := '{}'::jsonb;
	BEGIN
	  SELECT m.id, m.user_id, m.store_id, m.role, m.status
	    INTO v_old
	    FROM public.user_store_memberships m
	    WHERE m.id = p_membership_id
	    FOR UPDATE;
	
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_MEMBERSHIP_NOT_FOUND';
	  END IF;
	
	  -- FIX H-7: Autorización usa nueva sobrecarga has_store_role(p_user_id, p_store_id, p_roles)
	  -- service_role (API route) pasa p_caller_id=session.user.id → verifica acceso de ese user
	  -- authenticated pasa p_caller_id=NULL → v_caller_uid=auth.uid() → verifica su propio acceso
	  IF NOT public.has_store_role(v_caller_uid, v_old.store_id, ARRAY['admin', 'manager']) THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED: Caller must be admin or manager of the store.';
	  END IF;
	
	  IF p_role IS NOT NULL AND p_role <> v_old.role THEN
	    v_changes := v_changes || jsonb_build_object('role', jsonb_build_object('old', v_old.role::text, 'new', p_role::text));
	    UPDATE public.user_store_memberships SET role = p_role, updated_at = now() WHERE id = p_membership_id;
	  END IF;
	
	  IF p_status IS NOT NULL AND p_status <> v_old.status::text THEN
	    IF p_status NOT IN ('active', 'revoked') THEN
	      RAISE EXCEPTION 'ERR_INVALID_STATUS: %', p_status;
	    END IF;
	    v_changes := v_changes || jsonb_build_object('status', jsonb_build_object('old', v_old.status, 'new', p_status));
	    UPDATE public.user_store_memberships SET status = p_status::membership_status, updated_at = now() WHERE id = p_membership_id;
	  END IF;
	
	  IF v_changes <> '{}'::jsonb THEN
	    INSERT INTO public.user_audit_log (performed_by, target_user_id, action, old_values, new_values, metadata)
	    VALUES (
	      v_caller_uid, v_old.user_id, 'MEMBERSHIP_UPDATED',
	      jsonb_build_object('membership_id', p_membership_id, 'store_id', v_old.store_id, 'role', v_old.role::text, 'status', v_old.status),
	      v_changes,
	      jsonb_build_object('store_id', v_old.store_id)
	    );
	  END IF;
	
	  RETURN jsonb_build_object('success', true, 'membership_id', p_membership_id, 'changes', v_changes);
	END;
	$function$
2026-09-13 06:16:38.196 UTC [22482] ERROR:  type plan_t does not exist
2026-09-13 06:16:38.196 UTC [22482] STATEMENT:  CREATE OR REPLACE FUNCTION public.managed_update_tenant_plan(
	  p_tenant_id uuid,
	  p_plan plan_t,
	  p_subscription_status text DEFAULT NULL,
	  p_caller_id uuid DEFAULT NULL
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  -- FIX H-7: anti-spoofing — service_role (Stripe webhook) puede pasar NULL caller_id
	  v_caller_uid uuid := CASE WHEN auth.role() = 'service_role'
	    THEN COALESCE(p_caller_id, auth.uid()) ELSE auth.uid() END;
	  v_old_plan plan_t;
	  v_old_status text;
	  v_role public.user_role;
	BEGIN
	  -- Validación: si caller_uid es NULL (service_role anónimo para Stripe webhook), permitir
	  -- Si caller_uid NO es NULL, debe ser admin/superadmin
	  IF v_caller_uid IS NOT NULL THEN
	    SELECT role INTO v_role FROM public.profiles WHERE id = v_caller_uid;
	    IF v_role IS NULL OR v_role NOT IN ('admin', 'superadmin') THEN
	      RAISE EXCEPTION 'ERR_UNAUTHORIZED: Only admins can change tenant plan';
	    END IF;
	  END IF;
	
	  SELECT plan, subscription_status INTO v_old_plan, v_old_status
	    FROM public.tenants WHERE id = p_tenant_id FOR UPDATE;
	
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_TENANT_NOT_FOUND: %', p_tenant_id;
	  END IF;
	
	  UPDATE public.tenants SET
	    plan = p_plan,
	    subscription_status = COALESCE(p_subscription_status, subscription_status),
	    updated_at = now()
	  WHERE id = p_tenant_id;
	
	  UPDATE public.profiles SET
	    plan = p_plan,
	    updated_at = now()
	  WHERE tenant_id = p_tenant_id AND deleted_at IS NULL;
	
	  INSERT INTO public.user_audit_log (performed_by, target_user_id, action, old_values, new_values, metadata)
	  VALUES (
	    v_caller_uid, NULL, 'TENANT_PLAN_UPDATED',
	    jsonb_build_object('old_plan', v_old_plan::text, 'old_status', v_old_status),
	    jsonb_build_object('new_plan', p_plan::text, 'new_status', COALESCE(p_subscription_status, v_old_status)),
	    jsonb_build_object('tenant_id', p_tenant_id)
	  );
	
	  RETURN jsonb_build_object(
	    'success', true,
	    'tenant_id', p_tenant_id,
	    'old_plan', v_old_plan::text,
	    'new_plan', p_plan::text
	  );
	END;
	$function$
2026-09-13 06:16:38.217 UTC [22485] ERROR:  type public.user_role does not exist
2026-09-13 06:16:38.217 UTC [22485] STATEMENT:  CREATE OR REPLACE FUNCTION public.managed_update_user(
	  p_user_id uuid,
	  p_full_name text DEFAULT NULL::text,
	  p_role public.user_role DEFAULT NULL::user_role,
	  p_role_id uuid DEFAULT NULL::uuid,
	  p_is_active boolean DEFAULT NULL::boolean,
	  p_max_stores_limit int DEFAULT NULL::int,
	  p_max_users_limit int DEFAULT NULL::int,
	  p_plan plan_t DEFAULT NULL::plan_t,
	  p_caller_id uuid DEFAULT NULL::uuid
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  v_old RECORD;
	  -- FIX H-7: anti-spoofing — service_role puede pasar p_caller_id explícito
	  v_caller_uid uuid := CASE WHEN auth.role() = 'service_role'
	    THEN COALESCE(p_caller_id, auth.uid()) ELSE auth.uid() END;
	  v_caller_role public.user_role;
	  v_changes jsonb := '{}'::jsonb;
	BEGIN
	  -- Validar caller es admin (usa v_caller_uid que ahora NO es spoofable para authenticated)
	  SELECT role INTO v_caller_role FROM public.profiles WHERE id = v_caller_uid;
	  IF v_caller_role IS NULL OR v_caller_role NOT IN ('admin', 'superadmin') THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED: Only admins can update users.';
	  END IF;
	
	  SELECT * INTO v_old FROM public.profiles WHERE id = p_user_id AND deleted_at IS NULL FOR UPDATE;
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_USER_NOT_FOUND: %', p_user_id;
	  END IF;
	
	  IF p_is_active = false AND p_user_id = v_caller_uid THEN
	    RAISE EXCEPTION 'ERR_SELF_DEACTIVATE_BLOCKED: Cannot deactivate own account.';
	  END IF;
	
	  IF p_full_name IS NOT NULL AND p_full_name <> v_old.full_name THEN
	    v_changes := v_changes || jsonb_build_object('full_name', jsonb_build_object('old', v_old.full_name, 'new', p_full_name));
	    UPDATE public.profiles SET full_name = p_full_name, updated_at = now() WHERE id = p_user_id;
	  END IF;
	
	  IF p_role IS NOT NULL AND p_role <> v_old.role THEN
	    v_changes := v_changes || jsonb_build_object('role', jsonb_build_object('old', v_old.role::text, 'new', p_role::text));
	    UPDATE public.profiles SET role = p_role, updated_at = now() WHERE id = p_user_id;
	  END IF;
	
	  IF p_role_id IS NOT NULL AND (v_old.role_id IS NULL OR p_role_id <> v_old.role_id) THEN
	    v_changes := v_changes || jsonb_build_object('role_id', jsonb_build_object('old', v_old.role_id, 'new', p_role_id));
	    UPDATE public.profiles SET role_id = p_role_id, updated_at = now() WHERE id = p_user_id;
	  END IF;
	
	  IF p_is_active IS NOT NULL AND p_is_active <> v_old.is_active THEN
	    v_changes := v_changes || jsonb_build_object('is_active', jsonb_build_object('old', v_old.is_active, 'new', p_is_active));
	    UPDATE public.profiles SET is_active = p_is_active, updated_at = now() WHERE id = p_user_id;
	  END IF;
	
	  IF p_max_stores_limit IS NOT NULL AND (v_old.max_stores_limit IS NULL OR p_max_stores_limit <> v_old.max_stores_limit) THEN
	    v_changes := v_changes || jsonb_build_object('max_stores_limit', jsonb_build_object('old', v_old.max_stores_limit, 'new', p_max_stores_limit));
	    UPDATE public.profiles SET max_stores_limit = p_max_stores_limit, updated_at = now() WHERE id = p_user_id;
	  END IF;
	
	  IF p_max_users_limit IS NOT NULL AND (v_old.max_users_limit IS NULL OR p_max_users_limit <> v_old.max_users_limit) THEN
	    v_changes := v_changes || jsonb_build_object('max_users_limit', jsonb_build_object('old', v_old.max_users_limit, 'new', p_max_users_limit));
	    UPDATE public.profiles SET max_users_limit = p_max_users_limit, updated_at = now() WHERE id = p_user_id;
	  END IF;
	
	  IF p_plan IS NOT NULL AND p_plan <> v_old.plan THEN
	    v_changes := v_changes || jsonb_build_object('plan', jsonb_build_object('old', v_old.plan::text, 'new', p_plan::text));
	    UPDATE public.profiles SET plan = p_plan, updated_at = now() WHERE id = p_user_id;
	  END IF;
	
	  IF v_changes <> '{}'::jsonb THEN
	    INSERT INTO public.user_audit_log (performed_by, target_user_id, action, old_values, new_values, metadata)
	    VALUES (
	      v_caller_uid, p_user_id, 'USER_UPDATED',
	      jsonb_build_object(
	        'full_name', v_old.full_name, 'role', v_old.role::text,
	        'is_active', v_old.is_active, 'plan', v_old.plan::text
	      ),
	      v_changes,
	      jsonb_build_object('fields_changed', jsonb_object_keys(v_changes))
	    );
	  END IF;
	
	  RETURN jsonb_build_object('success', true, 'user_id', p_user_id, 'changes', v_changes);
	END;
	$function$
2026-09-13 06:16:38.247 UTC [22490] ERROR:  type "user_role" does not exist at character 204
2026-09-13 06:16:38.247 UTC [22490] STATEMENT:  CREATE OR REPLACE FUNCTION public.on_auth_user_created()
	RETURNS trigger
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO 'public', 'extensions'
	AS $function$
	DECLARE
	  v_role_id UUID;
	  v_role_enum USER_ROLE;
	  v_metadata_role TEXT;
	  v_company_name TEXT;
	  v_full_name TEXT;
	  v_tenant_id UUID;
	BEGIN
	  -- Extract role from metadata if present
	  v_metadata_role := NEW.raw_user_meta_data->>'role';
	
	  -- Normalize and convert to enum
	  IF v_metadata_role IS NOT NULL THEN
	    BEGIN
	      v_role_enum := v_metadata_role::USER_ROLE;
	    EXCEPTION WHEN OTHERS THEN
	      v_role_enum := 'tenant_admin'::USER_ROLE;
	    END;
	  ELSE
	    -- Iteración 13: self-signup users are tenant_admin by default
	    v_role_enum := 'tenant_admin'::USER_ROLE;
	  END IF;
	
	  -- Extract company_name and full_name from metadata
	  v_company_name := NEW.raw_user_meta_data->>'company_name';
	  v_full_name := COALESCE(NEW.raw_user_meta_data->>'full_name', split_part(NEW.email, '@', 1));
	
	  -- Iteración 13: Create tenant for self-signup users
	  -- If user was created by admin (via managed_create_user_v2), they may already
	  -- have a tenant_id set via p_target_user_id. In that case, skip tenant creation.
	  IF v_company_name IS NOT NULL THEN
	    INSERT INTO public.tenants (name, owner_id, plan, subscription_status, trial_ends_at, is_active)
	    VALUES (
	      v_company_name,
	      NEW.id,
	      'free'::plan_t,
	      'trial',
	      now() + interval '14 days',
	      true
	    )
	    RETURNING id INTO v_tenant_id;
	  END IF;
	
	  -- Get matching role_id from public.roles
	  SELECT id INTO v_role_id FROM public.roles
	    WHERE name = v_metadata_role OR lower(name) = lower(v_role_enum::text)
	    LIMIT 1;
	
	  -- Insert profile with tenant_id
	  INSERT INTO public.profiles (id, email, full_name, role, role_id, is_active, tenant_id, plan, created_at, updated_at)
	  VALUES (
	    NEW.id,
	    NEW.email,
	    v_full_name,
	    v_role_enum,
	    v_role_id,
	    true,
	    v_tenant_id,
	    'free'::plan_t,
	    now(),
	    now()
	  )
	  ON CONFLICT (id) DO UPDATE SET
	    email = EXCLUDED.email,
	    full_name = EXCLUDED.full_name,
	    role = EXCLUDED.role,
	    role_id = EXCLUDED.role_id,
	    tenant_id = COALESCE(profiles.tenant_id, EXCLUDED.tenant_id),
	    plan = EXCLUDED.plan,
	    is_active = true,
	    updated_at = now();
	
	  -- Audit log
	  INSERT INTO public.user_audit_log (performed_by, target_user_id, action, new_values, metadata)
	  VALUES (
	    NEW.id, NEW.id, 'USER_REGISTERED_PUBLICLY',
	    jsonb_build_object('email', NEW.email, 'role', v_role_enum::text, 'tenant_id', v_tenant_id),
	    jsonb_build_object('company_name', v_company_name, 'source', 'self_signup')
	  );
	
	  RETURN NEW;
	END;
	$function$
2026-09-13 06:16:38.358 UTC [22503] ERROR:  type adjustment_item[] does not exist
2026-09-13 06:16:38.358 UTC [22503] STATEMENT:  CREATE OR REPLACE FUNCTION public.process_inventory_adjustment(p_store_id uuid, p_cashier_id uuid, p_items adjustment_item[], p_operation_date timestamp with time zone DEFAULT NULL::timestamp with time zone)
	 RETURNS uuid
	 LANGUAGE plpgsql
	 SECURITY DEFINER
	 SET search_path TO 'public'
	AS $function$
	DECLARE
	  v_adjustment_id UUID;
	  v_item public.adjustment_item;
	  v_difference NUMERIC;
	  v_effective_date TIMESTAMP WITH TIME ZONE := COALESCE(p_operation_date, NOW());
	BEGIN
	  -- Validación forward-only locking
	  PERFORM public.validate_operation_date(p_operation_date, p_store_id);
	
	  INSERT INTO public.inventory_adjustments (store_id, created_by, status, created_at)
	  VALUES (p_store_id, p_cashier_id, 'PROCESSING', v_effective_date)
	  RETURNING id INTO v_adjustment_id;
	
	  FOREACH v_item IN ARRAY p_items
	  LOOP
	    v_difference := v_item.counted_quantity - v_item.expected_quantity;
	    INSERT INTO public.inventory_adjustment_items (adjustment_id, product_id, expected_quantity, counted_quantity, created_at)
	    VALUES (v_adjustment_id, v_item.product_id, v_item.expected_quantity, v_item.counted_quantity, v_effective_date);
	
	    PERFORM public.register_stock_movement(
	        p_product_id := v_item.product_id,
	        p_store_id := p_store_id,
	        p_user_id := p_cashier_id,
	        p_quantity := v_difference,
	        p_movement_type := 'adjustment',
	        p_operation_date := v_effective_date
	    );
	  END LOOP;
	
	  UPDATE public.inventory_adjustments SET status = 'COMPLETED', updated_at = v_effective_date
	  WHERE id = v_adjustment_id;
	  RETURN v_adjustment_id;
	END;
	$function$
2026-09-13 06:16:38.399 UTC [22510] ERROR:  type "public.purchase_status_enum" does not exist at character 425
2026-09-13 06:16:38.399 UTC [22510] STATEMENT:  CREATE OR REPLACE FUNCTION public.receive_against_po(
	  p_po_id uuid,
	  p_received_items jsonb DEFAULT '[]'::jsonb,
	  p_user_id uuid DEFAULT NULL,
	  p_reception_date timestamptz DEFAULT now(),
	  p_invoice_number text DEFAULT NULL
	) RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO 'public', 'extensions'
	AS $func$
	DECLARE
	  v_store_id            uuid;
	  v_supplier_name       text;
	  v_po_status           public.purchase_status_enum;
	  v_item                jsonb;
	  v_item_id             uuid;
	  v_qty                 numeric;
	  v_qty_ordered         numeric;
	  v_qty_received_before numeric;
	  v_product_id          uuid;
	  v_unit_cost           numeric;
	  v_product_name        text;
	  v_all_received        boolean;
	  v_any_received        boolean;
	  v_new_status          public.purchase_status_enum;
	  v_receipt_id          uuid;
	  v_reception_items     jsonb := '[]'::jsonb;
	  v_count               integer := 0;
	  v_po_number           text;
	  v_invoice_to_pass     text;
	  v_sorted_items        jsonb;
	BEGIN
	  -- ─── 1. Cargar OC con lock exclusivo ───
	  SELECT po.store_id, po.supplier_name, po.status, po.po_number
	    INTO v_store_id, v_supplier_name, v_po_status, v_po_number
	  FROM public.purchase_orders po
	  WHERE po.id = p_po_id
	  FOR UPDATE;
	
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_PO_NOT_FOUND';
	  END IF;
	
	  -- ─── 2. Validar acceso (tenant-aware via has_store_access) ───
	  IF NOT public.has_store_access(v_store_id) THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED';
	  END IF;
	
	  -- ─── 3. Validar estado de la OC ───
	  IF v_po_status = 'cancelled' THEN
	    RAISE EXCEPTION 'ERR_PO_CANCELLED';
	  END IF;
	  -- Solo se puede recibir contra draft, sent o partial.
	  IF v_po_status NOT IN ('draft', 'sent', 'partial') THEN
	    RAISE EXCEPTION 'ERR_PO_NOT_RECEIVABLE: status % is terminal', v_po_status;
	  END IF;
	
	  -- ─── 4. Validar items no vacío (parity B3 register_reception) ───
	  IF p_received_items IS NULL OR jsonb_array_length(p_received_items) = 0 THEN
	    RAISE EXCEPTION 'ERR_EMPTY_ITEMS';
	  END IF;
	
	  -- ─── 5. Condición #5 (anti-deadlock): Ordenar items por po_item_id ASC ───
	  SELECT jsonb_agg(elem ORDER BY (elem->>'po_item_id'))
	    INTO v_sorted_items
	  FROM jsonb_array_elements(p_received_items) AS elem;
	
	  -- ─── 6. Procesar cada item: VALIDAR antes de actualizar ───
	  FOR v_item IN SELECT * FROM jsonb_array_elements(v_sorted_items) LOOP
	    v_item_id := NULLIF(v_item->>'po_item_id', '')::uuid;
	    v_qty     := COALESCE((v_item->>'quantity_received')::numeric, 0);
	
	    -- Validar po_item_id presente
	    IF v_item_id IS NULL THEN
	      RAISE EXCEPTION 'ERR_ITEM_ID_REQUIRED: missing po_item_id in received_items';
	    END IF;
	
	    -- Medio #6: qty > 0
	    IF v_qty <= 0 THEN
	      RAISE EXCEPTION 'ERR_NEGATIVE_QTY: qty % for item % must be > 0', v_qty, v_item_id;
	    END IF;
	
	    -- Cargar estado actual del item con lock FOR UPDATE
	    SELECT product_id, quantity_ordered, quantity_received, unit_cost, product_name
	      INTO v_product_id, v_qty_ordered, v_qty_received_before, v_unit_cost, v_product_name
	    FROM public.purchase_order_items
	    WHERE id = v_item_id AND po_id = p_po_id
	    FOR UPDATE;
	
	    IF NOT FOUND THEN
	      RAISE EXCEPTION 'ERR_ITEM_NOT_FOUND: %', v_item_id;
	    END IF;
	
	    -- Alto #5: over-receive check
	    IF (v_qty_received_before + v_qty) > v_qty_ordered THEN
	      RAISE EXCEPTION 'ERR_OVER_RECEIVE: item % ordered %, already received %, attempting %',
	        v_item_id, v_qty_ordered, v_qty_received_before, v_qty;
	    END IF;
	
	    -- Condición #1: todo item debe tener product_id NOT NULL
	    IF v_product_id IS NULL THEN
	      RAISE EXCEPTION 'ERR_PRODUCT_ID_REQUIRED: item % has no product_id', v_item_id;
	    END IF;
	
	    -- Medio #9: producto debe existir en store
	    IF NOT EXISTS (
	      SELECT 1 FROM public.products p
	      WHERE p.id = v_product_id AND p.store_id = v_store_id
	    ) THEN
	      RAISE EXCEPTION 'ERR_PRODUCT_NOT_IN_STORE: product % not in store %',
	        v_product_id, v_store_id;
	    END IF;
	
	    -- Acumular para register_reception (formato esperado: product_id, quantity, unit_cost, moneda, tasa)
	    v_reception_items := v_reception_items || jsonb_build_object(
	      'product_id',   v_product_id,
	      'product_name', v_product_name,
	      'quantity',     v_qty,
	      'unit_cost',    v_unit_cost,
	      'moneda_recepcion', 'CUP',
	      'tasa_cambio_recepcion', 1.0
	    );
	    v_count := v_count + 1;
	  END LOOP;
	
	  -- ─── 7. Condición #3: reference_doc NULL si no viene invoice_number ───
	  -- Evita colisión con UNIQUE INDEX idx_receipts_store_reference_doc
	  -- (store_id, reference_doc) en partial indexes.
	  -- El vínculo real receipt↔PO es receipts.po_id (G6).
	  IF p_invoice_number IS NULL OR p_invoice_number = '' THEN
	    v_invoice_to_pass := NULL;
	  ELSE
	    v_invoice_to_pass := p_invoice_number;
	  END IF;
	
	  -- ─── 8. Crear receipt vinculado vía register_reception ───
	  -- register_reception aplica las 8 validaciones B1-B5 + C1-C3 del v2.23.0
	  -- (no se relajan). G6 añade p_po_id opcional.
	  v_receipt_id := public.register_reception(
	    p_store_id        := v_store_id,
	    p_supplier        := v_supplier_name,
	    p_reception_date  := p_reception_date,
	    p_invoice_number  := v_invoice_to_pass,
	    p_items           := v_reception_items,
	    p_user_id         := p_user_id,
	    p_po_id           := p_po_id
	  );
	
	  -- ─── 9. Actualizar quantity_received en purchase_order_items ───
	  -- (SECURITY DEFINER bypasses RLS DENY UPDATE de G5)
	  FOR v_item IN SELECT * FROM jsonb_array_elements(v_sorted_items) LOOP
	    v_item_id := NULLIF(v_item->>'po_item_id', '')::uuid;
	    v_qty     := (v_item->>'quantity_received')::numeric;
	
	    UPDATE public.purchase_order_items
	    SET quantity_received = quantity_received + v_qty
	    WHERE id = v_item_id AND po_id = p_po_id;
	  END LOOP;
	
	  -- ─── 10. Recalcular status global (cast EXPLÍCITO a enum) ───
	  SELECT
	    BOOL_AND(quantity_received >= quantity_ordered),
	    BOOL_OR(quantity_received > 0)
	  INTO v_all_received, v_any_received
	  FROM public.purchase_order_items WHERE po_id = p_po_id;
	
	  v_new_status :=
	    CASE
	      WHEN v_all_received THEN 'received'::public.purchase_status_enum
	      WHEN v_any_received THEN 'partial'::public.purchase_status_enum
	      ELSE 'sent'::public.purchase_status_enum
	    END;
	
	  -- ─── 11. Actualizar OC + vincular receipt en notas ───
	  UPDATE public.purchase_orders
	  SET status       = v_new_status,
	      received_at  = CASE WHEN v_new_status = 'received' THEN NOW() ELSE received_at END,
	      notes        = COALESCE(notes, '') ||
	                     CASE WHEN COALESCE(notes, '') = '' THEN '' ELSE E'\n' END ||
	                     '[Receipt ' || v_receipt_id::text || ' linked at ' || NOW()::text || ']'
	  WHERE id = p_po_id;
	
	  -- ─── 12. CxP (Grupo 6): marcar receipt con payment_status='unpaid' ───
	  -- Solo si aún no está seteado (no sobrescribir si register_reception ya lo puso).
	  UPDATE public.receipts
	  SET payment_status     = COALESCE(NULLIF(payment_status, ''), 'unpaid'),
	      payment_terms_days = COALESCE(payment_terms_days, 30),
	      due_date           = COALESCE(due_date, (p_reception_date::date + INTERVAL '30 days')::date)
	  WHERE id = v_receipt_id;
	
	  -- ─── 13. Auditoría ───
	  INSERT INTO public.audit_logs (user_id, store_id, action, table_name, record_id, metadata)
	  VALUES (
	    p_user_id, v_store_id, 'po_received', 'purchase_orders', p_po_id,
	    jsonb_build_object(
	      'new_status', v_new_status::text,
	      'items_received', v_count,
	      'receipt_id', v_receipt_id,
	      'po_number', v_po_number,
	      'invoice_number', v_invoice_to_pass
	    )
	  );
	
	  RETURN jsonb_build_object(
	    'status', 'success',
	    'po_status', v_new_status::text,
	    'receipt_id', v_receipt_id,
	    'items_received', v_count
	  );
	END;
	$func$
2026-09-13 06:16:38.571 UTC [22546] ERROR:  type purchase_status_enum does not exist
2026-09-13 06:16:38.571 UTC [22546] STATEMENT:  CREATE OR REPLACE FUNCTION public.set_purchase_order_status(p_po_id uuid, p_new_status purchase_status_enum, p_user_id uuid DEFAULT NULL::uuid, p_reason text DEFAULT NULL::text)
	 RETURNS jsonb
	 LANGUAGE plpgsql
	 SECURITY DEFINER
	 SET search_path TO 'public', 'extensions'
	AS $function$
	DECLARE
	  v_store_id     uuid;
	  v_current      public.purchase_status_enum;
	  v_po_number    text;
	  v_allowed      text[];
	  v_is_allowed   boolean := false;
	BEGIN
	  -- ─── 1. Cargar PO con lock exclusivo ───
	  SELECT store_id, status, po_number
	    INTO v_store_id, v_current, v_po_number
	  FROM public.purchase_orders
	  WHERE id = p_po_id
	  FOR UPDATE;
	
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_PO_NOT_FOUND';
	  END IF;
	
	  -- ─── 2. Validar acceso (tenant-aware) ───
	  IF NOT public.has_store_access(v_store_id) THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED';
	  END IF;
	
	  -- ─── 3. Si no hay cambio, retornar success sin hacer nada ───
	  IF v_current = p_new_status THEN
	    RETURN jsonb_build_object(
	      'status', 'no_change',
	      'po_status', v_current::text,
	      'po_number', v_po_number
	    );
	  END IF;
	
	  -- ─── 4. Definir transiciones permitidas (state machine) ───
	  -- 'partial' y 'received' NUNCA son destino válido manualmente
	  -- (solo vía receive_against_po).
	  v_allowed := CASE v_current
	    WHEN 'draft'   THEN ARRAY['sent', 'cancelled']
	    WHEN 'sent'    THEN ARRAY['cancelled']
	    WHEN 'partial' THEN ARRAY['cancelled']
	    ELSE ARRAY[]::text[]  -- received, cancelled: terminal, no transitions
	  END;
	
	  -- ─── 5. Verificar transición permitida ───
	  SELECT EXISTS(SELECT 1 FROM unnest(v_allowed) a WHERE a = p_new_status::text)
	    INTO v_is_allowed;
	
	  IF NOT v_is_allowed THEN
	    RAISE EXCEPTION 'ERR_INVALID_TRANSITION: % → % not allowed (allowed: %)',
	      v_current::text, p_new_status::text,
	      CASE WHEN array_length(v_allowed, 1) IS NULL THEN 'none' ELSE array_to_string(v_allowed, ', ') END;
	  END IF;
	
	  -- ─── 6. Aplicar transición ───
	  UPDATE public.purchase_orders
	  SET status      = p_new_status,
	      received_at = CASE WHEN p_new_status = 'received' THEN NOW() ELSE received_at END
	  WHERE id = p_po_id;
	
	  -- ─── 7. Auditoría (action = PO_STATUS_CHANGED) ───
	  INSERT INTO public.audit_logs (user_id, store_id, action, table_name, record_id, metadata)
	  VALUES (
	    CASE WHEN auth.role() = 'service_role' THEN COALESCE(p_user_id, auth.uid()) ELSE auth.uid() END, v_store_id, 'PO_STATUS_CHANGED', 'purchase_orders', p_po_id,
	    jsonb_build_object(
	      'po_number', v_po_number,
	      'from_status', v_current::text,
	      'to_status', p_new_status::text,
	      'reason', p_reason
	    )
	  );
	
	  RETURN jsonb_build_object(
	    'status', 'success',
	    'po_status', p_new_status::text,
	    'po_number', v_po_number,
	    'previous_status', v_current::text
	  );
	END;
	$function$
2026-09-13 06:16:38.679 UTC [22570] ERROR:  type "user_role" does not exist at character 100
2026-09-13 06:16:38.679 UTC [22570] STATEMENT:  CREATE OR REPLACE FUNCTION public.validate_active_store()
	RETURNS TRIGGER AS $$
	DECLARE
	    v_role user_role;
	BEGIN
	    v_role := NEW.role;
	
	    -- Si role es 'costo' o 'admin' o 'superadmin', active_store_id es opcional
	    IF v_role = 'costo'::user_role
	       OR v_role = 'admin'::user_role
	       OR v_role = 'superadmin'::user_role THEN
	        -- Solo validar si active_store_id cambió o es INSERT
	        IF TG_OP = 'INSERT' OR (OLD.active_store_id IS DISTINCT FROM NEW.active_store_id) THEN
	            IF NEW.active_store_id IS NOT NULL THEN
	                IF NOT EXISTS (
	                    SELECT 1 FROM public.user_store_memberships
	                    WHERE user_id = NEW.id
	                      AND store_id = NEW.active_store_id
	                      AND status = 'active'
	                ) THEN
	                    IF TG_OP = 'UPDATE' THEN
	                        RAISE EXCEPTION 'ERR_INVALID_ACTIVE_STORE: El usuario no tiene membership activa en la tienda seleccionada.';
	                    END IF;
	                END IF;
	            END IF;
	        END IF;
	        RETURN NEW;
	    END IF;
	
	    -- Para roles operativos (encargado, clerk, warehouse, manager, usuario)
	    IF TG_OP = 'INSERT' THEN
	        -- En INSERT: ser permisivo. La función managed_create_user inserta
	        -- memberships justo después. No validar nada aquí.
	        RETURN NEW;
	    ELSE
	        -- UPDATE: solo validar si active_store_id cambió realmente
	        -- (evita bloquear updates de otros campos en perfiles intermedios)
	        IF OLD.active_store_id IS NOT DISTINCT FROM NEW.active_store_id THEN
	            -- active_store_id no cambió → permitir el UPDATE sin validar
	            RETURN NEW;
	        END IF;
	
	        -- active_store_id cambió → validar consistencia
	        IF NEW.active_store_id IS NULL THEN
	            IF v_role IN ('encargado'::user_role, 'clerk'::user_role, 'warehouse'::user_role) THEN
	                RAISE EXCEPTION 'ERR_STORE_REQUIRED: El rol % requiere una tienda activa asignada.', v_role;
	            END IF;
	        ELSE
	            IF NOT EXISTS (
	                SELECT 1 FROM public.user_store_memberships
	                WHERE user_id = NEW.id
	                  AND store_id = NEW.active_store_id
	                  AND status = 'active'
	            ) THEN
	                RAISE EXCEPTION 'ERR_INVALID_ACTIVE_STORE: El usuario no tiene membership activa en la tienda seleccionada.';
	            END IF;
	        END IF;
	        RETURN NEW;
	    END IF;
	END;
	$$ LANGUAGE plpgsql SECURITY DEFINER
2026-09-13 06:16:38.733 UTC [22579] ERROR:  type "public.user_role" does not exist at character 270
2026-09-13 06:16:38.733 UTC [22579] STATEMENT:  CREATE OR REPLACE FUNCTION public.validate_tenant_access(
	  p_user_id uuid,
	  p_store_id uuid
	)
	RETURNS boolean
	LANGUAGE plpgsql
	STABLE SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  v_user_tenant uuid;
	  v_store_tenant uuid;
	  v_user_role public.user_role;
	BEGIN
	  -- Global admin bypasses tenant check
	  SELECT role, tenant_id INTO v_user_role, v_user_tenant
	    FROM public.profiles WHERE id = p_user_id;
	
	  IF v_user_role IN ('admin', 'superadmin') THEN
	    RETURN true;
	  END IF;
	
	  -- Get store's tenant
	  SELECT tenant_id INTO v_store_tenant FROM public.stores WHERE id = p_store_id;
	
	  IF v_store_tenant IS NULL THEN
	    -- Legacy store without tenant — allow (backward compat)
	    RETURN true;
	  END IF;
	
	  -- Check tenant match
	  RETURN v_user_tenant = v_store_tenant;
	END;
	$function$
2026-09-13 06:16:38.889 UTC [20282] LOG:  received fast shutdown request
2026-09-13 06:16:38.889 UTC [20282] LOG:  aborting any active transactions
2026-09-13 06:16:38.891 UTC [20282] LOG:  background worker "logical replication launcher" (PID 20288) exited with exit code 1
2026-09-13 06:16:38.892 UTC [20283] LOG:  shutting down
2026-09-13 06:16:38.892 UTC [20283] LOG:  checkpoint starting: shutdown immediate
2026-09-13 06:16:38.895 UTC [20283] LOG:  checkpoint complete: wrote 360 buffers (2.2%); 0 WAL file(s) added, 0 removed, 0 recycled; write=0.003 s, sync=0.001 s, total=0.004 s; sync files=104, longest=0.001 s, average=0.000 s; distance=2709 kB, estimate=2709 kB; lsn=0/177E178, redo lsn=0/177E178
2026-09-13 06:16:38.898 UTC [20282] LOG:  database system is shut down
2026-09-13 06:19:11.207 UTC [22713] LOG:  starting PostgreSQL 17.6 on x86_64-pc-linux-gnu, compiled by gcc (Ubuntu 7.5.0-3ubuntu1~18.04) 7.5.0, 64-bit
2026-09-13 06:19:11.207 UTC [22713] LOG:  listening on IPv4 address "127.0.0.1", port 55432
2026-09-13 06:19:11.207 UTC [22713] LOG:  listening on Unix socket "/tmp/.s.PGSQL.55432"
2026-09-13 06:19:11.209 UTC [22716] LOG:  database system was shut down at 2026-09-13 06:19:10 UTC
2026-09-13 06:19:11.211 UTC [22713] LOG:  database system is ready to accept connections
2026-09-13 06:19:14.458 UTC [22730] ERROR:  extension "pgjwt" is not available
2026-09-13 06:19:14.458 UTC [22730] DETAIL:  Could not open extension control file "/tmp/my-project/harness/pg/share/postgresql/extension/pgjwt.control": No such file or directory.
2026-09-13 06:19:14.458 UTC [22730] HINT:  The extension must first be installed on the system where PostgreSQL is running.
2026-09-13 06:19:14.458 UTC [22730] STATEMENT:  CREATE EXTENSION IF NOT EXISTS pgjwt;
2026-09-13 06:19:14.995 UTC [22809] ERROR:  type "public.user_role" does not exist at character 301
2026-09-13 06:19:14.995 UTC [22809] STATEMENT:  CREATE OR REPLACE FUNCTION public.bulk_assign_memberships(
	  p_user_id uuid,
	  p_assignments jsonb
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  v_assignment jsonb;
	  v_affected int := 0;
	  v_failed int := 0;
	  v_store_id uuid;
	  v_role public.user_role;
	  v_status text;
	  v_caller_uid uuid := auth.uid();
	  v_changes jsonb := '[]'::jsonb;
	BEGIN
	  IF v_caller_uid IS NULL THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED';
	  END IF;
	
	  FOR v_assignment IN SELECT * FROM jsonb_array_elements(p_assignments) LOOP
	    BEGIN
	      v_store_id := (v_assignment->>'store_id')::uuid;
	      v_role := (v_assignment->>'role')::public.user_role;
	      v_status := COALESCE(v_assignment->>'status', 'active');
	
	      -- Validar caller tiene acceso al store
	      IF NOT public.is_admin() AND NOT public.has_store_role(v_store_id, ARRAY['admin', 'manager']) THEN
	        v_failed := v_failed + 1;
	        CONTINUE;
	      END IF;
	
	      INSERT INTO public.user_store_memberships (user_id, store_id, role, status)
	      VALUES (p_user_id, v_store_id, v_role, v_status)
	      ON CONFLICT (user_id, store_id) DO UPDATE SET
	        role = EXCLUDED.role,
	        status = EXCLUDED.status,
	        updated_at = now();
	
	      v_changes := v_changes || jsonb_build_object(jsonb_build_object(
	        'store_id', v_store_id,
	        'role', v_role::text,
	        'status', v_status
	      ));
	
	      v_affected := v_affected + 1;
	    EXCEPTION
	      WHEN foreign_key_violation THEN
	        v_failed := v_failed + 1;
	    END;
	  END LOOP;
	
	  -- Audit log atómico (solo si hubo cambios)
	  IF v_affected > 0 THEN
	    INSERT INTO public.user_audit_log (performed_by, target_user_id, action, new_values, metadata)
	    VALUES (
	      v_caller_uid, p_user_id, 'MEMBERSHIPS_BULK_ASSIGNED',
	      jsonb_build_object('assignments', v_changes),
	      jsonb_build_object('affected', v_affected, 'failed', v_failed)
	    );
	  END IF;
	
	  RETURN jsonb_build_object(
	    'success', true,
	    'affected', v_affected,
	    'failed', v_failed,
	    'user_id', p_user_id
	  );
	END;
	$function$
2026-09-13 06:19:15.024 UTC [22814] ERROR:  syntax error at or near "REVOKE" at character 5039
2026-09-13 06:19:15.024 UTC [22814] STATEMENT:  CREATE OR REPLACE FUNCTION public.bulk_soft_delete_stores(p_store_ids uuid[], p_deleted_by uuid, p_confirmation_token text, p_override_token text DEFAULT NULL::text, p_reason text DEFAULT NULL::text)
	 RETURNS jsonb
	 LANGUAGE plpgsql
	 SECURITY DEFINER
	 SET search_path TO 'public'
	AS $function$
	DECLARE
	  v_store_id UUID;
	  v_validation JSONB;
	  v_blockers JSONB;
	  v_errors JSONB[] := '{}'::jsonb[];
	  v_processed INTEGER := 0;
	  v_token_valid BOOLEAN;
	  v_has_protected BOOLEAN;
	  v_override_valid BOOLEAN;
	  v_confirmation_record RECORD;
	  v_override_record RECORD;
	  v_caller_role TEXT;
	BEGIN
	  -- ============================================================
	  -- AUTH CHECK: Solo admin puede ejecutar esta función
	  -- ============================================================
	  IF auth.uid() IS NOT NULL THEN
	    SELECT role INTO v_caller_role FROM public.profiles WHERE id = auth.uid();
	    IF v_caller_role IS NULL OR v_caller_role != 'admin' THEN
	      RAISE EXCEPTION 'ERR_PERMISSION_DENIED: Solo admin puede ejecutar bulk_soft_delete_stores';
	    END IF;
	  END IF;
	  -- Si auth.uid() IS NULL, es service_role — permitir
	
	  -- ============================================================
	  -- 1. VALIDATE confirmation_token
	  -- ============================================================
	  SELECT * INTO v_confirmation_record
	  FROM public.bulk_confirmation_tokens
	  WHERE token = p_confirmation_token
	    AND action = 'delete'
	    AND expires_at > NOW()
	    AND consumed_at IS NULL
	    AND is_override = false;
	
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_INVALID_CONFIRMATION_TOKEN';
	  END IF;
	
	  IF v_confirmation_record.store_ids != p_store_ids THEN
	    RAISE EXCEPTION 'ERR_STORE_IDS_MISMATCH';
	  END IF;
	
	  -- ============================================================
	  -- 2. VALIDATE tiendas protegidas requieren override_token
	  -- ============================================================
	  SELECT EXISTS(
	    SELECT 1 FROM public.stores
	    WHERE id = ANY(p_store_ids) AND backup_restore_protected = true
	  ) INTO v_has_protected;
	
	  IF v_has_protected THEN
	    IF p_override_token IS NULL THEN
	      RAISE EXCEPTION 'ERR_OVERRIDE_REQUIRED';
	    END IF;
	
	    SELECT * INTO v_override_record
	    FROM public.bulk_confirmation_tokens
	    WHERE token = p_override_token
	      AND is_override = true
	      AND override_for = p_confirmation_token
	      AND expires_at > NOW()
	      AND consumed_at IS NULL;
	
	    IF NOT FOUND THEN
	      RAISE EXCEPTION 'ERR_INVALID_OVERRIDE_TOKEN';
	    END IF;
	
	    IF v_override_record.store_ids != v_confirmation_record.store_ids THEN
	      RAISE EXCEPTION 'ERR_OVERRIDE_STORE_IDS_MISMATCH';
	    END IF;
	
	    IF v_override_record.created_by = v_confirmation_record.created_by THEN
	      RAISE EXCEPTION 'ERR_SAME_USER_OVERRIDE';
	    END IF;
	
	    UPDATE public.bulk_confirmation_tokens SET consumed_at = NOW()
	    WHERE token = p_override_token;
	  END IF;
	
	  UPDATE public.bulk_confirmation_tokens SET consumed_at = NOW()
	  WHERE token = p_confirmation_token;
	
	  -- ============================================================
	  -- 3. VALIDATE todas las tiendas
	  -- ============================================================
	  FOREACH v_store_id IN ARRAY p_store_ids LOOP
	    IF NOT EXISTS(SELECT 1 FROM public.stores WHERE id = v_store_id AND is_active = true) THEN
	      v_errors := array_append(v_errors, jsonb_build_object(
	        'store_id', v_store_id, 'reason', 'STORE_NOT_FOUND_OR_INACTIVE'
	      ));
	      CONTINUE;
	    END IF;
	
	    SELECT public.validate_store_can_be_modified(v_store_id, 'soft_delete') INTO v_validation;
	    v_blockers := v_validation->'blockers';
	
	    IF v_validation->>'can_modify' != 'true' THEN
	      v_errors := array_append(v_errors, jsonb_build_object(
	        'store_id', v_store_id, 'reason', 'HAS_BLOCKING_DEPENDENCIES', 'blockers', v_blockers
	      ));
	    END IF;
	  END LOOP;
	
	  IF array_length(v_errors, 1) IS NOT NULL THEN
	    RETURN jsonb_build_object(
	      'status', 'FAILED', 'processed', 0,
	      'total_requested', array_length(p_store_ids, 1),
	      'errors', to_jsonb(v_errors), 'reason', p_reason
	    );
	  END IF;
	
	  -- ============================================================
	  -- 4. EXECUTE
	  -- ============================================================
	  FOREACH v_store_id IN ARRAY p_store_ids LOOP
	    PERFORM public.soft_delete_store(v_store_id, p_deleted_by);
	    v_processed := v_processed + 1;
	  END LOOP;
	
	  INSERT INTO public.audit_logs (action, table_name, record_id, metadata)
	  VALUES (
	    'bulk_store_deleted', 'stores', NULL,
	    jsonb_build_object(
	      'store_ids', p_store_ids, 'deleted_by', p_deleted_by,
	      'reason', p_reason, 'processed', v_processed,
	      'had_protected_stores', v_has_protected,
	      'override_used', p_override_token IS NOT NULL, 'deleted_at', NOW()
	    )
	  );
	
	  RETURN jsonb_build_object(
	    'status', 'COMPLETED', 'processed', v_processed,
	    'total_requested', array_length(p_store_ids, 1),
	    'errors', '[]'::jsonb, 'reason', p_reason
	  );
	END;
	$function$
	 
	REVOKE EXECUTE ON FUNCTION public.bulk_soft_delete_stores FROM PUBLIC, anon, authenticated
2026-09-13 06:19:15.217 UTC [22839] ERROR:  relation "received_services" does not exist
2026-09-13 06:19:15.217 UTC [22839] CONTEXT:  compilation of PL/pgSQL function "calculate_service_distribution" near line 3
2026-09-13 06:19:15.217 UTC [22839] STATEMENT:  CREATE OR REPLACE FUNCTION calculate_service_distribution(
	  p_service_id UUID
	) RETURNS TABLE(
	  receipt_item_id UUID,
	  product_id UUID,
	  distribution_amount NUMERIC,
	  distribution_percentage NUMERIC
	) LANGUAGE plpgsql SECURITY DEFINER AS $$
	DECLARE
	  v_service received_services%ROWTYPE;
	  v_method TEXT;
	  v_total_amount NUMERIC;
	  v_total_value NUMERIC DEFAULT 0;
	  v_total_qty NUMERIC DEFAULT 0;
	  v_link service_reception_links%ROWTYPE;
	  v_receipt_id UUID;
	  v_item receipt_items%ROWTYPE;
	  v_allocated NUMERIC;
	BEGIN
	  SELECT * INTO v_service FROM received_services WHERE id = p_service_id;
	  IF NOT FOUND THEN RETURN; END IF;
	  
	  v_method := v_service.distribution_method;
	  v_total_amount := v_service.total_amount;
	
	  -- Para cada recepción vinculada
	  FOR v_link IN SELECT * FROM service_reception_links WHERE service_id = p_service_id AND allocated_amount > 0 LOOP
	    v_receipt_id := v_link.receipt_id;
	    v_allocated := v_link.allocated_amount;
	
	    -- Calcular base de distribución según método
	    IF v_method = 'amount' THEN
	      SELECT COALESCE(SUM(quantity * unit_cost), 0) INTO v_total_value
	      FROM receipt_items WHERE receipt_id = v_receipt_id;
	      
	      IF v_total_value > 0 THEN
	        FOR v_item IN SELECT * FROM receipt_items WHERE receipt_id = v_receipt_id LOOP
	          distribution_amount := v_allocated * (v_item.quantity * v_item.unit_cost / v_total_value);
	          distribution_percentage := (v_item.quantity * v_item.unit_cost / v_total_value) * 100;
	          receipt_item_id := v_item.id;
	          product_id := v_item.product_id;
	          RETURN NEXT;
	        END LOOP;
	      END IF;
	
	    ELSIF v_method = 'quantity' THEN
	      SELECT COALESCE(SUM(quantity), 0) INTO v_total_qty
	      FROM receipt_items WHERE receipt_id = v_receipt_id;
	      
	      IF v_total_qty > 0 THEN
	        FOR v_item IN SELECT * FROM receipt_items WHERE receipt_id = v_receipt_id LOOP
	          distribution_amount := v_allocated * (v_item.quantity / v_total_qty);
	          distribution_percentage := (v_item.quantity / v_total_qty) * 100;
	          receipt_item_id := v_item.id;
	          product_id := v_item.product_id;
	          RETURN NEXT;
	        END LOOP;
	      END IF;
	
	    ELSIF v_method = 'manual' THEN
	      -- En manual, la distribución se guarda directamente desde el cliente
	      RETURN;
	    END IF;
	  END LOOP;
	END;
	$$
2026-09-13 06:19:15.497 UTC [22874] ERROR:  syntax error at or near "REVOKE" at character 678
2026-09-13 06:19:15.497 UTC [22874] STATEMENT:  CREATE OR REPLACE FUNCTION public.check_bulk_ops_hourly_limit(p_user_id uuid, p_plan text)
	 RETURNS jsonb
	 LANGUAGE plpgsql
	 STABLE SECURITY DEFINER
	 SET search_path TO 'public'
	AS $function$
	DECLARE
	  v_limit INTEGER;
	  v_used INTEGER;
	BEGIN
	  v_limit := CASE p_plan
	    WHEN 'free' THEN 1
	    WHEN 'pro' THEN 20
	    WHEN 'enterprise' THEN 999999
	    ELSE 1
	  END;
	
	  SELECT COUNT(*) INTO v_used
	  FROM public.bulk_ops_log
	  WHERE user_id = p_user_id
	    AND initiated_at > NOW() - INTERVAL '1 hour';
	
	  RETURN jsonb_build_object(
	    'allowed', v_used < v_limit,
	    'used', v_used,
	    'limit', v_limit,
	    'remaining', GREATEST(0, v_limit - v_used)
	  );
	END;
	$function$
	 
	REVOKE EXECUTE ON FUNCTION public.check_bulk_ops_hourly_limit FROM PUBLIC, anon, authenticated
2026-09-13 06:19:15.577 UTC [22889] ERROR:  type plan_t does not exist
2026-09-13 06:19:15.577 UTC [22889] STATEMENT:  CREATE OR REPLACE FUNCTION public.check_tenant_store_quota(
	  p_tenant_id uuid,
	  p_plan plan_t DEFAULT NULL
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  v_plan plan_t;
	  v_limit int;
	  v_current int;
	  v_allowed boolean;
	BEGIN
	  -- Get plan from tenant if not passed
	  IF p_plan IS NULL THEN
	    SELECT plan INTO v_plan FROM public.tenants WHERE id = p_tenant_id;
	  ELSE
	    v_plan := p_plan;
	  END IF;
	
	  -- Plan limits (unified source of truth)
	  v_limit := CASE v_plan
	    WHEN 'free'::plan_t THEN 1
	    WHEN 'pro'::plan_t THEN 3
	    WHEN 'enterprise'::plan_t THEN 10
	    ELSE 1
	  END;
	
	  -- Count active stores in tenant
	  SELECT COUNT(*) INTO v_current
	    FROM public.stores
	    WHERE tenant_id = p_tenant_id AND is_active = true;
	
	  v_allowed := v_current < v_limit;
	
	  RETURN jsonb_build_object(
	    'allowed', v_allowed,
	    'current', v_current,
	    'limit', v_limit,
	    'plan', v_plan::text,
	    'tenant_id', p_tenant_id
	  );
	END;
	$function$
2026-09-13 06:19:16.418 UTC [22992] ERROR:  relation "public.profiles" does not exist at character 166
2026-09-13 06:19:16.418 UTC [22992] STATEMENT:  CREATE OR REPLACE FUNCTION public.current_user_tenant_id()
	RETURNS uuid
	LANGUAGE sql
	STABLE
	SECURITY DEFINER
	SET search_path TO public
	AS $$
	  SELECT tenant_id FROM public.profiles WHERE id = auth.uid();
	$$
2026-09-13 06:19:16.554 UTC [23011] ERROR:  relation "received_services" does not exist
2026-09-13 06:19:16.554 UTC [23011] CONTEXT:  compilation of PL/pgSQL function "distribute_service_cost_v2" near line 6
2026-09-13 06:19:16.554 UTC [23011] STATEMENT:  CREATE OR REPLACE FUNCTION public.distribute_service_cost_v2(
	  p_service_id uuid,
	  p_user_id uuid DEFAULT NULL
	) RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO 'public', 'extensions'
	AS $func$
	DECLARE
	  v_store_id uuid;
	  v_status text;
	  v_total_amount numeric;
	  v_service received_services%ROWTYPE;
	  v_method text;
	  v_link service_reception_links%ROWTYPE;
	  v_item receipt_items%ROWTYPE;
	  v_total_value numeric := 0;
	  v_total_qty numeric := 0;
	  v_allocated numeric;
	  v_dist_count integer := 0;
	  v_caller_uid uuid := COALESCE(p_user_id, auth.uid());
	BEGIN
	  PERFORM pg_advisory_xact_lock(hashtext(p_service_id::text));
	
	  SELECT * INTO v_service FROM received_services WHERE id = p_service_id FOR UPDATE;
	  IF NOT FOUND THEN RAISE EXCEPTION 'ERR_SERVICE_NOT_FOUND'; END IF;
	
	  v_store_id := v_service.store_id;
	  v_status := v_service.status;
	  v_total_amount := v_service.total_amount;
	  v_method := v_service.distribution_method;
	
	  IF NOT public.has_store_access(v_store_id) THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED';
	  END IF;
	
	  IF v_status != 'active' THEN
	    RAISE EXCEPTION 'ERR_SERVICE_NOT_ACTIVE: status % is not active', v_status;
	  END IF;
	
	  IF v_method = 'manual' THEN
	    RAISE EXCEPTION 'ERR_MANUAL_METHOD: use link_receipts_to_service for manual distribution';
	  END IF;
	
	  DELETE FROM service_cost_distributions WHERE service_id = p_service_id;
	
	  FOR v_link IN SELECT * FROM service_reception_links WHERE service_id = p_service_id AND allocated_amount > 0 ORDER BY receipt_id LOOP
	    v_allocated := v_link.allocated_amount;
	
	    IF v_method = 'amount' THEN
	      SELECT COALESCE(SUM(quantity * unit_cost), 0) INTO v_total_value
	      FROM receipt_items WHERE receipt_id = v_link.receipt_id;
	      IF v_total_value > 0 THEN
	        FOR v_item IN SELECT * FROM receipt_items WHERE receipt_id = v_link.receipt_id ORDER BY id LOOP
	          INSERT INTO service_cost_distributions
	            (service_id, receipt_id, receipt_item_id, product_id, distribution_amount, distribution_percentage)
	          VALUES
	            (p_service_id, v_link.receipt_id, v_item.id, v_item.product_id,
	             v_allocated * (v_item.quantity * v_item.unit_cost / v_total_value),
	             (v_item.quantity * v_item.unit_cost / v_total_value) * 100);
	          v_dist_count := v_dist_count + 1;
	        END LOOP;
	      END IF;
	
	    ELSIF v_method = 'quantity' THEN
	      SELECT COALESCE(SUM(quantity), 0) INTO v_total_qty
	      FROM receipt_items WHERE receipt_id = v_link.receipt_id;
	      IF v_total_qty > 0 THEN
	        FOR v_item IN SELECT * FROM receipt_items WHERE receipt_id = v_link.receipt_id ORDER BY id LOOP
	          INSERT INTO service_cost_distributions
	            (service_id, receipt_id, receipt_item_id, product_id, distribution_amount, distribution_percentage)
	          VALUES
	            (p_service_id, v_link.receipt_id, v_item.id, v_item.product_id,
	             v_allocated * (v_item.quantity / v_total_qty),
	             (v_item.quantity / v_total_qty) * 100);
	          v_dist_count := v_dist_count + 1;
	        END LOOP;
	      END IF;
	    END IF;
	  END LOOP;
	
	  -- Forzar recalculo de WAC para cada producto afectado
	  UPDATE receipt_items SET updated_at = NOW()
	  WHERE product_id IN (SELECT DISTINCT product_id FROM service_cost_distributions WHERE service_id = p_service_id);
	
	  INSERT INTO audit_logs (user_id, store_id, action, table_name, record_id, metadata)
	  VALUES (v_caller_uid, v_store_id, 'SERVICE_DISTRIBUTED', 'received_services', p_service_id,
	    jsonb_build_object('rows_distributed', v_dist_count, 'method', v_method, 'total_amount', v_total_amount));
	
	  RETURN jsonb_build_object('status', 'success', 'distributed_rows', v_dist_count);
	END;
	$func$
2026-09-13 06:19:16.645 UTC [23026] ERROR:  type "user_role" does not exist at character 116
2026-09-13 06:19:16.645 UTC [23026] STATEMENT:  CREATE OR REPLACE FUNCTION public.enforce_encargado_store_limit()
	RETURNS TRIGGER AS $$
	DECLARE
	    v_creator_role user_role;
	    v_limit integer;
	    v_current_count integer;
	BEGIN
	    IF NEW.created_by IS NULL THEN RETURN NEW; END IF;
	
	    SELECT role, max_stores_limit INTO v_creator_role, v_limit
	    FROM public.profiles WHERE id = NEW.created_by;
	
	    IF v_creator_role = 'encargado' THEN
	        SELECT COUNT(*) INTO v_current_count FROM public.stores WHERE created_by = NEW.created_by;
	        IF v_current_count >= v_limit THEN
	            RAISE EXCEPTION 'ERR_STORE_LIMIT_EXCEEDED: Maximum number of stores reached for this manager (%/%)', v_current_count, v_limit;
	        END IF;
	    END IF;
	    RETURN NEW;
	END;
	$$ LANGUAGE plpgsql
2026-09-13 06:19:16.676 UTC [23033] ERROR:  type "user_role" does not exist at character 115
2026-09-13 06:19:16.676 UTC [23033] STATEMENT:  CREATE OR REPLACE FUNCTION public.enforce_encargado_user_limit()
	RETURNS TRIGGER AS $$
	DECLARE
	    v_creator_role user_role;
	    v_limit integer;
	    v_current_count integer;
	BEGIN
	    IF NEW.created_by IS NULL THEN RETURN NEW; END IF;
	
	    SELECT role, max_users_limit INTO v_creator_role, v_limit
	    FROM public.profiles WHERE id = NEW.created_by;
	
	    IF v_creator_role = 'encargado' THEN
	        SELECT COUNT(*) INTO v_current_count FROM public.profiles WHERE created_by = NEW.created_by;
	        IF v_current_count >= v_limit THEN
	            RAISE EXCEPTION 'ERR_USER_LIMIT_EXCEEDED: Maximum number of users reached for this manager (%/%)', v_current_count, v_limit;
	        END IF;
	    END IF;
	    RETURN NEW;
	END;
	$$ LANGUAGE plpgsql
2026-09-13 06:19:16.971 UTC [23080] ERROR:  syntax error at or near "REVOKE" at character 1436
2026-09-13 06:19:16.971 UTC [23080] STATEMENT:  CREATE OR REPLACE FUNCTION public.generate_bulk_confirmation_token(p_store_ids uuid[], p_action text, p_user_id uuid)
	 RETURNS text
	 LANGUAGE plpgsql
	 SECURITY DEFINER
	 SET search_path TO 'public'
	AS $function$
	DECLARE
	  v_token TEXT;
	  v_has_protected BOOLEAN;
	  v_caller_role TEXT;
	BEGIN
	  -- AUTH CHECK: Solo admin
	  IF auth.uid() IS NOT NULL THEN
	    SELECT role INTO v_caller_role FROM public.profiles WHERE id = auth.uid();
	    IF v_caller_role IS NULL OR v_caller_role != 'admin' THEN
	      RAISE EXCEPTION 'ERR_PERMISSION_DENIED: Solo admin puede generar tokens bulk';
	    END IF;
	    -- Verificar que p_user_id coincide con auth.uid()
	    IF p_user_id != auth.uid() THEN
	      RAISE EXCEPTION 'ERR_PERMISSION_DENIED: p_user_id debe coincidir con el usuario autenticado';
	    END IF;
	  END IF;
	
	  IF p_action NOT IN ('delete', 'archive') THEN
	    RAISE EXCEPTION 'ERR_NON_DESTRUCTIVE_ACTION';
	  END IF;
	
	  SELECT EXISTS(
	    SELECT 1 FROM public.stores
	    WHERE id = ANY(p_store_ids) AND backup_restore_protected = true
	  ) INTO v_has_protected;
	
	  v_token := 'bct_' || replace(gen_random_uuid()::text, '-', '');
	
	  INSERT INTO public.bulk_confirmation_tokens (
	    token, store_ids, action, created_by, expires_at, metadata
	  ) VALUES (
	    v_token, p_store_ids, p_action, p_user_id,
	    NOW() + INTERVAL '10 minutes',
	    jsonb_build_object('has_protected_stores', v_has_protected)
	  );
	
	  RETURN v_token;
	END;
	$function$
	 
	REVOKE EXECUTE ON FUNCTION public.generate_bulk_confirmation_token FROM PUBLIC, anon, authenticated
2026-09-13 06:19:16.999 UTC [23087] ERROR:  syntax error at or near "REVOKE" at character 2432
2026-09-13 06:19:16.999 UTC [23087] STATEMENT:  CREATE OR REPLACE FUNCTION public.generate_bulk_override_token(p_confirmation_token text, p_override_user_id uuid, p_reason text DEFAULT NULL::text)
	 RETURNS text
	 LANGUAGE plpgsql
	 SECURITY DEFINER
	 SET search_path TO 'public'
	AS $function$
	DECLARE
	  v_original RECORD;
	  v_override_token TEXT;
	  v_override_user_role TEXT;
	BEGIN
	  -- AUTH CHECK: Solo admin, y debe ser auth.uid() == p_override_user_id
	  IF auth.uid() IS NOT NULL THEN
	    IF auth.uid() != p_override_user_id THEN
	      RAISE EXCEPTION 'ERR_PERMISSION_DENIED: p_override_user_id debe coincidir con el usuario autenticado';
	    END IF;
	
	    SELECT role INTO v_override_user_role FROM public.profiles WHERE id = p_override_user_id;
	    IF v_override_user_role IS NULL OR v_override_user_role != 'admin' THEN
	      RAISE EXCEPTION 'ERR_PERMISSION_DENIED: Solo admin puede generar override';
	    END IF;
	  END IF;
	
	  SELECT * INTO v_original
	  FROM public.bulk_confirmation_tokens
	  WHERE token = p_confirmation_token
	    AND consumed_at IS NULL
	    AND expires_at > NOW()
	    AND is_override = false;
	
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_INVALID_OR_EXPIRED_TOKEN';
	  END IF;
	
	  IF v_original.created_by = p_override_user_id THEN
	    RAISE EXCEPTION 'ERR_SAME_USER_OVERRIDE';
	  END IF;
	
	  IF EXISTS(
	    SELECT 1 FROM public.bulk_confirmation_tokens
	    WHERE override_for = p_confirmation_token
	      AND consumed_at IS NULL AND expires_at > NOW()
	  ) THEN
	    RAISE EXCEPTION 'ERR_OVERRIDE_ALREADY_EXISTS';
	  END IF;
	
	  v_override_token := 'bot_' || replace(gen_random_uuid()::text, '-', '');
	
	  INSERT INTO public.bulk_confirmation_tokens (
	    token, store_ids, action, created_by, expires_at,
	    is_override, override_for, metadata
	  ) VALUES (
	    v_override_token, v_original.store_ids, v_original.action, p_override_user_id,
	    NOW() + INTERVAL '10 minutes', true, p_confirmation_token,
	    jsonb_build_object('override_reason', p_reason,
	      'original_created_by', v_original.created_by,
	      'override_created_by', p_override_user_id)
	  );
	
	  INSERT INTO public.audit_logs (action, table_name, record_id, metadata)
	  VALUES (
	    'bulk_override_token_generated', 'stores', NULL,
	    jsonb_build_object('confirmation_token', p_confirmation_token,
	      'store_ids', v_original.store_ids,
	      'override_by', p_override_user_id,
	      'original_by', v_original.created_by, 'reason', p_reason)
	  );
	
	  RETURN v_override_token;
	END;
	$function$
	 
	REVOKE EXECUTE ON FUNCTION public.generate_bulk_override_token FROM PUBLIC, anon, authenticated
2026-09-13 06:19:17.156 UTC [23114] ERROR:  relation "public.backup_table_registry" does not exist at character 581
2026-09-13 06:19:17.156 UTC [23114] STATEMENT:  CREATE OR REPLACE FUNCTION public.get_backup_table_list(
	  p_include_excluded BOOLEAN DEFAULT FALSE
	)
	RETURNS TABLE (
	  table_name          TEXT,
	  tier                INTEGER,
	  filter_strategy     TEXT,
	  parent_table        TEXT,
	  parent_foreign_key  TEXT,
	  date_column         TEXT,
	  excluded_from_restore BOOLEAN,
	  exclude_reason      TEXT
	)
	LANGUAGE sql
	SECURITY DEFINER
	SET search_path = public
	AS $$
	  SELECT
	    table_name,
	    tier,
	    filter_strategy,
	    parent_table,
	    parent_foreign_key,
	    date_column,
	    excluded_from_restore,
	    exclude_reason
	  FROM public.backup_table_registry
	  WHERE (p_include_excluded OR excluded_from_restore = FALSE)
	  ORDER BY tier ASC, table_name ASC;
	$$
2026-09-13 06:19:17.314 UTC [23141] ERROR:  relation "v_global_operation_dates" does not exist at character 229
2026-09-13 06:19:17.314 UTC [23141] STATEMENT:  CREATE OR REPLACE FUNCTION public.get_global_max_operation_date(
	  p_store_id UUID DEFAULT NULL
	)
	RETURNS TIMESTAMP WITH TIME ZONE
	LANGUAGE sql
	SECURITY DEFINER
	SET search_path = public
	AS $$
	  SELECT MAX(operation_date)
	  FROM v_global_operation_dates
	  WHERE p_store_id IS NULL OR store_id = p_store_id;
	$$
2026-09-13 06:19:17.346 UTC [23148] ERROR:  relation "exchange_rates" does not exist at character 216
2026-09-13 06:19:17.346 UTC [23148] STATEMENT:  CREATE OR REPLACE FUNCTION get_latest_rate(p_currency TEXT, p_source TEXT)
	RETURNS TABLE(rate NUMERIC, rate_date DATE, captured_at TIMESTAMPTZ)
	LANGUAGE sql STABLE AS $$
	  SELECT rate, rate_date, captured_at
	  FROM exchange_rates
	  WHERE currency = p_currency AND source = p_source
	  ORDER BY rate_date DESC, captured_at DESC
	  LIMIT 1;
	$$
2026-09-13 06:19:17.375 UTC [23155] ERROR:  relation "public.products" does not exist at character 194
2026-09-13 06:19:17.375 UTC [23155] STATEMENT:  CREATE OR REPLACE FUNCTION public.get_low_stock_count(p_store_id uuid DEFAULT NULL)
	RETURNS bigint
	LANGUAGE sql
	SECURITY DEFINER
	SET search_path = public
	AS $$
	  SELECT COUNT(*)::bigint
	  FROM public.products
	  WHERE
	    (p_store_id IS NULL OR store_id = p_store_id)
	    AND is_active = true
	    AND stock_current > 0
	    AND min_stock IS NOT NULL
	    AND min_stock > 0
	    AND stock_current <= min_stock;
	$$
2026-09-13 06:19:17.525 UTC [23178] ERROR:  relation "receipt_items" does not exist at character 754
2026-09-13 06:19:17.525 UTC [23178] STATEMENT:  CREATE OR REPLACE FUNCTION get_product_cost_analysis(
	  p_product_id UUID,
	  p_store_id UUID
	) RETURNS TABLE(
	  receipt_id UUID,
	  receipt_date TIMESTAMPTZ,
	  quantity INT,
	  unit_cost FLOAT,
	  service_type TEXT,
	  service_amount NUMERIC,
	  total_cost NUMERIC,
	  unit_cost_final NUMERIC
	) LANGUAGE sql SECURITY DEFINER AS $$
	  SELECT 
	    ri.receipt_id,
	    r.reception_date,
	    ri.quantity,
	    ri.unit_cost,
	    st.name AS service_type,
	    COALESCE(scd.distribution_amount, 0) AS service_amount,
	    ri.unit_cost * ri.quantity + COALESCE(scd.distribution_amount, 0) AS total_cost,
	    CASE WHEN ri.quantity > 0 
	      THEN (ri.unit_cost * ri.quantity + COALESCE(scd.distribution_amount, 0)) / ri.quantity 
	      ELSE 0 END AS unit_cost_final
	  FROM receipt_items ri
	  JOIN receipts r ON r.id = ri.receipt_id
	  LEFT JOIN service_cost_distributions scd ON scd.receipt_item_id = ri.id
	  LEFT JOIN received_services rs ON rs.id = scd.service_id AND rs.status = 'active'
	  LEFT JOIN service_types st ON st.id = rs.service_type_id
	  WHERE ri.product_id = p_product_id
	    AND r.store_id = p_store_id
	    AND r.status = 'active'
	  ORDER BY r.reception_date DESC;
	$$
2026-09-13 06:19:17.628 UTC [23197] ERROR:  relation "exchange_rates" does not exist at character 252
2026-09-13 06:19:17.628 UTC [23197] STATEMENT:  CREATE OR REPLACE FUNCTION get_rate_history(
	  p_currency TEXT,
	  p_source TEXT,
	  p_days INT DEFAULT 30
	)
	RETURNS TABLE(rate_date DATE, rate NUMERIC, variation_daily NUMERIC)
	LANGUAGE sql STABLE AS $$
	  SELECT rate_date, rate, variation_daily
	  FROM exchange_rates
	  WHERE currency = p_currency AND source = p_source
	    AND rate_date >= CURRENT_DATE - p_days
	  ORDER BY rate_date ASC;
	$$
2026-09-13 06:19:17.655 UTC [23204] ERROR:  relation "exchange_rates" does not exist at character 363
2026-09-13 06:19:17.655 UTC [23204] STATEMENT:  CREATE OR REPLACE FUNCTION get_rate_variation(
	  p_currency TEXT,
	  p_source TEXT,
	  p_start_date DATE,
	  p_end_date DATE
	)
	RETURNS TABLE(
	  start_rate NUMERIC,
	  end_rate NUMERIC,
	  absolute_change NUMERIC,
	  percent_change NUMERIC,
	  daily_avg_growth NUMERIC,
	  monthly_avg_growth NUMERIC
	)
	LANGUAGE sql STABLE AS $$
	  WITH start_val AS (
	    SELECT rate FROM exchange_rates
	    WHERE currency = p_currency AND source = p_source AND rate_date <= p_start_date
	    ORDER BY rate_date DESC LIMIT 1
	  ),
	  end_val AS (
	    SELECT rate FROM exchange_rates
	    WHERE currency = p_currency AND source = p_source AND rate_date <= p_end_date
	    ORDER BY rate_date DESC LIMIT 1
	  )
	  SELECT
	    COALESCE(sv.rate, 0) AS start_rate,
	    COALESCE(ev.rate, 0) AS end_rate,
	    (COALESCE(ev.rate, 0) - COALESCE(sv.rate, 0)) AS absolute_change,
	    CASE WHEN sv.rate IS NOT NULL AND sv.rate > 0 THEN ((ev.rate - sv.rate) / sv.rate * 100) ELSE 0 END AS percent_change,
	    CASE WHEN sv.rate IS NOT NULL AND sv.rate > 0 AND ev.rate IS NOT NULL AND ev.rate > 0 AND p_end_date > p_start_date
	      THEN (POWER(ev.rate::FLOAT / sv.rate::FLOAT, 1.0 / EXTRACT(EPOCH FROM (p_end_date - p_start_date)) * 86400) - 1) * 100
	      ELSE 0 END AS daily_avg_growth,
	    CASE WHEN sv.rate IS NOT NULL AND sv.rate > 0 AND ev.rate IS NOT NULL AND ev.rate > 0 AND p_end_date > p_start_date
	      THEN (POWER(ev.rate::FLOAT / sv.rate::FLOAT, 30.44 / EXTRACT(EPOCH FROM (p_end_date - p_start_date)) * 86400) - 1) * 100
	      ELSE 0 END AS monthly_avg_growth
	  FROM start_val sv, end_val ev;
	$$
2026-09-13 06:19:17.891 UTC [23247] ERROR:  type "public.stores" does not exist
2026-09-13 06:19:17.891 UTC [23247] STATEMENT:  CREATE OR REPLACE FUNCTION public.get_transferable_stores(
	  p_user_id UUID,
	  p_current_store_id UUID
	)
	RETURNS SETOF public.stores
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO 'public'
	AS $$
	DECLARE
	  v_tenant_id UUID;
	BEGIN
	  -- 1. Obtener tenant_id de la tienda actual
	  SELECT tenant_id INTO v_tenant_id
	  FROM public.stores
	  WHERE id = p_current_store_id AND is_active = true;
	
	  -- 2. Devolver tiendas donde el caller tiene acceso (has_store_access_as)
	  --    EXCLUYENDO la tienda actual
	  --    Si tenant_id es NOT NULL, filtrar por mismo tenant.
	  --    Si tenant_id es NULL (legacy), mostrar todas las que el caller tiene acceso.
	  IF v_tenant_id IS NOT NULL THEN
	    RETURN QUERY
	    SELECT s.*
	    FROM public.stores s
	    WHERE s.tenant_id = v_tenant_id
	      AND s.id != p_current_store_id
	      AND s.is_active = true
	      AND s.is_archived = false
	      AND public.has_store_access_as(p_user_id, s.id)
	    ORDER BY s.name;
	  ELSE
	    -- Legacy: tiendas sin tenant_id — usar solo has_store_access_as
	    RETURN QUERY
	    SELECT s.*
	    FROM public.stores s
	    WHERE s.id != p_current_store_id
	      AND s.is_active = true
	      AND s.is_archived = false
	      AND public.has_store_access_as(p_user_id, s.id)
	    ORDER BY s.name;
	  END IF;
	END;
	$$
2026-09-13 06:19:18.057 UTC [23278] ERROR:  type user_role[] does not exist
2026-09-13 06:19:18.057 UTC [23278] STATEMENT:  CREATE OR REPLACE FUNCTION public.has_any_role(required_roles user_role[])
	 RETURNS boolean
	 LANGUAGE plpgsql
	 STABLE SECURITY DEFINER
	AS $$
	DECLARE
	    v_actual_role user_role;
	    r user_role;
	BEGIN
	    SELECT role INTO v_actual_role FROM public.profiles WHERE id = auth.uid();
	
	    FOREACH r IN ARRAY required_roles
	    LOOP
	        IF v_actual_role = r THEN RETURN true; END IF;
	        IF v_actual_role = 'encargado' AND r = 'manager' THEN RETURN true; END IF;
	        IF v_actual_role = 'usuario' AND (r = 'clerk' OR r = 'warehouse') THEN RETURN true; END IF;
	    END LOOP;
	
	    RETURN false;
	END;
	$$
2026-09-13 06:19:18.086 UTC [23285] ERROR:  relation "profiles" does not exist at character 461
2026-09-13 06:19:18.086 UTC [23285] STATEMENT:  CREATE OR REPLACE FUNCTION public.has_management_access_as(
	  p_user_id uuid,
	  p_store_id uuid
	) RETURNS boolean
	LANGUAGE sql SECURITY DEFINER
	SET search_path = public, pg_temp
	AS $$
	  -- service_role always passes
	  SELECT CASE WHEN auth.role() = 'service_role' THEN true
	    ELSE
	      -- Check if user has admin/manager/encargado role in their profile
	      -- OR has a membership with manager/admin role for the store
	      EXISTS (
	        SELECT 1 FROM profiles
	        WHERE id = p_user_id
	        AND role IN ('admin', 'manager', 'encargado')
	      )
	      OR EXISTS (
	        SELECT 1 FROM user_store_memberships
	        WHERE user_id = p_user_id
	        AND store_id = p_store_id
	        AND status = 'active'
	        AND role IN ('admin', 'manager')
	      )
	    END
	$$
2026-09-13 06:19:18.117 UTC [23292] ERROR:  type public.user_role does not exist
2026-09-13 06:19:18.117 UTC [23292] STATEMENT:  CREATE OR REPLACE FUNCTION public.has_role(p_user_id UUID, p_required_role public.user_role)
	 RETURNS boolean
	 LANGUAGE plpgsql
	 STABLE SECURITY DEFINER
	AS $$
	DECLARE
	    v_actual_role public.user_role;
	BEGIN
	    SELECT role INTO v_actual_role FROM public.profiles WHERE id = p_user_id;
	
	    IF v_actual_role IS NULL THEN
	        RETURN false;
	    END IF;
	
	    -- Compatibility mapping (mirroring the 1-arg version)
	    IF v_actual_role = 'encargado' AND p_required_role = 'manager' THEN RETURN true; END IF;
	    IF v_actual_role = 'usuario' AND (p_required_role = 'clerk' OR p_required_role = 'warehouse') THEN RETURN true; END IF;
	
	    RETURN v_actual_role = p_required_role;
	END;
	$$
2026-09-13 06:19:18.193 UTC [23307] ERROR:  relation "public.user_store_memberships" does not exist at character 279
2026-09-13 06:19:18.193 UTC [23307] STATEMENT:  CREATE OR REPLACE FUNCTION public.has_store_role(
	  p_user_id uuid,
	  p_store_id uuid,
	  p_roles text[]
	) RETURNS boolean
	LANGUAGE sql SECURITY DEFINER
	SET search_path = public, pg_temp
	AS $$
	  SELECT CASE WHEN auth.role() = 'service_role' THEN
	    EXISTS (
	      SELECT 1 FROM public.user_store_memberships m
	      WHERE m.user_id = p_user_id
	        AND m.store_id = p_store_id
	        AND m.status = 'active'
	        AND m.role::text = ANY(p_roles)
	    )
	  ELSE
	    EXISTS (
	      SELECT 1 FROM public.user_store_memberships m
	      WHERE m.user_id = auth.uid()
	        AND m.store_id = p_store_id
	        AND m.status = 'active'
	        AND m.role::text = ANY(p_roles)
	    )
	  END
	$$
2026-09-13 06:19:18.290 UTC [23326] ERROR:  relation "public.profiles" does not exist at character 173
2026-09-13 06:19:18.290 UTC [23326] STATEMENT:  CREATE OR REPLACE FUNCTION public.is_global_admin()
	RETURNS BOOLEAN
	LANGUAGE sql
	STABLE
	SECURITY DEFINER
	SET search_path = public
	AS $$
	  SELECT EXISTS (
	    SELECT 1 FROM public.profiles
	    WHERE id = auth.uid()
	      AND role = 'admin'
	  );
	$$
2026-09-13 06:19:18.343 UTC [23337] ERROR:  type user_role does not exist
2026-09-13 06:19:18.343 UTC [23337] STATEMENT:  CREATE OR REPLACE FUNCTION public.is_role_not_changed(p_user_id uuid, p_new_role user_role, p_new_role_id uuid)
	RETURNS boolean
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path = public, extensions
	AS $$
	DECLARE
	  v_old_role user_role;
	  v_old_role_id uuid;
	BEGIN
	  SELECT role, role_id INTO v_old_role, v_old_role_id
	  FROM public.profiles
	  WHERE id = p_user_id;
	
	  RETURN (p_new_role IS NOT DISTINCT FROM v_old_role)
	     AND (p_new_role_id IS NOT DISTINCT FROM v_old_role_id);
	END;
	$$
2026-09-13 06:19:18.395 UTC [23348] ERROR:  relation "public.user_store_memberships" does not exist at character 188
2026-09-13 06:19:18.395 UTC [23348] STATEMENT:  CREATE OR REPLACE FUNCTION public.is_store_member(p_store_id UUID)
	RETURNS BOOLEAN
	LANGUAGE sql
	STABLE
	SECURITY DEFINER
	SET search_path = public
	AS $$
	  SELECT EXISTS (
	    SELECT 1 FROM public.user_store_memberships
	    WHERE store_id = p_store_id
	      AND user_id = auth.uid()
	      AND status = 'active'
	  );
	$$
2026-09-13 06:19:18.426 UTC [23355] ERROR:  function public.current_user_tenant_id() does not exist at character 209
2026-09-13 06:19:18.426 UTC [23355] HINT:  No function matches the given name and argument types. You might need to add explicit type casts.
2026-09-13 06:19:18.426 UTC [23355] STATEMENT:  CREATE OR REPLACE FUNCTION public.is_tenant_member(p_tenant_id uuid)
	RETURNS boolean
	LANGUAGE sql
	STABLE
	SECURITY DEFINER
	SET search_path TO public
	AS $$
	  SELECT p_tenant_id IS NOT NULL
	   AND p_tenant_id = public.current_user_tenant_id();
	$$
2026-09-13 06:19:18.655 UTC [23390] ERROR:  type user_role does not exist
2026-09-13 06:19:18.655 UTC [23390] STATEMENT:  CREATE OR REPLACE FUNCTION public.managed_create_user(p_email text, p_full_name text, p_role user_role, p_store_id uuid DEFAULT NULL::uuid, p_memberships jsonb DEFAULT NULL::jsonb, p_max_stores integer DEFAULT 0, p_max_users integer DEFAULT 0, p_target_user_id uuid DEFAULT NULL::uuid)
	 RETURNS jsonb
	 LANGUAGE plpgsql
	 SECURITY DEFINER
	 SET search_path TO 'public', 'extensions'
	AS $function$
	DECLARE
	    v_user_id uuid;
	    v_role_id uuid;
	    v_active_store_id uuid;
	    m JSONB;
	    v_creator_role user_role;
	BEGIN
	    SELECT role INTO v_creator_role FROM public.profiles WHERE id = auth.uid();
	    IF v_creator_role IS NULL OR v_creator_role NOT IN ('admin', 'encargado') THEN
	        RAISE EXCEPTION 'ERR_UNAUTHORIZED: Only admins and managers can create users.';
	    END IF;
	
	    v_active_store_id := COALESCE((p_memberships->0->>'store_id')::UUID, p_store_id);
	
	    IF v_creator_role = 'encargado' AND v_active_store_id IS NOT NULL THEN
	        IF NOT public.has_store_access(v_active_store_id) THEN
	            RAISE EXCEPTION 'ERR_UNAUTHORIZED: No access to store %', v_active_store_id;
	        END IF;
	    END IF;
	
	    SELECT id INTO v_role_id FROM public.roles WHERE lower(name) = lower(p_role::text) OR (name = 'Cajero' AND p_role = 'clerk') OR (name = 'Almacenero' AND p_role = 'warehouse') LIMIT 1;
	    v_user_id := COALESCE(p_target_user_id, gen_random_uuid());
	
	    INSERT INTO public.profiles (id, email, full_name, role, role_id, active_store_id, is_active, created_by, max_stores_limit, max_users_limit, created_at, updated_at)
	    VALUES (v_user_id, p_email, p_full_name, p_role, v_role_id, v_active_store_id, true, auth.uid(), p_max_stores, p_max_users, now(), now())
	    ON CONFLICT (id) DO UPDATE SET email = EXCLUDED.email, full_name = EXCLUDED.full_name, role = EXCLUDED.role, role_id = EXCLUDED.role_id, active_store_id = EXCLUDED.active_store_id, updated_at = now()
	    RETURNING id INTO v_user_id;
	
	    IF p_memberships IS NOT NULL THEN
	        IF v_creator_role != 'admin' THEN
	            DELETE FROM public.user_store_memberships WHERE user_id = v_user_id AND store_id IN (SELECT store_id FROM public.user_store_memberships WHERE user_id = auth.uid() AND role IN ('encargado', 'manager'));
	        ELSE
	            DELETE FROM public.user_store_memberships WHERE user_id = v_user_id;
	        END IF;
	        FOR m IN SELECT * FROM jsonb_array_elements(p_memberships) LOOP
	            IF (m->>'store_id') IS NOT NULL AND (m->>'store_id') <> '' THEN
	                IF v_creator_role = 'admin' OR public.has_store_access((m->>'store_id')::UUID) THEN
	                    INSERT INTO public.user_store_memberships (user_id, store_id, role) VALUES (v_user_id, (m->>'store_id')::UUID, (m->>'role')::user_role) ON CONFLICT (user_id, store_id) DO UPDATE SET role = EXCLUDED.role;
	                END IF;
	            END IF;
	        END LOOP;
	    ELSIF p_store_id IS NOT NULL THEN
	        INSERT INTO public.user_store_memberships (user_id, store_id, role) VALUES (v_user_id, p_store_id, p_role) ON CONFLICT (user_id, store_id) DO UPDATE SET role = EXCLUDED.role;
	    END IF;
	    RETURN jsonb_build_object('success', true, 'user_id', v_user_id);
	END;
	$function$
2026-09-13 06:19:18.681 UTC [23397] ERROR:  type public.user_role does not exist
2026-09-13 06:19:18.681 UTC [23397] STATEMENT:  CREATE OR REPLACE FUNCTION public.managed_create_user_v2(
	  p_email text,
	  p_full_name text,
	  p_role public.user_role,
	  p_plan plan_t DEFAULT 'free'::plan_t,
	  p_store_id uuid DEFAULT NULL::uuid,
	  p_memberships jsonb DEFAULT NULL::jsonb,
	  p_max_stores int DEFAULT 0,
	  p_max_users int DEFAULT 0,
	  p_target_user_id uuid DEFAULT NULL::uuid,
	  p_creator_id uuid DEFAULT NULL::uuid
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  v_user_id uuid := COALESCE(p_target_user_id, gen_random_uuid());
	  v_role_id uuid;
	  v_active_store_id uuid;
	  m JSONB;
	  v_creator_role public.user_role;
	  v_creator_uid uuid := COALESCE(p_creator_id, auth.uid());
	BEGIN
	  -- Validar caller es admin o encargado
	  SELECT role INTO v_creator_role FROM public.profiles WHERE id = v_creator_uid;
	  IF v_creator_role IS NULL OR v_creator_role NOT IN ('admin', 'encargado', 'superadmin') THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED: Only admins and managers can create users.';
	  END IF;
	
	  -- Validar email único (entre perfiles activos)
	  IF EXISTS (SELECT 1 FROM public.profiles WHERE email = p_email AND deleted_at IS NULL) THEN
	    RAISE EXCEPTION 'ERR_EMAIL_ALREADY_EXISTS: %', p_email;
	  END IF;
	
	  v_active_store_id := COALESCE((p_memberships->0->>'store_id')::UUID, p_store_id);
	
	  -- Encargado solo puede crear en tiendas que gestiona
	  IF v_creator_role = 'encargado' AND v_active_store_id IS NOT NULL THEN
	    IF NOT public.has_store_access(v_active_store_id) THEN
	      RAISE EXCEPTION 'ERR_UNAUTHORIZED: No access to store %', v_active_store_id;
	    END IF;
	  END IF;
	
	  -- Buscar role_id
	  SELECT id INTO v_role_id FROM public.roles
	    WHERE lower(name) = lower(p_role::text)
	       OR (name = 'Cajero' AND p_role = 'clerk')
	       OR (name = 'Almacenero' AND p_role = 'warehouse')
	       OR (name = 'UserCosto' AND p_role = 'costo')
	    LIMIT 1;
	
	  -- INSERT profiles con plan
	  INSERT INTO public.profiles (
	    id, email, full_name, role, role_id, active_store_id, is_active,
	    created_by, max_stores_limit, max_users_limit, plan, created_at, updated_at
	  ) VALUES (
	    v_user_id, p_email, p_full_name, p_role, v_role_id, v_active_store_id, true,
	    v_creator_uid, p_max_stores, p_max_users, p_plan, now(), now()
	  )
	  ON CONFLICT (id) DO UPDATE SET
	    email = EXCLUDED.email,
	    full_name = EXCLUDED.full_name,
	    role = EXCLUDED.role,
	    role_id = EXCLUDED.role_id,
	    active_store_id = EXCLUDED.active_store_id,
	    plan = EXCLUDED.plan,
	    is_active = true,
	    deleted_at = NULL,
	    deletion_reason = NULL,
	    deleted_by = NULL,
	    updated_at = now()
	  RETURNING id INTO v_user_id;
	
	  -- Procesar memberships
	  IF p_memberships IS NOT NULL THEN
	    IF v_creator_role != 'admin' AND v_creator_role != 'superadmin' THEN
	      DELETE FROM public.user_store_memberships
	        WHERE user_id = v_user_id
	        AND store_id IN (
	          SELECT store_id FROM public.user_store_memberships
	          WHERE user_id = v_creator_uid AND role IN ('encargado', 'manager')
	        );
	    ELSE
	      DELETE FROM public.user_store_memberships WHERE user_id = v_user_id;
	    END IF;
	
	    FOR m IN SELECT * FROM jsonb_array_elements(p_memberships) LOOP
	      IF (m->>'store_id') IS NOT NULL AND (m->>'store_id') <> '' THEN
	        IF v_creator_role IN ('admin', 'superadmin') OR public.has_store_access((m->>'store_id')::UUID) THEN
	          INSERT INTO public.user_store_memberships (user_id, store_id, role)
	          VALUES (v_user_id, (m->>'store_id')::UUID, (m->>'role')::public.user_role)
	          ON CONFLICT (user_id, store_id) DO UPDATE SET role = EXCLUDED.role, status = 'active';
	        END IF;
	      END IF;
	    END LOOP;
	  ELSIF p_store_id IS NOT NULL THEN
	    INSERT INTO public.user_store_memberships (user_id, store_id, role)
	    VALUES (v_user_id, p_store_id, p_role)
	    ON CONFLICT (user_id, store_id) DO UPDATE SET role = EXCLUDED.role, status = 'active';
	  END IF;
	
	  -- Audit log atómico (regresión C-2 restaurada)
	  INSERT INTO public.user_audit_log (performed_by, target_user_id, action, new_values, metadata)
	  VALUES (
	    v_creator_uid, v_user_id, 'USER_CREATED',
	    jsonb_build_object(
	      'email', p_email, 'full_name', p_full_name, 'role', p_role::text,
	      'plan', p_plan::text, 'active_store_id', v_active_store_id,
	      'max_stores', p_max_stores, 'max_users', p_max_users
	    ),
	    jsonb_build_object('memberships_count', CASE WHEN p_memberships IS NOT NULL THEN jsonb_array_length(p_memberships) ELSE 0 END)
	  );
	
	  RETURN jsonb_build_object('success', true, 'user_id', v_user_id);
	END;
	$function$
2026-09-13 06:19:18.759 UTC [23412] ERROR:  type "public.user_role" does not exist at character 392
2026-09-13 06:19:18.759 UTC [23412] STATEMENT:  CREATE OR REPLACE FUNCTION public.managed_reset_password(
	  p_user_id uuid,
	  p_caller_id uuid DEFAULT NULL::uuid
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  -- FIX H-7: anti-spoofing
	  v_caller_uid uuid := CASE WHEN auth.role() = 'service_role'
	    THEN COALESCE(p_caller_id, auth.uid()) ELSE auth.uid() END;
	  v_caller_role public.user_role;
	  v_target_email text;
	BEGIN
	  SELECT role INTO v_caller_role FROM public.profiles WHERE id = v_caller_uid;
	  IF v_caller_role IS NULL OR v_caller_role NOT IN ('admin', 'superadmin') THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED';
	  END IF;
	
	  IF p_user_id = v_caller_uid THEN
	    RAISE EXCEPTION 'ERR_SELF_RESET_BLOCKED';
	  END IF;
	
	  SELECT email INTO v_target_email FROM public.profiles WHERE id = p_user_id AND deleted_at IS NULL;
	  IF v_target_email IS NULL THEN
	    RAISE EXCEPTION 'ERR_USER_NOT_FOUND';
	  END IF;
	
	  INSERT INTO public.user_audit_log (performed_by, target_user_id, action, metadata)
	  VALUES (
	    v_caller_uid, p_user_id, 'PASSWORD_RESET_REQUESTED',
	    jsonb_build_object('email', v_target_email, 'method', 'recovery_link')
	  );
	
	  RETURN jsonb_build_object('success', true, 'email', v_target_email);
	END;
	$function$
2026-09-13 06:19:18.819 UTC [23423] ERROR:  type "public.user_role" does not exist at character 427
2026-09-13 06:19:18.819 UTC [23423] STATEMENT:  CREATE OR REPLACE FUNCTION public.managed_soft_delete_user(
	  p_user_id uuid,
	  p_reason text,
	  p_caller_id uuid DEFAULT NULL::uuid
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  v_old RECORD;
	  -- FIX H-7: anti-spoofing
	  v_caller_uid uuid := CASE WHEN auth.role() = 'service_role'
	    THEN COALESCE(p_caller_id, auth.uid()) ELSE auth.uid() END;
	  v_caller_role public.user_role;
	  v_active_memberships_count int;
	  v_anon_email text;
	BEGIN
	  SELECT role INTO v_caller_role FROM public.profiles WHERE id = v_caller_uid;
	  IF v_caller_role IS NULL OR v_caller_role NOT IN ('admin', 'superadmin') THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED';
	  END IF;
	
	  IF p_user_id = v_caller_uid THEN
	    RAISE EXCEPTION 'ERR_SELF_DELETE_BLOCKED';
	  END IF;
	
	  SELECT id, email, full_name, role, plan, is_active INTO v_old
	    FROM public.profiles WHERE id = p_user_id AND deleted_at IS NULL FOR UPDATE;
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_USER_NOT_FOUND_OR_ALREADY_DELETED';
	  END IF;
	
	  SELECT COUNT(*) INTO v_active_memberships_count
	    FROM public.user_store_memberships
	    WHERE user_id = p_user_id AND status = 'active';
	  IF v_active_memberships_count > 0 THEN
	    RAISE EXCEPTION 'ERR_USER_HAS_ACTIVE_MEMBERSHIPS: % active. Revoke memberships first.', v_active_memberships_count;
	  END IF;
	
	  v_anon_email := 'deleted+' || substr(p_user_id::text, 1, 8) || '@anonymized.local';
	
	  UPDATE public.profiles SET
	    deleted_at = now(),
	    deletion_reason = p_reason,
	    deleted_by = v_caller_uid,
	    is_active = false,
	    full_name = '[deleted user]',
	    email = v_anon_email,
	    ai_api_key = NULL,
	    updated_at = now()
	  WHERE id = p_user_id;
	
	  UPDATE public.user_store_memberships SET
	    status = 'revoked',
	    updated_at = now()
	  WHERE user_id = p_user_id AND status = 'active';
	
	  INSERT INTO public.user_audit_log (performed_by, target_user_id, action, old_values, new_values, metadata)
	  VALUES (
	    v_caller_uid, p_user_id, 'USER_SOFT_DELETED',
	    jsonb_build_object(
	      'email', v_old.email, 'full_name', v_old.full_name,
	      'role', v_old.role::text, 'plan', v_old.plan::text, 'is_active', v_old.is_active
	    ),
	    jsonb_build_object(
	      'email', v_anon_email, 'full_name', '[deleted user]',
	      'is_active', false, 'deleted_at', now()
	    ),
	    jsonb_build_object('reason', p_reason, 'memberships_revoked', v_active_memberships_count)
	  );
	
	  RETURN jsonb_build_object(
	    'success', true,
	    'user_id', p_user_id,
	    'status', 'soft_deleted',
	    'note', 'auth.users preserved. API route should ban via auth.admin.updateUser.'
	  );
	END;
	$function$
2026-09-13 06:19:18.874 UTC [23434] ERROR:  type "public.user_role" does not exist at character 435
2026-09-13 06:19:18.874 UTC [23434] STATEMENT:  CREATE OR REPLACE FUNCTION public.managed_toggle_user_status(
	  p_user_id uuid,
	  p_is_active boolean,
	  p_caller_id uuid DEFAULT NULL::uuid
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  v_old RECORD;
	  -- FIX H-7: anti-spoofing
	  v_caller_uid uuid := CASE WHEN auth.role() = 'service_role'
	    THEN COALESCE(p_caller_id, auth.uid()) ELSE auth.uid() END;
	  v_caller_role public.user_role;
	BEGIN
	  SELECT role INTO v_caller_role FROM public.profiles WHERE id = v_caller_uid;
	  IF v_caller_role IS NULL OR v_caller_role NOT IN ('admin', 'superadmin', 'encargado', 'manager') THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED';
	  END IF;
	
	  IF p_is_active = false AND p_user_id = v_caller_uid THEN
	    RAISE EXCEPTION 'ERR_SELF_DEACTIVATE_BLOCKED';
	  END IF;
	
	  SELECT id, is_active, full_name INTO v_old
	    FROM public.profiles WHERE id = p_user_id AND deleted_at IS NULL FOR UPDATE;
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_USER_NOT_FOUND';
	  END IF;
	
	  IF v_old.is_active = p_is_active THEN
	    RETURN jsonb_build_object('success', true, 'no_change', true);
	  END IF;
	
	  UPDATE public.profiles SET is_active = p_is_active, updated_at = now() WHERE id = p_user_id;
	
	  INSERT INTO public.user_audit_log (performed_by, target_user_id, action, old_values, new_values)
	  VALUES (
	    v_caller_uid, p_user_id,
	    CASE WHEN p_is_active THEN 'USER_ACTIVATED' ELSE 'USER_DEACTIVATED' END,
	    jsonb_build_object('is_active', v_old.is_active),
	    jsonb_build_object('is_active', p_is_active)
	  );
	
	  RETURN jsonb_build_object('success', true, 'user_id', p_user_id, 'is_active', p_is_active);
	END;
	$function$
2026-09-13 06:19:18.900 UTC [23441] ERROR:  type public.user_role does not exist
2026-09-13 06:19:18.900 UTC [23441] STATEMENT:  CREATE OR REPLACE FUNCTION public.managed_update_membership(
	  p_membership_id uuid,
	  p_role public.user_role DEFAULT NULL::user_role,
	  p_status text DEFAULT NULL::text,
	  p_caller_id uuid DEFAULT NULL::uuid
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  v_old RECORD;
	  -- FIX H-7: anti-spoofing
	  v_caller_uid uuid := CASE WHEN auth.role() = 'service_role'
	    THEN COALESCE(p_caller_id, auth.uid()) ELSE auth.uid() END;
	  v_changes jsonb := '{}'::jsonb;
	BEGIN
	  SELECT m.id, m.user_id, m.store_id, m.role, m.status
	    INTO v_old
	    FROM public.user_store_memberships m
	    WHERE m.id = p_membership_id
	    FOR UPDATE;
	
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_MEMBERSHIP_NOT_FOUND';
	  END IF;
	
	  -- FIX H-7: Autorización usa nueva sobrecarga has_store_role(p_user_id, p_store_id, p_roles)
	  -- service_role (API route) pasa p_caller_id=session.user.id → verifica acceso de ese user
	  -- authenticated pasa p_caller_id=NULL → v_caller_uid=auth.uid() → verifica su propio acceso
	  IF NOT public.has_store_role(v_caller_uid, v_old.store_id, ARRAY['admin', 'manager']) THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED: Caller must be admin or manager of the store.';
	  END IF;
	
	  IF p_role IS NOT NULL AND p_role <> v_old.role THEN
	    v_changes := v_changes || jsonb_build_object('role', jsonb_build_object('old', v_old.role::text, 'new', p_role::text));
	    UPDATE public.user_store_memberships SET role = p_role, updated_at = now() WHERE id = p_membership_id;
	  END IF;
	
	  IF p_status IS NOT NULL AND p_status <> v_old.status::text THEN
	    IF p_status NOT IN ('active', 'revoked') THEN
	      RAISE EXCEPTION 'ERR_INVALID_STATUS: %', p_status;
	    END IF;
	    v_changes := v_changes || jsonb_build_object('status', jsonb_build_object('old', v_old.status, 'new', p_status));
	    UPDATE public.user_store_memberships SET status = p_status::membership_status, updated_at = now() WHERE id = p_membership_id;
	  END IF;
	
	  IF v_changes <> '{}'::jsonb THEN
	    INSERT INTO public.user_audit_log (performed_by, target_user_id, action, old_values, new_values, metadata)
	    VALUES (
	      v_caller_uid, v_old.user_id, 'MEMBERSHIP_UPDATED',
	      jsonb_build_object('membership_id', p_membership_id, 'store_id', v_old.store_id, 'role', v_old.role::text, 'status', v_old.status),
	      v_changes,
	      jsonb_build_object('store_id', v_old.store_id)
	    );
	  END IF;
	
	  RETURN jsonb_build_object('success', true, 'membership_id', p_membership_id, 'changes', v_changes);
	END;
	$function$
2026-09-13 06:19:18.927 UTC [23448] ERROR:  type plan_t does not exist
2026-09-13 06:19:18.927 UTC [23448] STATEMENT:  CREATE OR REPLACE FUNCTION public.managed_update_tenant_plan(
	  p_tenant_id uuid,
	  p_plan plan_t,
	  p_subscription_status text DEFAULT NULL,
	  p_caller_id uuid DEFAULT NULL
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  -- FIX H-7: anti-spoofing — service_role (Stripe webhook) puede pasar NULL caller_id
	  v_caller_uid uuid := CASE WHEN auth.role() = 'service_role'
	    THEN COALESCE(p_caller_id, auth.uid()) ELSE auth.uid() END;
	  v_old_plan plan_t;
	  v_old_status text;
	  v_role public.user_role;
	BEGIN
	  -- Validación: si caller_uid es NULL (service_role anónimo para Stripe webhook), permitir
	  -- Si caller_uid NO es NULL, debe ser admin/superadmin
	  IF v_caller_uid IS NOT NULL THEN
	    SELECT role INTO v_role FROM public.profiles WHERE id = v_caller_uid;
	    IF v_role IS NULL OR v_role NOT IN ('admin', 'superadmin') THEN
	      RAISE EXCEPTION 'ERR_UNAUTHORIZED: Only admins can change tenant plan';
	    END IF;
	  END IF;
	
	  SELECT plan, subscription_status INTO v_old_plan, v_old_status
	    FROM public.tenants WHERE id = p_tenant_id FOR UPDATE;
	
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_TENANT_NOT_FOUND: %', p_tenant_id;
	  END IF;
	
	  UPDATE public.tenants SET
	    plan = p_plan,
	    subscription_status = COALESCE(p_subscription_status, subscription_status),
	    updated_at = now()
	  WHERE id = p_tenant_id;
	
	  UPDATE public.profiles SET
	    plan = p_plan,
	    updated_at = now()
	  WHERE tenant_id = p_tenant_id AND deleted_at IS NULL;
	
	  INSERT INTO public.user_audit_log (performed_by, target_user_id, action, old_values, new_values, metadata)
	  VALUES (
	    v_caller_uid, NULL, 'TENANT_PLAN_UPDATED',
	    jsonb_build_object('old_plan', v_old_plan::text, 'old_status', v_old_status),
	    jsonb_build_object('new_plan', p_plan::text, 'new_status', COALESCE(p_subscription_status, v_old_status)),
	    jsonb_build_object('tenant_id', p_tenant_id)
	  );
	
	  RETURN jsonb_build_object(
	    'success', true,
	    'tenant_id', p_tenant_id,
	    'old_plan', v_old_plan::text,
	    'new_plan', p_plan::text
	  );
	END;
	$function$
2026-09-13 06:19:18.953 UTC [23455] ERROR:  type public.user_role does not exist
2026-09-13 06:19:18.953 UTC [23455] STATEMENT:  CREATE OR REPLACE FUNCTION public.managed_update_user(
	  p_user_id uuid,
	  p_full_name text DEFAULT NULL::text,
	  p_role public.user_role DEFAULT NULL::user_role,
	  p_role_id uuid DEFAULT NULL::uuid,
	  p_is_active boolean DEFAULT NULL::boolean,
	  p_max_stores_limit int DEFAULT NULL::int,
	  p_max_users_limit int DEFAULT NULL::int,
	  p_plan plan_t DEFAULT NULL::plan_t,
	  p_caller_id uuid DEFAULT NULL::uuid
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  v_old RECORD;
	  -- FIX H-7: anti-spoofing — service_role puede pasar p_caller_id explícito
	  v_caller_uid uuid := CASE WHEN auth.role() = 'service_role'
	    THEN COALESCE(p_caller_id, auth.uid()) ELSE auth.uid() END;
	  v_caller_role public.user_role;
	  v_changes jsonb := '{}'::jsonb;
	BEGIN
	  -- Validar caller es admin (usa v_caller_uid que ahora NO es spoofable para authenticated)
	  SELECT role INTO v_caller_role FROM public.profiles WHERE id = v_caller_uid;
	  IF v_caller_role IS NULL OR v_caller_role NOT IN ('admin', 'superadmin') THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED: Only admins can update users.';
	  END IF;
	
	  SELECT * INTO v_old FROM public.profiles WHERE id = p_user_id AND deleted_at IS NULL FOR UPDATE;
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_USER_NOT_FOUND: %', p_user_id;
	  END IF;
	
	  IF p_is_active = false AND p_user_id = v_caller_uid THEN
	    RAISE EXCEPTION 'ERR_SELF_DEACTIVATE_BLOCKED: Cannot deactivate own account.';
	  END IF;
	
	  IF p_full_name IS NOT NULL AND p_full_name <> v_old.full_name THEN
	    v_changes := v_changes || jsonb_build_object('full_name', jsonb_build_object('old', v_old.full_name, 'new', p_full_name));
	    UPDATE public.profiles SET full_name = p_full_name, updated_at = now() WHERE id = p_user_id;
	  END IF;
	
	  IF p_role IS NOT NULL AND p_role <> v_old.role THEN
	    v_changes := v_changes || jsonb_build_object('role', jsonb_build_object('old', v_old.role::text, 'new', p_role::text));
	    UPDATE public.profiles SET role = p_role, updated_at = now() WHERE id = p_user_id;
	  END IF;
	
	  IF p_role_id IS NOT NULL AND (v_old.role_id IS NULL OR p_role_id <> v_old.role_id) THEN
	    v_changes := v_changes || jsonb_build_object('role_id', jsonb_build_object('old', v_old.role_id, 'new', p_role_id));
	    UPDATE public.profiles SET role_id = p_role_id, updated_at = now() WHERE id = p_user_id;
	  END IF;
	
	  IF p_is_active IS NOT NULL AND p_is_active <> v_old.is_active THEN
	    v_changes := v_changes || jsonb_build_object('is_active', jsonb_build_object('old', v_old.is_active, 'new', p_is_active));
	    UPDATE public.profiles SET is_active = p_is_active, updated_at = now() WHERE id = p_user_id;
	  END IF;
	
	  IF p_max_stores_limit IS NOT NULL AND (v_old.max_stores_limit IS NULL OR p_max_stores_limit <> v_old.max_stores_limit) THEN
	    v_changes := v_changes || jsonb_build_object('max_stores_limit', jsonb_build_object('old', v_old.max_stores_limit, 'new', p_max_stores_limit));
	    UPDATE public.profiles SET max_stores_limit = p_max_stores_limit, updated_at = now() WHERE id = p_user_id;
	  END IF;
	
	  IF p_max_users_limit IS NOT NULL AND (v_old.max_users_limit IS NULL OR p_max_users_limit <> v_old.max_users_limit) THEN
	    v_changes := v_changes || jsonb_build_object('max_users_limit', jsonb_build_object('old', v_old.max_users_limit, 'new', p_max_users_limit));
	    UPDATE public.profiles SET max_users_limit = p_max_users_limit, updated_at = now() WHERE id = p_user_id;
	  END IF;
	
	  IF p_plan IS NOT NULL AND p_plan <> v_old.plan THEN
	    v_changes := v_changes || jsonb_build_object('plan', jsonb_build_object('old', v_old.plan::text, 'new', p_plan::text));
	    UPDATE public.profiles SET plan = p_plan, updated_at = now() WHERE id = p_user_id;
	  END IF;
	
	  IF v_changes <> '{}'::jsonb THEN
	    INSERT INTO public.user_audit_log (performed_by, target_user_id, action, old_values, new_values, metadata)
	    VALUES (
	      v_caller_uid, p_user_id, 'USER_UPDATED',
	      jsonb_build_object(
	        'full_name', v_old.full_name, 'role', v_old.role::text,
	        'is_active', v_old.is_active, 'plan', v_old.plan::text
	      ),
	      v_changes,
	      jsonb_build_object('fields_changed', jsonb_object_keys(v_changes))
	    );
	  END IF;
	
	  RETURN jsonb_build_object('success', true, 'user_id', p_user_id, 'changes', v_changes);
	END;
	$function$
2026-09-13 06:19:19.067 UTC [23470] ERROR:  type "user_role" does not exist at character 204
2026-09-13 06:19:19.067 UTC [23470] STATEMENT:  CREATE OR REPLACE FUNCTION public.on_auth_user_created()
	RETURNS trigger
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO 'public', 'extensions'
	AS $function$
	DECLARE
	  v_role_id UUID;
	  v_role_enum USER_ROLE;
	  v_metadata_role TEXT;
	  v_company_name TEXT;
	  v_full_name TEXT;
	  v_tenant_id UUID;
	BEGIN
	  -- Extract role from metadata if present
	  v_metadata_role := NEW.raw_user_meta_data->>'role';
	
	  -- Normalize and convert to enum
	  IF v_metadata_role IS NOT NULL THEN
	    BEGIN
	      v_role_enum := v_metadata_role::USER_ROLE;
	    EXCEPTION WHEN OTHERS THEN
	      v_role_enum := 'tenant_admin'::USER_ROLE;
	    END;
	  ELSE
	    -- Iteración 13: self-signup users are tenant_admin by default
	    v_role_enum := 'tenant_admin'::USER_ROLE;
	  END IF;
	
	  -- Extract company_name and full_name from metadata
	  v_company_name := NEW.raw_user_meta_data->>'company_name';
	  v_full_name := COALESCE(NEW.raw_user_meta_data->>'full_name', split_part(NEW.email, '@', 1));
	
	  -- Iteración 13: Create tenant for self-signup users
	  -- If user was created by admin (via managed_create_user_v2), they may already
	  -- have a tenant_id set via p_target_user_id. In that case, skip tenant creation.
	  IF v_company_name IS NOT NULL THEN
	    INSERT INTO public.tenants (name, owner_id, plan, subscription_status, trial_ends_at, is_active)
	    VALUES (
	      v_company_name,
	      NEW.id,
	      'free'::plan_t,
	      'trial',
	      now() + interval '14 days',
	      true
	    )
	    RETURNING id INTO v_tenant_id;
	  END IF;
	
	  -- Get matching role_id from public.roles
	  SELECT id INTO v_role_id FROM public.roles
	    WHERE name = v_metadata_role OR lower(name) = lower(v_role_enum::text)
	    LIMIT 1;
	
	  -- Insert profile with tenant_id
	  INSERT INTO public.profiles (id, email, full_name, role, role_id, is_active, tenant_id, plan, created_at, updated_at)
	  VALUES (
	    NEW.id,
	    NEW.email,
	    v_full_name,
	    v_role_enum,
	    v_role_id,
	    true,
	    v_tenant_id,
	    'free'::plan_t,
	    now(),
	    now()
	  )
	  ON CONFLICT (id) DO UPDATE SET
	    email = EXCLUDED.email,
	    full_name = EXCLUDED.full_name,
	    role = EXCLUDED.role,
	    role_id = EXCLUDED.role_id,
	    tenant_id = COALESCE(profiles.tenant_id, EXCLUDED.tenant_id),
	    plan = EXCLUDED.plan,
	    is_active = true,
	    updated_at = now();
	
	  -- Audit log
	  INSERT INTO public.user_audit_log (performed_by, target_user_id, action, new_values, metadata)
	  VALUES (
	    NEW.id, NEW.id, 'USER_REGISTERED_PUBLICLY',
	    jsonb_build_object('email', NEW.email, 'role', v_role_enum::text, 'tenant_id', v_tenant_id),
	    jsonb_build_object('company_name', v_company_name, 'source', 'self_signup')
	  );
	
	  RETURN NEW;
	END;
	$function$
2026-09-13 06:19:19.336 UTC [23517] ERROR:  type adjustment_item[] does not exist
2026-09-13 06:19:19.336 UTC [23517] STATEMENT:  CREATE OR REPLACE FUNCTION public.process_inventory_adjustment(p_store_id uuid, p_cashier_id uuid, p_items adjustment_item[], p_operation_date timestamp with time zone DEFAULT NULL::timestamp with time zone)
	 RETURNS uuid
	 LANGUAGE plpgsql
	 SECURITY DEFINER
	 SET search_path TO 'public'
	AS $function$
	DECLARE
	  v_adjustment_id UUID;
	  v_item public.adjustment_item;
	  v_difference NUMERIC;
	  v_effective_date TIMESTAMP WITH TIME ZONE := COALESCE(p_operation_date, NOW());
	BEGIN
	  -- Validación forward-only locking
	  PERFORM public.validate_operation_date(p_operation_date, p_store_id);
	
	  INSERT INTO public.inventory_adjustments (store_id, created_by, status, created_at)
	  VALUES (p_store_id, p_cashier_id, 'PROCESSING', v_effective_date)
	  RETURNING id INTO v_adjustment_id;
	
	  FOREACH v_item IN ARRAY p_items
	  LOOP
	    v_difference := v_item.counted_quantity - v_item.expected_quantity;
	    INSERT INTO public.inventory_adjustment_items (adjustment_id, product_id, expected_quantity, counted_quantity, created_at)
	    VALUES (v_adjustment_id, v_item.product_id, v_item.expected_quantity, v_item.counted_quantity, v_effective_date);
	
	    PERFORM public.register_stock_movement(
	        p_product_id := v_item.product_id,
	        p_store_id := p_store_id,
	        p_user_id := p_cashier_id,
	        p_quantity := v_difference,
	        p_movement_type := 'adjustment',
	        p_operation_date := v_effective_date
	    );
	  END LOOP;
	
	  UPDATE public.inventory_adjustments SET status = 'COMPLETED', updated_at = v_effective_date
	  WHERE id = v_adjustment_id;
	  RETURN v_adjustment_id;
	END;
	$function$
2026-09-13 06:19:19.525 UTC [23540] ERROR:  type "public.purchase_status_enum" does not exist at character 425
2026-09-13 06:19:19.525 UTC [23540] STATEMENT:  CREATE OR REPLACE FUNCTION public.receive_against_po(
	  p_po_id uuid,
	  p_received_items jsonb DEFAULT '[]'::jsonb,
	  p_user_id uuid DEFAULT NULL,
	  p_reception_date timestamptz DEFAULT now(),
	  p_invoice_number text DEFAULT NULL
	) RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO 'public', 'extensions'
	AS $func$
	DECLARE
	  v_store_id            uuid;
	  v_supplier_name       text;
	  v_po_status           public.purchase_status_enum;
	  v_item                jsonb;
	  v_item_id             uuid;
	  v_qty                 numeric;
	  v_qty_ordered         numeric;
	  v_qty_received_before numeric;
	  v_product_id          uuid;
	  v_unit_cost           numeric;
	  v_product_name        text;
	  v_all_received        boolean;
	  v_any_received        boolean;
	  v_new_status          public.purchase_status_enum;
	  v_receipt_id          uuid;
	  v_reception_items     jsonb := '[]'::jsonb;
	  v_count               integer := 0;
	  v_po_number           text;
	  v_invoice_to_pass     text;
	  v_sorted_items        jsonb;
	BEGIN
	  -- ─── 1. Cargar OC con lock exclusivo ───
	  SELECT po.store_id, po.supplier_name, po.status, po.po_number
	    INTO v_store_id, v_supplier_name, v_po_status, v_po_number
	  FROM public.purchase_orders po
	  WHERE po.id = p_po_id
	  FOR UPDATE;
	
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_PO_NOT_FOUND';
	  END IF;
	
	  -- ─── 2. Validar acceso (tenant-aware via has_store_access) ───
	  IF NOT public.has_store_access(v_store_id) THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED';
	  END IF;
	
	  -- ─── 3. Validar estado de la OC ───
	  IF v_po_status = 'cancelled' THEN
	    RAISE EXCEPTION 'ERR_PO_CANCELLED';
	  END IF;
	  -- Solo se puede recibir contra draft, sent o partial.
	  IF v_po_status NOT IN ('draft', 'sent', 'partial') THEN
	    RAISE EXCEPTION 'ERR_PO_NOT_RECEIVABLE: status % is terminal', v_po_status;
	  END IF;
	
	  -- ─── 4. Validar items no vacío (parity B3 register_reception) ───
	  IF p_received_items IS NULL OR jsonb_array_length(p_received_items) = 0 THEN
	    RAISE EXCEPTION 'ERR_EMPTY_ITEMS';
	  END IF;
	
	  -- ─── 5. Condición #5 (anti-deadlock): Ordenar items por po_item_id ASC ───
	  SELECT jsonb_agg(elem ORDER BY (elem->>'po_item_id'))
	    INTO v_sorted_items
	  FROM jsonb_array_elements(p_received_items) AS elem;
	
	  -- ─── 6. Procesar cada item: VALIDAR antes de actualizar ───
	  FOR v_item IN SELECT * FROM jsonb_array_elements(v_sorted_items) LOOP
	    v_item_id := NULLIF(v_item->>'po_item_id', '')::uuid;
	    v_qty     := COALESCE((v_item->>'quantity_received')::numeric, 0);
	
	    -- Validar po_item_id presente
	    IF v_item_id IS NULL THEN
	      RAISE EXCEPTION 'ERR_ITEM_ID_REQUIRED: missing po_item_id in received_items';
	    END IF;
	
	    -- Medio #6: qty > 0
	    IF v_qty <= 0 THEN
	      RAISE EXCEPTION 'ERR_NEGATIVE_QTY: qty % for item % must be > 0', v_qty, v_item_id;
	    END IF;
	
	    -- Cargar estado actual del item con lock FOR UPDATE
	    SELECT product_id, quantity_ordered, quantity_received, unit_cost, product_name
	      INTO v_product_id, v_qty_ordered, v_qty_received_before, v_unit_cost, v_product_name
	    FROM public.purchase_order_items
	    WHERE id = v_item_id AND po_id = p_po_id
	    FOR UPDATE;
	
	    IF NOT FOUND THEN
	      RAISE EXCEPTION 'ERR_ITEM_NOT_FOUND: %', v_item_id;
	    END IF;
	
	    -- Alto #5: over-receive check
	    IF (v_qty_received_before + v_qty) > v_qty_ordered THEN
	      RAISE EXCEPTION 'ERR_OVER_RECEIVE: item % ordered %, already received %, attempting %',
	        v_item_id, v_qty_ordered, v_qty_received_before, v_qty;
	    END IF;
	
	    -- Condición #1: todo item debe tener product_id NOT NULL
	    IF v_product_id IS NULL THEN
	      RAISE EXCEPTION 'ERR_PRODUCT_ID_REQUIRED: item % has no product_id', v_item_id;
	    END IF;
	
	    -- Medio #9: producto debe existir en store
	    IF NOT EXISTS (
	      SELECT 1 FROM public.products p
	      WHERE p.id = v_product_id AND p.store_id = v_store_id
	    ) THEN
	      RAISE EXCEPTION 'ERR_PRODUCT_NOT_IN_STORE: product % not in store %',
	        v_product_id, v_store_id;
	    END IF;
	
	    -- Acumular para register_reception (formato esperado: product_id, quantity, unit_cost, moneda, tasa)
	    v_reception_items := v_reception_items || jsonb_build_object(
	      'product_id',   v_product_id,
	      'product_name', v_product_name,
	      'quantity',     v_qty,
	      'unit_cost',    v_unit_cost,
	      'moneda_recepcion', 'CUP',
	      'tasa_cambio_recepcion', 1.0
	    );
	    v_count := v_count + 1;
	  END LOOP;
	
	  -- ─── 7. Condición #3: reference_doc NULL si no viene invoice_number ───
	  -- Evita colisión con UNIQUE INDEX idx_receipts_store_reference_doc
	  -- (store_id, reference_doc) en partial indexes.
	  -- El vínculo real receipt↔PO es receipts.po_id (G6).
	  IF p_invoice_number IS NULL OR p_invoice_number = '' THEN
	    v_invoice_to_pass := NULL;
	  ELSE
	    v_invoice_to_pass := p_invoice_number;
	  END IF;
	
	  -- ─── 8. Crear receipt vinculado vía register_reception ───
	  -- register_reception aplica las 8 validaciones B1-B5 + C1-C3 del v2.23.0
	  -- (no se relajan). G6 añade p_po_id opcional.
	  v_receipt_id := public.register_reception(
	    p_store_id        := v_store_id,
	    p_supplier        := v_supplier_name,
	    p_reception_date  := p_reception_date,
	    p_invoice_number  := v_invoice_to_pass,
	    p_items           := v_reception_items,
	    p_user_id         := p_user_id,
	    p_po_id           := p_po_id
	  );
	
	  -- ─── 9. Actualizar quantity_received en purchase_order_items ───
	  -- (SECURITY DEFINER bypasses RLS DENY UPDATE de G5)
	  FOR v_item IN SELECT * FROM jsonb_array_elements(v_sorted_items) LOOP
	    v_item_id := NULLIF(v_item->>'po_item_id', '')::uuid;
	    v_qty     := (v_item->>'quantity_received')::numeric;
	
	    UPDATE public.purchase_order_items
	    SET quantity_received = quantity_received + v_qty
	    WHERE id = v_item_id AND po_id = p_po_id;
	  END LOOP;
	
	  -- ─── 10. Recalcular status global (cast EXPLÍCITO a enum) ───
	  SELECT
	    BOOL_AND(quantity_received >= quantity_ordered),
	    BOOL_OR(quantity_received > 0)
	  INTO v_all_received, v_any_received
	  FROM public.purchase_order_items WHERE po_id = p_po_id;
	
	  v_new_status :=
	    CASE
	      WHEN v_all_received THEN 'received'::public.purchase_status_enum
	      WHEN v_any_received THEN 'partial'::public.purchase_status_enum
	      ELSE 'sent'::public.purchase_status_enum
	    END;
	
	  -- ─── 11. Actualizar OC + vincular receipt en notas ───
	  UPDATE public.purchase_orders
	  SET status       = v_new_status,
	      received_at  = CASE WHEN v_new_status = 'received' THEN NOW() ELSE received_at END,
	      notes        = COALESCE(notes, '') ||
	                     CASE WHEN COALESCE(notes, '') = '' THEN '' ELSE E'\n' END ||
	                     '[Receipt ' || v_receipt_id::text || ' linked at ' || NOW()::text || ']'
	  WHERE id = p_po_id;
	
	  -- ─── 12. CxP (Grupo 6): marcar receipt con payment_status='unpaid' ───
	  -- Solo si aún no está seteado (no sobrescribir si register_reception ya lo puso).
	  UPDATE public.receipts
	  SET payment_status     = COALESCE(NULLIF(payment_status, ''), 'unpaid'),
	      payment_terms_days = COALESCE(payment_terms_days, 30),
	      due_date           = COALESCE(due_date, (p_reception_date::date + INTERVAL '30 days')::date)
	  WHERE id = v_receipt_id;
	
	  -- ─── 13. Auditoría ───
	  INSERT INTO public.audit_logs (user_id, store_id, action, table_name, record_id, metadata)
	  VALUES (
	    p_user_id, v_store_id, 'po_received', 'purchase_orders', p_po_id,
	    jsonb_build_object(
	      'new_status', v_new_status::text,
	      'items_received', v_count,
	      'receipt_id', v_receipt_id,
	      'po_number', v_po_number,
	      'invoice_number', v_invoice_to_pass
	    )
	  );
	
	  RETURN jsonb_build_object(
	    'status', 'success',
	    'po_status', v_new_status::text,
	    'receipt_id', v_receipt_id,
	    'items_received', v_count
	  );
	END;
	$func$
2026-09-13 06:19:20.420 UTC [23675] ERROR:  type purchase_status_enum does not exist
2026-09-13 06:19:20.420 UTC [23675] STATEMENT:  CREATE OR REPLACE FUNCTION public.set_purchase_order_status(p_po_id uuid, p_new_status purchase_status_enum, p_user_id uuid DEFAULT NULL::uuid, p_reason text DEFAULT NULL::text)
	 RETURNS jsonb
	 LANGUAGE plpgsql
	 SECURITY DEFINER
	 SET search_path TO 'public', 'extensions'
	AS $function$
	DECLARE
	  v_store_id     uuid;
	  v_current      public.purchase_status_enum;
	  v_po_number    text;
	  v_allowed      text[];
	  v_is_allowed   boolean := false;
	BEGIN
	  -- ─── 1. Cargar PO con lock exclusivo ───
	  SELECT store_id, status, po_number
	    INTO v_store_id, v_current, v_po_number
	  FROM public.purchase_orders
	  WHERE id = p_po_id
	  FOR UPDATE;
	
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_PO_NOT_FOUND';
	  END IF;
	
	  -- ─── 2. Validar acceso (tenant-aware) ───
	  IF NOT public.has_store_access(v_store_id) THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED';
	  END IF;
	
	  -- ─── 3. Si no hay cambio, retornar success sin hacer nada ───
	  IF v_current = p_new_status THEN
	    RETURN jsonb_build_object(
	      'status', 'no_change',
	      'po_status', v_current::text,
	      'po_number', v_po_number
	    );
	  END IF;
	
	  -- ─── 4. Definir transiciones permitidas (state machine) ───
	  -- 'partial' y 'received' NUNCA son destino válido manualmente
	  -- (solo vía receive_against_po).
	  v_allowed := CASE v_current
	    WHEN 'draft'   THEN ARRAY['sent', 'cancelled']
	    WHEN 'sent'    THEN ARRAY['cancelled']
	    WHEN 'partial' THEN ARRAY['cancelled']
	    ELSE ARRAY[]::text[]  -- received, cancelled: terminal, no transitions
	  END;
	
	  -- ─── 5. Verificar transición permitida ───
	  SELECT EXISTS(SELECT 1 FROM unnest(v_allowed) a WHERE a = p_new_status::text)
	    INTO v_is_allowed;
	
	  IF NOT v_is_allowed THEN
	    RAISE EXCEPTION 'ERR_INVALID_TRANSITION: % → % not allowed (allowed: %)',
	      v_current::text, p_new_status::text,
	      CASE WHEN array_length(v_allowed, 1) IS NULL THEN 'none' ELSE array_to_string(v_allowed, ', ') END;
	  END IF;
	
	  -- ─── 6. Aplicar transición ───
	  UPDATE public.purchase_orders
	  SET status      = p_new_status,
	      received_at = CASE WHEN p_new_status = 'received' THEN NOW() ELSE received_at END
	  WHERE id = p_po_id;
	
	  -- ─── 7. Auditoría (action = PO_STATUS_CHANGED) ───
	  INSERT INTO public.audit_logs (user_id, store_id, action, table_name, record_id, metadata)
	  VALUES (
	    CASE WHEN auth.role() = 'service_role' THEN COALESCE(p_user_id, auth.uid()) ELSE auth.uid() END, v_store_id, 'PO_STATUS_CHANGED', 'purchase_orders', p_po_id,
	    jsonb_build_object(
	      'po_number', v_po_number,
	      'from_status', v_current::text,
	      'to_status', p_new_status::text,
	      'reason', p_reason
	    )
	  );
	
	  RETURN jsonb_build_object(
	    'status', 'success',
	    'po_status', p_new_status::text,
	    'po_number', v_po_number,
	    'previous_status', v_current::text
	  );
	END;
	$function$
2026-09-13 06:19:21.144 UTC [23766] ERROR:  type "user_role" does not exist at character 100
2026-09-13 06:19:21.144 UTC [23766] STATEMENT:  CREATE OR REPLACE FUNCTION public.validate_active_store()
	RETURNS TRIGGER AS $$
	DECLARE
	    v_role user_role;
	BEGIN
	    v_role := NEW.role;
	
	    -- Si role es 'costo' o 'admin' o 'superadmin', active_store_id es opcional
	    IF v_role = 'costo'::user_role
	       OR v_role = 'admin'::user_role
	       OR v_role = 'superadmin'::user_role THEN
	        -- Solo validar si active_store_id cambió o es INSERT
	        IF TG_OP = 'INSERT' OR (OLD.active_store_id IS DISTINCT FROM NEW.active_store_id) THEN
	            IF NEW.active_store_id IS NOT NULL THEN
	                IF NOT EXISTS (
	                    SELECT 1 FROM public.user_store_memberships
	                    WHERE user_id = NEW.id
	                      AND store_id = NEW.active_store_id
	                      AND status = 'active'
	                ) THEN
	                    IF TG_OP = 'UPDATE' THEN
	                        RAISE EXCEPTION 'ERR_INVALID_ACTIVE_STORE: El usuario no tiene membership activa en la tienda seleccionada.';
	                    END IF;
	                END IF;
	            END IF;
	        END IF;
	        RETURN NEW;
	    END IF;
	
	    -- Para roles operativos (encargado, clerk, warehouse, manager, usuario)
	    IF TG_OP = 'INSERT' THEN
	        -- En INSERT: ser permisivo. La función managed_create_user inserta
	        -- memberships justo después. No validar nada aquí.
	        RETURN NEW;
	    ELSE
	        -- UPDATE: solo validar si active_store_id cambió realmente
	        -- (evita bloquear updates de otros campos en perfiles intermedios)
	        IF OLD.active_store_id IS NOT DISTINCT FROM NEW.active_store_id THEN
	            -- active_store_id no cambió → permitir el UPDATE sin validar
	            RETURN NEW;
	        END IF;
	
	        -- active_store_id cambió → validar consistencia
	        IF NEW.active_store_id IS NULL THEN
	            IF v_role IN ('encargado'::user_role, 'clerk'::user_role, 'warehouse'::user_role) THEN
	                RAISE EXCEPTION 'ERR_STORE_REQUIRED: El rol % requiere una tienda activa asignada.', v_role;
	            END IF;
	        ELSE
	            IF NOT EXISTS (
	                SELECT 1 FROM public.user_store_memberships
	                WHERE user_id = NEW.id
	                  AND store_id = NEW.active_store_id
	                  AND status = 'active'
	            ) THEN
	                RAISE EXCEPTION 'ERR_INVALID_ACTIVE_STORE: El usuario no tiene membership activa en la tienda seleccionada.';
	            END IF;
	        END IF;
	        RETURN NEW;
	    END IF;
	END;
	$$ LANGUAGE plpgsql SECURITY DEFINER
2026-09-13 06:19:21.329 UTC [23797] ERROR:  type "public.user_role" does not exist at character 270
2026-09-13 06:19:21.329 UTC [23797] STATEMENT:  CREATE OR REPLACE FUNCTION public.validate_tenant_access(
	  p_user_id uuid,
	  p_store_id uuid
	)
	RETURNS boolean
	LANGUAGE plpgsql
	STABLE SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  v_user_tenant uuid;
	  v_store_tenant uuid;
	  v_user_role public.user_role;
	BEGIN
	  -- Global admin bypasses tenant check
	  SELECT role, tenant_id INTO v_user_role, v_user_tenant
	    FROM public.profiles WHERE id = p_user_id;
	
	  IF v_user_role IN ('admin', 'superadmin') THEN
	    RETURN true;
	  END IF;
	
	  -- Get store's tenant
	  SELECT tenant_id INTO v_store_tenant FROM public.stores WHERE id = p_store_id;
	
	  IF v_store_tenant IS NULL THEN
	    -- Legacy store without tenant — allow (backward compat)
	    RETURN true;
	  END IF;
	
	  -- Check tenant match
	  RETURN v_user_tenant = v_store_tenant;
	END;
	$function$
2026-09-13 06:19:21.895 UTC [23864] ERROR:  type "public.user_role" does not exist at character 301
2026-09-13 06:19:21.895 UTC [23864] STATEMENT:  CREATE OR REPLACE FUNCTION public.bulk_assign_memberships(
	  p_user_id uuid,
	  p_assignments jsonb
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  v_assignment jsonb;
	  v_affected int := 0;
	  v_failed int := 0;
	  v_store_id uuid;
	  v_role public.user_role;
	  v_status text;
	  v_caller_uid uuid := auth.uid();
	  v_changes jsonb := '[]'::jsonb;
	BEGIN
	  IF v_caller_uid IS NULL THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED';
	  END IF;
	
	  FOR v_assignment IN SELECT * FROM jsonb_array_elements(p_assignments) LOOP
	    BEGIN
	      v_store_id := (v_assignment->>'store_id')::uuid;
	      v_role := (v_assignment->>'role')::public.user_role;
	      v_status := COALESCE(v_assignment->>'status', 'active');
	
	      -- Validar caller tiene acceso al store
	      IF NOT public.is_admin() AND NOT public.has_store_role(v_store_id, ARRAY['admin', 'manager']) THEN
	        v_failed := v_failed + 1;
	        CONTINUE;
	      END IF;
	
	      INSERT INTO public.user_store_memberships (user_id, store_id, role, status)
	      VALUES (p_user_id, v_store_id, v_role, v_status)
	      ON CONFLICT (user_id, store_id) DO UPDATE SET
	        role = EXCLUDED.role,
	        status = EXCLUDED.status,
	        updated_at = now();
	
	      v_changes := v_changes || jsonb_build_object(jsonb_build_object(
	        'store_id', v_store_id,
	        'role', v_role::text,
	        'status', v_status
	      ));
	
	      v_affected := v_affected + 1;
	    EXCEPTION
	      WHEN foreign_key_violation THEN
	        v_failed := v_failed + 1;
	    END;
	  END LOOP;
	
	  -- Audit log atómico (solo si hubo cambios)
	  IF v_affected > 0 THEN
	    INSERT INTO public.user_audit_log (performed_by, target_user_id, action, new_values, metadata)
	    VALUES (
	      v_caller_uid, p_user_id, 'MEMBERSHIPS_BULK_ASSIGNED',
	      jsonb_build_object('assignments', v_changes),
	      jsonb_build_object('affected', v_affected, 'failed', v_failed)
	    );
	  END IF;
	
	  RETURN jsonb_build_object(
	    'success', true,
	    'affected', v_affected,
	    'failed', v_failed,
	    'user_id', p_user_id
	  );
	END;
	$function$
2026-09-13 06:19:21.922 UTC [23869] ERROR:  syntax error at or near "REVOKE" at character 5039
2026-09-13 06:19:21.922 UTC [23869] STATEMENT:  CREATE OR REPLACE FUNCTION public.bulk_soft_delete_stores(p_store_ids uuid[], p_deleted_by uuid, p_confirmation_token text, p_override_token text DEFAULT NULL::text, p_reason text DEFAULT NULL::text)
	 RETURNS jsonb
	 LANGUAGE plpgsql
	 SECURITY DEFINER
	 SET search_path TO 'public'
	AS $function$
	DECLARE
	  v_store_id UUID;
	  v_validation JSONB;
	  v_blockers JSONB;
	  v_errors JSONB[] := '{}'::jsonb[];
	  v_processed INTEGER := 0;
	  v_token_valid BOOLEAN;
	  v_has_protected BOOLEAN;
	  v_override_valid BOOLEAN;
	  v_confirmation_record RECORD;
	  v_override_record RECORD;
	  v_caller_role TEXT;
	BEGIN
	  -- ============================================================
	  -- AUTH CHECK: Solo admin puede ejecutar esta función
	  -- ============================================================
	  IF auth.uid() IS NOT NULL THEN
	    SELECT role INTO v_caller_role FROM public.profiles WHERE id = auth.uid();
	    IF v_caller_role IS NULL OR v_caller_role != 'admin' THEN
	      RAISE EXCEPTION 'ERR_PERMISSION_DENIED: Solo admin puede ejecutar bulk_soft_delete_stores';
	    END IF;
	  END IF;
	  -- Si auth.uid() IS NULL, es service_role — permitir
	
	  -- ============================================================
	  -- 1. VALIDATE confirmation_token
	  -- ============================================================
	  SELECT * INTO v_confirmation_record
	  FROM public.bulk_confirmation_tokens
	  WHERE token = p_confirmation_token
	    AND action = 'delete'
	    AND expires_at > NOW()
	    AND consumed_at IS NULL
	    AND is_override = false;
	
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_INVALID_CONFIRMATION_TOKEN';
	  END IF;
	
	  IF v_confirmation_record.store_ids != p_store_ids THEN
	    RAISE EXCEPTION 'ERR_STORE_IDS_MISMATCH';
	  END IF;
	
	  -- ============================================================
	  -- 2. VALIDATE tiendas protegidas requieren override_token
	  -- ============================================================
	  SELECT EXISTS(
	    SELECT 1 FROM public.stores
	    WHERE id = ANY(p_store_ids) AND backup_restore_protected = true
	  ) INTO v_has_protected;
	
	  IF v_has_protected THEN
	    IF p_override_token IS NULL THEN
	      RAISE EXCEPTION 'ERR_OVERRIDE_REQUIRED';
	    END IF;
	
	    SELECT * INTO v_override_record
	    FROM public.bulk_confirmation_tokens
	    WHERE token = p_override_token
	      AND is_override = true
	      AND override_for = p_confirmation_token
	      AND expires_at > NOW()
	      AND consumed_at IS NULL;
	
	    IF NOT FOUND THEN
	      RAISE EXCEPTION 'ERR_INVALID_OVERRIDE_TOKEN';
	    END IF;
	
	    IF v_override_record.store_ids != v_confirmation_record.store_ids THEN
	      RAISE EXCEPTION 'ERR_OVERRIDE_STORE_IDS_MISMATCH';
	    END IF;
	
	    IF v_override_record.created_by = v_confirmation_record.created_by THEN
	      RAISE EXCEPTION 'ERR_SAME_USER_OVERRIDE';
	    END IF;
	
	    UPDATE public.bulk_confirmation_tokens SET consumed_at = NOW()
	    WHERE token = p_override_token;
	  END IF;
	
	  UPDATE public.bulk_confirmation_tokens SET consumed_at = NOW()
	  WHERE token = p_confirmation_token;
	
	  -- ============================================================
	  -- 3. VALIDATE todas las tiendas
	  -- ============================================================
	  FOREACH v_store_id IN ARRAY p_store_ids LOOP
	    IF NOT EXISTS(SELECT 1 FROM public.stores WHERE id = v_store_id AND is_active = true) THEN
	      v_errors := array_append(v_errors, jsonb_build_object(
	        'store_id', v_store_id, 'reason', 'STORE_NOT_FOUND_OR_INACTIVE'
	      ));
	      CONTINUE;
	    END IF;
	
	    SELECT public.validate_store_can_be_modified(v_store_id, 'soft_delete') INTO v_validation;
	    v_blockers := v_validation->'blockers';
	
	    IF v_validation->>'can_modify' != 'true' THEN
	      v_errors := array_append(v_errors, jsonb_build_object(
	        'store_id', v_store_id, 'reason', 'HAS_BLOCKING_DEPENDENCIES', 'blockers', v_blockers
	      ));
	    END IF;
	  END LOOP;
	
	  IF array_length(v_errors, 1) IS NOT NULL THEN
	    RETURN jsonb_build_object(
	      'status', 'FAILED', 'processed', 0,
	      'total_requested', array_length(p_store_ids, 1),
	      'errors', to_jsonb(v_errors), 'reason', p_reason
	    );
	  END IF;
	
	  -- ============================================================
	  -- 4. EXECUTE
	  -- ============================================================
	  FOREACH v_store_id IN ARRAY p_store_ids LOOP
	    PERFORM public.soft_delete_store(v_store_id, p_deleted_by);
	    v_processed := v_processed + 1;
	  END LOOP;
	
	  INSERT INTO public.audit_logs (action, table_name, record_id, metadata)
	  VALUES (
	    'bulk_store_deleted', 'stores', NULL,
	    jsonb_build_object(
	      'store_ids', p_store_ids, 'deleted_by', p_deleted_by,
	      'reason', p_reason, 'processed', v_processed,
	      'had_protected_stores', v_has_protected,
	      'override_used', p_override_token IS NOT NULL, 'deleted_at', NOW()
	    )
	  );
	
	  RETURN jsonb_build_object(
	    'status', 'COMPLETED', 'processed', v_processed,
	    'total_requested', array_length(p_store_ids, 1),
	    'errors', '[]'::jsonb, 'reason', p_reason
	  );
	END;
	$function$
	 
	REVOKE EXECUTE ON FUNCTION public.bulk_soft_delete_stores FROM PUBLIC, anon, authenticated
2026-09-13 06:19:21.998 UTC [23878] ERROR:  relation "received_services" does not exist
2026-09-13 06:19:21.998 UTC [23878] CONTEXT:  compilation of PL/pgSQL function "calculate_service_distribution" near line 3
2026-09-13 06:19:21.998 UTC [23878] STATEMENT:  CREATE OR REPLACE FUNCTION calculate_service_distribution(
	  p_service_id UUID
	) RETURNS TABLE(
	  receipt_item_id UUID,
	  product_id UUID,
	  distribution_amount NUMERIC,
	  distribution_percentage NUMERIC
	) LANGUAGE plpgsql SECURITY DEFINER AS $$
	DECLARE
	  v_service received_services%ROWTYPE;
	  v_method TEXT;
	  v_total_amount NUMERIC;
	  v_total_value NUMERIC DEFAULT 0;
	  v_total_qty NUMERIC DEFAULT 0;
	  v_link service_reception_links%ROWTYPE;
	  v_receipt_id UUID;
	  v_item receipt_items%ROWTYPE;
	  v_allocated NUMERIC;
	BEGIN
	  SELECT * INTO v_service FROM received_services WHERE id = p_service_id;
	  IF NOT FOUND THEN RETURN; END IF;
	  
	  v_method := v_service.distribution_method;
	  v_total_amount := v_service.total_amount;
	
	  -- Para cada recepción vinculada
	  FOR v_link IN SELECT * FROM service_reception_links WHERE service_id = p_service_id AND allocated_amount > 0 LOOP
	    v_receipt_id := v_link.receipt_id;
	    v_allocated := v_link.allocated_amount;
	
	    -- Calcular base de distribución según método
	    IF v_method = 'amount' THEN
	      SELECT COALESCE(SUM(quantity * unit_cost), 0) INTO v_total_value
	      FROM receipt_items WHERE receipt_id = v_receipt_id;
	      
	      IF v_total_value > 0 THEN
	        FOR v_item IN SELECT * FROM receipt_items WHERE receipt_id = v_receipt_id LOOP
	          distribution_amount := v_allocated * (v_item.quantity * v_item.unit_cost / v_total_value);
	          distribution_percentage := (v_item.quantity * v_item.unit_cost / v_total_value) * 100;
	          receipt_item_id := v_item.id;
	          product_id := v_item.product_id;
	          RETURN NEXT;
	        END LOOP;
	      END IF;
	
	    ELSIF v_method = 'quantity' THEN
	      SELECT COALESCE(SUM(quantity), 0) INTO v_total_qty
	      FROM receipt_items WHERE receipt_id = v_receipt_id;
	      
	      IF v_total_qty > 0 THEN
	        FOR v_item IN SELECT * FROM receipt_items WHERE receipt_id = v_receipt_id LOOP
	          distribution_amount := v_allocated * (v_item.quantity / v_total_qty);
	          distribution_percentage := (v_item.quantity / v_total_qty) * 100;
	          receipt_item_id := v_item.id;
	          product_id := v_item.product_id;
	          RETURN NEXT;
	        END LOOP;
	      END IF;
	
	    ELSIF v_method = 'manual' THEN
	      -- En manual, la distribución se guarda directamente desde el cliente
	      RETURN;
	    END IF;
	  END LOOP;
	END;
	$$
2026-09-13 06:19:22.059 UTC [23890] ERROR:  syntax error at or near "REVOKE" at character 678
2026-09-13 06:19:22.059 UTC [23890] STATEMENT:  CREATE OR REPLACE FUNCTION public.check_bulk_ops_hourly_limit(p_user_id uuid, p_plan text)
	 RETURNS jsonb
	 LANGUAGE plpgsql
	 STABLE SECURITY DEFINER
	 SET search_path TO 'public'
	AS $function$
	DECLARE
	  v_limit INTEGER;
	  v_used INTEGER;
	BEGIN
	  v_limit := CASE p_plan
	    WHEN 'free' THEN 1
	    WHEN 'pro' THEN 20
	    WHEN 'enterprise' THEN 999999
	    ELSE 1
	  END;
	
	  SELECT COUNT(*) INTO v_used
	  FROM public.bulk_ops_log
	  WHERE user_id = p_user_id
	    AND initiated_at > NOW() - INTERVAL '1 hour';
	
	  RETURN jsonb_build_object(
	    'allowed', v_used < v_limit,
	    'used', v_used,
	    'limit', v_limit,
	    'remaining', GREATEST(0, v_limit - v_used)
	  );
	END;
	$function$
	 
	REVOKE EXECUTE ON FUNCTION public.check_bulk_ops_hourly_limit FROM PUBLIC, anon, authenticated
2026-09-13 06:19:22.099 UTC [23897] ERROR:  type plan_t does not exist
2026-09-13 06:19:22.099 UTC [23897] STATEMENT:  CREATE OR REPLACE FUNCTION public.check_tenant_store_quota(
	  p_tenant_id uuid,
	  p_plan plan_t DEFAULT NULL
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  v_plan plan_t;
	  v_limit int;
	  v_current int;
	  v_allowed boolean;
	BEGIN
	  -- Get plan from tenant if not passed
	  IF p_plan IS NULL THEN
	    SELECT plan INTO v_plan FROM public.tenants WHERE id = p_tenant_id;
	  ELSE
	    v_plan := p_plan;
	  END IF;
	
	  -- Plan limits (unified source of truth)
	  v_limit := CASE v_plan
	    WHEN 'free'::plan_t THEN 1
	    WHEN 'pro'::plan_t THEN 3
	    WHEN 'enterprise'::plan_t THEN 10
	    ELSE 1
	  END;
	
	  -- Count active stores in tenant
	  SELECT COUNT(*) INTO v_current
	    FROM public.stores
	    WHERE tenant_id = p_tenant_id AND is_active = true;
	
	  v_allowed := v_current < v_limit;
	
	  RETURN jsonb_build_object(
	    'allowed', v_allowed,
	    'current', v_current,
	    'limit', v_limit,
	    'plan', v_plan::text,
	    'tenant_id', p_tenant_id
	  );
	END;
	$function$
2026-09-13 06:19:22.212 UTC [23926] ERROR:  relation "public.profiles" does not exist at character 166
2026-09-13 06:19:22.212 UTC [23926] STATEMENT:  CREATE OR REPLACE FUNCTION public.current_user_tenant_id()
	RETURNS uuid
	LANGUAGE sql
	STABLE
	SECURITY DEFINER
	SET search_path TO public
	AS $$
	  SELECT tenant_id FROM public.profiles WHERE id = auth.uid();
	$$
2026-09-13 06:19:22.250 UTC [23934] ERROR:  relation "received_services" does not exist
2026-09-13 06:19:22.250 UTC [23934] CONTEXT:  compilation of PL/pgSQL function "distribute_service_cost_v2" near line 6
2026-09-13 06:19:22.250 UTC [23934] STATEMENT:  CREATE OR REPLACE FUNCTION public.distribute_service_cost_v2(
	  p_service_id uuid,
	  p_user_id uuid DEFAULT NULL
	) RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO 'public', 'extensions'
	AS $func$
	DECLARE
	  v_store_id uuid;
	  v_status text;
	  v_total_amount numeric;
	  v_service received_services%ROWTYPE;
	  v_method text;
	  v_link service_reception_links%ROWTYPE;
	  v_item receipt_items%ROWTYPE;
	  v_total_value numeric := 0;
	  v_total_qty numeric := 0;
	  v_allocated numeric;
	  v_dist_count integer := 0;
	  v_caller_uid uuid := COALESCE(p_user_id, auth.uid());
	BEGIN
	  PERFORM pg_advisory_xact_lock(hashtext(p_service_id::text));
	
	  SELECT * INTO v_service FROM received_services WHERE id = p_service_id FOR UPDATE;
	  IF NOT FOUND THEN RAISE EXCEPTION 'ERR_SERVICE_NOT_FOUND'; END IF;
	
	  v_store_id := v_service.store_id;
	  v_status := v_service.status;
	  v_total_amount := v_service.total_amount;
	  v_method := v_service.distribution_method;
	
	  IF NOT public.has_store_access(v_store_id) THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED';
	  END IF;
	
	  IF v_status != 'active' THEN
	    RAISE EXCEPTION 'ERR_SERVICE_NOT_ACTIVE: status % is not active', v_status;
	  END IF;
	
	  IF v_method = 'manual' THEN
	    RAISE EXCEPTION 'ERR_MANUAL_METHOD: use link_receipts_to_service for manual distribution';
	  END IF;
	
	  DELETE FROM service_cost_distributions WHERE service_id = p_service_id;
	
	  FOR v_link IN SELECT * FROM service_reception_links WHERE service_id = p_service_id AND allocated_amount > 0 ORDER BY receipt_id LOOP
	    v_allocated := v_link.allocated_amount;
	
	    IF v_method = 'amount' THEN
	      SELECT COALESCE(SUM(quantity * unit_cost), 0) INTO v_total_value
	      FROM receipt_items WHERE receipt_id = v_link.receipt_id;
	      IF v_total_value > 0 THEN
	        FOR v_item IN SELECT * FROM receipt_items WHERE receipt_id = v_link.receipt_id ORDER BY id LOOP
	          INSERT INTO service_cost_distributions
	            (service_id, receipt_id, receipt_item_id, product_id, distribution_amount, distribution_percentage)
	          VALUES
	            (p_service_id, v_link.receipt_id, v_item.id, v_item.product_id,
	             v_allocated * (v_item.quantity * v_item.unit_cost / v_total_value),
	             (v_item.quantity * v_item.unit_cost / v_total_value) * 100);
	          v_dist_count := v_dist_count + 1;
	        END LOOP;
	      END IF;
	
	    ELSIF v_method = 'quantity' THEN
	      SELECT COALESCE(SUM(quantity), 0) INTO v_total_qty
	      FROM receipt_items WHERE receipt_id = v_link.receipt_id;
	      IF v_total_qty > 0 THEN
	        FOR v_item IN SELECT * FROM receipt_items WHERE receipt_id = v_link.receipt_id ORDER BY id LOOP
	          INSERT INTO service_cost_distributions
	            (service_id, receipt_id, receipt_item_id, product_id, distribution_amount, distribution_percentage)
	          VALUES
	            (p_service_id, v_link.receipt_id, v_item.id, v_item.product_id,
	             v_allocated * (v_item.quantity / v_total_qty),
	             (v_item.quantity / v_total_qty) * 100);
	          v_dist_count := v_dist_count + 1;
	        END LOOP;
	      END IF;
	    END IF;
	  END LOOP;
	
	  -- Forzar recalculo de WAC para cada producto afectado
	  UPDATE receipt_items SET updated_at = NOW()
	  WHERE product_id IN (SELECT DISTINCT product_id FROM service_cost_distributions WHERE service_id = p_service_id);
	
	  INSERT INTO audit_logs (user_id, store_id, action, table_name, record_id, metadata)
	  VALUES (v_caller_uid, v_store_id, 'SERVICE_DISTRIBUTED', 'received_services', p_service_id,
	    jsonb_build_object('rows_distributed', v_dist_count, 'method', v_method, 'total_amount', v_total_amount));
	
	  RETURN jsonb_build_object('status', 'success', 'distributed_rows', v_dist_count);
	END;
	$func$
2026-09-13 06:19:22.295 UTC [23941] ERROR:  type "user_role" does not exist at character 116
2026-09-13 06:19:22.295 UTC [23941] STATEMENT:  CREATE OR REPLACE FUNCTION public.enforce_encargado_store_limit()
	RETURNS TRIGGER AS $$
	DECLARE
	    v_creator_role user_role;
	    v_limit integer;
	    v_current_count integer;
	BEGIN
	    IF NEW.created_by IS NULL THEN RETURN NEW; END IF;
	
	    SELECT role, max_stores_limit INTO v_creator_role, v_limit
	    FROM public.profiles WHERE id = NEW.created_by;
	
	    IF v_creator_role = 'encargado' THEN
	        SELECT COUNT(*) INTO v_current_count FROM public.stores WHERE created_by = NEW.created_by;
	        IF v_current_count >= v_limit THEN
	            RAISE EXCEPTION 'ERR_STORE_LIMIT_EXCEEDED: Maximum number of stores reached for this manager (%/%)', v_current_count, v_limit;
	        END IF;
	    END IF;
	    RETURN NEW;
	END;
	$$ LANGUAGE plpgsql
2026-09-13 06:19:22.323 UTC [23946] ERROR:  type "user_role" does not exist at character 115
2026-09-13 06:19:22.323 UTC [23946] STATEMENT:  CREATE OR REPLACE FUNCTION public.enforce_encargado_user_limit()
	RETURNS TRIGGER AS $$
	DECLARE
	    v_creator_role user_role;
	    v_limit integer;
	    v_current_count integer;
	BEGIN
	    IF NEW.created_by IS NULL THEN RETURN NEW; END IF;
	
	    SELECT role, max_users_limit INTO v_creator_role, v_limit
	    FROM public.profiles WHERE id = NEW.created_by;
	
	    IF v_creator_role = 'encargado' THEN
	        SELECT COUNT(*) INTO v_current_count FROM public.profiles WHERE created_by = NEW.created_by;
	        IF v_current_count >= v_limit THEN
	            RAISE EXCEPTION 'ERR_USER_LIMIT_EXCEEDED: Maximum number of users reached for this manager (%/%)', v_current_count, v_limit;
	        END IF;
	    END IF;
	    RETURN NEW;
	END;
	$$ LANGUAGE plpgsql
2026-09-13 06:19:22.385 UTC [23961] ERROR:  syntax error at or near "REVOKE" at character 1436
2026-09-13 06:19:22.385 UTC [23961] STATEMENT:  CREATE OR REPLACE FUNCTION public.generate_bulk_confirmation_token(p_store_ids uuid[], p_action text, p_user_id uuid)
	 RETURNS text
	 LANGUAGE plpgsql
	 SECURITY DEFINER
	 SET search_path TO 'public'
	AS $function$
	DECLARE
	  v_token TEXT;
	  v_has_protected BOOLEAN;
	  v_caller_role TEXT;
	BEGIN
	  -- AUTH CHECK: Solo admin
	  IF auth.uid() IS NOT NULL THEN
	    SELECT role INTO v_caller_role FROM public.profiles WHERE id = auth.uid();
	    IF v_caller_role IS NULL OR v_caller_role != 'admin' THEN
	      RAISE EXCEPTION 'ERR_PERMISSION_DENIED: Solo admin puede generar tokens bulk';
	    END IF;
	    -- Verificar que p_user_id coincide con auth.uid()
	    IF p_user_id != auth.uid() THEN
	      RAISE EXCEPTION 'ERR_PERMISSION_DENIED: p_user_id debe coincidir con el usuario autenticado';
	    END IF;
	  END IF;
	
	  IF p_action NOT IN ('delete', 'archive') THEN
	    RAISE EXCEPTION 'ERR_NON_DESTRUCTIVE_ACTION';
	  END IF;
	
	  SELECT EXISTS(
	    SELECT 1 FROM public.stores
	    WHERE id = ANY(p_store_ids) AND backup_restore_protected = true
	  ) INTO v_has_protected;
	
	  v_token := 'bct_' || replace(gen_random_uuid()::text, '-', '');
	
	  INSERT INTO public.bulk_confirmation_tokens (
	    token, store_ids, action, created_by, expires_at, metadata
	  ) VALUES (
	    v_token, p_store_ids, p_action, p_user_id,
	    NOW() + INTERVAL '10 minutes',
	    jsonb_build_object('has_protected_stores', v_has_protected)
	  );
	
	  RETURN v_token;
	END;
	$function$
	 
	REVOKE EXECUTE ON FUNCTION public.generate_bulk_confirmation_token FROM PUBLIC, anon, authenticated
2026-09-13 06:19:22.414 UTC [23966] ERROR:  syntax error at or near "REVOKE" at character 2432
2026-09-13 06:19:22.414 UTC [23966] STATEMENT:  CREATE OR REPLACE FUNCTION public.generate_bulk_override_token(p_confirmation_token text, p_override_user_id uuid, p_reason text DEFAULT NULL::text)
	 RETURNS text
	 LANGUAGE plpgsql
	 SECURITY DEFINER
	 SET search_path TO 'public'
	AS $function$
	DECLARE
	  v_original RECORD;
	  v_override_token TEXT;
	  v_override_user_role TEXT;
	BEGIN
	  -- AUTH CHECK: Solo admin, y debe ser auth.uid() == p_override_user_id
	  IF auth.uid() IS NOT NULL THEN
	    IF auth.uid() != p_override_user_id THEN
	      RAISE EXCEPTION 'ERR_PERMISSION_DENIED: p_override_user_id debe coincidir con el usuario autenticado';
	    END IF;
	
	    SELECT role INTO v_override_user_role FROM public.profiles WHERE id = p_override_user_id;
	    IF v_override_user_role IS NULL OR v_override_user_role != 'admin' THEN
	      RAISE EXCEPTION 'ERR_PERMISSION_DENIED: Solo admin puede generar override';
	    END IF;
	  END IF;
	
	  SELECT * INTO v_original
	  FROM public.bulk_confirmation_tokens
	  WHERE token = p_confirmation_token
	    AND consumed_at IS NULL
	    AND expires_at > NOW()
	    AND is_override = false;
	
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_INVALID_OR_EXPIRED_TOKEN';
	  END IF;
	
	  IF v_original.created_by = p_override_user_id THEN
	    RAISE EXCEPTION 'ERR_SAME_USER_OVERRIDE';
	  END IF;
	
	  IF EXISTS(
	    SELECT 1 FROM public.bulk_confirmation_tokens
	    WHERE override_for = p_confirmation_token
	      AND consumed_at IS NULL AND expires_at > NOW()
	  ) THEN
	    RAISE EXCEPTION 'ERR_OVERRIDE_ALREADY_EXISTS';
	  END IF;
	
	  v_override_token := 'bot_' || replace(gen_random_uuid()::text, '-', '');
	
	  INSERT INTO public.bulk_confirmation_tokens (
	    token, store_ids, action, created_by, expires_at,
	    is_override, override_for, metadata
	  ) VALUES (
	    v_override_token, v_original.store_ids, v_original.action, p_override_user_id,
	    NOW() + INTERVAL '10 minutes', true, p_confirmation_token,
	    jsonb_build_object('override_reason', p_reason,
	      'original_created_by', v_original.created_by,
	      'override_created_by', p_override_user_id)
	  );
	
	  INSERT INTO public.audit_logs (action, table_name, record_id, metadata)
	  VALUES (
	    'bulk_override_token_generated', 'stores', NULL,
	    jsonb_build_object('confirmation_token', p_confirmation_token,
	      'store_ids', v_original.store_ids,
	      'override_by', p_override_user_id,
	      'original_by', v_original.created_by, 'reason', p_reason)
	  );
	
	  RETURN v_override_token;
	END;
	$function$
	 
	REVOKE EXECUTE ON FUNCTION public.generate_bulk_override_token FROM PUBLIC, anon, authenticated
2026-09-13 06:19:22.456 UTC [23976] ERROR:  relation "public.backup_table_registry" does not exist at character 581
2026-09-13 06:19:22.456 UTC [23976] STATEMENT:  CREATE OR REPLACE FUNCTION public.get_backup_table_list(
	  p_include_excluded BOOLEAN DEFAULT FALSE
	)
	RETURNS TABLE (
	  table_name          TEXT,
	  tier                INTEGER,
	  filter_strategy     TEXT,
	  parent_table        TEXT,
	  parent_foreign_key  TEXT,
	  date_column         TEXT,
	  excluded_from_restore BOOLEAN,
	  exclude_reason      TEXT
	)
	LANGUAGE sql
	SECURITY DEFINER
	SET search_path = public
	AS $$
	  SELECT
	    table_name,
	    tier,
	    filter_strategy,
	    parent_table,
	    parent_foreign_key,
	    date_column,
	    excluded_from_restore,
	    exclude_reason
	  FROM public.backup_table_registry
	  WHERE (p_include_excluded OR excluded_from_restore = FALSE)
	  ORDER BY tier ASC, table_name ASC;
	$$
2026-09-13 06:19:22.501 UTC [23986] ERROR:  relation "v_global_operation_dates" does not exist at character 229
2026-09-13 06:19:22.501 UTC [23986] STATEMENT:  CREATE OR REPLACE FUNCTION public.get_global_max_operation_date(
	  p_store_id UUID DEFAULT NULL
	)
	RETURNS TIMESTAMP WITH TIME ZONE
	LANGUAGE sql
	SECURITY DEFINER
	SET search_path = public
	AS $$
	  SELECT MAX(operation_date)
	  FROM v_global_operation_dates
	  WHERE p_store_id IS NULL OR store_id = p_store_id;
	$$
2026-09-13 06:19:22.531 UTC [23991] ERROR:  relation "exchange_rates" does not exist at character 216
2026-09-13 06:19:22.531 UTC [23991] STATEMENT:  CREATE OR REPLACE FUNCTION get_latest_rate(p_currency TEXT, p_source TEXT)
	RETURNS TABLE(rate NUMERIC, rate_date DATE, captured_at TIMESTAMPTZ)
	LANGUAGE sql STABLE AS $$
	  SELECT rate, rate_date, captured_at
	  FROM exchange_rates
	  WHERE currency = p_currency AND source = p_source
	  ORDER BY rate_date DESC, captured_at DESC
	  LIMIT 1;
	$$
2026-09-13 06:19:22.558 UTC [23996] ERROR:  relation "public.products" does not exist at character 194
2026-09-13 06:19:22.558 UTC [23996] STATEMENT:  CREATE OR REPLACE FUNCTION public.get_low_stock_count(p_store_id uuid DEFAULT NULL)
	RETURNS bigint
	LANGUAGE sql
	SECURITY DEFINER
	SET search_path = public
	AS $$
	  SELECT COUNT(*)::bigint
	  FROM public.products
	  WHERE
	    (p_store_id IS NULL OR store_id = p_store_id)
	    AND is_active = true
	    AND stock_current > 0
	    AND min_stock IS NOT NULL
	    AND min_stock > 0
	    AND stock_current <= min_stock;
	$$
2026-09-13 06:19:22.600 UTC [24005] ERROR:  relation "receipt_items" does not exist at character 754
2026-09-13 06:19:22.600 UTC [24005] STATEMENT:  CREATE OR REPLACE FUNCTION get_product_cost_analysis(
	  p_product_id UUID,
	  p_store_id UUID
	) RETURNS TABLE(
	  receipt_id UUID,
	  receipt_date TIMESTAMPTZ,
	  quantity INT,
	  unit_cost FLOAT,
	  service_type TEXT,
	  service_amount NUMERIC,
	  total_cost NUMERIC,
	  unit_cost_final NUMERIC
	) LANGUAGE sql SECURITY DEFINER AS $$
	  SELECT 
	    ri.receipt_id,
	    r.reception_date,
	    ri.quantity,
	    ri.unit_cost,
	    st.name AS service_type,
	    COALESCE(scd.distribution_amount, 0) AS service_amount,
	    ri.unit_cost * ri.quantity + COALESCE(scd.distribution_amount, 0) AS total_cost,
	    CASE WHEN ri.quantity > 0 
	      THEN (ri.unit_cost * ri.quantity + COALESCE(scd.distribution_amount, 0)) / ri.quantity 
	      ELSE 0 END AS unit_cost_final
	  FROM receipt_items ri
	  JOIN receipts r ON r.id = ri.receipt_id
	  LEFT JOIN service_cost_distributions scd ON scd.receipt_item_id = ri.id
	  LEFT JOIN received_services rs ON rs.id = scd.service_id AND rs.status = 'active'
	  LEFT JOIN service_types st ON st.id = rs.service_type_id
	  WHERE ri.product_id = p_product_id
	    AND r.store_id = p_store_id
	    AND r.status = 'active'
	  ORDER BY r.reception_date DESC;
	$$
2026-09-13 06:19:22.642 UTC [24013] ERROR:  relation "exchange_rates" does not exist at character 252
2026-09-13 06:19:22.642 UTC [24013] STATEMENT:  CREATE OR REPLACE FUNCTION get_rate_history(
	  p_currency TEXT,
	  p_source TEXT,
	  p_days INT DEFAULT 30
	)
	RETURNS TABLE(rate_date DATE, rate NUMERIC, variation_daily NUMERIC)
	LANGUAGE sql STABLE AS $$
	  SELECT rate_date, rate, variation_daily
	  FROM exchange_rates
	  WHERE currency = p_currency AND source = p_source
	    AND rate_date >= CURRENT_DATE - p_days
	  ORDER BY rate_date ASC;
	$$
2026-09-13 06:19:22.667 UTC [24018] ERROR:  relation "exchange_rates" does not exist at character 363
2026-09-13 06:19:22.667 UTC [24018] STATEMENT:  CREATE OR REPLACE FUNCTION get_rate_variation(
	  p_currency TEXT,
	  p_source TEXT,
	  p_start_date DATE,
	  p_end_date DATE
	)
	RETURNS TABLE(
	  start_rate NUMERIC,
	  end_rate NUMERIC,
	  absolute_change NUMERIC,
	  percent_change NUMERIC,
	  daily_avg_growth NUMERIC,
	  monthly_avg_growth NUMERIC
	)
	LANGUAGE sql STABLE AS $$
	  WITH start_val AS (
	    SELECT rate FROM exchange_rates
	    WHERE currency = p_currency AND source = p_source AND rate_date <= p_start_date
	    ORDER BY rate_date DESC LIMIT 1
	  ),
	  end_val AS (
	    SELECT rate FROM exchange_rates
	    WHERE currency = p_currency AND source = p_source AND rate_date <= p_end_date
	    ORDER BY rate_date DESC LIMIT 1
	  )
	  SELECT
	    COALESCE(sv.rate, 0) AS start_rate,
	    COALESCE(ev.rate, 0) AS end_rate,
	    (COALESCE(ev.rate, 0) - COALESCE(sv.rate, 0)) AS absolute_change,
	    CASE WHEN sv.rate IS NOT NULL AND sv.rate > 0 THEN ((ev.rate - sv.rate) / sv.rate * 100) ELSE 0 END AS percent_change,
	    CASE WHEN sv.rate IS NOT NULL AND sv.rate > 0 AND ev.rate IS NOT NULL AND ev.rate > 0 AND p_end_date > p_start_date
	      THEN (POWER(ev.rate::FLOAT / sv.rate::FLOAT, 1.0 / EXTRACT(EPOCH FROM (p_end_date - p_start_date)) * 86400) - 1) * 100
	      ELSE 0 END AS daily_avg_growth,
	    CASE WHEN sv.rate IS NOT NULL AND sv.rate > 0 AND ev.rate IS NOT NULL AND ev.rate > 0 AND p_end_date > p_start_date
	      THEN (POWER(ev.rate::FLOAT / sv.rate::FLOAT, 30.44 / EXTRACT(EPOCH FROM (p_end_date - p_start_date)) * 86400) - 1) * 100
	      ELSE 0 END AS monthly_avg_growth
	  FROM start_val sv, end_val ev;
	$$
2026-09-13 06:19:22.724 UTC [24032] ERROR:  type "public.stores" does not exist
2026-09-13 06:19:22.724 UTC [24032] STATEMENT:  CREATE OR REPLACE FUNCTION public.get_transferable_stores(
	  p_user_id UUID,
	  p_current_store_id UUID
	)
	RETURNS SETOF public.stores
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO 'public'
	AS $$
	DECLARE
	  v_tenant_id UUID;
	BEGIN
	  -- 1. Obtener tenant_id de la tienda actual
	  SELECT tenant_id INTO v_tenant_id
	  FROM public.stores
	  WHERE id = p_current_store_id AND is_active = true;
	
	  -- 2. Devolver tiendas donde el caller tiene acceso (has_store_access_as)
	  --    EXCLUYENDO la tienda actual
	  --    Si tenant_id es NOT NULL, filtrar por mismo tenant.
	  --    Si tenant_id es NULL (legacy), mostrar todas las que el caller tiene acceso.
	  IF v_tenant_id IS NOT NULL THEN
	    RETURN QUERY
	    SELECT s.*
	    FROM public.stores s
	    WHERE s.tenant_id = v_tenant_id
	      AND s.id != p_current_store_id
	      AND s.is_active = true
	      AND s.is_archived = false
	      AND public.has_store_access_as(p_user_id, s.id)
	    ORDER BY s.name;
	  ELSE
	    -- Legacy: tiendas sin tenant_id — usar solo has_store_access_as
	    RETURN QUERY
	    SELECT s.*
	    FROM public.stores s
	    WHERE s.id != p_current_store_id
	      AND s.is_active = true
	      AND s.is_archived = false
	      AND public.has_store_access_as(p_user_id, s.id)
	    ORDER BY s.name;
	  END IF;
	END;
	$$
2026-09-13 06:19:22.767 UTC [24043] ERROR:  type user_role[] does not exist
2026-09-13 06:19:22.767 UTC [24043] STATEMENT:  CREATE OR REPLACE FUNCTION public.has_any_role(required_roles user_role[])
	 RETURNS boolean
	 LANGUAGE plpgsql
	 STABLE SECURITY DEFINER
	AS $$
	DECLARE
	    v_actual_role user_role;
	    r user_role;
	BEGIN
	    SELECT role INTO v_actual_role FROM public.profiles WHERE id = auth.uid();
	
	    FOREACH r IN ARRAY required_roles
	    LOOP
	        IF v_actual_role = r THEN RETURN true; END IF;
	        IF v_actual_role = 'encargado' AND r = 'manager' THEN RETURN true; END IF;
	        IF v_actual_role = 'usuario' AND (r = 'clerk' OR r = 'warehouse') THEN RETURN true; END IF;
	    END LOOP;
	
	    RETURN false;
	END;
	$$
2026-09-13 06:19:22.793 UTC [24048] ERROR:  relation "profiles" does not exist at character 461
2026-09-13 06:19:22.793 UTC [24048] STATEMENT:  CREATE OR REPLACE FUNCTION public.has_management_access_as(
	  p_user_id uuid,
	  p_store_id uuid
	) RETURNS boolean
	LANGUAGE sql SECURITY DEFINER
	SET search_path = public, pg_temp
	AS $$
	  -- service_role always passes
	  SELECT CASE WHEN auth.role() = 'service_role' THEN true
	    ELSE
	      -- Check if user has admin/manager/encargado role in their profile
	      -- OR has a membership with manager/admin role for the store
	      EXISTS (
	        SELECT 1 FROM profiles
	        WHERE id = p_user_id
	        AND role IN ('admin', 'manager', 'encargado')
	      )
	      OR EXISTS (
	        SELECT 1 FROM user_store_memberships
	        WHERE user_id = p_user_id
	        AND store_id = p_store_id
	        AND status = 'active'
	        AND role IN ('admin', 'manager')
	      )
	    END
	$$
2026-09-13 06:19:22.816 UTC [24053] ERROR:  type public.user_role does not exist
2026-09-13 06:19:22.816 UTC [24053] STATEMENT:  CREATE OR REPLACE FUNCTION public.has_role(p_user_id UUID, p_required_role public.user_role)
	 RETURNS boolean
	 LANGUAGE plpgsql
	 STABLE SECURITY DEFINER
	AS $$
	DECLARE
	    v_actual_role public.user_role;
	BEGIN
	    SELECT role INTO v_actual_role FROM public.profiles WHERE id = p_user_id;
	
	    IF v_actual_role IS NULL THEN
	        RETURN false;
	    END IF;
	
	    -- Compatibility mapping (mirroring the 1-arg version)
	    IF v_actual_role = 'encargado' AND p_required_role = 'manager' THEN RETURN true; END IF;
	    IF v_actual_role = 'usuario' AND (p_required_role = 'clerk' OR p_required_role = 'warehouse') THEN RETURN true; END IF;
	
	    RETURN v_actual_role = p_required_role;
	END;
	$$
2026-09-13 06:19:22.847 UTC [24060] ERROR:  relation "public.user_store_memberships" does not exist at character 279
2026-09-13 06:19:22.847 UTC [24060] STATEMENT:  CREATE OR REPLACE FUNCTION public.has_store_role(
	  p_user_id uuid,
	  p_store_id uuid,
	  p_roles text[]
	) RETURNS boolean
	LANGUAGE sql SECURITY DEFINER
	SET search_path = public, pg_temp
	AS $$
	  SELECT CASE WHEN auth.role() = 'service_role' THEN
	    EXISTS (
	      SELECT 1 FROM public.user_store_memberships m
	      WHERE m.user_id = p_user_id
	        AND m.store_id = p_store_id
	        AND m.status = 'active'
	        AND m.role::text = ANY(p_roles)
	    )
	  ELSE
	    EXISTS (
	      SELECT 1 FROM public.user_store_memberships m
	      WHERE m.user_id = auth.uid()
	        AND m.store_id = p_store_id
	        AND m.status = 'active'
	        AND m.role::text = ANY(p_roles)
	    )
	  END
	$$
2026-09-13 06:19:22.943 UTC [24068] ERROR:  relation "public.profiles" does not exist at character 173
2026-09-13 06:19:22.943 UTC [24068] STATEMENT:  CREATE OR REPLACE FUNCTION public.is_global_admin()
	RETURNS BOOLEAN
	LANGUAGE sql
	STABLE
	SECURITY DEFINER
	SET search_path = public
	AS $$
	  SELECT EXISTS (
	    SELECT 1 FROM public.profiles
	    WHERE id = auth.uid()
	      AND role = 'admin'
	  );
	$$
2026-09-13 06:19:22.974 UTC [24074] ERROR:  type user_role does not exist
2026-09-13 06:19:22.974 UTC [24074] STATEMENT:  CREATE OR REPLACE FUNCTION public.is_role_not_changed(p_user_id uuid, p_new_role user_role, p_new_role_id uuid)
	RETURNS boolean
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path = public, extensions
	AS $$
	DECLARE
	  v_old_role user_role;
	  v_old_role_id uuid;
	BEGIN
	  SELECT role, role_id INTO v_old_role, v_old_role_id
	  FROM public.profiles
	  WHERE id = p_user_id;
	
	  RETURN (p_new_role IS NOT DISTINCT FROM v_old_role)
	     AND (p_new_role_id IS NOT DISTINCT FROM v_old_role_id);
	END;
	$$
2026-09-13 06:19:23.001 UTC [24080] ERROR:  relation "public.user_store_memberships" does not exist at character 188
2026-09-13 06:19:23.001 UTC [24080] STATEMENT:  CREATE OR REPLACE FUNCTION public.is_store_member(p_store_id UUID)
	RETURNS BOOLEAN
	LANGUAGE sql
	STABLE
	SECURITY DEFINER
	SET search_path = public
	AS $$
	  SELECT EXISTS (
	    SELECT 1 FROM public.user_store_memberships
	    WHERE store_id = p_store_id
	      AND user_id = auth.uid()
	      AND status = 'active'
	  );
	$$
2026-09-13 06:19:23.024 UTC [24085] ERROR:  function public.current_user_tenant_id() does not exist at character 209
2026-09-13 06:19:23.024 UTC [24085] HINT:  No function matches the given name and argument types. You might need to add explicit type casts.
2026-09-13 06:19:23.024 UTC [24085] STATEMENT:  CREATE OR REPLACE FUNCTION public.is_tenant_member(p_tenant_id uuid)
	RETURNS boolean
	LANGUAGE sql
	STABLE
	SECURITY DEFINER
	SET search_path TO public
	AS $$
	  SELECT p_tenant_id IS NOT NULL
	   AND p_tenant_id = public.current_user_tenant_id();
	$$
2026-09-13 06:19:23.070 UTC [24097] ERROR:  type user_role does not exist
2026-09-13 06:19:23.070 UTC [24097] STATEMENT:  CREATE OR REPLACE FUNCTION public.managed_create_user(p_email text, p_full_name text, p_role user_role, p_store_id uuid DEFAULT NULL::uuid, p_memberships jsonb DEFAULT NULL::jsonb, p_max_stores integer DEFAULT 0, p_max_users integer DEFAULT 0, p_target_user_id uuid DEFAULT NULL::uuid)
	 RETURNS jsonb
	 LANGUAGE plpgsql
	 SECURITY DEFINER
	 SET search_path TO 'public', 'extensions'
	AS $function$
	DECLARE
	    v_user_id uuid;
	    v_role_id uuid;
	    v_active_store_id uuid;
	    m JSONB;
	    v_creator_role user_role;
	BEGIN
	    SELECT role INTO v_creator_role FROM public.profiles WHERE id = auth.uid();
	    IF v_creator_role IS NULL OR v_creator_role NOT IN ('admin', 'encargado') THEN
	        RAISE EXCEPTION 'ERR_UNAUTHORIZED: Only admins and managers can create users.';
	    END IF;
	
	    v_active_store_id := COALESCE((p_memberships->0->>'store_id')::UUID, p_store_id);
	
	    IF v_creator_role = 'encargado' AND v_active_store_id IS NOT NULL THEN
	        IF NOT public.has_store_access(v_active_store_id) THEN
	            RAISE EXCEPTION 'ERR_UNAUTHORIZED: No access to store %', v_active_store_id;
	        END IF;
	    END IF;
	
	    SELECT id INTO v_role_id FROM public.roles WHERE lower(name) = lower(p_role::text) OR (name = 'Cajero' AND p_role = 'clerk') OR (name = 'Almacenero' AND p_role = 'warehouse') LIMIT 1;
	    v_user_id := COALESCE(p_target_user_id, gen_random_uuid());
	
	    INSERT INTO public.profiles (id, email, full_name, role, role_id, active_store_id, is_active, created_by, max_stores_limit, max_users_limit, created_at, updated_at)
	    VALUES (v_user_id, p_email, p_full_name, p_role, v_role_id, v_active_store_id, true, auth.uid(), p_max_stores, p_max_users, now(), now())
	    ON CONFLICT (id) DO UPDATE SET email = EXCLUDED.email, full_name = EXCLUDED.full_name, role = EXCLUDED.role, role_id = EXCLUDED.role_id, active_store_id = EXCLUDED.active_store_id, updated_at = now()
	    RETURNING id INTO v_user_id;
	
	    IF p_memberships IS NOT NULL THEN
	        IF v_creator_role != 'admin' THEN
	            DELETE FROM public.user_store_memberships WHERE user_id = v_user_id AND store_id IN (SELECT store_id FROM public.user_store_memberships WHERE user_id = auth.uid() AND role IN ('encargado', 'manager'));
	        ELSE
	            DELETE FROM public.user_store_memberships WHERE user_id = v_user_id;
	        END IF;
	        FOR m IN SELECT * FROM jsonb_array_elements(p_memberships) LOOP
	            IF (m->>'store_id') IS NOT NULL AND (m->>'store_id') <> '' THEN
	                IF v_creator_role = 'admin' OR public.has_store_access((m->>'store_id')::UUID) THEN
	                    INSERT INTO public.user_store_memberships (user_id, store_id, role) VALUES (v_user_id, (m->>'store_id')::UUID, (m->>'role')::user_role) ON CONFLICT (user_id, store_id) DO UPDATE SET role = EXCLUDED.role;
	                END IF;
	            END IF;
	        END LOOP;
	    ELSIF p_store_id IS NOT NULL THEN
	        INSERT INTO public.user_store_memberships (user_id, store_id, role) VALUES (v_user_id, p_store_id, p_role) ON CONFLICT (user_id, store_id) DO UPDATE SET role = EXCLUDED.role;
	    END IF;
	    RETURN jsonb_build_object('success', true, 'user_id', v_user_id);
	END;
	$function$
2026-09-13 06:19:23.092 UTC [24102] ERROR:  type public.user_role does not exist
2026-09-13 06:19:23.092 UTC [24102] STATEMENT:  CREATE OR REPLACE FUNCTION public.managed_create_user_v2(
	  p_email text,
	  p_full_name text,
	  p_role public.user_role,
	  p_plan plan_t DEFAULT 'free'::plan_t,
	  p_store_id uuid DEFAULT NULL::uuid,
	  p_memberships jsonb DEFAULT NULL::jsonb,
	  p_max_stores int DEFAULT 0,
	  p_max_users int DEFAULT 0,
	  p_target_user_id uuid DEFAULT NULL::uuid,
	  p_creator_id uuid DEFAULT NULL::uuid
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  v_user_id uuid := COALESCE(p_target_user_id, gen_random_uuid());
	  v_role_id uuid;
	  v_active_store_id uuid;
	  m JSONB;
	  v_creator_role public.user_role;
	  v_creator_uid uuid := COALESCE(p_creator_id, auth.uid());
	BEGIN
	  -- Validar caller es admin o encargado
	  SELECT role INTO v_creator_role FROM public.profiles WHERE id = v_creator_uid;
	  IF v_creator_role IS NULL OR v_creator_role NOT IN ('admin', 'encargado', 'superadmin') THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED: Only admins and managers can create users.';
	  END IF;
	
	  -- Validar email único (entre perfiles activos)
	  IF EXISTS (SELECT 1 FROM public.profiles WHERE email = p_email AND deleted_at IS NULL) THEN
	    RAISE EXCEPTION 'ERR_EMAIL_ALREADY_EXISTS: %', p_email;
	  END IF;
	
	  v_active_store_id := COALESCE((p_memberships->0->>'store_id')::UUID, p_store_id);
	
	  -- Encargado solo puede crear en tiendas que gestiona
	  IF v_creator_role = 'encargado' AND v_active_store_id IS NOT NULL THEN
	    IF NOT public.has_store_access(v_active_store_id) THEN
	      RAISE EXCEPTION 'ERR_UNAUTHORIZED: No access to store %', v_active_store_id;
	    END IF;
	  END IF;
	
	  -- Buscar role_id
	  SELECT id INTO v_role_id FROM public.roles
	    WHERE lower(name) = lower(p_role::text)
	       OR (name = 'Cajero' AND p_role = 'clerk')
	       OR (name = 'Almacenero' AND p_role = 'warehouse')
	       OR (name = 'UserCosto' AND p_role = 'costo')
	    LIMIT 1;
	
	  -- INSERT profiles con plan
	  INSERT INTO public.profiles (
	    id, email, full_name, role, role_id, active_store_id, is_active,
	    created_by, max_stores_limit, max_users_limit, plan, created_at, updated_at
	  ) VALUES (
	    v_user_id, p_email, p_full_name, p_role, v_role_id, v_active_store_id, true,
	    v_creator_uid, p_max_stores, p_max_users, p_plan, now(), now()
	  )
	  ON CONFLICT (id) DO UPDATE SET
	    email = EXCLUDED.email,
	    full_name = EXCLUDED.full_name,
	    role = EXCLUDED.role,
	    role_id = EXCLUDED.role_id,
	    active_store_id = EXCLUDED.active_store_id,
	    plan = EXCLUDED.plan,
	    is_active = true,
	    deleted_at = NULL,
	    deletion_reason = NULL,
	    deleted_by = NULL,
	    updated_at = now()
	  RETURNING id INTO v_user_id;
	
	  -- Procesar memberships
	  IF p_memberships IS NOT NULL THEN
	    IF v_creator_role != 'admin' AND v_creator_role != 'superadmin' THEN
	      DELETE FROM public.user_store_memberships
	        WHERE user_id = v_user_id
	        AND store_id IN (
	          SELECT store_id FROM public.user_store_memberships
	          WHERE user_id = v_creator_uid AND role IN ('encargado', 'manager')
	        );
	    ELSE
	      DELETE FROM public.user_store_memberships WHERE user_id = v_user_id;
	    END IF;
	
	    FOR m IN SELECT * FROM jsonb_array_elements(p_memberships) LOOP
	      IF (m->>'store_id') IS NOT NULL AND (m->>'store_id') <> '' THEN
	        IF v_creator_role IN ('admin', 'superadmin') OR public.has_store_access((m->>'store_id')::UUID) THEN
	          INSERT INTO public.user_store_memberships (user_id, store_id, role)
	          VALUES (v_user_id, (m->>'store_id')::UUID, (m->>'role')::public.user_role)
	          ON CONFLICT (user_id, store_id) DO UPDATE SET role = EXCLUDED.role, status = 'active';
	        END IF;
	      END IF;
	    END LOOP;
	  ELSIF p_store_id IS NOT NULL THEN
	    INSERT INTO public.user_store_memberships (user_id, store_id, role)
	    VALUES (v_user_id, p_store_id, p_role)
	    ON CONFLICT (user_id, store_id) DO UPDATE SET role = EXCLUDED.role, status = 'active';
	  END IF;
	
	  -- Audit log atómico (regresión C-2 restaurada)
	  INSERT INTO public.user_audit_log (performed_by, target_user_id, action, new_values, metadata)
	  VALUES (
	    v_creator_uid, v_user_id, 'USER_CREATED',
	    jsonb_build_object(
	      'email', p_email, 'full_name', p_full_name, 'role', p_role::text,
	      'plan', p_plan::text, 'active_store_id', v_active_store_id,
	      'max_stores', p_max_stores, 'max_users', p_max_users
	    ),
	    jsonb_build_object('memberships_count', CASE WHEN p_memberships IS NOT NULL THEN jsonb_array_length(p_memberships) ELSE 0 END)
	  );
	
	  RETURN jsonb_build_object('success', true, 'user_id', v_user_id);
	END;
	$function$
2026-09-13 06:19:23.135 UTC [24109] ERROR:  type "public.user_role" does not exist at character 392
2026-09-13 06:19:23.135 UTC [24109] STATEMENT:  CREATE OR REPLACE FUNCTION public.managed_reset_password(
	  p_user_id uuid,
	  p_caller_id uuid DEFAULT NULL::uuid
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  -- FIX H-7: anti-spoofing
	  v_caller_uid uuid := CASE WHEN auth.role() = 'service_role'
	    THEN COALESCE(p_caller_id, auth.uid()) ELSE auth.uid() END;
	  v_caller_role public.user_role;
	  v_target_email text;
	BEGIN
	  SELECT role INTO v_caller_role FROM public.profiles WHERE id = v_caller_uid;
	  IF v_caller_role IS NULL OR v_caller_role NOT IN ('admin', 'superadmin') THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED';
	  END IF;
	
	  IF p_user_id = v_caller_uid THEN
	    RAISE EXCEPTION 'ERR_SELF_RESET_BLOCKED';
	  END IF;
	
	  SELECT email INTO v_target_email FROM public.profiles WHERE id = p_user_id AND deleted_at IS NULL;
	  IF v_target_email IS NULL THEN
	    RAISE EXCEPTION 'ERR_USER_NOT_FOUND';
	  END IF;
	
	  INSERT INTO public.user_audit_log (performed_by, target_user_id, action, metadata)
	  VALUES (
	    v_caller_uid, p_user_id, 'PASSWORD_RESET_REQUESTED',
	    jsonb_build_object('email', v_target_email, 'method', 'recovery_link')
	  );
	
	  RETURN jsonb_build_object('success', true, 'email', v_target_email);
	END;
	$function$
2026-09-13 06:19:23.165 UTC [24115] ERROR:  type "public.user_role" does not exist at character 427
2026-09-13 06:19:23.165 UTC [24115] STATEMENT:  CREATE OR REPLACE FUNCTION public.managed_soft_delete_user(
	  p_user_id uuid,
	  p_reason text,
	  p_caller_id uuid DEFAULT NULL::uuid
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  v_old RECORD;
	  -- FIX H-7: anti-spoofing
	  v_caller_uid uuid := CASE WHEN auth.role() = 'service_role'
	    THEN COALESCE(p_caller_id, auth.uid()) ELSE auth.uid() END;
	  v_caller_role public.user_role;
	  v_active_memberships_count int;
	  v_anon_email text;
	BEGIN
	  SELECT role INTO v_caller_role FROM public.profiles WHERE id = v_caller_uid;
	  IF v_caller_role IS NULL OR v_caller_role NOT IN ('admin', 'superadmin') THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED';
	  END IF;
	
	  IF p_user_id = v_caller_uid THEN
	    RAISE EXCEPTION 'ERR_SELF_DELETE_BLOCKED';
	  END IF;
	
	  SELECT id, email, full_name, role, plan, is_active INTO v_old
	    FROM public.profiles WHERE id = p_user_id AND deleted_at IS NULL FOR UPDATE;
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_USER_NOT_FOUND_OR_ALREADY_DELETED';
	  END IF;
	
	  SELECT COUNT(*) INTO v_active_memberships_count
	    FROM public.user_store_memberships
	    WHERE user_id = p_user_id AND status = 'active';
	  IF v_active_memberships_count > 0 THEN
	    RAISE EXCEPTION 'ERR_USER_HAS_ACTIVE_MEMBERSHIPS: % active. Revoke memberships first.', v_active_memberships_count;
	  END IF;
	
	  v_anon_email := 'deleted+' || substr(p_user_id::text, 1, 8) || '@anonymized.local';
	
	  UPDATE public.profiles SET
	    deleted_at = now(),
	    deletion_reason = p_reason,
	    deleted_by = v_caller_uid,
	    is_active = false,
	    full_name = '[deleted user]',
	    email = v_anon_email,
	    ai_api_key = NULL,
	    updated_at = now()
	  WHERE id = p_user_id;
	
	  UPDATE public.user_store_memberships SET
	    status = 'revoked',
	    updated_at = now()
	  WHERE user_id = p_user_id AND status = 'active';
	
	  INSERT INTO public.user_audit_log (performed_by, target_user_id, action, old_values, new_values, metadata)
	  VALUES (
	    v_caller_uid, p_user_id, 'USER_SOFT_DELETED',
	    jsonb_build_object(
	      'email', v_old.email, 'full_name', v_old.full_name,
	      'role', v_old.role::text, 'plan', v_old.plan::text, 'is_active', v_old.is_active
	    ),
	    jsonb_build_object(
	      'email', v_anon_email, 'full_name', '[deleted user]',
	      'is_active', false, 'deleted_at', now()
	    ),
	    jsonb_build_object('reason', p_reason, 'memberships_revoked', v_active_memberships_count)
	  );
	
	  RETURN jsonb_build_object(
	    'success', true,
	    'user_id', p_user_id,
	    'status', 'soft_deleted',
	    'note', 'auth.users preserved. API route should ban via auth.admin.updateUser.'
	  );
	END;
	$function$
2026-09-13 06:19:23.201 UTC [24121] ERROR:  type "public.user_role" does not exist at character 435
2026-09-13 06:19:23.201 UTC [24121] STATEMENT:  CREATE OR REPLACE FUNCTION public.managed_toggle_user_status(
	  p_user_id uuid,
	  p_is_active boolean,
	  p_caller_id uuid DEFAULT NULL::uuid
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  v_old RECORD;
	  -- FIX H-7: anti-spoofing
	  v_caller_uid uuid := CASE WHEN auth.role() = 'service_role'
	    THEN COALESCE(p_caller_id, auth.uid()) ELSE auth.uid() END;
	  v_caller_role public.user_role;
	BEGIN
	  SELECT role INTO v_caller_role FROM public.profiles WHERE id = v_caller_uid;
	  IF v_caller_role IS NULL OR v_caller_role NOT IN ('admin', 'superadmin', 'encargado', 'manager') THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED';
	  END IF;
	
	  IF p_is_active = false AND p_user_id = v_caller_uid THEN
	    RAISE EXCEPTION 'ERR_SELF_DEACTIVATE_BLOCKED';
	  END IF;
	
	  SELECT id, is_active, full_name INTO v_old
	    FROM public.profiles WHERE id = p_user_id AND deleted_at IS NULL FOR UPDATE;
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_USER_NOT_FOUND';
	  END IF;
	
	  IF v_old.is_active = p_is_active THEN
	    RETURN jsonb_build_object('success', true, 'no_change', true);
	  END IF;
	
	  UPDATE public.profiles SET is_active = p_is_active, updated_at = now() WHERE id = p_user_id;
	
	  INSERT INTO public.user_audit_log (performed_by, target_user_id, action, old_values, new_values)
	  VALUES (
	    v_caller_uid, p_user_id,
	    CASE WHEN p_is_active THEN 'USER_ACTIVATED' ELSE 'USER_DEACTIVATED' END,
	    jsonb_build_object('is_active', v_old.is_active),
	    jsonb_build_object('is_active', p_is_active)
	  );
	
	  RETURN jsonb_build_object('success', true, 'user_id', p_user_id, 'is_active', p_is_active);
	END;
	$function$
2026-09-13 06:19:23.224 UTC [24126] ERROR:  type public.user_role does not exist
2026-09-13 06:19:23.224 UTC [24126] STATEMENT:  CREATE OR REPLACE FUNCTION public.managed_update_membership(
	  p_membership_id uuid,
	  p_role public.user_role DEFAULT NULL::user_role,
	  p_status text DEFAULT NULL::text,
	  p_caller_id uuid DEFAULT NULL::uuid
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  v_old RECORD;
	  -- FIX H-7: anti-spoofing
	  v_caller_uid uuid := CASE WHEN auth.role() = 'service_role'
	    THEN COALESCE(p_caller_id, auth.uid()) ELSE auth.uid() END;
	  v_changes jsonb := '{}'::jsonb;
	BEGIN
	  SELECT m.id, m.user_id, m.store_id, m.role, m.status
	    INTO v_old
	    FROM public.user_store_memberships m
	    WHERE m.id = p_membership_id
	    FOR UPDATE;
	
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_MEMBERSHIP_NOT_FOUND';
	  END IF;
	
	  -- FIX H-7: Autorización usa nueva sobrecarga has_store_role(p_user_id, p_store_id, p_roles)
	  -- service_role (API route) pasa p_caller_id=session.user.id → verifica acceso de ese user
	  -- authenticated pasa p_caller_id=NULL → v_caller_uid=auth.uid() → verifica su propio acceso
	  IF NOT public.has_store_role(v_caller_uid, v_old.store_id, ARRAY['admin', 'manager']) THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED: Caller must be admin or manager of the store.';
	  END IF;
	
	  IF p_role IS NOT NULL AND p_role <> v_old.role THEN
	    v_changes := v_changes || jsonb_build_object('role', jsonb_build_object('old', v_old.role::text, 'new', p_role::text));
	    UPDATE public.user_store_memberships SET role = p_role, updated_at = now() WHERE id = p_membership_id;
	  END IF;
	
	  IF p_status IS NOT NULL AND p_status <> v_old.status::text THEN
	    IF p_status NOT IN ('active', 'revoked') THEN
	      RAISE EXCEPTION 'ERR_INVALID_STATUS: %', p_status;
	    END IF;
	    v_changes := v_changes || jsonb_build_object('status', jsonb_build_object('old', v_old.status, 'new', p_status));
	    UPDATE public.user_store_memberships SET status = p_status::membership_status, updated_at = now() WHERE id = p_membership_id;
	  END IF;
	
	  IF v_changes <> '{}'::jsonb THEN
	    INSERT INTO public.user_audit_log (performed_by, target_user_id, action, old_values, new_values, metadata)
	    VALUES (
	      v_caller_uid, v_old.user_id, 'MEMBERSHIP_UPDATED',
	      jsonb_build_object('membership_id', p_membership_id, 'store_id', v_old.store_id, 'role', v_old.role::text, 'status', v_old.status),
	      v_changes,
	      jsonb_build_object('store_id', v_old.store_id)
	    );
	  END IF;
	
	  RETURN jsonb_build_object('success', true, 'membership_id', p_membership_id, 'changes', v_changes);
	END;
	$function$
2026-09-13 06:19:23.251 UTC [24131] ERROR:  type plan_t does not exist
2026-09-13 06:19:23.251 UTC [24131] STATEMENT:  CREATE OR REPLACE FUNCTION public.managed_update_tenant_plan(
	  p_tenant_id uuid,
	  p_plan plan_t,
	  p_subscription_status text DEFAULT NULL,
	  p_caller_id uuid DEFAULT NULL
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  -- FIX H-7: anti-spoofing — service_role (Stripe webhook) puede pasar NULL caller_id
	  v_caller_uid uuid := CASE WHEN auth.role() = 'service_role'
	    THEN COALESCE(p_caller_id, auth.uid()) ELSE auth.uid() END;
	  v_old_plan plan_t;
	  v_old_status text;
	  v_role public.user_role;
	BEGIN
	  -- Validación: si caller_uid es NULL (service_role anónimo para Stripe webhook), permitir
	  -- Si caller_uid NO es NULL, debe ser admin/superadmin
	  IF v_caller_uid IS NOT NULL THEN
	    SELECT role INTO v_role FROM public.profiles WHERE id = v_caller_uid;
	    IF v_role IS NULL OR v_role NOT IN ('admin', 'superadmin') THEN
	      RAISE EXCEPTION 'ERR_UNAUTHORIZED: Only admins can change tenant plan';
	    END IF;
	  END IF;
	
	  SELECT plan, subscription_status INTO v_old_plan, v_old_status
	    FROM public.tenants WHERE id = p_tenant_id FOR UPDATE;
	
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_TENANT_NOT_FOUND: %', p_tenant_id;
	  END IF;
	
	  UPDATE public.tenants SET
	    plan = p_plan,
	    subscription_status = COALESCE(p_subscription_status, subscription_status),
	    updated_at = now()
	  WHERE id = p_tenant_id;
	
	  UPDATE public.profiles SET
	    plan = p_plan,
	    updated_at = now()
	  WHERE tenant_id = p_tenant_id AND deleted_at IS NULL;
	
	  INSERT INTO public.user_audit_log (performed_by, target_user_id, action, old_values, new_values, metadata)
	  VALUES (
	    v_caller_uid, NULL, 'TENANT_PLAN_UPDATED',
	    jsonb_build_object('old_plan', v_old_plan::text, 'old_status', v_old_status),
	    jsonb_build_object('new_plan', p_plan::text, 'new_status', COALESCE(p_subscription_status, v_old_status)),
	    jsonb_build_object('tenant_id', p_tenant_id)
	  );
	
	  RETURN jsonb_build_object(
	    'success', true,
	    'tenant_id', p_tenant_id,
	    'old_plan', v_old_plan::text,
	    'new_plan', p_plan::text
	  );
	END;
	$function$
2026-09-13 06:19:23.278 UTC [24136] ERROR:  type public.user_role does not exist
2026-09-13 06:19:23.278 UTC [24136] STATEMENT:  CREATE OR REPLACE FUNCTION public.managed_update_user(
	  p_user_id uuid,
	  p_full_name text DEFAULT NULL::text,
	  p_role public.user_role DEFAULT NULL::user_role,
	  p_role_id uuid DEFAULT NULL::uuid,
	  p_is_active boolean DEFAULT NULL::boolean,
	  p_max_stores_limit int DEFAULT NULL::int,
	  p_max_users_limit int DEFAULT NULL::int,
	  p_plan plan_t DEFAULT NULL::plan_t,
	  p_caller_id uuid DEFAULT NULL::uuid
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  v_old RECORD;
	  -- FIX H-7: anti-spoofing — service_role puede pasar p_caller_id explícito
	  v_caller_uid uuid := CASE WHEN auth.role() = 'service_role'
	    THEN COALESCE(p_caller_id, auth.uid()) ELSE auth.uid() END;
	  v_caller_role public.user_role;
	  v_changes jsonb := '{}'::jsonb;
	BEGIN
	  -- Validar caller es admin (usa v_caller_uid que ahora NO es spoofable para authenticated)
	  SELECT role INTO v_caller_role FROM public.profiles WHERE id = v_caller_uid;
	  IF v_caller_role IS NULL OR v_caller_role NOT IN ('admin', 'superadmin') THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED: Only admins can update users.';
	  END IF;
	
	  SELECT * INTO v_old FROM public.profiles WHERE id = p_user_id AND deleted_at IS NULL FOR UPDATE;
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_USER_NOT_FOUND: %', p_user_id;
	  END IF;
	
	  IF p_is_active = false AND p_user_id = v_caller_uid THEN
	    RAISE EXCEPTION 'ERR_SELF_DEACTIVATE_BLOCKED: Cannot deactivate own account.';
	  END IF;
	
	  IF p_full_name IS NOT NULL AND p_full_name <> v_old.full_name THEN
	    v_changes := v_changes || jsonb_build_object('full_name', jsonb_build_object('old', v_old.full_name, 'new', p_full_name));
	    UPDATE public.profiles SET full_name = p_full_name, updated_at = now() WHERE id = p_user_id;
	  END IF;
	
	  IF p_role IS NOT NULL AND p_role <> v_old.role THEN
	    v_changes := v_changes || jsonb_build_object('role', jsonb_build_object('old', v_old.role::text, 'new', p_role::text));
	    UPDATE public.profiles SET role = p_role, updated_at = now() WHERE id = p_user_id;
	  END IF;
	
	  IF p_role_id IS NOT NULL AND (v_old.role_id IS NULL OR p_role_id <> v_old.role_id) THEN
	    v_changes := v_changes || jsonb_build_object('role_id', jsonb_build_object('old', v_old.role_id, 'new', p_role_id));
	    UPDATE public.profiles SET role_id = p_role_id, updated_at = now() WHERE id = p_user_id;
	  END IF;
	
	  IF p_is_active IS NOT NULL AND p_is_active <> v_old.is_active THEN
	    v_changes := v_changes || jsonb_build_object('is_active', jsonb_build_object('old', v_old.is_active, 'new', p_is_active));
	    UPDATE public.profiles SET is_active = p_is_active, updated_at = now() WHERE id = p_user_id;
	  END IF;
	
	  IF p_max_stores_limit IS NOT NULL AND (v_old.max_stores_limit IS NULL OR p_max_stores_limit <> v_old.max_stores_limit) THEN
	    v_changes := v_changes || jsonb_build_object('max_stores_limit', jsonb_build_object('old', v_old.max_stores_limit, 'new', p_max_stores_limit));
	    UPDATE public.profiles SET max_stores_limit = p_max_stores_limit, updated_at = now() WHERE id = p_user_id;
	  END IF;
	
	  IF p_max_users_limit IS NOT NULL AND (v_old.max_users_limit IS NULL OR p_max_users_limit <> v_old.max_users_limit) THEN
	    v_changes := v_changes || jsonb_build_object('max_users_limit', jsonb_build_object('old', v_old.max_users_limit, 'new', p_max_users_limit));
	    UPDATE public.profiles SET max_users_limit = p_max_users_limit, updated_at = now() WHERE id = p_user_id;
	  END IF;
	
	  IF p_plan IS NOT NULL AND p_plan <> v_old.plan THEN
	    v_changes := v_changes || jsonb_build_object('plan', jsonb_build_object('old', v_old.plan::text, 'new', p_plan::text));
	    UPDATE public.profiles SET plan = p_plan, updated_at = now() WHERE id = p_user_id;
	  END IF;
	
	  IF v_changes <> '{}'::jsonb THEN
	    INSERT INTO public.user_audit_log (performed_by, target_user_id, action, old_values, new_values, metadata)
	    VALUES (
	      v_caller_uid, p_user_id, 'USER_UPDATED',
	      jsonb_build_object(
	        'full_name', v_old.full_name, 'role', v_old.role::text,
	        'is_active', v_old.is_active, 'plan', v_old.plan::text
	      ),
	      v_changes,
	      jsonb_build_object('fields_changed', jsonb_object_keys(v_changes))
	    );
	  END IF;
	
	  RETURN jsonb_build_object('success', true, 'user_id', p_user_id, 'changes', v_changes);
	END;
	$function$
2026-09-13 06:19:23.329 UTC [24143] ERROR:  type "user_role" does not exist at character 204
2026-09-13 06:19:23.329 UTC [24143] STATEMENT:  CREATE OR REPLACE FUNCTION public.on_auth_user_created()
	RETURNS trigger
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO 'public', 'extensions'
	AS $function$
	DECLARE
	  v_role_id UUID;
	  v_role_enum USER_ROLE;
	  v_metadata_role TEXT;
	  v_company_name TEXT;
	  v_full_name TEXT;
	  v_tenant_id UUID;
	BEGIN
	  -- Extract role from metadata if present
	  v_metadata_role := NEW.raw_user_meta_data->>'role';
	
	  -- Normalize and convert to enum
	  IF v_metadata_role IS NOT NULL THEN
	    BEGIN
	      v_role_enum := v_metadata_role::USER_ROLE;
	    EXCEPTION WHEN OTHERS THEN
	      v_role_enum := 'tenant_admin'::USER_ROLE;
	    END;
	  ELSE
	    -- Iteración 13: self-signup users are tenant_admin by default
	    v_role_enum := 'tenant_admin'::USER_ROLE;
	  END IF;
	
	  -- Extract company_name and full_name from metadata
	  v_company_name := NEW.raw_user_meta_data->>'company_name';
	  v_full_name := COALESCE(NEW.raw_user_meta_data->>'full_name', split_part(NEW.email, '@', 1));
	
	  -- Iteración 13: Create tenant for self-signup users
	  -- If user was created by admin (via managed_create_user_v2), they may already
	  -- have a tenant_id set via p_target_user_id. In that case, skip tenant creation.
	  IF v_company_name IS NOT NULL THEN
	    INSERT INTO public.tenants (name, owner_id, plan, subscription_status, trial_ends_at, is_active)
	    VALUES (
	      v_company_name,
	      NEW.id,
	      'free'::plan_t,
	      'trial',
	      now() + interval '14 days',
	      true
	    )
	    RETURNING id INTO v_tenant_id;
	  END IF;
	
	  -- Get matching role_id from public.roles
	  SELECT id INTO v_role_id FROM public.roles
	    WHERE name = v_metadata_role OR lower(name) = lower(v_role_enum::text)
	    LIMIT 1;
	
	  -- Insert profile with tenant_id
	  INSERT INTO public.profiles (id, email, full_name, role, role_id, is_active, tenant_id, plan, created_at, updated_at)
	  VALUES (
	    NEW.id,
	    NEW.email,
	    v_full_name,
	    v_role_enum,
	    v_role_id,
	    true,
	    v_tenant_id,
	    'free'::plan_t,
	    now(),
	    now()
	  )
	  ON CONFLICT (id) DO UPDATE SET
	    email = EXCLUDED.email,
	    full_name = EXCLUDED.full_name,
	    role = EXCLUDED.role,
	    role_id = EXCLUDED.role_id,
	    tenant_id = COALESCE(profiles.tenant_id, EXCLUDED.tenant_id),
	    plan = EXCLUDED.plan,
	    is_active = true,
	    updated_at = now();
	
	  -- Audit log
	  INSERT INTO public.user_audit_log (performed_by, target_user_id, action, new_values, metadata)
	  VALUES (
	    NEW.id, NEW.id, 'USER_REGISTERED_PUBLICLY',
	    jsonb_build_object('email', NEW.email, 'role', v_role_enum::text, 'tenant_id', v_tenant_id),
	    jsonb_build_object('company_name', v_company_name, 'source', 'self_signup')
	  );
	
	  RETURN NEW;
	END;
	$function$
2026-09-13 06:19:23.421 UTC [24158] ERROR:  type adjustment_item[] does not exist
2026-09-13 06:19:23.421 UTC [24158] STATEMENT:  CREATE OR REPLACE FUNCTION public.process_inventory_adjustment(p_store_id uuid, p_cashier_id uuid, p_items adjustment_item[], p_operation_date timestamp with time zone DEFAULT NULL::timestamp with time zone)
	 RETURNS uuid
	 LANGUAGE plpgsql
	 SECURITY DEFINER
	 SET search_path TO 'public'
	AS $function$
	DECLARE
	  v_adjustment_id UUID;
	  v_item public.adjustment_item;
	  v_difference NUMERIC;
	  v_effective_date TIMESTAMP WITH TIME ZONE := COALESCE(p_operation_date, NOW());
	BEGIN
	  -- Validación forward-only locking
	  PERFORM public.validate_operation_date(p_operation_date, p_store_id);
	
	  INSERT INTO public.inventory_adjustments (store_id, created_by, status, created_at)
	  VALUES (p_store_id, p_cashier_id, 'PROCESSING', v_effective_date)
	  RETURNING id INTO v_adjustment_id;
	
	  FOREACH v_item IN ARRAY p_items
	  LOOP
	    v_difference := v_item.counted_quantity - v_item.expected_quantity;
	    INSERT INTO public.inventory_adjustment_items (adjustment_id, product_id, expected_quantity, counted_quantity, created_at)
	    VALUES (v_adjustment_id, v_item.product_id, v_item.expected_quantity, v_item.counted_quantity, v_effective_date);
	
	    PERFORM public.register_stock_movement(
	        p_product_id := v_item.product_id,
	        p_store_id := p_store_id,
	        p_user_id := p_cashier_id,
	        p_quantity := v_difference,
	        p_movement_type := 'adjustment',
	        p_operation_date := v_effective_date
	    );
	  END LOOP;
	
	  UPDATE public.inventory_adjustments SET status = 'COMPLETED', updated_at = v_effective_date
	  WHERE id = v_adjustment_id;
	  RETURN v_adjustment_id;
	END;
	$function$
2026-09-13 06:19:23.464 UTC [24167] ERROR:  type "public.purchase_status_enum" does not exist at character 425
2026-09-13 06:19:23.464 UTC [24167] STATEMENT:  CREATE OR REPLACE FUNCTION public.receive_against_po(
	  p_po_id uuid,
	  p_received_items jsonb DEFAULT '[]'::jsonb,
	  p_user_id uuid DEFAULT NULL,
	  p_reception_date timestamptz DEFAULT now(),
	  p_invoice_number text DEFAULT NULL
	) RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO 'public', 'extensions'
	AS $func$
	DECLARE
	  v_store_id            uuid;
	  v_supplier_name       text;
	  v_po_status           public.purchase_status_enum;
	  v_item                jsonb;
	  v_item_id             uuid;
	  v_qty                 numeric;
	  v_qty_ordered         numeric;
	  v_qty_received_before numeric;
	  v_product_id          uuid;
	  v_unit_cost           numeric;
	  v_product_name        text;
	  v_all_received        boolean;
	  v_any_received        boolean;
	  v_new_status          public.purchase_status_enum;
	  v_receipt_id          uuid;
	  v_reception_items     jsonb := '[]'::jsonb;
	  v_count               integer := 0;
	  v_po_number           text;
	  v_invoice_to_pass     text;
	  v_sorted_items        jsonb;
	BEGIN
	  -- ─── 1. Cargar OC con lock exclusivo ───
	  SELECT po.store_id, po.supplier_name, po.status, po.po_number
	    INTO v_store_id, v_supplier_name, v_po_status, v_po_number
	  FROM public.purchase_orders po
	  WHERE po.id = p_po_id
	  FOR UPDATE;
	
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_PO_NOT_FOUND';
	  END IF;
	
	  -- ─── 2. Validar acceso (tenant-aware via has_store_access) ───
	  IF NOT public.has_store_access(v_store_id) THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED';
	  END IF;
	
	  -- ─── 3. Validar estado de la OC ───
	  IF v_po_status = 'cancelled' THEN
	    RAISE EXCEPTION 'ERR_PO_CANCELLED';
	  END IF;
	  -- Solo se puede recibir contra draft, sent o partial.
	  IF v_po_status NOT IN ('draft', 'sent', 'partial') THEN
	    RAISE EXCEPTION 'ERR_PO_NOT_RECEIVABLE: status % is terminal', v_po_status;
	  END IF;
	
	  -- ─── 4. Validar items no vacío (parity B3 register_reception) ───
	  IF p_received_items IS NULL OR jsonb_array_length(p_received_items) = 0 THEN
	    RAISE EXCEPTION 'ERR_EMPTY_ITEMS';
	  END IF;
	
	  -- ─── 5. Condición #5 (anti-deadlock): Ordenar items por po_item_id ASC ───
	  SELECT jsonb_agg(elem ORDER BY (elem->>'po_item_id'))
	    INTO v_sorted_items
	  FROM jsonb_array_elements(p_received_items) AS elem;
	
	  -- ─── 6. Procesar cada item: VALIDAR antes de actualizar ───
	  FOR v_item IN SELECT * FROM jsonb_array_elements(v_sorted_items) LOOP
	    v_item_id := NULLIF(v_item->>'po_item_id', '')::uuid;
	    v_qty     := COALESCE((v_item->>'quantity_received')::numeric, 0);
	
	    -- Validar po_item_id presente
	    IF v_item_id IS NULL THEN
	      RAISE EXCEPTION 'ERR_ITEM_ID_REQUIRED: missing po_item_id in received_items';
	    END IF;
	
	    -- Medio #6: qty > 0
	    IF v_qty <= 0 THEN
	      RAISE EXCEPTION 'ERR_NEGATIVE_QTY: qty % for item % must be > 0', v_qty, v_item_id;
	    END IF;
	
	    -- Cargar estado actual del item con lock FOR UPDATE
	    SELECT product_id, quantity_ordered, quantity_received, unit_cost, product_name
	      INTO v_product_id, v_qty_ordered, v_qty_received_before, v_unit_cost, v_product_name
	    FROM public.purchase_order_items
	    WHERE id = v_item_id AND po_id = p_po_id
	    FOR UPDATE;
	
	    IF NOT FOUND THEN
	      RAISE EXCEPTION 'ERR_ITEM_NOT_FOUND: %', v_item_id;
	    END IF;
	
	    -- Alto #5: over-receive check
	    IF (v_qty_received_before + v_qty) > v_qty_ordered THEN
	      RAISE EXCEPTION 'ERR_OVER_RECEIVE: item % ordered %, already received %, attempting %',
	        v_item_id, v_qty_ordered, v_qty_received_before, v_qty;
	    END IF;
	
	    -- Condición #1: todo item debe tener product_id NOT NULL
	    IF v_product_id IS NULL THEN
	      RAISE EXCEPTION 'ERR_PRODUCT_ID_REQUIRED: item % has no product_id', v_item_id;
	    END IF;
	
	    -- Medio #9: producto debe existir en store
	    IF NOT EXISTS (
	      SELECT 1 FROM public.products p
	      WHERE p.id = v_product_id AND p.store_id = v_store_id
	    ) THEN
	      RAISE EXCEPTION 'ERR_PRODUCT_NOT_IN_STORE: product % not in store %',
	        v_product_id, v_store_id;
	    END IF;
	
	    -- Acumular para register_reception (formato esperado: product_id, quantity, unit_cost, moneda, tasa)
	    v_reception_items := v_reception_items || jsonb_build_object(
	      'product_id',   v_product_id,
	      'product_name', v_product_name,
	      'quantity',     v_qty,
	      'unit_cost',    v_unit_cost,
	      'moneda_recepcion', 'CUP',
	      'tasa_cambio_recepcion', 1.0
	    );
	    v_count := v_count + 1;
	  END LOOP;
	
	  -- ─── 7. Condición #3: reference_doc NULL si no viene invoice_number ───
	  -- Evita colisión con UNIQUE INDEX idx_receipts_store_reference_doc
	  -- (store_id, reference_doc) en partial indexes.
	  -- El vínculo real receipt↔PO es receipts.po_id (G6).
	  IF p_invoice_number IS NULL OR p_invoice_number = '' THEN
	    v_invoice_to_pass := NULL;
	  ELSE
	    v_invoice_to_pass := p_invoice_number;
	  END IF;
	
	  -- ─── 8. Crear receipt vinculado vía register_reception ───
	  -- register_reception aplica las 8 validaciones B1-B5 + C1-C3 del v2.23.0
	  -- (no se relajan). G6 añade p_po_id opcional.
	  v_receipt_id := public.register_reception(
	    p_store_id        := v_store_id,
	    p_supplier        := v_supplier_name,
	    p_reception_date  := p_reception_date,
	    p_invoice_number  := v_invoice_to_pass,
	    p_items           := v_reception_items,
	    p_user_id         := p_user_id,
	    p_po_id           := p_po_id
	  );
	
	  -- ─── 9. Actualizar quantity_received en purchase_order_items ───
	  -- (SECURITY DEFINER bypasses RLS DENY UPDATE de G5)
	  FOR v_item IN SELECT * FROM jsonb_array_elements(v_sorted_items) LOOP
	    v_item_id := NULLIF(v_item->>'po_item_id', '')::uuid;
	    v_qty     := (v_item->>'quantity_received')::numeric;
	
	    UPDATE public.purchase_order_items
	    SET quantity_received = quantity_received + v_qty
	    WHERE id = v_item_id AND po_id = p_po_id;
	  END LOOP;
	
	  -- ─── 10. Recalcular status global (cast EXPLÍCITO a enum) ───
	  SELECT
	    BOOL_AND(quantity_received >= quantity_ordered),
	    BOOL_OR(quantity_received > 0)
	  INTO v_all_received, v_any_received
	  FROM public.purchase_order_items WHERE po_id = p_po_id;
	
	  v_new_status :=
	    CASE
	      WHEN v_all_received THEN 'received'::public.purchase_status_enum
	      WHEN v_any_received THEN 'partial'::public.purchase_status_enum
	      ELSE 'sent'::public.purchase_status_enum
	    END;
	
	  -- ─── 11. Actualizar OC + vincular receipt en notas ───
	  UPDATE public.purchase_orders
	  SET status       = v_new_status,
	      received_at  = CASE WHEN v_new_status = 'received' THEN NOW() ELSE received_at END,
	      notes        = COALESCE(notes, '') ||
	                     CASE WHEN COALESCE(notes, '') = '' THEN '' ELSE E'\n' END ||
	                     '[Receipt ' || v_receipt_id::text || ' linked at ' || NOW()::text || ']'
	  WHERE id = p_po_id;
	
	  -- ─── 12. CxP (Grupo 6): marcar receipt con payment_status='unpaid' ───
	  -- Solo si aún no está seteado (no sobrescribir si register_reception ya lo puso).
	  UPDATE public.receipts
	  SET payment_status     = COALESCE(NULLIF(payment_status, ''), 'unpaid'),
	      payment_terms_days = COALESCE(payment_terms_days, 30),
	      due_date           = COALESCE(due_date, (p_reception_date::date + INTERVAL '30 days')::date)
	  WHERE id = v_receipt_id;
	
	  -- ─── 13. Auditoría ───
	  INSERT INTO public.audit_logs (user_id, store_id, action, table_name, record_id, metadata)
	  VALUES (
	    p_user_id, v_store_id, 'po_received', 'purchase_orders', p_po_id,
	    jsonb_build_object(
	      'new_status', v_new_status::text,
	      'items_received', v_count,
	      'receipt_id', v_receipt_id,
	      'po_number', v_po_number,
	      'invoice_number', v_invoice_to_pass
	    )
	  );
	
	  RETURN jsonb_build_object(
	    'status', 'success',
	    'po_status', v_new_status::text,
	    'receipt_id', v_receipt_id,
	    'items_received', v_count
	  );
	END;
	$func$
2026-09-13 06:19:23.613 UTC [24204] ERROR:  type purchase_status_enum does not exist
2026-09-13 06:19:23.613 UTC [24204] STATEMENT:  CREATE OR REPLACE FUNCTION public.set_purchase_order_status(p_po_id uuid, p_new_status purchase_status_enum, p_user_id uuid DEFAULT NULL::uuid, p_reason text DEFAULT NULL::text)
	 RETURNS jsonb
	 LANGUAGE plpgsql
	 SECURITY DEFINER
	 SET search_path TO 'public', 'extensions'
	AS $function$
	DECLARE
	  v_store_id     uuid;
	  v_current      public.purchase_status_enum;
	  v_po_number    text;
	  v_allowed      text[];
	  v_is_allowed   boolean := false;
	BEGIN
	  -- ─── 1. Cargar PO con lock exclusivo ───
	  SELECT store_id, status, po_number
	    INTO v_store_id, v_current, v_po_number
	  FROM public.purchase_orders
	  WHERE id = p_po_id
	  FOR UPDATE;
	
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_PO_NOT_FOUND';
	  END IF;
	
	  -- ─── 2. Validar acceso (tenant-aware) ───
	  IF NOT public.has_store_access(v_store_id) THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED';
	  END IF;
	
	  -- ─── 3. Si no hay cambio, retornar success sin hacer nada ───
	  IF v_current = p_new_status THEN
	    RETURN jsonb_build_object(
	      'status', 'no_change',
	      'po_status', v_current::text,
	      'po_number', v_po_number
	    );
	  END IF;
	
	  -- ─── 4. Definir transiciones permitidas (state machine) ───
	  -- 'partial' y 'received' NUNCA son destino válido manualmente
	  -- (solo vía receive_against_po).
	  v_allowed := CASE v_current
	    WHEN 'draft'   THEN ARRAY['sent', 'cancelled']
	    WHEN 'sent'    THEN ARRAY['cancelled']
	    WHEN 'partial' THEN ARRAY['cancelled']
	    ELSE ARRAY[]::text[]  -- received, cancelled: terminal, no transitions
	  END;
	
	  -- ─── 5. Verificar transición permitida ───
	  SELECT EXISTS(SELECT 1 FROM unnest(v_allowed) a WHERE a = p_new_status::text)
	    INTO v_is_allowed;
	
	  IF NOT v_is_allowed THEN
	    RAISE EXCEPTION 'ERR_INVALID_TRANSITION: % → % not allowed (allowed: %)',
	      v_current::text, p_new_status::text,
	      CASE WHEN array_length(v_allowed, 1) IS NULL THEN 'none' ELSE array_to_string(v_allowed, ', ') END;
	  END IF;
	
	  -- ─── 6. Aplicar transición ───
	  UPDATE public.purchase_orders
	  SET status      = p_new_status,
	      received_at = CASE WHEN p_new_status = 'received' THEN NOW() ELSE received_at END
	  WHERE id = p_po_id;
	
	  -- ─── 7. Auditoría (action = PO_STATUS_CHANGED) ───
	  INSERT INTO public.audit_logs (user_id, store_id, action, table_name, record_id, metadata)
	  VALUES (
	    CASE WHEN auth.role() = 'service_role' THEN COALESCE(p_user_id, auth.uid()) ELSE auth.uid() END, v_store_id, 'PO_STATUS_CHANGED', 'purchase_orders', p_po_id,
	    jsonb_build_object(
	      'po_number', v_po_number,
	      'from_status', v_current::text,
	      'to_status', p_new_status::text,
	      'reason', p_reason
	    )
	  );
	
	  RETURN jsonb_build_object(
	    'status', 'success',
	    'po_status', p_new_status::text,
	    'po_number', v_po_number,
	    'previous_status', v_current::text
	  );
	END;
	$function$
2026-09-13 06:19:23.742 UTC [24230] ERROR:  type "user_role" does not exist at character 100
2026-09-13 06:19:23.742 UTC [24230] STATEMENT:  CREATE OR REPLACE FUNCTION public.validate_active_store()
	RETURNS TRIGGER AS $$
	DECLARE
	    v_role user_role;
	BEGIN
	    v_role := NEW.role;
	
	    -- Si role es 'costo' o 'admin' o 'superadmin', active_store_id es opcional
	    IF v_role = 'costo'::user_role
	       OR v_role = 'admin'::user_role
	       OR v_role = 'superadmin'::user_role THEN
	        -- Solo validar si active_store_id cambió o es INSERT
	        IF TG_OP = 'INSERT' OR (OLD.active_store_id IS DISTINCT FROM NEW.active_store_id) THEN
	            IF NEW.active_store_id IS NOT NULL THEN
	                IF NOT EXISTS (
	                    SELECT 1 FROM public.user_store_memberships
	                    WHERE user_id = NEW.id
	                      AND store_id = NEW.active_store_id
	                      AND status = 'active'
	                ) THEN
	                    IF TG_OP = 'UPDATE' THEN
	                        RAISE EXCEPTION 'ERR_INVALID_ACTIVE_STORE: El usuario no tiene membership activa en la tienda seleccionada.';
	                    END IF;
	                END IF;
	            END IF;
	        END IF;
	        RETURN NEW;
	    END IF;
	
	    -- Para roles operativos (encargado, clerk, warehouse, manager, usuario)
	    IF TG_OP = 'INSERT' THEN
	        -- En INSERT: ser permisivo. La función managed_create_user inserta
	        -- memberships justo después. No validar nada aquí.
	        RETURN NEW;
	    ELSE
	        -- UPDATE: solo validar si active_store_id cambió realmente
	        -- (evita bloquear updates de otros campos en perfiles intermedios)
	        IF OLD.active_store_id IS NOT DISTINCT FROM NEW.active_store_id THEN
	            -- active_store_id no cambió → permitir el UPDATE sin validar
	            RETURN NEW;
	        END IF;
	
	        -- active_store_id cambió → validar consistencia
	        IF NEW.active_store_id IS NULL THEN
	            IF v_role IN ('encargado'::user_role, 'clerk'::user_role, 'warehouse'::user_role) THEN
	                RAISE EXCEPTION 'ERR_STORE_REQUIRED: El rol % requiere una tienda activa asignada.', v_role;
	            END IF;
	        ELSE
	            IF NOT EXISTS (
	                SELECT 1 FROM public.user_store_memberships
	                WHERE user_id = NEW.id
	                  AND store_id = NEW.active_store_id
	                  AND status = 'active'
	            ) THEN
	                RAISE EXCEPTION 'ERR_INVALID_ACTIVE_STORE: El usuario no tiene membership activa en la tienda seleccionada.';
	            END IF;
	        END IF;
	        RETURN NEW;
	    END IF;
	END;
	$$ LANGUAGE plpgsql SECURITY DEFINER
2026-09-13 06:19:23.800 UTC [24241] ERROR:  type "public.user_role" does not exist at character 270
2026-09-13 06:19:23.800 UTC [24241] STATEMENT:  CREATE OR REPLACE FUNCTION public.validate_tenant_access(
	  p_user_id uuid,
	  p_store_id uuid
	)
	RETURNS boolean
	LANGUAGE plpgsql
	STABLE SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  v_user_tenant uuid;
	  v_store_tenant uuid;
	  v_user_role public.user_role;
	BEGIN
	  -- Global admin bypasses tenant check
	  SELECT role, tenant_id INTO v_user_role, v_user_tenant
	    FROM public.profiles WHERE id = p_user_id;
	
	  IF v_user_role IN ('admin', 'superadmin') THEN
	    RETURN true;
	  END IF;
	
	  -- Get store's tenant
	  SELECT tenant_id INTO v_store_tenant FROM public.stores WHERE id = p_store_id;
	
	  IF v_store_tenant IS NULL THEN
	    -- Legacy store without tenant — allow (backward compat)
	    RETURN true;
	  END IF;
	
	  -- Check tenant match
	  RETURN v_user_tenant = v_store_tenant;
	END;
	$function$
2026-09-13 06:19:23.985 UTC [24276] ERROR:  type "public.user_role" does not exist at character 301
2026-09-13 06:19:23.985 UTC [24276] STATEMENT:  CREATE OR REPLACE FUNCTION public.bulk_assign_memberships(
	  p_user_id uuid,
	  p_assignments jsonb
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  v_assignment jsonb;
	  v_affected int := 0;
	  v_failed int := 0;
	  v_store_id uuid;
	  v_role public.user_role;
	  v_status text;
	  v_caller_uid uuid := auth.uid();
	  v_changes jsonb := '[]'::jsonb;
	BEGIN
	  IF v_caller_uid IS NULL THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED';
	  END IF;
	
	  FOR v_assignment IN SELECT * FROM jsonb_array_elements(p_assignments) LOOP
	    BEGIN
	      v_store_id := (v_assignment->>'store_id')::uuid;
	      v_role := (v_assignment->>'role')::public.user_role;
	      v_status := COALESCE(v_assignment->>'status', 'active');
	
	      -- Validar caller tiene acceso al store
	      IF NOT public.is_admin() AND NOT public.has_store_role(v_store_id, ARRAY['admin', 'manager']) THEN
	        v_failed := v_failed + 1;
	        CONTINUE;
	      END IF;
	
	      INSERT INTO public.user_store_memberships (user_id, store_id, role, status)
	      VALUES (p_user_id, v_store_id, v_role, v_status)
	      ON CONFLICT (user_id, store_id) DO UPDATE SET
	        role = EXCLUDED.role,
	        status = EXCLUDED.status,
	        updated_at = now();
	
	      v_changes := v_changes || jsonb_build_object(jsonb_build_object(
	        'store_id', v_store_id,
	        'role', v_role::text,
	        'status', v_status
	      ));
	
	      v_affected := v_affected + 1;
	    EXCEPTION
	      WHEN foreign_key_violation THEN
	        v_failed := v_failed + 1;
	    END;
	  END LOOP;
	
	  -- Audit log atómico (solo si hubo cambios)
	  IF v_affected > 0 THEN
	    INSERT INTO public.user_audit_log (performed_by, target_user_id, action, new_values, metadata)
	    VALUES (
	      v_caller_uid, p_user_id, 'MEMBERSHIPS_BULK_ASSIGNED',
	      jsonb_build_object('assignments', v_changes),
	      jsonb_build_object('affected', v_affected, 'failed', v_failed)
	    );
	  END IF;
	
	  RETURN jsonb_build_object(
	    'success', true,
	    'affected', v_affected,
	    'failed', v_failed,
	    'user_id', p_user_id
	  );
	END;
	$function$
2026-09-13 06:19:24.011 UTC [24281] ERROR:  syntax error at or near "REVOKE" at character 5039
2026-09-13 06:19:24.011 UTC [24281] STATEMENT:  CREATE OR REPLACE FUNCTION public.bulk_soft_delete_stores(p_store_ids uuid[], p_deleted_by uuid, p_confirmation_token text, p_override_token text DEFAULT NULL::text, p_reason text DEFAULT NULL::text)
	 RETURNS jsonb
	 LANGUAGE plpgsql
	 SECURITY DEFINER
	 SET search_path TO 'public'
	AS $function$
	DECLARE
	  v_store_id UUID;
	  v_validation JSONB;
	  v_blockers JSONB;
	  v_errors JSONB[] := '{}'::jsonb[];
	  v_processed INTEGER := 0;
	  v_token_valid BOOLEAN;
	  v_has_protected BOOLEAN;
	  v_override_valid BOOLEAN;
	  v_confirmation_record RECORD;
	  v_override_record RECORD;
	  v_caller_role TEXT;
	BEGIN
	  -- ============================================================
	  -- AUTH CHECK: Solo admin puede ejecutar esta función
	  -- ============================================================
	  IF auth.uid() IS NOT NULL THEN
	    SELECT role INTO v_caller_role FROM public.profiles WHERE id = auth.uid();
	    IF v_caller_role IS NULL OR v_caller_role != 'admin' THEN
	      RAISE EXCEPTION 'ERR_PERMISSION_DENIED: Solo admin puede ejecutar bulk_soft_delete_stores';
	    END IF;
	  END IF;
	  -- Si auth.uid() IS NULL, es service_role — permitir
	
	  -- ============================================================
	  -- 1. VALIDATE confirmation_token
	  -- ============================================================
	  SELECT * INTO v_confirmation_record
	  FROM public.bulk_confirmation_tokens
	  WHERE token = p_confirmation_token
	    AND action = 'delete'
	    AND expires_at > NOW()
	    AND consumed_at IS NULL
	    AND is_override = false;
	
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_INVALID_CONFIRMATION_TOKEN';
	  END IF;
	
	  IF v_confirmation_record.store_ids != p_store_ids THEN
	    RAISE EXCEPTION 'ERR_STORE_IDS_MISMATCH';
	  END IF;
	
	  -- ============================================================
	  -- 2. VALIDATE tiendas protegidas requieren override_token
	  -- ============================================================
	  SELECT EXISTS(
	    SELECT 1 FROM public.stores
	    WHERE id = ANY(p_store_ids) AND backup_restore_protected = true
	  ) INTO v_has_protected;
	
	  IF v_has_protected THEN
	    IF p_override_token IS NULL THEN
	      RAISE EXCEPTION 'ERR_OVERRIDE_REQUIRED';
	    END IF;
	
	    SELECT * INTO v_override_record
	    FROM public.bulk_confirmation_tokens
	    WHERE token = p_override_token
	      AND is_override = true
	      AND override_for = p_confirmation_token
	      AND expires_at > NOW()
	      AND consumed_at IS NULL;
	
	    IF NOT FOUND THEN
	      RAISE EXCEPTION 'ERR_INVALID_OVERRIDE_TOKEN';
	    END IF;
	
	    IF v_override_record.store_ids != v_confirmation_record.store_ids THEN
	      RAISE EXCEPTION 'ERR_OVERRIDE_STORE_IDS_MISMATCH';
	    END IF;
	
	    IF v_override_record.created_by = v_confirmation_record.created_by THEN
	      RAISE EXCEPTION 'ERR_SAME_USER_OVERRIDE';
	    END IF;
	
	    UPDATE public.bulk_confirmation_tokens SET consumed_at = NOW()
	    WHERE token = p_override_token;
	  END IF;
	
	  UPDATE public.bulk_confirmation_tokens SET consumed_at = NOW()
	  WHERE token = p_confirmation_token;
	
	  -- ============================================================
	  -- 3. VALIDATE todas las tiendas
	  -- ============================================================
	  FOREACH v_store_id IN ARRAY p_store_ids LOOP
	    IF NOT EXISTS(SELECT 1 FROM public.stores WHERE id = v_store_id AND is_active = true) THEN
	      v_errors := array_append(v_errors, jsonb_build_object(
	        'store_id', v_store_id, 'reason', 'STORE_NOT_FOUND_OR_INACTIVE'
	      ));
	      CONTINUE;
	    END IF;
	
	    SELECT public.validate_store_can_be_modified(v_store_id, 'soft_delete') INTO v_validation;
	    v_blockers := v_validation->'blockers';
	
	    IF v_validation->>'can_modify' != 'true' THEN
	      v_errors := array_append(v_errors, jsonb_build_object(
	        'store_id', v_store_id, 'reason', 'HAS_BLOCKING_DEPENDENCIES', 'blockers', v_blockers
	      ));
	    END IF;
	  END LOOP;
	
	  IF array_length(v_errors, 1) IS NOT NULL THEN
	    RETURN jsonb_build_object(
	      'status', 'FAILED', 'processed', 0,
	      'total_requested', array_length(p_store_ids, 1),
	      'errors', to_jsonb(v_errors), 'reason', p_reason
	    );
	  END IF;
	
	  -- ============================================================
	  -- 4. EXECUTE
	  -- ============================================================
	  FOREACH v_store_id IN ARRAY p_store_ids LOOP
	    PERFORM public.soft_delete_store(v_store_id, p_deleted_by);
	    v_processed := v_processed + 1;
	  END LOOP;
	
	  INSERT INTO public.audit_logs (action, table_name, record_id, metadata)
	  VALUES (
	    'bulk_store_deleted', 'stores', NULL,
	    jsonb_build_object(
	      'store_ids', p_store_ids, 'deleted_by', p_deleted_by,
	      'reason', p_reason, 'processed', v_processed,
	      'had_protected_stores', v_has_protected,
	      'override_used', p_override_token IS NOT NULL, 'deleted_at', NOW()
	    )
	  );
	
	  RETURN jsonb_build_object(
	    'status', 'COMPLETED', 'processed', v_processed,
	    'total_requested', array_length(p_store_ids, 1),
	    'errors', '[]'::jsonb, 'reason', p_reason
	  );
	END;
	$function$
	 
	REVOKE EXECUTE ON FUNCTION public.bulk_soft_delete_stores FROM PUBLIC, anon, authenticated
2026-09-13 06:19:24.075 UTC [24290] ERROR:  relation "received_services" does not exist
2026-09-13 06:19:24.075 UTC [24290] CONTEXT:  compilation of PL/pgSQL function "calculate_service_distribution" near line 3
2026-09-13 06:19:24.075 UTC [24290] STATEMENT:  CREATE OR REPLACE FUNCTION calculate_service_distribution(
	  p_service_id UUID
	) RETURNS TABLE(
	  receipt_item_id UUID,
	  product_id UUID,
	  distribution_amount NUMERIC,
	  distribution_percentage NUMERIC
	) LANGUAGE plpgsql SECURITY DEFINER AS $$
	DECLARE
	  v_service received_services%ROWTYPE;
	  v_method TEXT;
	  v_total_amount NUMERIC;
	  v_total_value NUMERIC DEFAULT 0;
	  v_total_qty NUMERIC DEFAULT 0;
	  v_link service_reception_links%ROWTYPE;
	  v_receipt_id UUID;
	  v_item receipt_items%ROWTYPE;
	  v_allocated NUMERIC;
	BEGIN
	  SELECT * INTO v_service FROM received_services WHERE id = p_service_id;
	  IF NOT FOUND THEN RETURN; END IF;
	  
	  v_method := v_service.distribution_method;
	  v_total_amount := v_service.total_amount;
	
	  -- Para cada recepción vinculada
	  FOR v_link IN SELECT * FROM service_reception_links WHERE service_id = p_service_id AND allocated_amount > 0 LOOP
	    v_receipt_id := v_link.receipt_id;
	    v_allocated := v_link.allocated_amount;
	
	    -- Calcular base de distribución según método
	    IF v_method = 'amount' THEN
	      SELECT COALESCE(SUM(quantity * unit_cost), 0) INTO v_total_value
	      FROM receipt_items WHERE receipt_id = v_receipt_id;
	      
	      IF v_total_value > 0 THEN
	        FOR v_item IN SELECT * FROM receipt_items WHERE receipt_id = v_receipt_id LOOP
	          distribution_amount := v_allocated * (v_item.quantity * v_item.unit_cost / v_total_value);
	          distribution_percentage := (v_item.quantity * v_item.unit_cost / v_total_value) * 100;
	          receipt_item_id := v_item.id;
	          product_id := v_item.product_id;
	          RETURN NEXT;
	        END LOOP;
	      END IF;
	
	    ELSIF v_method = 'quantity' THEN
	      SELECT COALESCE(SUM(quantity), 0) INTO v_total_qty
	      FROM receipt_items WHERE receipt_id = v_receipt_id;
	      
	      IF v_total_qty > 0 THEN
	        FOR v_item IN SELECT * FROM receipt_items WHERE receipt_id = v_receipt_id LOOP
	          distribution_amount := v_allocated * (v_item.quantity / v_total_qty);
	          distribution_percentage := (v_item.quantity / v_total_qty) * 100;
	          receipt_item_id := v_item.id;
	          product_id := v_item.product_id;
	          RETURN NEXT;
	        END LOOP;
	      END IF;
	
	    ELSIF v_method = 'manual' THEN
	      -- En manual, la distribución se guarda directamente desde el cliente
	      RETURN;
	    END IF;
	  END LOOP;
	END;
	$$
2026-09-13 06:19:24.139 UTC [24302] ERROR:  syntax error at or near "REVOKE" at character 678
2026-09-13 06:19:24.139 UTC [24302] STATEMENT:  CREATE OR REPLACE FUNCTION public.check_bulk_ops_hourly_limit(p_user_id uuid, p_plan text)
	 RETURNS jsonb
	 LANGUAGE plpgsql
	 STABLE SECURITY DEFINER
	 SET search_path TO 'public'
	AS $function$
	DECLARE
	  v_limit INTEGER;
	  v_used INTEGER;
	BEGIN
	  v_limit := CASE p_plan
	    WHEN 'free' THEN 1
	    WHEN 'pro' THEN 20
	    WHEN 'enterprise' THEN 999999
	    ELSE 1
	  END;
	
	  SELECT COUNT(*) INTO v_used
	  FROM public.bulk_ops_log
	  WHERE user_id = p_user_id
	    AND initiated_at > NOW() - INTERVAL '1 hour';
	
	  RETURN jsonb_build_object(
	    'allowed', v_used < v_limit,
	    'used', v_used,
	    'limit', v_limit,
	    'remaining', GREATEST(0, v_limit - v_used)
	  );
	END;
	$function$
	 
	REVOKE EXECUTE ON FUNCTION public.check_bulk_ops_hourly_limit FROM PUBLIC, anon, authenticated
2026-09-13 06:19:24.169 UTC [24310] ERROR:  type plan_t does not exist
2026-09-13 06:19:24.169 UTC [24310] STATEMENT:  CREATE OR REPLACE FUNCTION public.check_tenant_store_quota(
	  p_tenant_id uuid,
	  p_plan plan_t DEFAULT NULL
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  v_plan plan_t;
	  v_limit int;
	  v_current int;
	  v_allowed boolean;
	BEGIN
	  -- Get plan from tenant if not passed
	  IF p_plan IS NULL THEN
	    SELECT plan INTO v_plan FROM public.tenants WHERE id = p_tenant_id;
	  ELSE
	    v_plan := p_plan;
	  END IF;
	
	  -- Plan limits (unified source of truth)
	  v_limit := CASE v_plan
	    WHEN 'free'::plan_t THEN 1
	    WHEN 'pro'::plan_t THEN 3
	    WHEN 'enterprise'::plan_t THEN 10
	    ELSE 1
	  END;
	
	  -- Count active stores in tenant
	  SELECT COUNT(*) INTO v_current
	    FROM public.stores
	    WHERE tenant_id = p_tenant_id AND is_active = true;
	
	  v_allowed := v_current < v_limit;
	
	  RETURN jsonb_build_object(
	    'allowed', v_allowed,
	    'current', v_current,
	    'limit', v_limit,
	    'plan', v_plan::text,
	    'tenant_id', p_tenant_id
	  );
	END;
	$function$
2026-09-13 06:19:24.279 UTC [24339] ERROR:  relation "public.profiles" does not exist at character 166
2026-09-13 06:19:24.279 UTC [24339] STATEMENT:  CREATE OR REPLACE FUNCTION public.current_user_tenant_id()
	RETURNS uuid
	LANGUAGE sql
	STABLE
	SECURITY DEFINER
	SET search_path TO public
	AS $$
	  SELECT tenant_id FROM public.profiles WHERE id = auth.uid();
	$$
2026-09-13 06:19:24.327 UTC [24347] ERROR:  relation "received_services" does not exist
2026-09-13 06:19:24.327 UTC [24347] CONTEXT:  compilation of PL/pgSQL function "distribute_service_cost_v2" near line 6
2026-09-13 06:19:24.327 UTC [24347] STATEMENT:  CREATE OR REPLACE FUNCTION public.distribute_service_cost_v2(
	  p_service_id uuid,
	  p_user_id uuid DEFAULT NULL
	) RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO 'public', 'extensions'
	AS $func$
	DECLARE
	  v_store_id uuid;
	  v_status text;
	  v_total_amount numeric;
	  v_service received_services%ROWTYPE;
	  v_method text;
	  v_link service_reception_links%ROWTYPE;
	  v_item receipt_items%ROWTYPE;
	  v_total_value numeric := 0;
	  v_total_qty numeric := 0;
	  v_allocated numeric;
	  v_dist_count integer := 0;
	  v_caller_uid uuid := COALESCE(p_user_id, auth.uid());
	BEGIN
	  PERFORM pg_advisory_xact_lock(hashtext(p_service_id::text));
	
	  SELECT * INTO v_service FROM received_services WHERE id = p_service_id FOR UPDATE;
	  IF NOT FOUND THEN RAISE EXCEPTION 'ERR_SERVICE_NOT_FOUND'; END IF;
	
	  v_store_id := v_service.store_id;
	  v_status := v_service.status;
	  v_total_amount := v_service.total_amount;
	  v_method := v_service.distribution_method;
	
	  IF NOT public.has_store_access(v_store_id) THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED';
	  END IF;
	
	  IF v_status != 'active' THEN
	    RAISE EXCEPTION 'ERR_SERVICE_NOT_ACTIVE: status % is not active', v_status;
	  END IF;
	
	  IF v_method = 'manual' THEN
	    RAISE EXCEPTION 'ERR_MANUAL_METHOD: use link_receipts_to_service for manual distribution';
	  END IF;
	
	  DELETE FROM service_cost_distributions WHERE service_id = p_service_id;
	
	  FOR v_link IN SELECT * FROM service_reception_links WHERE service_id = p_service_id AND allocated_amount > 0 ORDER BY receipt_id LOOP
	    v_allocated := v_link.allocated_amount;
	
	    IF v_method = 'amount' THEN
	      SELECT COALESCE(SUM(quantity * unit_cost), 0) INTO v_total_value
	      FROM receipt_items WHERE receipt_id = v_link.receipt_id;
	      IF v_total_value > 0 THEN
	        FOR v_item IN SELECT * FROM receipt_items WHERE receipt_id = v_link.receipt_id ORDER BY id LOOP
	          INSERT INTO service_cost_distributions
	            (service_id, receipt_id, receipt_item_id, product_id, distribution_amount, distribution_percentage)
	          VALUES
	            (p_service_id, v_link.receipt_id, v_item.id, v_item.product_id,
	             v_allocated * (v_item.quantity * v_item.unit_cost / v_total_value),
	             (v_item.quantity * v_item.unit_cost / v_total_value) * 100);
	          v_dist_count := v_dist_count + 1;
	        END LOOP;
	      END IF;
	
	    ELSIF v_method = 'quantity' THEN
	      SELECT COALESCE(SUM(quantity), 0) INTO v_total_qty
	      FROM receipt_items WHERE receipt_id = v_link.receipt_id;
	      IF v_total_qty > 0 THEN
	        FOR v_item IN SELECT * FROM receipt_items WHERE receipt_id = v_link.receipt_id ORDER BY id LOOP
	          INSERT INTO service_cost_distributions
	            (service_id, receipt_id, receipt_item_id, product_id, distribution_amount, distribution_percentage)
	          VALUES
	            (p_service_id, v_link.receipt_id, v_item.id, v_item.product_id,
	             v_allocated * (v_item.quantity / v_total_qty),
	             (v_item.quantity / v_total_qty) * 100);
	          v_dist_count := v_dist_count + 1;
	        END LOOP;
	      END IF;
	    END IF;
	  END LOOP;
	
	  -- Forzar recalculo de WAC para cada producto afectado
	  UPDATE receipt_items SET updated_at = NOW()
	  WHERE product_id IN (SELECT DISTINCT product_id FROM service_cost_distributions WHERE service_id = p_service_id);
	
	  INSERT INTO audit_logs (user_id, store_id, action, table_name, record_id, metadata)
	  VALUES (v_caller_uid, v_store_id, 'SERVICE_DISTRIBUTED', 'received_services', p_service_id,
	    jsonb_build_object('rows_distributed', v_dist_count, 'method', v_method, 'total_amount', v_total_amount));
	
	  RETURN jsonb_build_object('status', 'success', 'distributed_rows', v_dist_count);
	END;
	$func$
2026-09-13 06:19:24.365 UTC [24354] ERROR:  type "user_role" does not exist at character 116
2026-09-13 06:19:24.365 UTC [24354] STATEMENT:  CREATE OR REPLACE FUNCTION public.enforce_encargado_store_limit()
	RETURNS TRIGGER AS $$
	DECLARE
	    v_creator_role user_role;
	    v_limit integer;
	    v_current_count integer;
	BEGIN
	    IF NEW.created_by IS NULL THEN RETURN NEW; END IF;
	
	    SELECT role, max_stores_limit INTO v_creator_role, v_limit
	    FROM public.profiles WHERE id = NEW.created_by;
	
	    IF v_creator_role = 'encargado' THEN
	        SELECT COUNT(*) INTO v_current_count FROM public.stores WHERE created_by = NEW.created_by;
	        IF v_current_count >= v_limit THEN
	            RAISE EXCEPTION 'ERR_STORE_LIMIT_EXCEEDED: Maximum number of stores reached for this manager (%/%)', v_current_count, v_limit;
	        END IF;
	    END IF;
	    RETURN NEW;
	END;
	$$ LANGUAGE plpgsql
2026-09-13 06:19:24.395 UTC [24359] ERROR:  type "user_role" does not exist at character 115
2026-09-13 06:19:24.395 UTC [24359] STATEMENT:  CREATE OR REPLACE FUNCTION public.enforce_encargado_user_limit()
	RETURNS TRIGGER AS $$
	DECLARE
	    v_creator_role user_role;
	    v_limit integer;
	    v_current_count integer;
	BEGIN
	    IF NEW.created_by IS NULL THEN RETURN NEW; END IF;
	
	    SELECT role, max_users_limit INTO v_creator_role, v_limit
	    FROM public.profiles WHERE id = NEW.created_by;
	
	    IF v_creator_role = 'encargado' THEN
	        SELECT COUNT(*) INTO v_current_count FROM public.profiles WHERE created_by = NEW.created_by;
	        IF v_current_count >= v_limit THEN
	            RAISE EXCEPTION 'ERR_USER_LIMIT_EXCEEDED: Maximum number of users reached for this manager (%/%)', v_current_count, v_limit;
	        END IF;
	    END IF;
	    RETURN NEW;
	END;
	$$ LANGUAGE plpgsql
2026-09-13 06:19:24.454 UTC [24374] ERROR:  syntax error at or near "REVOKE" at character 1436
2026-09-13 06:19:24.454 UTC [24374] STATEMENT:  CREATE OR REPLACE FUNCTION public.generate_bulk_confirmation_token(p_store_ids uuid[], p_action text, p_user_id uuid)
	 RETURNS text
	 LANGUAGE plpgsql
	 SECURITY DEFINER
	 SET search_path TO 'public'
	AS $function$
	DECLARE
	  v_token TEXT;
	  v_has_protected BOOLEAN;
	  v_caller_role TEXT;
	BEGIN
	  -- AUTH CHECK: Solo admin
	  IF auth.uid() IS NOT NULL THEN
	    SELECT role INTO v_caller_role FROM public.profiles WHERE id = auth.uid();
	    IF v_caller_role IS NULL OR v_caller_role != 'admin' THEN
	      RAISE EXCEPTION 'ERR_PERMISSION_DENIED: Solo admin puede generar tokens bulk';
	    END IF;
	    -- Verificar que p_user_id coincide con auth.uid()
	    IF p_user_id != auth.uid() THEN
	      RAISE EXCEPTION 'ERR_PERMISSION_DENIED: p_user_id debe coincidir con el usuario autenticado';
	    END IF;
	  END IF;
	
	  IF p_action NOT IN ('delete', 'archive') THEN
	    RAISE EXCEPTION 'ERR_NON_DESTRUCTIVE_ACTION';
	  END IF;
	
	  SELECT EXISTS(
	    SELECT 1 FROM public.stores
	    WHERE id = ANY(p_store_ids) AND backup_restore_protected = true
	  ) INTO v_has_protected;
	
	  v_token := 'bct_' || replace(gen_random_uuid()::text, '-', '');
	
	  INSERT INTO public.bulk_confirmation_tokens (
	    token, store_ids, action, created_by, expires_at, metadata
	  ) VALUES (
	    v_token, p_store_ids, p_action, p_user_id,
	    NOW() + INTERVAL '10 minutes',
	    jsonb_build_object('has_protected_stores', v_has_protected)
	  );
	
	  RETURN v_token;
	END;
	$function$
	 
	REVOKE EXECUTE ON FUNCTION public.generate_bulk_confirmation_token FROM PUBLIC, anon, authenticated
2026-09-13 06:19:24.476 UTC [24379] ERROR:  syntax error at or near "REVOKE" at character 2432
2026-09-13 06:19:24.476 UTC [24379] STATEMENT:  CREATE OR REPLACE FUNCTION public.generate_bulk_override_token(p_confirmation_token text, p_override_user_id uuid, p_reason text DEFAULT NULL::text)
	 RETURNS text
	 LANGUAGE plpgsql
	 SECURITY DEFINER
	 SET search_path TO 'public'
	AS $function$
	DECLARE
	  v_original RECORD;
	  v_override_token TEXT;
	  v_override_user_role TEXT;
	BEGIN
	  -- AUTH CHECK: Solo admin, y debe ser auth.uid() == p_override_user_id
	  IF auth.uid() IS NOT NULL THEN
	    IF auth.uid() != p_override_user_id THEN
	      RAISE EXCEPTION 'ERR_PERMISSION_DENIED: p_override_user_id debe coincidir con el usuario autenticado';
	    END IF;
	
	    SELECT role INTO v_override_user_role FROM public.profiles WHERE id = p_override_user_id;
	    IF v_override_user_role IS NULL OR v_override_user_role != 'admin' THEN
	      RAISE EXCEPTION 'ERR_PERMISSION_DENIED: Solo admin puede generar override';
	    END IF;
	  END IF;
	
	  SELECT * INTO v_original
	  FROM public.bulk_confirmation_tokens
	  WHERE token = p_confirmation_token
	    AND consumed_at IS NULL
	    AND expires_at > NOW()
	    AND is_override = false;
	
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_INVALID_OR_EXPIRED_TOKEN';
	  END IF;
	
	  IF v_original.created_by = p_override_user_id THEN
	    RAISE EXCEPTION 'ERR_SAME_USER_OVERRIDE';
	  END IF;
	
	  IF EXISTS(
	    SELECT 1 FROM public.bulk_confirmation_tokens
	    WHERE override_for = p_confirmation_token
	      AND consumed_at IS NULL AND expires_at > NOW()
	  ) THEN
	    RAISE EXCEPTION 'ERR_OVERRIDE_ALREADY_EXISTS';
	  END IF;
	
	  v_override_token := 'bot_' || replace(gen_random_uuid()::text, '-', '');
	
	  INSERT INTO public.bulk_confirmation_tokens (
	    token, store_ids, action, created_by, expires_at,
	    is_override, override_for, metadata
	  ) VALUES (
	    v_override_token, v_original.store_ids, v_original.action, p_override_user_id,
	    NOW() + INTERVAL '10 minutes', true, p_confirmation_token,
	    jsonb_build_object('override_reason', p_reason,
	      'original_created_by', v_original.created_by,
	      'override_created_by', p_override_user_id)
	  );
	
	  INSERT INTO public.audit_logs (action, table_name, record_id, metadata)
	  VALUES (
	    'bulk_override_token_generated', 'stores', NULL,
	    jsonb_build_object('confirmation_token', p_confirmation_token,
	      'store_ids', v_original.store_ids,
	      'override_by', p_override_user_id,
	      'original_by', v_original.created_by, 'reason', p_reason)
	  );
	
	  RETURN v_override_token;
	END;
	$function$
	 
	REVOKE EXECUTE ON FUNCTION public.generate_bulk_override_token FROM PUBLIC, anon, authenticated
2026-09-13 06:19:24.520 UTC [24389] ERROR:  relation "public.backup_table_registry" does not exist at character 581
2026-09-13 06:19:24.520 UTC [24389] STATEMENT:  CREATE OR REPLACE FUNCTION public.get_backup_table_list(
	  p_include_excluded BOOLEAN DEFAULT FALSE
	)
	RETURNS TABLE (
	  table_name          TEXT,
	  tier                INTEGER,
	  filter_strategy     TEXT,
	  parent_table        TEXT,
	  parent_foreign_key  TEXT,
	  date_column         TEXT,
	  excluded_from_restore BOOLEAN,
	  exclude_reason      TEXT
	)
	LANGUAGE sql
	SECURITY DEFINER
	SET search_path = public
	AS $$
	  SELECT
	    table_name,
	    tier,
	    filter_strategy,
	    parent_table,
	    parent_foreign_key,
	    date_column,
	    excluded_from_restore,
	    exclude_reason
	  FROM public.backup_table_registry
	  WHERE (p_include_excluded OR excluded_from_restore = FALSE)
	  ORDER BY tier ASC, table_name ASC;
	$$
2026-09-13 06:19:24.560 UTC [24399] ERROR:  relation "v_global_operation_dates" does not exist at character 229
2026-09-13 06:19:24.560 UTC [24399] STATEMENT:  CREATE OR REPLACE FUNCTION public.get_global_max_operation_date(
	  p_store_id UUID DEFAULT NULL
	)
	RETURNS TIMESTAMP WITH TIME ZONE
	LANGUAGE sql
	SECURITY DEFINER
	SET search_path = public
	AS $$
	  SELECT MAX(operation_date)
	  FROM v_global_operation_dates
	  WHERE p_store_id IS NULL OR store_id = p_store_id;
	$$
2026-09-13 06:19:24.584 UTC [24404] ERROR:  relation "exchange_rates" does not exist at character 216
2026-09-13 06:19:24.584 UTC [24404] STATEMENT:  CREATE OR REPLACE FUNCTION get_latest_rate(p_currency TEXT, p_source TEXT)
	RETURNS TABLE(rate NUMERIC, rate_date DATE, captured_at TIMESTAMPTZ)
	LANGUAGE sql STABLE AS $$
	  SELECT rate, rate_date, captured_at
	  FROM exchange_rates
	  WHERE currency = p_currency AND source = p_source
	  ORDER BY rate_date DESC, captured_at DESC
	  LIMIT 1;
	$$
2026-09-13 06:19:24.615 UTC [24409] ERROR:  relation "public.products" does not exist at character 194
2026-09-13 06:19:24.615 UTC [24409] STATEMENT:  CREATE OR REPLACE FUNCTION public.get_low_stock_count(p_store_id uuid DEFAULT NULL)
	RETURNS bigint
	LANGUAGE sql
	SECURITY DEFINER
	SET search_path = public
	AS $$
	  SELECT COUNT(*)::bigint
	  FROM public.products
	  WHERE
	    (p_store_id IS NULL OR store_id = p_store_id)
	    AND is_active = true
	    AND stock_current > 0
	    AND min_stock IS NOT NULL
	    AND min_stock > 0
	    AND stock_current <= min_stock;
	$$
2026-09-13 06:19:24.652 UTC [24418] ERROR:  relation "receipt_items" does not exist at character 754
2026-09-13 06:19:24.652 UTC [24418] STATEMENT:  CREATE OR REPLACE FUNCTION get_product_cost_analysis(
	  p_product_id UUID,
	  p_store_id UUID
	) RETURNS TABLE(
	  receipt_id UUID,
	  receipt_date TIMESTAMPTZ,
	  quantity INT,
	  unit_cost FLOAT,
	  service_type TEXT,
	  service_amount NUMERIC,
	  total_cost NUMERIC,
	  unit_cost_final NUMERIC
	) LANGUAGE sql SECURITY DEFINER AS $$
	  SELECT 
	    ri.receipt_id,
	    r.reception_date,
	    ri.quantity,
	    ri.unit_cost,
	    st.name AS service_type,
	    COALESCE(scd.distribution_amount, 0) AS service_amount,
	    ri.unit_cost * ri.quantity + COALESCE(scd.distribution_amount, 0) AS total_cost,
	    CASE WHEN ri.quantity > 0 
	      THEN (ri.unit_cost * ri.quantity + COALESCE(scd.distribution_amount, 0)) / ri.quantity 
	      ELSE 0 END AS unit_cost_final
	  FROM receipt_items ri
	  JOIN receipts r ON r.id = ri.receipt_id
	  LEFT JOIN service_cost_distributions scd ON scd.receipt_item_id = ri.id
	  LEFT JOIN received_services rs ON rs.id = scd.service_id AND rs.status = 'active'
	  LEFT JOIN service_types st ON st.id = rs.service_type_id
	  WHERE ri.product_id = p_product_id
	    AND r.store_id = p_store_id
	    AND r.status = 'active'
	  ORDER BY r.reception_date DESC;
	$$
2026-09-13 06:19:24.688 UTC [24426] ERROR:  relation "exchange_rates" does not exist at character 252
2026-09-13 06:19:24.688 UTC [24426] STATEMENT:  CREATE OR REPLACE FUNCTION get_rate_history(
	  p_currency TEXT,
	  p_source TEXT,
	  p_days INT DEFAULT 30
	)
	RETURNS TABLE(rate_date DATE, rate NUMERIC, variation_daily NUMERIC)
	LANGUAGE sql STABLE AS $$
	  SELECT rate_date, rate, variation_daily
	  FROM exchange_rates
	  WHERE currency = p_currency AND source = p_source
	    AND rate_date >= CURRENT_DATE - p_days
	  ORDER BY rate_date ASC;
	$$
2026-09-13 06:19:24.714 UTC [24431] ERROR:  relation "exchange_rates" does not exist at character 363
2026-09-13 06:19:24.714 UTC [24431] STATEMENT:  CREATE OR REPLACE FUNCTION get_rate_variation(
	  p_currency TEXT,
	  p_source TEXT,
	  p_start_date DATE,
	  p_end_date DATE
	)
	RETURNS TABLE(
	  start_rate NUMERIC,
	  end_rate NUMERIC,
	  absolute_change NUMERIC,
	  percent_change NUMERIC,
	  daily_avg_growth NUMERIC,
	  monthly_avg_growth NUMERIC
	)
	LANGUAGE sql STABLE AS $$
	  WITH start_val AS (
	    SELECT rate FROM exchange_rates
	    WHERE currency = p_currency AND source = p_source AND rate_date <= p_start_date
	    ORDER BY rate_date DESC LIMIT 1
	  ),
	  end_val AS (
	    SELECT rate FROM exchange_rates
	    WHERE currency = p_currency AND source = p_source AND rate_date <= p_end_date
	    ORDER BY rate_date DESC LIMIT 1
	  )
	  SELECT
	    COALESCE(sv.rate, 0) AS start_rate,
	    COALESCE(ev.rate, 0) AS end_rate,
	    (COALESCE(ev.rate, 0) - COALESCE(sv.rate, 0)) AS absolute_change,
	    CASE WHEN sv.rate IS NOT NULL AND sv.rate > 0 THEN ((ev.rate - sv.rate) / sv.rate * 100) ELSE 0 END AS percent_change,
	    CASE WHEN sv.rate IS NOT NULL AND sv.rate > 0 AND ev.rate IS NOT NULL AND ev.rate > 0 AND p_end_date > p_start_date
	      THEN (POWER(ev.rate::FLOAT / sv.rate::FLOAT, 1.0 / EXTRACT(EPOCH FROM (p_end_date - p_start_date)) * 86400) - 1) * 100
	      ELSE 0 END AS daily_avg_growth,
	    CASE WHEN sv.rate IS NOT NULL AND sv.rate > 0 AND ev.rate IS NOT NULL AND ev.rate > 0 AND p_end_date > p_start_date
	      THEN (POWER(ev.rate::FLOAT / sv.rate::FLOAT, 30.44 / EXTRACT(EPOCH FROM (p_end_date - p_start_date)) * 86400) - 1) * 100
	      ELSE 0 END AS monthly_avg_growth
	  FROM start_val sv, end_val ev;
	$$
2026-09-13 06:19:24.780 UTC [24445] ERROR:  type "public.stores" does not exist
2026-09-13 06:19:24.780 UTC [24445] STATEMENT:  CREATE OR REPLACE FUNCTION public.get_transferable_stores(
	  p_user_id UUID,
	  p_current_store_id UUID
	)
	RETURNS SETOF public.stores
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO 'public'
	AS $$
	DECLARE
	  v_tenant_id UUID;
	BEGIN
	  -- 1. Obtener tenant_id de la tienda actual
	  SELECT tenant_id INTO v_tenant_id
	  FROM public.stores
	  WHERE id = p_current_store_id AND is_active = true;
	
	  -- 2. Devolver tiendas donde el caller tiene acceso (has_store_access_as)
	  --    EXCLUYENDO la tienda actual
	  --    Si tenant_id es NOT NULL, filtrar por mismo tenant.
	  --    Si tenant_id es NULL (legacy), mostrar todas las que el caller tiene acceso.
	  IF v_tenant_id IS NOT NULL THEN
	    RETURN QUERY
	    SELECT s.*
	    FROM public.stores s
	    WHERE s.tenant_id = v_tenant_id
	      AND s.id != p_current_store_id
	      AND s.is_active = true
	      AND s.is_archived = false
	      AND public.has_store_access_as(p_user_id, s.id)
	    ORDER BY s.name;
	  ELSE
	    -- Legacy: tiendas sin tenant_id — usar solo has_store_access_as
	    RETURN QUERY
	    SELECT s.*
	    FROM public.stores s
	    WHERE s.id != p_current_store_id
	      AND s.is_active = true
	      AND s.is_archived = false
	      AND public.has_store_access_as(p_user_id, s.id)
	    ORDER BY s.name;
	  END IF;
	END;
	$$
2026-09-13 06:19:24.887 UTC [24456] ERROR:  type user_role[] does not exist
2026-09-13 06:19:24.887 UTC [24456] STATEMENT:  CREATE OR REPLACE FUNCTION public.has_any_role(required_roles user_role[])
	 RETURNS boolean
	 LANGUAGE plpgsql
	 STABLE SECURITY DEFINER
	AS $$
	DECLARE
	    v_actual_role user_role;
	    r user_role;
	BEGIN
	    SELECT role INTO v_actual_role FROM public.profiles WHERE id = auth.uid();
	
	    FOREACH r IN ARRAY required_roles
	    LOOP
	        IF v_actual_role = r THEN RETURN true; END IF;
	        IF v_actual_role = 'encargado' AND r = 'manager' THEN RETURN true; END IF;
	        IF v_actual_role = 'usuario' AND (r = 'clerk' OR r = 'warehouse') THEN RETURN true; END IF;
	    END LOOP;
	
	    RETURN false;
	END;
	$$
2026-09-13 06:19:24.914 UTC [24461] ERROR:  relation "profiles" does not exist at character 461
2026-09-13 06:19:24.914 UTC [24461] STATEMENT:  CREATE OR REPLACE FUNCTION public.has_management_access_as(
	  p_user_id uuid,
	  p_store_id uuid
	) RETURNS boolean
	LANGUAGE sql SECURITY DEFINER
	SET search_path = public, pg_temp
	AS $$
	  -- service_role always passes
	  SELECT CASE WHEN auth.role() = 'service_role' THEN true
	    ELSE
	      -- Check if user has admin/manager/encargado role in their profile
	      -- OR has a membership with manager/admin role for the store
	      EXISTS (
	        SELECT 1 FROM profiles
	        WHERE id = p_user_id
	        AND role IN ('admin', 'manager', 'encargado')
	      )
	      OR EXISTS (
	        SELECT 1 FROM user_store_memberships
	        WHERE user_id = p_user_id
	        AND store_id = p_store_id
	        AND status = 'active'
	        AND role IN ('admin', 'manager')
	      )
	    END
	$$
2026-09-13 06:19:24.938 UTC [24466] ERROR:  type public.user_role does not exist
2026-09-13 06:19:24.938 UTC [24466] STATEMENT:  CREATE OR REPLACE FUNCTION public.has_role(p_user_id UUID, p_required_role public.user_role)
	 RETURNS boolean
	 LANGUAGE plpgsql
	 STABLE SECURITY DEFINER
	AS $$
	DECLARE
	    v_actual_role public.user_role;
	BEGIN
	    SELECT role INTO v_actual_role FROM public.profiles WHERE id = p_user_id;
	
	    IF v_actual_role IS NULL THEN
	        RETURN false;
	    END IF;
	
	    -- Compatibility mapping (mirroring the 1-arg version)
	    IF v_actual_role = 'encargado' AND p_required_role = 'manager' THEN RETURN true; END IF;
	    IF v_actual_role = 'usuario' AND (p_required_role = 'clerk' OR p_required_role = 'warehouse') THEN RETURN true; END IF;
	
	    RETURN v_actual_role = p_required_role;
	END;
	$$
2026-09-13 06:19:24.969 UTC [24473] ERROR:  relation "public.user_store_memberships" does not exist at character 279
2026-09-13 06:19:24.969 UTC [24473] STATEMENT:  CREATE OR REPLACE FUNCTION public.has_store_role(
	  p_user_id uuid,
	  p_store_id uuid,
	  p_roles text[]
	) RETURNS boolean
	LANGUAGE sql SECURITY DEFINER
	SET search_path = public, pg_temp
	AS $$
	  SELECT CASE WHEN auth.role() = 'service_role' THEN
	    EXISTS (
	      SELECT 1 FROM public.user_store_memberships m
	      WHERE m.user_id = p_user_id
	        AND m.store_id = p_store_id
	        AND m.status = 'active'
	        AND m.role::text = ANY(p_roles)
	    )
	  ELSE
	    EXISTS (
	      SELECT 1 FROM public.user_store_memberships m
	      WHERE m.user_id = auth.uid()
	        AND m.store_id = p_store_id
	        AND m.status = 'active'
	        AND m.role::text = ANY(p_roles)
	    )
	  END
	$$
2026-09-13 06:19:25.003 UTC [24481] ERROR:  relation "public.profiles" does not exist at character 173
2026-09-13 06:19:25.003 UTC [24481] STATEMENT:  CREATE OR REPLACE FUNCTION public.is_global_admin()
	RETURNS BOOLEAN
	LANGUAGE sql
	STABLE
	SECURITY DEFINER
	SET search_path = public
	AS $$
	  SELECT EXISTS (
	    SELECT 1 FROM public.profiles
	    WHERE id = auth.uid()
	      AND role = 'admin'
	  );
	$$
2026-09-13 06:19:25.031 UTC [24487] ERROR:  type user_role does not exist
2026-09-13 06:19:25.031 UTC [24487] STATEMENT:  CREATE OR REPLACE FUNCTION public.is_role_not_changed(p_user_id uuid, p_new_role user_role, p_new_role_id uuid)
	RETURNS boolean
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path = public, extensions
	AS $$
	DECLARE
	  v_old_role user_role;
	  v_old_role_id uuid;
	BEGIN
	  SELECT role, role_id INTO v_old_role, v_old_role_id
	  FROM public.profiles
	  WHERE id = p_user_id;
	
	  RETURN (p_new_role IS NOT DISTINCT FROM v_old_role)
	     AND (p_new_role_id IS NOT DISTINCT FROM v_old_role_id);
	END;
	$$
2026-09-13 06:19:25.057 UTC [24493] ERROR:  relation "public.user_store_memberships" does not exist at character 188
2026-09-13 06:19:25.057 UTC [24493] STATEMENT:  CREATE OR REPLACE FUNCTION public.is_store_member(p_store_id UUID)
	RETURNS BOOLEAN
	LANGUAGE sql
	STABLE
	SECURITY DEFINER
	SET search_path = public
	AS $$
	  SELECT EXISTS (
	    SELECT 1 FROM public.user_store_memberships
	    WHERE store_id = p_store_id
	      AND user_id = auth.uid()
	      AND status = 'active'
	  );
	$$
2026-09-13 06:19:25.079 UTC [24498] ERROR:  function public.current_user_tenant_id() does not exist at character 209
2026-09-13 06:19:25.079 UTC [24498] HINT:  No function matches the given name and argument types. You might need to add explicit type casts.
2026-09-13 06:19:25.079 UTC [24498] STATEMENT:  CREATE OR REPLACE FUNCTION public.is_tenant_member(p_tenant_id uuid)
	RETURNS boolean
	LANGUAGE sql
	STABLE
	SECURITY DEFINER
	SET search_path TO public
	AS $$
	  SELECT p_tenant_id IS NOT NULL
	   AND p_tenant_id = public.current_user_tenant_id();
	$$
2026-09-13 06:19:25.127 UTC [24510] ERROR:  type user_role does not exist
2026-09-13 06:19:25.127 UTC [24510] STATEMENT:  CREATE OR REPLACE FUNCTION public.managed_create_user(p_email text, p_full_name text, p_role user_role, p_store_id uuid DEFAULT NULL::uuid, p_memberships jsonb DEFAULT NULL::jsonb, p_max_stores integer DEFAULT 0, p_max_users integer DEFAULT 0, p_target_user_id uuid DEFAULT NULL::uuid)
	 RETURNS jsonb
	 LANGUAGE plpgsql
	 SECURITY DEFINER
	 SET search_path TO 'public', 'extensions'
	AS $function$
	DECLARE
	    v_user_id uuid;
	    v_role_id uuid;
	    v_active_store_id uuid;
	    m JSONB;
	    v_creator_role user_role;
	BEGIN
	    SELECT role INTO v_creator_role FROM public.profiles WHERE id = auth.uid();
	    IF v_creator_role IS NULL OR v_creator_role NOT IN ('admin', 'encargado') THEN
	        RAISE EXCEPTION 'ERR_UNAUTHORIZED: Only admins and managers can create users.';
	    END IF;
	
	    v_active_store_id := COALESCE((p_memberships->0->>'store_id')::UUID, p_store_id);
	
	    IF v_creator_role = 'encargado' AND v_active_store_id IS NOT NULL THEN
	        IF NOT public.has_store_access(v_active_store_id) THEN
	            RAISE EXCEPTION 'ERR_UNAUTHORIZED: No access to store %', v_active_store_id;
	        END IF;
	    END IF;
	
	    SELECT id INTO v_role_id FROM public.roles WHERE lower(name) = lower(p_role::text) OR (name = 'Cajero' AND p_role = 'clerk') OR (name = 'Almacenero' AND p_role = 'warehouse') LIMIT 1;
	    v_user_id := COALESCE(p_target_user_id, gen_random_uuid());
	
	    INSERT INTO public.profiles (id, email, full_name, role, role_id, active_store_id, is_active, created_by, max_stores_limit, max_users_limit, created_at, updated_at)
	    VALUES (v_user_id, p_email, p_full_name, p_role, v_role_id, v_active_store_id, true, auth.uid(), p_max_stores, p_max_users, now(), now())
	    ON CONFLICT (id) DO UPDATE SET email = EXCLUDED.email, full_name = EXCLUDED.full_name, role = EXCLUDED.role, role_id = EXCLUDED.role_id, active_store_id = EXCLUDED.active_store_id, updated_at = now()
	    RETURNING id INTO v_user_id;
	
	    IF p_memberships IS NOT NULL THEN
	        IF v_creator_role != 'admin' THEN
	            DELETE FROM public.user_store_memberships WHERE user_id = v_user_id AND store_id IN (SELECT store_id FROM public.user_store_memberships WHERE user_id = auth.uid() AND role IN ('encargado', 'manager'));
	        ELSE
	            DELETE FROM public.user_store_memberships WHERE user_id = v_user_id;
	        END IF;
	        FOR m IN SELECT * FROM jsonb_array_elements(p_memberships) LOOP
	            IF (m->>'store_id') IS NOT NULL AND (m->>'store_id') <> '' THEN
	                IF v_creator_role = 'admin' OR public.has_store_access((m->>'store_id')::UUID) THEN
	                    INSERT INTO public.user_store_memberships (user_id, store_id, role) VALUES (v_user_id, (m->>'store_id')::UUID, (m->>'role')::user_role) ON CONFLICT (user_id, store_id) DO UPDATE SET role = EXCLUDED.role;
	                END IF;
	            END IF;
	        END LOOP;
	    ELSIF p_store_id IS NOT NULL THEN
	        INSERT INTO public.user_store_memberships (user_id, store_id, role) VALUES (v_user_id, p_store_id, p_role) ON CONFLICT (user_id, store_id) DO UPDATE SET role = EXCLUDED.role;
	    END IF;
	    RETURN jsonb_build_object('success', true, 'user_id', v_user_id);
	END;
	$function$
2026-09-13 06:19:25.150 UTC [24515] ERROR:  type public.user_role does not exist
2026-09-13 06:19:25.150 UTC [24515] STATEMENT:  CREATE OR REPLACE FUNCTION public.managed_create_user_v2(
	  p_email text,
	  p_full_name text,
	  p_role public.user_role,
	  p_plan plan_t DEFAULT 'free'::plan_t,
	  p_store_id uuid DEFAULT NULL::uuid,
	  p_memberships jsonb DEFAULT NULL::jsonb,
	  p_max_stores int DEFAULT 0,
	  p_max_users int DEFAULT 0,
	  p_target_user_id uuid DEFAULT NULL::uuid,
	  p_creator_id uuid DEFAULT NULL::uuid
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  v_user_id uuid := COALESCE(p_target_user_id, gen_random_uuid());
	  v_role_id uuid;
	  v_active_store_id uuid;
	  m JSONB;
	  v_creator_role public.user_role;
	  v_creator_uid uuid := COALESCE(p_creator_id, auth.uid());
	BEGIN
	  -- Validar caller es admin o encargado
	  SELECT role INTO v_creator_role FROM public.profiles WHERE id = v_creator_uid;
	  IF v_creator_role IS NULL OR v_creator_role NOT IN ('admin', 'encargado', 'superadmin') THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED: Only admins and managers can create users.';
	  END IF;
	
	  -- Validar email único (entre perfiles activos)
	  IF EXISTS (SELECT 1 FROM public.profiles WHERE email = p_email AND deleted_at IS NULL) THEN
	    RAISE EXCEPTION 'ERR_EMAIL_ALREADY_EXISTS: %', p_email;
	  END IF;
	
	  v_active_store_id := COALESCE((p_memberships->0->>'store_id')::UUID, p_store_id);
	
	  -- Encargado solo puede crear en tiendas que gestiona
	  IF v_creator_role = 'encargado' AND v_active_store_id IS NOT NULL THEN
	    IF NOT public.has_store_access(v_active_store_id) THEN
	      RAISE EXCEPTION 'ERR_UNAUTHORIZED: No access to store %', v_active_store_id;
	    END IF;
	  END IF;
	
	  -- Buscar role_id
	  SELECT id INTO v_role_id FROM public.roles
	    WHERE lower(name) = lower(p_role::text)
	       OR (name = 'Cajero' AND p_role = 'clerk')
	       OR (name = 'Almacenero' AND p_role = 'warehouse')
	       OR (name = 'UserCosto' AND p_role = 'costo')
	    LIMIT 1;
	
	  -- INSERT profiles con plan
	  INSERT INTO public.profiles (
	    id, email, full_name, role, role_id, active_store_id, is_active,
	    created_by, max_stores_limit, max_users_limit, plan, created_at, updated_at
	  ) VALUES (
	    v_user_id, p_email, p_full_name, p_role, v_role_id, v_active_store_id, true,
	    v_creator_uid, p_max_stores, p_max_users, p_plan, now(), now()
	  )
	  ON CONFLICT (id) DO UPDATE SET
	    email = EXCLUDED.email,
	    full_name = EXCLUDED.full_name,
	    role = EXCLUDED.role,
	    role_id = EXCLUDED.role_id,
	    active_store_id = EXCLUDED.active_store_id,
	    plan = EXCLUDED.plan,
	    is_active = true,
	    deleted_at = NULL,
	    deletion_reason = NULL,
	    deleted_by = NULL,
	    updated_at = now()
	  RETURNING id INTO v_user_id;
	
	  -- Procesar memberships
	  IF p_memberships IS NOT NULL THEN
	    IF v_creator_role != 'admin' AND v_creator_role != 'superadmin' THEN
	      DELETE FROM public.user_store_memberships
	        WHERE user_id = v_user_id
	        AND store_id IN (
	          SELECT store_id FROM public.user_store_memberships
	          WHERE user_id = v_creator_uid AND role IN ('encargado', 'manager')
	        );
	    ELSE
	      DELETE FROM public.user_store_memberships WHERE user_id = v_user_id;
	    END IF;
	
	    FOR m IN SELECT * FROM jsonb_array_elements(p_memberships) LOOP
	      IF (m->>'store_id') IS NOT NULL AND (m->>'store_id') <> '' THEN
	        IF v_creator_role IN ('admin', 'superadmin') OR public.has_store_access((m->>'store_id')::UUID) THEN
	          INSERT INTO public.user_store_memberships (user_id, store_id, role)
	          VALUES (v_user_id, (m->>'store_id')::UUID, (m->>'role')::public.user_role)
	          ON CONFLICT (user_id, store_id) DO UPDATE SET role = EXCLUDED.role, status = 'active';
	        END IF;
	      END IF;
	    END LOOP;
	  ELSIF p_store_id IS NOT NULL THEN
	    INSERT INTO public.user_store_memberships (user_id, store_id, role)
	    VALUES (v_user_id, p_store_id, p_role)
	    ON CONFLICT (user_id, store_id) DO UPDATE SET role = EXCLUDED.role, status = 'active';
	  END IF;
	
	  -- Audit log atómico (regresión C-2 restaurada)
	  INSERT INTO public.user_audit_log (performed_by, target_user_id, action, new_values, metadata)
	  VALUES (
	    v_creator_uid, v_user_id, 'USER_CREATED',
	    jsonb_build_object(
	      'email', p_email, 'full_name', p_full_name, 'role', p_role::text,
	      'plan', p_plan::text, 'active_store_id', v_active_store_id,
	      'max_stores', p_max_stores, 'max_users', p_max_users
	    ),
	    jsonb_build_object('memberships_count', CASE WHEN p_memberships IS NOT NULL THEN jsonb_array_length(p_memberships) ELSE 0 END)
	  );
	
	  RETURN jsonb_build_object('success', true, 'user_id', v_user_id);
	END;
	$function$
2026-09-13 06:19:25.184 UTC [24522] ERROR:  type "public.user_role" does not exist at character 392
2026-09-13 06:19:25.184 UTC [24522] STATEMENT:  CREATE OR REPLACE FUNCTION public.managed_reset_password(
	  p_user_id uuid,
	  p_caller_id uuid DEFAULT NULL::uuid
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  -- FIX H-7: anti-spoofing
	  v_caller_uid uuid := CASE WHEN auth.role() = 'service_role'
	    THEN COALESCE(p_caller_id, auth.uid()) ELSE auth.uid() END;
	  v_caller_role public.user_role;
	  v_target_email text;
	BEGIN
	  SELECT role INTO v_caller_role FROM public.profiles WHERE id = v_caller_uid;
	  IF v_caller_role IS NULL OR v_caller_role NOT IN ('admin', 'superadmin') THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED';
	  END IF;
	
	  IF p_user_id = v_caller_uid THEN
	    RAISE EXCEPTION 'ERR_SELF_RESET_BLOCKED';
	  END IF;
	
	  SELECT email INTO v_target_email FROM public.profiles WHERE id = p_user_id AND deleted_at IS NULL;
	  IF v_target_email IS NULL THEN
	    RAISE EXCEPTION 'ERR_USER_NOT_FOUND';
	  END IF;
	
	  INSERT INTO public.user_audit_log (performed_by, target_user_id, action, metadata)
	  VALUES (
	    v_caller_uid, p_user_id, 'PASSWORD_RESET_REQUESTED',
	    jsonb_build_object('email', v_target_email, 'method', 'recovery_link')
	  );
	
	  RETURN jsonb_build_object('success', true, 'email', v_target_email);
	END;
	$function$
2026-09-13 06:19:25.216 UTC [24528] ERROR:  type "public.user_role" does not exist at character 427
2026-09-13 06:19:25.216 UTC [24528] STATEMENT:  CREATE OR REPLACE FUNCTION public.managed_soft_delete_user(
	  p_user_id uuid,
	  p_reason text,
	  p_caller_id uuid DEFAULT NULL::uuid
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  v_old RECORD;
	  -- FIX H-7: anti-spoofing
	  v_caller_uid uuid := CASE WHEN auth.role() = 'service_role'
	    THEN COALESCE(p_caller_id, auth.uid()) ELSE auth.uid() END;
	  v_caller_role public.user_role;
	  v_active_memberships_count int;
	  v_anon_email text;
	BEGIN
	  SELECT role INTO v_caller_role FROM public.profiles WHERE id = v_caller_uid;
	  IF v_caller_role IS NULL OR v_caller_role NOT IN ('admin', 'superadmin') THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED';
	  END IF;
	
	  IF p_user_id = v_caller_uid THEN
	    RAISE EXCEPTION 'ERR_SELF_DELETE_BLOCKED';
	  END IF;
	
	  SELECT id, email, full_name, role, plan, is_active INTO v_old
	    FROM public.profiles WHERE id = p_user_id AND deleted_at IS NULL FOR UPDATE;
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_USER_NOT_FOUND_OR_ALREADY_DELETED';
	  END IF;
	
	  SELECT COUNT(*) INTO v_active_memberships_count
	    FROM public.user_store_memberships
	    WHERE user_id = p_user_id AND status = 'active';
	  IF v_active_memberships_count > 0 THEN
	    RAISE EXCEPTION 'ERR_USER_HAS_ACTIVE_MEMBERSHIPS: % active. Revoke memberships first.', v_active_memberships_count;
	  END IF;
	
	  v_anon_email := 'deleted+' || substr(p_user_id::text, 1, 8) || '@anonymized.local';
	
	  UPDATE public.profiles SET
	    deleted_at = now(),
	    deletion_reason = p_reason,
	    deleted_by = v_caller_uid,
	    is_active = false,
	    full_name = '[deleted user]',
	    email = v_anon_email,
	    ai_api_key = NULL,
	    updated_at = now()
	  WHERE id = p_user_id;
	
	  UPDATE public.user_store_memberships SET
	    status = 'revoked',
	    updated_at = now()
	  WHERE user_id = p_user_id AND status = 'active';
	
	  INSERT INTO public.user_audit_log (performed_by, target_user_id, action, old_values, new_values, metadata)
	  VALUES (
	    v_caller_uid, p_user_id, 'USER_SOFT_DELETED',
	    jsonb_build_object(
	      'email', v_old.email, 'full_name', v_old.full_name,
	      'role', v_old.role::text, 'plan', v_old.plan::text, 'is_active', v_old.is_active
	    ),
	    jsonb_build_object(
	      'email', v_anon_email, 'full_name', '[deleted user]',
	      'is_active', false, 'deleted_at', now()
	    ),
	    jsonb_build_object('reason', p_reason, 'memberships_revoked', v_active_memberships_count)
	  );
	
	  RETURN jsonb_build_object(
	    'success', true,
	    'user_id', p_user_id,
	    'status', 'soft_deleted',
	    'note', 'auth.users preserved. API route should ban via auth.admin.updateUser.'
	  );
	END;
	$function$
2026-09-13 06:19:25.246 UTC [24534] ERROR:  type "public.user_role" does not exist at character 435
2026-09-13 06:19:25.246 UTC [24534] STATEMENT:  CREATE OR REPLACE FUNCTION public.managed_toggle_user_status(
	  p_user_id uuid,
	  p_is_active boolean,
	  p_caller_id uuid DEFAULT NULL::uuid
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  v_old RECORD;
	  -- FIX H-7: anti-spoofing
	  v_caller_uid uuid := CASE WHEN auth.role() = 'service_role'
	    THEN COALESCE(p_caller_id, auth.uid()) ELSE auth.uid() END;
	  v_caller_role public.user_role;
	BEGIN
	  SELECT role INTO v_caller_role FROM public.profiles WHERE id = v_caller_uid;
	  IF v_caller_role IS NULL OR v_caller_role NOT IN ('admin', 'superadmin', 'encargado', 'manager') THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED';
	  END IF;
	
	  IF p_is_active = false AND p_user_id = v_caller_uid THEN
	    RAISE EXCEPTION 'ERR_SELF_DEACTIVATE_BLOCKED';
	  END IF;
	
	  SELECT id, is_active, full_name INTO v_old
	    FROM public.profiles WHERE id = p_user_id AND deleted_at IS NULL FOR UPDATE;
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_USER_NOT_FOUND';
	  END IF;
	
	  IF v_old.is_active = p_is_active THEN
	    RETURN jsonb_build_object('success', true, 'no_change', true);
	  END IF;
	
	  UPDATE public.profiles SET is_active = p_is_active, updated_at = now() WHERE id = p_user_id;
	
	  INSERT INTO public.user_audit_log (performed_by, target_user_id, action, old_values, new_values)
	  VALUES (
	    v_caller_uid, p_user_id,
	    CASE WHEN p_is_active THEN 'USER_ACTIVATED' ELSE 'USER_DEACTIVATED' END,
	    jsonb_build_object('is_active', v_old.is_active),
	    jsonb_build_object('is_active', p_is_active)
	  );
	
	  RETURN jsonb_build_object('success', true, 'user_id', p_user_id, 'is_active', p_is_active);
	END;
	$function$
2026-09-13 06:19:25.267 UTC [24539] ERROR:  type public.user_role does not exist
2026-09-13 06:19:25.267 UTC [24539] STATEMENT:  CREATE OR REPLACE FUNCTION public.managed_update_membership(
	  p_membership_id uuid,
	  p_role public.user_role DEFAULT NULL::user_role,
	  p_status text DEFAULT NULL::text,
	  p_caller_id uuid DEFAULT NULL::uuid
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  v_old RECORD;
	  -- FIX H-7: anti-spoofing
	  v_caller_uid uuid := CASE WHEN auth.role() = 'service_role'
	    THEN COALESCE(p_caller_id, auth.uid()) ELSE auth.uid() END;
	  v_changes jsonb := '{}'::jsonb;
	BEGIN
	  SELECT m.id, m.user_id, m.store_id, m.role, m.status
	    INTO v_old
	    FROM public.user_store_memberships m
	    WHERE m.id = p_membership_id
	    FOR UPDATE;
	
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_MEMBERSHIP_NOT_FOUND';
	  END IF;
	
	  -- FIX H-7: Autorización usa nueva sobrecarga has_store_role(p_user_id, p_store_id, p_roles)
	  -- service_role (API route) pasa p_caller_id=session.user.id → verifica acceso de ese user
	  -- authenticated pasa p_caller_id=NULL → v_caller_uid=auth.uid() → verifica su propio acceso
	  IF NOT public.has_store_role(v_caller_uid, v_old.store_id, ARRAY['admin', 'manager']) THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED: Caller must be admin or manager of the store.';
	  END IF;
	
	  IF p_role IS NOT NULL AND p_role <> v_old.role THEN
	    v_changes := v_changes || jsonb_build_object('role', jsonb_build_object('old', v_old.role::text, 'new', p_role::text));
	    UPDATE public.user_store_memberships SET role = p_role, updated_at = now() WHERE id = p_membership_id;
	  END IF;
	
	  IF p_status IS NOT NULL AND p_status <> v_old.status::text THEN
	    IF p_status NOT IN ('active', 'revoked') THEN
	      RAISE EXCEPTION 'ERR_INVALID_STATUS: %', p_status;
	    END IF;
	    v_changes := v_changes || jsonb_build_object('status', jsonb_build_object('old', v_old.status, 'new', p_status));
	    UPDATE public.user_store_memberships SET status = p_status::membership_status, updated_at = now() WHERE id = p_membership_id;
	  END IF;
	
	  IF v_changes <> '{}'::jsonb THEN
	    INSERT INTO public.user_audit_log (performed_by, target_user_id, action, old_values, new_values, metadata)
	    VALUES (
	      v_caller_uid, v_old.user_id, 'MEMBERSHIP_UPDATED',
	      jsonb_build_object('membership_id', p_membership_id, 'store_id', v_old.store_id, 'role', v_old.role::text, 'status', v_old.status),
	      v_changes,
	      jsonb_build_object('store_id', v_old.store_id)
	    );
	  END IF;
	
	  RETURN jsonb_build_object('success', true, 'membership_id', p_membership_id, 'changes', v_changes);
	END;
	$function$
2026-09-13 06:19:25.290 UTC [24544] ERROR:  type plan_t does not exist
2026-09-13 06:19:25.290 UTC [24544] STATEMENT:  CREATE OR REPLACE FUNCTION public.managed_update_tenant_plan(
	  p_tenant_id uuid,
	  p_plan plan_t,
	  p_subscription_status text DEFAULT NULL,
	  p_caller_id uuid DEFAULT NULL
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  -- FIX H-7: anti-spoofing — service_role (Stripe webhook) puede pasar NULL caller_id
	  v_caller_uid uuid := CASE WHEN auth.role() = 'service_role'
	    THEN COALESCE(p_caller_id, auth.uid()) ELSE auth.uid() END;
	  v_old_plan plan_t;
	  v_old_status text;
	  v_role public.user_role;
	BEGIN
	  -- Validación: si caller_uid es NULL (service_role anónimo para Stripe webhook), permitir
	  -- Si caller_uid NO es NULL, debe ser admin/superadmin
	  IF v_caller_uid IS NOT NULL THEN
	    SELECT role INTO v_role FROM public.profiles WHERE id = v_caller_uid;
	    IF v_role IS NULL OR v_role NOT IN ('admin', 'superadmin') THEN
	      RAISE EXCEPTION 'ERR_UNAUTHORIZED: Only admins can change tenant plan';
	    END IF;
	  END IF;
	
	  SELECT plan, subscription_status INTO v_old_plan, v_old_status
	    FROM public.tenants WHERE id = p_tenant_id FOR UPDATE;
	
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_TENANT_NOT_FOUND: %', p_tenant_id;
	  END IF;
	
	  UPDATE public.tenants SET
	    plan = p_plan,
	    subscription_status = COALESCE(p_subscription_status, subscription_status),
	    updated_at = now()
	  WHERE id = p_tenant_id;
	
	  UPDATE public.profiles SET
	    plan = p_plan,
	    updated_at = now()
	  WHERE tenant_id = p_tenant_id AND deleted_at IS NULL;
	
	  INSERT INTO public.user_audit_log (performed_by, target_user_id, action, old_values, new_values, metadata)
	  VALUES (
	    v_caller_uid, NULL, 'TENANT_PLAN_UPDATED',
	    jsonb_build_object('old_plan', v_old_plan::text, 'old_status', v_old_status),
	    jsonb_build_object('new_plan', p_plan::text, 'new_status', COALESCE(p_subscription_status, v_old_status)),
	    jsonb_build_object('tenant_id', p_tenant_id)
	  );
	
	  RETURN jsonb_build_object(
	    'success', true,
	    'tenant_id', p_tenant_id,
	    'old_plan', v_old_plan::text,
	    'new_plan', p_plan::text
	  );
	END;
	$function$
2026-09-13 06:19:25.315 UTC [24549] ERROR:  type public.user_role does not exist
2026-09-13 06:19:25.315 UTC [24549] STATEMENT:  CREATE OR REPLACE FUNCTION public.managed_update_user(
	  p_user_id uuid,
	  p_full_name text DEFAULT NULL::text,
	  p_role public.user_role DEFAULT NULL::user_role,
	  p_role_id uuid DEFAULT NULL::uuid,
	  p_is_active boolean DEFAULT NULL::boolean,
	  p_max_stores_limit int DEFAULT NULL::int,
	  p_max_users_limit int DEFAULT NULL::int,
	  p_plan plan_t DEFAULT NULL::plan_t,
	  p_caller_id uuid DEFAULT NULL::uuid
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  v_old RECORD;
	  -- FIX H-7: anti-spoofing — service_role puede pasar p_caller_id explícito
	  v_caller_uid uuid := CASE WHEN auth.role() = 'service_role'
	    THEN COALESCE(p_caller_id, auth.uid()) ELSE auth.uid() END;
	  v_caller_role public.user_role;
	  v_changes jsonb := '{}'::jsonb;
	BEGIN
	  -- Validar caller es admin (usa v_caller_uid que ahora NO es spoofable para authenticated)
	  SELECT role INTO v_caller_role FROM public.profiles WHERE id = v_caller_uid;
	  IF v_caller_role IS NULL OR v_caller_role NOT IN ('admin', 'superadmin') THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED: Only admins can update users.';
	  END IF;
	
	  SELECT * INTO v_old FROM public.profiles WHERE id = p_user_id AND deleted_at IS NULL FOR UPDATE;
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_USER_NOT_FOUND: %', p_user_id;
	  END IF;
	
	  IF p_is_active = false AND p_user_id = v_caller_uid THEN
	    RAISE EXCEPTION 'ERR_SELF_DEACTIVATE_BLOCKED: Cannot deactivate own account.';
	  END IF;
	
	  IF p_full_name IS NOT NULL AND p_full_name <> v_old.full_name THEN
	    v_changes := v_changes || jsonb_build_object('full_name', jsonb_build_object('old', v_old.full_name, 'new', p_full_name));
	    UPDATE public.profiles SET full_name = p_full_name, updated_at = now() WHERE id = p_user_id;
	  END IF;
	
	  IF p_role IS NOT NULL AND p_role <> v_old.role THEN
	    v_changes := v_changes || jsonb_build_object('role', jsonb_build_object('old', v_old.role::text, 'new', p_role::text));
	    UPDATE public.profiles SET role = p_role, updated_at = now() WHERE id = p_user_id;
	  END IF;
	
	  IF p_role_id IS NOT NULL AND (v_old.role_id IS NULL OR p_role_id <> v_old.role_id) THEN
	    v_changes := v_changes || jsonb_build_object('role_id', jsonb_build_object('old', v_old.role_id, 'new', p_role_id));
	    UPDATE public.profiles SET role_id = p_role_id, updated_at = now() WHERE id = p_user_id;
	  END IF;
	
	  IF p_is_active IS NOT NULL AND p_is_active <> v_old.is_active THEN
	    v_changes := v_changes || jsonb_build_object('is_active', jsonb_build_object('old', v_old.is_active, 'new', p_is_active));
	    UPDATE public.profiles SET is_active = p_is_active, updated_at = now() WHERE id = p_user_id;
	  END IF;
	
	  IF p_max_stores_limit IS NOT NULL AND (v_old.max_stores_limit IS NULL OR p_max_stores_limit <> v_old.max_stores_limit) THEN
	    v_changes := v_changes || jsonb_build_object('max_stores_limit', jsonb_build_object('old', v_old.max_stores_limit, 'new', p_max_stores_limit));
	    UPDATE public.profiles SET max_stores_limit = p_max_stores_limit, updated_at = now() WHERE id = p_user_id;
	  END IF;
	
	  IF p_max_users_limit IS NOT NULL AND (v_old.max_users_limit IS NULL OR p_max_users_limit <> v_old.max_users_limit) THEN
	    v_changes := v_changes || jsonb_build_object('max_users_limit', jsonb_build_object('old', v_old.max_users_limit, 'new', p_max_users_limit));
	    UPDATE public.profiles SET max_users_limit = p_max_users_limit, updated_at = now() WHERE id = p_user_id;
	  END IF;
	
	  IF p_plan IS NOT NULL AND p_plan <> v_old.plan THEN
	    v_changes := v_changes || jsonb_build_object('plan', jsonb_build_object('old', v_old.plan::text, 'new', p_plan::text));
	    UPDATE public.profiles SET plan = p_plan, updated_at = now() WHERE id = p_user_id;
	  END IF;
	
	  IF v_changes <> '{}'::jsonb THEN
	    INSERT INTO public.user_audit_log (performed_by, target_user_id, action, old_values, new_values, metadata)
	    VALUES (
	      v_caller_uid, p_user_id, 'USER_UPDATED',
	      jsonb_build_object(
	        'full_name', v_old.full_name, 'role', v_old.role::text,
	        'is_active', v_old.is_active, 'plan', v_old.plan::text
	      ),
	      v_changes,
	      jsonb_build_object('fields_changed', jsonb_object_keys(v_changes))
	    );
	  END IF;
	
	  RETURN jsonb_build_object('success', true, 'user_id', p_user_id, 'changes', v_changes);
	END;
	$function$
2026-09-13 06:19:25.356 UTC [24556] ERROR:  type "user_role" does not exist at character 204
2026-09-13 06:19:25.356 UTC [24556] STATEMENT:  CREATE OR REPLACE FUNCTION public.on_auth_user_created()
	RETURNS trigger
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO 'public', 'extensions'
	AS $function$
	DECLARE
	  v_role_id UUID;
	  v_role_enum USER_ROLE;
	  v_metadata_role TEXT;
	  v_company_name TEXT;
	  v_full_name TEXT;
	  v_tenant_id UUID;
	BEGIN
	  -- Extract role from metadata if present
	  v_metadata_role := NEW.raw_user_meta_data->>'role';
	
	  -- Normalize and convert to enum
	  IF v_metadata_role IS NOT NULL THEN
	    BEGIN
	      v_role_enum := v_metadata_role::USER_ROLE;
	    EXCEPTION WHEN OTHERS THEN
	      v_role_enum := 'tenant_admin'::USER_ROLE;
	    END;
	  ELSE
	    -- Iteración 13: self-signup users are tenant_admin by default
	    v_role_enum := 'tenant_admin'::USER_ROLE;
	  END IF;
	
	  -- Extract company_name and full_name from metadata
	  v_company_name := NEW.raw_user_meta_data->>'company_name';
	  v_full_name := COALESCE(NEW.raw_user_meta_data->>'full_name', split_part(NEW.email, '@', 1));
	
	  -- Iteración 13: Create tenant for self-signup users
	  -- If user was created by admin (via managed_create_user_v2), they may already
	  -- have a tenant_id set via p_target_user_id. In that case, skip tenant creation.
	  IF v_company_name IS NOT NULL THEN
	    INSERT INTO public.tenants (name, owner_id, plan, subscription_status, trial_ends_at, is_active)
	    VALUES (
	      v_company_name,
	      NEW.id,
	      'free'::plan_t,
	      'trial',
	      now() + interval '14 days',
	      true
	    )
	    RETURNING id INTO v_tenant_id;
	  END IF;
	
	  -- Get matching role_id from public.roles
	  SELECT id INTO v_role_id FROM public.roles
	    WHERE name = v_metadata_role OR lower(name) = lower(v_role_enum::text)
	    LIMIT 1;
	
	  -- Insert profile with tenant_id
	  INSERT INTO public.profiles (id, email, full_name, role, role_id, is_active, tenant_id, plan, created_at, updated_at)
	  VALUES (
	    NEW.id,
	    NEW.email,
	    v_full_name,
	    v_role_enum,
	    v_role_id,
	    true,
	    v_tenant_id,
	    'free'::plan_t,
	    now(),
	    now()
	  )
	  ON CONFLICT (id) DO UPDATE SET
	    email = EXCLUDED.email,
	    full_name = EXCLUDED.full_name,
	    role = EXCLUDED.role,
	    role_id = EXCLUDED.role_id,
	    tenant_id = COALESCE(profiles.tenant_id, EXCLUDED.tenant_id),
	    plan = EXCLUDED.plan,
	    is_active = true,
	    updated_at = now();
	
	  -- Audit log
	  INSERT INTO public.user_audit_log (performed_by, target_user_id, action, new_values, metadata)
	  VALUES (
	    NEW.id, NEW.id, 'USER_REGISTERED_PUBLICLY',
	    jsonb_build_object('email', NEW.email, 'role', v_role_enum::text, 'tenant_id', v_tenant_id),
	    jsonb_build_object('company_name', v_company_name, 'source', 'self_signup')
	  );
	
	  RETURN NEW;
	END;
	$function$
2026-09-13 06:19:25.415 UTC [24571] ERROR:  type adjustment_item[] does not exist
2026-09-13 06:19:25.415 UTC [24571] STATEMENT:  CREATE OR REPLACE FUNCTION public.process_inventory_adjustment(p_store_id uuid, p_cashier_id uuid, p_items adjustment_item[], p_operation_date timestamp with time zone DEFAULT NULL::timestamp with time zone)
	 RETURNS uuid
	 LANGUAGE plpgsql
	 SECURITY DEFINER
	 SET search_path TO 'public'
	AS $function$
	DECLARE
	  v_adjustment_id UUID;
	  v_item public.adjustment_item;
	  v_difference NUMERIC;
	  v_effective_date TIMESTAMP WITH TIME ZONE := COALESCE(p_operation_date, NOW());
	BEGIN
	  -- Validación forward-only locking
	  PERFORM public.validate_operation_date(p_operation_date, p_store_id);
	
	  INSERT INTO public.inventory_adjustments (store_id, created_by, status, created_at)
	  VALUES (p_store_id, p_cashier_id, 'PROCESSING', v_effective_date)
	  RETURNING id INTO v_adjustment_id;
	
	  FOREACH v_item IN ARRAY p_items
	  LOOP
	    v_difference := v_item.counted_quantity - v_item.expected_quantity;
	    INSERT INTO public.inventory_adjustment_items (adjustment_id, product_id, expected_quantity, counted_quantity, created_at)
	    VALUES (v_adjustment_id, v_item.product_id, v_item.expected_quantity, v_item.counted_quantity, v_effective_date);
	
	    PERFORM public.register_stock_movement(
	        p_product_id := v_item.product_id,
	        p_store_id := p_store_id,
	        p_user_id := p_cashier_id,
	        p_quantity := v_difference,
	        p_movement_type := 'adjustment',
	        p_operation_date := v_effective_date
	    );
	  END LOOP;
	
	  UPDATE public.inventory_adjustments SET status = 'COMPLETED', updated_at = v_effective_date
	  WHERE id = v_adjustment_id;
	  RETURN v_adjustment_id;
	END;
	$function$
2026-09-13 06:19:25.457 UTC [24580] ERROR:  type "public.purchase_status_enum" does not exist at character 425
2026-09-13 06:19:25.457 UTC [24580] STATEMENT:  CREATE OR REPLACE FUNCTION public.receive_against_po(
	  p_po_id uuid,
	  p_received_items jsonb DEFAULT '[]'::jsonb,
	  p_user_id uuid DEFAULT NULL,
	  p_reception_date timestamptz DEFAULT now(),
	  p_invoice_number text DEFAULT NULL
	) RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO 'public', 'extensions'
	AS $func$
	DECLARE
	  v_store_id            uuid;
	  v_supplier_name       text;
	  v_po_status           public.purchase_status_enum;
	  v_item                jsonb;
	  v_item_id             uuid;
	  v_qty                 numeric;
	  v_qty_ordered         numeric;
	  v_qty_received_before numeric;
	  v_product_id          uuid;
	  v_unit_cost           numeric;
	  v_product_name        text;
	  v_all_received        boolean;
	  v_any_received        boolean;
	  v_new_status          public.purchase_status_enum;
	  v_receipt_id          uuid;
	  v_reception_items     jsonb := '[]'::jsonb;
	  v_count               integer := 0;
	  v_po_number           text;
	  v_invoice_to_pass     text;
	  v_sorted_items        jsonb;
	BEGIN
	  -- ─── 1. Cargar OC con lock exclusivo ───
	  SELECT po.store_id, po.supplier_name, po.status, po.po_number
	    INTO v_store_id, v_supplier_name, v_po_status, v_po_number
	  FROM public.purchase_orders po
	  WHERE po.id = p_po_id
	  FOR UPDATE;
	
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_PO_NOT_FOUND';
	  END IF;
	
	  -- ─── 2. Validar acceso (tenant-aware via has_store_access) ───
	  IF NOT public.has_store_access(v_store_id) THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED';
	  END IF;
	
	  -- ─── 3. Validar estado de la OC ───
	  IF v_po_status = 'cancelled' THEN
	    RAISE EXCEPTION 'ERR_PO_CANCELLED';
	  END IF;
	  -- Solo se puede recibir contra draft, sent o partial.
	  IF v_po_status NOT IN ('draft', 'sent', 'partial') THEN
	    RAISE EXCEPTION 'ERR_PO_NOT_RECEIVABLE: status % is terminal', v_po_status;
	  END IF;
	
	  -- ─── 4. Validar items no vacío (parity B3 register_reception) ───
	  IF p_received_items IS NULL OR jsonb_array_length(p_received_items) = 0 THEN
	    RAISE EXCEPTION 'ERR_EMPTY_ITEMS';
	  END IF;
	
	  -- ─── 5. Condición #5 (anti-deadlock): Ordenar items por po_item_id ASC ───
	  SELECT jsonb_agg(elem ORDER BY (elem->>'po_item_id'))
	    INTO v_sorted_items
	  FROM jsonb_array_elements(p_received_items) AS elem;
	
	  -- ─── 6. Procesar cada item: VALIDAR antes de actualizar ───
	  FOR v_item IN SELECT * FROM jsonb_array_elements(v_sorted_items) LOOP
	    v_item_id := NULLIF(v_item->>'po_item_id', '')::uuid;
	    v_qty     := COALESCE((v_item->>'quantity_received')::numeric, 0);
	
	    -- Validar po_item_id presente
	    IF v_item_id IS NULL THEN
	      RAISE EXCEPTION 'ERR_ITEM_ID_REQUIRED: missing po_item_id in received_items';
	    END IF;
	
	    -- Medio #6: qty > 0
	    IF v_qty <= 0 THEN
	      RAISE EXCEPTION 'ERR_NEGATIVE_QTY: qty % for item % must be > 0', v_qty, v_item_id;
	    END IF;
	
	    -- Cargar estado actual del item con lock FOR UPDATE
	    SELECT product_id, quantity_ordered, quantity_received, unit_cost, product_name
	      INTO v_product_id, v_qty_ordered, v_qty_received_before, v_unit_cost, v_product_name
	    FROM public.purchase_order_items
	    WHERE id = v_item_id AND po_id = p_po_id
	    FOR UPDATE;
	
	    IF NOT FOUND THEN
	      RAISE EXCEPTION 'ERR_ITEM_NOT_FOUND: %', v_item_id;
	    END IF;
	
	    -- Alto #5: over-receive check
	    IF (v_qty_received_before + v_qty) > v_qty_ordered THEN
	      RAISE EXCEPTION 'ERR_OVER_RECEIVE: item % ordered %, already received %, attempting %',
	        v_item_id, v_qty_ordered, v_qty_received_before, v_qty;
	    END IF;
	
	    -- Condición #1: todo item debe tener product_id NOT NULL
	    IF v_product_id IS NULL THEN
	      RAISE EXCEPTION 'ERR_PRODUCT_ID_REQUIRED: item % has no product_id', v_item_id;
	    END IF;
	
	    -- Medio #9: producto debe existir en store
	    IF NOT EXISTS (
	      SELECT 1 FROM public.products p
	      WHERE p.id = v_product_id AND p.store_id = v_store_id
	    ) THEN
	      RAISE EXCEPTION 'ERR_PRODUCT_NOT_IN_STORE: product % not in store %',
	        v_product_id, v_store_id;
	    END IF;
	
	    -- Acumular para register_reception (formato esperado: product_id, quantity, unit_cost, moneda, tasa)
	    v_reception_items := v_reception_items || jsonb_build_object(
	      'product_id',   v_product_id,
	      'product_name', v_product_name,
	      'quantity',     v_qty,
	      'unit_cost',    v_unit_cost,
	      'moneda_recepcion', 'CUP',
	      'tasa_cambio_recepcion', 1.0
	    );
	    v_count := v_count + 1;
	  END LOOP;
	
	  -- ─── 7. Condición #3: reference_doc NULL si no viene invoice_number ───
	  -- Evita colisión con UNIQUE INDEX idx_receipts_store_reference_doc
	  -- (store_id, reference_doc) en partial indexes.
	  -- El vínculo real receipt↔PO es receipts.po_id (G6).
	  IF p_invoice_number IS NULL OR p_invoice_number = '' THEN
	    v_invoice_to_pass := NULL;
	  ELSE
	    v_invoice_to_pass := p_invoice_number;
	  END IF;
	
	  -- ─── 8. Crear receipt vinculado vía register_reception ───
	  -- register_reception aplica las 8 validaciones B1-B5 + C1-C3 del v2.23.0
	  -- (no se relajan). G6 añade p_po_id opcional.
	  v_receipt_id := public.register_reception(
	    p_store_id        := v_store_id,
	    p_supplier        := v_supplier_name,
	    p_reception_date  := p_reception_date,
	    p_invoice_number  := v_invoice_to_pass,
	    p_items           := v_reception_items,
	    p_user_id         := p_user_id,
	    p_po_id           := p_po_id
	  );
	
	  -- ─── 9. Actualizar quantity_received en purchase_order_items ───
	  -- (SECURITY DEFINER bypasses RLS DENY UPDATE de G5)
	  FOR v_item IN SELECT * FROM jsonb_array_elements(v_sorted_items) LOOP
	    v_item_id := NULLIF(v_item->>'po_item_id', '')::uuid;
	    v_qty     := (v_item->>'quantity_received')::numeric;
	
	    UPDATE public.purchase_order_items
	    SET quantity_received = quantity_received + v_qty
	    WHERE id = v_item_id AND po_id = p_po_id;
	  END LOOP;
	
	  -- ─── 10. Recalcular status global (cast EXPLÍCITO a enum) ───
	  SELECT
	    BOOL_AND(quantity_received >= quantity_ordered),
	    BOOL_OR(quantity_received > 0)
	  INTO v_all_received, v_any_received
	  FROM public.purchase_order_items WHERE po_id = p_po_id;
	
	  v_new_status :=
	    CASE
	      WHEN v_all_received THEN 'received'::public.purchase_status_enum
	      WHEN v_any_received THEN 'partial'::public.purchase_status_enum
	      ELSE 'sent'::public.purchase_status_enum
	    END;
	
	  -- ─── 11. Actualizar OC + vincular receipt en notas ───
	  UPDATE public.purchase_orders
	  SET status       = v_new_status,
	      received_at  = CASE WHEN v_new_status = 'received' THEN NOW() ELSE received_at END,
	      notes        = COALESCE(notes, '') ||
	                     CASE WHEN COALESCE(notes, '') = '' THEN '' ELSE E'\n' END ||
	                     '[Receipt ' || v_receipt_id::text || ' linked at ' || NOW()::text || ']'
	  WHERE id = p_po_id;
	
	  -- ─── 12. CxP (Grupo 6): marcar receipt con payment_status='unpaid' ───
	  -- Solo si aún no está seteado (no sobrescribir si register_reception ya lo puso).
	  UPDATE public.receipts
	  SET payment_status     = COALESCE(NULLIF(payment_status, ''), 'unpaid'),
	      payment_terms_days = COALESCE(payment_terms_days, 30),
	      due_date           = COALESCE(due_date, (p_reception_date::date + INTERVAL '30 days')::date)
	  WHERE id = v_receipt_id;
	
	  -- ─── 13. Auditoría ───
	  INSERT INTO public.audit_logs (user_id, store_id, action, table_name, record_id, metadata)
	  VALUES (
	    p_user_id, v_store_id, 'po_received', 'purchase_orders', p_po_id,
	    jsonb_build_object(
	      'new_status', v_new_status::text,
	      'items_received', v_count,
	      'receipt_id', v_receipt_id,
	      'po_number', v_po_number,
	      'invoice_number', v_invoice_to_pass
	    )
	  );
	
	  RETURN jsonb_build_object(
	    'status', 'success',
	    'po_status', v_new_status::text,
	    'receipt_id', v_receipt_id,
	    'items_received', v_count
	  );
	END;
	$func$
2026-09-13 06:19:25.680 UTC [24617] ERROR:  type purchase_status_enum does not exist
2026-09-13 06:19:25.680 UTC [24617] STATEMENT:  CREATE OR REPLACE FUNCTION public.set_purchase_order_status(p_po_id uuid, p_new_status purchase_status_enum, p_user_id uuid DEFAULT NULL::uuid, p_reason text DEFAULT NULL::text)
	 RETURNS jsonb
	 LANGUAGE plpgsql
	 SECURITY DEFINER
	 SET search_path TO 'public', 'extensions'
	AS $function$
	DECLARE
	  v_store_id     uuid;
	  v_current      public.purchase_status_enum;
	  v_po_number    text;
	  v_allowed      text[];
	  v_is_allowed   boolean := false;
	BEGIN
	  -- ─── 1. Cargar PO con lock exclusivo ───
	  SELECT store_id, status, po_number
	    INTO v_store_id, v_current, v_po_number
	  FROM public.purchase_orders
	  WHERE id = p_po_id
	  FOR UPDATE;
	
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_PO_NOT_FOUND';
	  END IF;
	
	  -- ─── 2. Validar acceso (tenant-aware) ───
	  IF NOT public.has_store_access(v_store_id) THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED';
	  END IF;
	
	  -- ─── 3. Si no hay cambio, retornar success sin hacer nada ───
	  IF v_current = p_new_status THEN
	    RETURN jsonb_build_object(
	      'status', 'no_change',
	      'po_status', v_current::text,
	      'po_number', v_po_number
	    );
	  END IF;
	
	  -- ─── 4. Definir transiciones permitidas (state machine) ───
	  -- 'partial' y 'received' NUNCA son destino válido manualmente
	  -- (solo vía receive_against_po).
	  v_allowed := CASE v_current
	    WHEN 'draft'   THEN ARRAY['sent', 'cancelled']
	    WHEN 'sent'    THEN ARRAY['cancelled']
	    WHEN 'partial' THEN ARRAY['cancelled']
	    ELSE ARRAY[]::text[]  -- received, cancelled: terminal, no transitions
	  END;
	
	  -- ─── 5. Verificar transición permitida ───
	  SELECT EXISTS(SELECT 1 FROM unnest(v_allowed) a WHERE a = p_new_status::text)
	    INTO v_is_allowed;
	
	  IF NOT v_is_allowed THEN
	    RAISE EXCEPTION 'ERR_INVALID_TRANSITION: % → % not allowed (allowed: %)',
	      v_current::text, p_new_status::text,
	      CASE WHEN array_length(v_allowed, 1) IS NULL THEN 'none' ELSE array_to_string(v_allowed, ', ') END;
	  END IF;
	
	  -- ─── 6. Aplicar transición ───
	  UPDATE public.purchase_orders
	  SET status      = p_new_status,
	      received_at = CASE WHEN p_new_status = 'received' THEN NOW() ELSE received_at END
	  WHERE id = p_po_id;
	
	  -- ─── 7. Auditoría (action = PO_STATUS_CHANGED) ───
	  INSERT INTO public.audit_logs (user_id, store_id, action, table_name, record_id, metadata)
	  VALUES (
	    CASE WHEN auth.role() = 'service_role' THEN COALESCE(p_user_id, auth.uid()) ELSE auth.uid() END, v_store_id, 'PO_STATUS_CHANGED', 'purchase_orders', p_po_id,
	    jsonb_build_object(
	      'po_number', v_po_number,
	      'from_status', v_current::text,
	      'to_status', p_new_status::text,
	      'reason', p_reason
	    )
	  );
	
	  RETURN jsonb_build_object(
	    'status', 'success',
	    'po_status', p_new_status::text,
	    'po_number', v_po_number,
	    'previous_status', v_current::text
	  );
	END;
	$function$
2026-09-13 06:19:25.797 UTC [24643] ERROR:  type "user_role" does not exist at character 100
2026-09-13 06:19:25.797 UTC [24643] STATEMENT:  CREATE OR REPLACE FUNCTION public.validate_active_store()
	RETURNS TRIGGER AS $$
	DECLARE
	    v_role user_role;
	BEGIN
	    v_role := NEW.role;
	
	    -- Si role es 'costo' o 'admin' o 'superadmin', active_store_id es opcional
	    IF v_role = 'costo'::user_role
	       OR v_role = 'admin'::user_role
	       OR v_role = 'superadmin'::user_role THEN
	        -- Solo validar si active_store_id cambió o es INSERT
	        IF TG_OP = 'INSERT' OR (OLD.active_store_id IS DISTINCT FROM NEW.active_store_id) THEN
	            IF NEW.active_store_id IS NOT NULL THEN
	                IF NOT EXISTS (
	                    SELECT 1 FROM public.user_store_memberships
	                    WHERE user_id = NEW.id
	                      AND store_id = NEW.active_store_id
	                      AND status = 'active'
	                ) THEN
	                    IF TG_OP = 'UPDATE' THEN
	                        RAISE EXCEPTION 'ERR_INVALID_ACTIVE_STORE: El usuario no tiene membership activa en la tienda seleccionada.';
	                    END IF;
	                END IF;
	            END IF;
	        END IF;
	        RETURN NEW;
	    END IF;
	
	    -- Para roles operativos (encargado, clerk, warehouse, manager, usuario)
	    IF TG_OP = 'INSERT' THEN
	        -- En INSERT: ser permisivo. La función managed_create_user inserta
	        -- memberships justo después. No validar nada aquí.
	        RETURN NEW;
	    ELSE
	        -- UPDATE: solo validar si active_store_id cambió realmente
	        -- (evita bloquear updates de otros campos en perfiles intermedios)
	        IF OLD.active_store_id IS NOT DISTINCT FROM NEW.active_store_id THEN
	            -- active_store_id no cambió → permitir el UPDATE sin validar
	            RETURN NEW;
	        END IF;
	
	        -- active_store_id cambió → validar consistencia
	        IF NEW.active_store_id IS NULL THEN
	            IF v_role IN ('encargado'::user_role, 'clerk'::user_role, 'warehouse'::user_role) THEN
	                RAISE EXCEPTION 'ERR_STORE_REQUIRED: El rol % requiere una tienda activa asignada.', v_role;
	            END IF;
	        ELSE
	            IF NOT EXISTS (
	                SELECT 1 FROM public.user_store_memberships
	                WHERE user_id = NEW.id
	                  AND store_id = NEW.active_store_id
	                  AND status = 'active'
	            ) THEN
	                RAISE EXCEPTION 'ERR_INVALID_ACTIVE_STORE: El usuario no tiene membership activa en la tienda seleccionada.';
	            END IF;
	        END IF;
	        RETURN NEW;
	    END IF;
	END;
	$$ LANGUAGE plpgsql SECURITY DEFINER
2026-09-13 06:19:25.847 UTC [24654] ERROR:  type "public.user_role" does not exist at character 270
2026-09-13 06:19:25.847 UTC [24654] STATEMENT:  CREATE OR REPLACE FUNCTION public.validate_tenant_access(
	  p_user_id uuid,
	  p_store_id uuid
	)
	RETURNS boolean
	LANGUAGE plpgsql
	STABLE SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  v_user_tenant uuid;
	  v_store_tenant uuid;
	  v_user_role public.user_role;
	BEGIN
	  -- Global admin bypasses tenant check
	  SELECT role, tenant_id INTO v_user_role, v_user_tenant
	    FROM public.profiles WHERE id = p_user_id;
	
	  IF v_user_role IN ('admin', 'superadmin') THEN
	    RETURN true;
	  END IF;
	
	  -- Get store's tenant
	  SELECT tenant_id INTO v_store_tenant FROM public.stores WHERE id = p_store_id;
	
	  IF v_store_tenant IS NULL THEN
	    -- Legacy store without tenant — allow (backward compat)
	    RETURN true;
	  END IF;
	
	  -- Check tenant match
	  RETURN v_user_tenant = v_store_tenant;
	END;
	$function$
2026-09-13 06:19:26.005 UTC [24689] ERROR:  type "public.user_role" does not exist at character 301
2026-09-13 06:19:26.005 UTC [24689] STATEMENT:  CREATE OR REPLACE FUNCTION public.bulk_assign_memberships(
	  p_user_id uuid,
	  p_assignments jsonb
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  v_assignment jsonb;
	  v_affected int := 0;
	  v_failed int := 0;
	  v_store_id uuid;
	  v_role public.user_role;
	  v_status text;
	  v_caller_uid uuid := auth.uid();
	  v_changes jsonb := '[]'::jsonb;
	BEGIN
	  IF v_caller_uid IS NULL THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED';
	  END IF;
	
	  FOR v_assignment IN SELECT * FROM jsonb_array_elements(p_assignments) LOOP
	    BEGIN
	      v_store_id := (v_assignment->>'store_id')::uuid;
	      v_role := (v_assignment->>'role')::public.user_role;
	      v_status := COALESCE(v_assignment->>'status', 'active');
	
	      -- Validar caller tiene acceso al store
	      IF NOT public.is_admin() AND NOT public.has_store_role(v_store_id, ARRAY['admin', 'manager']) THEN
	        v_failed := v_failed + 1;
	        CONTINUE;
	      END IF;
	
	      INSERT INTO public.user_store_memberships (user_id, store_id, role, status)
	      VALUES (p_user_id, v_store_id, v_role, v_status)
	      ON CONFLICT (user_id, store_id) DO UPDATE SET
	        role = EXCLUDED.role,
	        status = EXCLUDED.status,
	        updated_at = now();
	
	      v_changes := v_changes || jsonb_build_object(jsonb_build_object(
	        'store_id', v_store_id,
	        'role', v_role::text,
	        'status', v_status
	      ));
	
	      v_affected := v_affected + 1;
	    EXCEPTION
	      WHEN foreign_key_violation THEN
	        v_failed := v_failed + 1;
	    END;
	  END LOOP;
	
	  -- Audit log atómico (solo si hubo cambios)
	  IF v_affected > 0 THEN
	    INSERT INTO public.user_audit_log (performed_by, target_user_id, action, new_values, metadata)
	    VALUES (
	      v_caller_uid, p_user_id, 'MEMBERSHIPS_BULK_ASSIGNED',
	      jsonb_build_object('assignments', v_changes),
	      jsonb_build_object('affected', v_affected, 'failed', v_failed)
	    );
	  END IF;
	
	  RETURN jsonb_build_object(
	    'success', true,
	    'affected', v_affected,
	    'failed', v_failed,
	    'user_id', p_user_id
	  );
	END;
	$function$
2026-09-13 06:19:26.038 UTC [24694] ERROR:  syntax error at or near "REVOKE" at character 5039
2026-09-13 06:19:26.038 UTC [24694] STATEMENT:  CREATE OR REPLACE FUNCTION public.bulk_soft_delete_stores(p_store_ids uuid[], p_deleted_by uuid, p_confirmation_token text, p_override_token text DEFAULT NULL::text, p_reason text DEFAULT NULL::text)
	 RETURNS jsonb
	 LANGUAGE plpgsql
	 SECURITY DEFINER
	 SET search_path TO 'public'
	AS $function$
	DECLARE
	  v_store_id UUID;
	  v_validation JSONB;
	  v_blockers JSONB;
	  v_errors JSONB[] := '{}'::jsonb[];
	  v_processed INTEGER := 0;
	  v_token_valid BOOLEAN;
	  v_has_protected BOOLEAN;
	  v_override_valid BOOLEAN;
	  v_confirmation_record RECORD;
	  v_override_record RECORD;
	  v_caller_role TEXT;
	BEGIN
	  -- ============================================================
	  -- AUTH CHECK: Solo admin puede ejecutar esta función
	  -- ============================================================
	  IF auth.uid() IS NOT NULL THEN
	    SELECT role INTO v_caller_role FROM public.profiles WHERE id = auth.uid();
	    IF v_caller_role IS NULL OR v_caller_role != 'admin' THEN
	      RAISE EXCEPTION 'ERR_PERMISSION_DENIED: Solo admin puede ejecutar bulk_soft_delete_stores';
	    END IF;
	  END IF;
	  -- Si auth.uid() IS NULL, es service_role — permitir
	
	  -- ============================================================
	  -- 1. VALIDATE confirmation_token
	  -- ============================================================
	  SELECT * INTO v_confirmation_record
	  FROM public.bulk_confirmation_tokens
	  WHERE token = p_confirmation_token
	    AND action = 'delete'
	    AND expires_at > NOW()
	    AND consumed_at IS NULL
	    AND is_override = false;
	
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_INVALID_CONFIRMATION_TOKEN';
	  END IF;
	
	  IF v_confirmation_record.store_ids != p_store_ids THEN
	    RAISE EXCEPTION 'ERR_STORE_IDS_MISMATCH';
	  END IF;
	
	  -- ============================================================
	  -- 2. VALIDATE tiendas protegidas requieren override_token
	  -- ============================================================
	  SELECT EXISTS(
	    SELECT 1 FROM public.stores
	    WHERE id = ANY(p_store_ids) AND backup_restore_protected = true
	  ) INTO v_has_protected;
	
	  IF v_has_protected THEN
	    IF p_override_token IS NULL THEN
	      RAISE EXCEPTION 'ERR_OVERRIDE_REQUIRED';
	    END IF;
	
	    SELECT * INTO v_override_record
	    FROM public.bulk_confirmation_tokens
	    WHERE token = p_override_token
	      AND is_override = true
	      AND override_for = p_confirmation_token
	      AND expires_at > NOW()
	      AND consumed_at IS NULL;
	
	    IF NOT FOUND THEN
	      RAISE EXCEPTION 'ERR_INVALID_OVERRIDE_TOKEN';
	    END IF;
	
	    IF v_override_record.store_ids != v_confirmation_record.store_ids THEN
	      RAISE EXCEPTION 'ERR_OVERRIDE_STORE_IDS_MISMATCH';
	    END IF;
	
	    IF v_override_record.created_by = v_confirmation_record.created_by THEN
	      RAISE EXCEPTION 'ERR_SAME_USER_OVERRIDE';
	    END IF;
	
	    UPDATE public.bulk_confirmation_tokens SET consumed_at = NOW()
	    WHERE token = p_override_token;
	  END IF;
	
	  UPDATE public.bulk_confirmation_tokens SET consumed_at = NOW()
	  WHERE token = p_confirmation_token;
	
	  -- ============================================================
	  -- 3. VALIDATE todas las tiendas
	  -- ============================================================
	  FOREACH v_store_id IN ARRAY p_store_ids LOOP
	    IF NOT EXISTS(SELECT 1 FROM public.stores WHERE id = v_store_id AND is_active = true) THEN
	      v_errors := array_append(v_errors, jsonb_build_object(
	        'store_id', v_store_id, 'reason', 'STORE_NOT_FOUND_OR_INACTIVE'
	      ));
	      CONTINUE;
	    END IF;
	
	    SELECT public.validate_store_can_be_modified(v_store_id, 'soft_delete') INTO v_validation;
	    v_blockers := v_validation->'blockers';
	
	    IF v_validation->>'can_modify' != 'true' THEN
	      v_errors := array_append(v_errors, jsonb_build_object(
	        'store_id', v_store_id, 'reason', 'HAS_BLOCKING_DEPENDENCIES', 'blockers', v_blockers
	      ));
	    END IF;
	  END LOOP;
	
	  IF array_length(v_errors, 1) IS NOT NULL THEN
	    RETURN jsonb_build_object(
	      'status', 'FAILED', 'processed', 0,
	      'total_requested', array_length(p_store_ids, 1),
	      'errors', to_jsonb(v_errors), 'reason', p_reason
	    );
	  END IF;
	
	  -- ============================================================
	  -- 4. EXECUTE
	  -- ============================================================
	  FOREACH v_store_id IN ARRAY p_store_ids LOOP
	    PERFORM public.soft_delete_store(v_store_id, p_deleted_by);
	    v_processed := v_processed + 1;
	  END LOOP;
	
	  INSERT INTO public.audit_logs (action, table_name, record_id, metadata)
	  VALUES (
	    'bulk_store_deleted', 'stores', NULL,
	    jsonb_build_object(
	      'store_ids', p_store_ids, 'deleted_by', p_deleted_by,
	      'reason', p_reason, 'processed', v_processed,
	      'had_protected_stores', v_has_protected,
	      'override_used', p_override_token IS NOT NULL, 'deleted_at', NOW()
	    )
	  );
	
	  RETURN jsonb_build_object(
	    'status', 'COMPLETED', 'processed', v_processed,
	    'total_requested', array_length(p_store_ids, 1),
	    'errors', '[]'::jsonb, 'reason', p_reason
	  );
	END;
	$function$
	 
	REVOKE EXECUTE ON FUNCTION public.bulk_soft_delete_stores FROM PUBLIC, anon, authenticated
2026-09-13 06:19:26.095 UTC [24703] ERROR:  relation "received_services" does not exist
2026-09-13 06:19:26.095 UTC [24703] CONTEXT:  compilation of PL/pgSQL function "calculate_service_distribution" near line 3
2026-09-13 06:19:26.095 UTC [24703] STATEMENT:  CREATE OR REPLACE FUNCTION calculate_service_distribution(
	  p_service_id UUID
	) RETURNS TABLE(
	  receipt_item_id UUID,
	  product_id UUID,
	  distribution_amount NUMERIC,
	  distribution_percentage NUMERIC
	) LANGUAGE plpgsql SECURITY DEFINER AS $$
	DECLARE
	  v_service received_services%ROWTYPE;
	  v_method TEXT;
	  v_total_amount NUMERIC;
	  v_total_value NUMERIC DEFAULT 0;
	  v_total_qty NUMERIC DEFAULT 0;
	  v_link service_reception_links%ROWTYPE;
	  v_receipt_id UUID;
	  v_item receipt_items%ROWTYPE;
	  v_allocated NUMERIC;
	BEGIN
	  SELECT * INTO v_service FROM received_services WHERE id = p_service_id;
	  IF NOT FOUND THEN RETURN; END IF;
	  
	  v_method := v_service.distribution_method;
	  v_total_amount := v_service.total_amount;
	
	  -- Para cada recepción vinculada
	  FOR v_link IN SELECT * FROM service_reception_links WHERE service_id = p_service_id AND allocated_amount > 0 LOOP
	    v_receipt_id := v_link.receipt_id;
	    v_allocated := v_link.allocated_amount;
	
	    -- Calcular base de distribución según método
	    IF v_method = 'amount' THEN
	      SELECT COALESCE(SUM(quantity * unit_cost), 0) INTO v_total_value
	      FROM receipt_items WHERE receipt_id = v_receipt_id;
	      
	      IF v_total_value > 0 THEN
	        FOR v_item IN SELECT * FROM receipt_items WHERE receipt_id = v_receipt_id LOOP
	          distribution_amount := v_allocated * (v_item.quantity * v_item.unit_cost / v_total_value);
	          distribution_percentage := (v_item.quantity * v_item.unit_cost / v_total_value) * 100;
	          receipt_item_id := v_item.id;
	          product_id := v_item.product_id;
	          RETURN NEXT;
	        END LOOP;
	      END IF;
	
	    ELSIF v_method = 'quantity' THEN
	      SELECT COALESCE(SUM(quantity), 0) INTO v_total_qty
	      FROM receipt_items WHERE receipt_id = v_receipt_id;
	      
	      IF v_total_qty > 0 THEN
	        FOR v_item IN SELECT * FROM receipt_items WHERE receipt_id = v_receipt_id LOOP
	          distribution_amount := v_allocated * (v_item.quantity / v_total_qty);
	          distribution_percentage := (v_item.quantity / v_total_qty) * 100;
	          receipt_item_id := v_item.id;
	          product_id := v_item.product_id;
	          RETURN NEXT;
	        END LOOP;
	      END IF;
	
	    ELSIF v_method = 'manual' THEN
	      -- En manual, la distribución se guarda directamente desde el cliente
	      RETURN;
	    END IF;
	  END LOOP;
	END;
	$$
2026-09-13 06:19:26.202 UTC [24715] ERROR:  syntax error at or near "REVOKE" at character 678
2026-09-13 06:19:26.202 UTC [24715] STATEMENT:  CREATE OR REPLACE FUNCTION public.check_bulk_ops_hourly_limit(p_user_id uuid, p_plan text)
	 RETURNS jsonb
	 LANGUAGE plpgsql
	 STABLE SECURITY DEFINER
	 SET search_path TO 'public'
	AS $function$
	DECLARE
	  v_limit INTEGER;
	  v_used INTEGER;
	BEGIN
	  v_limit := CASE p_plan
	    WHEN 'free' THEN 1
	    WHEN 'pro' THEN 20
	    WHEN 'enterprise' THEN 999999
	    ELSE 1
	  END;
	
	  SELECT COUNT(*) INTO v_used
	  FROM public.bulk_ops_log
	  WHERE user_id = p_user_id
	    AND initiated_at > NOW() - INTERVAL '1 hour';
	
	  RETURN jsonb_build_object(
	    'allowed', v_used < v_limit,
	    'used', v_used,
	    'limit', v_limit,
	    'remaining', GREATEST(0, v_limit - v_used)
	  );
	END;
	$function$
	 
	REVOKE EXECUTE ON FUNCTION public.check_bulk_ops_hourly_limit FROM PUBLIC, anon, authenticated
2026-09-13 06:19:26.236 UTC [24722] ERROR:  type plan_t does not exist
2026-09-13 06:19:26.236 UTC [24722] STATEMENT:  CREATE OR REPLACE FUNCTION public.check_tenant_store_quota(
	  p_tenant_id uuid,
	  p_plan plan_t DEFAULT NULL
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  v_plan plan_t;
	  v_limit int;
	  v_current int;
	  v_allowed boolean;
	BEGIN
	  -- Get plan from tenant if not passed
	  IF p_plan IS NULL THEN
	    SELECT plan INTO v_plan FROM public.tenants WHERE id = p_tenant_id;
	  ELSE
	    v_plan := p_plan;
	  END IF;
	
	  -- Plan limits (unified source of truth)
	  v_limit := CASE v_plan
	    WHEN 'free'::plan_t THEN 1
	    WHEN 'pro'::plan_t THEN 3
	    WHEN 'enterprise'::plan_t THEN 10
	    ELSE 1
	  END;
	
	  -- Count active stores in tenant
	  SELECT COUNT(*) INTO v_current
	    FROM public.stores
	    WHERE tenant_id = p_tenant_id AND is_active = true;
	
	  v_allowed := v_current < v_limit;
	
	  RETURN jsonb_build_object(
	    'allowed', v_allowed,
	    'current', v_current,
	    'limit', v_limit,
	    'plan', v_plan::text,
	    'tenant_id', p_tenant_id
	  );
	END;
	$function$
2026-09-13 06:19:26.344 UTC [24751] ERROR:  relation "public.profiles" does not exist at character 166
2026-09-13 06:19:26.344 UTC [24751] STATEMENT:  CREATE OR REPLACE FUNCTION public.current_user_tenant_id()
	RETURNS uuid
	LANGUAGE sql
	STABLE
	SECURITY DEFINER
	SET search_path TO public
	AS $$
	  SELECT tenant_id FROM public.profiles WHERE id = auth.uid();
	$$
2026-09-13 06:19:26.395 UTC [24759] ERROR:  relation "received_services" does not exist
2026-09-13 06:19:26.395 UTC [24759] CONTEXT:  compilation of PL/pgSQL function "distribute_service_cost_v2" near line 6
2026-09-13 06:19:26.395 UTC [24759] STATEMENT:  CREATE OR REPLACE FUNCTION public.distribute_service_cost_v2(
	  p_service_id uuid,
	  p_user_id uuid DEFAULT NULL
	) RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO 'public', 'extensions'
	AS $func$
	DECLARE
	  v_store_id uuid;
	  v_status text;
	  v_total_amount numeric;
	  v_service received_services%ROWTYPE;
	  v_method text;
	  v_link service_reception_links%ROWTYPE;
	  v_item receipt_items%ROWTYPE;
	  v_total_value numeric := 0;
	  v_total_qty numeric := 0;
	  v_allocated numeric;
	  v_dist_count integer := 0;
	  v_caller_uid uuid := COALESCE(p_user_id, auth.uid());
	BEGIN
	  PERFORM pg_advisory_xact_lock(hashtext(p_service_id::text));
	
	  SELECT * INTO v_service FROM received_services WHERE id = p_service_id FOR UPDATE;
	  IF NOT FOUND THEN RAISE EXCEPTION 'ERR_SERVICE_NOT_FOUND'; END IF;
	
	  v_store_id := v_service.store_id;
	  v_status := v_service.status;
	  v_total_amount := v_service.total_amount;
	  v_method := v_service.distribution_method;
	
	  IF NOT public.has_store_access(v_store_id) THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED';
	  END IF;
	
	  IF v_status != 'active' THEN
	    RAISE EXCEPTION 'ERR_SERVICE_NOT_ACTIVE: status % is not active', v_status;
	  END IF;
	
	  IF v_method = 'manual' THEN
	    RAISE EXCEPTION 'ERR_MANUAL_METHOD: use link_receipts_to_service for manual distribution';
	  END IF;
	
	  DELETE FROM service_cost_distributions WHERE service_id = p_service_id;
	
	  FOR v_link IN SELECT * FROM service_reception_links WHERE service_id = p_service_id AND allocated_amount > 0 ORDER BY receipt_id LOOP
	    v_allocated := v_link.allocated_amount;
	
	    IF v_method = 'amount' THEN
	      SELECT COALESCE(SUM(quantity * unit_cost), 0) INTO v_total_value
	      FROM receipt_items WHERE receipt_id = v_link.receipt_id;
	      IF v_total_value > 0 THEN
	        FOR v_item IN SELECT * FROM receipt_items WHERE receipt_id = v_link.receipt_id ORDER BY id LOOP
	          INSERT INTO service_cost_distributions
	            (service_id, receipt_id, receipt_item_id, product_id, distribution_amount, distribution_percentage)
	          VALUES
	            (p_service_id, v_link.receipt_id, v_item.id, v_item.product_id,
	             v_allocated * (v_item.quantity * v_item.unit_cost / v_total_value),
	             (v_item.quantity * v_item.unit_cost / v_total_value) * 100);
	          v_dist_count := v_dist_count + 1;
	        END LOOP;
	      END IF;
	
	    ELSIF v_method = 'quantity' THEN
	      SELECT COALESCE(SUM(quantity), 0) INTO v_total_qty
	      FROM receipt_items WHERE receipt_id = v_link.receipt_id;
	      IF v_total_qty > 0 THEN
	        FOR v_item IN SELECT * FROM receipt_items WHERE receipt_id = v_link.receipt_id ORDER BY id LOOP
	          INSERT INTO service_cost_distributions
	            (service_id, receipt_id, receipt_item_id, product_id, distribution_amount, distribution_percentage)
	          VALUES
	            (p_service_id, v_link.receipt_id, v_item.id, v_item.product_id,
	             v_allocated * (v_item.quantity / v_total_qty),
	             (v_item.quantity / v_total_qty) * 100);
	          v_dist_count := v_dist_count + 1;
	        END LOOP;
	      END IF;
	    END IF;
	  END LOOP;
	
	  -- Forzar recalculo de WAC para cada producto afectado
	  UPDATE receipt_items SET updated_at = NOW()
	  WHERE product_id IN (SELECT DISTINCT product_id FROM service_cost_distributions WHERE service_id = p_service_id);
	
	  INSERT INTO audit_logs (user_id, store_id, action, table_name, record_id, metadata)
	  VALUES (v_caller_uid, v_store_id, 'SERVICE_DISTRIBUTED', 'received_services', p_service_id,
	    jsonb_build_object('rows_distributed', v_dist_count, 'method', v_method, 'total_amount', v_total_amount));
	
	  RETURN jsonb_build_object('status', 'success', 'distributed_rows', v_dist_count);
	END;
	$func$
2026-09-13 06:19:26.430 UTC [24766] ERROR:  type "user_role" does not exist at character 116
2026-09-13 06:19:26.430 UTC [24766] STATEMENT:  CREATE OR REPLACE FUNCTION public.enforce_encargado_store_limit()
	RETURNS TRIGGER AS $$
	DECLARE
	    v_creator_role user_role;
	    v_limit integer;
	    v_current_count integer;
	BEGIN
	    IF NEW.created_by IS NULL THEN RETURN NEW; END IF;
	
	    SELECT role, max_stores_limit INTO v_creator_role, v_limit
	    FROM public.profiles WHERE id = NEW.created_by;
	
	    IF v_creator_role = 'encargado' THEN
	        SELECT COUNT(*) INTO v_current_count FROM public.stores WHERE created_by = NEW.created_by;
	        IF v_current_count >= v_limit THEN
	            RAISE EXCEPTION 'ERR_STORE_LIMIT_EXCEEDED: Maximum number of stores reached for this manager (%/%)', v_current_count, v_limit;
	        END IF;
	    END IF;
	    RETURN NEW;
	END;
	$$ LANGUAGE plpgsql
2026-09-13 06:19:26.458 UTC [24771] ERROR:  type "user_role" does not exist at character 115
2026-09-13 06:19:26.458 UTC [24771] STATEMENT:  CREATE OR REPLACE FUNCTION public.enforce_encargado_user_limit()
	RETURNS TRIGGER AS $$
	DECLARE
	    v_creator_role user_role;
	    v_limit integer;
	    v_current_count integer;
	BEGIN
	    IF NEW.created_by IS NULL THEN RETURN NEW; END IF;
	
	    SELECT role, max_users_limit INTO v_creator_role, v_limit
	    FROM public.profiles WHERE id = NEW.created_by;
	
	    IF v_creator_role = 'encargado' THEN
	        SELECT COUNT(*) INTO v_current_count FROM public.profiles WHERE created_by = NEW.created_by;
	        IF v_current_count >= v_limit THEN
	            RAISE EXCEPTION 'ERR_USER_LIMIT_EXCEEDED: Maximum number of users reached for this manager (%/%)', v_current_count, v_limit;
	        END IF;
	    END IF;
	    RETURN NEW;
	END;
	$$ LANGUAGE plpgsql
2026-09-13 06:19:26.518 UTC [24786] ERROR:  syntax error at or near "REVOKE" at character 1436
2026-09-13 06:19:26.518 UTC [24786] STATEMENT:  CREATE OR REPLACE FUNCTION public.generate_bulk_confirmation_token(p_store_ids uuid[], p_action text, p_user_id uuid)
	 RETURNS text
	 LANGUAGE plpgsql
	 SECURITY DEFINER
	 SET search_path TO 'public'
	AS $function$
	DECLARE
	  v_token TEXT;
	  v_has_protected BOOLEAN;
	  v_caller_role TEXT;
	BEGIN
	  -- AUTH CHECK: Solo admin
	  IF auth.uid() IS NOT NULL THEN
	    SELECT role INTO v_caller_role FROM public.profiles WHERE id = auth.uid();
	    IF v_caller_role IS NULL OR v_caller_role != 'admin' THEN
	      RAISE EXCEPTION 'ERR_PERMISSION_DENIED: Solo admin puede generar tokens bulk';
	    END IF;
	    -- Verificar que p_user_id coincide con auth.uid()
	    IF p_user_id != auth.uid() THEN
	      RAISE EXCEPTION 'ERR_PERMISSION_DENIED: p_user_id debe coincidir con el usuario autenticado';
	    END IF;
	  END IF;
	
	  IF p_action NOT IN ('delete', 'archive') THEN
	    RAISE EXCEPTION 'ERR_NON_DESTRUCTIVE_ACTION';
	  END IF;
	
	  SELECT EXISTS(
	    SELECT 1 FROM public.stores
	    WHERE id = ANY(p_store_ids) AND backup_restore_protected = true
	  ) INTO v_has_protected;
	
	  v_token := 'bct_' || replace(gen_random_uuid()::text, '-', '');
	
	  INSERT INTO public.bulk_confirmation_tokens (
	    token, store_ids, action, created_by, expires_at, metadata
	  ) VALUES (
	    v_token, p_store_ids, p_action, p_user_id,
	    NOW() + INTERVAL '10 minutes',
	    jsonb_build_object('has_protected_stores', v_has_protected)
	  );
	
	  RETURN v_token;
	END;
	$function$
	 
	REVOKE EXECUTE ON FUNCTION public.generate_bulk_confirmation_token FROM PUBLIC, anon, authenticated
2026-09-13 06:19:26.540 UTC [24791] ERROR:  syntax error at or near "REVOKE" at character 2432
2026-09-13 06:19:26.540 UTC [24791] STATEMENT:  CREATE OR REPLACE FUNCTION public.generate_bulk_override_token(p_confirmation_token text, p_override_user_id uuid, p_reason text DEFAULT NULL::text)
	 RETURNS text
	 LANGUAGE plpgsql
	 SECURITY DEFINER
	 SET search_path TO 'public'
	AS $function$
	DECLARE
	  v_original RECORD;
	  v_override_token TEXT;
	  v_override_user_role TEXT;
	BEGIN
	  -- AUTH CHECK: Solo admin, y debe ser auth.uid() == p_override_user_id
	  IF auth.uid() IS NOT NULL THEN
	    IF auth.uid() != p_override_user_id THEN
	      RAISE EXCEPTION 'ERR_PERMISSION_DENIED: p_override_user_id debe coincidir con el usuario autenticado';
	    END IF;
	
	    SELECT role INTO v_override_user_role FROM public.profiles WHERE id = p_override_user_id;
	    IF v_override_user_role IS NULL OR v_override_user_role != 'admin' THEN
	      RAISE EXCEPTION 'ERR_PERMISSION_DENIED: Solo admin puede generar override';
	    END IF;
	  END IF;
	
	  SELECT * INTO v_original
	  FROM public.bulk_confirmation_tokens
	  WHERE token = p_confirmation_token
	    AND consumed_at IS NULL
	    AND expires_at > NOW()
	    AND is_override = false;
	
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_INVALID_OR_EXPIRED_TOKEN';
	  END IF;
	
	  IF v_original.created_by = p_override_user_id THEN
	    RAISE EXCEPTION 'ERR_SAME_USER_OVERRIDE';
	  END IF;
	
	  IF EXISTS(
	    SELECT 1 FROM public.bulk_confirmation_tokens
	    WHERE override_for = p_confirmation_token
	      AND consumed_at IS NULL AND expires_at > NOW()
	  ) THEN
	    RAISE EXCEPTION 'ERR_OVERRIDE_ALREADY_EXISTS';
	  END IF;
	
	  v_override_token := 'bot_' || replace(gen_random_uuid()::text, '-', '');
	
	  INSERT INTO public.bulk_confirmation_tokens (
	    token, store_ids, action, created_by, expires_at,
	    is_override, override_for, metadata
	  ) VALUES (
	    v_override_token, v_original.store_ids, v_original.action, p_override_user_id,
	    NOW() + INTERVAL '10 minutes', true, p_confirmation_token,
	    jsonb_build_object('override_reason', p_reason,
	      'original_created_by', v_original.created_by,
	      'override_created_by', p_override_user_id)
	  );
	
	  INSERT INTO public.audit_logs (action, table_name, record_id, metadata)
	  VALUES (
	    'bulk_override_token_generated', 'stores', NULL,
	    jsonb_build_object('confirmation_token', p_confirmation_token,
	      'store_ids', v_original.store_ids,
	      'override_by', p_override_user_id,
	      'original_by', v_original.created_by, 'reason', p_reason)
	  );
	
	  RETURN v_override_token;
	END;
	$function$
	 
	REVOKE EXECUTE ON FUNCTION public.generate_bulk_override_token FROM PUBLIC, anon, authenticated
2026-09-13 06:19:26.579 UTC [24801] ERROR:  relation "public.backup_table_registry" does not exist at character 581
2026-09-13 06:19:26.579 UTC [24801] STATEMENT:  CREATE OR REPLACE FUNCTION public.get_backup_table_list(
	  p_include_excluded BOOLEAN DEFAULT FALSE
	)
	RETURNS TABLE (
	  table_name          TEXT,
	  tier                INTEGER,
	  filter_strategy     TEXT,
	  parent_table        TEXT,
	  parent_foreign_key  TEXT,
	  date_column         TEXT,
	  excluded_from_restore BOOLEAN,
	  exclude_reason      TEXT
	)
	LANGUAGE sql
	SECURITY DEFINER
	SET search_path = public
	AS $$
	  SELECT
	    table_name,
	    tier,
	    filter_strategy,
	    parent_table,
	    parent_foreign_key,
	    date_column,
	    excluded_from_restore,
	    exclude_reason
	  FROM public.backup_table_registry
	  WHERE (p_include_excluded OR excluded_from_restore = FALSE)
	  ORDER BY tier ASC, table_name ASC;
	$$
2026-09-13 06:19:26.627 UTC [24811] ERROR:  relation "v_global_operation_dates" does not exist at character 229
2026-09-13 06:19:26.627 UTC [24811] STATEMENT:  CREATE OR REPLACE FUNCTION public.get_global_max_operation_date(
	  p_store_id UUID DEFAULT NULL
	)
	RETURNS TIMESTAMP WITH TIME ZONE
	LANGUAGE sql
	SECURITY DEFINER
	SET search_path = public
	AS $$
	  SELECT MAX(operation_date)
	  FROM v_global_operation_dates
	  WHERE p_store_id IS NULL OR store_id = p_store_id;
	$$
2026-09-13 06:19:26.653 UTC [24816] ERROR:  relation "exchange_rates" does not exist at character 216
2026-09-13 06:19:26.653 UTC [24816] STATEMENT:  CREATE OR REPLACE FUNCTION get_latest_rate(p_currency TEXT, p_source TEXT)
	RETURNS TABLE(rate NUMERIC, rate_date DATE, captured_at TIMESTAMPTZ)
	LANGUAGE sql STABLE AS $$
	  SELECT rate, rate_date, captured_at
	  FROM exchange_rates
	  WHERE currency = p_currency AND source = p_source
	  ORDER BY rate_date DESC, captured_at DESC
	  LIMIT 1;
	$$
2026-09-13 06:19:26.683 UTC [24821] ERROR:  relation "public.products" does not exist at character 194
2026-09-13 06:19:26.683 UTC [24821] STATEMENT:  CREATE OR REPLACE FUNCTION public.get_low_stock_count(p_store_id uuid DEFAULT NULL)
	RETURNS bigint
	LANGUAGE sql
	SECURITY DEFINER
	SET search_path = public
	AS $$
	  SELECT COUNT(*)::bigint
	  FROM public.products
	  WHERE
	    (p_store_id IS NULL OR store_id = p_store_id)
	    AND is_active = true
	    AND stock_current > 0
	    AND min_stock IS NOT NULL
	    AND min_stock > 0
	    AND stock_current <= min_stock;
	$$
2026-09-13 06:19:26.728 UTC [24830] ERROR:  relation "receipt_items" does not exist at character 754
2026-09-13 06:19:26.728 UTC [24830] STATEMENT:  CREATE OR REPLACE FUNCTION get_product_cost_analysis(
	  p_product_id UUID,
	  p_store_id UUID
	) RETURNS TABLE(
	  receipt_id UUID,
	  receipt_date TIMESTAMPTZ,
	  quantity INT,
	  unit_cost FLOAT,
	  service_type TEXT,
	  service_amount NUMERIC,
	  total_cost NUMERIC,
	  unit_cost_final NUMERIC
	) LANGUAGE sql SECURITY DEFINER AS $$
	  SELECT 
	    ri.receipt_id,
	    r.reception_date,
	    ri.quantity,
	    ri.unit_cost,
	    st.name AS service_type,
	    COALESCE(scd.distribution_amount, 0) AS service_amount,
	    ri.unit_cost * ri.quantity + COALESCE(scd.distribution_amount, 0) AS total_cost,
	    CASE WHEN ri.quantity > 0 
	      THEN (ri.unit_cost * ri.quantity + COALESCE(scd.distribution_amount, 0)) / ri.quantity 
	      ELSE 0 END AS unit_cost_final
	  FROM receipt_items ri
	  JOIN receipts r ON r.id = ri.receipt_id
	  LEFT JOIN service_cost_distributions scd ON scd.receipt_item_id = ri.id
	  LEFT JOIN received_services rs ON rs.id = scd.service_id AND rs.status = 'active'
	  LEFT JOIN service_types st ON st.id = rs.service_type_id
	  WHERE ri.product_id = p_product_id
	    AND r.store_id = p_store_id
	    AND r.status = 'active'
	  ORDER BY r.reception_date DESC;
	$$
2026-09-13 06:19:26.761 UTC [24838] ERROR:  relation "exchange_rates" does not exist at character 252
2026-09-13 06:19:26.761 UTC [24838] STATEMENT:  CREATE OR REPLACE FUNCTION get_rate_history(
	  p_currency TEXT,
	  p_source TEXT,
	  p_days INT DEFAULT 30
	)
	RETURNS TABLE(rate_date DATE, rate NUMERIC, variation_daily NUMERIC)
	LANGUAGE sql STABLE AS $$
	  SELECT rate_date, rate, variation_daily
	  FROM exchange_rates
	  WHERE currency = p_currency AND source = p_source
	    AND rate_date >= CURRENT_DATE - p_days
	  ORDER BY rate_date ASC;
	$$
2026-09-13 06:19:26.799 UTC [24843] ERROR:  relation "exchange_rates" does not exist at character 363
2026-09-13 06:19:26.799 UTC [24843] STATEMENT:  CREATE OR REPLACE FUNCTION get_rate_variation(
	  p_currency TEXT,
	  p_source TEXT,
	  p_start_date DATE,
	  p_end_date DATE
	)
	RETURNS TABLE(
	  start_rate NUMERIC,
	  end_rate NUMERIC,
	  absolute_change NUMERIC,
	  percent_change NUMERIC,
	  daily_avg_growth NUMERIC,
	  monthly_avg_growth NUMERIC
	)
	LANGUAGE sql STABLE AS $$
	  WITH start_val AS (
	    SELECT rate FROM exchange_rates
	    WHERE currency = p_currency AND source = p_source AND rate_date <= p_start_date
	    ORDER BY rate_date DESC LIMIT 1
	  ),
	  end_val AS (
	    SELECT rate FROM exchange_rates
	    WHERE currency = p_currency AND source = p_source AND rate_date <= p_end_date
	    ORDER BY rate_date DESC LIMIT 1
	  )
	  SELECT
	    COALESCE(sv.rate, 0) AS start_rate,
	    COALESCE(ev.rate, 0) AS end_rate,
	    (COALESCE(ev.rate, 0) - COALESCE(sv.rate, 0)) AS absolute_change,
	    CASE WHEN sv.rate IS NOT NULL AND sv.rate > 0 THEN ((ev.rate - sv.rate) / sv.rate * 100) ELSE 0 END AS percent_change,
	    CASE WHEN sv.rate IS NOT NULL AND sv.rate > 0 AND ev.rate IS NOT NULL AND ev.rate > 0 AND p_end_date > p_start_date
	      THEN (POWER(ev.rate::FLOAT / sv.rate::FLOAT, 1.0 / EXTRACT(EPOCH FROM (p_end_date - p_start_date)) * 86400) - 1) * 100
	      ELSE 0 END AS daily_avg_growth,
	    CASE WHEN sv.rate IS NOT NULL AND sv.rate > 0 AND ev.rate IS NOT NULL AND ev.rate > 0 AND p_end_date > p_start_date
	      THEN (POWER(ev.rate::FLOAT / sv.rate::FLOAT, 30.44 / EXTRACT(EPOCH FROM (p_end_date - p_start_date)) * 86400) - 1) * 100
	      ELSE 0 END AS monthly_avg_growth
	  FROM start_val sv, end_val ev;
	$$
2026-09-13 06:19:26.854 UTC [24857] ERROR:  type "public.stores" does not exist
2026-09-13 06:19:26.854 UTC [24857] STATEMENT:  CREATE OR REPLACE FUNCTION public.get_transferable_stores(
	  p_user_id UUID,
	  p_current_store_id UUID
	)
	RETURNS SETOF public.stores
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO 'public'
	AS $$
	DECLARE
	  v_tenant_id UUID;
	BEGIN
	  -- 1. Obtener tenant_id de la tienda actual
	  SELECT tenant_id INTO v_tenant_id
	  FROM public.stores
	  WHERE id = p_current_store_id AND is_active = true;
	
	  -- 2. Devolver tiendas donde el caller tiene acceso (has_store_access_as)
	  --    EXCLUYENDO la tienda actual
	  --    Si tenant_id es NOT NULL, filtrar por mismo tenant.
	  --    Si tenant_id es NULL (legacy), mostrar todas las que el caller tiene acceso.
	  IF v_tenant_id IS NOT NULL THEN
	    RETURN QUERY
	    SELECT s.*
	    FROM public.stores s
	    WHERE s.tenant_id = v_tenant_id
	      AND s.id != p_current_store_id
	      AND s.is_active = true
	      AND s.is_archived = false
	      AND public.has_store_access_as(p_user_id, s.id)
	    ORDER BY s.name;
	  ELSE
	    -- Legacy: tiendas sin tenant_id — usar solo has_store_access_as
	    RETURN QUERY
	    SELECT s.*
	    FROM public.stores s
	    WHERE s.id != p_current_store_id
	      AND s.is_active = true
	      AND s.is_archived = false
	      AND public.has_store_access_as(p_user_id, s.id)
	    ORDER BY s.name;
	  END IF;
	END;
	$$
2026-09-13 06:19:26.899 UTC [24868] ERROR:  type user_role[] does not exist
2026-09-13 06:19:26.899 UTC [24868] STATEMENT:  CREATE OR REPLACE FUNCTION public.has_any_role(required_roles user_role[])
	 RETURNS boolean
	 LANGUAGE plpgsql
	 STABLE SECURITY DEFINER
	AS $$
	DECLARE
	    v_actual_role user_role;
	    r user_role;
	BEGIN
	    SELECT role INTO v_actual_role FROM public.profiles WHERE id = auth.uid();
	
	    FOREACH r IN ARRAY required_roles
	    LOOP
	        IF v_actual_role = r THEN RETURN true; END IF;
	        IF v_actual_role = 'encargado' AND r = 'manager' THEN RETURN true; END IF;
	        IF v_actual_role = 'usuario' AND (r = 'clerk' OR r = 'warehouse') THEN RETURN true; END IF;
	    END LOOP;
	
	    RETURN false;
	END;
	$$
2026-09-13 06:19:26.923 UTC [24873] ERROR:  relation "profiles" does not exist at character 461
2026-09-13 06:19:26.923 UTC [24873] STATEMENT:  CREATE OR REPLACE FUNCTION public.has_management_access_as(
	  p_user_id uuid,
	  p_store_id uuid
	) RETURNS boolean
	LANGUAGE sql SECURITY DEFINER
	SET search_path = public, pg_temp
	AS $$
	  -- service_role always passes
	  SELECT CASE WHEN auth.role() = 'service_role' THEN true
	    ELSE
	      -- Check if user has admin/manager/encargado role in their profile
	      -- OR has a membership with manager/admin role for the store
	      EXISTS (
	        SELECT 1 FROM profiles
	        WHERE id = p_user_id
	        AND role IN ('admin', 'manager', 'encargado')
	      )
	      OR EXISTS (
	        SELECT 1 FROM user_store_memberships
	        WHERE user_id = p_user_id
	        AND store_id = p_store_id
	        AND status = 'active'
	        AND role IN ('admin', 'manager')
	      )
	    END
	$$
2026-09-13 06:19:26.945 UTC [24878] ERROR:  type public.user_role does not exist
2026-09-13 06:19:26.945 UTC [24878] STATEMENT:  CREATE OR REPLACE FUNCTION public.has_role(p_user_id UUID, p_required_role public.user_role)
	 RETURNS boolean
	 LANGUAGE plpgsql
	 STABLE SECURITY DEFINER
	AS $$
	DECLARE
	    v_actual_role public.user_role;
	BEGIN
	    SELECT role INTO v_actual_role FROM public.profiles WHERE id = p_user_id;
	
	    IF v_actual_role IS NULL THEN
	        RETURN false;
	    END IF;
	
	    -- Compatibility mapping (mirroring the 1-arg version)
	    IF v_actual_role = 'encargado' AND p_required_role = 'manager' THEN RETURN true; END IF;
	    IF v_actual_role = 'usuario' AND (p_required_role = 'clerk' OR p_required_role = 'warehouse') THEN RETURN true; END IF;
	
	    RETURN v_actual_role = p_required_role;
	END;
	$$
2026-09-13 06:19:26.975 UTC [24885] ERROR:  relation "public.user_store_memberships" does not exist at character 279
2026-09-13 06:19:26.975 UTC [24885] STATEMENT:  CREATE OR REPLACE FUNCTION public.has_store_role(
	  p_user_id uuid,
	  p_store_id uuid,
	  p_roles text[]
	) RETURNS boolean
	LANGUAGE sql SECURITY DEFINER
	SET search_path = public, pg_temp
	AS $$
	  SELECT CASE WHEN auth.role() = 'service_role' THEN
	    EXISTS (
	      SELECT 1 FROM public.user_store_memberships m
	      WHERE m.user_id = p_user_id
	        AND m.store_id = p_store_id
	        AND m.status = 'active'
	        AND m.role::text = ANY(p_roles)
	    )
	  ELSE
	    EXISTS (
	      SELECT 1 FROM public.user_store_memberships m
	      WHERE m.user_id = auth.uid()
	        AND m.store_id = p_store_id
	        AND m.status = 'active'
	        AND m.role::text = ANY(p_roles)
	    )
	  END
	$$
2026-09-13 06:19:27.009 UTC [24893] ERROR:  relation "public.profiles" does not exist at character 173
2026-09-13 06:19:27.009 UTC [24893] STATEMENT:  CREATE OR REPLACE FUNCTION public.is_global_admin()
	RETURNS BOOLEAN
	LANGUAGE sql
	STABLE
	SECURITY DEFINER
	SET search_path = public
	AS $$
	  SELECT EXISTS (
	    SELECT 1 FROM public.profiles
	    WHERE id = auth.uid()
	      AND role = 'admin'
	  );
	$$
2026-09-13 06:19:27.036 UTC [24899] ERROR:  type user_role does not exist
2026-09-13 06:19:27.036 UTC [24899] STATEMENT:  CREATE OR REPLACE FUNCTION public.is_role_not_changed(p_user_id uuid, p_new_role user_role, p_new_role_id uuid)
	RETURNS boolean
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path = public, extensions
	AS $$
	DECLARE
	  v_old_role user_role;
	  v_old_role_id uuid;
	BEGIN
	  SELECT role, role_id INTO v_old_role, v_old_role_id
	  FROM public.profiles
	  WHERE id = p_user_id;
	
	  RETURN (p_new_role IS NOT DISTINCT FROM v_old_role)
	     AND (p_new_role_id IS NOT DISTINCT FROM v_old_role_id);
	END;
	$$
2026-09-13 06:19:27.061 UTC [24905] ERROR:  relation "public.user_store_memberships" does not exist at character 188
2026-09-13 06:19:27.061 UTC [24905] STATEMENT:  CREATE OR REPLACE FUNCTION public.is_store_member(p_store_id UUID)
	RETURNS BOOLEAN
	LANGUAGE sql
	STABLE
	SECURITY DEFINER
	SET search_path = public
	AS $$
	  SELECT EXISTS (
	    SELECT 1 FROM public.user_store_memberships
	    WHERE store_id = p_store_id
	      AND user_id = auth.uid()
	      AND status = 'active'
	  );
	$$
2026-09-13 06:19:27.084 UTC [24910] ERROR:  function public.current_user_tenant_id() does not exist at character 209
2026-09-13 06:19:27.084 UTC [24910] HINT:  No function matches the given name and argument types. You might need to add explicit type casts.
2026-09-13 06:19:27.084 UTC [24910] STATEMENT:  CREATE OR REPLACE FUNCTION public.is_tenant_member(p_tenant_id uuid)
	RETURNS boolean
	LANGUAGE sql
	STABLE
	SECURITY DEFINER
	SET search_path TO public
	AS $$
	  SELECT p_tenant_id IS NOT NULL
	   AND p_tenant_id = public.current_user_tenant_id();
	$$
2026-09-13 06:19:27.136 UTC [24922] ERROR:  type user_role does not exist
2026-09-13 06:19:27.136 UTC [24922] STATEMENT:  CREATE OR REPLACE FUNCTION public.managed_create_user(p_email text, p_full_name text, p_role user_role, p_store_id uuid DEFAULT NULL::uuid, p_memberships jsonb DEFAULT NULL::jsonb, p_max_stores integer DEFAULT 0, p_max_users integer DEFAULT 0, p_target_user_id uuid DEFAULT NULL::uuid)
	 RETURNS jsonb
	 LANGUAGE plpgsql
	 SECURITY DEFINER
	 SET search_path TO 'public', 'extensions'
	AS $function$
	DECLARE
	    v_user_id uuid;
	    v_role_id uuid;
	    v_active_store_id uuid;
	    m JSONB;
	    v_creator_role user_role;
	BEGIN
	    SELECT role INTO v_creator_role FROM public.profiles WHERE id = auth.uid();
	    IF v_creator_role IS NULL OR v_creator_role NOT IN ('admin', 'encargado') THEN
	        RAISE EXCEPTION 'ERR_UNAUTHORIZED: Only admins and managers can create users.';
	    END IF;
	
	    v_active_store_id := COALESCE((p_memberships->0->>'store_id')::UUID, p_store_id);
	
	    IF v_creator_role = 'encargado' AND v_active_store_id IS NOT NULL THEN
	        IF NOT public.has_store_access(v_active_store_id) THEN
	            RAISE EXCEPTION 'ERR_UNAUTHORIZED: No access to store %', v_active_store_id;
	        END IF;
	    END IF;
	
	    SELECT id INTO v_role_id FROM public.roles WHERE lower(name) = lower(p_role::text) OR (name = 'Cajero' AND p_role = 'clerk') OR (name = 'Almacenero' AND p_role = 'warehouse') LIMIT 1;
	    v_user_id := COALESCE(p_target_user_id, gen_random_uuid());
	
	    INSERT INTO public.profiles (id, email, full_name, role, role_id, active_store_id, is_active, created_by, max_stores_limit, max_users_limit, created_at, updated_at)
	    VALUES (v_user_id, p_email, p_full_name, p_role, v_role_id, v_active_store_id, true, auth.uid(), p_max_stores, p_max_users, now(), now())
	    ON CONFLICT (id) DO UPDATE SET email = EXCLUDED.email, full_name = EXCLUDED.full_name, role = EXCLUDED.role, role_id = EXCLUDED.role_id, active_store_id = EXCLUDED.active_store_id, updated_at = now()
	    RETURNING id INTO v_user_id;
	
	    IF p_memberships IS NOT NULL THEN
	        IF v_creator_role != 'admin' THEN
	            DELETE FROM public.user_store_memberships WHERE user_id = v_user_id AND store_id IN (SELECT store_id FROM public.user_store_memberships WHERE user_id = auth.uid() AND role IN ('encargado', 'manager'));
	        ELSE
	            DELETE FROM public.user_store_memberships WHERE user_id = v_user_id;
	        END IF;
	        FOR m IN SELECT * FROM jsonb_array_elements(p_memberships) LOOP
	            IF (m->>'store_id') IS NOT NULL AND (m->>'store_id') <> '' THEN
	                IF v_creator_role = 'admin' OR public.has_store_access((m->>'store_id')::UUID) THEN
	                    INSERT INTO public.user_store_memberships (user_id, store_id, role) VALUES (v_user_id, (m->>'store_id')::UUID, (m->>'role')::user_role) ON CONFLICT (user_id, store_id) DO UPDATE SET role = EXCLUDED.role;
	                END IF;
	            END IF;
	        END LOOP;
	    ELSIF p_store_id IS NOT NULL THEN
	        INSERT INTO public.user_store_memberships (user_id, store_id, role) VALUES (v_user_id, p_store_id, p_role) ON CONFLICT (user_id, store_id) DO UPDATE SET role = EXCLUDED.role;
	    END IF;
	    RETURN jsonb_build_object('success', true, 'user_id', v_user_id);
	END;
	$function$
2026-09-13 06:19:27.159 UTC [24927] ERROR:  type public.user_role does not exist
2026-09-13 06:19:27.159 UTC [24927] STATEMENT:  CREATE OR REPLACE FUNCTION public.managed_create_user_v2(
	  p_email text,
	  p_full_name text,
	  p_role public.user_role,
	  p_plan plan_t DEFAULT 'free'::plan_t,
	  p_store_id uuid DEFAULT NULL::uuid,
	  p_memberships jsonb DEFAULT NULL::jsonb,
	  p_max_stores int DEFAULT 0,
	  p_max_users int DEFAULT 0,
	  p_target_user_id uuid DEFAULT NULL::uuid,
	  p_creator_id uuid DEFAULT NULL::uuid
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  v_user_id uuid := COALESCE(p_target_user_id, gen_random_uuid());
	  v_role_id uuid;
	  v_active_store_id uuid;
	  m JSONB;
	  v_creator_role public.user_role;
	  v_creator_uid uuid := COALESCE(p_creator_id, auth.uid());
	BEGIN
	  -- Validar caller es admin o encargado
	  SELECT role INTO v_creator_role FROM public.profiles WHERE id = v_creator_uid;
	  IF v_creator_role IS NULL OR v_creator_role NOT IN ('admin', 'encargado', 'superadmin') THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED: Only admins and managers can create users.';
	  END IF;
	
	  -- Validar email único (entre perfiles activos)
	  IF EXISTS (SELECT 1 FROM public.profiles WHERE email = p_email AND deleted_at IS NULL) THEN
	    RAISE EXCEPTION 'ERR_EMAIL_ALREADY_EXISTS: %', p_email;
	  END IF;
	
	  v_active_store_id := COALESCE((p_memberships->0->>'store_id')::UUID, p_store_id);
	
	  -- Encargado solo puede crear en tiendas que gestiona
	  IF v_creator_role = 'encargado' AND v_active_store_id IS NOT NULL THEN
	    IF NOT public.has_store_access(v_active_store_id) THEN
	      RAISE EXCEPTION 'ERR_UNAUTHORIZED: No access to store %', v_active_store_id;
	    END IF;
	  END IF;
	
	  -- Buscar role_id
	  SELECT id INTO v_role_id FROM public.roles
	    WHERE lower(name) = lower(p_role::text)
	       OR (name = 'Cajero' AND p_role = 'clerk')
	       OR (name = 'Almacenero' AND p_role = 'warehouse')
	       OR (name = 'UserCosto' AND p_role = 'costo')
	    LIMIT 1;
	
	  -- INSERT profiles con plan
	  INSERT INTO public.profiles (
	    id, email, full_name, role, role_id, active_store_id, is_active,
	    created_by, max_stores_limit, max_users_limit, plan, created_at, updated_at
	  ) VALUES (
	    v_user_id, p_email, p_full_name, p_role, v_role_id, v_active_store_id, true,
	    v_creator_uid, p_max_stores, p_max_users, p_plan, now(), now()
	  )
	  ON CONFLICT (id) DO UPDATE SET
	    email = EXCLUDED.email,
	    full_name = EXCLUDED.full_name,
	    role = EXCLUDED.role,
	    role_id = EXCLUDED.role_id,
	    active_store_id = EXCLUDED.active_store_id,
	    plan = EXCLUDED.plan,
	    is_active = true,
	    deleted_at = NULL,
	    deletion_reason = NULL,
	    deleted_by = NULL,
	    updated_at = now()
	  RETURNING id INTO v_user_id;
	
	  -- Procesar memberships
	  IF p_memberships IS NOT NULL THEN
	    IF v_creator_role != 'admin' AND v_creator_role != 'superadmin' THEN
	      DELETE FROM public.user_store_memberships
	        WHERE user_id = v_user_id
	        AND store_id IN (
	          SELECT store_id FROM public.user_store_memberships
	          WHERE user_id = v_creator_uid AND role IN ('encargado', 'manager')
	        );
	    ELSE
	      DELETE FROM public.user_store_memberships WHERE user_id = v_user_id;
	    END IF;
	
	    FOR m IN SELECT * FROM jsonb_array_elements(p_memberships) LOOP
	      IF (m->>'store_id') IS NOT NULL AND (m->>'store_id') <> '' THEN
	        IF v_creator_role IN ('admin', 'superadmin') OR public.has_store_access((m->>'store_id')::UUID) THEN
	          INSERT INTO public.user_store_memberships (user_id, store_id, role)
	          VALUES (v_user_id, (m->>'store_id')::UUID, (m->>'role')::public.user_role)
	          ON CONFLICT (user_id, store_id) DO UPDATE SET role = EXCLUDED.role, status = 'active';
	        END IF;
	      END IF;
	    END LOOP;
	  ELSIF p_store_id IS NOT NULL THEN
	    INSERT INTO public.user_store_memberships (user_id, store_id, role)
	    VALUES (v_user_id, p_store_id, p_role)
	    ON CONFLICT (user_id, store_id) DO UPDATE SET role = EXCLUDED.role, status = 'active';
	  END IF;
	
	  -- Audit log atómico (regresión C-2 restaurada)
	  INSERT INTO public.user_audit_log (performed_by, target_user_id, action, new_values, metadata)
	  VALUES (
	    v_creator_uid, v_user_id, 'USER_CREATED',
	    jsonb_build_object(
	      'email', p_email, 'full_name', p_full_name, 'role', p_role::text,
	      'plan', p_plan::text, 'active_store_id', v_active_store_id,
	      'max_stores', p_max_stores, 'max_users', p_max_users
	    ),
	    jsonb_build_object('memberships_count', CASE WHEN p_memberships IS NOT NULL THEN jsonb_array_length(p_memberships) ELSE 0 END)
	  );
	
	  RETURN jsonb_build_object('success', true, 'user_id', v_user_id);
	END;
	$function$
2026-09-13 06:19:27.198 UTC [24934] ERROR:  type "public.user_role" does not exist at character 392
2026-09-13 06:19:27.198 UTC [24934] STATEMENT:  CREATE OR REPLACE FUNCTION public.managed_reset_password(
	  p_user_id uuid,
	  p_caller_id uuid DEFAULT NULL::uuid
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  -- FIX H-7: anti-spoofing
	  v_caller_uid uuid := CASE WHEN auth.role() = 'service_role'
	    THEN COALESCE(p_caller_id, auth.uid()) ELSE auth.uid() END;
	  v_caller_role public.user_role;
	  v_target_email text;
	BEGIN
	  SELECT role INTO v_caller_role FROM public.profiles WHERE id = v_caller_uid;
	  IF v_caller_role IS NULL OR v_caller_role NOT IN ('admin', 'superadmin') THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED';
	  END IF;
	
	  IF p_user_id = v_caller_uid THEN
	    RAISE EXCEPTION 'ERR_SELF_RESET_BLOCKED';
	  END IF;
	
	  SELECT email INTO v_target_email FROM public.profiles WHERE id = p_user_id AND deleted_at IS NULL;
	  IF v_target_email IS NULL THEN
	    RAISE EXCEPTION 'ERR_USER_NOT_FOUND';
	  END IF;
	
	  INSERT INTO public.user_audit_log (performed_by, target_user_id, action, metadata)
	  VALUES (
	    v_caller_uid, p_user_id, 'PASSWORD_RESET_REQUESTED',
	    jsonb_build_object('email', v_target_email, 'method', 'recovery_link')
	  );
	
	  RETURN jsonb_build_object('success', true, 'email', v_target_email);
	END;
	$function$
2026-09-13 06:19:27.234 UTC [24940] ERROR:  type "public.user_role" does not exist at character 427
2026-09-13 06:19:27.234 UTC [24940] STATEMENT:  CREATE OR REPLACE FUNCTION public.managed_soft_delete_user(
	  p_user_id uuid,
	  p_reason text,
	  p_caller_id uuid DEFAULT NULL::uuid
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  v_old RECORD;
	  -- FIX H-7: anti-spoofing
	  v_caller_uid uuid := CASE WHEN auth.role() = 'service_role'
	    THEN COALESCE(p_caller_id, auth.uid()) ELSE auth.uid() END;
	  v_caller_role public.user_role;
	  v_active_memberships_count int;
	  v_anon_email text;
	BEGIN
	  SELECT role INTO v_caller_role FROM public.profiles WHERE id = v_caller_uid;
	  IF v_caller_role IS NULL OR v_caller_role NOT IN ('admin', 'superadmin') THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED';
	  END IF;
	
	  IF p_user_id = v_caller_uid THEN
	    RAISE EXCEPTION 'ERR_SELF_DELETE_BLOCKED';
	  END IF;
	
	  SELECT id, email, full_name, role, plan, is_active INTO v_old
	    FROM public.profiles WHERE id = p_user_id AND deleted_at IS NULL FOR UPDATE;
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_USER_NOT_FOUND_OR_ALREADY_DELETED';
	  END IF;
	
	  SELECT COUNT(*) INTO v_active_memberships_count
	    FROM public.user_store_memberships
	    WHERE user_id = p_user_id AND status = 'active';
	  IF v_active_memberships_count > 0 THEN
	    RAISE EXCEPTION 'ERR_USER_HAS_ACTIVE_MEMBERSHIPS: % active. Revoke memberships first.', v_active_memberships_count;
	  END IF;
	
	  v_anon_email := 'deleted+' || substr(p_user_id::text, 1, 8) || '@anonymized.local';
	
	  UPDATE public.profiles SET
	    deleted_at = now(),
	    deletion_reason = p_reason,
	    deleted_by = v_caller_uid,
	    is_active = false,
	    full_name = '[deleted user]',
	    email = v_anon_email,
	    ai_api_key = NULL,
	    updated_at = now()
	  WHERE id = p_user_id;
	
	  UPDATE public.user_store_memberships SET
	    status = 'revoked',
	    updated_at = now()
	  WHERE user_id = p_user_id AND status = 'active';
	
	  INSERT INTO public.user_audit_log (performed_by, target_user_id, action, old_values, new_values, metadata)
	  VALUES (
	    v_caller_uid, p_user_id, 'USER_SOFT_DELETED',
	    jsonb_build_object(
	      'email', v_old.email, 'full_name', v_old.full_name,
	      'role', v_old.role::text, 'plan', v_old.plan::text, 'is_active', v_old.is_active
	    ),
	    jsonb_build_object(
	      'email', v_anon_email, 'full_name', '[deleted user]',
	      'is_active', false, 'deleted_at', now()
	    ),
	    jsonb_build_object('reason', p_reason, 'memberships_revoked', v_active_memberships_count)
	  );
	
	  RETURN jsonb_build_object(
	    'success', true,
	    'user_id', p_user_id,
	    'status', 'soft_deleted',
	    'note', 'auth.users preserved. API route should ban via auth.admin.updateUser.'
	  );
	END;
	$function$
2026-09-13 06:19:27.266 UTC [24946] ERROR:  type "public.user_role" does not exist at character 435
2026-09-13 06:19:27.266 UTC [24946] STATEMENT:  CREATE OR REPLACE FUNCTION public.managed_toggle_user_status(
	  p_user_id uuid,
	  p_is_active boolean,
	  p_caller_id uuid DEFAULT NULL::uuid
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  v_old RECORD;
	  -- FIX H-7: anti-spoofing
	  v_caller_uid uuid := CASE WHEN auth.role() = 'service_role'
	    THEN COALESCE(p_caller_id, auth.uid()) ELSE auth.uid() END;
	  v_caller_role public.user_role;
	BEGIN
	  SELECT role INTO v_caller_role FROM public.profiles WHERE id = v_caller_uid;
	  IF v_caller_role IS NULL OR v_caller_role NOT IN ('admin', 'superadmin', 'encargado', 'manager') THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED';
	  END IF;
	
	  IF p_is_active = false AND p_user_id = v_caller_uid THEN
	    RAISE EXCEPTION 'ERR_SELF_DEACTIVATE_BLOCKED';
	  END IF;
	
	  SELECT id, is_active, full_name INTO v_old
	    FROM public.profiles WHERE id = p_user_id AND deleted_at IS NULL FOR UPDATE;
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_USER_NOT_FOUND';
	  END IF;
	
	  IF v_old.is_active = p_is_active THEN
	    RETURN jsonb_build_object('success', true, 'no_change', true);
	  END IF;
	
	  UPDATE public.profiles SET is_active = p_is_active, updated_at = now() WHERE id = p_user_id;
	
	  INSERT INTO public.user_audit_log (performed_by, target_user_id, action, old_values, new_values)
	  VALUES (
	    v_caller_uid, p_user_id,
	    CASE WHEN p_is_active THEN 'USER_ACTIVATED' ELSE 'USER_DEACTIVATED' END,
	    jsonb_build_object('is_active', v_old.is_active),
	    jsonb_build_object('is_active', p_is_active)
	  );
	
	  RETURN jsonb_build_object('success', true, 'user_id', p_user_id, 'is_active', p_is_active);
	END;
	$function$
2026-09-13 06:19:27.308 UTC [24951] ERROR:  type public.user_role does not exist
2026-09-13 06:19:27.308 UTC [24951] STATEMENT:  CREATE OR REPLACE FUNCTION public.managed_update_membership(
	  p_membership_id uuid,
	  p_role public.user_role DEFAULT NULL::user_role,
	  p_status text DEFAULT NULL::text,
	  p_caller_id uuid DEFAULT NULL::uuid
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  v_old RECORD;
	  -- FIX H-7: anti-spoofing
	  v_caller_uid uuid := CASE WHEN auth.role() = 'service_role'
	    THEN COALESCE(p_caller_id, auth.uid()) ELSE auth.uid() END;
	  v_changes jsonb := '{}'::jsonb;
	BEGIN
	  SELECT m.id, m.user_id, m.store_id, m.role, m.status
	    INTO v_old
	    FROM public.user_store_memberships m
	    WHERE m.id = p_membership_id
	    FOR UPDATE;
	
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_MEMBERSHIP_NOT_FOUND';
	  END IF;
	
	  -- FIX H-7: Autorización usa nueva sobrecarga has_store_role(p_user_id, p_store_id, p_roles)
	  -- service_role (API route) pasa p_caller_id=session.user.id → verifica acceso de ese user
	  -- authenticated pasa p_caller_id=NULL → v_caller_uid=auth.uid() → verifica su propio acceso
	  IF NOT public.has_store_role(v_caller_uid, v_old.store_id, ARRAY['admin', 'manager']) THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED: Caller must be admin or manager of the store.';
	  END IF;
	
	  IF p_role IS NOT NULL AND p_role <> v_old.role THEN
	    v_changes := v_changes || jsonb_build_object('role', jsonb_build_object('old', v_old.role::text, 'new', p_role::text));
	    UPDATE public.user_store_memberships SET role = p_role, updated_at = now() WHERE id = p_membership_id;
	  END IF;
	
	  IF p_status IS NOT NULL AND p_status <> v_old.status::text THEN
	    IF p_status NOT IN ('active', 'revoked') THEN
	      RAISE EXCEPTION 'ERR_INVALID_STATUS: %', p_status;
	    END IF;
	    v_changes := v_changes || jsonb_build_object('status', jsonb_build_object('old', v_old.status, 'new', p_status));
	    UPDATE public.user_store_memberships SET status = p_status::membership_status, updated_at = now() WHERE id = p_membership_id;
	  END IF;
	
	  IF v_changes <> '{}'::jsonb THEN
	    INSERT INTO public.user_audit_log (performed_by, target_user_id, action, old_values, new_values, metadata)
	    VALUES (
	      v_caller_uid, v_old.user_id, 'MEMBERSHIP_UPDATED',
	      jsonb_build_object('membership_id', p_membership_id, 'store_id', v_old.store_id, 'role', v_old.role::text, 'status', v_old.status),
	      v_changes,
	      jsonb_build_object('store_id', v_old.store_id)
	    );
	  END IF;
	
	  RETURN jsonb_build_object('success', true, 'membership_id', p_membership_id, 'changes', v_changes);
	END;
	$function$
2026-09-13 06:19:27.336 UTC [24956] ERROR:  type plan_t does not exist
2026-09-13 06:19:27.336 UTC [24956] STATEMENT:  CREATE OR REPLACE FUNCTION public.managed_update_tenant_plan(
	  p_tenant_id uuid,
	  p_plan plan_t,
	  p_subscription_status text DEFAULT NULL,
	  p_caller_id uuid DEFAULT NULL
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  -- FIX H-7: anti-spoofing — service_role (Stripe webhook) puede pasar NULL caller_id
	  v_caller_uid uuid := CASE WHEN auth.role() = 'service_role'
	    THEN COALESCE(p_caller_id, auth.uid()) ELSE auth.uid() END;
	  v_old_plan plan_t;
	  v_old_status text;
	  v_role public.user_role;
	BEGIN
	  -- Validación: si caller_uid es NULL (service_role anónimo para Stripe webhook), permitir
	  -- Si caller_uid NO es NULL, debe ser admin/superadmin
	  IF v_caller_uid IS NOT NULL THEN
	    SELECT role INTO v_role FROM public.profiles WHERE id = v_caller_uid;
	    IF v_role IS NULL OR v_role NOT IN ('admin', 'superadmin') THEN
	      RAISE EXCEPTION 'ERR_UNAUTHORIZED: Only admins can change tenant plan';
	    END IF;
	  END IF;
	
	  SELECT plan, subscription_status INTO v_old_plan, v_old_status
	    FROM public.tenants WHERE id = p_tenant_id FOR UPDATE;
	
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_TENANT_NOT_FOUND: %', p_tenant_id;
	  END IF;
	
	  UPDATE public.tenants SET
	    plan = p_plan,
	    subscription_status = COALESCE(p_subscription_status, subscription_status),
	    updated_at = now()
	  WHERE id = p_tenant_id;
	
	  UPDATE public.profiles SET
	    plan = p_plan,
	    updated_at = now()
	  WHERE tenant_id = p_tenant_id AND deleted_at IS NULL;
	
	  INSERT INTO public.user_audit_log (performed_by, target_user_id, action, old_values, new_values, metadata)
	  VALUES (
	    v_caller_uid, NULL, 'TENANT_PLAN_UPDATED',
	    jsonb_build_object('old_plan', v_old_plan::text, 'old_status', v_old_status),
	    jsonb_build_object('new_plan', p_plan::text, 'new_status', COALESCE(p_subscription_status, v_old_status)),
	    jsonb_build_object('tenant_id', p_tenant_id)
	  );
	
	  RETURN jsonb_build_object(
	    'success', true,
	    'tenant_id', p_tenant_id,
	    'old_plan', v_old_plan::text,
	    'new_plan', p_plan::text
	  );
	END;
	$function$
2026-09-13 06:19:27.358 UTC [24961] ERROR:  type public.user_role does not exist
2026-09-13 06:19:27.358 UTC [24961] STATEMENT:  CREATE OR REPLACE FUNCTION public.managed_update_user(
	  p_user_id uuid,
	  p_full_name text DEFAULT NULL::text,
	  p_role public.user_role DEFAULT NULL::user_role,
	  p_role_id uuid DEFAULT NULL::uuid,
	  p_is_active boolean DEFAULT NULL::boolean,
	  p_max_stores_limit int DEFAULT NULL::int,
	  p_max_users_limit int DEFAULT NULL::int,
	  p_plan plan_t DEFAULT NULL::plan_t,
	  p_caller_id uuid DEFAULT NULL::uuid
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  v_old RECORD;
	  -- FIX H-7: anti-spoofing — service_role puede pasar p_caller_id explícito
	  v_caller_uid uuid := CASE WHEN auth.role() = 'service_role'
	    THEN COALESCE(p_caller_id, auth.uid()) ELSE auth.uid() END;
	  v_caller_role public.user_role;
	  v_changes jsonb := '{}'::jsonb;
	BEGIN
	  -- Validar caller es admin (usa v_caller_uid que ahora NO es spoofable para authenticated)
	  SELECT role INTO v_caller_role FROM public.profiles WHERE id = v_caller_uid;
	  IF v_caller_role IS NULL OR v_caller_role NOT IN ('admin', 'superadmin') THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED: Only admins can update users.';
	  END IF;
	
	  SELECT * INTO v_old FROM public.profiles WHERE id = p_user_id AND deleted_at IS NULL FOR UPDATE;
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_USER_NOT_FOUND: %', p_user_id;
	  END IF;
	
	  IF p_is_active = false AND p_user_id = v_caller_uid THEN
	    RAISE EXCEPTION 'ERR_SELF_DEACTIVATE_BLOCKED: Cannot deactivate own account.';
	  END IF;
	
	  IF p_full_name IS NOT NULL AND p_full_name <> v_old.full_name THEN
	    v_changes := v_changes || jsonb_build_object('full_name', jsonb_build_object('old', v_old.full_name, 'new', p_full_name));
	    UPDATE public.profiles SET full_name = p_full_name, updated_at = now() WHERE id = p_user_id;
	  END IF;
	
	  IF p_role IS NOT NULL AND p_role <> v_old.role THEN
	    v_changes := v_changes || jsonb_build_object('role', jsonb_build_object('old', v_old.role::text, 'new', p_role::text));
	    UPDATE public.profiles SET role = p_role, updated_at = now() WHERE id = p_user_id;
	  END IF;
	
	  IF p_role_id IS NOT NULL AND (v_old.role_id IS NULL OR p_role_id <> v_old.role_id) THEN
	    v_changes := v_changes || jsonb_build_object('role_id', jsonb_build_object('old', v_old.role_id, 'new', p_role_id));
	    UPDATE public.profiles SET role_id = p_role_id, updated_at = now() WHERE id = p_user_id;
	  END IF;
	
	  IF p_is_active IS NOT NULL AND p_is_active <> v_old.is_active THEN
	    v_changes := v_changes || jsonb_build_object('is_active', jsonb_build_object('old', v_old.is_active, 'new', p_is_active));
	    UPDATE public.profiles SET is_active = p_is_active, updated_at = now() WHERE id = p_user_id;
	  END IF;
	
	  IF p_max_stores_limit IS NOT NULL AND (v_old.max_stores_limit IS NULL OR p_max_stores_limit <> v_old.max_stores_limit) THEN
	    v_changes := v_changes || jsonb_build_object('max_stores_limit', jsonb_build_object('old', v_old.max_stores_limit, 'new', p_max_stores_limit));
	    UPDATE public.profiles SET max_stores_limit = p_max_stores_limit, updated_at = now() WHERE id = p_user_id;
	  END IF;
	
	  IF p_max_users_limit IS NOT NULL AND (v_old.max_users_limit IS NULL OR p_max_users_limit <> v_old.max_users_limit) THEN
	    v_changes := v_changes || jsonb_build_object('max_users_limit', jsonb_build_object('old', v_old.max_users_limit, 'new', p_max_users_limit));
	    UPDATE public.profiles SET max_users_limit = p_max_users_limit, updated_at = now() WHERE id = p_user_id;
	  END IF;
	
	  IF p_plan IS NOT NULL AND p_plan <> v_old.plan THEN
	    v_changes := v_changes || jsonb_build_object('plan', jsonb_build_object('old', v_old.plan::text, 'new', p_plan::text));
	    UPDATE public.profiles SET plan = p_plan, updated_at = now() WHERE id = p_user_id;
	  END IF;
	
	  IF v_changes <> '{}'::jsonb THEN
	    INSERT INTO public.user_audit_log (performed_by, target_user_id, action, old_values, new_values, metadata)
	    VALUES (
	      v_caller_uid, p_user_id, 'USER_UPDATED',
	      jsonb_build_object(
	        'full_name', v_old.full_name, 'role', v_old.role::text,
	        'is_active', v_old.is_active, 'plan', v_old.plan::text
	      ),
	      v_changes,
	      jsonb_build_object('fields_changed', jsonb_object_keys(v_changes))
	    );
	  END IF;
	
	  RETURN jsonb_build_object('success', true, 'user_id', p_user_id, 'changes', v_changes);
	END;
	$function$
2026-09-13 06:19:27.420 UTC [24968] ERROR:  type "user_role" does not exist at character 204
2026-09-13 06:19:27.420 UTC [24968] STATEMENT:  CREATE OR REPLACE FUNCTION public.on_auth_user_created()
	RETURNS trigger
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO 'public', 'extensions'
	AS $function$
	DECLARE
	  v_role_id UUID;
	  v_role_enum USER_ROLE;
	  v_metadata_role TEXT;
	  v_company_name TEXT;
	  v_full_name TEXT;
	  v_tenant_id UUID;
	BEGIN
	  -- Extract role from metadata if present
	  v_metadata_role := NEW.raw_user_meta_data->>'role';
	
	  -- Normalize and convert to enum
	  IF v_metadata_role IS NOT NULL THEN
	    BEGIN
	      v_role_enum := v_metadata_role::USER_ROLE;
	    EXCEPTION WHEN OTHERS THEN
	      v_role_enum := 'tenant_admin'::USER_ROLE;
	    END;
	  ELSE
	    -- Iteración 13: self-signup users are tenant_admin by default
	    v_role_enum := 'tenant_admin'::USER_ROLE;
	  END IF;
	
	  -- Extract company_name and full_name from metadata
	  v_company_name := NEW.raw_user_meta_data->>'company_name';
	  v_full_name := COALESCE(NEW.raw_user_meta_data->>'full_name', split_part(NEW.email, '@', 1));
	
	  -- Iteración 13: Create tenant for self-signup users
	  -- If user was created by admin (via managed_create_user_v2), they may already
	  -- have a tenant_id set via p_target_user_id. In that case, skip tenant creation.
	  IF v_company_name IS NOT NULL THEN
	    INSERT INTO public.tenants (name, owner_id, plan, subscription_status, trial_ends_at, is_active)
	    VALUES (
	      v_company_name,
	      NEW.id,
	      'free'::plan_t,
	      'trial',
	      now() + interval '14 days',
	      true
	    )
	    RETURNING id INTO v_tenant_id;
	  END IF;
	
	  -- Get matching role_id from public.roles
	  SELECT id INTO v_role_id FROM public.roles
	    WHERE name = v_metadata_role OR lower(name) = lower(v_role_enum::text)
	    LIMIT 1;
	
	  -- Insert profile with tenant_id
	  INSERT INTO public.profiles (id, email, full_name, role, role_id, is_active, tenant_id, plan, created_at, updated_at)
	  VALUES (
	    NEW.id,
	    NEW.email,
	    v_full_name,
	    v_role_enum,
	    v_role_id,
	    true,
	    v_tenant_id,
	    'free'::plan_t,
	    now(),
	    now()
	  )
	  ON CONFLICT (id) DO UPDATE SET
	    email = EXCLUDED.email,
	    full_name = EXCLUDED.full_name,
	    role = EXCLUDED.role,
	    role_id = EXCLUDED.role_id,
	    tenant_id = COALESCE(profiles.tenant_id, EXCLUDED.tenant_id),
	    plan = EXCLUDED.plan,
	    is_active = true,
	    updated_at = now();
	
	  -- Audit log
	  INSERT INTO public.user_audit_log (performed_by, target_user_id, action, new_values, metadata)
	  VALUES (
	    NEW.id, NEW.id, 'USER_REGISTERED_PUBLICLY',
	    jsonb_build_object('email', NEW.email, 'role', v_role_enum::text, 'tenant_id', v_tenant_id),
	    jsonb_build_object('company_name', v_company_name, 'source', 'self_signup')
	  );
	
	  RETURN NEW;
	END;
	$function$
2026-09-13 06:19:27.515 UTC [24983] ERROR:  type adjustment_item[] does not exist
2026-09-13 06:19:27.515 UTC [24983] STATEMENT:  CREATE OR REPLACE FUNCTION public.process_inventory_adjustment(p_store_id uuid, p_cashier_id uuid, p_items adjustment_item[], p_operation_date timestamp with time zone DEFAULT NULL::timestamp with time zone)
	 RETURNS uuid
	 LANGUAGE plpgsql
	 SECURITY DEFINER
	 SET search_path TO 'public'
	AS $function$
	DECLARE
	  v_adjustment_id UUID;
	  v_item public.adjustment_item;
	  v_difference NUMERIC;
	  v_effective_date TIMESTAMP WITH TIME ZONE := COALESCE(p_operation_date, NOW());
	BEGIN
	  -- Validación forward-only locking
	  PERFORM public.validate_operation_date(p_operation_date, p_store_id);
	
	  INSERT INTO public.inventory_adjustments (store_id, created_by, status, created_at)
	  VALUES (p_store_id, p_cashier_id, 'PROCESSING', v_effective_date)
	  RETURNING id INTO v_adjustment_id;
	
	  FOREACH v_item IN ARRAY p_items
	  LOOP
	    v_difference := v_item.counted_quantity - v_item.expected_quantity;
	    INSERT INTO public.inventory_adjustment_items (adjustment_id, product_id, expected_quantity, counted_quantity, created_at)
	    VALUES (v_adjustment_id, v_item.product_id, v_item.expected_quantity, v_item.counted_quantity, v_effective_date);
	
	    PERFORM public.register_stock_movement(
	        p_product_id := v_item.product_id,
	        p_store_id := p_store_id,
	        p_user_id := p_cashier_id,
	        p_quantity := v_difference,
	        p_movement_type := 'adjustment',
	        p_operation_date := v_effective_date
	    );
	  END LOOP;
	
	  UPDATE public.inventory_adjustments SET status = 'COMPLETED', updated_at = v_effective_date
	  WHERE id = v_adjustment_id;
	  RETURN v_adjustment_id;
	END;
	$function$
2026-09-13 06:19:27.571 UTC [24992] ERROR:  type "public.purchase_status_enum" does not exist at character 425
2026-09-13 06:19:27.571 UTC [24992] STATEMENT:  CREATE OR REPLACE FUNCTION public.receive_against_po(
	  p_po_id uuid,
	  p_received_items jsonb DEFAULT '[]'::jsonb,
	  p_user_id uuid DEFAULT NULL,
	  p_reception_date timestamptz DEFAULT now(),
	  p_invoice_number text DEFAULT NULL
	) RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO 'public', 'extensions'
	AS $func$
	DECLARE
	  v_store_id            uuid;
	  v_supplier_name       text;
	  v_po_status           public.purchase_status_enum;
	  v_item                jsonb;
	  v_item_id             uuid;
	  v_qty                 numeric;
	  v_qty_ordered         numeric;
	  v_qty_received_before numeric;
	  v_product_id          uuid;
	  v_unit_cost           numeric;
	  v_product_name        text;
	  v_all_received        boolean;
	  v_any_received        boolean;
	  v_new_status          public.purchase_status_enum;
	  v_receipt_id          uuid;
	  v_reception_items     jsonb := '[]'::jsonb;
	  v_count               integer := 0;
	  v_po_number           text;
	  v_invoice_to_pass     text;
	  v_sorted_items        jsonb;
	BEGIN
	  -- ─── 1. Cargar OC con lock exclusivo ───
	  SELECT po.store_id, po.supplier_name, po.status, po.po_number
	    INTO v_store_id, v_supplier_name, v_po_status, v_po_number
	  FROM public.purchase_orders po
	  WHERE po.id = p_po_id
	  FOR UPDATE;
	
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_PO_NOT_FOUND';
	  END IF;
	
	  -- ─── 2. Validar acceso (tenant-aware via has_store_access) ───
	  IF NOT public.has_store_access(v_store_id) THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED';
	  END IF;
	
	  -- ─── 3. Validar estado de la OC ───
	  IF v_po_status = 'cancelled' THEN
	    RAISE EXCEPTION 'ERR_PO_CANCELLED';
	  END IF;
	  -- Solo se puede recibir contra draft, sent o partial.
	  IF v_po_status NOT IN ('draft', 'sent', 'partial') THEN
	    RAISE EXCEPTION 'ERR_PO_NOT_RECEIVABLE: status % is terminal', v_po_status;
	  END IF;
	
	  -- ─── 4. Validar items no vacío (parity B3 register_reception) ───
	  IF p_received_items IS NULL OR jsonb_array_length(p_received_items) = 0 THEN
	    RAISE EXCEPTION 'ERR_EMPTY_ITEMS';
	  END IF;
	
	  -- ─── 5. Condición #5 (anti-deadlock): Ordenar items por po_item_id ASC ───
	  SELECT jsonb_agg(elem ORDER BY (elem->>'po_item_id'))
	    INTO v_sorted_items
	  FROM jsonb_array_elements(p_received_items) AS elem;
	
	  -- ─── 6. Procesar cada item: VALIDAR antes de actualizar ───
	  FOR v_item IN SELECT * FROM jsonb_array_elements(v_sorted_items) LOOP
	    v_item_id := NULLIF(v_item->>'po_item_id', '')::uuid;
	    v_qty     := COALESCE((v_item->>'quantity_received')::numeric, 0);
	
	    -- Validar po_item_id presente
	    IF v_item_id IS NULL THEN
	      RAISE EXCEPTION 'ERR_ITEM_ID_REQUIRED: missing po_item_id in received_items';
	    END IF;
	
	    -- Medio #6: qty > 0
	    IF v_qty <= 0 THEN
	      RAISE EXCEPTION 'ERR_NEGATIVE_QTY: qty % for item % must be > 0', v_qty, v_item_id;
	    END IF;
	
	    -- Cargar estado actual del item con lock FOR UPDATE
	    SELECT product_id, quantity_ordered, quantity_received, unit_cost, product_name
	      INTO v_product_id, v_qty_ordered, v_qty_received_before, v_unit_cost, v_product_name
	    FROM public.purchase_order_items
	    WHERE id = v_item_id AND po_id = p_po_id
	    FOR UPDATE;
	
	    IF NOT FOUND THEN
	      RAISE EXCEPTION 'ERR_ITEM_NOT_FOUND: %', v_item_id;
	    END IF;
	
	    -- Alto #5: over-receive check
	    IF (v_qty_received_before + v_qty) > v_qty_ordered THEN
	      RAISE EXCEPTION 'ERR_OVER_RECEIVE: item % ordered %, already received %, attempting %',
	        v_item_id, v_qty_ordered, v_qty_received_before, v_qty;
	    END IF;
	
	    -- Condición #1: todo item debe tener product_id NOT NULL
	    IF v_product_id IS NULL THEN
	      RAISE EXCEPTION 'ERR_PRODUCT_ID_REQUIRED: item % has no product_id', v_item_id;
	    END IF;
	
	    -- Medio #9: producto debe existir en store
	    IF NOT EXISTS (
	      SELECT 1 FROM public.products p
	      WHERE p.id = v_product_id AND p.store_id = v_store_id
	    ) THEN
	      RAISE EXCEPTION 'ERR_PRODUCT_NOT_IN_STORE: product % not in store %',
	        v_product_id, v_store_id;
	    END IF;
	
	    -- Acumular para register_reception (formato esperado: product_id, quantity, unit_cost, moneda, tasa)
	    v_reception_items := v_reception_items || jsonb_build_object(
	      'product_id',   v_product_id,
	      'product_name', v_product_name,
	      'quantity',     v_qty,
	      'unit_cost',    v_unit_cost,
	      'moneda_recepcion', 'CUP',
	      'tasa_cambio_recepcion', 1.0
	    );
	    v_count := v_count + 1;
	  END LOOP;
	
	  -- ─── 7. Condición #3: reference_doc NULL si no viene invoice_number ───
	  -- Evita colisión con UNIQUE INDEX idx_receipts_store_reference_doc
	  -- (store_id, reference_doc) en partial indexes.
	  -- El vínculo real receipt↔PO es receipts.po_id (G6).
	  IF p_invoice_number IS NULL OR p_invoice_number = '' THEN
	    v_invoice_to_pass := NULL;
	  ELSE
	    v_invoice_to_pass := p_invoice_number;
	  END IF;
	
	  -- ─── 8. Crear receipt vinculado vía register_reception ───
	  -- register_reception aplica las 8 validaciones B1-B5 + C1-C3 del v2.23.0
	  -- (no se relajan). G6 añade p_po_id opcional.
	  v_receipt_id := public.register_reception(
	    p_store_id        := v_store_id,
	    p_supplier        := v_supplier_name,
	    p_reception_date  := p_reception_date,
	    p_invoice_number  := v_invoice_to_pass,
	    p_items           := v_reception_items,
	    p_user_id         := p_user_id,
	    p_po_id           := p_po_id
	  );
	
	  -- ─── 9. Actualizar quantity_received en purchase_order_items ───
	  -- (SECURITY DEFINER bypasses RLS DENY UPDATE de G5)
	  FOR v_item IN SELECT * FROM jsonb_array_elements(v_sorted_items) LOOP
	    v_item_id := NULLIF(v_item->>'po_item_id', '')::uuid;
	    v_qty     := (v_item->>'quantity_received')::numeric;
	
	    UPDATE public.purchase_order_items
	    SET quantity_received = quantity_received + v_qty
	    WHERE id = v_item_id AND po_id = p_po_id;
	  END LOOP;
	
	  -- ─── 10. Recalcular status global (cast EXPLÍCITO a enum) ───
	  SELECT
	    BOOL_AND(quantity_received >= quantity_ordered),
	    BOOL_OR(quantity_received > 0)
	  INTO v_all_received, v_any_received
	  FROM public.purchase_order_items WHERE po_id = p_po_id;
	
	  v_new_status :=
	    CASE
	      WHEN v_all_received THEN 'received'::public.purchase_status_enum
	      WHEN v_any_received THEN 'partial'::public.purchase_status_enum
	      ELSE 'sent'::public.purchase_status_enum
	    END;
	
	  -- ─── 11. Actualizar OC + vincular receipt en notas ───
	  UPDATE public.purchase_orders
	  SET status       = v_new_status,
	      received_at  = CASE WHEN v_new_status = 'received' THEN NOW() ELSE received_at END,
	      notes        = COALESCE(notes, '') ||
	                     CASE WHEN COALESCE(notes, '') = '' THEN '' ELSE E'\n' END ||
	                     '[Receipt ' || v_receipt_id::text || ' linked at ' || NOW()::text || ']'
	  WHERE id = p_po_id;
	
	  -- ─── 12. CxP (Grupo 6): marcar receipt con payment_status='unpaid' ───
	  -- Solo si aún no está seteado (no sobrescribir si register_reception ya lo puso).
	  UPDATE public.receipts
	  SET payment_status     = COALESCE(NULLIF(payment_status, ''), 'unpaid'),
	      payment_terms_days = COALESCE(payment_terms_days, 30),
	      due_date           = COALESCE(due_date, (p_reception_date::date + INTERVAL '30 days')::date)
	  WHERE id = v_receipt_id;
	
	  -- ─── 13. Auditoría ───
	  INSERT INTO public.audit_logs (user_id, store_id, action, table_name, record_id, metadata)
	  VALUES (
	    p_user_id, v_store_id, 'po_received', 'purchase_orders', p_po_id,
	    jsonb_build_object(
	      'new_status', v_new_status::text,
	      'items_received', v_count,
	      'receipt_id', v_receipt_id,
	      'po_number', v_po_number,
	      'invoice_number', v_invoice_to_pass
	    )
	  );
	
	  RETURN jsonb_build_object(
	    'status', 'success',
	    'po_status', v_new_status::text,
	    'receipt_id', v_receipt_id,
	    'items_received', v_count
	  );
	END;
	$func$
2026-09-13 06:19:27.734 UTC [25029] ERROR:  type purchase_status_enum does not exist
2026-09-13 06:19:27.734 UTC [25029] STATEMENT:  CREATE OR REPLACE FUNCTION public.set_purchase_order_status(p_po_id uuid, p_new_status purchase_status_enum, p_user_id uuid DEFAULT NULL::uuid, p_reason text DEFAULT NULL::text)
	 RETURNS jsonb
	 LANGUAGE plpgsql
	 SECURITY DEFINER
	 SET search_path TO 'public', 'extensions'
	AS $function$
	DECLARE
	  v_store_id     uuid;
	  v_current      public.purchase_status_enum;
	  v_po_number    text;
	  v_allowed      text[];
	  v_is_allowed   boolean := false;
	BEGIN
	  -- ─── 1. Cargar PO con lock exclusivo ───
	  SELECT store_id, status, po_number
	    INTO v_store_id, v_current, v_po_number
	  FROM public.purchase_orders
	  WHERE id = p_po_id
	  FOR UPDATE;
	
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_PO_NOT_FOUND';
	  END IF;
	
	  -- ─── 2. Validar acceso (tenant-aware) ───
	  IF NOT public.has_store_access(v_store_id) THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED';
	  END IF;
	
	  -- ─── 3. Si no hay cambio, retornar success sin hacer nada ───
	  IF v_current = p_new_status THEN
	    RETURN jsonb_build_object(
	      'status', 'no_change',
	      'po_status', v_current::text,
	      'po_number', v_po_number
	    );
	  END IF;
	
	  -- ─── 4. Definir transiciones permitidas (state machine) ───
	  -- 'partial' y 'received' NUNCA son destino válido manualmente
	  -- (solo vía receive_against_po).
	  v_allowed := CASE v_current
	    WHEN 'draft'   THEN ARRAY['sent', 'cancelled']
	    WHEN 'sent'    THEN ARRAY['cancelled']
	    WHEN 'partial' THEN ARRAY['cancelled']
	    ELSE ARRAY[]::text[]  -- received, cancelled: terminal, no transitions
	  END;
	
	  -- ─── 5. Verificar transición permitida ───
	  SELECT EXISTS(SELECT 1 FROM unnest(v_allowed) a WHERE a = p_new_status::text)
	    INTO v_is_allowed;
	
	  IF NOT v_is_allowed THEN
	    RAISE EXCEPTION 'ERR_INVALID_TRANSITION: % → % not allowed (allowed: %)',
	      v_current::text, p_new_status::text,
	      CASE WHEN array_length(v_allowed, 1) IS NULL THEN 'none' ELSE array_to_string(v_allowed, ', ') END;
	  END IF;
	
	  -- ─── 6. Aplicar transición ───
	  UPDATE public.purchase_orders
	  SET status      = p_new_status,
	      received_at = CASE WHEN p_new_status = 'received' THEN NOW() ELSE received_at END
	  WHERE id = p_po_id;
	
	  -- ─── 7. Auditoría (action = PO_STATUS_CHANGED) ───
	  INSERT INTO public.audit_logs (user_id, store_id, action, table_name, record_id, metadata)
	  VALUES (
	    CASE WHEN auth.role() = 'service_role' THEN COALESCE(p_user_id, auth.uid()) ELSE auth.uid() END, v_store_id, 'PO_STATUS_CHANGED', 'purchase_orders', p_po_id,
	    jsonb_build_object(
	      'po_number', v_po_number,
	      'from_status', v_current::text,
	      'to_status', p_new_status::text,
	      'reason', p_reason
	    )
	  );
	
	  RETURN jsonb_build_object(
	    'status', 'success',
	    'po_status', p_new_status::text,
	    'po_number', v_po_number,
	    'previous_status', v_current::text
	  );
	END;
	$function$
2026-09-13 06:19:27.843 UTC [25055] ERROR:  type "user_role" does not exist at character 100
2026-09-13 06:19:27.843 UTC [25055] STATEMENT:  CREATE OR REPLACE FUNCTION public.validate_active_store()
	RETURNS TRIGGER AS $$
	DECLARE
	    v_role user_role;
	BEGIN
	    v_role := NEW.role;
	
	    -- Si role es 'costo' o 'admin' o 'superadmin', active_store_id es opcional
	    IF v_role = 'costo'::user_role
	       OR v_role = 'admin'::user_role
	       OR v_role = 'superadmin'::user_role THEN
	        -- Solo validar si active_store_id cambió o es INSERT
	        IF TG_OP = 'INSERT' OR (OLD.active_store_id IS DISTINCT FROM NEW.active_store_id) THEN
	            IF NEW.active_store_id IS NOT NULL THEN
	                IF NOT EXISTS (
	                    SELECT 1 FROM public.user_store_memberships
	                    WHERE user_id = NEW.id
	                      AND store_id = NEW.active_store_id
	                      AND status = 'active'
	                ) THEN
	                    IF TG_OP = 'UPDATE' THEN
	                        RAISE EXCEPTION 'ERR_INVALID_ACTIVE_STORE: El usuario no tiene membership activa en la tienda seleccionada.';
	                    END IF;
	                END IF;
	            END IF;
	        END IF;
	        RETURN NEW;
	    END IF;
	
	    -- Para roles operativos (encargado, clerk, warehouse, manager, usuario)
	    IF TG_OP = 'INSERT' THEN
	        -- En INSERT: ser permisivo. La función managed_create_user inserta
	        -- memberships justo después. No validar nada aquí.
	        RETURN NEW;
	    ELSE
	        -- UPDATE: solo validar si active_store_id cambió realmente
	        -- (evita bloquear updates de otros campos en perfiles intermedios)
	        IF OLD.active_store_id IS NOT DISTINCT FROM NEW.active_store_id THEN
	            -- active_store_id no cambió → permitir el UPDATE sin validar
	            RETURN NEW;
	        END IF;
	
	        -- active_store_id cambió → validar consistencia
	        IF NEW.active_store_id IS NULL THEN
	            IF v_role IN ('encargado'::user_role, 'clerk'::user_role, 'warehouse'::user_role) THEN
	                RAISE EXCEPTION 'ERR_STORE_REQUIRED: El rol % requiere una tienda activa asignada.', v_role;
	            END IF;
	        ELSE
	            IF NOT EXISTS (
	                SELECT 1 FROM public.user_store_memberships
	                WHERE user_id = NEW.id
	                  AND store_id = NEW.active_store_id
	                  AND status = 'active'
	            ) THEN
	                RAISE EXCEPTION 'ERR_INVALID_ACTIVE_STORE: El usuario no tiene membership activa en la tienda seleccionada.';
	            END IF;
	        END IF;
	        RETURN NEW;
	    END IF;
	END;
	$$ LANGUAGE plpgsql SECURITY DEFINER
2026-09-13 06:19:27.890 UTC [25066] ERROR:  type "public.user_role" does not exist at character 270
2026-09-13 06:19:27.890 UTC [25066] STATEMENT:  CREATE OR REPLACE FUNCTION public.validate_tenant_access(
	  p_user_id uuid,
	  p_store_id uuid
	)
	RETURNS boolean
	LANGUAGE plpgsql
	STABLE SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  v_user_tenant uuid;
	  v_store_tenant uuid;
	  v_user_role public.user_role;
	BEGIN
	  -- Global admin bypasses tenant check
	  SELECT role, tenant_id INTO v_user_role, v_user_tenant
	    FROM public.profiles WHERE id = p_user_id;
	
	  IF v_user_role IN ('admin', 'superadmin') THEN
	    RETURN true;
	  END IF;
	
	  -- Get store's tenant
	  SELECT tenant_id INTO v_store_tenant FROM public.stores WHERE id = p_store_id;
	
	  IF v_store_tenant IS NULL THEN
	    -- Legacy store without tenant — allow (backward compat)
	    RETURN true;
	  END IF;
	
	  -- Check tenant match
	  RETURN v_user_tenant = v_store_tenant;
	END;
	$function$
2026-09-13 06:19:28.072 UTC [25101] ERROR:  type "public.user_role" does not exist at character 301
2026-09-13 06:19:28.072 UTC [25101] STATEMENT:  CREATE OR REPLACE FUNCTION public.bulk_assign_memberships(
	  p_user_id uuid,
	  p_assignments jsonb
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  v_assignment jsonb;
	  v_affected int := 0;
	  v_failed int := 0;
	  v_store_id uuid;
	  v_role public.user_role;
	  v_status text;
	  v_caller_uid uuid := auth.uid();
	  v_changes jsonb := '[]'::jsonb;
	BEGIN
	  IF v_caller_uid IS NULL THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED';
	  END IF;
	
	  FOR v_assignment IN SELECT * FROM jsonb_array_elements(p_assignments) LOOP
	    BEGIN
	      v_store_id := (v_assignment->>'store_id')::uuid;
	      v_role := (v_assignment->>'role')::public.user_role;
	      v_status := COALESCE(v_assignment->>'status', 'active');
	
	      -- Validar caller tiene acceso al store
	      IF NOT public.is_admin() AND NOT public.has_store_role(v_store_id, ARRAY['admin', 'manager']) THEN
	        v_failed := v_failed + 1;
	        CONTINUE;
	      END IF;
	
	      INSERT INTO public.user_store_memberships (user_id, store_id, role, status)
	      VALUES (p_user_id, v_store_id, v_role, v_status)
	      ON CONFLICT (user_id, store_id) DO UPDATE SET
	        role = EXCLUDED.role,
	        status = EXCLUDED.status,
	        updated_at = now();
	
	      v_changes := v_changes || jsonb_build_object(jsonb_build_object(
	        'store_id', v_store_id,
	        'role', v_role::text,
	        'status', v_status
	      ));
	
	      v_affected := v_affected + 1;
	    EXCEPTION
	      WHEN foreign_key_violation THEN
	        v_failed := v_failed + 1;
	    END;
	  END LOOP;
	
	  -- Audit log atómico (solo si hubo cambios)
	  IF v_affected > 0 THEN
	    INSERT INTO public.user_audit_log (performed_by, target_user_id, action, new_values, metadata)
	    VALUES (
	      v_caller_uid, p_user_id, 'MEMBERSHIPS_BULK_ASSIGNED',
	      jsonb_build_object('assignments', v_changes),
	      jsonb_build_object('affected', v_affected, 'failed', v_failed)
	    );
	  END IF;
	
	  RETURN jsonb_build_object(
	    'success', true,
	    'affected', v_affected,
	    'failed', v_failed,
	    'user_id', p_user_id
	  );
	END;
	$function$
2026-09-13 06:19:28.100 UTC [25106] ERROR:  syntax error at or near "REVOKE" at character 5039
2026-09-13 06:19:28.100 UTC [25106] STATEMENT:  CREATE OR REPLACE FUNCTION public.bulk_soft_delete_stores(p_store_ids uuid[], p_deleted_by uuid, p_confirmation_token text, p_override_token text DEFAULT NULL::text, p_reason text DEFAULT NULL::text)
	 RETURNS jsonb
	 LANGUAGE plpgsql
	 SECURITY DEFINER
	 SET search_path TO 'public'
	AS $function$
	DECLARE
	  v_store_id UUID;
	  v_validation JSONB;
	  v_blockers JSONB;
	  v_errors JSONB[] := '{}'::jsonb[];
	  v_processed INTEGER := 0;
	  v_token_valid BOOLEAN;
	  v_has_protected BOOLEAN;
	  v_override_valid BOOLEAN;
	  v_confirmation_record RECORD;
	  v_override_record RECORD;
	  v_caller_role TEXT;
	BEGIN
	  -- ============================================================
	  -- AUTH CHECK: Solo admin puede ejecutar esta función
	  -- ============================================================
	  IF auth.uid() IS NOT NULL THEN
	    SELECT role INTO v_caller_role FROM public.profiles WHERE id = auth.uid();
	    IF v_caller_role IS NULL OR v_caller_role != 'admin' THEN
	      RAISE EXCEPTION 'ERR_PERMISSION_DENIED: Solo admin puede ejecutar bulk_soft_delete_stores';
	    END IF;
	  END IF;
	  -- Si auth.uid() IS NULL, es service_role — permitir
	
	  -- ============================================================
	  -- 1. VALIDATE confirmation_token
	  -- ============================================================
	  SELECT * INTO v_confirmation_record
	  FROM public.bulk_confirmation_tokens
	  WHERE token = p_confirmation_token
	    AND action = 'delete'
	    AND expires_at > NOW()
	    AND consumed_at IS NULL
	    AND is_override = false;
	
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_INVALID_CONFIRMATION_TOKEN';
	  END IF;
	
	  IF v_confirmation_record.store_ids != p_store_ids THEN
	    RAISE EXCEPTION 'ERR_STORE_IDS_MISMATCH';
	  END IF;
	
	  -- ============================================================
	  -- 2. VALIDATE tiendas protegidas requieren override_token
	  -- ============================================================
	  SELECT EXISTS(
	    SELECT 1 FROM public.stores
	    WHERE id = ANY(p_store_ids) AND backup_restore_protected = true
	  ) INTO v_has_protected;
	
	  IF v_has_protected THEN
	    IF p_override_token IS NULL THEN
	      RAISE EXCEPTION 'ERR_OVERRIDE_REQUIRED';
	    END IF;
	
	    SELECT * INTO v_override_record
	    FROM public.bulk_confirmation_tokens
	    WHERE token = p_override_token
	      AND is_override = true
	      AND override_for = p_confirmation_token
	      AND expires_at > NOW()
	      AND consumed_at IS NULL;
	
	    IF NOT FOUND THEN
	      RAISE EXCEPTION 'ERR_INVALID_OVERRIDE_TOKEN';
	    END IF;
	
	    IF v_override_record.store_ids != v_confirmation_record.store_ids THEN
	      RAISE EXCEPTION 'ERR_OVERRIDE_STORE_IDS_MISMATCH';
	    END IF;
	
	    IF v_override_record.created_by = v_confirmation_record.created_by THEN
	      RAISE EXCEPTION 'ERR_SAME_USER_OVERRIDE';
	    END IF;
	
	    UPDATE public.bulk_confirmation_tokens SET consumed_at = NOW()
	    WHERE token = p_override_token;
	  END IF;
	
	  UPDATE public.bulk_confirmation_tokens SET consumed_at = NOW()
	  WHERE token = p_confirmation_token;
	
	  -- ============================================================
	  -- 3. VALIDATE todas las tiendas
	  -- ============================================================
	  FOREACH v_store_id IN ARRAY p_store_ids LOOP
	    IF NOT EXISTS(SELECT 1 FROM public.stores WHERE id = v_store_id AND is_active = true) THEN
	      v_errors := array_append(v_errors, jsonb_build_object(
	        'store_id', v_store_id, 'reason', 'STORE_NOT_FOUND_OR_INACTIVE'
	      ));
	      CONTINUE;
	    END IF;
	
	    SELECT public.validate_store_can_be_modified(v_store_id, 'soft_delete') INTO v_validation;
	    v_blockers := v_validation->'blockers';
	
	    IF v_validation->>'can_modify' != 'true' THEN
	      v_errors := array_append(v_errors, jsonb_build_object(
	        'store_id', v_store_id, 'reason', 'HAS_BLOCKING_DEPENDENCIES', 'blockers', v_blockers
	      ));
	    END IF;
	  END LOOP;
	
	  IF array_length(v_errors, 1) IS NOT NULL THEN
	    RETURN jsonb_build_object(
	      'status', 'FAILED', 'processed', 0,
	      'total_requested', array_length(p_store_ids, 1),
	      'errors', to_jsonb(v_errors), 'reason', p_reason
	    );
	  END IF;
	
	  -- ============================================================
	  -- 4. EXECUTE
	  -- ============================================================
	  FOREACH v_store_id IN ARRAY p_store_ids LOOP
	    PERFORM public.soft_delete_store(v_store_id, p_deleted_by);
	    v_processed := v_processed + 1;
	  END LOOP;
	
	  INSERT INTO public.audit_logs (action, table_name, record_id, metadata)
	  VALUES (
	    'bulk_store_deleted', 'stores', NULL,
	    jsonb_build_object(
	      'store_ids', p_store_ids, 'deleted_by', p_deleted_by,
	      'reason', p_reason, 'processed', v_processed,
	      'had_protected_stores', v_has_protected,
	      'override_used', p_override_token IS NOT NULL, 'deleted_at', NOW()
	    )
	  );
	
	  RETURN jsonb_build_object(
	    'status', 'COMPLETED', 'processed', v_processed,
	    'total_requested', array_length(p_store_ids, 1),
	    'errors', '[]'::jsonb, 'reason', p_reason
	  );
	END;
	$function$
	 
	REVOKE EXECUTE ON FUNCTION public.bulk_soft_delete_stores FROM PUBLIC, anon, authenticated
2026-09-13 06:19:28.178 UTC [25115] ERROR:  relation "received_services" does not exist
2026-09-13 06:19:28.178 UTC [25115] CONTEXT:  compilation of PL/pgSQL function "calculate_service_distribution" near line 3
2026-09-13 06:19:28.178 UTC [25115] STATEMENT:  CREATE OR REPLACE FUNCTION calculate_service_distribution(
	  p_service_id UUID
	) RETURNS TABLE(
	  receipt_item_id UUID,
	  product_id UUID,
	  distribution_amount NUMERIC,
	  distribution_percentage NUMERIC
	) LANGUAGE plpgsql SECURITY DEFINER AS $$
	DECLARE
	  v_service received_services%ROWTYPE;
	  v_method TEXT;
	  v_total_amount NUMERIC;
	  v_total_value NUMERIC DEFAULT 0;
	  v_total_qty NUMERIC DEFAULT 0;
	  v_link service_reception_links%ROWTYPE;
	  v_receipt_id UUID;
	  v_item receipt_items%ROWTYPE;
	  v_allocated NUMERIC;
	BEGIN
	  SELECT * INTO v_service FROM received_services WHERE id = p_service_id;
	  IF NOT FOUND THEN RETURN; END IF;
	  
	  v_method := v_service.distribution_method;
	  v_total_amount := v_service.total_amount;
	
	  -- Para cada recepción vinculada
	  FOR v_link IN SELECT * FROM service_reception_links WHERE service_id = p_service_id AND allocated_amount > 0 LOOP
	    v_receipt_id := v_link.receipt_id;
	    v_allocated := v_link.allocated_amount;
	
	    -- Calcular base de distribución según método
	    IF v_method = 'amount' THEN
	      SELECT COALESCE(SUM(quantity * unit_cost), 0) INTO v_total_value
	      FROM receipt_items WHERE receipt_id = v_receipt_id;
	      
	      IF v_total_value > 0 THEN
	        FOR v_item IN SELECT * FROM receipt_items WHERE receipt_id = v_receipt_id LOOP
	          distribution_amount := v_allocated * (v_item.quantity * v_item.unit_cost / v_total_value);
	          distribution_percentage := (v_item.quantity * v_item.unit_cost / v_total_value) * 100;
	          receipt_item_id := v_item.id;
	          product_id := v_item.product_id;
	          RETURN NEXT;
	        END LOOP;
	      END IF;
	
	    ELSIF v_method = 'quantity' THEN
	      SELECT COALESCE(SUM(quantity), 0) INTO v_total_qty
	      FROM receipt_items WHERE receipt_id = v_receipt_id;
	      
	      IF v_total_qty > 0 THEN
	        FOR v_item IN SELECT * FROM receipt_items WHERE receipt_id = v_receipt_id LOOP
	          distribution_amount := v_allocated * (v_item.quantity / v_total_qty);
	          distribution_percentage := (v_item.quantity / v_total_qty) * 100;
	          receipt_item_id := v_item.id;
	          product_id := v_item.product_id;
	          RETURN NEXT;
	        END LOOP;
	      END IF;
	
	    ELSIF v_method = 'manual' THEN
	      -- En manual, la distribución se guarda directamente desde el cliente
	      RETURN;
	    END IF;
	  END LOOP;
	END;
	$$
2026-09-13 06:19:28.229 UTC [25127] ERROR:  syntax error at or near "REVOKE" at character 678
2026-09-13 06:19:28.229 UTC [25127] STATEMENT:  CREATE OR REPLACE FUNCTION public.check_bulk_ops_hourly_limit(p_user_id uuid, p_plan text)
	 RETURNS jsonb
	 LANGUAGE plpgsql
	 STABLE SECURITY DEFINER
	 SET search_path TO 'public'
	AS $function$
	DECLARE
	  v_limit INTEGER;
	  v_used INTEGER;
	BEGIN
	  v_limit := CASE p_plan
	    WHEN 'free' THEN 1
	    WHEN 'pro' THEN 20
	    WHEN 'enterprise' THEN 999999
	    ELSE 1
	  END;
	
	  SELECT COUNT(*) INTO v_used
	  FROM public.bulk_ops_log
	  WHERE user_id = p_user_id
	    AND initiated_at > NOW() - INTERVAL '1 hour';
	
	  RETURN jsonb_build_object(
	    'allowed', v_used < v_limit,
	    'used', v_used,
	    'limit', v_limit,
	    'remaining', GREATEST(0, v_limit - v_used)
	  );
	END;
	$function$
	 
	REVOKE EXECUTE ON FUNCTION public.check_bulk_ops_hourly_limit FROM PUBLIC, anon, authenticated
2026-09-13 06:19:28.259 UTC [25134] ERROR:  type plan_t does not exist
2026-09-13 06:19:28.259 UTC [25134] STATEMENT:  CREATE OR REPLACE FUNCTION public.check_tenant_store_quota(
	  p_tenant_id uuid,
	  p_plan plan_t DEFAULT NULL
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  v_plan plan_t;
	  v_limit int;
	  v_current int;
	  v_allowed boolean;
	BEGIN
	  -- Get plan from tenant if not passed
	  IF p_plan IS NULL THEN
	    SELECT plan INTO v_plan FROM public.tenants WHERE id = p_tenant_id;
	  ELSE
	    v_plan := p_plan;
	  END IF;
	
	  -- Plan limits (unified source of truth)
	  v_limit := CASE v_plan
	    WHEN 'free'::plan_t THEN 1
	    WHEN 'pro'::plan_t THEN 3
	    WHEN 'enterprise'::plan_t THEN 10
	    ELSE 1
	  END;
	
	  -- Count active stores in tenant
	  SELECT COUNT(*) INTO v_current
	    FROM public.stores
	    WHERE tenant_id = p_tenant_id AND is_active = true;
	
	  v_allowed := v_current < v_limit;
	
	  RETURN jsonb_build_object(
	    'allowed', v_allowed,
	    'current', v_current,
	    'limit', v_limit,
	    'plan', v_plan::text,
	    'tenant_id', p_tenant_id
	  );
	END;
	$function$
2026-09-13 06:19:28.368 UTC [25163] ERROR:  relation "public.profiles" does not exist at character 166
2026-09-13 06:19:28.368 UTC [25163] STATEMENT:  CREATE OR REPLACE FUNCTION public.current_user_tenant_id()
	RETURNS uuid
	LANGUAGE sql
	STABLE
	SECURITY DEFINER
	SET search_path TO public
	AS $$
	  SELECT tenant_id FROM public.profiles WHERE id = auth.uid();
	$$
2026-09-13 06:19:28.418 UTC [25171] ERROR:  relation "received_services" does not exist
2026-09-13 06:19:28.418 UTC [25171] CONTEXT:  compilation of PL/pgSQL function "distribute_service_cost_v2" near line 6
2026-09-13 06:19:28.418 UTC [25171] STATEMENT:  CREATE OR REPLACE FUNCTION public.distribute_service_cost_v2(
	  p_service_id uuid,
	  p_user_id uuid DEFAULT NULL
	) RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO 'public', 'extensions'
	AS $func$
	DECLARE
	  v_store_id uuid;
	  v_status text;
	  v_total_amount numeric;
	  v_service received_services%ROWTYPE;
	  v_method text;
	  v_link service_reception_links%ROWTYPE;
	  v_item receipt_items%ROWTYPE;
	  v_total_value numeric := 0;
	  v_total_qty numeric := 0;
	  v_allocated numeric;
	  v_dist_count integer := 0;
	  v_caller_uid uuid := COALESCE(p_user_id, auth.uid());
	BEGIN
	  PERFORM pg_advisory_xact_lock(hashtext(p_service_id::text));
	
	  SELECT * INTO v_service FROM received_services WHERE id = p_service_id FOR UPDATE;
	  IF NOT FOUND THEN RAISE EXCEPTION 'ERR_SERVICE_NOT_FOUND'; END IF;
	
	  v_store_id := v_service.store_id;
	  v_status := v_service.status;
	  v_total_amount := v_service.total_amount;
	  v_method := v_service.distribution_method;
	
	  IF NOT public.has_store_access(v_store_id) THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED';
	  END IF;
	
	  IF v_status != 'active' THEN
	    RAISE EXCEPTION 'ERR_SERVICE_NOT_ACTIVE: status % is not active', v_status;
	  END IF;
	
	  IF v_method = 'manual' THEN
	    RAISE EXCEPTION 'ERR_MANUAL_METHOD: use link_receipts_to_service for manual distribution';
	  END IF;
	
	  DELETE FROM service_cost_distributions WHERE service_id = p_service_id;
	
	  FOR v_link IN SELECT * FROM service_reception_links WHERE service_id = p_service_id AND allocated_amount > 0 ORDER BY receipt_id LOOP
	    v_allocated := v_link.allocated_amount;
	
	    IF v_method = 'amount' THEN
	      SELECT COALESCE(SUM(quantity * unit_cost), 0) INTO v_total_value
	      FROM receipt_items WHERE receipt_id = v_link.receipt_id;
	      IF v_total_value > 0 THEN
	        FOR v_item IN SELECT * FROM receipt_items WHERE receipt_id = v_link.receipt_id ORDER BY id LOOP
	          INSERT INTO service_cost_distributions
	            (service_id, receipt_id, receipt_item_id, product_id, distribution_amount, distribution_percentage)
	          VALUES
	            (p_service_id, v_link.receipt_id, v_item.id, v_item.product_id,
	             v_allocated * (v_item.quantity * v_item.unit_cost / v_total_value),
	             (v_item.quantity * v_item.unit_cost / v_total_value) * 100);
	          v_dist_count := v_dist_count + 1;
	        END LOOP;
	      END IF;
	
	    ELSIF v_method = 'quantity' THEN
	      SELECT COALESCE(SUM(quantity), 0) INTO v_total_qty
	      FROM receipt_items WHERE receipt_id = v_link.receipt_id;
	      IF v_total_qty > 0 THEN
	        FOR v_item IN SELECT * FROM receipt_items WHERE receipt_id = v_link.receipt_id ORDER BY id LOOP
	          INSERT INTO service_cost_distributions
	            (service_id, receipt_id, receipt_item_id, product_id, distribution_amount, distribution_percentage)
	          VALUES
	            (p_service_id, v_link.receipt_id, v_item.id, v_item.product_id,
	             v_allocated * (v_item.quantity / v_total_qty),
	             (v_item.quantity / v_total_qty) * 100);
	          v_dist_count := v_dist_count + 1;
	        END LOOP;
	      END IF;
	    END IF;
	  END LOOP;
	
	  -- Forzar recalculo de WAC para cada producto afectado
	  UPDATE receipt_items SET updated_at = NOW()
	  WHERE product_id IN (SELECT DISTINCT product_id FROM service_cost_distributions WHERE service_id = p_service_id);
	
	  INSERT INTO audit_logs (user_id, store_id, action, table_name, record_id, metadata)
	  VALUES (v_caller_uid, v_store_id, 'SERVICE_DISTRIBUTED', 'received_services', p_service_id,
	    jsonb_build_object('rows_distributed', v_dist_count, 'method', v_method, 'total_amount', v_total_amount));
	
	  RETURN jsonb_build_object('status', 'success', 'distributed_rows', v_dist_count);
	END;
	$func$
2026-09-13 06:19:28.453 UTC [25178] ERROR:  type "user_role" does not exist at character 116
2026-09-13 06:19:28.453 UTC [25178] STATEMENT:  CREATE OR REPLACE FUNCTION public.enforce_encargado_store_limit()
	RETURNS TRIGGER AS $$
	DECLARE
	    v_creator_role user_role;
	    v_limit integer;
	    v_current_count integer;
	BEGIN
	    IF NEW.created_by IS NULL THEN RETURN NEW; END IF;
	
	    SELECT role, max_stores_limit INTO v_creator_role, v_limit
	    FROM public.profiles WHERE id = NEW.created_by;
	
	    IF v_creator_role = 'encargado' THEN
	        SELECT COUNT(*) INTO v_current_count FROM public.stores WHERE created_by = NEW.created_by;
	        IF v_current_count >= v_limit THEN
	            RAISE EXCEPTION 'ERR_STORE_LIMIT_EXCEEDED: Maximum number of stores reached for this manager (%/%)', v_current_count, v_limit;
	        END IF;
	    END IF;
	    RETURN NEW;
	END;
	$$ LANGUAGE plpgsql
2026-09-13 06:19:28.480 UTC [25183] ERROR:  type "user_role" does not exist at character 115
2026-09-13 06:19:28.480 UTC [25183] STATEMENT:  CREATE OR REPLACE FUNCTION public.enforce_encargado_user_limit()
	RETURNS TRIGGER AS $$
	DECLARE
	    v_creator_role user_role;
	    v_limit integer;
	    v_current_count integer;
	BEGIN
	    IF NEW.created_by IS NULL THEN RETURN NEW; END IF;
	
	    SELECT role, max_users_limit INTO v_creator_role, v_limit
	    FROM public.profiles WHERE id = NEW.created_by;
	
	    IF v_creator_role = 'encargado' THEN
	        SELECT COUNT(*) INTO v_current_count FROM public.profiles WHERE created_by = NEW.created_by;
	        IF v_current_count >= v_limit THEN
	            RAISE EXCEPTION 'ERR_USER_LIMIT_EXCEEDED: Maximum number of users reached for this manager (%/%)', v_current_count, v_limit;
	        END IF;
	    END IF;
	    RETURN NEW;
	END;
	$$ LANGUAGE plpgsql
2026-09-13 06:19:28.540 UTC [25198] ERROR:  syntax error at or near "REVOKE" at character 1436
2026-09-13 06:19:28.540 UTC [25198] STATEMENT:  CREATE OR REPLACE FUNCTION public.generate_bulk_confirmation_token(p_store_ids uuid[], p_action text, p_user_id uuid)
	 RETURNS text
	 LANGUAGE plpgsql
	 SECURITY DEFINER
	 SET search_path TO 'public'
	AS $function$
	DECLARE
	  v_token TEXT;
	  v_has_protected BOOLEAN;
	  v_caller_role TEXT;
	BEGIN
	  -- AUTH CHECK: Solo admin
	  IF auth.uid() IS NOT NULL THEN
	    SELECT role INTO v_caller_role FROM public.profiles WHERE id = auth.uid();
	    IF v_caller_role IS NULL OR v_caller_role != 'admin' THEN
	      RAISE EXCEPTION 'ERR_PERMISSION_DENIED: Solo admin puede generar tokens bulk';
	    END IF;
	    -- Verificar que p_user_id coincide con auth.uid()
	    IF p_user_id != auth.uid() THEN
	      RAISE EXCEPTION 'ERR_PERMISSION_DENIED: p_user_id debe coincidir con el usuario autenticado';
	    END IF;
	  END IF;
	
	  IF p_action NOT IN ('delete', 'archive') THEN
	    RAISE EXCEPTION 'ERR_NON_DESTRUCTIVE_ACTION';
	  END IF;
	
	  SELECT EXISTS(
	    SELECT 1 FROM public.stores
	    WHERE id = ANY(p_store_ids) AND backup_restore_protected = true
	  ) INTO v_has_protected;
	
	  v_token := 'bct_' || replace(gen_random_uuid()::text, '-', '');
	
	  INSERT INTO public.bulk_confirmation_tokens (
	    token, store_ids, action, created_by, expires_at, metadata
	  ) VALUES (
	    v_token, p_store_ids, p_action, p_user_id,
	    NOW() + INTERVAL '10 minutes',
	    jsonb_build_object('has_protected_stores', v_has_protected)
	  );
	
	  RETURN v_token;
	END;
	$function$
	 
	REVOKE EXECUTE ON FUNCTION public.generate_bulk_confirmation_token FROM PUBLIC, anon, authenticated
2026-09-13 06:19:28.562 UTC [25203] ERROR:  syntax error at or near "REVOKE" at character 2432
2026-09-13 06:19:28.562 UTC [25203] STATEMENT:  CREATE OR REPLACE FUNCTION public.generate_bulk_override_token(p_confirmation_token text, p_override_user_id uuid, p_reason text DEFAULT NULL::text)
	 RETURNS text
	 LANGUAGE plpgsql
	 SECURITY DEFINER
	 SET search_path TO 'public'
	AS $function$
	DECLARE
	  v_original RECORD;
	  v_override_token TEXT;
	  v_override_user_role TEXT;
	BEGIN
	  -- AUTH CHECK: Solo admin, y debe ser auth.uid() == p_override_user_id
	  IF auth.uid() IS NOT NULL THEN
	    IF auth.uid() != p_override_user_id THEN
	      RAISE EXCEPTION 'ERR_PERMISSION_DENIED: p_override_user_id debe coincidir con el usuario autenticado';
	    END IF;
	
	    SELECT role INTO v_override_user_role FROM public.profiles WHERE id = p_override_user_id;
	    IF v_override_user_role IS NULL OR v_override_user_role != 'admin' THEN
	      RAISE EXCEPTION 'ERR_PERMISSION_DENIED: Solo admin puede generar override';
	    END IF;
	  END IF;
	
	  SELECT * INTO v_original
	  FROM public.bulk_confirmation_tokens
	  WHERE token = p_confirmation_token
	    AND consumed_at IS NULL
	    AND expires_at > NOW()
	    AND is_override = false;
	
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_INVALID_OR_EXPIRED_TOKEN';
	  END IF;
	
	  IF v_original.created_by = p_override_user_id THEN
	    RAISE EXCEPTION 'ERR_SAME_USER_OVERRIDE';
	  END IF;
	
	  IF EXISTS(
	    SELECT 1 FROM public.bulk_confirmation_tokens
	    WHERE override_for = p_confirmation_token
	      AND consumed_at IS NULL AND expires_at > NOW()
	  ) THEN
	    RAISE EXCEPTION 'ERR_OVERRIDE_ALREADY_EXISTS';
	  END IF;
	
	  v_override_token := 'bot_' || replace(gen_random_uuid()::text, '-', '');
	
	  INSERT INTO public.bulk_confirmation_tokens (
	    token, store_ids, action, created_by, expires_at,
	    is_override, override_for, metadata
	  ) VALUES (
	    v_override_token, v_original.store_ids, v_original.action, p_override_user_id,
	    NOW() + INTERVAL '10 minutes', true, p_confirmation_token,
	    jsonb_build_object('override_reason', p_reason,
	      'original_created_by', v_original.created_by,
	      'override_created_by', p_override_user_id)
	  );
	
	  INSERT INTO public.audit_logs (action, table_name, record_id, metadata)
	  VALUES (
	    'bulk_override_token_generated', 'stores', NULL,
	    jsonb_build_object('confirmation_token', p_confirmation_token,
	      'store_ids', v_original.store_ids,
	      'override_by', p_override_user_id,
	      'original_by', v_original.created_by, 'reason', p_reason)
	  );
	
	  RETURN v_override_token;
	END;
	$function$
	 
	REVOKE EXECUTE ON FUNCTION public.generate_bulk_override_token FROM PUBLIC, anon, authenticated
2026-09-13 06:19:28.672 UTC [25213] ERROR:  relation "public.backup_table_registry" does not exist at character 581
2026-09-13 06:19:28.672 UTC [25213] STATEMENT:  CREATE OR REPLACE FUNCTION public.get_backup_table_list(
	  p_include_excluded BOOLEAN DEFAULT FALSE
	)
	RETURNS TABLE (
	  table_name          TEXT,
	  tier                INTEGER,
	  filter_strategy     TEXT,
	  parent_table        TEXT,
	  parent_foreign_key  TEXT,
	  date_column         TEXT,
	  excluded_from_restore BOOLEAN,
	  exclude_reason      TEXT
	)
	LANGUAGE sql
	SECURITY DEFINER
	SET search_path = public
	AS $$
	  SELECT
	    table_name,
	    tier,
	    filter_strategy,
	    parent_table,
	    parent_foreign_key,
	    date_column,
	    excluded_from_restore,
	    exclude_reason
	  FROM public.backup_table_registry
	  WHERE (p_include_excluded OR excluded_from_restore = FALSE)
	  ORDER BY tier ASC, table_name ASC;
	$$
2026-09-13 06:19:28.862 UTC [25223] ERROR:  relation "v_global_operation_dates" does not exist at character 229
2026-09-13 06:19:28.862 UTC [25223] STATEMENT:  CREATE OR REPLACE FUNCTION public.get_global_max_operation_date(
	  p_store_id UUID DEFAULT NULL
	)
	RETURNS TIMESTAMP WITH TIME ZONE
	LANGUAGE sql
	SECURITY DEFINER
	SET search_path = public
	AS $$
	  SELECT MAX(operation_date)
	  FROM v_global_operation_dates
	  WHERE p_store_id IS NULL OR store_id = p_store_id;
	$$
2026-09-13 06:19:28.939 UTC [25228] ERROR:  relation "exchange_rates" does not exist at character 216
2026-09-13 06:19:28.939 UTC [25228] STATEMENT:  CREATE OR REPLACE FUNCTION get_latest_rate(p_currency TEXT, p_source TEXT)
	RETURNS TABLE(rate NUMERIC, rate_date DATE, captured_at TIMESTAMPTZ)
	LANGUAGE sql STABLE AS $$
	  SELECT rate, rate_date, captured_at
	  FROM exchange_rates
	  WHERE currency = p_currency AND source = p_source
	  ORDER BY rate_date DESC, captured_at DESC
	  LIMIT 1;
	$$
2026-09-13 06:19:28.962 UTC [25233] ERROR:  relation "public.products" does not exist at character 194
2026-09-13 06:19:28.962 UTC [25233] STATEMENT:  CREATE OR REPLACE FUNCTION public.get_low_stock_count(p_store_id uuid DEFAULT NULL)
	RETURNS bigint
	LANGUAGE sql
	SECURITY DEFINER
	SET search_path = public
	AS $$
	  SELECT COUNT(*)::bigint
	  FROM public.products
	  WHERE
	    (p_store_id IS NULL OR store_id = p_store_id)
	    AND is_active = true
	    AND stock_current > 0
	    AND min_stock IS NOT NULL
	    AND min_stock > 0
	    AND stock_current <= min_stock;
	$$
2026-09-13 06:19:29.064 UTC [25242] ERROR:  relation "receipt_items" does not exist at character 754
2026-09-13 06:19:29.064 UTC [25242] STATEMENT:  CREATE OR REPLACE FUNCTION get_product_cost_analysis(
	  p_product_id UUID,
	  p_store_id UUID
	) RETURNS TABLE(
	  receipt_id UUID,
	  receipt_date TIMESTAMPTZ,
	  quantity INT,
	  unit_cost FLOAT,
	  service_type TEXT,
	  service_amount NUMERIC,
	  total_cost NUMERIC,
	  unit_cost_final NUMERIC
	) LANGUAGE sql SECURITY DEFINER AS $$
	  SELECT 
	    ri.receipt_id,
	    r.reception_date,
	    ri.quantity,
	    ri.unit_cost,
	    st.name AS service_type,
	    COALESCE(scd.distribution_amount, 0) AS service_amount,
	    ri.unit_cost * ri.quantity + COALESCE(scd.distribution_amount, 0) AS total_cost,
	    CASE WHEN ri.quantity > 0 
	      THEN (ri.unit_cost * ri.quantity + COALESCE(scd.distribution_amount, 0)) / ri.quantity 
	      ELSE 0 END AS unit_cost_final
	  FROM receipt_items ri
	  JOIN receipts r ON r.id = ri.receipt_id
	  LEFT JOIN service_cost_distributions scd ON scd.receipt_item_id = ri.id
	  LEFT JOIN received_services rs ON rs.id = scd.service_id AND rs.status = 'active'
	  LEFT JOIN service_types st ON st.id = rs.service_type_id
	  WHERE ri.product_id = p_product_id
	    AND r.store_id = p_store_id
	    AND r.status = 'active'
	  ORDER BY r.reception_date DESC;
	$$
2026-09-13 06:19:29.101 UTC [25250] ERROR:  relation "exchange_rates" does not exist at character 252
2026-09-13 06:19:29.101 UTC [25250] STATEMENT:  CREATE OR REPLACE FUNCTION get_rate_history(
	  p_currency TEXT,
	  p_source TEXT,
	  p_days INT DEFAULT 30
	)
	RETURNS TABLE(rate_date DATE, rate NUMERIC, variation_daily NUMERIC)
	LANGUAGE sql STABLE AS $$
	  SELECT rate_date, rate, variation_daily
	  FROM exchange_rates
	  WHERE currency = p_currency AND source = p_source
	    AND rate_date >= CURRENT_DATE - p_days
	  ORDER BY rate_date ASC;
	$$
2026-09-13 06:19:29.125 UTC [25255] ERROR:  relation "exchange_rates" does not exist at character 363
2026-09-13 06:19:29.125 UTC [25255] STATEMENT:  CREATE OR REPLACE FUNCTION get_rate_variation(
	  p_currency TEXT,
	  p_source TEXT,
	  p_start_date DATE,
	  p_end_date DATE
	)
	RETURNS TABLE(
	  start_rate NUMERIC,
	  end_rate NUMERIC,
	  absolute_change NUMERIC,
	  percent_change NUMERIC,
	  daily_avg_growth NUMERIC,
	  monthly_avg_growth NUMERIC
	)
	LANGUAGE sql STABLE AS $$
	  WITH start_val AS (
	    SELECT rate FROM exchange_rates
	    WHERE currency = p_currency AND source = p_source AND rate_date <= p_start_date
	    ORDER BY rate_date DESC LIMIT 1
	  ),
	  end_val AS (
	    SELECT rate FROM exchange_rates
	    WHERE currency = p_currency AND source = p_source AND rate_date <= p_end_date
	    ORDER BY rate_date DESC LIMIT 1
	  )
	  SELECT
	    COALESCE(sv.rate, 0) AS start_rate,
	    COALESCE(ev.rate, 0) AS end_rate,
	    (COALESCE(ev.rate, 0) - COALESCE(sv.rate, 0)) AS absolute_change,
	    CASE WHEN sv.rate IS NOT NULL AND sv.rate > 0 THEN ((ev.rate - sv.rate) / sv.rate * 100) ELSE 0 END AS percent_change,
	    CASE WHEN sv.rate IS NOT NULL AND sv.rate > 0 AND ev.rate IS NOT NULL AND ev.rate > 0 AND p_end_date > p_start_date
	      THEN (POWER(ev.rate::FLOAT / sv.rate::FLOAT, 1.0 / EXTRACT(EPOCH FROM (p_end_date - p_start_date)) * 86400) - 1) * 100
	      ELSE 0 END AS daily_avg_growth,
	    CASE WHEN sv.rate IS NOT NULL AND sv.rate > 0 AND ev.rate IS NOT NULL AND ev.rate > 0 AND p_end_date > p_start_date
	      THEN (POWER(ev.rate::FLOAT / sv.rate::FLOAT, 30.44 / EXTRACT(EPOCH FROM (p_end_date - p_start_date)) * 86400) - 1) * 100
	      ELSE 0 END AS monthly_avg_growth
	  FROM start_val sv, end_val ev;
	$$
2026-09-13 06:19:29.182 UTC [25269] ERROR:  type "public.stores" does not exist
2026-09-13 06:19:29.182 UTC [25269] STATEMENT:  CREATE OR REPLACE FUNCTION public.get_transferable_stores(
	  p_user_id UUID,
	  p_current_store_id UUID
	)
	RETURNS SETOF public.stores
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO 'public'
	AS $$
	DECLARE
	  v_tenant_id UUID;
	BEGIN
	  -- 1. Obtener tenant_id de la tienda actual
	  SELECT tenant_id INTO v_tenant_id
	  FROM public.stores
	  WHERE id = p_current_store_id AND is_active = true;
	
	  -- 2. Devolver tiendas donde el caller tiene acceso (has_store_access_as)
	  --    EXCLUYENDO la tienda actual
	  --    Si tenant_id es NOT NULL, filtrar por mismo tenant.
	  --    Si tenant_id es NULL (legacy), mostrar todas las que el caller tiene acceso.
	  IF v_tenant_id IS NOT NULL THEN
	    RETURN QUERY
	    SELECT s.*
	    FROM public.stores s
	    WHERE s.tenant_id = v_tenant_id
	      AND s.id != p_current_store_id
	      AND s.is_active = true
	      AND s.is_archived = false
	      AND public.has_store_access_as(p_user_id, s.id)
	    ORDER BY s.name;
	  ELSE
	    -- Legacy: tiendas sin tenant_id — usar solo has_store_access_as
	    RETURN QUERY
	    SELECT s.*
	    FROM public.stores s
	    WHERE s.id != p_current_store_id
	      AND s.is_active = true
	      AND s.is_archived = false
	      AND public.has_store_access_as(p_user_id, s.id)
	    ORDER BY s.name;
	  END IF;
	END;
	$$
2026-09-13 06:19:29.228 UTC [25280] ERROR:  type user_role[] does not exist
2026-09-13 06:19:29.228 UTC [25280] STATEMENT:  CREATE OR REPLACE FUNCTION public.has_any_role(required_roles user_role[])
	 RETURNS boolean
	 LANGUAGE plpgsql
	 STABLE SECURITY DEFINER
	AS $$
	DECLARE
	    v_actual_role user_role;
	    r user_role;
	BEGIN
	    SELECT role INTO v_actual_role FROM public.profiles WHERE id = auth.uid();
	
	    FOREACH r IN ARRAY required_roles
	    LOOP
	        IF v_actual_role = r THEN RETURN true; END IF;
	        IF v_actual_role = 'encargado' AND r = 'manager' THEN RETURN true; END IF;
	        IF v_actual_role = 'usuario' AND (r = 'clerk' OR r = 'warehouse') THEN RETURN true; END IF;
	    END LOOP;
	
	    RETURN false;
	END;
	$$
2026-09-13 06:19:29.252 UTC [25285] ERROR:  relation "profiles" does not exist at character 461
2026-09-13 06:19:29.252 UTC [25285] STATEMENT:  CREATE OR REPLACE FUNCTION public.has_management_access_as(
	  p_user_id uuid,
	  p_store_id uuid
	) RETURNS boolean
	LANGUAGE sql SECURITY DEFINER
	SET search_path = public, pg_temp
	AS $$
	  -- service_role always passes
	  SELECT CASE WHEN auth.role() = 'service_role' THEN true
	    ELSE
	      -- Check if user has admin/manager/encargado role in their profile
	      -- OR has a membership with manager/admin role for the store
	      EXISTS (
	        SELECT 1 FROM profiles
	        WHERE id = p_user_id
	        AND role IN ('admin', 'manager', 'encargado')
	      )
	      OR EXISTS (
	        SELECT 1 FROM user_store_memberships
	        WHERE user_id = p_user_id
	        AND store_id = p_store_id
	        AND status = 'active'
	        AND role IN ('admin', 'manager')
	      )
	    END
	$$
2026-09-13 06:19:29.274 UTC [25290] ERROR:  type public.user_role does not exist
2026-09-13 06:19:29.274 UTC [25290] STATEMENT:  CREATE OR REPLACE FUNCTION public.has_role(p_user_id UUID, p_required_role public.user_role)
	 RETURNS boolean
	 LANGUAGE plpgsql
	 STABLE SECURITY DEFINER
	AS $$
	DECLARE
	    v_actual_role public.user_role;
	BEGIN
	    SELECT role INTO v_actual_role FROM public.profiles WHERE id = p_user_id;
	
	    IF v_actual_role IS NULL THEN
	        RETURN false;
	    END IF;
	
	    -- Compatibility mapping (mirroring the 1-arg version)
	    IF v_actual_role = 'encargado' AND p_required_role = 'manager' THEN RETURN true; END IF;
	    IF v_actual_role = 'usuario' AND (p_required_role = 'clerk' OR p_required_role = 'warehouse') THEN RETURN true; END IF;
	
	    RETURN v_actual_role = p_required_role;
	END;
	$$
2026-09-13 06:19:29.305 UTC [25297] ERROR:  relation "public.user_store_memberships" does not exist at character 279
2026-09-13 06:19:29.305 UTC [25297] STATEMENT:  CREATE OR REPLACE FUNCTION public.has_store_role(
	  p_user_id uuid,
	  p_store_id uuid,
	  p_roles text[]
	) RETURNS boolean
	LANGUAGE sql SECURITY DEFINER
	SET search_path = public, pg_temp
	AS $$
	  SELECT CASE WHEN auth.role() = 'service_role' THEN
	    EXISTS (
	      SELECT 1 FROM public.user_store_memberships m
	      WHERE m.user_id = p_user_id
	        AND m.store_id = p_store_id
	        AND m.status = 'active'
	        AND m.role::text = ANY(p_roles)
	    )
	  ELSE
	    EXISTS (
	      SELECT 1 FROM public.user_store_memberships m
	      WHERE m.user_id = auth.uid()
	        AND m.store_id = p_store_id
	        AND m.status = 'active'
	        AND m.role::text = ANY(p_roles)
	    )
	  END
	$$
2026-09-13 06:19:29.340 UTC [25305] ERROR:  relation "public.profiles" does not exist at character 173
2026-09-13 06:19:29.340 UTC [25305] STATEMENT:  CREATE OR REPLACE FUNCTION public.is_global_admin()
	RETURNS BOOLEAN
	LANGUAGE sql
	STABLE
	SECURITY DEFINER
	SET search_path = public
	AS $$
	  SELECT EXISTS (
	    SELECT 1 FROM public.profiles
	    WHERE id = auth.uid()
	      AND role = 'admin'
	  );
	$$
2026-09-13 06:19:29.367 UTC [25311] ERROR:  type user_role does not exist
2026-09-13 06:19:29.367 UTC [25311] STATEMENT:  CREATE OR REPLACE FUNCTION public.is_role_not_changed(p_user_id uuid, p_new_role user_role, p_new_role_id uuid)
	RETURNS boolean
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path = public, extensions
	AS $$
	DECLARE
	  v_old_role user_role;
	  v_old_role_id uuid;
	BEGIN
	  SELECT role, role_id INTO v_old_role, v_old_role_id
	  FROM public.profiles
	  WHERE id = p_user_id;
	
	  RETURN (p_new_role IS NOT DISTINCT FROM v_old_role)
	     AND (p_new_role_id IS NOT DISTINCT FROM v_old_role_id);
	END;
	$$
2026-09-13 06:19:29.396 UTC [25317] ERROR:  relation "public.user_store_memberships" does not exist at character 188
2026-09-13 06:19:29.396 UTC [25317] STATEMENT:  CREATE OR REPLACE FUNCTION public.is_store_member(p_store_id UUID)
	RETURNS BOOLEAN
	LANGUAGE sql
	STABLE
	SECURITY DEFINER
	SET search_path = public
	AS $$
	  SELECT EXISTS (
	    SELECT 1 FROM public.user_store_memberships
	    WHERE store_id = p_store_id
	      AND user_id = auth.uid()
	      AND status = 'active'
	  );
	$$
2026-09-13 06:19:29.419 UTC [25322] ERROR:  function public.current_user_tenant_id() does not exist at character 209
2026-09-13 06:19:29.419 UTC [25322] HINT:  No function matches the given name and argument types. You might need to add explicit type casts.
2026-09-13 06:19:29.419 UTC [25322] STATEMENT:  CREATE OR REPLACE FUNCTION public.is_tenant_member(p_tenant_id uuid)
	RETURNS boolean
	LANGUAGE sql
	STABLE
	SECURITY DEFINER
	SET search_path TO public
	AS $$
	  SELECT p_tenant_id IS NOT NULL
	   AND p_tenant_id = public.current_user_tenant_id();
	$$
2026-09-13 06:19:29.467 UTC [25334] ERROR:  type user_role does not exist
2026-09-13 06:19:29.467 UTC [25334] STATEMENT:  CREATE OR REPLACE FUNCTION public.managed_create_user(p_email text, p_full_name text, p_role user_role, p_store_id uuid DEFAULT NULL::uuid, p_memberships jsonb DEFAULT NULL::jsonb, p_max_stores integer DEFAULT 0, p_max_users integer DEFAULT 0, p_target_user_id uuid DEFAULT NULL::uuid)
	 RETURNS jsonb
	 LANGUAGE plpgsql
	 SECURITY DEFINER
	 SET search_path TO 'public', 'extensions'
	AS $function$
	DECLARE
	    v_user_id uuid;
	    v_role_id uuid;
	    v_active_store_id uuid;
	    m JSONB;
	    v_creator_role user_role;
	BEGIN
	    SELECT role INTO v_creator_role FROM public.profiles WHERE id = auth.uid();
	    IF v_creator_role IS NULL OR v_creator_role NOT IN ('admin', 'encargado') THEN
	        RAISE EXCEPTION 'ERR_UNAUTHORIZED: Only admins and managers can create users.';
	    END IF;
	
	    v_active_store_id := COALESCE((p_memberships->0->>'store_id')::UUID, p_store_id);
	
	    IF v_creator_role = 'encargado' AND v_active_store_id IS NOT NULL THEN
	        IF NOT public.has_store_access(v_active_store_id) THEN
	            RAISE EXCEPTION 'ERR_UNAUTHORIZED: No access to store %', v_active_store_id;
	        END IF;
	    END IF;
	
	    SELECT id INTO v_role_id FROM public.roles WHERE lower(name) = lower(p_role::text) OR (name = 'Cajero' AND p_role = 'clerk') OR (name = 'Almacenero' AND p_role = 'warehouse') LIMIT 1;
	    v_user_id := COALESCE(p_target_user_id, gen_random_uuid());
	
	    INSERT INTO public.profiles (id, email, full_name, role, role_id, active_store_id, is_active, created_by, max_stores_limit, max_users_limit, created_at, updated_at)
	    VALUES (v_user_id, p_email, p_full_name, p_role, v_role_id, v_active_store_id, true, auth.uid(), p_max_stores, p_max_users, now(), now())
	    ON CONFLICT (id) DO UPDATE SET email = EXCLUDED.email, full_name = EXCLUDED.full_name, role = EXCLUDED.role, role_id = EXCLUDED.role_id, active_store_id = EXCLUDED.active_store_id, updated_at = now()
	    RETURNING id INTO v_user_id;
	
	    IF p_memberships IS NOT NULL THEN
	        IF v_creator_role != 'admin' THEN
	            DELETE FROM public.user_store_memberships WHERE user_id = v_user_id AND store_id IN (SELECT store_id FROM public.user_store_memberships WHERE user_id = auth.uid() AND role IN ('encargado', 'manager'));
	        ELSE
	            DELETE FROM public.user_store_memberships WHERE user_id = v_user_id;
	        END IF;
	        FOR m IN SELECT * FROM jsonb_array_elements(p_memberships) LOOP
	            IF (m->>'store_id') IS NOT NULL AND (m->>'store_id') <> '' THEN
	                IF v_creator_role = 'admin' OR public.has_store_access((m->>'store_id')::UUID) THEN
	                    INSERT INTO public.user_store_memberships (user_id, store_id, role) VALUES (v_user_id, (m->>'store_id')::UUID, (m->>'role')::user_role) ON CONFLICT (user_id, store_id) DO UPDATE SET role = EXCLUDED.role;
	                END IF;
	            END IF;
	        END LOOP;
	    ELSIF p_store_id IS NOT NULL THEN
	        INSERT INTO public.user_store_memberships (user_id, store_id, role) VALUES (v_user_id, p_store_id, p_role) ON CONFLICT (user_id, store_id) DO UPDATE SET role = EXCLUDED.role;
	    END IF;
	    RETURN jsonb_build_object('success', true, 'user_id', v_user_id);
	END;
	$function$
2026-09-13 06:19:29.490 UTC [25339] ERROR:  type public.user_role does not exist
2026-09-13 06:19:29.490 UTC [25339] STATEMENT:  CREATE OR REPLACE FUNCTION public.managed_create_user_v2(
	  p_email text,
	  p_full_name text,
	  p_role public.user_role,
	  p_plan plan_t DEFAULT 'free'::plan_t,
	  p_store_id uuid DEFAULT NULL::uuid,
	  p_memberships jsonb DEFAULT NULL::jsonb,
	  p_max_stores int DEFAULT 0,
	  p_max_users int DEFAULT 0,
	  p_target_user_id uuid DEFAULT NULL::uuid,
	  p_creator_id uuid DEFAULT NULL::uuid
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  v_user_id uuid := COALESCE(p_target_user_id, gen_random_uuid());
	  v_role_id uuid;
	  v_active_store_id uuid;
	  m JSONB;
	  v_creator_role public.user_role;
	  v_creator_uid uuid := COALESCE(p_creator_id, auth.uid());
	BEGIN
	  -- Validar caller es admin o encargado
	  SELECT role INTO v_creator_role FROM public.profiles WHERE id = v_creator_uid;
	  IF v_creator_role IS NULL OR v_creator_role NOT IN ('admin', 'encargado', 'superadmin') THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED: Only admins and managers can create users.';
	  END IF;
	
	  -- Validar email único (entre perfiles activos)
	  IF EXISTS (SELECT 1 FROM public.profiles WHERE email = p_email AND deleted_at IS NULL) THEN
	    RAISE EXCEPTION 'ERR_EMAIL_ALREADY_EXISTS: %', p_email;
	  END IF;
	
	  v_active_store_id := COALESCE((p_memberships->0->>'store_id')::UUID, p_store_id);
	
	  -- Encargado solo puede crear en tiendas que gestiona
	  IF v_creator_role = 'encargado' AND v_active_store_id IS NOT NULL THEN
	    IF NOT public.has_store_access(v_active_store_id) THEN
	      RAISE EXCEPTION 'ERR_UNAUTHORIZED: No access to store %', v_active_store_id;
	    END IF;
	  END IF;
	
	  -- Buscar role_id
	  SELECT id INTO v_role_id FROM public.roles
	    WHERE lower(name) = lower(p_role::text)
	       OR (name = 'Cajero' AND p_role = 'clerk')
	       OR (name = 'Almacenero' AND p_role = 'warehouse')
	       OR (name = 'UserCosto' AND p_role = 'costo')
	    LIMIT 1;
	
	  -- INSERT profiles con plan
	  INSERT INTO public.profiles (
	    id, email, full_name, role, role_id, active_store_id, is_active,
	    created_by, max_stores_limit, max_users_limit, plan, created_at, updated_at
	  ) VALUES (
	    v_user_id, p_email, p_full_name, p_role, v_role_id, v_active_store_id, true,
	    v_creator_uid, p_max_stores, p_max_users, p_plan, now(), now()
	  )
	  ON CONFLICT (id) DO UPDATE SET
	    email = EXCLUDED.email,
	    full_name = EXCLUDED.full_name,
	    role = EXCLUDED.role,
	    role_id = EXCLUDED.role_id,
	    active_store_id = EXCLUDED.active_store_id,
	    plan = EXCLUDED.plan,
	    is_active = true,
	    deleted_at = NULL,
	    deletion_reason = NULL,
	    deleted_by = NULL,
	    updated_at = now()
	  RETURNING id INTO v_user_id;
	
	  -- Procesar memberships
	  IF p_memberships IS NOT NULL THEN
	    IF v_creator_role != 'admin' AND v_creator_role != 'superadmin' THEN
	      DELETE FROM public.user_store_memberships
	        WHERE user_id = v_user_id
	        AND store_id IN (
	          SELECT store_id FROM public.user_store_memberships
	          WHERE user_id = v_creator_uid AND role IN ('encargado', 'manager')
	        );
	    ELSE
	      DELETE FROM public.user_store_memberships WHERE user_id = v_user_id;
	    END IF;
	
	    FOR m IN SELECT * FROM jsonb_array_elements(p_memberships) LOOP
	      IF (m->>'store_id') IS NOT NULL AND (m->>'store_id') <> '' THEN
	        IF v_creator_role IN ('admin', 'superadmin') OR public.has_store_access((m->>'store_id')::UUID) THEN
	          INSERT INTO public.user_store_memberships (user_id, store_id, role)
	          VALUES (v_user_id, (m->>'store_id')::UUID, (m->>'role')::public.user_role)
	          ON CONFLICT (user_id, store_id) DO UPDATE SET role = EXCLUDED.role, status = 'active';
	        END IF;
	      END IF;
	    END LOOP;
	  ELSIF p_store_id IS NOT NULL THEN
	    INSERT INTO public.user_store_memberships (user_id, store_id, role)
	    VALUES (v_user_id, p_store_id, p_role)
	    ON CONFLICT (user_id, store_id) DO UPDATE SET role = EXCLUDED.role, status = 'active';
	  END IF;
	
	  -- Audit log atómico (regresión C-2 restaurada)
	  INSERT INTO public.user_audit_log (performed_by, target_user_id, action, new_values, metadata)
	  VALUES (
	    v_creator_uid, v_user_id, 'USER_CREATED',
	    jsonb_build_object(
	      'email', p_email, 'full_name', p_full_name, 'role', p_role::text,
	      'plan', p_plan::text, 'active_store_id', v_active_store_id,
	      'max_stores', p_max_stores, 'max_users', p_max_users
	    ),
	    jsonb_build_object('memberships_count', CASE WHEN p_memberships IS NOT NULL THEN jsonb_array_length(p_memberships) ELSE 0 END)
	  );
	
	  RETURN jsonb_build_object('success', true, 'user_id', v_user_id);
	END;
	$function$
2026-09-13 06:19:29.535 UTC [25346] ERROR:  type "public.user_role" does not exist at character 392
2026-09-13 06:19:29.535 UTC [25346] STATEMENT:  CREATE OR REPLACE FUNCTION public.managed_reset_password(
	  p_user_id uuid,
	  p_caller_id uuid DEFAULT NULL::uuid
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  -- FIX H-7: anti-spoofing
	  v_caller_uid uuid := CASE WHEN auth.role() = 'service_role'
	    THEN COALESCE(p_caller_id, auth.uid()) ELSE auth.uid() END;
	  v_caller_role public.user_role;
	  v_target_email text;
	BEGIN
	  SELECT role INTO v_caller_role FROM public.profiles WHERE id = v_caller_uid;
	  IF v_caller_role IS NULL OR v_caller_role NOT IN ('admin', 'superadmin') THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED';
	  END IF;
	
	  IF p_user_id = v_caller_uid THEN
	    RAISE EXCEPTION 'ERR_SELF_RESET_BLOCKED';
	  END IF;
	
	  SELECT email INTO v_target_email FROM public.profiles WHERE id = p_user_id AND deleted_at IS NULL;
	  IF v_target_email IS NULL THEN
	    RAISE EXCEPTION 'ERR_USER_NOT_FOUND';
	  END IF;
	
	  INSERT INTO public.user_audit_log (performed_by, target_user_id, action, metadata)
	  VALUES (
	    v_caller_uid, p_user_id, 'PASSWORD_RESET_REQUESTED',
	    jsonb_build_object('email', v_target_email, 'method', 'recovery_link')
	  );
	
	  RETURN jsonb_build_object('success', true, 'email', v_target_email);
	END;
	$function$
2026-09-13 06:19:29.566 UTC [25352] ERROR:  type "public.user_role" does not exist at character 427
2026-09-13 06:19:29.566 UTC [25352] STATEMENT:  CREATE OR REPLACE FUNCTION public.managed_soft_delete_user(
	  p_user_id uuid,
	  p_reason text,
	  p_caller_id uuid DEFAULT NULL::uuid
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  v_old RECORD;
	  -- FIX H-7: anti-spoofing
	  v_caller_uid uuid := CASE WHEN auth.role() = 'service_role'
	    THEN COALESCE(p_caller_id, auth.uid()) ELSE auth.uid() END;
	  v_caller_role public.user_role;
	  v_active_memberships_count int;
	  v_anon_email text;
	BEGIN
	  SELECT role INTO v_caller_role FROM public.profiles WHERE id = v_caller_uid;
	  IF v_caller_role IS NULL OR v_caller_role NOT IN ('admin', 'superadmin') THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED';
	  END IF;
	
	  IF p_user_id = v_caller_uid THEN
	    RAISE EXCEPTION 'ERR_SELF_DELETE_BLOCKED';
	  END IF;
	
	  SELECT id, email, full_name, role, plan, is_active INTO v_old
	    FROM public.profiles WHERE id = p_user_id AND deleted_at IS NULL FOR UPDATE;
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_USER_NOT_FOUND_OR_ALREADY_DELETED';
	  END IF;
	
	  SELECT COUNT(*) INTO v_active_memberships_count
	    FROM public.user_store_memberships
	    WHERE user_id = p_user_id AND status = 'active';
	  IF v_active_memberships_count > 0 THEN
	    RAISE EXCEPTION 'ERR_USER_HAS_ACTIVE_MEMBERSHIPS: % active. Revoke memberships first.', v_active_memberships_count;
	  END IF;
	
	  v_anon_email := 'deleted+' || substr(p_user_id::text, 1, 8) || '@anonymized.local';
	
	  UPDATE public.profiles SET
	    deleted_at = now(),
	    deletion_reason = p_reason,
	    deleted_by = v_caller_uid,
	    is_active = false,
	    full_name = '[deleted user]',
	    email = v_anon_email,
	    ai_api_key = NULL,
	    updated_at = now()
	  WHERE id = p_user_id;
	
	  UPDATE public.user_store_memberships SET
	    status = 'revoked',
	    updated_at = now()
	  WHERE user_id = p_user_id AND status = 'active';
	
	  INSERT INTO public.user_audit_log (performed_by, target_user_id, action, old_values, new_values, metadata)
	  VALUES (
	    v_caller_uid, p_user_id, 'USER_SOFT_DELETED',
	    jsonb_build_object(
	      'email', v_old.email, 'full_name', v_old.full_name,
	      'role', v_old.role::text, 'plan', v_old.plan::text, 'is_active', v_old.is_active
	    ),
	    jsonb_build_object(
	      'email', v_anon_email, 'full_name', '[deleted user]',
	      'is_active', false, 'deleted_at', now()
	    ),
	    jsonb_build_object('reason', p_reason, 'memberships_revoked', v_active_memberships_count)
	  );
	
	  RETURN jsonb_build_object(
	    'success', true,
	    'user_id', p_user_id,
	    'status', 'soft_deleted',
	    'note', 'auth.users preserved. API route should ban via auth.admin.updateUser.'
	  );
	END;
	$function$
2026-09-13 06:19:29.597 UTC [25358] ERROR:  type "public.user_role" does not exist at character 435
2026-09-13 06:19:29.597 UTC [25358] STATEMENT:  CREATE OR REPLACE FUNCTION public.managed_toggle_user_status(
	  p_user_id uuid,
	  p_is_active boolean,
	  p_caller_id uuid DEFAULT NULL::uuid
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  v_old RECORD;
	  -- FIX H-7: anti-spoofing
	  v_caller_uid uuid := CASE WHEN auth.role() = 'service_role'
	    THEN COALESCE(p_caller_id, auth.uid()) ELSE auth.uid() END;
	  v_caller_role public.user_role;
	BEGIN
	  SELECT role INTO v_caller_role FROM public.profiles WHERE id = v_caller_uid;
	  IF v_caller_role IS NULL OR v_caller_role NOT IN ('admin', 'superadmin', 'encargado', 'manager') THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED';
	  END IF;
	
	  IF p_is_active = false AND p_user_id = v_caller_uid THEN
	    RAISE EXCEPTION 'ERR_SELF_DEACTIVATE_BLOCKED';
	  END IF;
	
	  SELECT id, is_active, full_name INTO v_old
	    FROM public.profiles WHERE id = p_user_id AND deleted_at IS NULL FOR UPDATE;
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_USER_NOT_FOUND';
	  END IF;
	
	  IF v_old.is_active = p_is_active THEN
	    RETURN jsonb_build_object('success', true, 'no_change', true);
	  END IF;
	
	  UPDATE public.profiles SET is_active = p_is_active, updated_at = now() WHERE id = p_user_id;
	
	  INSERT INTO public.user_audit_log (performed_by, target_user_id, action, old_values, new_values)
	  VALUES (
	    v_caller_uid, p_user_id,
	    CASE WHEN p_is_active THEN 'USER_ACTIVATED' ELSE 'USER_DEACTIVATED' END,
	    jsonb_build_object('is_active', v_old.is_active),
	    jsonb_build_object('is_active', p_is_active)
	  );
	
	  RETURN jsonb_build_object('success', true, 'user_id', p_user_id, 'is_active', p_is_active);
	END;
	$function$
2026-09-13 06:19:29.620 UTC [25363] ERROR:  type public.user_role does not exist
2026-09-13 06:19:29.620 UTC [25363] STATEMENT:  CREATE OR REPLACE FUNCTION public.managed_update_membership(
	  p_membership_id uuid,
	  p_role public.user_role DEFAULT NULL::user_role,
	  p_status text DEFAULT NULL::text,
	  p_caller_id uuid DEFAULT NULL::uuid
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  v_old RECORD;
	  -- FIX H-7: anti-spoofing
	  v_caller_uid uuid := CASE WHEN auth.role() = 'service_role'
	    THEN COALESCE(p_caller_id, auth.uid()) ELSE auth.uid() END;
	  v_changes jsonb := '{}'::jsonb;
	BEGIN
	  SELECT m.id, m.user_id, m.store_id, m.role, m.status
	    INTO v_old
	    FROM public.user_store_memberships m
	    WHERE m.id = p_membership_id
	    FOR UPDATE;
	
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_MEMBERSHIP_NOT_FOUND';
	  END IF;
	
	  -- FIX H-7: Autorización usa nueva sobrecarga has_store_role(p_user_id, p_store_id, p_roles)
	  -- service_role (API route) pasa p_caller_id=session.user.id → verifica acceso de ese user
	  -- authenticated pasa p_caller_id=NULL → v_caller_uid=auth.uid() → verifica su propio acceso
	  IF NOT public.has_store_role(v_caller_uid, v_old.store_id, ARRAY['admin', 'manager']) THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED: Caller must be admin or manager of the store.';
	  END IF;
	
	  IF p_role IS NOT NULL AND p_role <> v_old.role THEN
	    v_changes := v_changes || jsonb_build_object('role', jsonb_build_object('old', v_old.role::text, 'new', p_role::text));
	    UPDATE public.user_store_memberships SET role = p_role, updated_at = now() WHERE id = p_membership_id;
	  END IF;
	
	  IF p_status IS NOT NULL AND p_status <> v_old.status::text THEN
	    IF p_status NOT IN ('active', 'revoked') THEN
	      RAISE EXCEPTION 'ERR_INVALID_STATUS: %', p_status;
	    END IF;
	    v_changes := v_changes || jsonb_build_object('status', jsonb_build_object('old', v_old.status, 'new', p_status));
	    UPDATE public.user_store_memberships SET status = p_status::membership_status, updated_at = now() WHERE id = p_membership_id;
	  END IF;
	
	  IF v_changes <> '{}'::jsonb THEN
	    INSERT INTO public.user_audit_log (performed_by, target_user_id, action, old_values, new_values, metadata)
	    VALUES (
	      v_caller_uid, v_old.user_id, 'MEMBERSHIP_UPDATED',
	      jsonb_build_object('membership_id', p_membership_id, 'store_id', v_old.store_id, 'role', v_old.role::text, 'status', v_old.status),
	      v_changes,
	      jsonb_build_object('store_id', v_old.store_id)
	    );
	  END IF;
	
	  RETURN jsonb_build_object('success', true, 'membership_id', p_membership_id, 'changes', v_changes);
	END;
	$function$
2026-09-13 06:19:29.643 UTC [25368] ERROR:  type plan_t does not exist
2026-09-13 06:19:29.643 UTC [25368] STATEMENT:  CREATE OR REPLACE FUNCTION public.managed_update_tenant_plan(
	  p_tenant_id uuid,
	  p_plan plan_t,
	  p_subscription_status text DEFAULT NULL,
	  p_caller_id uuid DEFAULT NULL
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  -- FIX H-7: anti-spoofing — service_role (Stripe webhook) puede pasar NULL caller_id
	  v_caller_uid uuid := CASE WHEN auth.role() = 'service_role'
	    THEN COALESCE(p_caller_id, auth.uid()) ELSE auth.uid() END;
	  v_old_plan plan_t;
	  v_old_status text;
	  v_role public.user_role;
	BEGIN
	  -- Validación: si caller_uid es NULL (service_role anónimo para Stripe webhook), permitir
	  -- Si caller_uid NO es NULL, debe ser admin/superadmin
	  IF v_caller_uid IS NOT NULL THEN
	    SELECT role INTO v_role FROM public.profiles WHERE id = v_caller_uid;
	    IF v_role IS NULL OR v_role NOT IN ('admin', 'superadmin') THEN
	      RAISE EXCEPTION 'ERR_UNAUTHORIZED: Only admins can change tenant plan';
	    END IF;
	  END IF;
	
	  SELECT plan, subscription_status INTO v_old_plan, v_old_status
	    FROM public.tenants WHERE id = p_tenant_id FOR UPDATE;
	
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_TENANT_NOT_FOUND: %', p_tenant_id;
	  END IF;
	
	  UPDATE public.tenants SET
	    plan = p_plan,
	    subscription_status = COALESCE(p_subscription_status, subscription_status),
	    updated_at = now()
	  WHERE id = p_tenant_id;
	
	  UPDATE public.profiles SET
	    plan = p_plan,
	    updated_at = now()
	  WHERE tenant_id = p_tenant_id AND deleted_at IS NULL;
	
	  INSERT INTO public.user_audit_log (performed_by, target_user_id, action, old_values, new_values, metadata)
	  VALUES (
	    v_caller_uid, NULL, 'TENANT_PLAN_UPDATED',
	    jsonb_build_object('old_plan', v_old_plan::text, 'old_status', v_old_status),
	    jsonb_build_object('new_plan', p_plan::text, 'new_status', COALESCE(p_subscription_status, v_old_status)),
	    jsonb_build_object('tenant_id', p_tenant_id)
	  );
	
	  RETURN jsonb_build_object(
	    'success', true,
	    'tenant_id', p_tenant_id,
	    'old_plan', v_old_plan::text,
	    'new_plan', p_plan::text
	  );
	END;
	$function$
2026-09-13 06:19:29.666 UTC [25373] ERROR:  type public.user_role does not exist
2026-09-13 06:19:29.666 UTC [25373] STATEMENT:  CREATE OR REPLACE FUNCTION public.managed_update_user(
	  p_user_id uuid,
	  p_full_name text DEFAULT NULL::text,
	  p_role public.user_role DEFAULT NULL::user_role,
	  p_role_id uuid DEFAULT NULL::uuid,
	  p_is_active boolean DEFAULT NULL::boolean,
	  p_max_stores_limit int DEFAULT NULL::int,
	  p_max_users_limit int DEFAULT NULL::int,
	  p_plan plan_t DEFAULT NULL::plan_t,
	  p_caller_id uuid DEFAULT NULL::uuid
	)
	RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  v_old RECORD;
	  -- FIX H-7: anti-spoofing — service_role puede pasar p_caller_id explícito
	  v_caller_uid uuid := CASE WHEN auth.role() = 'service_role'
	    THEN COALESCE(p_caller_id, auth.uid()) ELSE auth.uid() END;
	  v_caller_role public.user_role;
	  v_changes jsonb := '{}'::jsonb;
	BEGIN
	  -- Validar caller es admin (usa v_caller_uid que ahora NO es spoofable para authenticated)
	  SELECT role INTO v_caller_role FROM public.profiles WHERE id = v_caller_uid;
	  IF v_caller_role IS NULL OR v_caller_role NOT IN ('admin', 'superadmin') THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED: Only admins can update users.';
	  END IF;
	
	  SELECT * INTO v_old FROM public.profiles WHERE id = p_user_id AND deleted_at IS NULL FOR UPDATE;
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_USER_NOT_FOUND: %', p_user_id;
	  END IF;
	
	  IF p_is_active = false AND p_user_id = v_caller_uid THEN
	    RAISE EXCEPTION 'ERR_SELF_DEACTIVATE_BLOCKED: Cannot deactivate own account.';
	  END IF;
	
	  IF p_full_name IS NOT NULL AND p_full_name <> v_old.full_name THEN
	    v_changes := v_changes || jsonb_build_object('full_name', jsonb_build_object('old', v_old.full_name, 'new', p_full_name));
	    UPDATE public.profiles SET full_name = p_full_name, updated_at = now() WHERE id = p_user_id;
	  END IF;
	
	  IF p_role IS NOT NULL AND p_role <> v_old.role THEN
	    v_changes := v_changes || jsonb_build_object('role', jsonb_build_object('old', v_old.role::text, 'new', p_role::text));
	    UPDATE public.profiles SET role = p_role, updated_at = now() WHERE id = p_user_id;
	  END IF;
	
	  IF p_role_id IS NOT NULL AND (v_old.role_id IS NULL OR p_role_id <> v_old.role_id) THEN
	    v_changes := v_changes || jsonb_build_object('role_id', jsonb_build_object('old', v_old.role_id, 'new', p_role_id));
	    UPDATE public.profiles SET role_id = p_role_id, updated_at = now() WHERE id = p_user_id;
	  END IF;
	
	  IF p_is_active IS NOT NULL AND p_is_active <> v_old.is_active THEN
	    v_changes := v_changes || jsonb_build_object('is_active', jsonb_build_object('old', v_old.is_active, 'new', p_is_active));
	    UPDATE public.profiles SET is_active = p_is_active, updated_at = now() WHERE id = p_user_id;
	  END IF;
	
	  IF p_max_stores_limit IS NOT NULL AND (v_old.max_stores_limit IS NULL OR p_max_stores_limit <> v_old.max_stores_limit) THEN
	    v_changes := v_changes || jsonb_build_object('max_stores_limit', jsonb_build_object('old', v_old.max_stores_limit, 'new', p_max_stores_limit));
	    UPDATE public.profiles SET max_stores_limit = p_max_stores_limit, updated_at = now() WHERE id = p_user_id;
	  END IF;
	
	  IF p_max_users_limit IS NOT NULL AND (v_old.max_users_limit IS NULL OR p_max_users_limit <> v_old.max_users_limit) THEN
	    v_changes := v_changes || jsonb_build_object('max_users_limit', jsonb_build_object('old', v_old.max_users_limit, 'new', p_max_users_limit));
	    UPDATE public.profiles SET max_users_limit = p_max_users_limit, updated_at = now() WHERE id = p_user_id;
	  END IF;
	
	  IF p_plan IS NOT NULL AND p_plan <> v_old.plan THEN
	    v_changes := v_changes || jsonb_build_object('plan', jsonb_build_object('old', v_old.plan::text, 'new', p_plan::text));
	    UPDATE public.profiles SET plan = p_plan, updated_at = now() WHERE id = p_user_id;
	  END IF;
	
	  IF v_changes <> '{}'::jsonb THEN
	    INSERT INTO public.user_audit_log (performed_by, target_user_id, action, old_values, new_values, metadata)
	    VALUES (
	      v_caller_uid, p_user_id, 'USER_UPDATED',
	      jsonb_build_object(
	        'full_name', v_old.full_name, 'role', v_old.role::text,
	        'is_active', v_old.is_active, 'plan', v_old.plan::text
	      ),
	      v_changes,
	      jsonb_build_object('fields_changed', jsonb_object_keys(v_changes))
	    );
	  END IF;
	
	  RETURN jsonb_build_object('success', true, 'user_id', p_user_id, 'changes', v_changes);
	END;
	$function$
2026-09-13 06:19:29.700 UTC [25380] ERROR:  type "user_role" does not exist at character 204
2026-09-13 06:19:29.700 UTC [25380] STATEMENT:  CREATE OR REPLACE FUNCTION public.on_auth_user_created()
	RETURNS trigger
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO 'public', 'extensions'
	AS $function$
	DECLARE
	  v_role_id UUID;
	  v_role_enum USER_ROLE;
	  v_metadata_role TEXT;
	  v_company_name TEXT;
	  v_full_name TEXT;
	  v_tenant_id UUID;
	BEGIN
	  -- Extract role from metadata if present
	  v_metadata_role := NEW.raw_user_meta_data->>'role';
	
	  -- Normalize and convert to enum
	  IF v_metadata_role IS NOT NULL THEN
	    BEGIN
	      v_role_enum := v_metadata_role::USER_ROLE;
	    EXCEPTION WHEN OTHERS THEN
	      v_role_enum := 'tenant_admin'::USER_ROLE;
	    END;
	  ELSE
	    -- Iteración 13: self-signup users are tenant_admin by default
	    v_role_enum := 'tenant_admin'::USER_ROLE;
	  END IF;
	
	  -- Extract company_name and full_name from metadata
	  v_company_name := NEW.raw_user_meta_data->>'company_name';
	  v_full_name := COALESCE(NEW.raw_user_meta_data->>'full_name', split_part(NEW.email, '@', 1));
	
	  -- Iteración 13: Create tenant for self-signup users
	  -- If user was created by admin (via managed_create_user_v2), they may already
	  -- have a tenant_id set via p_target_user_id. In that case, skip tenant creation.
	  IF v_company_name IS NOT NULL THEN
	    INSERT INTO public.tenants (name, owner_id, plan, subscription_status, trial_ends_at, is_active)
	    VALUES (
	      v_company_name,
	      NEW.id,
	      'free'::plan_t,
	      'trial',
	      now() + interval '14 days',
	      true
	    )
	    RETURNING id INTO v_tenant_id;
	  END IF;
	
	  -- Get matching role_id from public.roles
	  SELECT id INTO v_role_id FROM public.roles
	    WHERE name = v_metadata_role OR lower(name) = lower(v_role_enum::text)
	    LIMIT 1;
	
	  -- Insert profile with tenant_id
	  INSERT INTO public.profiles (id, email, full_name, role, role_id, is_active, tenant_id, plan, created_at, updated_at)
	  VALUES (
	    NEW.id,
	    NEW.email,
	    v_full_name,
	    v_role_enum,
	    v_role_id,
	    true,
	    v_tenant_id,
	    'free'::plan_t,
	    now(),
	    now()
	  )
	  ON CONFLICT (id) DO UPDATE SET
	    email = EXCLUDED.email,
	    full_name = EXCLUDED.full_name,
	    role = EXCLUDED.role,
	    role_id = EXCLUDED.role_id,
	    tenant_id = COALESCE(profiles.tenant_id, EXCLUDED.tenant_id),
	    plan = EXCLUDED.plan,
	    is_active = true,
	    updated_at = now();
	
	  -- Audit log
	  INSERT INTO public.user_audit_log (performed_by, target_user_id, action, new_values, metadata)
	  VALUES (
	    NEW.id, NEW.id, 'USER_REGISTERED_PUBLICLY',
	    jsonb_build_object('email', NEW.email, 'role', v_role_enum::text, 'tenant_id', v_tenant_id),
	    jsonb_build_object('company_name', v_company_name, 'source', 'self_signup')
	  );
	
	  RETURN NEW;
	END;
	$function$
2026-09-13 06:19:29.761 UTC [25395] ERROR:  type adjustment_item[] does not exist
2026-09-13 06:19:29.761 UTC [25395] STATEMENT:  CREATE OR REPLACE FUNCTION public.process_inventory_adjustment(p_store_id uuid, p_cashier_id uuid, p_items adjustment_item[], p_operation_date timestamp with time zone DEFAULT NULL::timestamp with time zone)
	 RETURNS uuid
	 LANGUAGE plpgsql
	 SECURITY DEFINER
	 SET search_path TO 'public'
	AS $function$
	DECLARE
	  v_adjustment_id UUID;
	  v_item public.adjustment_item;
	  v_difference NUMERIC;
	  v_effective_date TIMESTAMP WITH TIME ZONE := COALESCE(p_operation_date, NOW());
	BEGIN
	  -- Validación forward-only locking
	  PERFORM public.validate_operation_date(p_operation_date, p_store_id);
	
	  INSERT INTO public.inventory_adjustments (store_id, created_by, status, created_at)
	  VALUES (p_store_id, p_cashier_id, 'PROCESSING', v_effective_date)
	  RETURNING id INTO v_adjustment_id;
	
	  FOREACH v_item IN ARRAY p_items
	  LOOP
	    v_difference := v_item.counted_quantity - v_item.expected_quantity;
	    INSERT INTO public.inventory_adjustment_items (adjustment_id, product_id, expected_quantity, counted_quantity, created_at)
	    VALUES (v_adjustment_id, v_item.product_id, v_item.expected_quantity, v_item.counted_quantity, v_effective_date);
	
	    PERFORM public.register_stock_movement(
	        p_product_id := v_item.product_id,
	        p_store_id := p_store_id,
	        p_user_id := p_cashier_id,
	        p_quantity := v_difference,
	        p_movement_type := 'adjustment',
	        p_operation_date := v_effective_date
	    );
	  END LOOP;
	
	  UPDATE public.inventory_adjustments SET status = 'COMPLETED', updated_at = v_effective_date
	  WHERE id = v_adjustment_id;
	  RETURN v_adjustment_id;
	END;
	$function$
2026-09-13 06:19:29.813 UTC [25404] ERROR:  type "public.purchase_status_enum" does not exist at character 425
2026-09-13 06:19:29.813 UTC [25404] STATEMENT:  CREATE OR REPLACE FUNCTION public.receive_against_po(
	  p_po_id uuid,
	  p_received_items jsonb DEFAULT '[]'::jsonb,
	  p_user_id uuid DEFAULT NULL,
	  p_reception_date timestamptz DEFAULT now(),
	  p_invoice_number text DEFAULT NULL
	) RETURNS jsonb
	LANGUAGE plpgsql
	SECURITY DEFINER
	SET search_path TO 'public', 'extensions'
	AS $func$
	DECLARE
	  v_store_id            uuid;
	  v_supplier_name       text;
	  v_po_status           public.purchase_status_enum;
	  v_item                jsonb;
	  v_item_id             uuid;
	  v_qty                 numeric;
	  v_qty_ordered         numeric;
	  v_qty_received_before numeric;
	  v_product_id          uuid;
	  v_unit_cost           numeric;
	  v_product_name        text;
	  v_all_received        boolean;
	  v_any_received        boolean;
	  v_new_status          public.purchase_status_enum;
	  v_receipt_id          uuid;
	  v_reception_items     jsonb := '[]'::jsonb;
	  v_count               integer := 0;
	  v_po_number           text;
	  v_invoice_to_pass     text;
	  v_sorted_items        jsonb;
	BEGIN
	  -- ─── 1. Cargar OC con lock exclusivo ───
	  SELECT po.store_id, po.supplier_name, po.status, po.po_number
	    INTO v_store_id, v_supplier_name, v_po_status, v_po_number
	  FROM public.purchase_orders po
	  WHERE po.id = p_po_id
	  FOR UPDATE;
	
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_PO_NOT_FOUND';
	  END IF;
	
	  -- ─── 2. Validar acceso (tenant-aware via has_store_access) ───
	  IF NOT public.has_store_access(v_store_id) THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED';
	  END IF;
	
	  -- ─── 3. Validar estado de la OC ───
	  IF v_po_status = 'cancelled' THEN
	    RAISE EXCEPTION 'ERR_PO_CANCELLED';
	  END IF;
	  -- Solo se puede recibir contra draft, sent o partial.
	  IF v_po_status NOT IN ('draft', 'sent', 'partial') THEN
	    RAISE EXCEPTION 'ERR_PO_NOT_RECEIVABLE: status % is terminal', v_po_status;
	  END IF;
	
	  -- ─── 4. Validar items no vacío (parity B3 register_reception) ───
	  IF p_received_items IS NULL OR jsonb_array_length(p_received_items) = 0 THEN
	    RAISE EXCEPTION 'ERR_EMPTY_ITEMS';
	  END IF;
	
	  -- ─── 5. Condición #5 (anti-deadlock): Ordenar items por po_item_id ASC ───
	  SELECT jsonb_agg(elem ORDER BY (elem->>'po_item_id'))
	    INTO v_sorted_items
	  FROM jsonb_array_elements(p_received_items) AS elem;
	
	  -- ─── 6. Procesar cada item: VALIDAR antes de actualizar ───
	  FOR v_item IN SELECT * FROM jsonb_array_elements(v_sorted_items) LOOP
	    v_item_id := NULLIF(v_item->>'po_item_id', '')::uuid;
	    v_qty     := COALESCE((v_item->>'quantity_received')::numeric, 0);
	
	    -- Validar po_item_id presente
	    IF v_item_id IS NULL THEN
	      RAISE EXCEPTION 'ERR_ITEM_ID_REQUIRED: missing po_item_id in received_items';
	    END IF;
	
	    -- Medio #6: qty > 0
	    IF v_qty <= 0 THEN
	      RAISE EXCEPTION 'ERR_NEGATIVE_QTY: qty % for item % must be > 0', v_qty, v_item_id;
	    END IF;
	
	    -- Cargar estado actual del item con lock FOR UPDATE
	    SELECT product_id, quantity_ordered, quantity_received, unit_cost, product_name
	      INTO v_product_id, v_qty_ordered, v_qty_received_before, v_unit_cost, v_product_name
	    FROM public.purchase_order_items
	    WHERE id = v_item_id AND po_id = p_po_id
	    FOR UPDATE;
	
	    IF NOT FOUND THEN
	      RAISE EXCEPTION 'ERR_ITEM_NOT_FOUND: %', v_item_id;
	    END IF;
	
	    -- Alto #5: over-receive check
	    IF (v_qty_received_before + v_qty) > v_qty_ordered THEN
	      RAISE EXCEPTION 'ERR_OVER_RECEIVE: item % ordered %, already received %, attempting %',
	        v_item_id, v_qty_ordered, v_qty_received_before, v_qty;
	    END IF;
	
	    -- Condición #1: todo item debe tener product_id NOT NULL
	    IF v_product_id IS NULL THEN
	      RAISE EXCEPTION 'ERR_PRODUCT_ID_REQUIRED: item % has no product_id', v_item_id;
	    END IF;
	
	    -- Medio #9: producto debe existir en store
	    IF NOT EXISTS (
	      SELECT 1 FROM public.products p
	      WHERE p.id = v_product_id AND p.store_id = v_store_id
	    ) THEN
	      RAISE EXCEPTION 'ERR_PRODUCT_NOT_IN_STORE: product % not in store %',
	        v_product_id, v_store_id;
	    END IF;
	
	    -- Acumular para register_reception (formato esperado: product_id, quantity, unit_cost, moneda, tasa)
	    v_reception_items := v_reception_items || jsonb_build_object(
	      'product_id',   v_product_id,
	      'product_name', v_product_name,
	      'quantity',     v_qty,
	      'unit_cost',    v_unit_cost,
	      'moneda_recepcion', 'CUP',
	      'tasa_cambio_recepcion', 1.0
	    );
	    v_count := v_count + 1;
	  END LOOP;
	
	  -- ─── 7. Condición #3: reference_doc NULL si no viene invoice_number ───
	  -- Evita colisión con UNIQUE INDEX idx_receipts_store_reference_doc
	  -- (store_id, reference_doc) en partial indexes.
	  -- El vínculo real receipt↔PO es receipts.po_id (G6).
	  IF p_invoice_number IS NULL OR p_invoice_number = '' THEN
	    v_invoice_to_pass := NULL;
	  ELSE
	    v_invoice_to_pass := p_invoice_number;
	  END IF;
	
	  -- ─── 8. Crear receipt vinculado vía register_reception ───
	  -- register_reception aplica las 8 validaciones B1-B5 + C1-C3 del v2.23.0
	  -- (no se relajan). G6 añade p_po_id opcional.
	  v_receipt_id := public.register_reception(
	    p_store_id        := v_store_id,
	    p_supplier        := v_supplier_name,
	    p_reception_date  := p_reception_date,
	    p_invoice_number  := v_invoice_to_pass,
	    p_items           := v_reception_items,
	    p_user_id         := p_user_id,
	    p_po_id           := p_po_id
	  );
	
	  -- ─── 9. Actualizar quantity_received en purchase_order_items ───
	  -- (SECURITY DEFINER bypasses RLS DENY UPDATE de G5)
	  FOR v_item IN SELECT * FROM jsonb_array_elements(v_sorted_items) LOOP
	    v_item_id := NULLIF(v_item->>'po_item_id', '')::uuid;
	    v_qty     := (v_item->>'quantity_received')::numeric;
	
	    UPDATE public.purchase_order_items
	    SET quantity_received = quantity_received + v_qty
	    WHERE id = v_item_id AND po_id = p_po_id;
	  END LOOP;
	
	  -- ─── 10. Recalcular status global (cast EXPLÍCITO a enum) ───
	  SELECT
	    BOOL_AND(quantity_received >= quantity_ordered),
	    BOOL_OR(quantity_received > 0)
	  INTO v_all_received, v_any_received
	  FROM public.purchase_order_items WHERE po_id = p_po_id;
	
	  v_new_status :=
	    CASE
	      WHEN v_all_received THEN 'received'::public.purchase_status_enum
	      WHEN v_any_received THEN 'partial'::public.purchase_status_enum
	      ELSE 'sent'::public.purchase_status_enum
	    END;
	
	  -- ─── 11. Actualizar OC + vincular receipt en notas ───
	  UPDATE public.purchase_orders
	  SET status       = v_new_status,
	      received_at  = CASE WHEN v_new_status = 'received' THEN NOW() ELSE received_at END,
	      notes        = COALESCE(notes, '') ||
	                     CASE WHEN COALESCE(notes, '') = '' THEN '' ELSE E'\n' END ||
	                     '[Receipt ' || v_receipt_id::text || ' linked at ' || NOW()::text || ']'
	  WHERE id = p_po_id;
	
	  -- ─── 12. CxP (Grupo 6): marcar receipt con payment_status='unpaid' ───
	  -- Solo si aún no está seteado (no sobrescribir si register_reception ya lo puso).
	  UPDATE public.receipts
	  SET payment_status     = COALESCE(NULLIF(payment_status, ''), 'unpaid'),
	      payment_terms_days = COALESCE(payment_terms_days, 30),
	      due_date           = COALESCE(due_date, (p_reception_date::date + INTERVAL '30 days')::date)
	  WHERE id = v_receipt_id;
	
	  -- ─── 13. Auditoría ───
	  INSERT INTO public.audit_logs (user_id, store_id, action, table_name, record_id, metadata)
	  VALUES (
	    p_user_id, v_store_id, 'po_received', 'purchase_orders', p_po_id,
	    jsonb_build_object(
	      'new_status', v_new_status::text,
	      'items_received', v_count,
	      'receipt_id', v_receipt_id,
	      'po_number', v_po_number,
	      'invoice_number', v_invoice_to_pass
	    )
	  );
	
	  RETURN jsonb_build_object(
	    'status', 'success',
	    'po_status', v_new_status::text,
	    'receipt_id', v_receipt_id,
	    'items_received', v_count
	  );
	END;
	$func$
2026-09-13 06:19:29.958 UTC [25441] ERROR:  type purchase_status_enum does not exist
2026-09-13 06:19:29.958 UTC [25441] STATEMENT:  CREATE OR REPLACE FUNCTION public.set_purchase_order_status(p_po_id uuid, p_new_status purchase_status_enum, p_user_id uuid DEFAULT NULL::uuid, p_reason text DEFAULT NULL::text)
	 RETURNS jsonb
	 LANGUAGE plpgsql
	 SECURITY DEFINER
	 SET search_path TO 'public', 'extensions'
	AS $function$
	DECLARE
	  v_store_id     uuid;
	  v_current      public.purchase_status_enum;
	  v_po_number    text;
	  v_allowed      text[];
	  v_is_allowed   boolean := false;
	BEGIN
	  -- ─── 1. Cargar PO con lock exclusivo ───
	  SELECT store_id, status, po_number
	    INTO v_store_id, v_current, v_po_number
	  FROM public.purchase_orders
	  WHERE id = p_po_id
	  FOR UPDATE;
	
	  IF NOT FOUND THEN
	    RAISE EXCEPTION 'ERR_PO_NOT_FOUND';
	  END IF;
	
	  -- ─── 2. Validar acceso (tenant-aware) ───
	  IF NOT public.has_store_access(v_store_id) THEN
	    RAISE EXCEPTION 'ERR_UNAUTHORIZED';
	  END IF;
	
	  -- ─── 3. Si no hay cambio, retornar success sin hacer nada ───
	  IF v_current = p_new_status THEN
	    RETURN jsonb_build_object(
	      'status', 'no_change',
	      'po_status', v_current::text,
	      'po_number', v_po_number
	    );
	  END IF;
	
	  -- ─── 4. Definir transiciones permitidas (state machine) ───
	  -- 'partial' y 'received' NUNCA son destino válido manualmente
	  -- (solo vía receive_against_po).
	  v_allowed := CASE v_current
	    WHEN 'draft'   THEN ARRAY['sent', 'cancelled']
	    WHEN 'sent'    THEN ARRAY['cancelled']
	    WHEN 'partial' THEN ARRAY['cancelled']
	    ELSE ARRAY[]::text[]  -- received, cancelled: terminal, no transitions
	  END;
	
	  -- ─── 5. Verificar transición permitida ───
	  SELECT EXISTS(SELECT 1 FROM unnest(v_allowed) a WHERE a = p_new_status::text)
	    INTO v_is_allowed;
	
	  IF NOT v_is_allowed THEN
	    RAISE EXCEPTION 'ERR_INVALID_TRANSITION: % → % not allowed (allowed: %)',
	      v_current::text, p_new_status::text,
	      CASE WHEN array_length(v_allowed, 1) IS NULL THEN 'none' ELSE array_to_string(v_allowed, ', ') END;
	  END IF;
	
	  -- ─── 6. Aplicar transición ───
	  UPDATE public.purchase_orders
	  SET status      = p_new_status,
	      received_at = CASE WHEN p_new_status = 'received' THEN NOW() ELSE received_at END
	  WHERE id = p_po_id;
	
	  -- ─── 7. Auditoría (action = PO_STATUS_CHANGED) ───
	  INSERT INTO public.audit_logs (user_id, store_id, action, table_name, record_id, metadata)
	  VALUES (
	    CASE WHEN auth.role() = 'service_role' THEN COALESCE(p_user_id, auth.uid()) ELSE auth.uid() END, v_store_id, 'PO_STATUS_CHANGED', 'purchase_orders', p_po_id,
	    jsonb_build_object(
	      'po_number', v_po_number,
	      'from_status', v_current::text,
	      'to_status', p_new_status::text,
	      'reason', p_reason
	    )
	  );
	
	  RETURN jsonb_build_object(
	    'status', 'success',
	    'po_status', p_new_status::text,
	    'po_number', v_po_number,
	    'previous_status', v_current::text
	  );
	END;
	$function$
2026-09-13 06:19:30.067 UTC [25467] ERROR:  type "user_role" does not exist at character 100
2026-09-13 06:19:30.067 UTC [25467] STATEMENT:  CREATE OR REPLACE FUNCTION public.validate_active_store()
	RETURNS TRIGGER AS $$
	DECLARE
	    v_role user_role;
	BEGIN
	    v_role := NEW.role;
	
	    -- Si role es 'costo' o 'admin' o 'superadmin', active_store_id es opcional
	    IF v_role = 'costo'::user_role
	       OR v_role = 'admin'::user_role
	       OR v_role = 'superadmin'::user_role THEN
	        -- Solo validar si active_store_id cambió o es INSERT
	        IF TG_OP = 'INSERT' OR (OLD.active_store_id IS DISTINCT FROM NEW.active_store_id) THEN
	            IF NEW.active_store_id IS NOT NULL THEN
	                IF NOT EXISTS (
	                    SELECT 1 FROM public.user_store_memberships
	                    WHERE user_id = NEW.id
	                      AND store_id = NEW.active_store_id
	                      AND status = 'active'
	                ) THEN
	                    IF TG_OP = 'UPDATE' THEN
	                        RAISE EXCEPTION 'ERR_INVALID_ACTIVE_STORE: El usuario no tiene membership activa en la tienda seleccionada.';
	                    END IF;
	                END IF;
	            END IF;
	        END IF;
	        RETURN NEW;
	    END IF;
	
	    -- Para roles operativos (encargado, clerk, warehouse, manager, usuario)
	    IF TG_OP = 'INSERT' THEN
	        -- En INSERT: ser permisivo. La función managed_create_user inserta
	        -- memberships justo después. No validar nada aquí.
	        RETURN NEW;
	    ELSE
	        -- UPDATE: solo validar si active_store_id cambió realmente
	        -- (evita bloquear updates de otros campos en perfiles intermedios)
	        IF OLD.active_store_id IS NOT DISTINCT FROM NEW.active_store_id THEN
	            -- active_store_id no cambió → permitir el UPDATE sin validar
	            RETURN NEW;
	        END IF;
	
	        -- active_store_id cambió → validar consistencia
	        IF NEW.active_store_id IS NULL THEN
	            IF v_role IN ('encargado'::user_role, 'clerk'::user_role, 'warehouse'::user_role) THEN
	                RAISE EXCEPTION 'ERR_STORE_REQUIRED: El rol % requiere una tienda activa asignada.', v_role;
	            END IF;
	        ELSE
	            IF NOT EXISTS (
	                SELECT 1 FROM public.user_store_memberships
	                WHERE user_id = NEW.id
	                  AND store_id = NEW.active_store_id
	                  AND status = 'active'
	            ) THEN
	                RAISE EXCEPTION 'ERR_INVALID_ACTIVE_STORE: El usuario no tiene membership activa en la tienda seleccionada.';
	            END IF;
	        END IF;
	        RETURN NEW;
	    END IF;
	END;
	$$ LANGUAGE plpgsql SECURITY DEFINER
2026-09-13 06:19:30.118 UTC [25478] ERROR:  type "public.user_role" does not exist at character 270
2026-09-13 06:19:30.118 UTC [25478] STATEMENT:  CREATE OR REPLACE FUNCTION public.validate_tenant_access(
	  p_user_id uuid,
	  p_store_id uuid
	)
	RETURNS boolean
	LANGUAGE plpgsql
	STABLE SECURITY DEFINER
	SET search_path TO public, pg_temp
	AS $function$
	DECLARE
	  v_user_tenant uuid;
	  v_store_tenant uuid;
	  v_user_role public.user_role;
	BEGIN
	  -- Global admin bypasses tenant check
	  SELECT role, tenant_id INTO v_user_role, v_user_tenant
	    FROM public.profiles WHERE id = p_user_id;
	
	  IF v_user_role IN ('admin', 'superadmin') THEN
	    RETURN true;
	  END IF;
	
	  -- Get store's tenant
	  SELECT tenant_id INTO v_store_tenant FROM public.stores WHERE id = p_store_id;
	
	  IF v_store_tenant IS NULL THEN
	    -- Legacy store without tenant — allow (backward compat)
	    RETURN true;
	  END IF;
	
	  -- Check tenant match
	  RETURN v_user_tenant = v_store_tenant;
	END;
	$function$
2026-09-13 06:19:30.238 UTC [22713] LOG:  received fast shutdown request
2026-09-13 06:19:30.238 UTC [22713] LOG:  aborting any active transactions
2026-09-13 06:19:30.241 UTC [22713] LOG:  background worker "logical replication launcher" (PID 22719) exited with exit code 1
2026-09-13 06:19:30.241 UTC [22714] LOG:  shutting down
2026-09-13 06:19:30.241 UTC [22714] LOG:  checkpoint starting: shutdown immediate
2026-09-13 06:19:30.244 UTC [22714] LOG:  checkpoint complete: wrote 360 buffers (2.2%); 0 WAL file(s) added, 0 removed, 0 recycled; write=0.003 s, sync=0.001 s, total=0.003 s; sync files=104, longest=0.001 s, average=0.000 s; distance=2700 kB, estimate=2700 kB; lsn=0/177BDD0, redo lsn=0/177BDD0
2026-09-13 06:19:30.247 UTC [22713] LOG:  database system is shut down
