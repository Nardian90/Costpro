# REM-INV-4B — Matriz de Remediación

| ID | Finding | Severity | PRE | Root cause | Fix | POST | Production | Status |
|----|---------|----------|-----|------------|-----|------|------------|--------|
| RC-3 | `get_transferable_stores(p_user_id, p_current_store_id)`: `p_user_id` client-controlled llega a `has_store_access_as()`; `p_current_store_id` client-controlled deriva el tenant (y su rama legacy elimina el filtro de tenant). Enumeración cross-user/cross-store/cross-tenant de filas completas de `stores` bajo SECURITY DEFINER (RLS bypass demostrado). | **P1** | EXPLOITABLE (staging E2/E2b/E3/LEG-1/LEG-2/E6/E8-oracle — ver 07) | La identidad autoridad debe ser la del JWT firmado por la plataforma (`auth.uid()`); el código evalúa la autorización para un sujeto suministrado por el cliente (`p_user_id`) sin vincularlo al caller. Trust boundary rota en el borde del parámetro RPC. RLS no protege (SECURITY DEFINER como owner `postgres`); la API no media (PostgREST expone el RPC directamente a `authenticated`). | Opción C (§13): reutilización del patrón RC-1 validado en REM-INV-4A-R — gate de identidad al inicio del body: bajo `authenticated`, `p_user_id` debe ser `IS DISTINCT FROM auth.uid()` → DENY; `service_role` (ruta confiable de servidor) exceptuado. Firma, header, proacl intactos. Migración `20260915000002`. | DENIED 11/11 vectores (E2/E2b/E3/E4/E5/E6/E6b/E7/E7b/LEG-1/LEG-2) + oráculo E8 cerrado (ERR vs ERR) + flujos legítimos E1a/E1b/E6c PASS (ver 10) | Aplicado: md5 `bfd8a24b`→`d0c24292` (HTTP 201); ACL/RLS/policies/triggers sin drift; zero-touch 15/15 PASS; security-contract 134/134 | **CLOSED** |

## Findings contenidos (fuera de alcance de este gate — documentados, NO reparados aquí)

| ID | Finding | Severity | Estado | Recomendado |
|----|---------|----------|--------|-------------|
| RES-4B-1 | `has_store_role(uuid,uuid,text[])` (3-arg) mantiene EXECUTE a PUBLIC/anon/authenticated en LIVE (proacl `=X/postgres,anon=X,...`). Oráculo booleano de membresía (user, store, rol) si se conocen UUIDs. Cero callers de browser; llamadores LIVE son funciones DEFINER. | P3-latent (UUIDs no adivinables; sin EXECUTE anónimo alcanzable vía PostgREST con JWT válido es alcanzable por authenticated) | DOCUMENTADO — no bloquea este gate (superficie distinta a RC-3) | Próximo gate de ACL: demostrar callers legítimos y REVOKE PUBLIC/anon (§19) |
| RES-4B-2 | `current_user_tenant_id()` mantiene EXECUTE a PUBLIC/anon. La función deriva de `auth.uid()` interno → anon obtiene NULL (sin disclosure). | P3 | DOCUMENTADO | Próximo gate de ACL (misma pasada) |
| RES-4B-3 | `docs/ITERATION_8_*.md` contienen un fragmento TRUNCADO de 10 chars de un PAT (`sbp_...​...` literal) — no utilizable (token real: 44 chars). Pre-existente, no introducido por este gate. | Higiene | DOCUMENTADO (13-secret-scan.txt) | Tarea de housekeeping: reemplazar fragmento por `REDACTED` |
| RES-4B-4 | Rama legacy de `get_transferable_stores` (`p_current_store_id` inexistente/tenant NULL) sigue alcanzable POST-fix, pero con sujeto forzado al propio caller (`auth.uid()`): solo devuelve tiendas del PROPIO caller — sin disclosure de terceros. Oráculo de existencia de tienda (legado vs tenant) no revela datos de otros usuarios. | P3-residual | DOCUMENTADO | Evaluar en gate futuro: exigir acceso del caller a `p_current_store_id` (cambio de contrato — fuera del mínimo de este gate) |

Regla de contención aplicada: ningún finding de la lista bloquea la certificación aislada de RC-3 (superficies y cadenas de autorización independientes).
