I have fixed the type error reported during build by ensuring `src/lib/dexie.ts` contains the `IPVSettings` interface and the `ipv_settings` table.

The error was: Module '"@/lib/dexie"' has no exported member 'IPVSettings'.
This was caused by a partial or failed write to the file in the previous attempt.

I have restored:
1. `IPVSettings` interface.
2. `ipv_settings` table in `IPVDatabase`.
3. Database version bump to 7.

I have also verified that `src/components/views/terminal/views/ipv/IncomeReceiptSection.tsx` correctly imports these members.
