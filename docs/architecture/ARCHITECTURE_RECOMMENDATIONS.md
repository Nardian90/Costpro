# ARCHITECTURE RECOMMENDATIONS — CostPro v8.1
## AI Evolution & Technical Debt Resolution Plan

**Fecha:** 2026-03-28
**Origen:** Phase 17 — AI Architecture Evolution Engine
**Confianza:** 95%

---

## 1. CRITICAL CONCERNS: "GOD COMPONENTS"

Two main components concentrate a disproportionate number of dependencies, increasing system fragility and complicating unit testing.

### 1.1 CostSheetView.tsx
- **Dependencies:** 38
- **Instability:** 1.0 (Highest)
- **Coupling Score:** 3.8
- **Recommendation:**
    - Decouple the logic into a `useCostSheetView` hook (currently missing or underutilized).
    - Split the UI into smaller sub-views: `CostSheetEditor`, `CostSheetAnalytics`, `CostSheetGovernance`.
    - Move logic for template exploration to a dedicated service.

### 1.2 IPVView.tsx
- **Dependencies:** 33
- **Instability:** 1.0 (Highest)
- **Coupling Score:** 3.3
- **Recommendation:**
    - Modularize the `MatchingEngine` interaction.
    - Create a `MatchingContext` to provide state to sub-components instead of prop-drilling.
    - Extract the report generation logic to `IPVReportView`.

---

## 2. INTEGRITY GAPS: ORPHAN VIEWS & UNDOCUMENTED LOGIC

The system has matured faster than its documentation, leading to significant gaps in the Knowledge Graph.

### 2.1 Orphan Views (23 identified)
Views like `DashboardView`, `SystemHealthView`, and `AuditLogsView` are technically implemented but not registered in any formal workflow in `workflows.json`.
- **Recommendation:**
    - Define a `gestion_monitoreo_salud` workflow for `SystemHealthView`.
    - Define a `gobierno_auditoria` workflow for `AuditLogsView`.
    - Ensure all views in `src/components/views/terminal/views/` belong to at least one business process.

### 2.2 Undocumented Components (28 identified)
Critical types and infrastructure components (e.g., `src/types/index.ts`, `src/lib/ipv/costEngine.ts`) are missing from `knowledge/components.json`.
- **Recommendation:**
    - Batch update `knowledge/components.json` using the AST Discovery output from Phase 1.
    - Prioritize documentation for `HealthDashboard` components.

---

## 3. STRUCTURAL IMPROVEMENTS

### 3.1 Type System Centralization
- **Issue:** Many components have inline types or redundant definitions in `src/types/`.
- **Recommendation:** Standardize on a single domain-driven type hierarchy. Move all shared interfaces to `src/contracts/` or consolidate `src/types/index.ts`.

### 3.2 Service Layer Hardening
- **Issue:** Components are directly using `supabaseClient` (51 refs) and `dexie` (33 refs) instead of going through services.
- **Recommendation:** Enforce the Service Pattern. UI components should only consume `hooks` which in turn consume `services`. Direct DB access from the UI should be flagged in linting.

---

## 4. NEXT STEPS (Phase 18)

1. **Automated Refactoring:** Use `scripts/remediation/` to extract logic from God Components.
2. **Knowledge Sync:** Force a full re-run of Phase 2 and 7 to capture the undocumented components.
3. **Workflow Expansion:** Map the 23 orphan views to their respective business goals.

---
*Generated by JULES-CostPro-v8.1-Evolution-Engine*
