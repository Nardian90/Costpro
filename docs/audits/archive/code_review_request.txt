I have implemented a fix for the AI communication error described by the user.

Changes:
1. Updated `src/lib/ai/adapters/fallback-adapter.ts`:
   - Added logic to detect quota/balance errors across all providers.
   - Replaced the generic error message with a specific one asking the user to configure their own API keys.
2. Updated `src/lib/ai/orchestrator.ts`:
   - Modified `getLLMProviderWithUserKey` to only include the DeepSeek fallback if a valid key is provided in the environment. This prevents trying to use a provider with an empty/default key which would inevitably fail.
3. Updated `src/components/ui/ChatBot.tsx`:
   - Enhanced the `handleSend` catch block to intercept the "Límite de IA alcanzado" error.
   - Instead of a toast, it now displays the error as a chat message from the assistant, providing immediate guidance to the user.

Files modified:
- src/lib/ai/adapters/fallback-adapter.ts
- src/lib/ai/orchestrator.ts
- src/components/ui/ChatBot.tsx

Please review the logic and the user-facing messaging.
