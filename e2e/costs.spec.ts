import { test, expect } from '@playwright/test';

test.describe('Cost Sheet Flow', () => {
  test('should allow creating a new cost sheet', async ({ page }) => {
    // await page.goto('/costs');
    // await page.click('text=Nueva Ficha');

    // Fill header
    // await page.fill('input[name="name"]', 'Ficha de Prueba');

    // Check calculations
    // ...

    // Save
    // await page.click('text=Guardar');
  });
});
