/**
 * Empty module stub for optional runtime-only dependencies.
 * These packages are dynamically imported and guarded at runtime,
 * but vitest needs to resolve them statically. This stub satisfies
 * the import without providing any real functionality.
 */
export {};
